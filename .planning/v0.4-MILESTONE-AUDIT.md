---
milestone: v0.4
audited: 2026-05-11T19:00:00.000Z
status: tech_debt
scores:
  requirements: 24/24
  phases: 7/7
  integration: passed
  flows: 11✅/3⚠/0❌ (post-rescore distribution from UI-RESCORE.md)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 16-capture-pipeline-correctness
    items:
      - "16-HUMAN-UAT.md: 3 items pending — Playwright e2e specs need live run; manual UX exercise of failed-state inbox flow; full-form ingredient round-trip."
  - phase: 17-history-feature-restoration
    items:
      - "17-HUMAN-UAT.md: 3 items pending — cooking-log-history.spec.ts live run; cooking-log-create-finalize.spec.ts live run; visual check of /cooking-logs/[id] at iPhone-shape viewport."
  - phase: 18-identity-management
    items:
      - "18-HUMAN-UAT.md: 4 items pending — settings-member-rename.spec.ts live run; onboarding-household-full.spec.ts live run; cross-phone realtime member.updated propagation test; Settings Copy button manual smoke."
  - phase: 19-validation-surface-fixes
    items:
      - "19-HUMAN-UAT.md: 3 items pending — operator Web Push round-trip on both iPhones (PUSH-ROUNDTRIP.md fill-in); capture-photo.spec.ts live run; Settings Notifications Card 4-state happy path on real iPhone."
  - phase: 21-pillar-6-deficit-pass-+-rescore
    items:
      - "21-HUMAN-UAT.md: 2 items pending — fill PUSH-ROUNDTRIP.md to make push surface flip non-provisional; acknowledge D-21-05 deviation (per-surface ui-reviews/* left byte-identical)."
deferred_scope_cuts:
  - "capture-quick stays Mixed (P-12-Q02 422-as-network-loss copy + P-12-Q03 no submit debounce — never opened as v0.4 reqs)"
  - "capture-url stays Mixed (URL-01 # TODO(productize) explicitly out of scope per PROJECT.md)"
  - "exports stays Mixed (P-12-E02 offline button + P-12-E03 double-fetch race — not on milestone docket)"
---

# v0.4 Milestone Audit — Audit Remediation & Identity Polish

## Summary

**Status:** `tech_debt` — all 24 requirements structurally verified across 7 phases; accumulated HUMAN-UAT items (operator/iPhone runtime validation) constitute the tech-debt review burden.

**Architectural milestone outcome:** v0.4 closed the **6 Pillar 6 deficit drivers** identified in the v0.3 UI-AUDIT, lifting the cumulative-mean UI score from **20.21/24 to 21.71/24 (+1.50)** and flipping **6 surfaces** from ⚠ Mixed to ✅ Feels Al Dente. Verdict distribution shifted from **5✅/9⚠/0❌** to **11✅/3⚠/0❌** — see `.planning/v0.4/UI-RESCORE.md`.

## Requirements coverage (3-source cross-reference)

All 24 requirements verified across REQUIREMENTS.md traceability ↔ phase VERIFICATION.md ↔ phase SUMMARY.md frontmatter.

| REQ-ID | Phase | VERIFICATION | Evidence |
|--------|-------|--------------|----------|
| **INV-01** | 15 | passed | `services/voting.compute_vote_state` member_count default removed; `HomeDecide.tsx`/`VoteSummary.tsx` consume `session.members.length` via `useSession()`; backend pytest passes |
| **INV-02** | 15 | passed | Atomic `update(CookingLog).where(rating.is_(None)).values(...).returning(id)` + rowcount gate at `cooking_logs.py:188`; double-tap E2E append; pytest passes |
| **CAP-01** | 16 | human_needed | `RecipeStatus.failed` + Alembic 0006 + `_record_failure` writes status='failed' + retry endpoint resets failed→draft; 2 pytest passes |
| **CAP-02** | 16 | human_needed | RecipeDraftCard failed-state Card + AlertDialog Supprimer + dual fetch in /inbox; i18n keys under recipes.promotion.* |
| **CAP-03** | 16 | human_needed | French unit-whitelist parser in RecipeForm.tsx; negative regression canary `getByText('4 tomates 4 tomates').toHaveCount(0)` |
| **HIST-01** | 17 | human_needed | `GET /api/cooking-logs?days=N` registered; list page rewired; 10/10 pytest passing live |
| **HIST-02** | 17 | human_needed | `frontend/app/cooking-logs/[id]/page.tsx` shipped (paper-grain + Fraunces italic + rating chip + back-link) |
| **FIX-01** | 17 | human_needed | `_household_today_in_tz` + `_cooked_at_in_tz_date` helpers via zoneinfo; both callsites use household-tz boundary |
| **IDM-01** | 18 | human_needed | PATCH /api/households/me + 409 uniqueness + `member.updated` broadcast; 3 pytest passes |
| **IDM-02** | 18 | human_needed | Pencil rename affordance on Settings Membre Card; useSession refresh; renameMe helper |
| **IDM-03** | 18 | human_needed | POST /join 422 HOUSEHOLD_FULL gate before color uniqueness check; pytest passes |
| **IDM-04** | 18 | human_needed | Onboarding 422 discriminator → paper-grain "Foyer complet" terminal Card; i18n keys |
| **FIX-04** | 18 | human_needed | Explicit Copy button on Settings invite-code Card (h-12, Lucide Copy icon) |
| **VAL-01** | 19 | human_needed | `paper-grain` removed from `sheet.tsx`; `capture-photo.spec.ts` no test.fixme; toBeInViewport() assertions |
| **VAL-02** | 19 | human_needed | Settings Notifications Card with 4-state UI (default/granted/denied/unsupported); i18n keys |
| **VAL-03** | 19 | human_needed | `POST /api/push/test` admin endpoint (no broadcast); /styleguide dev-only "Tester le Web Push" button; pytest passes |
| **VAL-04** | 19 | human_needed | `.planning/v0.4/PUSH-ROUNDTRIP.md` template with [pending: operator] slots; operator fill-in pending |
| **FIX-02** | 19 | human_needed | UUID date components dropped from `cli/seed.py:459,489`; `test_seed_cross_day_no_duplicates` passes |
| **TOK-01** | 20 | passed | 5 emerald-replacement tokens in globals.css; 7 audit-cited surfaces migrated |
| **TOK-02** | 20 | passed | 10 member-color tokens; MEMBER_COLORS bgVar/fgVar threaded through MemberDot via getMemberColorVars |
| **TOK-03** | 20 | passed | /styleguide Phase 20 tokens section with 4 emerald swatches + 5 member chips |
| **FIX-03** | 20 | passed | Settings Historique Card + HomeDecide partner-waiting strings routed through next-intl |
| **P6-01** | 21 | human_needed | 6 surfaces flipped Mixed→Al Dente (vote, cooking-log, capture-full, capture-photo, history, push) — 2× the ≥3 target |
| **P6-02** | 21 | human_needed | `.planning/v0.4/UI-RESCORE.md` cumulative-mean delta +1.50 documented under SAME 6-pillar rubric |

**Outcome:** 24/24 requirements satisfied. 0 unsatisfied, 0 orphaned. 17 with HUMAN-UAT pending (operator runtime validation), 7 fully passed without UAT.

## Phase coverage

| Phase | Plans | Verification | Notes |
|-------|-------|--------------|-------|
| 15. Tier 1 invariant fixes | 4/4 | passed (8/8 must-haves) | First Python test runner in repo; atomic UPDATE+rowcount pattern |
| 16. Capture pipeline correctness | 5/5 | human_needed (8/8) | `failed` terminal state shipped; ingredient parser whitelist |
| 17. History feature restoration | 3/3 | human_needed (4/4) | TZ-01 fix via zoneinfo household-tz boundary; 2 unfixme'd specs |
| 18. Identity management | 4/4 | human_needed (4/4) | Rename + capacity 422 + Copy button + AlertDialog Supprimer |
| 19. Validation surface fixes | 6/6 | human_needed (5/5) | Sheet-01 fix; push admin endpoint; PUSH-ROUNDTRIP.md template |
| 20. Token-completeness sweep | 3/3 | passed (4/4) | 15 new semantic tokens; FIX-03 i18n sweep |
| 21. Pillar 6 deficit pass + rescore | 1/1 | human_needed (4/4) | UI-RESCORE.md milestone capstone; 6 verdicts flipped |

## Cross-phase integration (inlined — UI-RESCORE.md is the canonical artifact)

Phase 21's `.planning/v0.4/UI-RESCORE.md` IS the cross-phase integration audit. Each surface flip cites the specific Phase 15-20 commit SHA that closed its v0.3 Pillar 6 driver. Integration paths verified:

- **Phase 15 → Phase 20:** `MEMBER_COUNT` removal (15-03) + emerald-token migration (20-02) jointly close vote/cooking-log surface flips.
- **Phase 16 → Phase 20:** `RecipeStatus.failed` enum (16-01) + emerald-token migration (20-02) work cleanly side-by-side on Recipe surfaces.
- **Phase 17 → Phase 20:** History detail page (17-02) consumes Phase 20 tokens for rating chip.
- **Phase 18 → Phase 19/20:** Settings page absorbs multiple landings (rename Pencil, Copy button, Notifications Card, i18n migration) without conflict — single-file convergence verified at `frontend/app/settings/page.tsx`.
- **Phase 19 → Phase 21:** Push admin endpoint + Settings Notifications Card jointly close the push surface's Pillar 6 driver pair.
- **Realtime contract (invariant #4):** invariant explicitly preserved across all 7 phases. New `member.updated` event added (Phase 18), no broadcasts removed, admin-test endpoint deliberately non-broadcast (Phase 19 VAL-03 D-19-11).
- **Locked vocabulary contract:** `RecipeStatus.failed` shipped in same commit-shape across `models/recipe.py` + `lib/recipes.ts` + Alembic migration (Phase 16-01). Zero drift.

## Tech debt (HUMAN-UAT cluster)

17 of 24 requirements carry HUMAN-UAT items deferring runtime validation to physical iPhones / live Playwright runs. This is **expected tech debt** — autonomous mode produces structurally-correct work but cannot exercise iOS Safari PWA, OS notification permission lifecycles, or cross-phone realtime feel.

The full list lives in `.planning/phases/{phase}/{phase}-HUMAN-UAT.md` for phases 16/17/18/19/21. Total: **15 individual UAT line items** awaiting operator pass-through.

**Highest-value HUMAN-UAT item:** `.planning/v0.4/PUSH-ROUNDTRIP.md` (VAL-04 / P-12-Pu-05 closure) — fills the operator deferral from v0.3 Phase 12 with evidence of end-to-end Web Push delivery on both household iPhones.

## Nyquist compliance

`workflow.nyquist_validation` is **disabled** for this project (`nyquist_validation_enabled: false` per init). No VALIDATION.md files expected or produced. Skip.

## Architecture invariants (CLAUDE.md)

All 8 invariants from CLAUDE.md hold after v0.4:

1. **#1 (5 capture surfaces, one shape):** terminal-state set extended to `{structured, failed}` cleanly; all 5 surfaces still return drafts; promotion still server-side `BackgroundTask`.
2. **#2 (voting state computed):** `member_count` parameter contract preserved; backend default removed; frontend mirror branch order unchanged.
3. **#3 (denormalized same-tx):** atomic UPDATE pattern preserved cooking_logs+recipes same-transaction commit.
4. **#4 (broadcast contract):** `member.updated` event added; `cooking.finalized` preserved on both finalize branches; admin push-test deliberately non-broadcast.
5. **#5 (raw inputs preserved):** untouched.
6. **#6 (French via next-intl):** code-layer break in settings/HomeDecide closed (FIX-03).
7. **#7 (single uvicorn worker):** untouched; household.timezone (Phase 17 FIX-01) consumes the same column that APScheduler already uses.
8. **#8 (HttpOnly cookie auth):** untouched.

## Status verdict

`tech_debt` — all 24 requirements structurally satisfied; accumulated HUMAN-UAT items represent operator-side runtime verification that requires physical hardware / iOS Safari PWA / both household devices. Per the audit decision matrix (`tech_debt` ≠ blocker), the milestone CAN ship; the HUMAN-UAT cluster is tracked for follow-through via `/gsd-audit-uat`.

**3 deferred scope cuts** (capture-quick, capture-url, exports staying ⚠ Mixed) are explicit, documented, and the result of deliberate scope discipline — not gaps.

## Next steps

- `/gsd-complete-milestone v0.4` — archive milestone with `tech_debt` status acknowledged.
- `/gsd-cleanup` — archive phase directories.
- `/gsd-audit-uat` — surface HUMAN-UAT items for operator pass-through after archive.
- Operator runtime pass: physical iPhone runs of the Playwright suite + PUSH-ROUNDTRIP.md fill-in.

---

*v0.4 closes the audit-driven correctness pass + identity polish. The "feels Al Dente" verdict distribution improvement (5→11 surfaces) is the milestone's primary outcome under the same 6-pillar rubric — no rubric revision, just driver-by-driver closure.*
