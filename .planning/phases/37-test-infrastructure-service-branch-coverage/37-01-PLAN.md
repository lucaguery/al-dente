---
phase: 37-test-infrastructure-service-branch-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/app/services/svg_sanitizer_test.py
  - backend/tests/test_svg_sanitizer.py
autonomous: true
requirements: [COV-06, COV-07]
tags: [pytest, fixtures, test-infrastructure, coverage]

must_haves:
  truths:
    - "`uv run pytest` exits 0 with zero failures (96 previously-failing tests now pass)"
    - "Re-running pytest a second time also exits 0 (autouse seed fixture is idempotent)"
    - "`backend/app/services/svg_sanitizer_test.py` no longer exists in the source tree (so coverage stops counting its 77 statements as untested source)"
    - "`backend/tests/test_svg_sanitizer.py` exists and its tests run (re-located file is discovered by pytest)"
    - "Git history is preserved for the relocated test file (via `git mv`)"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Autouse session-scoped `_seeded_database` fixture invoking `app.cli.seed.run_test_seed` once per session, plus the existing per-test `db_session` and `client` fixtures (unmodified)"
      contains: "scope=\"session\", autouse=True"
    - path: "backend/tests/test_svg_sanitizer.py"
      provides: "All SVG sanitizer unit tests, runnable via `pytest backend/tests/test_svg_sanitizer.py`"
      contains: "from app.services.svg_sanitizer import sanitize_recipe_svg"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "app.cli.seed.run_test_seed"
      via: "direct function call from autouse fixture (preferred over argv-parsing main())"
      pattern: "from app.cli.seed import run_test_seed"
    - from: "backend/tests/test_svg_sanitizer.py"
      to: "app.services.svg_sanitizer"
      via: "absolute import (relocated file still imports the production sanitizer)"
      pattern: "from app.services.svg_sanitizer import"
---

<objective>
Unblock the 96 backend tests that currently fail with `AssertionError: seed Postgres has no member with auth_token='test-token-luca'` by adding an autouse session-scoped seed fixture (D-37-01), and relocate the misplaced `svg_sanitizer_test.py` from `app/services/` to `tests/` (D-37-02) so coverage stops counting it as untested source.

Purpose: This is the unblock prerequisite for Plans 37-02 and 37-03. There is no point writing rules-file unit tests while 96 unrelated tests are red and 77 statements of source-tree noise drag the baseline.

Output:
- `backend/tests/conftest.py` updated with `_seeded_database` autouse fixture (existing `db_session`/`client` fixtures unchanged).
- `backend/app/services/svg_sanitizer_test.py` removed; `backend/tests/test_svg_sanitizer.py` exists at the new location with git history preserved via `git mv`.
- `uv run pytest` exits 0 across the full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-test-infrastructure-service-branch-coverage/37-CONTEXT.md
@.planning/quick/260519-uxn-add-pytest-cov-to-backend-run-baseline-c/260519-uxn-SUMMARY.md
@backend/tests/conftest.py
@backend/app/cli/seed.py
@backend/app/services/svg_sanitizer_test.py
@CLAUDE.md
@backend/CLAUDE.md
@.planning/CLAUDE.md

<interfaces>
<!-- Key contracts extracted from the codebase the executor must use as-is. -->

From `backend/app/cli/seed.py`:
- `run_test_seed() -> None` — idempotent function that calls `SessionLocal()` internally, merges seed rows, commits. Reads `SEED_AUTH_TOKEN` and `SEED_AUTH_TOKEN_PARTNER` env vars with defaults `"test-token-luca"` and `"test-token-partner"`.
- `_guard_environment() -> None` — exits hard if `ENVIRONMENT != "test"` or if `"aldente_test"` not in `database_url`. The autouse fixture MUST call this guard (directly or via `run_test_seed`'s normal path) so the fixture refuses to corrupt a non-test DB.
- `main()` — argv parser; the autouse fixture should NOT call `main()` because that parses `sys.argv` (pytest's argv would be misread). Call `_guard_environment()` then `run_test_seed()` directly.

From `backend/tests/conftest.py` (existing — DO NOT MODIFY these):
- `TEST_DATABASE_URL` module constant (line 27).
- `_engine` and `_TestSessionLocal` module singletons (lines 32-35).
- `db_session` per-test fixture (lines 38-52) — connection-scoped transaction rolled back at teardown. The autouse seed fixture runs BEFORE any `db_session` is opened, so seeded rows are committed before the per-test transaction begins; per-test inserts roll back individually without disturbing the seed.
- `client` per-test fixture (lines 55-71) — overrides `get_db` via `app.dependency_overrides`.

From `backend/app/services/svg_sanitizer_test.py` (top of file):
- Imports: `import re`, `import pytest`, `from app.services.svg_sanitizer import sanitize_recipe_svg` — already absolute, no rewrites needed.
- Module docstring references invocation `cd backend && uv run pytest app/services/svg_sanitizer_test.py -v` — update to `cd backend && uv run pytest tests/test_svg_sanitizer.py -v`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add autouse session-scoped seed fixture to conftest.py</name>
  <files>backend/tests/conftest.py</files>
  <behavior>
    - After applying the change, running `uv run pytest backend/tests/test_recipes.py::test_list_recipes -x` (or any test that previously called `_seeded_member()`) passes instead of raising `AssertionError: seed Postgres has no member with auth_token='test-token-luca'`.
    - Re-running pytest a second time still exits 0 — seed is idempotent (verified by existing `tests/test_seed_idempotency.py`).
    - The existing per-test `db_session` and `client` fixtures behave identically (no contract change; rolled-back transactions still isolate per-test inserts).
    - The new fixture refuses to run against a non-test DB (because it routes through `app.cli.seed._guard_environment()`).
  </behavior>
  <action>
Per D-37-01 (LOCKED): add ONE new autouse session-scoped pytest fixture named `_seeded_database` to `backend/tests/conftest.py`. Append it AFTER the existing `db_session` and `client` fixtures — do not modify, reorder, or wrap those (scope_fence — Plan 37-01 may add to conftest.py but must not touch the existing fixtures).

Fixture shape:
- Decorator: `@pytest.fixture(scope="session", autouse=True)`
- Name: `_seeded_database` (leading underscore — fixture is implicit via autouse, not requested by name).
- Body: import `run_test_seed` and `_guard_environment` from `app.cli.seed` lazily inside the fixture (avoids triggering settings import at module-load if env is misconfigured); call `_guard_environment()` then `run_test_seed()`; yield (no teardown — the data persists for the session, per-test rollbacks isolate test inserts).
- The import MUST be `from app.cli.seed import run_test_seed, _guard_environment` (call the underlying functions directly, NOT `main()`, per D-37-01 implementation shape — calling `main()` would re-parse `sys.argv` which pytest controls).
- Error path: if `run_test_seed()` raises (e.g. DB unmigrated, env missing), wrap the raised exception in a `pytest.fail(...)` with a clear message that points at `TESTING.md` Quick Start (4-command bootstrap). Do NOT silently skip — D-37-01 edge case mandates a loud failure.
- Add a short docstring referencing D-37-01 and explaining that this runs ONCE per session, before any per-test transaction, so seeded rows are committed and visible to all tests while per-test inserts still roll back.

Do not change `TEST_DATABASE_URL`, `_engine`, `_TestSessionLocal`, `db_session`, or `client`. Do not add new module-level imports beyond what the fixture needs (the lazy import inside the fixture body is intentional).

Reference: D-37-01 in `.planning/phases/37-test-infrastructure-service-branch-coverage/37-CONTEXT.md`.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest backend/tests/test_recipes.py -x --no-header 2>&1 | tail -5</automated>
  </verify>
  <done>The previously-failing tests in `test_recipes.py` now pass; pytest exit code is 0; the autouse fixture appears in `backend/tests/conftest.py` with `scope="session", autouse=True`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Relocate svg_sanitizer_test.py to tests/ via git mv</name>
  <files>backend/app/services/svg_sanitizer_test.py, backend/tests/test_svg_sanitizer.py</files>
  <behavior>
    - After the move, `git log --follow backend/tests/test_svg_sanitizer.py` shows the file's prior history (the `git mv` preserves it).
    - `backend/app/services/svg_sanitizer_test.py` does not exist.
    - `cd backend && uv run pytest tests/test_svg_sanitizer.py -v` runs and all sanitizer tests pass.
    - Subsequent coverage runs no longer report `app/services/svg_sanitizer_test.py` as a measured source file at 0%.
  </behavior>
  <action>
Per D-37-02 (LOCKED): relocate the misplaced test file. Use `git mv` to preserve history — NOT `cp` + `rm`, NOT `mv` (plain `mv` followed by `git add`/`git rm` confuses `git log --follow`).

Steps:
1. From the repo root, run `git mv backend/app/services/svg_sanitizer_test.py backend/tests/test_svg_sanitizer.py`. Git records this as a rename in the staging index.
2. Open the new file at `backend/tests/test_svg_sanitizer.py` and verify the imports are already absolute (`from app.services.svg_sanitizer import sanitize_recipe_svg`) — per the interfaces block, they are. No import-path edits required.
3. Update the module docstring's invocation example from `cd backend && uv run pytest app/services/svg_sanitizer_test.py -v` to `cd backend && uv run pytest tests/test_svg_sanitizer.py -v`. This is the only content edit; do not refactor tests in this plan (scope_fence — Plan 37-01 may make minor import adjustments and the docstring path correction, nothing else).
4. Verify the relocation: `git status` should show a single rename entry (R) plus possibly a modification (M) for the docstring; `cd backend && uv run pytest tests/test_svg_sanitizer.py -v` should report all tests passing.

Do not modify `app/services/svg_sanitizer.py` (the production sanitizer) — that's a source file and out of scope per the scope_fence.

Reference: D-37-02 in `.planning/phases/37-test-infrastructure-service-branch-coverage/37-CONTEXT.md`.
  </action>
  <verify>
    <automated>test ! -f backend/app/services/svg_sanitizer_test.py && test -f backend/tests/test_svg_sanitizer.py && cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_svg_sanitizer.py -v --no-header 2>&1 | tail -5</automated>
  </verify>
  <done>Old path absent, new path present, all sanitizer tests pass at the new location, git records the move as a rename.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full-suite green run + coverage delta capture</name>
  <files>(no files modified — verification only)</files>
  <behavior>
    - `uv run pytest` exits 0 across the entire backend suite (no `--maxfail`, no `-k` filter).
    - The coverage report shows `app/services/svg_sanitizer_test.py` is GONE from the per-file table (the file no longer exists; coverage does not measure it).
    - The repo-wide coverage number is captured in the SUMMARY for delta-tracking (no `fail_under` asserted per D-37-05; this is observation, not enforcement).
  </behavior>
  <action>
Run the full backend test suite with coverage and capture the numbers for the Plan 37-01 SUMMARY.md.

Command (uses the same env-var pattern from `quick/260519-uxn` baseline so results are comparable):

```
cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest --cov=app --cov-report=term-missing --cov-report=json:.coverage.json
```

Acceptance:
- Exit code is 0.
- The terminal output's last "FAILED" tally is zero. If any test still fails, do NOT patch source — diagnose: most likely the seed-fixture import order or `_guard_environment` is sys.exit'ing on missing env. Fix the fixture (Task 1) or env passing, not the failing test. If a test reveals a real bug in source, capture it as a follow-up TODO in the SUMMARY and stop — per scope_fence, Plan 37-01 must not modify any source file.
- The number of `pass`/`fail`/`error` counts is recorded in the SUMMARY (compare to baseline 63 passed / 96 failed / 2 errors).
- The repo-wide line coverage number (from `coverage report` line `TOTAL ...`) is recorded in the SUMMARY along with the per-file figures for the 4 rules files (`app/services/voting.py`, `app/services/algorithm.py`, `app/services/shortlist.py`, `app/auth.py`) so Plans 37-02 / 37-03 can measure their lift.
- Confirm `app/services/svg_sanitizer_test.py` is NOT in the per-file coverage table (its row should be gone; the rows that move slightly are expected).

Do NOT add a `fail_under` threshold to `backend/pyproject.toml` (D-37-05 — Phase 39's job).
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest --cov=app --cov-report=term-missing 2>&1 | tail -50</automated>
  </verify>
  <done>Pytest exit code is 0; full-suite pass count is recorded in SUMMARY.md; coverage baseline-vs-now table for the 4 rules files is recorded; svg_sanitizer_test.py is no longer in the per-file coverage table.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest → seed CLI | Autouse fixture invokes `run_test_seed()` which writes to the test DB; relies on `_guard_environment` to refuse non-test DBs. |
| developer machine → test DB | The Docker-Compose Postgres on 5433 is the only legitimate target; `_guard_environment` enforces the `"aldente_test"` substring check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Tampering | `_seeded_database` fixture writing to wrong DB | mitigate | Fixture calls `_guard_environment()` BEFORE `run_test_seed()`; guard sys.exits if `ENVIRONMENT != "test"` or `"aldente_test"` not in `database_url`. Per existing `app/cli/seed.py:132-152`. |
| T-37-01-02 | DoS | Slow session-startup if seed runs every pytest invocation | accept | Seed is ~150ms (per `RUNBOOK.md` measurements); session-scoped so it runs ONCE per pytest invocation, not per test. Acceptable for the dev/CI loop. |
| T-37-01-03 | Information disclosure | Fixture surfacing DB credentials in pytest failure traceback | accept | DB URL appears in env var, not arg; pytest's traceback formatter does not expand env on `sys.exit`. The existing baseline already exposes the same URL on guard exit — no regression. |
| T-37-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — only fixture wiring + a file relocation. Re-running `uv sync` is not required. Audit table N/A. |

</threat_model>

<verification>
Repo-level acceptance:
1. `cd backend && uv run pytest` exits 0 (no `--maxfail`, no `-k`, no `--no-cov`).
2. `git log --follow backend/tests/test_svg_sanitizer.py` shows commit history from before the rename (proves `git mv` preserved history).
3. `test ! -f backend/app/services/svg_sanitizer_test.py` returns true.
4. Coverage JSON report at `backend/.coverage.json` parses cleanly and `app/services/svg_sanitizer_test.py` is absent from `files` dictionary.
</verification>

<success_criteria>
- All 4 `must_haves.truths` are demonstrably true (full-suite pass, idempotent re-run, old path absent, new path present + tests pass, git history preserved).
- `_seeded_database` fixture appears in `backend/tests/conftest.py` with the locked shape (autouse, session-scoped, calls `_guard_environment` then `run_test_seed`).
- SUMMARY.md records: before-vs-after pass/fail counts, before-vs-after per-file coverage numbers for the 4 rules files, and a note that `svg_sanitizer_test.py` is no longer measured as source.
- No source file under `backend/app/` was modified (scope_fence enforced).
- No `fail_under` threshold added to `backend/pyproject.toml` (D-37-05).
</success_criteria>

<output>
Create `.planning/phases/37-test-infrastructure-service-branch-coverage/37-01-SUMMARY.md` when done. Include the before-vs-after coverage table for the 4 rules files (numerator/denominator + percentage) and the before-vs-after pass/fail counts.
</output>
