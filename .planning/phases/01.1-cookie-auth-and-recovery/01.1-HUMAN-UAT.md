---
status: partial
phase: 01.1-cookie-auth-and-recovery
source: [01.1-VERIFICATION.md]
started: 2026-05-06T08:32:00.000Z
updated: 2026-05-06T08:32:00.000Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. iOS PWA force-quit → reopen
expected: After installing the app to iOS Home Screen, force-quitting via the app switcher, and reopening — the user should remain authenticated (no redirect to onboarding) because the session is held in an HttpOnly cookie, not localStorage which Safari evicts

result: [pending]

### 2. Invite code copy on iOS Safari
expected: On the `/settings` page, tapping the copy button next to the invite code calls `navigator.clipboard.writeText(invite_code)` and shows a Sonner success toast — requires user gesture on iOS, cannot be automated

result: [pending]

### 3. Device-wipe idempotent rejoin
expected: On a second device (or after clearing Safari data), navigating to /onboarding/join and entering the same household invite code + same member name (case-insensitive) returns the original member identity (same member_id, same auth_token) without creating a duplicate row in the `members` table

result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
