---
phase: 40-pure-frontend-restyles
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/cooking-logs/[id]/page.tsx
autonomous: true
requirements:
  - DRIFT-01
must_haves:
  truths:
    - "frontend/app/cooking-logs/[id]/page.tsx contains zero references to Fraunces italic typography"
    - "frontend/app/cooking-logs/[id]/page.tsx contains zero references to bg-surface-rose-100"
    - "frontend/app/cooking-logs/[id]/page.tsx contains zero references to Sober Kitchen, cookbook-chapter-opener, Phase 17, or D-17-05"
    - "The loved rating chip uses the valide-tint background + terracotta border + terracotta text (bg-[var(--color-valide-tint)] text-primary border border-primary)"
    - "The liked rating chip uses bg-card border border-border text-foreground (no terracotta, no valide-tint)"
    - "The disliked rating chip is unchanged (bg-muted text-muted-foreground border border-border)"
    - "The absolute-date header renders in Geist 500 with no italic styling"
    - "The file header comment cites ADR-0004 and Phase 40 CONTEXT.md instead of Phase 17 / Sober Kitchen / D-17-05"
  artifacts:
    - path: frontend/app/cooking-logs/[id]/page.tsx
      provides: "Cooking-log detail page with La Grille tokens only"
      contains: "bg-[var(--color-valide-tint)] text-primary border border-primary"
  key_links: []
---

<objective>
Surgical token sweep on `frontend/app/cooking-logs/[id]/page.tsx` per DRIFT-01 (D-10 through D-14). Replace the three rating-chip class strings, drop the Fraunces italic absolute-date header gesture, and rewrite the file-header comment to cite ADR-0004 + Phase 40 CONTEXT.md.

Purpose: The cooking-log detail page is the last documented La Grille drift. The "loved" chip uses `bg-surface-rose-100` (a Sober Kitchen rose token from the retired register). The "liked" chip incorrectly co-opts the terracotta validé-tint (which by token convention is reserved for the validated voting state — see globals.css comment around line 71). The absolute-date header uses Fraunces italic as a "cookbook-chapter-opener gesture" — La Grille has no italic chapter openers. Phase 40 closes the drift with one file edit.

Output: Single-file edit. No new files, no new tests (DRIFT-01 verification is grep-based static analysis).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-pure-frontend-restyles/40-CONTEXT.md
@.planning/phases/40-pure-frontend-restyles/40-RESEARCH.md
@docs/adr/0004-modern-sober-refresh.md
@.claude/skills/sketch-findings-al-dente/SKILL.md
@.claude/skills/sketch-findings-al-dente/references/migration.md

@frontend/app/cooking-logs/[id]/page.tsx
@frontend/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the loved rating chip class (D-10)</name>
  <files>frontend/app/cooking-logs/[id]/page.tsx</files>
  <read_first>frontend/app/cooking-logs/[id]/page.tsx, frontend/app/globals.css</read_first>
  <action>
  In `frontend/app/cooking-logs/[id]/page.tsx`, locate the rating-chip class function (around line 48 today — confirm by `grep -n "bg-surface-rose-100" frontend/app/cooking-logs/[id]/page.tsx`). Replace the `"loved"` branch:

  Before (line 48 today):
  ```ts
  return `${base} bg-surface-rose-100 text-primary border border-primary/40`;
  ```

  After:
  ```ts
  return `${base} bg-[var(--color-valide-tint)] text-primary border border-primary`;
  ```

  Rationale (per D-10): the loved chip now reuses the same valide-tint + terracotta border + terracotta text as the Accueil "Validé" voting state — strongest cooking-log signal aligns visually with strongest voting signal. The `border-primary/40` opacity drops to full `border-primary`.

  Do NOT touch the liked / disliked branches in this task; those are Tasks 2 and 3.
  </action>
  <verify>grep -F 'bg-[var(--color-valide-tint)] text-primary border border-primary' frontend/app/cooking-logs/[id]/page.tsx</verify>
  <acceptance_criteria>
    - `grep -F 'bg-surface-rose-100' frontend/app/cooking-logs/[id]/page.tsx | wc -l` returns `0`
    - `grep -F 'bg-[var(--color-valide-tint)] text-primary border border-primary' frontend/app/cooking-logs/[id]/page.tsx | wc -l` returns ≥1
    - The "loved" case in the rating-chip function returns the new string
  </acceptance_criteria>
  <done>Loved chip uses La Grille valide-tint + terracotta</done>
</task>

<task type="auto">
  <name>Task 2: Replace the liked rating chip class (D-11)</name>
  <files>frontend/app/cooking-logs/[id]/page.tsx</files>
  <read_first>frontend/app/cooking-logs/[id]/page.tsx</read_first>
  <action>
  In `frontend/app/cooking-logs/[id]/page.tsx`, locate the `"liked"` branch of the rating-chip function (around line 50 today). Replace:

  Before:
  ```ts
  return `${base} bg-[var(--color-valide-tint)] text-foreground border border-[var(--color-valide-border-faint)]`;
  ```

  After:
  ```ts
  return `${base} bg-card border border-border text-foreground`;
  ```

  Rationale (per D-11): liked is neutral-positive. No terracotta needed. The valide-tint token is reserved exclusively for the validated voting state (see globals.css comment around line 71). Frees the tint for the loved chip (Task 1) and the library minimal-mode validé pill (Plan 40-03).

  Do NOT touch the loved or disliked branches.
  </action>
  <verify>grep -F 'bg-card border border-border text-foreground' frontend/app/cooking-logs/[id]/page.tsx</verify>
  <acceptance_criteria>
    - The "liked" case in the rating-chip function returns `${base} bg-card border border-border text-foreground` (or equivalent ordering — must include `bg-card`, `border border-border`, `text-foreground`)
    - `grep -F 'var(--color-valide-border-faint)' frontend/app/cooking-logs/[id]/page.tsx | wc -l` returns `0` (the legacy faint-border token is gone)
    - Only ONE occurrence of `var(--color-valide-tint)` remains in the file — the one belonging to the loved chip from Task 1
  </acceptance_criteria>
  <done>Liked chip is neutral; valide-tint reserved for loved + voting validated state</done>
</task>

<task type="auto">
  <name>Task 3: Drop Fraunces italic on the absolute-date header (D-13)</name>
  <files>frontend/app/cooking-logs/[id]/page.tsx</files>
  <read_first>frontend/app/cooking-logs/[id]/page.tsx</read_first>
  <action>
  Locate the absolute-date header in the file (the element that renders the cooking-log date in Fraunces italic — search for `Fraunces`, `italic`, or the header rendering). Replace its className so the date renders in Geist 500 with no italic:

  - Remove any `font-fraunces`, `font-[Fraunces]`, or `italic` classes from the date header element.
  - Replace with `text-3xl font-medium tracking-tight text-foreground` (or whichever heading scale the existing detail-view headers across La Grille use — pick the size that matches other recipe-detail / cooking-log headers).

  If the date is rendered inside a heading element (likely `<h1>` or `<h2>`), keep the heading level but swap the className.

  Rationale (per D-13): La Grille has no italic chapter-opener gesture. All detail-view headers render in Geist 500.
  </action>
  <verify>grep -E "Fraunces|font-fraunces|font-\[Fraunces\]" frontend/app/cooking-logs/[id]/page.tsx | wc -l</verify>
  <acceptance_criteria>
    - `grep -E "Fraunces|font-fraunces|font-\[Fraunces\]" frontend/app/cooking-logs/[id]/page.tsx | wc -l` returns `0`
    - The date header element no longer has an `italic` class
    - The date header renders (visually verified manually) in Geist 500
  </acceptance_criteria>
  <done>No Fraunces references; date header in Geist 500</done>
</task>

<task type="auto">
  <name>Task 4: Rewrite the file-header comment block (D-14)</name>
  <files>frontend/app/cooking-logs/[id]/page.tsx</files>
  <read_first>frontend/app/cooking-logs/[id]/page.tsx</read_first>
  <action>
  Replace lines 3-23 (the existing comment block referencing Phase 17 / HIST-02 / Fraunces / cookbook-chapter-opener / Sober Kitchen — confirm exact line range by reading the file's current state) with a 5-7 line La Grille header comment. Suggested content:

  ```tsx
  // Cooking-log detail page.
  //
  // Visual register: La Grille · Soft warmth per ADR-0004
  // (docs/adr/0004-modern-sober-refresh.md). Token sources:
  // frontend/app/globals.css. Locked decisions for this file
  // ship in .planning/phases/40-pure-frontend-restyles/40-CONTEXT.md
  // (D-10 through D-14).
  ```

  Strike all references to:
  - `Phase 8`, `Phase 17`, `HIST-02`, `D-17-05`
  - `Sober Kitchen`
  - `Fraunces`
  - `cookbook-chapter-opener`
  - `Cormorant`, `Caveat`, `paper-grain`

  Preserve any non-comment code immediately following the comment block. Do not change the imports.
  </action>
  <verify>grep -E "Phase 17|HIST-02|D-17-05|Sober Kitchen|cookbook-chapter-opener|Fraunces|Cormorant|Caveat|paper-grain" frontend/app/cooking-logs/[id]/page.tsx | wc -l</verify>
  <acceptance_criteria>
    - File header comment contains `ADR-0004` and `Phase 40` and `globals.css`
    - `grep -E "Phase 17|HIST-02|D-17-05|Sober Kitchen|cookbook-chapter-opener|Fraunces|Cormorant|Caveat|paper-grain" frontend/app/cooking-logs/[id]/page.tsx | wc -l` returns `0`
    - File imports are unchanged from pre-task state
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Header comment cites ADR-0004 + Phase 40 only</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run lint` exits 0
- [ ] `cd frontend && npm run build` exits 0
- [ ] `grep -E "Fraunces|bg-surface-rose-100|cookbook-chapter-opener|Sober Kitchen|Phase 17|HIST-02|D-17-05" frontend/app/cooking-logs/\[id\]/page.tsx | wc -l` returns 0
- [ ] `grep -F 'bg-[var(--color-valide-tint)] text-primary border border-primary' frontend/app/cooking-logs/\[id\]/page.tsx | wc -l` returns ≥1 (loved chip)
- [ ] `grep -F 'bg-card border border-border text-foreground' frontend/app/cooking-logs/\[id\]/page.tsx | wc -l` returns ≥1 (liked chip)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Zero Sober Kitchen / Fraunces references in the cooking-log detail page
- Loved chip uses La Grille valide-tint + terracotta
- Liked chip uses neutral card surface
- Disliked chip unchanged
- File header comment cites only ADR-0004 + Phase 40 CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/40-pure-frontend-restyles/40-05-SUMMARY.md`
</output>
