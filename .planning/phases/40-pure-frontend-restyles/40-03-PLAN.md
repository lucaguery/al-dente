---
phase: 40-pure-frontend-restyles
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/LibraryViewSwitch.tsx
  - frontend/components/RecipeRowMinimal.tsx
  - frontend/app/library/page.tsx
  - frontend/lib/i18n/fr.json
  - frontend/tests/e2e/library-minimal-view.spec.ts
autonomous: true
requirements:
  - LIB-01
must_haves:
  truths:
    - "LibraryViewSwitch exports a LibraryView type that accepts three modes: grid, list (row-with-thumb), and minimal (text-only)"
    - "RecipeRowMinimal renders a single recipe as numbered index + name + meta + optional validé tag pill, with no <img> element"
    - "/library renders the minimal mode when the user selects the third switch option and persists the choice in localStorage across page reloads"
  artifacts:
    - path: frontend/components/RecipeRowMinimal.tsx
      provides: "Text-only recipe row component; no photo column"
      contains: "RecipeRowMinimal"
      min_lines: 40
    - path: frontend/components/LibraryViewSwitch.tsx
      provides: "3-state library view switch (grid / list / minimal)"
      contains: "minimal"
    - path: frontend/tests/e2e/library-minimal-view.spec.ts
      provides: "Playwright spec verifying minimal mode renders without photos and persists across reload"
      contains: "test("
  key_links:
    - from: frontend/app/library/page.tsx
      to: frontend/components/RecipeRowMinimal.tsx
      via: "Conditional render when view === 'minimal'"
      pattern: "RecipeRowMinimal"
    - from: frontend/components/LibraryViewSwitch.tsx
      to: localStorage
      via: "View persistence key (existing key unchanged)"
      pattern: "localStorage"
---

<objective>
Add a third text-only mode to `LibraryViewSwitch` and ship a new `RecipeRowMinimal` component per LIB-01 (sketch lines 1687-1693).

Purpose: Bibliothèque today offers two modes (grid-with-photo, row-with-thumb). The La Grille sketch shows a third "text-only" mode — pure numbered rows with no photo column — that matches the "space carries" discipline used on Accueil and recipe-detail. Adds a fast scan-the-whole-library mode for users who already know what their library contains visually.

Output: New `RecipeRowMinimal` component, 3-state `LibraryViewSwitch`, library page wiring, fr.json keys, Playwright persistence spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-pure-frontend-restyles/40-CONTEXT.md
@.planning/phases/40-pure-frontend-restyles/40-RESEARCH.md
@docs/adr/0004-modern-sober-refresh.md
@.claude/skills/sketch-findings-al-dente/SKILL.md

@frontend/components/LibraryViewSwitch.tsx
@frontend/components/RecipeRow.tsx
@frontend/app/library/page.tsx
@frontend/lib/votes.ts
@frontend/app/globals.css
@frontend/lib/i18n/fr.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend LibraryView type to 3 modes</name>
  <files>frontend/components/LibraryViewSwitch.tsx</files>
  <read_first>frontend/components/LibraryViewSwitch.tsx</read_first>
  <action>
  Modify `frontend/components/LibraryViewSwitch.tsx`:

  1. Change the exported type from `export type LibraryView = "grid" | "list";` to `export type LibraryView = "grid" | "list" | "minimal";`.
  2. Extend the `VIEWS` array to include a third entry with `key: "minimal"`. Pick an icon from `lucide-react` that signals text-only — `AlignJustify` is the clearest semantic ("text rows"); fallback options include `List` (already used for `list` mode — do NOT reuse) or `Menu`. Use `AlignJustify` and import it next to the existing icon imports.
  3. Update the `VIEWS` constant to declare the third icon's `Icon` type if it uses a specific union type.
  4. The button's `aria-label` should read from i18n (`library.view_switch.minimal` — add in Task 4) — pick up the existing `useTranslations` pattern if present, or hardcode `"Vue texte"` if the existing two buttons hardcode their aria-labels (match existing convention).
  5. Visual: third button uses the same hairline-border styling as the existing two; active state uses the same `bg-foreground text-background` or token swap that the existing two use. Do NOT redesign the switch — only add the third option.
  </action>
  <verify>cd frontend && npm run lint</verify>
  <acceptance_criteria>
    - `frontend/components/LibraryViewSwitch.tsx` exports `LibraryView` type with three values: `"grid" | "list" | "minimal"` (in any order)
    - `VIEWS` array contains an entry with `key: "minimal"`
    - File imports `AlignJustify` (or chosen icon) from `lucide-react`
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Switch accepts 3 modes, builds clean</done>
</task>

<task type="auto">
  <name>Task 2: Create RecipeRowMinimal component</name>
  <files>frontend/components/RecipeRowMinimal.tsx</files>
  <read_first>frontend/components/RecipeRow.tsx, frontend/lib/votes.ts, frontend/app/globals.css</read_first>
  <action>
  Create `frontend/components/RecipeRowMinimal.tsx` as a sibling to `RecipeRow.tsx`. Same props shape — accept a `recipe` (use the same `RecipeResponse` type that `RecipeRow` uses) and optional `validated` boolean (or compute internally — see below).

  Visual structure per sketch lines 1687-1693:

  ```
  <Link href={`/recipes/${recipe.id}`} className="flex items-center gap-4 w-full py-3 border-b border-border">
    <span className="text-caption tabular-nums shrink-0 text-muted-foreground">{String(index + 1).padStart(2, '0')}</span>
    <span className="flex-1 text-base font-medium text-foreground truncate">{recipe.name}</span>
    <span className="text-caption text-muted-foreground shrink-0">{meta}</span>
    {validated && (
      <span className="text-caption tabular-nums px-2 py-0.5 rounded-sm bg-[var(--color-valide-tint)] text-primary border border-primary">validé</span>
    )}
  </Link>
  ```

  Where:
  - `index` is passed as a prop (the row's position in the list).
  - `meta` is `recipe.cuisine` translated through next-intl (`cuisines.{recipe.cuisine}`) — fallback to empty string if unset.
  - `validated` is computed by the parent: at this stage of the rewrite, accept `validated?: boolean` as an optional prop. The parent (library page) is responsible for computing it (see Task 3 for the source of truth).

  Constraints (verifiable):
  - File does NOT contain `<img` or `<Image` (no photo column — that's the entire point of this component).
  - File uses La Grille tokens only: `text-foreground`, `text-muted-foreground`, `text-primary`, `border-border`, `var(--color-valide-tint)`. No Sober Kitchen tokens.
  - Component is a default export `RecipeRowMinimal` AND a named export of the same name (match `RecipeRow.tsx` export convention — read it first to confirm).
  </action>
  <verify>cd frontend && npm run lint && grep -E "<img|<Image" components/RecipeRowMinimal.tsx | wc -l</verify>
  <acceptance_criteria>
    - `frontend/components/RecipeRowMinimal.tsx` exists with ≥40 lines
    - File exports `RecipeRowMinimal` (named export confirmed by `grep "export.*RecipeRowMinimal" frontend/components/RecipeRowMinimal.tsx`)
    - `grep -E "<img|<Image" frontend/components/RecipeRowMinimal.tsx | wc -l` returns `0`
    - File contains `border-b border-border`, `text-caption`, `tabular-nums`
    - File does NOT contain `useSignedPhotoUrl` (no photo loading)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Component renders without photo column, La Grille tokens only</done>
</task>

<task type="auto">
  <name>Task 3: Wire minimal mode into the library page</name>
  <files>frontend/app/library/page.tsx</files>
  <read_first>frontend/app/library/page.tsx, frontend/components/LibraryViewSwitch.tsx, frontend/components/RecipeRowMinimal.tsx, frontend/lib/votes.ts</read_first>
  <action>
  Locate the library page that consumes `LibraryViewSwitch` — likely `frontend/app/library/page.tsx`, but if the route is different (e.g. `frontend/app/recipes/page.tsx` for the recipe library) READ the existing import sites of `LibraryViewSwitch` to find the correct file. Use `grep -rn "LibraryViewSwitch" frontend/app/` to confirm.

  In the library page:

  1. Import `RecipeRowMinimal` from `@/components/RecipeRowMinimal`.
  2. Extend the existing render branch (which today switches between grid and list) with a third branch for `view === "minimal"`. Render the recipe list as a single `<div className="divide-y divide-border">` wrapping `recipes.map((r, i) => <RecipeRowMinimal key={r.id} recipe={r} index={i} validated={...} />)`.
  3. Compute `validated` per recipe: the cheap path per CONTEXT.md line 61-62 is to read today's `daily_shortlists.shortlist_recipes` and check `compute_vote_state(votes_for_recipe, member_count) === 'validated'` for the current member. If the library page does not already have access to the daily shortlist, pass `validated={false}` for all rows as a stub (do NOT introduce a new API call in this plan — the validé pill is a nice-to-have, not load-bearing for LIB-01's verification). Document the stub with a `// TODO(productize): wire daily-shortlist validated state` comment.
  4. The localStorage persistence key for view state — DO NOT change it. The existing key already accepts arbitrary string values; only the value set grows.
  5. Ensure the third switch option triggers the new render branch (no unhandled case at runtime).
  </action>
  <verify>cd frontend && npm run lint && grep -c "RecipeRowMinimal" app/library/page.tsx</verify>
  <acceptance_criteria>
    - The library page file imports `RecipeRowMinimal`
    - The library page contains a branch that renders `RecipeRowMinimal` when `view === "minimal"`
    - `npm run lint` exits 0
    - No new `useState` resets the persisted view; existing localStorage key shape preserved
  </acceptance_criteria>
  <done>Library page renders minimal mode without crashing</done>
</task>

<task type="auto">
  <name>Task 4: Add fr.json i18n keys for library minimal mode</name>
  <files>frontend/lib/i18n/fr.json</files>
  <read_first>frontend/lib/i18n/fr.json</read_first>
  <action>
  Add (or update if existing) keys:
  - `library.view_switch.minimal` → `"Vue texte"` (aria-label / accessible name for the third switch button)
  - `library.minimal.validated_pill` → `"validé"` (the green pill text)

  Validate JSON parses. Diff is additive.
  </action>
  <verify>cd frontend && node -e "JSON.parse(require('fs').readFileSync('lib/i18n/fr.json'))" && echo OK</verify>
  <acceptance_criteria>
    - `frontend/lib/i18n/fr.json` parses
    - Contains keys `library.view_switch.minimal` and `library.minimal.validated_pill`
  </acceptance_criteria>
  <done>Keys present, JSON valid</done>
</task>

<task type="auto">
  <name>Task 5: Playwright spec — library-minimal-view</name>
  <files>frontend/tests/e2e/library-minimal-view.spec.ts</files>
  <read_first>frontend/tests/e2e/ (list existing library specs), frontend/playwright.config.ts, frontend/components/RecipeRowMinimal.tsx</read_first>
  <action>
  Create `frontend/tests/e2e/library-minimal-view.spec.ts`. Required assertions (authenticated synthetic-seed context):

  1. Navigate to the library route (likely `/library` — confirm by reading the page file).
  2. Click the third button in the `LibraryViewSwitch` (selector: third button inside the switch group; or `[aria-label="Vue texte"]`).
  3. Assert that no `<img>` elements appear inside the recipe list region (selector for the list region depends on existing markup — use the parent of the recipe rows): `await expect(listRegion.locator('img')).toHaveCount(0)`.
  4. Assert that numbered indices `01`, `02` are visible: `await expect(page.getByText(/^01$/)).toBeVisible()`.
  5. Reload the page (`await page.reload()`).
  6. Assert the minimal mode is still active: again `await expect(listRegion.locator('img')).toHaveCount(0)` AND the third switch button has its active state class/attribute (selector depends on switch markup — read `LibraryViewSwitch.tsx` to find the active-state signal).
  </action>
  <verify>cd frontend && npx playwright test tests/e2e/library-minimal-view.spec.ts --reporter=line</verify>
  <acceptance_criteria>
    - `frontend/tests/e2e/library-minimal-view.spec.ts` exists
    - Spec contains a `page.reload()` and re-assertion (persistence check)
    - Spec contains assertion that `<img>` count is 0 in the list region when minimal is active
  </acceptance_criteria>
  <done>Spec exists with persistence + no-photo assertions</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run lint` exits 0
- [ ] `cd frontend && npm run build` exits 0
- [ ] `grep -E "<img|<Image" frontend/components/RecipeRowMinimal.tsx | wc -l` returns 0
- [ ] `cd frontend && npx playwright test tests/e2e/library-minimal-view.spec.ts` exits 0
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- `LibraryView` type is now 3-state
- `RecipeRowMinimal` component exists with no photo loading
- Minimal mode persists across reload via existing localStorage key
- No Sober Kitchen tokens introduced
</success_criteria>

<output>
After completion, create `.planning/phases/40-pure-frontend-restyles/40-03-SUMMARY.md`
</output>
