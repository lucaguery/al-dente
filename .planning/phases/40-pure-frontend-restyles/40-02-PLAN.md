---
phase: 40-pure-frontend-restyles
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/onboarding/welcome/page.tsx
  - frontend/lib/i18n/fr.json
  - frontend/tests/e2e/onboarding-welcome-la-grille.spec.ts
autonomous: true
requirements:
  - ONBO-01
must_haves:
  truths:
    - "/onboarding/welcome renders the wordmark-centric composition with no <Card> components"
    - "/onboarding/welcome renders the centered Al Dente. wordmark with the accent dot"
    - "/onboarding/welcome renders the italic-emphasis tagline 'On mange quoi *ce soir* ?'"
    - "/onboarding/welcome renders the sub-tagline 'Une app pour deux. Pour décider ensemble, sans se relancer toute la soirée.'"
    - "/onboarding/welcome renders the primary filled-dark CTA + ghost hairline CTA pair"
    - "/onboarding/welcome renders the footer marketing line 'cuisine partagée · 0 frais · 0 pub'"
  artifacts:
    - path: frontend/app/onboarding/welcome/page.tsx
      provides: "Wordmark-centric onboarding composition; no Card components"
      min_lines: 50
    - path: frontend/tests/e2e/onboarding-welcome-la-grille.spec.ts
      provides: "Playwright spec asserting Card-free DOM + wordmark + tagline + button pair + footer"
      contains: "test("
  key_links:
    - from: frontend/app/onboarding/welcome/page.tsx
      to: /onboarding/create
      via: "Link href for primary CTA 'Créer notre foyer'"
      pattern: "href=[\"']/onboarding/create"
    - from: frontend/app/onboarding/welcome/page.tsx
      to: /onboarding/join
      via: "Link href for ghost CTA 'Rejoindre avec un code'"
      pattern: "href=[\"']/onboarding/join"
---

<objective>
Rewrite `/onboarding/welcome` to the La Grille wordmark-centric composition per ONBO-01 (sketch lines 2060-2076).

Purpose: Onboarding welcome is the first surface every new household sees. The current Card-wrapped CTA pair reads as Sober Kitchen. The sketch composition is unambiguous: centered wordmark + tagline + sub-tagline + button pair + footer marketing line. This is the smallest of the rewrites (current file is 2.8 KB) and the most visually authoritative — it sets the tone for the entire La Grille register.

Output: Rewritten page file, fr.json additions, Playwright spec validating the composition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-pure-frontend-restyles/40-CONTEXT.md
@.planning/phases/40-pure-frontend-restyles/40-RESEARCH.md
@docs/adr/0004-modern-sober-refresh.md
@.claude/skills/sketch-findings-al-dente/SKILL.md

@frontend/app/onboarding/welcome/page.tsx
@frontend/components/BrandIcon.tsx
@frontend/components/ui/button.tsx
@frontend/app/globals.css
@frontend/lib/i18n/fr.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fr.json i18n keys for onboarding welcome</name>
  <files>frontend/lib/i18n/fr.json</files>
  <read_first>frontend/lib/i18n/fr.json</read_first>
  <action>
  Add (or update if existing) keys under the `onboarding.welcome` namespace:

  - `onboarding.welcome.tagline_lead` → `"On mange quoi "`
  - `onboarding.welcome.tagline_emphasis` → `"ce soir"`
  - `onboarding.welcome.tagline_tail` → `" ?"`
  - `onboarding.welcome.sub_tagline` → `"Une app pour deux. Pour décider ensemble, sans se relancer toute la soirée."`
  - `onboarding.welcome.primary_cta` → `"Créer notre foyer"` (verify existing value; only add/correct, do not duplicate)
  - `onboarding.welcome.ghost_cta` → `"Rejoindre avec un code"` (verify existing value)
  - `onboarding.welcome.footer` → `"cuisine partagée · 0 frais · 0 pub"`

  Split tagline into three keys so the italic emphasis on `"ce soir"` can be wrapped in `<em>` cleanly. Validate JSON parses.
  </action>
  <verify>cd frontend && node -e "JSON.parse(require('fs').readFileSync('lib/i18n/fr.json'))" && echo OK</verify>
  <acceptance_criteria>
    - `frontend/lib/i18n/fr.json` parses as valid JSON
    - Contains keys: `onboarding.welcome.tagline_lead`, `onboarding.welcome.tagline_emphasis`, `onboarding.welcome.tagline_tail`, `onboarding.welcome.sub_tagline`, `onboarding.welcome.primary_cta`, `onboarding.welcome.ghost_cta`, `onboarding.welcome.footer`
    - Diff is additive (no existing key removed or renamed)
  </acceptance_criteria>
  <done>JSON valid, all 7 keys present</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite frontend/app/onboarding/welcome/page.tsx (ONBO-01)</name>
  <files>frontend/app/onboarding/welcome/page.tsx</files>
  <read_first>frontend/app/onboarding/welcome/page.tsx, .claude/skills/sketch-findings-al-dente/sources/002-refresh-direction-explorations/index.html, frontend/components/BrandIcon.tsx, frontend/components/ui/button.tsx, frontend/app/globals.css, frontend/lib/i18n/fr.json</read_first>
  <action>
  Completely rewrite `frontend/app/onboarding/welcome/page.tsx` to match sketch lines 2060-2076.

  Visual structure (top to bottom, centered on `bg-background` `#FAFAF7`):

  1. Optional `BrandIcon` table-à-manger logomark at ~72px, centered. Sketch shows the logomark at the top; keep.
  2. Wordmark `Al Dente.` rendered as `<h1>` in Geist 500, `text-4xl tracking-tight`, with the `.` rendered as an accent dot in `text-primary` (`#A8523C`).
  3. Tagline: `<p>` in Geist 400, `text-lg text-muted-foreground`, rendering `"On mange quoi <em>ce soir</em> ?"` — use three i18n keys (`tagline_lead`, `tagline_emphasis` inside `<em className="italic font-medium text-foreground">`, `tagline_tail`). The italic on `ce soir` is the single typographic emphasis on this page.
  4. Sub-tagline: `<p>` in Geist 400, `text-base text-muted-foreground`, max-width `~32ch`, centered: "Une app pour deux. Pour décider ensemble, sans se relancer toute la soirée."
  5. Button pair, vertical stack with `gap-3`, max-width `~20rem`:
     - Primary filled-dark: `<Link href="/onboarding/create"><Button variant="default" size="lg" className="w-full">Créer notre foyer</Button></Link>` — uses the default shadcn button (dark fill `bg-foreground text-background`, no shadow).
     - Ghost hairline: `<Link href="/onboarding/join"><Button variant="outline" size="lg" className="w-full">Rejoindre avec un code</Button></Link>` — outline variant with `border-border text-foreground` hairline.
  6. Footer marketing line: `<p>` in Geist 400, `text-caption text-muted-foreground`, centered, with `mt-12`: `"cuisine partagée · 0 frais · 0 pub"`.

  Layout: `min-h-screen flex flex-col items-center justify-center gap-6 px-6 py-12 bg-background`.

  Constraints (verifiable):
  - REMOVE the `import { Card } from "@/components/ui/card"` and both `<Card>` JSX usages currently in the file.
  - REMOVE the Phase 6 D-Voice callout pattern comments — replace the file header with a 5-line La Grille comment citing ADR-0004 + Phase 40 CONTEXT.md.
  - PRESERVE the `useTranslations` `next-intl` pattern; all visible strings flow through `onboarding.welcome.*` keys.
  - PRESERVE the `Link` href values (`/onboarding/create` and `/onboarding/join`) — those are the existing routes.
  - Do NOT introduce Fraunces, Cormorant, Caveat, paper-grain, or any Sober Kitchen tokens.
  </action>
  <verify>cd frontend && npm run lint && grep -E "<Card|from.*\"card\"" app/onboarding/welcome/page.tsx | wc -l</verify>
  <acceptance_criteria>
    - `frontend/app/onboarding/welcome/page.tsx` ESLint passes
    - `grep -E "<Card|from.*\"card\"" frontend/app/onboarding/welcome/page.tsx | wc -l` returns `0`
    - File contains `Al Dente.` text (or split form `Al Dente` + `.`)
    - File contains `<em` (the italic emphasis on `ce soir`)
    - File contains `href="/onboarding/create"` and `href="/onboarding/join"`
    - File does NOT contain `Fraunces`, `Cormorant`, `Caveat`, `paper-grain`, `Sober Kitchen`, `bg-surface-rose-100`
  </acceptance_criteria>
  <done>Lint clean, Card-free, wordmark + tagline + button pair + footer present</done>
</task>

<task type="auto">
  <name>Task 3: Playwright spec — onboarding-welcome-la-grille</name>
  <files>frontend/tests/e2e/onboarding-welcome-la-grille.spec.ts</files>
  <read_first>frontend/tests/e2e/ (list existing onboarding spec if any), frontend/playwright.config.ts, frontend/app/onboarding/welcome/page.tsx</read_first>
  <action>
  Create `frontend/tests/e2e/onboarding-welcome-la-grille.spec.ts`. Required assertions (unauthenticated context — no seed cookie needed):

  1. Navigate to `/onboarding/welcome`.
  2. Assert no Card surface: `await expect(page.locator('[data-slot="card"]')).toHaveCount(0)`.
  3. Assert wordmark visible: `await expect(page.getByRole('heading', { level: 1, name: /Al Dente/i })).toBeVisible()`.
  4. Assert italic emphasis on `ce soir`: `await expect(page.locator('em')).toContainText('ce soir')`.
  5. Assert sub-tagline visible: `await expect(page.getByText(/Une app pour deux/i)).toBeVisible()`.
  6. Assert primary CTA visible and links to `/onboarding/create`: `const create = page.getByRole('link', { name: /Créer notre foyer/i }); await expect(create).toBeVisible(); await expect(create).toHaveAttribute('href', '/onboarding/create');`.
  7. Same for ghost CTA → `/onboarding/join`.
  8. Assert footer line visible: `await expect(page.getByText(/cuisine partagée.*0 frais.*0 pub/i)).toBeVisible()`.
  </action>
  <verify>cd frontend && npx playwright test tests/e2e/onboarding-welcome-la-grille.spec.ts --reporter=line</verify>
  <acceptance_criteria>
    - `frontend/tests/e2e/onboarding-welcome-la-grille.spec.ts` exists
    - Spec contains ≥7 `expect(...)` assertions
    - Spec runs without modifying production routes; uses unauthenticated context
  </acceptance_criteria>
  <done>Spec file exists with full composition assertions</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run lint` exits 0
- [ ] `cd frontend && npm run build` exits 0
- [ ] `grep -E "<Card|from.*\"card\"" frontend/app/onboarding/welcome/page.tsx | wc -l` returns 0
- [ ] `grep -E "Fraunces|Cormorant|Caveat|paper-grain|Sober Kitchen" frontend/app/onboarding/welcome/page.tsx | wc -l` returns 0
- [ ] `cd frontend && npx playwright test tests/e2e/onboarding-welcome-la-grille.spec.ts` exits 0
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No `<Card>` usage in `/onboarding/welcome`
- Wordmark + italic-emphasis tagline + sub-tagline + button pair + footer all render per sketch lines 2060-2076
- Both CTAs preserve their existing route targets
</success_criteria>

<output>
After completion, create `.planning/phases/40-pure-frontend-restyles/40-02-SUMMARY.md`
</output>
