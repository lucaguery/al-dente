---
phase: 05-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/globals.css
autonomous: true
requirements: [DESIGN-03, DESIGN-05, DESIGN-06, DESIGN-08]
must_haves:
  truths:
    - "App surfaces (anywhere `bg-background`, `bg-card`, `bg-primary` resolves) read as warm cream + terracotta + warm-taupe — no rose #F43F5E and no slate"
    - "Cards render with warm-brown two-layer shadows (paper-on-wood feel), not cool floating box-shadow"
    - "CSS exposes `--ease-craft`, `--duration-fast: 150ms`, `--duration-normal: 280ms` tokens consumable as Tailwind v4 utilities"
    - "`.paper-grain` utility class exists in @layer utilities and renders a multiplied warm-noise overlay via ::before pseudo, ready to be applied by Plan 05 to card primitives"
    - "All v0.1 token names (`--primary`, `--ring`, `--shadow-card`, `--surface-rose-100`, `--color-valide-tint`, `--radius-*`) are preserved or aliased — no component breakage"
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "Slow Food artisanal token system (colors, warm shadows, motion CSS tokens, paper-grain utility class)"
      contains: "oklch(0.595 0.135 35)"
  key_links:
    - from: "globals.css :root --primary"
      to: "Tailwind --color-primary mapping in @theme inline"
      via: "var() reference"
      pattern: "--color-primary:\\s*var\\(--primary\\)"
    - from: "globals.css --shadow-card"
      to: "Tailwind shadow-card utility"
      via: "@theme inline shadow scale exposure"
      pattern: "--shadow-card:.*rgba\\(74,\\s*56,\\s*40"
    - from: "globals.css .paper-grain utility"
      to: "/textures/paper-grain.svg asset (Plan 02)"
      via: "background-image url('/textures/paper-grain.svg')"
      pattern: "background-image:\\s*url\\(['\"]?/textures/paper-grain\\.svg"
---

<objective>
Migrate `frontend/app/globals.css` to the Slow Food artisanal token system: terracotta primary at h≈35° replacing rose, warm-cream/warm-taupe/ink neutrals replacing slate, two-layer warm-brown shadows replacing cool floating shadows, motion CSS tokens (`--ease-craft`, `--duration-fast`, `--duration-normal`), and the `.paper-grain` utility class wired to `/textures/paper-grain.svg`. Preserve all v0.1 token names per DESIGN-03; no component churn.

Purpose: Establishes the foundation every subsequent Phase 5 plan and every later v0.2 phase consumes. After this plan ships, any screen consuming `bg-background`, `bg-card`, `text-primary`, `shadow-card` automatically reflects the new palette.
Output: A single rewritten `frontend/app/globals.css` whose `:root`, `.dark`, `@theme inline`, `@layer utilities`, and the new motion + paper-grain blocks match the UI-SPEC verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@.planning/phases/05-design-system-foundation/05-CONTEXT.md
@.planning/notes/v0.2-design-direction.md
@frontend/app/globals.css
@frontend/AGENTS.md

<interfaces>
<!-- Existing v0.1 globals.css token shape that MUST be preserved.
     Names below are referenced by Tailwind utilities all over the app.
     Values change; names stay. -->

From frontend/app/globals.css (v0.1 baseline):
- @theme inline keys to keep: --color-background, --color-foreground, --color-card, --color-card-foreground, --color-primary, --color-primary-foreground, --color-secondary, --color-secondary-foreground, --color-muted, --color-muted-foreground, --color-accent, --color-accent-foreground, --color-destructive, --color-border, --color-input, --color-ring, --color-popover, --color-popover-foreground, --color-sidebar* (8 keys), --color-chart-* (5 keys), --color-surface-muted, --color-foreground-muted, --color-surface-rose-50, --color-surface-rose-100, --color-valide-tint, --shadow-card, --shadow-card-hover, --shadow-nav, --radius-{sm,md,lg,xl,2xl,3xl,4xl}, --font-sans, --font-mono, --font-heading
- :root and .dark CSS variables: same names, values rewritten per UI-SPEC §Color tables
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate :root and .dark blocks to terracotta + warm-cream + warm-taupe palette (DESIGN-03)</name>
  <files>frontend/app/globals.css</files>
  <read_first>
    - .planning/phases/05-design-system-foundation/05-UI-SPEC.md (§Color, lines 170-290 — exact OKLCH tables for light + dark + token preservation matrix)
    - frontend/app/globals.css (current :root and .dark blocks — lines 86-177)
    - frontend/AGENTS.md (Next.js 16 docs warning)
  </read_first>
  <action>
Rewrite the `:root { ... }` block in `frontend/app/globals.css` (currently lines 86-133) to the EXACT light-mode OKLCH values from UI-SPEC §Color "Final OKLCH values — light mode":

```
:root {
  --background: oklch(0.985 0.008 60);
  --foreground: oklch(0.22 0.018 50);
  --card: oklch(0.992 0.006 60);
  --card-foreground: oklch(0.22 0.018 50);
  --popover: oklch(0.992 0.006 60);
  --popover-foreground: oklch(0.22 0.018 50);
  --primary: oklch(0.595 0.135 35);
  --primary-foreground: oklch(0.985 0.008 60);
  --secondary: oklch(0.945 0.012 50);
  --secondary-foreground: oklch(0.28 0.015 50);
  --muted: oklch(0.945 0.012 50);
  --muted-foreground: oklch(0.50 0.014 50);
  --accent: oklch(0.945 0.012 50);
  --accent-foreground: oklch(0.28 0.015 50);
  --destructive: oklch(0.55 0.20 25);
  --border: oklch(0.88 0.012 50);
  --input: oklch(0.88 0.012 50);
  --ring: oklch(0.595 0.135 35);
  --chart-1: oklch(0.87 0 0);
  --chart-2: oklch(0.556 0 0);
  --chart-3: oklch(0.439 0 0);
  --chart-4: oklch(0.371 0 0);
  --chart-5: oklch(0.269 0 0);
  --radius: 0.75rem;
  --sidebar: oklch(0.975 0.01 50);
  --sidebar-foreground: oklch(0.22 0.018 50);
  --sidebar-primary: oklch(0.28 0.015 50);
  --sidebar-primary-foreground: oklch(0.985 0.008 60);
  --sidebar-accent: oklch(0.945 0.012 50);
  --sidebar-accent-foreground: oklch(0.28 0.015 50);
  --sidebar-border: oklch(0.88 0.012 50);
  --sidebar-ring: oklch(0.595 0.135 35);
  --surface-muted: oklch(0.945 0.012 50);
  --foreground-muted: oklch(0.50 0.014 50);
  --surface-rose-50: oklch(0.97 0.022 35);
  --surface-rose-100: oklch(0.94 0.045 35);
  --valide-tint: oklch(0.93 0.07 145);
}
```

Then rewrite the `.dark { ... }` block (currently lines 135-177) to UI-SPEC §Color "Final OKLCH values — dark mode":

```
.dark {
  --background: oklch(0.16 0.012 50);
  --foreground: oklch(0.94 0.008 50);
  --card: oklch(0.21 0.014 50);
  --card-foreground: oklch(0.94 0.008 50);
  --popover: oklch(0.21 0.014 50);
  --popover-foreground: oklch(0.94 0.008 50);
  --primary: oklch(0.70 0.13 35);
  --primary-foreground: oklch(0.16 0.012 50);
  --secondary: oklch(0.26 0.012 50);
  --secondary-foreground: oklch(0.94 0.008 50);
  --muted: oklch(0.26 0.012 50);
  --muted-foreground: oklch(0.66 0.010 50);
  --accent: oklch(0.26 0.012 50);
  --accent-foreground: oklch(0.94 0.008 50);
  --destructive: oklch(0.65 0.18 25);
  --border: oklch(1 0 0 / 12%);
  --input: oklch(1 0 0 / 16%);
  --ring: oklch(0.70 0.13 35);
  --chart-1: oklch(0.87 0 0);
  --chart-2: oklch(0.556 0 0);
  --chart-3: oklch(0.439 0 0);
  --chart-4: oklch(0.371 0 0);
  --chart-5: oklch(0.269 0 0);
  --sidebar: oklch(0.21 0.014 50);
  --sidebar-foreground: oklch(0.94 0.008 50);
  --sidebar-primary: oklch(0.70 0.13 35);
  --sidebar-primary-foreground: oklch(0.16 0.012 50);
  --sidebar-accent: oklch(0.26 0.012 50);
  --sidebar-accent-foreground: oklch(0.94 0.008 50);
  --sidebar-border: oklch(1 0 0 / 12%);
  --sidebar-ring: oklch(0.70 0.13 35);
  --surface-muted: oklch(0.24 0.012 50);
  --foreground-muted: oklch(0.66 0.010 50);
  --surface-rose-50: oklch(0.22 0.025 35);
  --surface-rose-100: oklch(0.27 0.045 35);
  --valide-tint: oklch(0.30 0.06 145);
}
```

Also update the existing `@media (prefers-color-scheme: dark) { :root { ... } }` block at lines 183-191 to mirror the new dark `--surface-muted` and `--foreground-muted` values:
- `--surface-muted: oklch(0.24 0.012 50);`
- `--foreground-muted: oklch(0.66 0.010 50);`

Preserve every existing CSS variable name. Do NOT rename `--color-valide-tint` (already non-accented per UI-SPEC §Color "Token preservation matrix" — the accented `--color-validé-tint` was a documentation typo only and never appeared in CSS). Do NOT delete the `chart-*`, `radius`, or `sidebar-*` keys.
  </action>
  <verify>
    <automated>grep -F "oklch(0.595 0.135 35)" frontend/app/globals.css && grep -F "oklch(0.985 0.008 60)" frontend/app/globals.css && grep -F "oklch(0.70 0.13 35)" frontend/app/globals.css && grep -F "oklch(0.945 0.012 50)" frontend/app/globals.css && ! grep -F "0.246 16.5" frontend/app/globals.css && ! grep -F "0.19 16.5" frontend/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/globals.css` contains `oklch(0.595 0.135 35)` (light primary terracotta)
    - `frontend/app/globals.css` contains `oklch(0.70 0.13 35)` (dark primary terracotta)
    - `frontend/app/globals.css` contains `oklch(0.22 0.018 50)` (light foreground ink)
    - `frontend/app/globals.css` contains `oklch(0.945 0.012 50)` (light secondary warm-taupe)
    - `frontend/app/globals.css` contains `oklch(0.97 0.022 35)` (light surface-rose-50 terracotta wash)
    - `frontend/app/globals.css` does NOT contain `0.246 16.5` or `0.19 16.5` (rose hue purged)
    - `frontend/app/globals.css` does NOT contain `oklch(0.18 0.01 60)` (old foreground purged)
    - All token names from §interfaces above remain present in the file (preserved)
  </acceptance_criteria>
  <done>:root and .dark blocks rewritten with terracotta + warm-taupe + warm-cream + ink palette per UI-SPEC §Color verbatim; all v0.1 token names preserved; rose hue (16.5) and old slate-leaning values fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Replace cool shadows with two-layer warm-brown shadows + add motion CSS tokens (DESIGN-05, DESIGN-06)</name>
  <files>frontend/app/globals.css</files>
  <read_first>
    - .planning/phases/05-design-system-foundation/05-UI-SPEC.md (§Shadows lines 294-335 + §Motion lines 408-490)
    - frontend/app/globals.css (current @theme inline shadow block lines 74-84)
  </read_first>
  <action>
Inside the existing `@theme inline { ... }` block in `frontend/app/globals.css`, replace the three `--shadow-*` declarations (currently lines 79-83) with the warm two-layer shadows per UI-SPEC §Shadows:

```
  --shadow-card:
    0 1px 2px 0 rgba(74, 56, 40, 0.06),
    0 2px 4px 0 rgba(74, 56, 40, 0.05);
  --shadow-card-hover:
    0 1px 2px 0 rgba(74, 56, 40, 0.08),
    0 4px 10px 0 rgba(74, 56, 40, 0.07);
  --shadow-nav:
    0 -1px 0 0 rgba(74, 56, 40, 0.08);
```

Within the same `@theme inline { ... }` block, append the three motion tokens per UI-SPEC §Motion "Tokens (in `globals.css` `@theme inline`)":

```
  --ease-craft: cubic-bezier(0.32, 0.72, 0.0, 1);
  --duration-fast: 150ms;
  --duration-normal: 280ms;
```

Then add a `.dark` shadow override block AFTER the closing `}` of the `.dark { ... }` declaration (i.e., a separate `.dark { --shadow-card: ...; --shadow-card-hover: ...; --shadow-nav: ...; }` block) per UI-SPEC §Shadows "Dark-mode shadows":

```
.dark {
  --shadow-card:
    0 1px 2px 0 rgba(0, 0, 0, 0.30),
    0 2px 4px 0 rgba(0, 0, 0, 0.22);
  --shadow-card-hover:
    0 1px 2px 0 rgba(0, 0, 0, 0.36),
    0 4px 10px 0 rgba(0, 0, 0, 0.28);
  --shadow-nav:
    0 -1px 0 0 rgba(255, 255, 255, 0.06);
}
```

Note: This second `.dark { ... }` block is intentionally separate from the OKLCH .dark block (Task 1) so the shadow-color overrides are visually grouped with the shadow tokens, mirroring the UI-SPEC organization. Tailwind v4 / browsers concatenate same-selector rules, so this works.

Do NOT remove or rename any existing `--shadow-*` token names. Do NOT touch the `prefers-reduced-motion` block at the bottom of the file (lines 254-261) — it stays as-is per UI-SPEC §Motion "Reduced-motion contract".
  </action>
  <verify>
    <automated>grep -F "rgba(74, 56, 40, 0.06)" frontend/app/globals.css && grep -F "rgba(74, 56, 40, 0.07)" frontend/app/globals.css && grep -F "cubic-bezier(0.32, 0.72, 0.0, 1)" frontend/app/globals.css && grep -F "--duration-fast: 150ms" frontend/app/globals.css && grep -F "--duration-normal: 280ms" frontend/app/globals.css && grep -F "rgba(0, 0, 0, 0.30)" frontend/app/globals.css && ! grep -F "rgba(15, 15, 20" frontend/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/globals.css` contains `rgba(74, 56, 40, 0.06)` (light shadow contact layer)
    - `frontend/app/globals.css` contains `rgba(74, 56, 40, 0.07)` (light shadow hover ambient)
    - `frontend/app/globals.css` contains `cubic-bezier(0.32, 0.72, 0.0, 1)` (ease-craft curve)
    - `frontend/app/globals.css` contains `--duration-fast: 150ms`
    - `frontend/app/globals.css` contains `--duration-normal: 280ms`
    - `frontend/app/globals.css` contains `rgba(0, 0, 0, 0.30)` (dark shadow override)
    - `frontend/app/globals.css` does NOT contain `rgba(15, 15, 20` (cool shadow purged)
    - The `@media (prefers-reduced-motion: reduce)` rule remains in the file unchanged
  </acceptance_criteria>
  <done>Warm two-layer shadows replace cool shadows in light + dark; ease-craft + duration-fast + duration-normal motion tokens appear in @theme inline; reduced-motion rule preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add `.paper-grain` utility class wired to /textures/paper-grain.svg (DESIGN-04, DESIGN-08)</name>
  <files>frontend/app/globals.css</files>
  <read_first>
    - .planning/phases/05-design-system-foundation/05-UI-SPEC.md (§Paper-Grain Texture lines 338-405, exact CSS contract)
    - frontend/app/globals.css (current @layer utilities block lines 216-249)
  </read_first>
  <action>
Inside the existing `@layer utilities { ... }` block in `frontend/app/globals.css` (currently containing `.text-display`, `.text-title`, `.text-body`, `.text-caption`), append the `.paper-grain` utility per UI-SPEC §Paper-Grain "CSS application":

```
  .paper-grain {
    position: relative;
  }
  .paper-grain::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image: url('/textures/paper-grain.svg');
    background-repeat: repeat;
    background-size: 240px 240px;
    opacity: 0.06;
    mix-blend-mode: multiply;
    pointer-events: none;
    border-radius: inherit;
    z-index: 0;
  }
  .paper-grain > * {
    position: relative;
    z-index: 1;
  }
```

Then add a top-level `.dark .paper-grain::before { ... }` rule AFTER the `@layer utilities` closing brace (this rule must be outside `@layer` so it has the same cascade weight as `.dark` overrides elsewhere):

```
.dark .paper-grain::before {
  opacity: 0.10;
  mix-blend-mode: overlay;
}
```

Important: do NOT modify the existing `.text-display`, `.text-title`, `.text-body`, `.text-caption` utilities in this plan — Plan 03 owns the type-scale rewrite. This task adds the `.paper-grain` utility only.

The SVG asset itself is created in Plan 02 (Wave 1, parallel) at `frontend/public/textures/paper-grain.svg`. Once both plans ship, the utility resolves to a real image.
  </action>
  <verify>
    <automated>grep -F ".paper-grain" frontend/app/globals.css && grep -F "url('/textures/paper-grain.svg')" frontend/app/globals.css && grep -F "background-size: 240px 240px" frontend/app/globals.css && grep -F "mix-blend-mode: multiply" frontend/app/globals.css && grep -F ".dark .paper-grain::before" frontend/app/globals.css && grep -F "mix-blend-mode: overlay" frontend/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/globals.css` contains `.paper-grain {` selector inside `@layer utilities`
    - `frontend/app/globals.css` contains `background-image: url('/textures/paper-grain.svg')`
    - `frontend/app/globals.css` contains `background-size: 240px 240px`
    - `frontend/app/globals.css` contains `mix-blend-mode: multiply` (light)
    - `frontend/app/globals.css` contains `border-radius: inherit` (so utility respects parent radius)
    - `frontend/app/globals.css` contains `.dark .paper-grain::before` selector
    - `frontend/app/globals.css` contains `mix-blend-mode: overlay` (dark)
    - The pre-existing `.text-display`, `.text-title`, `.text-body`, `.text-caption` utilities are still present (untouched by this task)
  </acceptance_criteria>
  <done>`.paper-grain` utility renders a multiplied warm-noise overlay via ::before pseudo with respect to parent radius; dark-mode override flips blend mode to overlay; SVG URL points to /textures/paper-grain.svg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Phase 5 plans modify only frontend CSS / typography / texture assets. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Information disclosure) | globals.css | accept | Pure design tokens; no secrets, no PII, no user-controlled input. |
| T-05-02 | T (Tampering) | globals.css | accept | Static asset bundled with frontend deploy; integrity protected by Vercel deploy pipeline (no runtime mutation). |
| T-05-03 | D (DoS) | --paper-grain SVG asset | accept | Single ~1KB SVG repeated as background-image; no network calls; no CPU regression vs prior shadows. |
</threat_model>

<verification>
After all 3 tasks land:
- `grep -F "0.246 16.5" frontend/app/globals.css` returns empty (rose hue gone)
- `grep -F "rgba(15, 15, 20" frontend/app/globals.css` returns empty (cool shadows gone)
- `grep -F "oklch(0.595 0.135 35)" frontend/app/globals.css` returns 1 hit (terracotta primary present)
- `grep -F "--duration-normal: 280ms" frontend/app/globals.css` returns 1 hit
- `grep -F ".paper-grain::before" frontend/app/globals.css` returns ≥1 hit (light + dark = 2 expected)
- `cd frontend && npm run build` succeeds (no Tailwind v4 compile error)
- The 15 primitives in `frontend/components/ui/*` still type-check and Phase 5 Plan 05 can later add the `paper-grain` className without a missing-utility error.
</verification>

<success_criteria>
- All v0.1 token names from `<interfaces>` block remain in `frontend/app/globals.css` (DESIGN-03 traceability)
- Light + dark `.dark` blocks contain the exact OKLCH values from UI-SPEC §Color tables
- `--shadow-card`, `--shadow-card-hover`, `--shadow-nav` use `rgba(74, 56, 40, *)` warm-brown components in light, near-black in dark (DESIGN-05)
- `--ease-craft`, `--duration-fast`, `--duration-normal` exist as `@theme inline` keys consumable as Tailwind utilities (DESIGN-06 CSS half)
- `.paper-grain` utility exists in `@layer utilities` referencing `/textures/paper-grain.svg` (DESIGN-04 CSS half)
- `prefers-reduced-motion: reduce` block at the bottom of `globals.css` is preserved unchanged
- All design tokens for color + shadow + motion live in `globals.css` (DESIGN-08 partial — Plan 02 + Plan 03 finish DESIGN-08)
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-foundation/05-01-SUMMARY.md` documenting:
- Exact OKLCH values landed (light + dark)
- Shadow value verification (warm-brown components)
- Motion token names exposed
- Paper-grain utility selector path
- Any deviation from UI-SPEC and rationale
</output>
