---
phase: 05-design-system-foundation
plan: 06
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05]
files_modified:
  - frontend/app/styleguide/page.tsx
autonomous: false
requirements: [DESIGN-08]
must_haves:
  truths:
    - "Browsing to `/styleguide` in `npm run dev` renders every primitive variant (Button, Input, Textarea, Card, Dialog, Sheet, Select, AlertDialog, Skeleton, Sonner trigger, Badge, Tabs, Separator, ScrollArea, Label) without console errors"
    - "The Color section shows the 60/30/10 split swatches (background cream + secondary warm-taupe + primary terracotta + destructive + valide-tint + surface-rose-100) labeled with hex + OKLCH"
    - "The Typography section renders `.text-display`, `.text-title`, `.text-body`, `.text-caption` with French diacritic-heavy sample copy verbatim from UI-SPEC §Copywriting Contract (« Al Dente. À la maison. », « Tagliatelles aux cèpes », « On laisse mijoter à feu doux pendant trois quarts d'heure. C'est la patience qui fait le goût — pas l'effort. », « Cuit le 7 mai », « Catégorie »)"
    - "The Shadows section shows three cards with shadow-card / shadow-card-hover (interactive hover) / shadow-nav (rendered as labeled hairline)"
    - "The Motion section renders the press-feedback button (scale 1 → 0.98 over duration-fast) and the slide-up demo (over duration-normal) — both using ease-craft"
    - "The Texture section shows three cards at small/medium/large sizes with paper-grain visibly applied + a counter-example surface labeled `NOT applied here` (a Button surface and the body bg)"
    - "The route ships behind a `// TODO(milestone-close): remove after v0.2 ships` comment; the file is gated so it does NOT render in production builds (process.env.NODE_ENV check OR notFound() in production)"
    - "Internal `/gsd-ui-review` on /styleguide scores ≥ 22/24 across the 6 pillars"
  artifacts:
    - path: "frontend/app/styleguide/page.tsx"
      provides: "Temporary acceptance gate route demonstrating every Phase 5 token + primitive in one place; production-gated"
      contains: "TODO(milestone-close)"
  key_links:
    - from: "styleguide/page.tsx"
      to: "All 15 components/ui/* primitives + globals.css tokens + paper-grain SVG + lib/motion.ts"
      via: "imports + JSX + className utilities"
      pattern: "(import .* from \"@/components/ui)"
    - from: "styleguide/page.tsx production gate"
      to: "process.env.NODE_ENV === 'development' OR notFound()"
      via: "runtime check at top of page"
      pattern: "process\\.env\\.NODE_ENV"
---

<objective>
Create `frontend/app/styleguide/page.tsx` as the Phase 5 acceptance gate — a temporary dev-only route showing every Phase 5 token + primitive in one place, with French diacritic-heavy sample copy verbatim from UI-SPEC §Copywriting Contract. The route is the single place to confirm the foundation is wired correctly before Phases 6-9 consume it. Gate the route so it does NOT render in production builds (Phase 5 ships only the foundation; the styleguide is internal QA, not user-facing). Marker comment documents v0.2 milestone-close removal.

Purpose: Without the styleguide, "did the tokens propagate correctly?" requires walking through each app screen one by one. With it, a single route smoke-tests the entire Phase 5 surface — and gives `/gsd-ui-review` a focused page to score against the 22/24 acceptance threshold.
Output: One new file at `frontend/app/styleguide/page.tsx` that renders the 10-section layout from UI-SPEC §`/styleguide` Route Layout, gated behind a NODE_ENV check, marked for milestone-close removal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@.planning/phases/05-design-system-foundation/05-01-PLAN.md
@.planning/phases/05-design-system-foundation/05-03-PLAN.md
@.planning/phases/05-design-system-foundation/05-04-PLAN.md
@.planning/phases/05-design-system-foundation/05-05-PLAN.md
@frontend/AGENTS.md

<interfaces>
<!-- All Phase 5 outputs the styleguide consumes: -->

From frontend/components/ui/* (after Plan 05):
- Button, Input, Textarea, Card (CardHeader/CardTitle/CardContent/CardDescription/CardFooter), Dialog (DialogTrigger/DialogContent/DialogHeader/DialogTitle/DialogDescription/DialogFooter), Sheet, Select (SelectTrigger/SelectContent/SelectItem/SelectValue), AlertDialog, Skeleton, Badge, Tabs (TabsList/TabsTrigger/TabsContent), Separator, ScrollArea, Label
- Toaster (mounted at root; trigger via `import { toast } from "sonner"`)

From frontend/app/globals.css (after Plans 01, 03):
- Tailwind utility classes: `bg-background`, `bg-card`, `bg-secondary`, `bg-muted`, `bg-primary`, `bg-destructive`, `bg-surface-rose-100`, `text-foreground`, `text-foreground-muted`, `text-primary`, `text-display`, `text-title`, `text-body`, `text-caption`, `paper-grain`, `shadow-card`, `shadow-card-hover`, `shadow-nav`, `duration-fast`, `duration-normal`, `ease-craft`, `font-display`, `font-body`

From frontend/lib/motion.ts (after Plan 04):
- `transitions.fast`, `transitions.normal`, `variants.pressFeedback`, `variants.slideUp`

<!-- Next.js 16 App Router patterns (heed AGENTS.md - this version may have breaking changes;
     consult frontend/node_modules/next/dist/docs/ if any API question arises): -->
- `app/<segment>/page.tsx` exports a default React component
- Client interactivity (button onClick, useState) requires `"use client"` directive at top of file
- `notFound()` from "next/navigation" returns 404
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the styleguide route with full UI-SPEC layout, French diacritic copy, and production gate</name>
  <files>frontend/app/styleguide/page.tsx</files>
  <read_first>
    - .planning/phases/05-design-system-foundation/05-UI-SPEC.md (§`/styleguide` Route Layout lines 536-618 — exact 10-section structure + success criteria; §Copywriting Contract lines 622-636 — verbatim French diacritic samples)
    - frontend/AGENTS.md (Next.js 16 breaking-change warning — consult `frontend/node_modules/next/dist/docs/` if any next API question arises during this task)
    - frontend/app/layout.tsx (current layout shell — confirm Toaster is mounted so styleguide can fire toasts)
    - frontend/components/ui/button.tsx (confirm Button variants: default, outline, secondary, ghost, destructive, link)
    - frontend/components/ui/badge.tsx (confirm Badge variants: default, secondary, destructive, outline, ghost, link)
    - frontend/components/ui/tabs.tsx (confirm Tabs variants: default, line)
  </read_first>
  <action>
First verify `frontend/app/styleguide/` does not already exist. If it does, halt and report — Phase 5 ships a fresh route.

Otherwise create `frontend/app/styleguide/page.tsx` with the following structure. The file MUST start with the milestone-close marker and the production gate.

Structure (top-down):

1. `"use client";` directive at the top — the page uses interactive demos (Dialog/Sheet triggers, motion buttons, dark-mode preview toggle).

2. Marker comment block per UI-SPEC §`/styleguide` Route Layout "Marker":
```
// TODO(milestone-close): remove after v0.2 ships.
// This route exists only as the Phase 5 acceptance gate. The v0.2 milestone
// audit (post-Phase 9) closes the v0.2 milestone by deleting this directory.
// Strings on this page intentionally bypass next-intl — see UI-SPEC §Copywriting Contract.
```

3. Production gate per UI-SPEC §CONTEXT.md "Acceptance Gate" + Claude's Discretion: gate via NODE_ENV. After the imports, before the default export, add a `notFound` early-exit when `process.env.NODE_ENV === "production"`. Use `import { notFound } from "next/navigation"` and call `notFound()` in the page component body, NOT at module load (server components evaluate module load at build).
```tsx
import { notFound } from "next/navigation";
// ...
export default function StyleguidePage() {
  if (process.env.NODE_ENV === "production") {
    notFound();
  }
  return ( /* content */ );
}
```

4. Imports — every primitive used:
```tsx
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Label } from "@/components/ui/label";
import { Badge } from "@/components/ui/badge";
import { Skeleton } from "@/components/ui/skeleton";
import { Separator } from "@/components/ui/separator";
import { ScrollArea } from "@/components/ui/scroll-area";
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from "@/components/ui/select";
import { Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription } from "@/components/ui/dialog";
import { Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle, SheetDescription } from "@/components/ui/sheet";
import { AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogAction, AlertDialogCancel } from "@/components/ui/alert-dialog";
import { motion } from "framer-motion";
import { variants, transitions } from "@/lib/motion";
import { toast } from "sonner";
import { notFound } from "next/navigation";
import { useState } from "react";
```

5. Page layout — the outer wrapper from UI-SPEC §`/styleguide` Route Layout "Layout (top-down)":
```tsx
<main className="max-w-2xl mx-auto px-6 pt-12 pb-24 flex flex-col gap-12">
```

6. Ten sections per UI-SPEC layout. Each section is wrapped in `<section className="flex flex-col gap-6">` with an `<h2 className="text-title">{heading}</h2>` followed by the demo content. Use the EXACT headings from UI-SPEC:

  (a) **"Tokens / Color"** — Render six swatches as a grid of 96×96 paper-grain cards. Each swatch: a colored block with `bg-{token}` over which a label shows the token name + hex + OKLCH. The six tokens to display:
  - `--background` cream `oklch(0.985 0.008 60)` `#FBF9F4`
  - `--secondary` warm-taupe `oklch(0.945 0.012 50)` `#F0E9E0`
  - `--primary` terracotta `oklch(0.595 0.135 35)` `#C45A3F`
  - `--destructive` `oklch(0.55 0.20 25)` `#B23A1F`
  - `--valide-tint` emerald `oklch(0.93 0.07 145)`
  - `--surface-rose-100` faint terracotta wash `oklch(0.94 0.045 35)` `#F1D2BD`

  Below the grid, render a second row labeled "Dark mode preview" wrapping the same six swatches in a `<div className="dark">` so the .dark class scopes the OKLCH .dark values for that subtree only. Per UI-SPEC §`/styleguide` Route Layout success criterion 4: "Light + dark previews of the color section side-by-side."

  (b) **"Tokens / Typography"** — Render the four type-scale utilities, each with the verbatim French sample copy from UI-SPEC §Copywriting Contract "Styleguide-only sample copy":
  ```tsx
  <p className="text-display">« Al Dente. À la maison. »</p>
  <p className="text-title">« Tagliatelles aux cèpes »</p>
  <p className="text-body">« On laisse mijoter à feu doux pendant trois quarts d'heure. C'est la patience qui fait le goût — pas l'effort. »</p>
  <p className="text-caption">« Cuit le 7 mai »</p>
  <Label>Catégorie</Label>
  ```
  These five strings are HARDCODED French (not next-intl) per UI-SPEC: "This is the ONLY place in the codebase exempted from the next-intl invariant, and the exemption is bounded by the v0.2 milestone close."

  (c) **"Tokens / Shadows"** — Three Cards in a vertical stack:
  - First Card with `className="shadow-card"` labeled "shadow-card (resting)"
  - Second Card with `className="shadow-card hover:shadow-card-hover transition-shadow duration-fast ease-craft"` labeled "shadow-card-hover (hover me)"
  - A `<div className="shadow-nav h-1">` rendered as the hairline labeled "shadow-nav (hairline above bottom nav)"

  (d) **"Tokens / Motion"** — Two interactive demos using `frontend/lib/motion.ts`:
  - A press-feedback Button: wrap a Button in `motion.div` with `variants={variants.pressFeedback}` initial="rest" whileTap="pressed". Label: "Tap-press feedback (scale 1 → 0.98 over duration-fast)".
  - A slide-up demo: `useState` for visibility; trigger Button toggles a `motion.div` using `variants={variants.slideUp}` initial="hidden" animate={visible ? "visible" : "hidden"}. Label: "Slide up (duration-normal)".
  - Below the two demos, a paragraph: "Reduced motion: enable the OS-level `Reduce Motion` setting and reload — all transitions clamp to 0ms via `globals.css` `@media (prefers-reduced-motion: reduce)`."

  (e) **"Texture / Paper-grain"** — Three Cards side-by-side at three different sizes:
  - Small: `className="paper-grain w-32 h-20 rounded-xl bg-card border border-border p-3 text-caption"` content "small"
  - Medium: `w-48 h-32` content "medium"
  - Large: `w-full h-40` content "large"
  - Below: a Button styled with `className="paper-grain"` (counter-example) labeled "NOT applied here (button surface — paper-grain reserved for cards only)" — but per UI-SPEC primitives, Button does NOT have paper-grain at the primitive level, so wrapping a Button in `paper-grain` here is the visual demonstration of "what NOT to do, gated to documentation purposes." Add a comment in the JSX: `{/* Anti-pattern demo: paper-grain on a button surface — UI-SPEC §Paper-Grain "NOT applied to" */}`
  - Final: an explicit "body bg" call-out — render a `<div className="bg-background p-6">Body background — paper-grain NOT applied here</div>`

  (f) **"Primitives / Buttons"** — Render Button in every variant × size combination:
  - Variants: `default`, `outline`, `secondary`, `ghost`, `destructive`, `link`
  - Sizes: `default`, `xs`, `sm`, `lg`
  - Plus icon variants: `icon`, `icon-xs`, `icon-sm`, `icon-lg` (use the lucide ChevronRight icon)
  - Plus disabled and aria-invalid demos for `default`

  (g) **"Primitives / Form controls"** — Input (default, focused via `autoFocus`, disabled, aria-invalid), Textarea (default, disabled, aria-invalid), Label paired with an Input, Select with three SelectItems (`Hiver`, `Printemps`, `Été` — French season names with diacritic on `Été`).

  (h) **"Primitives / Surfaces"** — Card (default size + sm size), Dialog (button to open with French content), Sheet (four buttons opening from each side: top/right/bottom/left). Each Dialog and Sheet uses sample French copy: title `« Confirmer »`, description `Cette action est définitive.`

  (i) **"Primitives / Feedback"** — Skeleton (3 sizes — line `<Skeleton className="h-4 w-32" />`, block `<Skeleton className="h-20 w-full" />`, avatar `<Skeleton className="h-10 w-10 rounded-full" />`), five Sonner toast trigger Buttons firing `toast()`, `toast.success()`, `toast.info()`, `toast.warning()`, `toast.error()` with French copy (`Sauvegardé`, `Information`, `Attention`, `Erreur`), Badge variants (default, secondary, destructive, outline, ghost, link).

  (j) **"Primitives / Navigation + structure"** — Tabs (variant `default` + variant `line` with French labels `Recettes`, `Notes`, `Photos`), Separator (horizontal + vertical with content on either side), ScrollArea (200px tall with overflowing French recipe ingredient list of 20+ items), AlertDialog trigger button — the AlertDialog uses destructive copy `Supprimer cet élément ?` and `Cette action est définitive.` per UI-SPEC §Copywriting Contract "Empty / error / destructive states".

7. The `<main>` element should also document the dark-mode toggle. Per UI-SPEC §`/styleguide` Route Layout success criterion 4 "Light + dark previews of the color section side-by-side": include a small `<button>` at the top of the page that toggles `document.documentElement.classList.toggle("dark")` so the entire page can be visually verified in both modes.

Hard rules:
- Strings in `text-display`, `text-title`, `text-body`, `text-caption`, `Label`, Dialog/Sheet/AlertDialog content MUST match UI-SPEC §Copywriting Contract verbatim (`« Al Dente. À la maison. »`, `« Tagliatelles aux cèpes »`, etc.). These are the diacritic stress test — copy/paste from UI-SPEC, do NOT paraphrase.
- The route MUST be gated by `process.env.NODE_ENV === "production" → notFound()` so a stray prod build never exposes it.
- The marker comment `TODO(milestone-close)` MUST be at the top of the file so a `grep TODO(milestone-close)` audit at v0.2 close finds the file for removal.
- Do NOT translate the French strings through next-intl — UI-SPEC §Copywriting Contract explicitly exempts the styleguide.
- Do NOT add CSS to globals.css. All styling lives in className utilities consumed from Plan 01.
- If any Next.js 16 App Router API used here behaves differently than expected, consult `frontend/node_modules/next/dist/docs/` per AGENTS.md.
  </action>
  <verify>
    <automated>test -f frontend/app/styleguide/page.tsx && grep -F 'TODO(milestone-close)' frontend/app/styleguide/page.tsx && grep -F 'process.env.NODE_ENV' frontend/app/styleguide/page.tsx && grep -F 'notFound()' frontend/app/styleguide/page.tsx && grep -F '« Al Dente. À la maison. »' frontend/app/styleguide/page.tsx && grep -F '« Tagliatelles aux cèpes »' frontend/app/styleguide/page.tsx && grep -F 'mijoter à feu doux' frontend/app/styleguide/page.tsx && grep -F 'Cuit le 7 mai' frontend/app/styleguide/page.tsx && grep -F 'Catégorie' frontend/app/styleguide/page.tsx && grep -F 'text-display' frontend/app/styleguide/page.tsx && grep -F 'text-title' frontend/app/styleguide/page.tsx && grep -F 'text-body' frontend/app/styleguide/page.tsx && grep -F 'text-caption' frontend/app/styleguide/page.tsx && grep -F 'shadow-card-hover' frontend/app/styleguide/page.tsx && grep -F 'shadow-nav' frontend/app/styleguide/page.tsx && grep -F 'paper-grain' frontend/app/styleguide/page.tsx && grep -F 'variants.pressFeedback' frontend/app/styleguide/page.tsx && grep -F 'variants.slideUp' frontend/app/styleguide/page.tsx && grep -F 'AlertDialog' frontend/app/styleguide/page.tsx && grep -F '"use client"' frontend/app/styleguide/page.tsx && cd frontend && npx tsc --noEmit 2>&1 | (! grep -i "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/styleguide/page.tsx` exists
    - File starts with `"use client"` directive
    - File contains `// TODO(milestone-close)` marker comment
    - File contains `process.env.NODE_ENV === "production"` gate paired with `notFound()` call from `next/navigation`
    - File imports all 15 primitive families from `@/components/ui/*`
    - File imports `variants` and `transitions` from `@/lib/motion`
    - File imports `toast` from `sonner` and `motion` from `framer-motion`
    - File contains the verbatim diacritic strings: `« Al Dente. À la maison. »`, `« Tagliatelles aux cèpes »`, `mijoter à feu doux`, `Cuit le 7 mai`, `Catégorie`
    - File renders the four type-scale utilities (`text-display`, `text-title`, `text-body`, `text-caption`) at least once each
    - File renders `shadow-card`, `shadow-card-hover`, `shadow-nav` at least once each
    - File contains `paper-grain` className applications (≥3 — small/medium/large texture demo)
    - File references `variants.pressFeedback` and `variants.slideUp` from motion.ts
    - File renders an `AlertDialog` with the destructive copy `Supprimer cet élément ?` / `Cette action est définitive.`
    - `npx tsc --noEmit` succeeds with the new file in the project graph (no TS errors)
    - `npm run lint` passes for the new file
  </acceptance_criteria>
  <done>frontend/app/styleguide/page.tsx exists, gated for production, marked for milestone-close removal, renders the 10-section UI-SPEC layout with verbatim French diacritic copy, type-checks cleanly, and is ready for visual smoke-test in dev.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual smoke-test the styleguide route in dev</name>
  <files>frontend/app/styleguide/page.tsx (read-only — page rendered in browser, not modified)</files>
  <action>Run `cd frontend && npm run dev`, browse to http://localhost:3000/styleguide, walk the 10 sections per the how-to-verify checklist below, toggle dark mode, run real-device iPhone Safari diacritic test. Capture results in 05-06-SUMMARY.md. If any visual defect surfaces, do NOT modify Plan 06's page.tsx — instead patch the upstream plan (token in 01, font in 03, primitive in 05).</action>
  <verify>
    <automated>cd frontend && npm run dev &amp; DEV_PID=$!; sleep 8; curl -sf http://localhost:3000/styleguide -o /dev/null &amp;&amp; echo "styleguide route responds"; kill $DEV_PID 2&gt;/dev/null</automated>
  </verify>
  <done>User has typed "approved" after visually verifying all 10 sections render per UI-SPEC, no console errors, French diacritics render cleanly on iOS Safari, /gsd-ui-review on /styleguide scores ≥ 22/24.</done>
  <what-built>
A temporary `/styleguide` route at `frontend/app/styleguide/page.tsx` rendering every Phase 5 token + primitive in one place, gated for production, marked for v0.2 milestone-close removal. The page is the manual visual acceptance gate for Phase 5: confirms tokens propagated, fonts loaded, paper-grain visible on cards, warm shadows visible, motion responds at 150ms / 280ms.
  </what-built>
  <how-to-verify>
1. From the repo root, run `cd frontend && npm run dev`. Wait for the dev server to log `ready on http://localhost:3000`.
2. In a browser (preferably the user's iPhone Safari for the diacritic test, but desktop Safari/Chrome works for the first pass), open `http://localhost:3000/styleguide`.
3. Walk through the 10 sections top-to-bottom and verify each:
   - **Tokens / Color**: The 6 light-mode swatches read as cream / warm-taupe / terracotta / red-brown / emerald-tint / faint-terracotta. The dark-mode preview row shows their inverted counterparts. NO rose `#F43F5E`. NO slate.
   - **Tokens / Typography**: `« Al Dente. À la maison. »` renders in Fraunces italic at ~32-44px (clamp). The capital `À`, lowercase `é`, guillemets `«»` render WITHOUT glyph substitution (no fallback face flicker, no broken accent positioning). `Tagliatelles aux cèpes` renders in upright Fraunces ~24px. The body paragraph renders in IBM Plex Sans ~16px and the `î`, `'` apostrophe, `—` em-dash render correctly.
   - **Tokens / Shadows**: The first card has a barely-perceptible warm shadow (paper-on-wood). The second card visibly lifts on hover (`shadow-card-hover` is stronger). The hairline strip at the bottom is the `shadow-nav` line. NO floating cool drop-shadow.
   - **Tokens / Motion**: Tap the press-feedback button — it scales briefly (~150ms ease-craft) on press. Toggle the slide-up demo — the card slides up over ~280ms with the craft curve (slightly different feel than default ease).
   - **Texture / Paper-grain**: All three cards (small, medium, large) show subtle warm noise. The "NOT applied here" Button surface (counter-example) shows what's wrong if grain is over-applied. The body bg div shows clean cream — no grain.
   - **Primitives / Buttons**: All variant × size combos render. The `default` variant button is terracotta. `destructive` is the quieted brick red. `link` underlines on hover.
   - **Primitives / Form controls**: Input, Textarea, Label, Select all read warm + warm-bordered. Click the Select; SelectContent opens with paper-grain visible on the popover surface.
   - **Primitives / Surfaces**: Click "Open Dialog" — the modal opens with paper-grain on the content surface, warm overlay (not black/10), warm shadow. Click each of the four "Open Sheet" buttons — sheets slide in with paper-grain content.
   - **Primitives / Feedback**: Skeletons pulse warm-taupe (kraft paper feel, not gray). Click each toast trigger — toasts appear with the new color tokens. Badge variants render token-driven.
   - **Primitives / Navigation + structure**: Tabs default + line variant render with terracotta active indicator. ScrollArea scrolls cleanly. AlertDialog opens with `Supprimer cet élément ?` / `Cette action est définitive.` and the paper-grain content surface.
4. Toggle the dark-mode button at the top. Re-walk every section. Verify dark-mode shadows use higher alpha (visible on dark surfaces); paper-grain blends via `overlay` instead of `multiply`.
5. Open browser devtools → Console. There must be ZERO red errors. Yellow warnings about React hydration or unused props are acceptable.
6. Open devtools → Computed. On the `text-display` element, confirm `font-family` resolves to a Fraunces-family stack. On a `text-body` element, confirm `font-family` resolves to IBM Plex Sans.
7. (Optional but strongly recommended) Open the page on the user's iPhone via the local network dev URL to verify French diacritics render cleanly on iOS Safari at PWA-compressed sizes — UI-SPEC §Typography "Backup pairing trigger" requires this real-device verification before Fraunces is accepted as the Phase 5 lock-in.
8. (Optional) Run internal `/gsd-ui-review` on `/styleguide` and capture the score. UI-SPEC §`/styleguide` Route Layout success criterion 6 requires ≥ 22/24.
  </how-to-verify>
  <resume-signal>
Type "approved" if all checks pass. Type "fail: <description>" with specifics if any of the 7 verification steps surfaced a defect — describe what didn't render correctly so the executor can patch (most likely fix is a Plan 01/03/05 token or className correction; this Plan 06 page should not need re-edits unless the section structure itself failed).

If only the iPhone diacritic test (step 7) failed: invoke the UI-SPEC §Typography "Backup pairing" path — swap Fraunces → Instrument Serif and IBM Plex Sans → DM Sans in `frontend/app/layout.tsx` only, document the swap in 05-03-SUMMARY.md and 05-06-SUMMARY.md, and re-run this checkpoint. Do NOT modify any other plans.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → /styleguide route | Dev-only route gated by `process.env.NODE_ENV` check + `notFound()` in production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | I (Information disclosure — internal route exposed in prod) | /styleguide | mitigate | `process.env.NODE_ENV === "production" → notFound()` runtime gate at the top of the page component. NODE_ENV is set to "production" by Vercel automatically on prod deploys, so the page returns the standard Next.js 404 in prod. Belt-and-braces: the v0.2 milestone-close audit task (post-Phase 9) deletes the entire `frontend/app/styleguide/` directory — gated by the `// TODO(milestone-close)` grep that the audit runs. |
| T-05-14 | T (Tampering with hardcoded French strings bypassing next-intl) | /styleguide page strings | accept | UI-SPEC §Copywriting Contract explicitly exempts the styleguide route from the next-intl invariant. The exemption is bounded — when the directory is deleted at v0.2 close, the exemption disappears. The CLAUDE.md `// TODO(productize)` convention does NOT apply here because the strings are not productize-later debt; they're acceptance-test fixtures. |
| T-05-15 | E (Elevation via stray production bundle) | /styleguide route in production build | mitigate | Two-layer defense: (1) `notFound()` gate at runtime; (2) milestone-close task deletes the directory at v0.2 close. Even if a developer accidentally bypasses gate (1), gate (2) ensures the route does not survive the milestone. Additionally, `// TODO(milestone-close)` grep is part of the v0.2 close audit — any stray styleguide imports anywhere else in the codebase would be flagged. |
| T-05-16 | (n/a) | new route adding to bundle size | accept | Dev bundle only — the `notFound()` gate ensures prod build still emits the route file but it returns 404 immediately, so SSR cost is negligible. Tree-shaking removes the unused branches. |
</threat_model>

<verification>
After Task 1 lands and Task 2 (human checkpoint) passes:
- `test -f frontend/app/styleguide/page.tsx` succeeds
- `grep -F 'TODO(milestone-close)' frontend/app/styleguide/page.tsx` returns ≥1 hit
- `grep -F 'process.env.NODE_ENV' frontend/app/styleguide/page.tsx` returns ≥1 hit
- `cd frontend && npm run build` succeeds (production build emits the route as a 404-on-runtime stub)
- `cd frontend && npm run dev` + browse `http://localhost:3000/styleguide` renders the 10-section page without console errors
- Visual confirmation per Task 2 step list (terracotta + warm shadows + Fraunces + IBM Plex Sans + paper-grain on cards but not on Buttons or body)
- Dark-mode toggle verifiable (`document.documentElement.classList.toggle("dark")` button works)
- French diacritic rendering on iOS Safari at PWA-compressed sizes verified — UI-SPEC §Typography Fraunces vs Instrument Serif fallback decision finalized
</verification>

<success_criteria>
- DESIGN-08 closed (acceptance verification half): the styleguide proves all design tokens propagate from `globals.css` through every primitive without per-component hardcoded colors or shadows. The grep audit `grep -rn "oklch(\|rgba(\|box-shadow:" frontend/components/` returns 0 hits outside of `frontend/app/globals.css` (Phase 5 SUMMARY documents the audit result).
- Phase 5 ships: every primitive renders, French diacritics render cleanly on iOS Safari, paper-grain visible on every card-like surface but NOT on body bg / buttons / chrome, warm shadows present, motion at 150ms/280ms perceptibly under the ease-craft curve
- `/styleguide` is production-gated and marked for milestone-close removal — no leak into v0.2 user-facing surface
- All 8 DESIGN-* requirements (DESIGN-01 through DESIGN-08) closed across Plans 01-06; UI-REVIEW on `/styleguide` ≥ 22/24
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-foundation/05-06-SUMMARY.md` documenting:
- Path to the new route
- Production gate mechanism (NODE_ENV check + notFound) confirmed
- Visual smoke-test outcomes per Task 2's 7 steps (one line per step)
- iPhone Safari diacritic test outcome (PASS / backup-pairing-triggered) — if backup pairing triggered, link to the layout.tsx swap commit
- `/gsd-ui-review` score on /styleguide (target ≥ 22/24)
- DESIGN-08 cross-component audit result (grep `oklch(`, `rgba(`, `box-shadow:` in `frontend/components/` — must return 0 hits outside `frontend/app/globals.css`)
- Outstanding items for the v0.2 milestone-close audit (the route + the temporary `--font-heading` / `--font-sans` aliases in `globals.css` are the two cleanup items)
</output>
