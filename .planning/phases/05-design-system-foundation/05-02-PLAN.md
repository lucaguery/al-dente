---
phase: 05-design-system-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/textures/paper-grain.svg
autonomous: true
requirements: [DESIGN-04]
must_haves:
  truths:
    - "Browsing to /textures/paper-grain.svg in the deployed PWA returns a 240×240 warm-brown noise SVG"
    - "The asset is small enough (~1KB) to load with no perceptible latency on a card-render path"
    - "When Plan 01's `.paper-grain` utility lands, applying `paper-grain` to a card surface paints a subtle warm-noise overlay at 6% opacity (light) / 10% (dark)"
  artifacts:
    - path: "frontend/public/textures/paper-grain.svg"
      provides: "Paper-grain noise texture asset (single source of truth, used by .paper-grain CSS utility)"
      contains: "feTurbulence"
  key_links:
    - from: "frontend/public/textures/paper-grain.svg"
      to: ".paper-grain CSS utility (added by Plan 01)"
      via: "URL-relative reference (/textures/paper-grain.svg from public/)"
      pattern: "feTurbulence[^>]*baseFrequency=\"0\\.92\""
---

<objective>
Create the single SVG noise asset that anchors the Slow Food artisanal texture identity at `frontend/public/textures/paper-grain.svg`. The asset is a 240×240 fractal-noise SVG with a warm-brown color matrix at 55% alpha, deterministically seeded for reproducible builds, with `stitchTiles="stitch"` so it tiles seamlessly when repeated as `background-image`.

Purpose: Without this asset, Plan 01's `.paper-grain` utility class points at a 404. With it, every card surface in the app gets the "recipe card on a kitchen counter" warmth that the v0.2 design direction is built around.
Output: `frontend/public/textures/paper-grain.svg` (~1KB, deterministic, SEO-safe, no external references).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@.planning/notes/v0.2-design-direction.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the paper-grain SVG asset with exact UI-SPEC content (DESIGN-04)</name>
  <files>frontend/public/textures/paper-grain.svg</files>
  <read_first>
    - .planning/phases/05-design-system-foundation/05-UI-SPEC.md (§Paper-Grain Texture lines 338-405 — exact SVG content + reasoning per attribute)
  </read_first>
  <action>
First verify the destination directory exists. If `frontend/public/textures/` does not exist, create it.

Then write `frontend/public/textures/paper-grain.svg` with the EXACT content from UI-SPEC §Paper-Grain Texture "Exact SVG content for `frontend/public/textures/paper-grain.svg`":

```xml
<svg xmlns="http://www.w3.org/2000/svg" width="240" height="240" viewBox="0 0 240 240">
  <filter id="grain">
    <feTurbulence type="fractalNoise" baseFrequency="0.92" numOctaves="2" seed="7" stitchTiles="stitch"/>
    <feColorMatrix values="0 0 0 0 0.29
                           0 0 0 0 0.22
                           0 0 0 0 0.16
                           0 0 0 0.55 0"/>
  </filter>
  <rect width="240" height="240" filter="url(#grain)"/>
</svg>
```

Per UI-SPEC reasoning (do NOT alter):
- `width / height = 240` — square tile, large enough that repetition is invisible at recipe-card sizes (327×120) and finalize-card sizes (327×80).
- `baseFrequency="0.92"` — fine-grain, paper-fiber feel; degrades gracefully on retina.
- `numOctaves="2"` — single octave is too uniform; 3+ becomes "marble." Two gives organic variance.
- `seed="7"` — deterministic seed so the texture is reproducible across builds (no flicker between deploys).
- `stitchTiles="stitch"` — eliminates the seam visible at tile borders when repeated as `background-image`.
- `feColorMatrix` values — converts grayscale noise to a warm-brown tint at R=0.29, G=0.22, B=0.16, alpha 0.55 (matches the wood-shadow color family from Plan 01 — coherent palette).

Do NOT add `xmlns:xlink`, `<title>`, `<desc>`, or any external image references. The SVG must be self-contained, deterministic, and ~1KB after gzip.

Do NOT add CSS to globals.css in this plan — Plan 01 owns the `.paper-grain` utility wiring.
  </action>
  <verify>
    <automated>test -f frontend/public/textures/paper-grain.svg && grep -F 'baseFrequency="0.92"' frontend/public/textures/paper-grain.svg && grep -F 'numOctaves="2"' frontend/public/textures/paper-grain.svg && grep -F 'seed="7"' frontend/public/textures/paper-grain.svg && grep -F 'stitchTiles="stitch"' frontend/public/textures/paper-grain.svg && grep -F 'fractalNoise' frontend/public/textures/paper-grain.svg && grep -F '0.29' frontend/public/textures/paper-grain.svg && grep -F '0.55' frontend/public/textures/paper-grain.svg && [ $(wc -c < frontend/public/textures/paper-grain.svg) -lt 2048 ]</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/public/textures/paper-grain.svg` exists
    - File contains `baseFrequency="0.92"`
    - File contains `numOctaves="2"`
    - File contains `seed="7"`
    - File contains `stitchTiles="stitch"`
    - File contains `type="fractalNoise"`
    - File contains the warm-brown color matrix value `0.29` (red channel)
    - File contains the alpha multiplier `0.55`
    - File contains `<rect width="240" height="240"` (the fill rect)
    - File size is under 2KB (sanity bound — UI-SPEC says ~1KB target)
    - File contains no external image references (`<image href=`, `xlink:href` for raster) and no script tags
  </acceptance_criteria>
  <done>SVG asset exists at the exact path the `.paper-grain` utility (Plan 01) references, contains the UI-SPEC verbatim noise + color-matrix definition, tiles seamlessly via `stitchTiles="stitch"`, and is bundled into the Vercel deploy as a static asset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/ static asset → browser | Static SVG served by Vercel CDN. No user input crosses the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T (Tampering) | paper-grain.svg as background-image | accept | Asset is bundled at build time; no runtime fetch from user-supplied URL; Vercel deploy integrity covers tamper detection. No `<script>` allowed in SVG (verified in acceptance criteria). |
| T-05-05 | E (Elevation of privilege) | SVG as background-image (not inline) | accept | Used only as CSS `background-image: url(...)` — browsers do not execute scripts in SVGs loaded as images (per HTML spec). XXE / inline-script vectors do not apply. |
| T-05-06 | I (Information disclosure) | Static asset path | accept | `/textures/paper-grain.svg` reveals nothing sensitive; standard web-asset path. |
</threat_model>

<verification>
- `test -f frontend/public/textures/paper-grain.svg` succeeds
- File contains every UI-SPEC attribute (verified by Task 1's automated grep set)
- After both Plan 01 and Plan 02 land, manually loading `http://localhost:3000/textures/paper-grain.svg` in dev returns the SVG with `Content-Type: image/svg+xml`
- After Plan 05 applies the `paper-grain` className to Card primitives, recipe cards in the app render the noise overlay at the specified opacity
- File size sanity-bound under 2KB
</verification>

<success_criteria>
- DESIGN-04 (paper-grain texture anchor — asset half) closed: SVG exists at the canonical path
- The asset's noise frequency, octaves, seed, color matrix, and tiling are deterministic — running this plan twice produces an identical file
- No external dependencies, no scripts, no user input — pure static asset
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-foundation/05-02-SUMMARY.md` documenting:
- File path and size
- Visual confirmation method (manually open the SVG in a browser to verify it renders as warm-brown noise)
- Any deviation from UI-SPEC verbatim content (there should be none)
</output>
