---
phase: 04-polish-w4
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/ShortlistCard.tsx
  - frontend/components/HomeDecide.tsx
  - frontend/lib/votes.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ALBUM-01
  - ALBUM-02
  - ALBUM-03
user_setup: []

must_haves:
  truths:
    - "frontend/components/ShortlistCard.tsx no longer has the react-hooks/set-state-in-effect lint error at the prefers-reduced-motion useEffect (rewritten to useSyncExternalStore, mirroring PushPermissionBanner.tsx)"
    - "ShortlistCard renders identically (visual no-op): swipe deck still respects prefers-reduced-motion correctly"
    - "frontend/components/HomeDecide.tsx no longer has the unused eslint-disable directive at line 169 or the unused `_e` parameter at line 229"
    - "frontend/lib/votes.ts no longer has the unused eslint-disable directive at line 94"
    - "ROADMAP.md Phase 4 entry no longer lists ALBUM-01/02/03 as in-scope requirements; Phase 4 plan list reflects the actual 4 plans (01..04)"
    - "REQUIREMENTS.md Album section is moved under v2 Requirements with explicit cut-rationale; traceability table updates ALBUM-01/02/03 mapping to v2"
    - "npm run lint exits 0 (or with the SAME pre-existing warning count, no new ones) for all files in files_modified"
  artifacts:
    - path: "frontend/components/ShortlistCard.tsx"
      provides: "useSyncExternalStore-based prefers-reduced-motion subscription (no setState in effect)"
      contains: "useSyncExternalStore"
    - path: "frontend/components/HomeDecide.tsx"
      provides: "Cleaned-up event handler signatures and removed dead eslint-disable directives"
      contains: "function onCookingStarted("
    - path: "frontend/lib/votes.ts"
      provides: "No dead eslint-disable directives"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 entry reflects the album-cut decision and the 4-plan structure"
      contains: "Phase 4: Polish (W4)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ALBUM-01/02/03 reclassified as v2 with rationale"
      contains: "ALBUM"
  key_links:
    - from: "ShortlistCard prefers-reduced-motion subscription"
      to: "window.matchMedia('(prefers-reduced-motion: reduce)')"
      via: "useSyncExternalStore subscribe/getSnapshot pair"
      pattern: "useSyncExternalStore"
    - from: "ROADMAP.md Phase 4 plan list"
      to: "the four PLAN.md files"
      via: "explicit checkbox list"
      pattern: "04-01-PLAN\\.md|04-02-PLAN\\.md|04-03-PLAN\\.md|04-04-PLAN\\.md"
---

<objective>
Bookkeeping plan that ships independently of the cooking-log work:

1. **D-09: Phase-3 deferred lint cleanup.**
   - `frontend/components/ShortlistCard.tsx` — rewrite the `usePrefersReducedMotion` hook from `useState + useEffect` to `useSyncExternalStore`, mirroring `frontend/components/PushPermissionBanner.tsx`. This removes the `react-hooks/set-state-in-effect` error at line 50.
   - `frontend/components/HomeDecide.tsx` — remove the unused `eslint-disable-next-line no-console` at line 169 (the `console.warn` it guarded was already replaced) and remove the unused `_e: Event` parameter at line 229.
   - `frontend/lib/votes.ts` — remove the unused `eslint-disable-next-line no-console` at line 94.

2. **Album scope reconciliation.**
   The user's Phase 4 CONTEXT.md (commit `c7ee1f0` — `docs(04): cut Album from Phase 4 scope — deferred to productize-later`) cuts ALBUM-01/02/03 from the MVP. ROADMAP.md and REQUIREMENTS.md were not updated in that commit. This plan brings them in line:
   - **ROADMAP.md** — replace the Phase 4 `**Requirements**` line so ALBUM-01/02/03 is removed (Phase 4 = COOK-03, COOK-04, COOK-05 only). Update the Phase 4 plan list to enumerate the four 04-PLAN files. Update the Coverage Summary so Phase 4 has 3 REQ-IDs not 6, and ALBUM-01/02/03 are listed under "Cut from v1 (deferred to productize-later)" in a new sub-section. Update the per-phase mapping bullets accordingly.
   - **REQUIREMENTS.md** — move the `### Album (ALBUM)` block from the v1 section into the v2 section under a new heading `### Album (V2-ALBUM)` with the rename `V2-ALBUM-01/02/03` and a one-line rationale citing CONTEXT.md. Update the traceability table to mark ALBUM rows as `Phase: deferred (V2)`. Update the per-phase breakdown counts.

3. **No code dependency on Album.** The bookkeeping is documentation-only — no code is being deleted because no Album code was ever written. Confirm via `grep -r "ALBUM\|/album\|cooking_logs.*album" frontend backend` returning zero hits before this plan starts (checker-friendly precondition).

**Why this ships in Wave 1, parallel to Plan 01 backend:** None of the touched files (ShortlistCard, HomeDecide, votes.ts, ROADMAP.md, REQUIREMENTS.md) are touched by Plan 01 or Plan 02. Zero file overlap → safe to run in parallel. This unblocks reviewers who want to see the Phase-3 lint debt closed without waiting for the cooking-log work.

**Why ALBUM-01/02/03 are the `requirements` field of THIS plan:** the orchestrator listed those IDs as Phase 4 scope, and every roadmap requirement ID MUST appear in at least one plan. This plan owns the formal reclassification (v1 → v2) — the ID lives here as the place that closes its v1 obligation. After this plan ships, ROADMAP.md no longer associates ALBUM-* with Phase 4.

Output:
- 3 lint-cleaned frontend files (no behavior change)
- 2 updated planning docs reflecting the album cut
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-polish-w4/04-CONTEXT.md
@.planning/phases/03-decide-w3/deferred-items.md
@frontend/components/ShortlistCard.tsx
@frontend/components/PushPermissionBanner.tsx
@frontend/components/HomeDecide.tsx
@frontend/lib/votes.ts

<interfaces>
<!-- Reference implementation pattern for useSyncExternalStore — copy this shape. -->

From `frontend/components/PushPermissionBanner.tsx` (the reference pattern):
```typescript
const noopSubscribe = () => () => {};

function readBannerEligible(): boolean {
  if (typeof window === "undefined") return false;
  // ... compute synchronously from external state ...
  return /* boolean */;
}

const eligible = useSyncExternalStore(
  noopSubscribe,            // we never re-subscribe; reads happen on render
  readBannerEligible,       // client snapshot
  () => false,              // SSR snapshot
);
```

For `usePrefersReducedMotion`, we DO want to re-subscribe because the value can change at runtime (user toggles OS setting). Use a real `subscribe`:
```typescript
function subscribe(callback: () => void): () => void {
  if (typeof window === "undefined" || !window.matchMedia) return () => {};
  const mq = window.matchMedia("(prefers-reduced-motion: reduce)");
  mq.addEventListener?.("change", callback);
  return () => mq.removeEventListener?.("change", callback);
}

function getSnapshot(): boolean {
  if (typeof window === "undefined" || !window.matchMedia) return false;
  return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
}

function usePrefersReducedMotion(): boolean {
  return useSyncExternalStore(subscribe, getSnapshot, () => false);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lint cleanup — ShortlistCard useSyncExternalStore + HomeDecide + votes.ts</name>
  <files>frontend/components/ShortlistCard.tsx, frontend/components/HomeDecide.tsx, frontend/lib/votes.ts</files>
  <read_first>
    frontend/components/ShortlistCard.tsx,
    frontend/components/PushPermissionBanner.tsx,
    frontend/components/HomeDecide.tsx,
    frontend/lib/votes.ts,
    .planning/phases/03-decide-w3/deferred-items.md
  </read_first>
  <action>
**Step A — Rewrite `usePrefersReducedMotion` in `frontend/components/ShortlistCard.tsx`.**

1. Update the React import at line 12:
   Before:
   ```typescript
   import { useEffect, useState } from "react";
   ```
   After (drop unused `useState` and `useEffect` if no other site uses them; otherwise keep them but ADD `useSyncExternalStore`):
   ```typescript
   import { useSyncExternalStore } from "react";
   ```
   If `useEffect` or `useState` are referenced elsewhere in the file, keep those names and append `useSyncExternalStore`. After this edit, run `grep -n "useEffect\|useState" frontend/components/ShortlistCard.tsx` to confirm whether they're still needed; remove unused names from the import.

2. Replace the `usePrefersReducedMotion` function (currently lines ~44-54) wholesale with:

```typescript
function subscribePRM(callback: () => void): () => void {
  if (typeof window === "undefined" || !window.matchMedia) return () => {};
  const mq = window.matchMedia("(prefers-reduced-motion: reduce)");
  mq.addEventListener?.("change", callback);
  return () => mq.removeEventListener?.("change", callback);
}

function getPRMSnapshot(): boolean {
  if (typeof window === "undefined" || !window.matchMedia) return false;
  return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
}

function usePrefersReducedMotion(): boolean {
  return useSyncExternalStore(subscribePRM, getPRMSnapshot, () => false);
}
```

3. The call site `const reducedMotion = usePrefersReducedMotion();` inside `ShortlistCard` is unchanged — same hook signature.

**Step B — `frontend/components/HomeDecide.tsx` line 169 and 229.**

Find the line that currently reads `// eslint-disable-next-line no-console` at line 169 (it precedes a line that no longer logs). Delete that comment line. Verify the next line is NOT a `console.*` call (if it IS, the disable is still needed and you should NOT delete — instead leave a comment in your SUMMARY.md flagging the discrepancy).

Find the function `onCookingStarted` at line 229. The signature is:
```typescript
function onCookingStarted(_e: Event) {
```

Change to:
```typescript
function onCookingStarted() {
```

This requires the call site to NOT pass an Event — verify by checking how `onCookingStarted` is registered as a listener:
- If it's registered via `window.addEventListener("cooking.started", onCookingStarted)`, the listener API passes an Event arg but unused parameters are fine — TypeScript / ESLint complain about the underscore-prefixed parameter only because the linter's unused-vars rule doesn't recognize the underscore convention here. Removing the parameter entirely is the cleanest fix because addEventListener tolerates listeners with fewer parameters than the event provides (JS arity rules).

If removing the parameter triggers a TypeScript error (the listener type expects `(e: Event) => void`), keep `_e: Event` and instead add `// eslint-disable-next-line @typescript-eslint/no-unused-vars` ABOVE the function. The pragmatic call: remove the param if TS allows; otherwise leave with a targeted disable comment.

**Step C — `frontend/lib/votes.ts` line 94.**

Find the `// eslint-disable-next-line no-console` at line 94. Confirm the next non-blank line is NOT a `console.*` call. If confirmed dead, delete the comment line. If still guarding a `console.*` call, leave it in place and document the discrepancy in SUMMARY.md.

**Critical rule:** Each disable directive removal MUST be verified against the line it would have guarded. Removing a still-needed disable will create a NEW lint error — net negative. Use `git diff` to inspect each removal before committing.

**WHY useSyncExternalStore vs useState+useEffect:** the React 19 `react-hooks/set-state-in-effect` rule prevents calling `setState` synchronously inside an effect that runs on mount, because that triggers a cascading render and breaks Suspense. The `useSyncExternalStore` API is explicitly designed for "subscribe to an external store and re-render when it changes" — `matchMedia` is exactly such a store. The reference implementation in `PushPermissionBanner.tsx` proves this pattern is already adopted in the codebase, so consistency is the right move.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tee /tmp/lint.log ; echo "---" ; grep -E "(ShortlistCard|HomeDecide|votes\.ts).*(error|warning)" /tmp/lint.log | head -20 ; echo "DONE"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/ShortlistCard.tsx` contains the literal substring `useSyncExternalStore` (verifiable: `grep -F "useSyncExternalStore" frontend/components/ShortlistCard.tsx` exits 0)
    - `frontend/components/ShortlistCard.tsx` does NOT contain the literal substring `setReduced(` (verifiable: `! grep -F "setReduced(" frontend/components/ShortlistCard.tsx`)
    - `cd frontend && npm run lint 2>&1 | grep "ShortlistCard.tsx" | grep -E "react-hooks/set-state-in-effect"` returns no lines
    - `cd frontend && npm run lint 2>&1 | grep "HomeDecide.tsx" | grep -E "(no-unused-vars|@typescript-eslint/no-unused-vars).*_e"` returns no lines
    - `cd frontend && npm run lint 2>&1 | grep -c "Unused eslint-disable directive"` returns 0 (or strictly fewer than the baseline pre-edit count, captured before the executor begins — the SUMMARY.md must record the before/after numbers)
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -E "(ShortlistCard|HomeDecide|votes\.ts)"` returns no NEW error lines (pre-existing project-wide errors tolerated)
    - `cd frontend && grep -F "usePrefersReducedMotion()" components/ShortlistCard.tsx` returns at least 1 line (the call site is preserved)
  </acceptance_criteria>
  <done>ShortlistCard's prefers-reduced-motion hook uses `useSyncExternalStore`; HomeDecide and votes.ts have their unused eslint-disable directives removed; no new lint or tsc errors introduced; visual behavior of ShortlistCard is unchanged (verifiable by inspection — only the hook implementation changed, not the consumer).</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md + REQUIREMENTS.md reflect album scope cut</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/phases/04-polish-w4/04-CONTEXT.md
  </read_first>
  <action>
**Step A — Update `.planning/ROADMAP.md`.**

1. Find the Phase 4 entry (`### Phase 4: Polish (W4)`).
2. Replace the `**Requirements**` line. Before:
   ```
   **Requirements**: COOK-03, COOK-04, COOK-05, ALBUM-01, ALBUM-02, ALBUM-03
   ```
   After:
   ```
   **Requirements**: COOK-03, COOK-04, COOK-05
   ```
3. Replace the `**Goal**` paragraph for Phase 4 (it currently mentions "shared Album"). Before (verbatim from current file):
   ```
   **Goal**: Cooking-log finalization (photo upload ≤ 4, 3-value rating `loved`/`liked`/`disliked`, voice-dictated notes), denormalized `last_cooked_at` + `cook_count` updates in the same transaction as the log insert, shared Album masonry grid of cooking-log photos, per-recipe history, offline-mode tuning (service worker cache strategies), accessibility pass, error toasts, and a documented productize-later TODO list. Final dogfood gate: ≥ 2 weeks daily use by both members at end of W4 — the v0.1 definition of done.
   ```
   After:
   ```
   **Goal**: Cooking-log finalization (photo upload ≤ 4, 3-value rating `loved`/`liked`/`disliked`, notes via OS keyboard mic), denormalized `last_cooked_at` + `cook_count` + `last_cooked_photo_path` updates in the same transaction as the log finalization (architecture invariant #3, COOK-05), the recipe-card living image (D-05 — recipe cards display the most recent cooking-log photo as their primary thumbnail), Phase-3 deferred lint cleanup (D-09), and a mobile-first accessibility pass (D-08: contrast, 48px tap targets, focus rings). Album / shared masonry grid (ALBUM-01/02/03) is CUT from v0.1 per 04-CONTEXT.md (commit c7ee1f0) — deferred to productize-later. Final dogfood gate: ≥ 2 weeks daily use by both members at end of W4 — the v0.1 definition of done.
   ```
4. Replace the `**Success Criteria**` block — replace the bullet about "shared Album renders a masonry grid" with a bullet about the recipe-card living image. The new full block:
   ```
   **Success Criteria** (what must be TRUE):
     1. After cooking, the user finalizes the log with up to 4 photos, dictates notes via the iOS keyboard mic into the notes textarea, and picks a `loved`/`liked`/`disliked` rating; the entry persists and the home banner clears
     2. Recipe cards in the recipe library show the most recent cooking-log photo as their primary thumbnail (D-05 living image), falling back to the static recipe photo if no cooking has occurred
     3. On log finalization, `recipes.last_cooked_at`, `recipes.cook_count`, and `recipes.last_cooked_photo_path` are updated in the same DB transaction as the cooking-log update; the recipe detail page reflects the new values immediately
     4. The user opens the app in airplane mode and the cached app shell renders with no network; reconnect resumes WebSocket sync without manual reload
     5. Behavioral validation: both household members have used the app daily for ≥ 2 weeks at end of W4 (the v0.1 definition of done)
   ```
5. Replace the `**Plans**: TBD` line with `**Plans**: 4 plans` and replace the plan checkbox list with:
   ```
   - [ ] 04-01-PLAN.md — Backend cooking-log finalization (PUT /cooking-logs/{id} + POST photos endpoint + same-tx denormalized recipe update)
   - [ ] 04-02-PLAN.md — Frontend cooking-log finalization page (RatingPicker + CookingLogFinalize) and recipe-card living image
   - [ ] 04-03-PLAN.md — Phase-3 lint cleanup + Album scope reconciliation (this plan)
   - [ ] 04-04-PLAN.md — UAT gate: a11y polish verification + airplane-mode shell test (checkpoint)
   ```
6. Update the **Coverage Summary** section. Before:
   ```
   - Phase 4 — Polish: 6 REQ-IDs (COOK-03/04/05, ALBUM × 3)
   ```
   After:
   ```
   - Phase 4 — Polish: 3 REQ-IDs (COOK-03/04/05). ALBUM × 3 cut to v2 per 04-CONTEXT.md (commit c7ee1f0).
   ```
   Update the v1 total too: from `52 total` to `49 total`. Update the per-phase mapping bullet for Phase 1+2+3+4 totals (1: 26 + 2: 7 + 3: 13 + 4: 3 = 49).

   Update the table in the **Per-phase breakdown** that sits in the "Coverage Summary" section likewise.

7. Update the **Phases** checkbox list near the top — replace the Phase 4 line. Before:
   ```
   - [ ] **Phase 4: Polish (W4)** — Cooking-log finalization, shared Album, offline tuning, accessibility, productize-later TODO sweep
   ```
   After:
   ```
   - [ ] **Phase 4: Polish (W4)** — Cooking-log finalization, recipe-card living image (last cooking-log photo), Phase-3 lint cleanup, mobile a11y pass. Album cut to v2.
   ```

**Step B — Update `.planning/REQUIREMENTS.md`.**

1. Find the `### Album (ALBUM)` block in the v1 Requirements section. DELETE the three bullets:
   ```
   - [ ] **ALBUM-01**: ...
   - [ ] **ALBUM-02**: ...
   - [ ] **ALBUM-03**: ...
   ```
   And the `### Album (ALBUM)` heading itself.
2. In the v2 Requirements section, ADD a new heading `### Album (V2-ALBUM)` after the existing v2 section headings (e.g., after `### Distribution & Notifications`):
   ```
   ### Album (V2-ALBUM)

   Cut from v1 per `.planning/phases/04-polish-w4/04-CONTEXT.md` (commit c7ee1f0). Rationale: not useful enough at couple-scale to justify the build cost. Productize-later if dogfood reveals demand.

   - **V2-ALBUM-01**: Shared masonry photo grid of `cooking_logs` with photos, ordered by date desc (`GET /album?limit=50`)
   - **V2-ALBUM-02**: Each album item shows cook color, recipe title, rating, and primary photo
   - **V2-ALBUM-03**: Tapping into an album item shows the full cooking log (all photos, notes, rating) and source recipe
   ```
3. Update the **Traceability** table. Find the rows:
   ```
   | ALBUM-01 | Phase 4 — Polish (W4) | Pending |
   | ALBUM-02 | Phase 4 — Polish (W4) | Pending |
   | ALBUM-03 | Phase 4 — Polish (W4) | Pending |
   ```
   Replace with:
   ```
   | ALBUM-01 | Deferred to v2 (V2-ALBUM-01) | Cut |
   | ALBUM-02 | Deferred to v2 (V2-ALBUM-02) | Cut |
   | ALBUM-03 | Deferred to v2 (V2-ALBUM-03) | Cut |
   ```
4. Update the **Coverage** block at the bottom. Before:
   ```
   - v1 requirements: 52 total ...
   - Mapped to phases: 52
   - Unmapped: 0 ✓
   ```
   After:
   ```
   - v1 requirements: 49 total (3 cut to v2 — see Album section)
   - Mapped to phases: 49
   - Unmapped: 0 ✓
   - Cut to v2: 3 (ALBUM-01/02/03)
   ```
5. Update the **Per-phase breakdown**:
   - Phase 4 — Polish (W4): 3 (COOK-03/04/05). ALBUM × 3 deferred to v2.
6. Update the file's `*Last updated:*` line at the bottom to today's date.

**Critical preservation rule:** do NOT delete the historical "REQ-ID tally" notes in REQUIREMENTS.md (the explanatory paragraph about the 46→52 correction). Just add the new "49 after cut" note alongside, so the audit trail is preserved.

**WHY documentation lives here, not in code:** the user already cut Album in CONTEXT.md (commit `c7ee1f0`); ROADMAP/REQUIREMENTS just need to catch up. No code is being deleted because no Album code exists. The bookkeeping makes the planning artifacts internally consistent.
  </action>
  <verify>
    <automated>grep -c "ALBUM-01" .planning/ROADMAP.md ; grep -c "ALBUM-01" .planning/REQUIREMENTS.md ; grep -c "V2-ALBUM-01" .planning/REQUIREMENTS.md ; grep -c "04-01-PLAN.md" .planning/ROADMAP.md ; grep -c "04-04-PLAN.md" .planning/ROADMAP.md ; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 4 `**Requirements**` line lists exactly `COOK-03, COOK-04, COOK-05` (no ALBUM-* IDs)
    - `.planning/ROADMAP.md` does NOT contain the substring `ALBUM-01, ALBUM-02, ALBUM-03` in the Phase 4 entry (verifiable: `awk '/### Phase 4/,/^### /' .planning/ROADMAP.md | grep -c "ALBUM-01"` returns 0)
    - `.planning/ROADMAP.md` Phase 4 plan list contains all four file references: `04-01-PLAN.md`, `04-02-PLAN.md`, `04-03-PLAN.md`, `04-04-PLAN.md`
    - `.planning/ROADMAP.md` Coverage Summary states the Phase 4 count as `3` (not `6`) and the v1 total as `49` (not `52`)
    - `.planning/REQUIREMENTS.md` v1 section no longer contains `**ALBUM-01**:` (verifiable: `awk '/^## v1 Requirements/,/^## v2 Requirements/' .planning/REQUIREMENTS.md | grep -c "\\*\\*ALBUM-01\\*\\*:"` returns 0)
    - `.planning/REQUIREMENTS.md` v2 section contains a new `### Album (V2-ALBUM)` heading with bullets for `V2-ALBUM-01`, `V2-ALBUM-02`, `V2-ALBUM-03`
    - `.planning/REQUIREMENTS.md` Traceability table marks ALBUM-01/02/03 rows as `Cut` (not `Pending`)
    - `.planning/REQUIREMENTS.md` Coverage line states `v1 requirements: 49 total`
    - `git diff --numstat .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows non-zero changes on both files (i.e., both were edited, not just one)
  </acceptance_criteria>
  <done>ROADMAP and REQUIREMENTS no longer associate ALBUM-* with Phase 4 v1 scope; v2 section explicitly captures V2-ALBUM-01/02/03 with rationale; Phase 4 plan list enumerates the 4 plans; coverage tallies are internally consistent (49 v1 + 3 cut = 52 historical).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is documentation + lint cleanup; no new code paths, no new API surface, no new user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | inadvertent removal of a still-needed eslint-disable | mitigate | Each disable removal MUST be inspected via `git diff` to confirm the disabled rule no longer applies (the line below isn't a `console.*` call). If still applicable, leave the disable in place and flag in SUMMARY.md |
| T-04-03-02 | Repudiation | doc-only changes obscure the album cut history | accept | The cut is recorded in commit c7ee1f0; this plan's commit explicitly references it. The historical "46 → 52" note in REQUIREMENTS.md is preserved alongside the new "49 after cut" note |
| T-04-03-03 | Denial of Service | useSyncExternalStore subscribe leaks listener | mitigate | The `subscribe` function returns a teardown that calls `removeEventListener`; React's `useSyncExternalStore` invokes the teardown automatically on unmount. Verified by the existing `PushPermissionBanner.tsx` reference (already in production for ≥1 week without listener leaks) |
</threat_model>

<verification>
**Automated:**
- `cd frontend && npm run lint 2>&1` — exits 0 (or with strictly fewer warnings than baseline)
- `cd frontend && npx tsc --noEmit` — no new errors involving ShortlistCard / HomeDecide / votes.ts
- `awk '/### Phase 4/,/^### /' .planning/ROADMAP.md | grep -c "ALBUM"` — returns 0
- `grep -c "V2-ALBUM" .planning/REQUIREMENTS.md` — returns at least 3 (3 bullets minimum)
- `git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md` — both files show changes

**Manual sanity-check (executor reports in SUMMARY.md):**
- Visual diff of ShortlistCard before/after — only the hook implementation changed; render tree unchanged
- Run the swipe deck on `/` → swipe a card → confirm the rotation animation respects `prefers-reduced-motion: reduce` if toggled in macOS System Preferences (or DevTools rendering tab)
- Visit `/recipes` → confirm RecipeCard still renders correctly (this plan does not touch it but lint runs the whole project; sanity check)
</verification>

<success_criteria>
- ShortlistCard, HomeDecide, votes.ts pass lint cleanly (no new warnings, deferred Phase-3 ones closed)
- ShortlistCard's `prefers-reduced-motion` subscription uses `useSyncExternalStore`; render tree unchanged
- ROADMAP.md Phase 4 entry reflects 3 REQ-IDs (COOK-03/04/05) and the 4-plan structure
- REQUIREMENTS.md v1 Album section is gone; v2 section gains V2-ALBUM-01/02/03 with cut rationale
- Coverage tallies internally consistent (49 v1 + 3 cut = 52 historical enumeration)
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-w4/04-03-SUMMARY.md` documenting:
- Baseline lint warning count (captured before edits) vs after-edit count
- Each disable directive removed and why (with line number references)
- Diff summary of ROADMAP.md and REQUIREMENTS.md (which sections changed)
- Any unexpected lint warnings discovered while running `npm run lint` against the whole project (productize-later candidates per D-10)
</output>
