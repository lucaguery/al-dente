---
phase: 32-port-locked-screens-to-sober-kitchen
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - frontend/components/Marginalia.tsx
  - frontend/components/BrandLoader.tsx
  - frontend/components/LedgerCard.tsx
  - frontend/components/TableVote.tsx
  - frontend/components/ui/sonner.tsx
  - frontend/components/HomeDecide.tsx
  - frontend/components/RecipeForm.tsx
  - frontend/components/RecipeThread/SystemBubble.tsx
  - frontend/components/VoiceModifySheet.tsx
  - frontend/components/SearchInput.tsx
  - frontend/app/onboarding/create/page.tsx
  - frontend/app/onboarding/join/page.tsx
autonomous: true
requirements:
  - SOBER-07
  - SOBER-08
tags: [design-system, primitives, components, sweep]
must_haves:
  truths:
    - "<Marginalia> primitive renders Caveat text at sm/md/lg sizes with optional slant"
    - "<BrandLoader> renders the brand-mark SVG with stroke-dasharray animation at default (96px) and sm (~18px) sizes"
    - "<LedgerCard> renders patine card with --patina inline style overriding the global default"
    - "<TableVote> renders the table-scene with per-seat states derived from individual votes (NOT a stored state column)"
    - "All 12 spinner call-sites (HomeDecide × 2, RecipeForm × 1, SystemBubble × 3, VoiceModifySheet × 1, SearchInput × 1, onboarding/create × 1, onboarding/join × 2, sonner Toaster × 1) use <BrandLoader>; zero animate-spin / Loader2 / Spinner / LoadingSpinner outside BrandLoader.tsx"
    - "PinLabel.tsx is unchanged (Phase 28 API lock preserved)"
    - "No new broadcast_to_household events added (invariant #4)"
    - "No state column added to votes or daily_shortlist_recipes (invariant #2 preserved)"
  artifacts:
    - path: "frontend/components/Marginalia.tsx"
      provides: "<Marginalia size sm/md/lg slant> Caveat wrapper"
      exports: ["Marginalia", "MarginaliaProps"]
    - path: "frontend/components/BrandLoader.tsx"
      provides: "<BrandLoader size default/sm> drawing-stroke animation"
      exports: ["BrandLoader", "BrandLoaderProps"]
    - path: "frontend/components/LedgerCard.tsx"
      provides: "<LedgerCard patina 0|1|2|3 dogear?> patine card"
      exports: ["LedgerCard", "LedgerCardProps"]
    - path: "frontend/components/TableVote.tsx"
      provides: "<TableVote votes members myMemberId size?> table-scene voting visual"
      exports: ["TableVote", "TableVoteProps"]
  key_links:
    - from: "frontend/components/BrandLoader.tsx"
      to: "frontend/components/BrandIcon.tsx"
      via: "compose BrandIcon SVG inside .loader-brand wrapper"
      pattern: "BrandIcon"
    - from: "frontend/components/TableVote.tsx"
      to: "frontend/lib/votes.ts"
      via: "computeVoteState consumed for aggregate + per-vote derivation for per-seat state"
      pattern: "computeVoteState"
    - from: "frontend/components/ui/sonner.tsx"
      to: "frontend/components/BrandLoader.tsx"
      via: "Toaster icons.loading prop set to <BrandLoader size=\"sm\" />"
      pattern: "BrandLoader"
---

<objective>
Introduce the four Sober Kitchen primitives — `<Marginalia>`, `<BrandLoader>`, `<LedgerCard>`, `<TableVote>` — and execute the strict cross-cutting spinner sweep (12 sites → BrandLoader). Per CONTEXT D-02 + D-05 + D-14 + D-15.

Purpose: Closes SOBER-07 (marginalia register — Caveat available as a composable primitive) + SOBER-08 (grep gate: zero `animate-spin` outside BrandLoader). Provides the building blocks 32-03 (Accueil), 32-04 (Bibliothèque), 32-05 (Recette) compose.
Output: 4 new components + 12 spinner sites swept + grep gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-port-locked-screens-to-sober-kitchen/32-CONTEXT.md
@.planning/phases/32-port-locked-screens-to-sober-kitchen/32-RESEARCH.md
@.planning/phases/32-port-locked-screens-to-sober-kitchen/32-UI-SPEC.md
@.planning/phases/32-port-locked-screens-to-sober-kitchen/32-01-SUMMARY.md
@docs/design-system.html
@CLAUDE.md
@frontend/AGENTS.md

<interfaces>
<!-- Key interfaces this plan consumes / produces -->

From frontend/lib/votes.ts (already exists — DO NOT MODIFY):
```typescript
export type VoteValue = "yes" | "no";
export type VoteState = "valide" | "pressenti" | "conteste" | "rejete" | "sans_avis";
export type ShortlistVote = {
  shortlist_id: string;
  recipe_id: string;
  member_id: string;
  vote: VoteValue;
};
export function computeVoteState(
  votes: readonly { vote: VoteValue }[],
  memberCount: number = 2,
): VoteState;
```

From frontend/components/BrandIcon.tsx (already exists — DO NOT MODIFY):
```typescript
export function BrandIcon({
  size = 48,
  strokeWidth = 6,
  className,
  "aria-label": ariaLabel,
}: {
  size?: number;
  strokeWidth?: number;
  className?: string;
  "aria-label"?: string;
}): JSX.Element;
// Renders viewBox="0 0 160 160", two <path> elements with stroke=currentColor.
```

HouseholdMember shape (from frontend/components/SessionProvider — `session.me` and `session.members[]`):
```typescript
type HouseholdMember = {
  id: string;
  name: string;
  color_hex: string;
};
// Note: session.me has shape { id, name, color_hex } (HomeDecide.tsx line 54).
// The MEMBER_COLORS table in frontend/lib/colors.ts maps color_hex to one of:
// rose | amber | emerald | sky | violet — see existing usage in member-color tokens.
```

CSS classes available from globals.css (added by 32-01):
- .marginalia, .marginalia-sm, .marginalia-md, .marginalia-lg, .marginalia.slant
- .ledger-card, .ledger-card::before, .ledger-card::after, .dogear, .patina-stamp
- .table-scene, .table-scene.ts-72, .table-scene.ts-56, .table-plate, .table-seat
- .seat-north, .seat-south, .seat-east, .seat-west
- .seat-state-valide, .seat-state-pressenti, .seat-state-neutral, .seat-state-rejected, .seat-state-contested
- .loader-brand, .loader-brand.loader-brand-sm
- @keyframes drawLoop

Sonner Toaster icons.loading prop (from frontend/components/ui/sonner.tsx line 27-29):
```tsx
loading: (<Loader2Icon className="size-4 animate-spin" />),
// One-line swap to: loading: (<BrandLoader size="sm" />),
```
</interfaces>

<call_site_inventory>
<!-- Verified Loader2 / animate-spin sites — see RESEARCH.md §Spinner-sweep call-site inventory + grep output -->

| # | File | Line | Current | Replacement |
|---|------|------|---------|-------------|
| 1 | frontend/components/HomeDecide.tsx | 350-353 | `<Loader2 aria-hidden className="h-8 w-8 animate-spin text-primary" />` (centered page) | `<BrandLoader aria-label="Chargement" />` |
| 2 | frontend/components/HomeDecide.tsx | 394-397 | same shape (cold-load) | `<BrandLoader aria-label="Chargement" />` |
| 3 | frontend/components/RecipeForm.tsx | 614 | `<Loader2 className="animate-spin h-4 w-4 mr-2" />` (inline submit) | `<BrandLoader size="sm" aria-label="Chargement" className="mr-2" />` |
| 4 | frontend/components/RecipeThread/SystemBubble.tsx | 139 | `<Loader2 size={14} className="animate-spin" aria-hidden />` | `<BrandLoader size="sm" aria-label="Chargement" />` |
| 5 | frontend/components/RecipeThread/SystemBubble.tsx | 286 | same | `<BrandLoader size="sm" aria-label="Chargement" />` |
| 6 | frontend/components/RecipeThread/SystemBubble.tsx | 485 | same | `<BrandLoader size="sm" aria-label="Chargement" />` |
| 7 | frontend/components/VoiceModifySheet.tsx | 109 | `<Loader2 className="mr-2 h-4 w-4 animate-spin" />` | `<BrandLoader size="sm" aria-label="Chargement" className="mr-2" />` |
| 8 | frontend/components/SearchInput.tsx | 101-103 | `<Loader2 className="h-4 w-4 animate-spin text-foreground-muted" />` | `<BrandLoader size="sm" aria-label="Chargement" className="text-foreground-muted" />` |
| 9 | frontend/app/onboarding/create/page.tsx | 143 | `<Loader2 className="animate-spin h-4 w-4 mr-2" aria-hidden />` | `<BrandLoader size="sm" aria-label="Chargement" className="mr-2" />` |
| 10 | frontend/app/onboarding/join/page.tsx | 309-310 | `<Loader2 className="h-4 w-4 animate-spin text-foreground-muted" />` (inline verify) | `<BrandLoader size="sm" aria-label="Chargement" className="text-foreground-muted" />` |
| 11 | frontend/app/onboarding/join/page.tsx | 345 | `<Loader2 className="animate-spin h-4 w-4 mr-2" aria-hidden />` (submit) | `<BrandLoader size="sm" aria-label="Chargement" className="mr-2" />` |
| 12 | frontend/components/ui/sonner.tsx | 28 | `<Loader2Icon className="size-4 animate-spin" />` (Toaster icons.loading) | `<BrandLoader size="sm" />` |

In every file, also remove the `Loader2` / `Loader2Icon` import from the `lucide-react` import statement. If lucide-react no longer imports anything in that file, drop the entire import line.
</call_site_inventory>
</context>

<implementation_notes>
- **Open question A6 (conteste per-seat mapping) resolution:** Per UI-SPEC §7.2 and RESEARCH Open Question 2 — when aggregate state is `conteste` (1 yes + 1 no in couple-scale), the yes-voter seat receives `seat-state-valide` (per doc mock line 1515), the no-voter seat receives `seat-state-contested` (strike-through). The yes voter's seat becomes "this is the one I want — and they struck through it" — captures the editorial drama of disagreement.
- **Open question A4 (BrandLoader size sm dasharray) resolution:** Keep `stroke-dasharray: 220` at 18px scale. SVG viewBox-relative dasharray scales proportionally. Visual check at plan close — if jagged, the planner may add a `.loader-brand.loader-brand-sm svg path { stroke-dasharray: 80; }` override but the default keeps doc parity.
- **PinLabel.tsx untouched (D-05 "if straightforward"):** PinLabel operates at 12px (sub-register below `.marginalia-sm` 16px). Composing `<Marginalia size="sm">` would require font-size override back to 12px, defeating the primitive. Leave PinLabel alone — 32-01 already gives it Caveat resolution via `var(--font-marginalia)`.
- **No `<Spinner>` or `<LoadingSpinner>` shim:** D-15 explicit — `BrandLoader` is the single export. Any temptation to keep a `Spinner.tsx` re-export for migration ergonomics is rejected — the sweep is atomic this plan.
- **Component file location:** Flat in `frontend/components/` (existing convention per CLAUDE.md). Do NOT introduce `frontend/components/sober/` subfolder (UI-SPEC §7 leaves this to planner; flat keeps grep/import noise low).
- **TableVote DOM contract:** For couple-scale (2 members) the component renders only `.seat-north` and `.seat-south` (skips east/west to keep the DOM tight). Future N>2 households will render additional seats — `<TableVote>` reads `members.length` to decide which seats to mount.
- **No backend changes.** Invariant #2 guard (D-19) is enforced by `<TableVote>` deriving per-seat state from `votes: ShortlistVote[]` directly — there is NO `state` column anywhere.
</implementation_notes>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the four primitives — Marginalia, BrandLoader, LedgerCard, TableVote</name>
  <files>frontend/components/Marginalia.tsx, frontend/components/BrandLoader.tsx, frontend/components/LedgerCard.tsx, frontend/components/TableVote.tsx</files>
  <read_first>
    - frontend/components/BrandIcon.tsx (full file — the SVG paths BrandLoader composes)
    - frontend/components/RecipeThread/PinLabel.tsx (Caveat reference; do NOT modify, but study the `var(--font-marginalia)` pattern)
    - frontend/lib/votes.ts (full file — `computeVoteState` signature TableVote consumes)
    - frontend/lib/colors.ts (MEMBER_COLORS table — TableVote uses to map color_hex → member-token slot)
    - docs/design-system.html lines 170-184 (marginalia CSS) + 186-236 (patine CSS) + 238-297 (table-scene CSS) + 299-320 (loader CSS)
    - .planning/phases/32-port-locked-screens-to-sober-kitchen/32-UI-SPEC.md §7 (Primitives Contract — all 4 component specs)
    - .planning/phases/32-port-locked-screens-to-sober-kitchen/32-RESEARCH.md §Pattern 3 (LedgerCard composition) + Pattern 4 (TableVote seat-state) + Pattern 5 (BrandLoader animation)
    - frontend/components/HomeDecide.tsx lines 54, 84-95 (HouseholdMember shape, me/partner resolution from session)
  </read_first>
  <action>
Create four new component files in `frontend/components/`. Each is small (under 100 lines).

**A. `frontend/components/Marginalia.tsx`:**
```tsx
"use client";

// Phase 32 §15.B — Caveat marginalia wrapper.
// Composes the .marginalia / .marginalia-sm|md|lg / .marginalia.slant
// utility classes from globals.css (added in 32-01 §15.B).
// Per CONTEXT D-05 + UI-SPEC §7.3.
//
// Sub-16px sites (PinLabel at 12px) do NOT use this primitive — they
// inline `var(--font-marginalia)` directly. <Marginalia> starts at sm=1rem.

import type { ReactNode, CSSProperties } from "react";

export type MarginaliaSize = "sm" | "md" | "lg";

export interface MarginaliaProps {
  size?: MarginaliaSize;
  slant?: boolean;
  as?: "p" | "span" | "div";
  className?: string;
  style?: CSSProperties;
  children: ReactNode;
}

export function Marginalia({
  size = "sm",
  slant = false,
  as: Tag = "p",
  className,
  style,
  children,
}: MarginaliaProps) {
  const cls = [
    "marginalia",
    `marginalia-${size}`,
    slant ? "slant" : null,
    className,
  ]
    .filter(Boolean)
    .join(" ");
  return (
    <Tag className={cls} style={style}>
      {children}
    </Tag>
  );
}

export default Marginalia;
```

**B. `frontend/components/BrandLoader.tsx`:**
```tsx
"use client";

// Phase 32 §15.C — Brand-mark drawing-stroke loader.
// Composes BrandIcon SVG inside .loader-brand wrapper. The
// stroke-dasharray animation lives in globals.css (added in 32-01).
// Per CONTEXT D-05, D-14, D-15 + UI-SPEC §7.4 + RESEARCH Pattern 5.
//
// prefers-reduced-motion handled by globals.css (per-loader-brand fallback
// + global animation-duration: 0ms !important rule).

import { BrandIcon } from "@/components/BrandIcon";

export type BrandLoaderSize = "default" | "sm";

export interface BrandLoaderProps {
  size?: BrandLoaderSize;
  className?: string;
  "aria-label"?: string;
}

export function BrandLoader({
  size = "default",
  className,
  "aria-label": ariaLabel = "Chargement",
}: BrandLoaderProps) {
  // BrandIcon's intrinsic SVG size; .loader-brand and .loader-brand-sm
  // CSS overrides the wrapper dimensions. SVG scales via viewBox.
  const svgSize = size === "sm" ? 18 : 96;
  const cls = [
    "loader-brand",
    size === "sm" ? "loader-brand-sm" : null,
    className,
  ]
    .filter(Boolean)
    .join(" ");
  return (
    <div className={cls} role="img" aria-label={ariaLabel}>
      <BrandIcon size={svgSize} strokeWidth={6} aria-hidden />
    </div>
  );
}

export default BrandLoader;
```

**C. `frontend/components/LedgerCard.tsx`:**
```tsx
"use client";

// Phase 32 §15.C — Patine ledger card.
// Wraps a <article> (or chosen element) with .ledger-card class and an
// inline --patina CSS var consumed by the patine overlays (::before /
// ::after / .dogear) in globals.css (added in 32-01).
// Per CONTEXT D-05, D-07 + UI-SPEC §7.1 + RESEARCH Pattern 3.
//
// Consumers must NOT add `paper-grain` class — .ledger-card::after
// provides its own dot-grid grain (RESEARCH Pitfall 1).
// Dogear renders when `dogear` prop OR when patina >= 3 (Héritage).

import type { ReactNode, CSSProperties } from "react";

export type PatinaLevel = 0 | 1 | 2 | 3;

export interface LedgerCardProps {
  patina: PatinaLevel;
  dogear?: boolean;
  as?: "article" | "div";
  className?: string;
  style?: CSSProperties;
  children: ReactNode;
}

export function LedgerCard({
  patina,
  dogear,
  as: Tag = "article",
  className,
  style,
  children,
}: LedgerCardProps) {
  const showDogear = dogear ?? patina >= 3;
  const cls = ["ledger-card", className].filter(Boolean).join(" ");
  return (
    <Tag
      className={cls}
      style={{ ...style, ["--patina" as string]: patina } as CSSProperties}
    >
      {children}
      {showDogear ? (
        <span className="dogear" aria-hidden>
          {/* Doc line 1612: SVG corner-fold. Foreground = border-mix. */}
          <svg viewBox="0 0 26 26" width={26} height={26}>
            <path
              d="M 0 0 L 26 0 L 26 26 Z"
              fill="color-mix(in oklch, var(--border) 60%, transparent)"
            />
            <path
              d="M 0 0 L 26 0 L 14 12 Z"
              fill="var(--card)"
            />
          </svg>
        </span>
      ) : null}
    </Tag>
  );
}

export default LedgerCard;
```

**D. `frontend/components/TableVote.tsx`:**
```tsx
"use client";

// Phase 32 §15.C — Table-à-manger voting scene.
// Renders the round table + per-seat states derived from individual votes.
// Per CONTEXT D-05, D-19 (invariant #2 — voting state COMPUTED, not stored).
// Per UI-SPEC §7.2 + RESEARCH Pattern 4.
//
// Per-seat state derivation rules (couple-scale, N=2):
//   - aggregate = computeVoteState(votes, members.length) from lib/votes.ts
//   - per-seat = derived from each member's individual vote:
//     - valide aggregate → both seats = seat-state-valide
//     - rejete aggregate → both seats = seat-state-rejected (with directional push)
//     - conteste aggregate (1 yes + 1 no) → yes voter = seat-state-valide,
//                                            no voter = seat-state-contested (per UI-SPEC §7.2 + A6 resolution)
//     - pressenti aggregate → yes voter = seat-state-pressenti, others = neutral
//     - sans_avis aggregate → all seats = seat-state-neutral

import type { CSSProperties } from "react";
import { computeVoteState, type ShortlistVote } from "@/lib/votes";

export type HouseholdMemberLite = {
  id: string;
  name: string;
  color_hex: string;
};

export type TableVoteSize = "ts-90" | "ts-72" | "ts-56";

export interface TableVoteProps {
  votes: readonly ShortlistVote[];
  members: readonly HouseholdMemberLite[];
  myMemberId: string;
  size?: TableVoteSize;
  className?: string;
}

type SeatPosition = "north" | "south" | "east" | "west";

type SeatPlan = {
  position: SeatPosition;
  member: HouseholdMemberLite;
};

function memberInitial(member: HouseholdMemberLite): string {
  return (member.name?.[0] ?? "?").toUpperCase();
}

function memberSlot(colorHex: string): "rose" | "amber" | "emerald" | "sky" | "violet" {
  // Cheap mapping by hex prefix; MEMBER_COLORS in lib/colors.ts is the
  // canonical map, but a direct switch keeps TableVote self-contained.
  // Order matches the desaturated sober palette from §15.A.
  const hex = colorHex.toUpperCase();
  if (hex === "#C0364A" || hex === "#F43F5E") return "rose";
  if (hex === "#C98512" || hex === "#F59E0B") return "amber";
  if (hex === "#0D8A64" || hex === "#10B981") return "emerald";
  if (hex === "#0879AD" || hex === "#0EA5E9") return "sky";
  if (hex === "#6E46C1" || hex === "#8B5CF6") return "violet";
  return "rose";
}

function seatStateClass(
  aggregate: ReturnType<typeof computeVoteState>,
  memberVote: "yes" | "no" | undefined,
  totalMembers: number,
): string {
  if (aggregate === "valide") return "seat-state-valide";
  if (aggregate === "rejete") return "seat-state-rejected";
  if (aggregate === "conteste") {
    if (memberVote === "yes") return "seat-state-valide";
    if (memberVote === "no") return "seat-state-contested";
    return "seat-state-neutral";
  }
  if (aggregate === "pressenti") {
    if (memberVote === "yes") return "seat-state-pressenti";
    return "seat-state-neutral";
  }
  // sans_avis or fallback
  void totalMembers;
  return "seat-state-neutral";
}

export function TableVote({
  votes,
  members,
  myMemberId,
  size = "ts-90",
  className,
}: TableVoteProps) {
  const me = members.find((m) => m.id === myMemberId);
  const others = members.filter((m) => m.id !== myMemberId);
  const aggregate = computeVoteState(
    votes.map((v) => ({ vote: v.vote })),
    members.length,
  );

  // Couple-scale (N=2): me = north, partner = south. Larger N: east+west used.
  const plan: SeatPlan[] = [];
  if (me) plan.push({ position: "north", member: me });
  if (others[0]) plan.push({ position: "south", member: others[0] });
  if (others[1]) plan.push({ position: "east", member: others[1] });
  if (others[2]) plan.push({ position: "west", member: others[2] });

  return (
    <div
      className={`table-scene ${size} ${className ?? ""}`.trim()}
      aria-label={`Vote: ${aggregate}`}
      role="img"
    >
      <div className="table-plate" aria-hidden />
      {plan.map(({ position, member }) => {
        const memberVote = votes.find((v) => v.member_id === member.id)?.vote;
        const stateClass = seatStateClass(aggregate, memberVote, members.length);
        const slot = memberSlot(member.color_hex);
        const seatStyle: CSSProperties = {
          background: `var(--color-member-${slot}-bg)`,
          color: `var(--color-member-${slot}-foreground)`,
        };
        return (
          <span
            key={member.id}
            className={`table-seat seat-${position} ${stateClass}`}
            style={seatStyle}
            aria-label={`${member.name}: ${memberVote ?? "sans avis"}`}
          >
            {memberInitial(member)}
          </span>
        );
      })}
    </div>
  );
}

export default TableVote;
```

Per CONTEXT D-05 + D-19 + UI-SPEC §7.1-7.4. The four primitives are pure — no state mutation, no API calls, no new realtime broadcasts (D-20).
  </action>
  <verify>
    <automated>cd frontend && test -f components/Marginalia.tsx && test -f components/BrandLoader.tsx && test -f components/LedgerCard.tsx && test -f components/TableVote.tsx && grep -l "export function Marginalia" components/Marginalia.tsx && grep -l "export function BrandLoader" components/BrandLoader.tsx && grep -l "export function LedgerCard" components/LedgerCard.tsx && grep -l "export function TableVote" components/TableVote.tsx && grep -c "computeVoteState" components/TableVote.tsx && grep -c "BrandIcon" components/BrandLoader.tsx && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/components/Marginalia.tsx` exits 0
    - `test -f frontend/components/BrandLoader.tsx` exits 0
    - `test -f frontend/components/LedgerCard.tsx` exits 0
    - `test -f frontend/components/TableVote.tsx` exits 0
    - `grep -c "export function Marginalia" frontend/components/Marginalia.tsx` returns 1
    - `grep -c "export function BrandLoader" frontend/components/BrandLoader.tsx` returns 1
    - `grep -c "export function LedgerCard" frontend/components/LedgerCard.tsx` returns 1
    - `grep -c "export function TableVote" frontend/components/TableVote.tsx` returns 1
    - `grep -c "computeVoteState" frontend/components/TableVote.tsx` returns at least 1 (TableVote consumes the canonical state machine)
    - `grep -c "BrandIcon" frontend/components/BrandLoader.tsx` returns at least 1 (BrandLoader composes the SVG)
    - `grep -c "animate-spin" frontend/components/BrandLoader.tsx` returns 0 (uses CSS @keyframes drawLoop, not tailwind animate-spin)
    - `grep -c "patina" frontend/components/LedgerCard.tsx` returns at least 2 (prop + inline style)
    - `grep -c "dogear" frontend/components/LedgerCard.tsx` returns at least 2 (conditional render + SVG)
    - `grep -c "Caveat\\|cursive" frontend/components/Marginalia.tsx` returns 0 (no direct font ref; uses .marginalia class which resolves via globals.css)
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l` returns 0 (TypeScript clean)
    - `cd frontend && npx next build --webpack 2>&1 | grep -E "Failed|Error" | wc -l` returns 0
  </acceptance_criteria>
  <done>Four primitives exist, TypeScript-clean, exporting their stated APIs. Components are pure (no API calls, no realtime broadcasts). 32-03..05 can now import them.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap all 12 spinner call-sites to BrandLoader</name>
  <files>frontend/components/HomeDecide.tsx, frontend/components/RecipeForm.tsx, frontend/components/RecipeThread/SystemBubble.tsx, frontend/components/VoiceModifySheet.tsx, frontend/components/SearchInput.tsx, frontend/app/onboarding/create/page.tsx, frontend/app/onboarding/join/page.tsx, frontend/components/ui/sonner.tsx</files>
  <read_first>
    - frontend/components/BrandLoader.tsx (the primitive created in Task 1)
    - frontend/components/HomeDecide.tsx (lines 14-15 imports, lines 350-356 and 394-400 spinner sites)
    - frontend/components/RecipeForm.tsx (line 10 import, line 614 spinner)
    - frontend/components/RecipeThread/SystemBubble.tsx (line 18 import, lines 139, 286, 485 spinner sites)
    - frontend/components/VoiceModifySheet.tsx (line 17 import, line 109 spinner)
    - frontend/components/SearchInput.tsx (line 13 import, lines 101-104 spinner)
    - frontend/app/onboarding/create/page.tsx (line 7 import, line 143 spinner)
    - frontend/app/onboarding/join/page.tsx (line 7 import, lines 309-310 and 345 spinner sites)
    - frontend/components/ui/sonner.tsx (full file — 50 lines; line 5 import, line 28 loading icon)
    - .planning/phases/32-port-locked-screens-to-sober-kitchen/32-UI-SPEC.md §8.1 (Spinner sweep table — file/line/replacement)
    - .planning/phases/32-port-locked-screens-to-sober-kitchen/32-CONTEXT.md D-14 + D-15 (strict grep gate + no Spinner shim)
  </read_first>
  <action>
Apply the 12-site sweep per the call_site_inventory table in `<context>` above. For each file:

**1. Remove the `Loader2` / `Loader2Icon` import** from the `lucide-react` import statement. Example:
- `import { Loader2 } from "lucide-react";` → delete the import entirely
- `import { Loader2, Sparkles, ArrowRight } from "lucide-react";` → `import { Sparkles, ArrowRight } from "lucide-react";`
- `import { Search, X, Loader2 } from "lucide-react";` → `import { Search, X } from "lucide-react";`
- `import { ChevronLeft, Loader2 } from "lucide-react";` → `import { ChevronLeft } from "lucide-react";`
- `import { CircleCheckIcon, InfoIcon, TriangleAlertIcon, OctagonXIcon, Loader2Icon } from "lucide-react"` → `import { CircleCheckIcon, InfoIcon, TriangleAlertIcon, OctagonXIcon } from "lucide-react"`

**2. Add the BrandLoader import** at an appropriate location (alongside other component imports):
```tsx
import { BrandLoader } from "@/components/BrandLoader";
```

**3. Replace each Loader2 JSX usage** per the inventory table. Detailed replacements:

**File: `frontend/components/HomeDecide.tsx`**

Line 14: `import { Loader2 } from "lucide-react";` → DELETE THIS LINE.
After existing component imports (around line 19), ADD:
```tsx
import { BrandLoader } from "@/components/BrandLoader";
```
Lines 350-354 (first spinner block):
```tsx
<Loader2
  aria-hidden
  className="h-8 w-8 animate-spin text-primary"
/>
```
→
```tsx
<BrandLoader aria-label="Chargement" />
```
Lines 394-397 (second spinner block — identical shape): same replacement.

The comment on line 76 (`// showing the Loader2 synchronously causes a visible flash...`) can be left as-is (it's referring to the historical reason for `useDelayedFlag`; the spinner identity has changed but the delay logic is preserved).

**File: `frontend/components/RecipeForm.tsx`**

Line 10: `import { Loader2, ChevronLeft } from "lucide-react";` → `import { ChevronLeft } from "lucide-react";`
ADD: `import { BrandLoader } from "@/components/BrandLoader";`
Line 614: `<Loader2 className="animate-spin h-4 w-4 mr-2" />` → `<BrandLoader size="sm" className="mr-2" />`

**File: `frontend/components/RecipeThread/SystemBubble.tsx`**

Line 18: `import { Loader2, Sparkles, ArrowRight } from "lucide-react";` → `import { Sparkles, ArrowRight } from "lucide-react";`
ADD: `import { BrandLoader } from "@/components/BrandLoader";` (after the lucide import).
Lines 139, 286, 485: `<Loader2 size={14} className="animate-spin" aria-hidden />` → `<BrandLoader size="sm" aria-label="Chargement" />` (all three sites).

Per CONTEXT D-16 — this is the only allowed change to SystemBubble.tsx (and the surrounding RecipeThread directory) in Phase 32. Do NOT modify the bubble structure, copy, or any other prop.

**File: `frontend/components/VoiceModifySheet.tsx`**

Line 17: `import { Loader2 } from "lucide-react";` → DELETE.
ADD: `import { BrandLoader } from "@/components/BrandLoader";`
Line 109: `<Loader2 className="mr-2 h-4 w-4 animate-spin" />` → `<BrandLoader size="sm" className="mr-2" />`

**File: `frontend/components/SearchInput.tsx`**

Line 13: `import { Search, X, Loader2 } from "lucide-react";` → `import { Search, X } from "lucide-react";`
ADD: `import { BrandLoader } from "@/components/BrandLoader";`
Lines 101-104: 
```tsx
<Loader2
  aria-hidden
  className="h-4 w-4 animate-spin text-foreground-muted"
/>
```
→
```tsx
<BrandLoader size="sm" aria-label="Chargement" className="text-foreground-muted" />
```
The comment on line 4 referencing `Loader2 spinner` can be left as historical (it describes the surface, not the implementation); if it bothers cleanliness, update to `BrandLoader spinner`.

**File: `frontend/app/onboarding/create/page.tsx`**

Line 7: `import { ChevronLeft, Loader2 } from "lucide-react";` → `import { ChevronLeft } from "lucide-react";`
ADD: `import { BrandLoader } from "@/components/BrandLoader";`
Line 143: `<Loader2 className="animate-spin h-4 w-4 mr-2" aria-hidden />` → `<BrandLoader size="sm" className="mr-2" />`

**File: `frontend/app/onboarding/join/page.tsx`**

Line 7: `import { ChevronLeft, Loader2 } from "lucide-react";` → `import { ChevronLeft } from "lucide-react";`
ADD: `import { BrandLoader } from "@/components/BrandLoader";`
Lines 309-311:
```tsx
<Loader2
  aria-hidden
  className="h-4 w-4 animate-spin text-foreground-muted"
/>
```
→
```tsx
<BrandLoader size="sm" aria-label="Chargement" className="text-foreground-muted" />
```
Line 345: `<Loader2 className="animate-spin h-4 w-4 mr-2" aria-hidden />` → `<BrandLoader size="sm" className="mr-2" />`

**File: `frontend/components/ui/sonner.tsx`** (Pattern 6 — one-line swap)

Line 5: `import { CircleCheckIcon, InfoIcon, TriangleAlertIcon, OctagonXIcon, Loader2Icon } from "lucide-react"` → `import { CircleCheckIcon, InfoIcon, TriangleAlertIcon, OctagonXIcon } from "lucide-react"`
ADD (after lucide imports): `import { BrandLoader } from "@/components/BrandLoader"`
Lines 27-29: 
```tsx
loading: (
  <Loader2Icon className="size-4 animate-spin" />
),
```
→
```tsx
loading: (
  <BrandLoader size="sm" />
),
```

Per CONTEXT D-14 (strict grep gate at plan close) + D-15 (single BrandLoader export, no Spinner shim).
  </action>
  <verify>
    <automated>cd frontend && grep -rn "Loader2\|Loader2Icon\|animate-spin" components/ app/ 2>/dev/null | grep -v node_modules | grep -v ".next" | grep -v "BrandLoader.tsx" | wc -l && grep -rn "BrandLoader" components/ app/ 2>/dev/null | grep -v node_modules | wc -l && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l && npx next build --webpack 2>&1 | grep -E "Failed|Error " | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - **GREP GATE (mandatory, D-14):** `grep -rn "animate-spin\|Spinner\|LoadingSpinner" frontend/ --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=tests` returns 0 matches that are NOT inside `BrandLoader.tsx`. Use: `grep -rn "animate-spin\\|Spinner\\|LoadingSpinner" frontend/components/ frontend/app/ frontend/lib/ 2>/dev/null | grep -v BrandLoader.tsx | wc -l` returns 0.
    - **Loader2 import gate:** `grep -rn "from \"lucide-react\"" frontend/ 2>/dev/null | grep -v node_modules | grep -v .next | xargs -I {} echo {} | grep -E "Loader2" | wc -l` returns 0
    - **Loader2Icon import gate (sonner specifically):** `grep -n "Loader2Icon" frontend/components/ui/sonner.tsx` returns 0
    - **BrandLoader adoption gate:** `grep -rn "BrandLoader" frontend/components/ frontend/app/ 2>/dev/null | grep -v node_modules | wc -l` returns at least 13 (1 per call-site + the source file + Toaster)
    - `grep -c "BrandLoader" frontend/components/HomeDecide.tsx` returns at least 2 (2 sites)
    - `grep -c "BrandLoader" frontend/components/RecipeThread/SystemBubble.tsx` returns at least 3 (3 sites)
    - `grep -c "BrandLoader" frontend/app/onboarding/join/page.tsx` returns at least 2 (2 sites)
    - `grep -c "BrandLoader" frontend/components/ui/sonner.tsx` returns at least 1 (Toaster icons.loading)
    - `grep -c "Loader2Icon" frontend/components/ui/sonner.tsx` returns 0 (import removed)
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l` returns 0
    - `cd frontend && npx next build --webpack 2>&1 | grep -E "Failed|Error " | wc -l` returns 0
    - **Invariant #2 backend grep (regression guard):** `grep -rn "state.*column\\|vote_state.*Mapped" backend/app/models/ 2>/dev/null | wc -l` returns 0 (no new state column on votes/daily_shortlist_recipes — this plan should not have touched backend)
    - **Invariant #4 backend grep:** `git diff --name-only HEAD | grep "^backend/" | wc -l` returns 0 in this plan's diff (Phase 32 is frontend-only)
    - **PinLabel untouched:** `git diff HEAD frontend/components/RecipeThread/PinLabel.tsx 2>/dev/null | wc -l` returns 0 (no changes — D-05 lock)
  </acceptance_criteria>
  <done>Strict grep gate green: zero `animate-spin` / `Spinner` / `LoadingSpinner` / `Loader2` / `Loader2Icon` references outside BrandLoader.tsx itself. All 12 sites swept atomically. PinLabel.tsx untouched. Backend untouched. Build + TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→React tree | Primitives render text/SVG only; no user input crosses to API |
| TableVote → lib/votes.ts | TableVote consumes typed VoteValue / ShortlistVote; no string concatenation into SQL or HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | I (Information disclosure) | TableVote renders member names + initials | accept | Member names are intra-household data already broadcast via realtime; TableVote does not expose any new data path or aggregate that wasn't already visible in shortlist views. |
| T-32-02-02 | T (Tampering) | LedgerCard inline style with --patina from props | accept | `patina` is typed as `0 | 1 | 2 | 3` (discriminated union); cookCountToPatina (next plan) returns this exact type from an integer `cook_count`. No string-typed value reaches the inline style; no XSS risk. |
| T-32-02-03 | I | BrandLoader composes BrandIcon SVG | accept | BrandIcon SVG is hand-authored, static paths — no user content interpolated. No `dangerouslySetInnerHTML` used anywhere in primitives. |
| T-32-02-04 | T | Marginalia renders ReactNode children | accept | React text-node default behavior — children are escaped HTML entities. No `dangerouslySetInnerHTML` used. Consumers in 32-03..05 must pass plain strings (no rich HTML). |
| T-32-02-05 | T | TableVote derives state from votes prop | mitigate | The `seatStateClass` function is a pure mapping (aggregate + memberVote → class string); no user-controlled value reaches the class string (all class names are static literals). Inline style `background: var(--color-member-${slot}-bg)` interpolates only the slot name from a discriminated union via `memberSlot()`. |

This plan introduces visual primitives that render text-as-children and SVG. No new auth surface, no API endpoint, no localStorage. Sonner Toaster's `icons.loading` prop accepts an existing approved component (BrandLoader) — no behavioral change to the toast pipeline.
</threat_model>

<verification>
**At plan close (32-02 is the grep-gate plan):**
1. `grep -rn "animate-spin\|Spinner\|LoadingSpinner" frontend/components/ frontend/app/ frontend/lib/ | grep -v BrandLoader.tsx | wc -l` returns 0
2. `grep -rn "Loader2\|Loader2Icon" frontend/ --exclude-dir=node_modules --exclude-dir=.next | wc -l` returns 0
3. `cd frontend && npx next build --webpack` succeeds
4. `cd frontend && npx tsc --noEmit` returns no errors
5. Manual: dev server up, navigate to `/recipes/new`, submit a draft → confirm BrandLoader (handwritten-stroke animation) replaces the old Loader2 spinner in the SystemBubble.
6. Manual: trigger any sonner toast (e.g. a vote action) → confirm loading state shows BrandLoader, not the lucide spinner.
7. Manual: visit `/recipes` (search input), type a query → BrandLoader appears in the input during the debounce.
8. iOS PWA Caveat manual gate (§15.D) — recommended to run at this plan close before 32-03 starts: build a production bundle (`npx next build --webpack`), serve via `next start`, open in iOS Safari → Add to Home Screen → launch standalone → navigate to `/recipes/[id]` with a manually-edited recipe → confirm PinLabel renders in handwritten Caveat (not generic cursive).
9. Manual: visually inspect `<BrandLoader size="sm">` at 18px — confirm the stroke animation is visible and not jagged (A4 resolution check).
</verification>

<success_criteria>
- 4 new primitive components exported with stated APIs (Marginalia, BrandLoader, LedgerCard, TableVote)
- 12 spinner call-sites swept to BrandLoader
- Sonner Toaster icons.loading uses BrandLoader
- Zero `animate-spin` / `Spinner` / `LoadingSpinner` / `Loader2` / `Loader2Icon` references outside BrandLoader.tsx
- PinLabel.tsx untouched (Phase 28 contract)
- TableVote consumes computeVoteState — no state column on backend (invariant #2)
- No new broadcast_to_household events (invariant #4)
- TypeScript + webpack build clean
- iOS PWA Caveat manual check (§15.D gate) recorded
</success_criteria>

<output>
After completion, create `.planning/phases/32-port-locked-screens-to-sober-kitchen/32-02-SUMMARY.md` summarizing:
- 4 primitives created (file paths + exported API surface)
- 12 spinner sites swept (file:line pairs + before/after token forms)
- Grep gate results (verbatim grep counts)
- Open question A4 status: BrandLoader sm visual quality at 18px verified / needs adjustment
- iOS PWA Caveat §15.D gate result
- Any deviations from D-14 strict gate (should be none)
- Conteste per-seat A6 resolution: applied per UI-SPEC §7.2 (yes voter = seat-state-valide, no voter = seat-state-contested) — note visual gate at 32-03 close
</output>
