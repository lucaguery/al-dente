---
phase: 07-decide-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/HomeDecide.tsx
autonomous: true
requirements:
  - DECIDE-01
must_haves:
  truths:
    - "User opens the daily-decide screen and sees a Fraunces-italic display-serif date header (e.g., 'vendredi 8 mai') sitting above the deck — reads as a cookbook chapter date, not a UI timestamp."
    - "User sees the date rendered via the standard browser Intl.DateTimeFormat API in French locale ('fr-FR') with weekday + day + month — no year (too granular for daily-decide)."
    - "User experiences the 'chip → header → deck → thumb buttons → bottom nav' visual rhythm with the new header inserted between any cold-start chip and the deck/summary phase swap."
    - "User in a no-shortlist state still sees the page render correctly — the header is conditional on having content below it, OR (alternative) sits above EmptyState, per executor judgment within UI-SPEC bounds."
    - "All existing behavioral logic (initial fetch, three realtime listeners, vote / regenerate / delegate / cook / banner-skip handlers) is byte-for-byte preserved."
  artifacts:
    - path: "frontend/components/HomeDecide.tsx"
      provides: "Display-serif date header above the deck/summary phase swap, using Phase 5 text-display token + browser Intl.DateTimeFormat('fr-FR')"
      contains: "text-display"
  key_links:
    - from: "frontend/components/HomeDecide.tsx"
      to: "Phase 5 typography (text-display Fraunces italic)"
      via: "<h1 className='text-display ...'>{formattedDate}</h1>"
      pattern: "text-display"
    - from: "frontend/components/HomeDecide.tsx"
      to: "Browser Intl API (no new i18n key required)"
      via: "new Intl.DateTimeFormat('fr-FR', { weekday: 'long', day: 'numeric', month: 'long' }).format(new Date())"
      pattern: "Intl.DateTimeFormat"
---

<objective>
Close DECIDE-01 (daily shortlist screen re-themed with new tokens) by adding a Fraunces-italic display-serif date header to `HomeDecide.tsx` between the existing cold-start chip and the deck/summary phase swap.

The deliverable is **one new `<header>` block** at the top of the deck-phase render. It uses:
- Phase 5's `text-display` utility class (Fraunces italic, weight 500, opsz=96, clamp 32–44px) per UI-SPEC §"Typography > Date header above deck".
- The browser `Intl.DateTimeFormat('fr-FR', { weekday: 'long', day: 'numeric', month: 'long' })` API to produce the French-locale date string ("vendredi 8 mai") — **NO new i18n key**, per UI-SPEC §"Surface 1" point 3 ("locale-aware via the standard browser `Intl` API").

Section spacing inherits the existing flat `flex flex-col flex-1` outer container; the new header uses `pt-8 pb-2` (32px top, 8px bottom) to give the date display-text breathing room above the deck. This matches the visual rhythm specified in UI-SPEC §"Surface 1" point 2: chip (`mt-4`) → header (`pt-8 pb-2`) → deck (`pt-4`) → thumb buttons (`pb-8`) → bottom nav.

This plan also explicitly does NOT:
- Wrap the date in a Card (UI-SPEC §"Surface 1" point 4 chooses NO Card wrap — the page already has multiple cards below; adding a date Card would over-compose).
- Touch any behavioral logic (initial fetch, realtime listeners, handlers) — UI-SPEC: "The cosmetic header addition is the only direct change to HomeDecide.tsx."
- Add a new i18n key (browser Intl is locale-aware).

Output: 1 file modified (~440 LOC → ~445 LOC after the header insertion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-decide-polish/07-CONTEXT.md
@.planning/phases/07-decide-polish/07-UI-SPEC.md
@.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@.planning/notes/v0.2-design-direction.md
@frontend/CLAUDE.md
@frontend/AGENTS.md

<interfaces>
<!-- Existing render structure in HomeDecide.tsx (the executor will INSERT the header at the indicated point in the "shortlist exists" return branch). -->

From `frontend/components/HomeDecide.tsx` (existing — the deck-phase render at lines 389–432):
```tsx
return (
  <div className="flex flex-col flex-1">
    <PushPermissionBanner />
    {cookingBannerVisible && activeLog && (
      <CookingBanner
        logId={activeLog.id}
        recipeTitle={recipeTitleFor(activeLog.recipe_id, shortlist.recipes)}
        onSkip={handleBannerSkip}
      />
    )}
    {showCorpusColdStart && <ColdStartChip />}

    {/* ← Phase 7 INSERTS the <header> with the date HERE, immediately above the conditional render below. */}

    {allVoted ? (
      <VoteSummary
        recipes={dealableRecipes}
        votes={shortlist.votes}
        me={me}
        partner={partner}
        memberCount={MEMBER_COUNT}
        onCookStart={handleCookStart}
        onDelegate={handleDelegate}
        onRegenerate={() => setRegenOpen(true)}
        cookInFlight={cookInFlight}
        delegateInFlight={delegateInFlight}
      />
    ) : (
      <ShortlistDeck
        shortlistId={shortlist.shortlist_id}
        recipes={unvotedByMe}
        votes={shortlist.votes}
        me={me}
        partner={partner}
        onVoteApplied={handleVoteApplied}
      />
    )}

    <RegenerateSheet
      open={regenOpen}
      onOpenChange={setRegenOpen}
      onApply={handleRegenerate}
      submitting={regenSubmitting}
    />
  </div>
);
```

The "no shortlist for today" branch at lines 345–368 (the EmptyState path) is NOT in scope for the header insertion — UI-SPEC specifies the date header above "the deck/summary phase swap" specifically. The empty-state branch already presents its own EmptyState heading via the inherited Phase 6 EmptyState component.

From Phase 5 type-scale (Phase 5 §Typography, already wired in globals.css @theme):
- `text-display` — Fraunces italic, weight 500, opsz=96, font-size clamp(2rem, 5vw, 2.75rem) (i.e., 32–44px). Italic axis baked into the utility per Phase 5 §Typography (display-serif's editorial moment).
- `text-foreground` — deep ink (warm-cream-on-deep-ink color anchor).

From the browser `Intl` API (locale-aware date formatting):
```ts
new Intl.DateTimeFormat('fr-FR', {
  weekday: 'long',
  day: 'numeric',
  month: 'long',
}).format(new Date());
// Example output: "vendredi 8 mai"
// Lowercase per French convention; no year (too granular for daily-decide context).
```

UI-SPEC §"Surface 1" point 4: NO Card wrap on the header. The date renders as standalone display text against the page background (`bg-background` cream).
</interfaces>
</context>

<read_first>
- **`.planning/phases/07-decide-polish/07-UI-SPEC.md` §"Surface 1 — HomeDecide page (date header + section spacing)"** — the layout block, the section-spacing contract, the rationale for NO Card wrap on the date header, and the Intl.DateTimeFormat usage.
- **`.planning/phases/07-decide-polish/07-UI-SPEC.md` §"Component Inventory > frontend/components/HomeDecide.tsx"** — confirms "The cosmetic header addition is the only direct change to HomeDecide.tsx" + behavioral logic is byte-for-byte preserved.
- **`.planning/phases/07-decide-polish/07-UI-SPEC.md` §"Typography > Date header above deck (HomeDecide — NEW)"** — `text-display` (Fraunces italic, weight 500, opsz=96, clamp 32–44px); editorial date moment.
- **`.planning/phases/07-decide-polish/07-UI-SPEC.md` §"Paper-Grain > HomeDecide date header"** — explicitly: NO paper-grain on the header.
- **`.planning/phases/07-decide-polish/07-CONTEXT.md` §"Daily Shortlist Screen (HomeDecide)"** — the section-spacing inheritance from Phase 5 spacing scale.
- **`.planning/phases/05-design-system-foundation/05-UI-SPEC.md` §Typography** — the `text-display` utility's exact value (Fraunces italic, weight, opsz, font-size clamp). Confirm the utility exists and includes the italic axis automatically.
- **`frontend/components/HomeDecide.tsx`** (entire ~440 LOC) — read end to end before editing. The insertion point is between line 399 (`{showCorpusColdStart && <ColdStartChip />}`) and line 401 (`{allVoted ? (`). The "no shortlist" branch at lines 345–368 is OUT OF SCOPE for this plan.
- **`frontend/AGENTS.md`** — Next.js 16.2.4 has training-data drift; if any framework-specific question arises (none expected; `<header>` is a plain HTML element, `<h1>` likewise, `Intl.DateTimeFormat` is a browser global), consult `frontend/node_modules/next/dist/docs/`.
- **MDN: `Intl.DateTimeFormat`** — confirm `'fr-FR'` locale + `weekday: 'long'` + `day: 'numeric'` + `month: 'long'` produces the expected lowercase French long-form date.
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HomeDecide — add Fraunces-italic date header above the deck/summary phase swap</name>
  <files>frontend/components/HomeDecide.tsx</files>
  <read_first>
    - 07-UI-SPEC.md §"Surface 1 — HomeDecide page" — full section
    - 07-UI-SPEC.md §"Typography > Date header above deck" — the type token
    - 07-UI-SPEC.md §"Component Inventory > HomeDecide.tsx" — the change shape
    - 07-UI-SPEC.md §"Paper-Grain > HomeDecide date header" — NO paper-grain
    - frontend/components/HomeDecide.tsx (full file, ~440 LOC) — read end to end
    - frontend/app/globals.css §typography or @theme — confirm `text-display` utility exists and is Fraunces italic at the Phase 5 spec
  </read_first>
  <action>
    Edit `frontend/components/HomeDecide.tsx` with a single insertion in the "shortlist exists" return branch.

    **Edit 1A — compute the formatted date inside the component body, just before the return statement.** Find the line just before the second `return (` statement at line 389 (the one inside `if (shortlist === null)`'s sibling, NOT the one inside it). Looking at the file structure: lines 386–388 declare `showCorpusColdStart`; line 389 starts `return (`. Insert the date computation BEFORE line 389, immediately after the `showCorpusColdStart` declaration.

    The formatted date constant — INSIDE the component function body, not at module scope (the date must be evaluated at render time so the user sees the current date when they open the app):

    ```tsx
    // Phase 7 / DECIDE-01 — display-serif date header above the deck.
    // Browser Intl.DateTimeFormat with 'fr-FR' locale; no new i18n key per
    // 07-UI-SPEC §"Surface 1" point 3 (locale-aware via the standard
    // browser Intl API). Lowercase per French convention. No year — too
    // granular for daily-decide. Example: "vendredi 8 mai".
    const formattedDate = new Intl.DateTimeFormat("fr-FR", {
      weekday: "long",
      day: "numeric",
      month: "long",
    }).format(new Date());
    ```

    The constant MUST be declared inside the `HomeDecide()` function body. Placing it at module scope would freeze the date at JS-bundle-load time (the user opening the app at midnight would see yesterday's date).

    **Edit 1B — insert the `<header>` JSX block in the "shortlist exists" return branch.** Locate the existing JSX structure:
    ```tsx
    return (
      <div className="flex flex-col flex-1">
        <PushPermissionBanner />
        {cookingBannerVisible && activeLog && (
          <CookingBanner
            logId={activeLog.id}
            recipeTitle={recipeTitleFor(activeLog.recipe_id, shortlist.recipes)}
            onSkip={handleBannerSkip}
          />
        )}
        {showCorpusColdStart && <ColdStartChip />}

        {allVoted ? (
          <VoteSummary ... />
        ) : (
          <ShortlistDeck ... />
        )}
        ...
    ```

    Insert the `<header>` block AFTER the `{showCorpusColdStart && <ColdStartChip />}` line and BEFORE the `{allVoted ? (...)}` ternary. The exact insertion:

    ```tsx
        {showCorpusColdStart && <ColdStartChip />}

        <header className="px-6 pt-8 pb-2">
          <h1 className="text-display text-foreground">
            {formattedDate}
          </h1>
        </header>

        {allVoted ? (
          <VoteSummary ... />
        ) : (
          <ShortlistDeck ... />
        )}
    ```

    Notes:
    - **`<header>`** (semantic HTML5 element) — appropriate landmark for a page-level heading region.
    - **`className="px-6 pt-8 pb-2"`** — 24px horizontal page padding (matches Phase 5 page-edge inset), 32px top, 8px bottom. The 32px top gives breathing room above the deck; the 8px bottom yields rhythm into the deck container's existing `pt-4`.
    - **`<h1 className="text-display text-foreground">`** — `text-display` is the Phase 5 type-scale token for Fraunces italic 32–44px clamp; `text-foreground` is the deep-ink color anchor. The italic axis is baked into the utility per Phase 5 §Typography.
    - **`{formattedDate}`** — text-node interpolation of the const declared in Edit 1A. React's auto-escaping applies (the value is a plain string from a browser API, but escaping is good hygiene).
    - **NO Card wrap** — UI-SPEC §"Paper-Grain > HomeDecide date header" + §"Surface 1" point 4 explicitly chose NO Card wrap. The header sits as standalone display text on the page background.
    - **NO `paper-grain` className** — anti-pattern per Phase 5 invariant (full-page-header chrome stays grain-free).

    **Edit 1C — DO NOT touch the "no shortlist for today" return branch at lines 345–368.** That branch renders the EmptyState component (which already has its own `text-title` heading via Phase 6 retheme). UI-SPEC §"Surface 1" specifies the header above "the deck/summary phase swap" — the empty branch is a separate, pre-rendered branch that does not have the deck/summary phase swap. Adding the header to that branch would create a "vendredi 8 mai" + "Pas encore de shortlist" double-heading composition that competes for visual hierarchy. Leave the empty branch untouched; the date header is exclusive to the shortlist-exists return.

    **Edit 1D — preserve everything else byte-for-byte.** Specifically:
    - The `"use client"` directive — unchanged.
    - The Phase 3 file-header comment — unchanged.
    - All imports — unchanged. **No new imports needed** — `Intl.DateTimeFormat` is a browser global; `<header>` and `<h1>` are HTML elements; `text-display`, `text-foreground`, `px-6`, `pt-8`, `pb-2` are Tailwind classes already wired by Phase 5.
    - The `COOK_BANNER_SKIP_KEY`, `MEMBER_COUNT` constants at lines 50–51 — unchanged.
    - The `Member` type at line 53 — unchanged.
    - The state hooks (lines 60–71): `shortlist`, `shortlistLoaded`, `activeLog`, `bannerSkipped`, `regenOpen`, `regenSubmitting`, `delegateInFlight`, `cookInFlight`, `validéToastedFor` — unchanged.
    - The `me` and `partner` `useMemo`s at lines 76–88 — unchanged.
    - The initial fetch `useEffect` at lines 92+ — unchanged.
    - All realtime listeners (`vote.created`, `shortlist.created`, `cooking.started`, `cooking.finalized`) — unchanged.
    - All handlers (`handleVoteApplied`, `handleDelegate`, `handleRegenerate`, `handleCookStart`, `handleBannerSkip`) — unchanged.
    - The `cookingBannerVisible` const, `dealableRecipes` derivation, `myVotes` Set, `unvotedByMe`, `allVoted`, `showCorpusColdStart` derivations — unchanged.
    - The "no shortlist" early return branch at lines 345–368 — unchanged (Edit 1C).
    - The `recipeTitleFor` helper at lines 435+ — unchanged.

    **Why this exact insertion:** the date header reads as a cookbook chapter divider — the user opens the app, sees "vendredi 8 mai" in Fraunces italic at 32–44px, and the daily-decide loop frames itself as today's chapter. Browser Intl.DateTimeFormat handles French locale + diacritics + day/month names correctly; no i18n key needed. The 32px top / 8px bottom asymmetry pulls the header up away from the chip and lets it breathe; the deck container's existing `pt-4` resumes rhythm into the cards below.
  </action>
  <acceptance_criteria>
    - `grep -c "Intl.DateTimeFormat" frontend/components/HomeDecide.tsx` returns 1.
    - `grep -c "fr-FR" frontend/components/HomeDecide.tsx` returns 1.
    - `grep -c "weekday: \"long\"\|day: \"numeric\"\|month: \"long\"" frontend/components/HomeDecide.tsx` returns ≥ 3 (one per option key, in the order specified).
    - `grep -c "text-display" frontend/components/HomeDecide.tsx` returns 1.
    - `grep -c "<header" frontend/components/HomeDecide.tsx` returns 1.
    - `grep -c "<h1" frontend/components/HomeDecide.tsx` returns 1.
    - `grep -c "formattedDate" frontend/components/HomeDecide.tsx` returns ≥ 2 (declaration + JSX interpolation).
    - `grep -c "px-6 pt-8 pb-2" frontend/components/HomeDecide.tsx` returns 1.
    - `git diff frontend/lib/i18n/fr.json` shows zero changes (no new i18n key).
    - The "no shortlist" branch (the early-return at line 345 area) is NOT modified — `grep -A 25 "if (shortlist === null)" frontend/components/HomeDecide.tsx` shows the same `<EmptyState ... />` markup as before, no `<header>` or `text-display` insertion.
    - All existing handlers preserved: `grep -c "handleVoteApplied\|handleDelegate\|handleRegenerate\|handleCookStart\|handleBannerSkip" frontend/components/HomeDecide.tsx` returns ≥ 5.
    - All realtime DOM-event listener bindings preserved: `grep -c "VOTE_CREATED_DOM_EVENT\|SHORTLIST_CREATED_DOM_EVENT\|COOKING_STARTED_DOM_EVENT\|COOKING_FINALIZED_DOM_EVENT" frontend/components/HomeDecide.tsx` returns ≥ 4.
    - `cd frontend && npx tsc --noEmit` passes.
    - `cd frontend && npm run lint` passes.
    - `cd frontend && npm run dev` boots; the daily-decide screen renders the new header without console errors when a shortlist is present.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "Intl.DateTimeFormat" components/HomeDecide.tsx && grep -q "fr-FR" components/HomeDecide.tsx && grep -q "text-display" components/HomeDecide.tsx && grep -q "<header" components/HomeDecide.tsx && grep -q "formattedDate" components/HomeDecide.tsx && grep -q "px-6 pt-8 pb-2" components/HomeDecide.tsx && [ "$(grep -c "handleVoteApplied\|handleDelegate\|handleRegenerate\|handleCookStart\|handleBannerSkip" components/HomeDecide.tsx)" -ge 5 ] && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>
    HomeDecide.tsx renders a Fraunces-italic display-serif date header (`text-display`) above the deck/summary phase swap, populated by `Intl.DateTimeFormat('fr-FR', { weekday: 'long', day: 'numeric', month: 'long' })`. The header sits between any cold-start chip and the deck/summary, with `px-6 pt-8 pb-2` spacing. All behavioral logic (initial fetch, four realtime listeners, all handlers, all derivations) is byte-for-byte preserved. The "no shortlist" early-return branch is untouched. ZERO new i18n keys.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — Phase 7 introduces zero new HTTP routes, zero new data inputs, zero new auth surfaces. This plan touches a single client component (`HomeDecide.tsx`) that already orchestrates the full daily-decide loop including realtime DOM events from `RealtimeProvider`. **Phase 7 does NOT modify any handler, any realtime binding, any fetch call, or any data flow — it only inserts a new `<header>` block populated by a browser Intl API.**

The new code runs `Intl.DateTimeFormat('fr-FR', ...)` on `new Date()` — both browser globals — and renders the resulting plain string inside React's auto-escaping JSX. No untrusted input, no external network call, no storage write.

All trust boundaries inherited from Phase 01.1 (cookie-auth) and v0.1 (RealtimeProvider DOM events + shortlist data layer) remain unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Spoofing | `new Date()` in the date header | accept | The user's local clock is the only source. A user spoofing their own clock affects only their own date display — a UX inconvenience, not a security issue. The realtime spine (vote.created, shortlist.created) operates on server-emitted events and is unaffected. |
| T-07-04-02 | Information Disclosure | `formattedDate` rendered in `<h1>` | accept | The date string ("vendredi 8 mai") contains no PII, no household data, no recipe data. It is identical for any user opening the app on the same calendar day in the fr-FR locale. |
| T-07-04-03 | XSS via formattedDate | `<h1>{formattedDate}</h1>` | accept | `Intl.DateTimeFormat.prototype.format()` returns a sanitized plain string, not HTML. React's text-node escaping applies to the interpolation. Verification: `grep -c "dangerouslySetInnerHTML" frontend/components/HomeDecide.tsx` returns 0. |
| T-07-04-04 | Tampering | `Intl.DateTimeFormat` browser global | accept | Browser-provided platform API. Tampering would require running malicious code in the browser context, which is a higher-class threat than ASVS L1 covers. |
| T-07-04-05 | Inherited — realtime spine | DOM CustomEvents (vote.created, shortlist.created, cooking.started, cooking.finalized) | accept | **Inherited from v0.1.** Phase 7 does NOT modify the listener bindings or the event-handling logic. The Phase 7 header insertion is layout-only. |

**Verification check (automated):** `grep -c "dangerouslySetInnerHTML" frontend/components/HomeDecide.tsx` returns 0.
</threat_model>

<verification>
After Task 1 completes:

1. `cd frontend && npm run lint` — PASSES.
2. `cd frontend && npx tsc --noEmit` — PASSES.
3. Phase 7 grep queries (UI-SPEC §"Verification queries"):
   - Query #15: `grep -n "text-display\|Intl.DateTimeFormat" frontend/components/HomeDecide.tsx` — at least 1 hit on each.
   - Query #14: `git diff frontend/lib/i18n/fr.json` — zero changes.
4. Behavioral preservation check (custom):
   - `grep -c "handleVoteApplied\|handleDelegate\|handleRegenerate\|handleCookStart\|handleBannerSkip" frontend/components/HomeDecide.tsx` returns ≥ 5.
   - `grep -c "VOTE_CREATED_DOM_EVENT\|SHORTLIST_CREATED_DOM_EVENT\|COOKING_STARTED_DOM_EVENT\|COOKING_FINALIZED_DOM_EVENT" frontend/components/HomeDecide.tsx` returns ≥ 4.
   - `grep -c "fetchTodayShortlist\|getActiveCookingLog" frontend/components/HomeDecide.tsx` returns ≥ 2 (initial fetch preserved).
5. `cd frontend && npm run dev` boots; the daily-decide screen with a shortlist present renders the new header.
6. Real-device smoke (deferred to Phase 7's combined orchestrator-owned test): open the daily-decide screen on iPhone Safari PWA → confirm the Fraunces-italic date "vendredi 8 mai" (or current day) renders above the deck. Confirm French diacritics in date strings (e.g. "août", "février") render crisply at the PWA-compressed size.
7. STRIDE verification: `grep -c "dangerouslySetInnerHTML" frontend/components/HomeDecide.tsx` returns 0.
</verification>

<success_criteria>
- HomeDecide.tsx renders a `<header className="px-6 pt-8 pb-2"><h1 className="text-display text-foreground">{formattedDate}</h1></header>` block between the cold-start chip and the deck/summary phase swap.
- `formattedDate` is computed at render time via `new Intl.DateTimeFormat('fr-FR', { weekday: 'long', day: 'numeric', month: 'long' }).format(new Date())`.
- NO new i18n key.
- NO Card wrap on the header (per UI-SPEC anti-pattern guard).
- NO paper-grain on the header (Phase 5 anti-pattern: chrome stays grain-free).
- The "no shortlist" early-return branch is untouched.
- All behavioral logic byte-for-byte preserved: initial fetch, 4 realtime listeners, all handlers, all derivations, the `recipeTitleFor` helper, the state hooks, the `me`/`partner` `useMemo`s.
- `npm run lint` + `npx tsc --noEmit` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-decide-polish/07-04-SUMMARY.md` documenting:
- The exact insertion point of the header block (line numbers post-edit).
- The exact `formattedDate` const declaration (with comment).
- The exact `<header>` + `<h1>` JSX block.
- Confirmation that the "no shortlist" early-return branch is untouched.
- Confirmation that initial fetch, all 4 realtime listeners, all handlers, and the `recipeTitleFor` helper are byte-for-byte preserved (with grep counts).
- Confirmation that `git diff frontend/lib/i18n/fr.json` shows zero changes.
- Output of all 3 grep verification queries (text-display + Intl.DateTimeFormat + handler-preservation count).
- Note on the date format produced (e.g., "vendredi 8 mai") and confirmation that the lowercase French convention is respected by Intl.DateTimeFormat with the `'fr-FR'` locale.
</output>
</content>
</invoke>