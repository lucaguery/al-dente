---
phase: 38-endpoint-contract-invariant-coverage
plan: "04"
type: execute
wave: 4
depends_on: ["38-03"]
files_modified:
  - backend/tests/test_architecture_invariants.py
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05, INV-06, INV-07, INV-08, COV-01]
tags: [pytest, architecture-invariants, regression-tests, coverage-floor, COV-01]
must_haves:
  truths:
    - "8 named regression tests exist, one per CLAUDE.md architecture invariant (INV-01..08)"
    - "Each invariant test passes when the invariant holds AND demonstrably fails when the invariant is violated (D-38-03 break-observe-revert ritual completed for each)"
    - "Repo-wide TOTAL line coverage is ≥ 85% — COV-01 closed"
    - "No source file under backend/app/ is modified by this plan (break-observe-revert leaves working tree clean)"
  artifacts:
    - path: "backend/tests/test_architecture_invariants.py"
      provides: "8 named regression tests in a single file (or split per discretion)"
      contains: "test_invariant_01_all_five_capture_surfaces_dispatch_through_promote_draft|test_invariant_02_votes_table_has_no_state_column|test_invariant_03_cooking_log_atomically_updates_last_cooked_at|test_invariant_04_household_mutations_broadcast|test_invariant_05_first_user_turn_position_zero_immutable|test_invariant_06_single_apscheduler_job_per_household|test_invariant_07_aldente_auth_cookie_wins_over_bearer|test_invariant_08_httpexception_details_not_raw_english"
  key_links:
    - from: "test_invariant_02 (no state column)"
      to: "models/vote.py"
      via: "inspect.getmembers OR SQLAlchemy table.columns iteration"
      pattern: "Vote.__table__.columns"
    - from: "test_invariant_04 (broadcast)"
      to: "services/realtime.broadcast_to_household"
      via: "monkeypatch + call-count assertion"
      pattern: "broadcast_to_household"
    - from: "test_invariant_07 (cookie wins)"
      to: "auth.py _extract_token"
      via: "TestClient with both cookie and Bearer headers set"
      pattern: "_extract_token|AUTH_COOKIE_NAME"
    - from: "test_invariant_08 (French-only HTTPException)"
      to: "every routers/*.py HTTPException raise site"
      via: "import all routers, walk APIRouter routes, dispatch known-bad inputs, collect detail strings"
      pattern: "HTTPException.*detail"
---

<objective>
Land 8 named architecture-invariant regression tests — one per CLAUDE.md invariant — then close COV-01 (repo-wide line coverage ≥ 85%). This is the **last test-only plan** in v0.8: after this ships, every CLAUDE.md invariant has a regression net and Phase 39 can lock the coverage floor in CI.

**Locked invariant mapping (CLAUDE.md ↔ REQUIREMENTS.md INV-XX):**

| INV-XX | CLAUDE.md # | Topic | Spy / assertion point |
|--------|-------------|-------|----------------------|
| INV-01 | #1 | All 5 capture surfaces dispatch through `promote_draft(recipe_id)` | grep routers/recipes.py for capture endpoints; assert they each schedule `promote_draft` (or its BackgroundTask) |
| INV-02 | #2 | Votes table has no `state` column; vote state computed via `services/voting.compute_vote_state` | `Vote.__table__.columns` introspection — assert no column named `state` exists |
| INV-03 | #3 | `recipes.last_cooked_at` and `recipes.cook_count` update atomically with `cooking_logs` insert (same DB transaction) | insert a CookingLog via the router; assert both `last_cooked_at` and `cook_count` were updated; assert the update happens in the same transaction (no partial state visible) |
| INV-04 | #4 | Every household-mutation endpoint broadcasts via `services/realtime.broadcast_to_household` | monkeypatch `broadcast_to_household` to capture calls; trigger a mutation (e.g. POST /recipes/quick); assert call count ≥ 1 |
| INV-05 | #5 | First user turn (position 0) preserves capture payload verbatim and is immutable; `recipes.source_capture` column no longer exists | inspect `Recipe.__table__.columns` for absence of `source_capture`; create a recipe, inspect `RecipeTurn(position=0)` payload matches input; attempt to PATCH/UPDATE the position-0 turn and assert it's rejected (no API endpoint exists OR direct DB write succeeds but is not exposed through API — assert the latter) |
| INV-06 | #7 | APScheduler runs in-process and registers exactly one shortlist job per household at lifespan startup | inspect `app.main.scheduler.get_jobs()` after a household is created via API; assert exactly one job with the household's id in the job-id; assert the scheduler is the module-level singleton (not re-instantiated per worker) |
| INV-07 | #8 | `aldente_auth` HttpOnly cookie wins over Bearer-header fallback (mutually exclusive) | TestClient request with BOTH a valid cookie and a Bearer header where Bearer points at a DIFFERENT member; assert response uses the cookie's member (server-trusted), NOT the Bearer member |
| INV-08 | #6 | Backend French-only — every `HTTPException(detail=...)` user-visible string is either a French translation key or a recognized internal code, NOT raw English prose | walk every `@router.{get,post,...}` route; collect all HTTPException details from this run; assert no detail matches a "raw English sentence" pattern (multi-word, no underscores, English vocabulary). Per `<interfaces>` block below, the locked convention is **internal English codes** (e.g. `"recipe not found"`, `"shortlist not found"`) — those are not user-visible French strings BUT they ARE recognized internal codes the frontend translates. The test asserts the convention is **consistent**: every detail is either short snake_case-like ("recipe not found", "household full", "HOUSEHOLD_FULL") OR a French string — never multi-sentence English error messages with capitalization. |

**Break-observe-revert ritual (D-38-03):** for each invariant test, the executor:
1. Runs the test (it passes).
2. Applies a 1-line edit to a source file that violates the invariant (e.g. add a `state = Column(String)` to `Vote` for INV-02).
3. Re-runs ONLY that one test — it must fail.
4. `git checkout -- backend/app/...` to revert.
5. Re-runs the test — it passes again.
6. Records both outcomes in 38-04-SUMMARY.md.

After all 8 tests are demonstrated, run the full suite with `--cov=app` and assert TOTAL line coverage ≥ 85%. If under, add targeted gap-closure tests in the same plan (D-38-06).

Purpose: lock CLAUDE.md invariants against regression; close COV-01 to enable Phase 39's CI gate.
Output: 1-2 test files (planner picks consolidated `test_architecture_invariants.py` or split `test_invariant_NN_*.py` per readability), full-suite coverage ≥ 85%, 8 INV-XX + COV-01 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-endpoint-contract-invariant-coverage/38-CONTEXT.md
@.planning/phases/38-endpoint-contract-invariant-coverage/38-03-SUMMARY.md
@./CLAUDE.md
@backend/CLAUDE.md
@backend/tests/conftest.py
@backend/app/main.py
@backend/app/auth.py
@backend/app/services/realtime.py
@backend/app/routers/recipes.py

<interfaces>
<!-- HTTPException detail convention (confirmed by reading routers in 38-CONTEXT.md gather step): -->
<!-- All HTTPException details across 10 routers use INTERNAL ENGLISH CODES (short, lowercase, snake-case-ish): -->
<!--   - "recipe not found", "shortlist not found", "cooking log not found" -->
<!--   - "invite_code not found", "household not found" -->
<!--   - "color already taken by another member" -->
<!--   - "endpoint must be https://" -->
<!--   - Dict form: {"detail": "household full", "code": "HOUSEHOLD_FULL", "max_members": 5} -->
<!-- -->
<!-- These are NOT user-visible French; they are internal codes the frontend translates via next-intl. -->
<!-- INV-08's test asserts CONSISTENCY of this convention: no multi-sentence English prose, no leaked raw English (e.g. "An error occurred while processing your request."). -->

INV-04 spy pattern (matches 37-03 LLM TurnKind test approach):
    from unittest.mock import AsyncMock
    monkeypatch.setattr(
        "app.services.realtime.broadcast_to_household",
        AsyncMock(),
    )
    # ALSO monkeypatch the named import inside routers/recipes.py if it imports as `from app.services.realtime import broadcast_to_household`:
    monkeypatch.setattr(
        "app.routers.recipes.broadcast_to_household",
        AsyncMock(),
        raising=False,
    )

INV-07 cookie-wins pattern (matches 37-02 test_auth_unit.py approach):
    # The seeded member has Bearer=SEED_TOKEN. Create a SECOND member with a different token.
    # Set TestClient cookies to SEED_TOKEN (member A); set Authorization to "Bearer <member-B-token>".
    # Hit GET /auth/ws-token — assert response.json()["token"] == SEED_TOKEN (member A, the cookie's owner), NOT member B's token.
    client.cookies.set(AUTH_COOKIE_NAME, SEED_TOKEN)
    response = client.get("/auth/ws-token", headers={"Authorization": f"Bearer {other_token}"})
    assert response.json()["token"] == SEED_TOKEN

INV-06 scheduler introspection:
    from app.main import scheduler   # module-level AsyncIOScheduler singleton
    jobs = scheduler.get_jobs()
    # Assert exactly one job per known household_id; job ids likely follow pattern f"shortlist_{household_id}".

INV-05 source_capture absence check:
    from app.models.recipe import Recipe
    assert "source_capture" not in Recipe.__table__.columns

INV-02 Vote no state column:
    from app.models.vote import Vote
    assert "state" not in Vote.__table__.columns

**Break suggestions for break-observe-revert (D-38-03), per invariant:**
- INV-01: comment out `promote_draft(...)` call in one capture endpoint in routers/recipes.py
- INV-02: add `state = Column(String, nullable=True)` to models/vote.py
- INV-03: in routers/cooking_logs.py, comment out the `recipe.last_cooked_at = ...` assignment
- INV-04: comment out one `await broadcast_to_household(...)` call in routers/recipes.py
- INV-05: add `source_capture = Column(JSON)` back to models/recipe.py
- INV-06: change the per-household scheduler registration loop's iteration count
- INV-07: in auth.py `_extract_token`, swap the order so Bearer wins over cookie
- INV-08: introduce `HTTPException(500, "An unexpected error occurred while processing your request.")` in one router

For each, the test must fail on the broken source and pass on the reverted source. Executor MUST revert with `git checkout -- backend/app/<file>` before moving to the next invariant; never leave a broken source on disk.
</interfaces>
</context>

<scope_fence>
**Allowed files (this plan):**
- backend/tests/test_architecture_invariants.py (NEW — consolidated) OR `backend/tests/test_invariant_<N>_<slug>.py` × 8 (planner picks). Default to consolidated: easier review, shared imports, single file to extend in Phase 39.
- Additional gap-closure test files (NEW) IF after invariant tests land, repo coverage is still < 85%. D-38-06: target highest-statement-count gaps (most likely `app/services/llm.py` paths not yet exercised).

**Forbidden in this plan:**
- Permanently modifying any file under backend/app/ (the break-observe-revert ritual TOUCHES source momentarily but MUST revert; final `git diff --stat main -- backend/app/` must be empty)
- Adding `fail_under` to backend/pyproject.toml (Phase 39's job — even though COV-01 wording mentions it, D-38-06 explicitly defers the assertion mechanism to Phase 39 per D-37-05)
- Any CI workflow file (Phase 39)
- Any frontend file (out of milestone scope)
- The router contract test files (Plans 38-02/03's scope — do not re-edit)
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Write 8 invariant tests + perform break-observe-revert ritual for each</name>
  <files>backend/tests/test_architecture_invariants.py</files>
  <action>Create `backend/tests/test_architecture_invariants.py` with the 8 test functions named per the `must_haves.artifacts.contains` glob. Each test:

  - Has a docstring citing the CLAUDE.md invariant # and REQ-ID INV-XX it pins.
  - Uses the spy/introspection pattern from the `<interfaces>` block.
  - On pass, prints nothing (standard pytest convention); on fail, the assertion message explicitly names the invariant ("INV-04: broadcast_to_household was never called after POST /recipes/quick — invariant #4 violated").

  For INV-08 (HTTPException details), implement as follows: import every router module, walk `router.routes`, for each route try to dispatch a known-bad request via TestClient that triggers an HTTPException, then collect the response `detail` strings. Assert each detail is short (< 60 chars), lowercase-or-snake_case-or-French (no English capital-letter sentences like "An error occurred"), OR a dict with a `code` key. Documenting the regex/heuristic used in a module-level constant `_INTERNAL_CODE_PATTERN`.

  For INV-04, the spy must monkeypatch the broadcast function AT THE NAMED-IMPORT site in `routers/recipes.py` (not just at `app.services.realtime`) — see the `<interfaces>` block. The realtime import shape in routers determines this; verify at execution time.

  **After writing each test, perform the break-observe-revert ritual (D-38-03):**
  1. Run `pytest tests/test_architecture_invariants.py::test_invariant_NN_<slug> -x` → pass.
  2. Apply the break suggested in `<interfaces>` for that invariant (use Edit tool on the source file).
  3. Re-run the same single test → it MUST fail with a clear assertion message naming the invariant.
  4. `git checkout -- backend/app/<file>` to revert.
  5. Re-run → pass again.
  6. Record the broken-test stderr/stdout snippet (first 5 lines) in a side-buffer to paste into 38-04-SUMMARY.md.

  Do this for ALL 8 invariants sequentially. At the end of this task, `git diff --stat main -- backend/app/` MUST be empty (every break reverted).</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_architecture_invariants.py -v 2>&amp;1 | tail -15 &amp;&amp; cd .. &amp;&amp; git diff --stat main -- backend/app/ | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>8 invariant tests exist, all pass. Each was demonstrated to fail on a broken source via the break-observe-revert ritual. `git diff --stat main -- backend/app/` is empty. Each test's broken-mode output is captured for SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 2: Measure repo coverage; if &lt; 85%, add gap-closure tests (D-38-06)</name>
  <files>backend/tests/test_architecture_invariants.py (extended) OR new gap-closure test files (NEW, planner-discretion)</files>
  <action>Run the full-suite coverage measurement using DOT notation (per D-37-02-03):

  ```
  cd backend &amp;&amp; uv run pytest --cov=app --cov-report=term-missing 2>&amp;1 | tail -40
  ```

  Parse the TOTAL line at the bottom. Expected: ≥ 85% (post-37 was 64.5%; Plans 38-02/03 push routers from ~25-50% to ~80%+; combined with 4 rules-files at 100%, llm.py at ~75%+, and the 8 new invariant tests adding incidental coverage, repo total should land ≥ 85%).

  **If TOTAL &lt; 85%:**
  1. Identify the 3-5 files with the most uncovered lines from `term-missing` output.
  2. Add targeted tests for those gaps in a new file `backend/tests/test_coverage_gap_closure.py` (or extend test_architecture_invariants.py if the gaps are invariant-adjacent).
  3. Re-run coverage measurement.
  4. Repeat until TOTAL ≥ 85% (D-38-06: 85% is a FLOOR; do not raise the bar).

  **If TOTAL ≥ 85% on first run:** record the number and skip the gap-closure step. Do NOT add tests beyond what's needed to clear the floor (per D-38-06: "No raising the bar — 85% is the floor").

  Per D-38-06 wording: "after Plan 38-04 lands the 8 invariant tests, run `pytest --cov=app --cov-report=term-missing` and capture the new repo total. If still under 85% after invariant tests, the same plan (38-04) adds targeted follow-up tests on the highest-statement-count gaps (likely additional llm.py paths)."

  Do NOT add `fail_under = 85` to pyproject.toml in this task — that mechanism lands in Phase 39 (per D-37-05 carried forward). This plan only proves the coverage is reachable; Phase 39 locks it.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest --cov=app --cov-report=term 2>&amp;1 | grep -E "^TOTAL" | awk '{print $NF}' | sed 's/%//' | awk '{ if ($1 + 0 >= 85) print "PASS: " $1 "%"; else { print "FAIL: " $1 "% (need >= 85)"; exit 1 } }'</automated>
  </verify>
  <done>Full-suite repo-wide line coverage TOTAL ≥ 85%, measured via dot-notation `--cov=app`. If gap-closure tests were needed, they are scoped tightly to the highest-statement-count uncovered lines (no scope creep into integration tests for low-impact files).</done>
</task>

<task type="auto">
  <name>Task 3: Confirm clean source tree + write SUMMARY.md + close 9 REQ-IDs</name>
  <files>.planning/phases/38-endpoint-contract-invariant-coverage/38-04-SUMMARY.md</files>
  <action>Final verification + documentation pass.

  Verify `git diff --stat main -- backend/app/` is empty (the break-observe-revert ritual left no residue).

  Write 38-04-SUMMARY.md with:
  - **Invariant mapping table** (the same 8-row table from `<objective>`) confirming the CLAUDE.md ↔ INV-XX mapping is correct.
  - **Break-observe-revert evidence**: for each of the 8 invariants, paste a 3-5 line snippet showing the test failure output when the source was broken (proving D-38-03 was satisfied). Format as a fenced code block per invariant.
  - **Final coverage table**: repo TOTAL line %, branch %, plus per-file % for the 4 rules files (should still be 100%), the 10 routers (should be 75%+), llm.py, and any other file that improved.
  - **REQ-IDs closed**: 9 IDs — INV-01, INV-02, INV-03, INV-04, INV-05, INV-06, INV-07, INV-08, COV-01.
  - **Phase 39 handoff**: this plan proved 85% is reachable; Phase 39 adds the `fail_under = 85` to `pyproject.toml` and wires the CI gate.
  - **Self-check checklist**: 8 tests written ✓ / all 8 demonstrated to fail-on-violation ✓ / repo coverage ≥ 85% ✓ / `git diff --stat main -- backend/app/` empty ✓ / no `fail_under` added (Phase 39's job) ✓.

  Document any follow-up TODOs (e.g. if INV-04 monkeypatch coverage is incomplete because some mutations don't broadcast — surface as a real product bug for `/gsd-quick` follow-up, do NOT patch in this plan per scope_fence).</action>
  <verify>
    <automated>test -f .planning/phases/38-endpoint-contract-invariant-coverage/38-04-SUMMARY.md &amp;&amp; git diff --stat main -- backend/app/ | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>SUMMARY.md exists with the invariant mapping table, break-observe-revert evidence for all 8 invariants, final coverage table, and 9 REQ-IDs marked closed. `git diff --stat main -- backend/app/` is empty. Phase 38 is shippable.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_architecture_invariants.py` → 8 passed, 0 failed.
- Each of the 8 invariants was demonstrated to FAIL when source was broken (output captured in SUMMARY.md per D-38-03).
- `pytest --cov=app --cov-report=term` → TOTAL line ≥ 85% (COV-01 reached).
- `git diff --stat main -- backend/app/` → empty (no source modified permanently).
- INV-XX ↔ CLAUDE.md invariant mapping verified in SUMMARY.md (specifically: INV-06 → CLAUDE.md #7, INV-07 → CLAUDE.md #8, INV-08 → CLAUDE.md #6 — the non-identity mappings called out in the constraints).
</verification>

<success_criteria>
- 8 named architecture-invariant regression tests exist and pass.
- D-38-03 break-observe-revert ritual completed for all 8 (each test demonstrably fails on violated invariant).
- COV-01 closed: repo-wide line coverage ≥ 85%.
- 9 REQ-IDs closed in this plan (INV-01..08 + COV-01).
- Phase 38 closes 19 of 33 milestone requirements (5 from 38-02 + 5 from 38-03 + 9 from 38-04).
- Phase 39 has a clear handoff: add `fail_under = 85` to pyproject.toml + write CI workflow.
</success_criteria>

<output>
Create `.planning/phases/38-endpoint-contract-invariant-coverage/38-04-SUMMARY.md` when done.
</output>
