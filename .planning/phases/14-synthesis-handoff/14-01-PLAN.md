---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v0.3/check-assessment.sh
  - .planning/v0.3/ASSESSMENT.md
autonomous: true
requirements:
  - SYNTH-01
  - SYNTH-02
must_haves:
  truths:
    - "Pre-commit grep gate script exists and exits 0 against an empty-but-correctly-shaped ASSESSMENT.md"
    - "ASSESSMENT.md exists at .planning/v0.3/ASSESSMENT.md"
    - "ASSESSMENT.md contains a '## Ranked findings' section with three '### Tier 1' / '### Tier 2' / '### Tier 3' subsections"
    - "Section 3 (Ranked findings) contains 27 entries total (2 Tier 1 + 8 Tier 2 + 17 Tier 3) per resolved tier-tally projection"
    - "Every entry uses the D-05 schema (Title / Tier / Impact axes / Observed / Where / Pattern (clusters only) / Why this impacts feels-Al-Dente / Sources)"
    - "No forbidden D-05 fields appear (Recommendation / Fix / Action / Proposed Phase / Effort / Priority / Next Step / TODO / When to address / v0.4 plan)"
    - "Draft grep-gate run against the in-progress ASSESSMENT.md after Section 3 draft exits 0 (anti-prescription discipline holds mid-draft)"
  artifacts:
    - path: ".planning/v0.3/check-assessment.sh"
      provides: "D-07/D-08 pre-commit grep gate (composite regex, single command, exit 0/1)"
      contains: "v0\\.4|should (fix|add|build|implement|do|consider|address|tackle|prioritize)"
    - path: ".planning/v0.3/ASSESSMENT.md"
      provides: "Section 3 (Ranked findings) drafted with 27 entries across 3 tiers; Sections 1/2/4 to be composed in Plan 2"
      min_lines: 400
      contains: "## Ranked findings"
  key_links:
    - from: ".planning/v0.3/ASSESSMENT.md (entries)"
      to: ".planning/v0.3/WALKTHROUGH.md"
      via: "anchor cite"
      pattern: "WALKTHROUGH\\.md §"
    - from: ".planning/v0.3/ASSESSMENT.md (entries)"
      to: ".planning/v0.3/UI-AUDIT.md"
      via: "anchor cite"
      pattern: "UI-AUDIT\\.md §"
    - from: ".planning/v0.3/ASSESSMENT.md (entries)"
      to: ".planning/v0.3/ui-reviews/<surface>-UI-REVIEW.md"
      via: "Pillar N anchor"
      pattern: "ui-reviews/.*-UI-REVIEW\\.md Pillar"
    - from: ".planning/v0.3/ASSESSMENT.md (entries)"
      to: "lucaguery/al-dente issues"
      via: "Issue #N reference"
      pattern: "Issue #[1-8]"
    - from: ".planning/v0.3/check-assessment.sh"
      to: ".planning/v0.3/ASSESSMENT.md"
      via: "grep -inE composite regex"
      pattern: "phase \\(1\\[5-9\\]\\|\\[2-9\\]\\[0-9\\]\\)"
---

<objective>
Build the load-bearing content of `.planning/v0.3/ASSESSMENT.md`: implement the D-07/D-08 grep gate as a script, then draft **Section 3 (Ranked findings)** — the 27 ranked entries grouped into Tier 1 (2) / Tier 2 (8) / Tier 3 (17) per the locked D-03 rubric. Apply the schema-enforced finding template (D-05) and observational voice (D-06) to every entry. Cite source artifacts using the inherited Phase 12/13 anchor format.

Purpose: Section 3 is ~70-80% of the document's word count and ~95% of its analytical content. Plan 2 composes the framing sections (Executive summary / Ranking method / Inputs to next milestone) once Section 3 is fixed — this plan locks the entry set, scores, and tier assignments so Plan 2 can compose the framing prose against a known artifact.

Output: `.planning/v0.3/check-assessment.sh` (executable; D-08 composite regex; exits 0/1) AND `.planning/v0.3/ASSESSMENT.md` containing Section 3 in full plus structural placeholders for Sections 1/2/4 (so the file passes the grep gate as drafted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-synthesis-handoff/14-CONTEXT.md
@.planning/phases/14-synthesis-handoff/14-RESEARCH.md
@.planning/v0.3/WALKTHROUGH.md
@.planning/v0.3/UI-AUDIT.md
@CLAUDE.md

<!-- Per-surface UI-REVIEWs are read on-demand for axis-(i) identity-signature scoring evidence. -->
<!-- All 14 files live at .planning/v0.3/ui-reviews/<surface>-UI-REVIEW.md. -->

<resolved_tensions>
<!-- All T-1..T-7 and N-1..N-5 resolutions are PRE-LOCKED in this plan. The executor does NOT re-derive. -->

- **T-1 (MEMBER_COLORS 4 vs 5 swatches):** Cite 5 per UI-AUDIT.md "Cross-cutting observations" bullet 12 reconciliation. Record the WALKTHROUGH-vs-live-code delta as Tier 3 entry B-21-adjacent ("WALKTHROUGH MEMBER_COLORS 4-vs-5 swatch delta"). Issue #7 text reconciliation deferred.
- **T-2 (P-12-Sh-02 severity):** Inherit the post-sweep friction tag. No standalone entry; reconciliation history is NOT documented in Section 3.
- **T-3 (Push three-gap):** ONE cluster entry (B-13 "Push UX three-gap cluster"). Pu-04 audit-environment-only AND Pu-05 operator-deferred fold under Pu-02 friction in this single entry.
- **T-4 (TZ-01 score band):** STRICT rubric — axis (iii):1, total 1, Tier 3 (entry B-8). Cross-surface footprint captured in `Where`/`Sources` fields, not via axis bumping.
- **T-5 (B-16 decorative img a11y):** axis (iii):1 → total 1 → Tier 3. Assistive-tech users ARE users.
- **T-6 (P-12-Pu-01 headless-Chromium audit-blocker):** EXCLUDED from ranking entirely. Documented in Section 1 "Limits of this assessment" by Plan 2.
- **T-7 (Tier 1 size — 2 vs recalibrate):** ACCEPT 2 entries (B-3, B-4). The 2-entry Tier 1 IS the milestone-level finding. Rubric thresholds NOT recalibrated.
- **N-1 (validation-error UX cluster):** ONE merged cluster (C-3 absorbs C-5). Surface set: capture-quick + capture-full + cooking-log + exports + onboarding (race-409). Score (i:0 / ii:0 / iii:1, total 1) → Tier 3.
- **N-2 (history feature decommissioning):** THREE distinct entries (B-5 missing detail route, B-10 missing GET endpoint, B-11 buried IA). Umbrella observation appears in Plan 2's Executive Summary, NOT as a 4th ranked entry.
- **N-3 (5-member capacity propagation):** NO additional cluster entry. B-6 + B-7 stand standalone; the compounding observation appears in B-7's `Why this impacts feels-Al-Dente` field as a citation back to B-6.
- **N-4 (persistent prod-data anomalies):** NOT ranked. Documented in Section 1 "Limits of this assessment" by Plan 2.
- **N-5 (verdict-driving correlation):** NOT ranked. Documented in Plan 2's Executive Summary §1 paragraph 2.
</resolved_tensions>

<final_entry_set>
<!-- The exact 27 entries to draft in Section 3. Names + scores + tier assignments PRE-LOCKED per RESEARCH.md candidate scoring + tension resolutions above. -->
<!-- Order WITHIN tier: by total score descending, axis (i) descending, axis (ii) descending. Per D-02 "ordered-but-not-strict-1..N within tier" — order matters but the doc does NOT over-claim 1-vs-2 precision. -->

<!-- RECONCILIATION TRAIL — RESEARCH.md candidate clusters C-1..C-9 → final 27 entries (no re-derivation needed):
     C-1 → ranked Tier 3 entry 11 (Token-completeness gap, 5 surfaces).
     C-2 → ranked Tier 3 entry 13 (No-debounce-on-submit cluster, 4 surfaces).
     C-3 → ranked Tier 3 entry 14 (Validation-error UX cluster, 5 surfaces; absorbs C-5 per N-1).
     C-4 → ranked Tier 2 entry 3 (Capture-pipeline missing terminal state) — promoted standalone (axis ii:1, total 3) rather than clustered, per RESEARCH §"Candidate Cluster Taxonomy" treatment of axis-ii-bearing patterns.
     C-5 → MERGED into C-3 per N-1 resolution; not a standalone entry. Merged-state preserved in C-3's `Pattern` field (5 surfaces incl. onboarding race-409 and exports offline-clickable).
     C-6 → ranked Tier 3 entry 12 (shadcn-default icons survived re-themeing, 4 surfaces).
     C-7 → UMBRELLA observation (history-feature decommissioning) — surfaced in Plan 2 Executive Summary §1, NOT a ranked entry. Three component findings stay distinct ranked entries (B-5, B-10, B-11) per N-2 resolution.
     C-8 → ranked Tier 3 entry 15 (i18n drift, 3 surfaces).
     C-9 → UMBRELLA observation (verdict-driving correlation, N-5) — surfaced in Plan 2 Executive Summary §1 paragraph 2, NOT a ranked entry per N-5 resolution.
     Result: 5 ranked cluster entries (C-1, C-2, C-3, C-6, C-8) + 2 umbrella observations (C-7, C-9) + 1 standalone-promoted (C-4) + 1 merged-into-other (C-5) = 9 candidates accounted for.
     Standalone blockers B-1..B-21 are unaffected by this trail; see RESEARCH §"Candidate Standalone Blocker Entries" for their derivation. -->

**Tier 1** (total ≥4) — 2 entries
1. **B-3** — Architecture invariant #2 broken: `MEMBER_COUNT=2` hardcoded; vote-state mis-computed in ≠2-member households (Issue #4) — `(i:2 / ii:2 / iii:1, total 5)`
2. **B-4** — Cooking log re-finalize doubles `cook_count` — invariant #3 violated (Issue #5) — `(i:0 / ii:2 / iii:2, total 4)`

**Tier 2** (total 2-3) — 8 entries
3. **C-4** — Capture-pipeline missing terminal state (Gemini-failed-silently) — `(i:0 / ii:1 / iii:2, total 3)`
4. **B-2** — Ingredient parser corrupts `<int> <noun>` lines (Issue #2) — `(i:1 / ii:0 / iii:2, total 3)`
5. **B-5** — Per-log detail route `/cooking-logs/{id}` missing in Next.js (Issue #6) — `(i:0 / ii:1 / iii:2, total 3)`
6. **B-6** — 5-member household at color-palette capacity ceiling — silent failure (Issue #7) — `(i:1 / ii:0 / iii:2, total 3)`
7. **B-7** — `PATCH /api/households/me` returns 405 — member name unchangeable post-onboarding (Issue #8) — `(i:0 / ii:1 / iii:2, total 3)`
8. **B-10** — CL-01: `GET /api/cooking-logs?days=14` endpoint missing; history page renders empty — `(i:0 / ii:1 / iii:2, total 3)`
9. **B-1** — Sheet-01: `paper-grain` overrides Tailwind `fixed` on bottom sheets (Issue #1) — `(i:0 / ii:0 / iii:2, total 2)`
10. **B-13** — Push UX three-gap cluster — `(i:0 / ii:0 / iii:2, total 2)`

**Tier 3** (total 0-1) — 17 entries
11. **C-1** — Token-completeness gap — Tailwind-palette-literal cluster (5 surfaces) — `(i:1 / ii:0 / iii:0, total 1)`
12. **C-6** — shadcn-default icons survived re-themeing (4 surfaces) — `(i:1 / ii:0 / iii:0, total 1)`
13. **C-2** — No-debounce-on-submit cluster (4 surfaces) — `(i:0 / ii:0 / iii:1, total 1)`
14. **C-3** — Validation-error UX cluster (5 surfaces; merged C-3+C-5 per N-1) — `(i:0 / ii:0 / iii:1, total 1)`
15. **C-8** — i18n drift — next-intl invariant #6 (3 surfaces) — `(i:0 / ii:1 / iii:0, total 1)`
16. **B-8** — TZ-01: `func.date(cooked_at) == DateType.today()` UTC vs local-tz mismatch — `(i:0 / ii:0 / iii:1, total 1)`
17. **B-9** — URL-01: URL extraction is `# TODO(productize)`; drafts never promote — `(i:0 / ii:0 / iii:1, total 1)`
18. **B-11** — History feature buried + decommissioned (cross-cutting friction) — `(i:0 / ii:0 / iii:1, total 1)`
19. **B-12** — `cooking.finalized` 7th broadcast event missing from canonical docstring (doc rot) — `(i:0 / ii:1 / iii:0, total 1)`
20. **B-14** — capture-full title-only submit creates orphan `structured` recipe with null ingredients — `(i:0 / ii:0 / iii:1, total 1)`
21. **B-15** — Install-PWA banner occludes vote affordances on first load — `(i:0 / ii:0 / iii:1, total 1)`
22. **B-16** — Decorative `<img>` traps pointer events on shortlist deck card — `(i:0 / ii:0 / iii:1, total 1)`
23. **B-17** — `/onboarding/welcome` reachable for authenticated user; no redirect guard — `(i:0 / ii:0 / iii:1, total 1)`
24. **B-18** — Recipe-detail page has no vote affordance — `(i:0 / ii:0 / iii:1, total 1)`
25. **B-19** — No "Quitter le foyer" path; leaving requires backend intervention — `(i:0 / ii:0 / iii:1, total 1)`
26. **B-20** — Capture tab/button copy drift from documentation — `(i:0 / ii:0 / iii:0, total 0)`
27. **B-21** — POLISH-02 backlog hygiene + MEMBER_COLORS 4→5 swatch audit-time delta — `(i:0 / ii:0 / iii:0, total 0)`

<!-- B-21 absorbs the T-1 audit-time delta as a sub-bullet to keep the entry count at 27 (within D-01 target 25-35). -->
</final_entry_set>

<grep_gate_regex>
<!-- D-08 composite regex; verbatim from RESEARCH.md §"Anti-Prescription Compliance Checklist". -->
<!-- The script wraps this in a `grep && exit 1 || exit 0` shell to invert grep's success-on-match exit code. -->

```
v0\.4|should (fix|add|build|implement|do|consider|address|tackle|prioritize)|\<(recommend|propose|suggest)(ed|s)?\>|must (fix|build|add|implement|do|address|prioritize)|next milestone (should|will|must|needs to)|TODO|action (item|step|plan)|next step|(roadmap|plan).{0,20}(for|of) v0|phase (1[5-9]|[2-9][0-9])
```
</grep_gate_regex>

<schema_template>
<!-- D-05 finding template. The executor uses this verbatim for every entry. -->

```markdown
#### B-NN — <Title — short, descriptive, observational>

- **Tier:** <1 | 2 | 3>
- **Impact axes:** (i:N / ii:N / iii:N, total N)
- **Observed:** <descriptive prose; what was seen. Cites WALKTHROUGH/UI-AUDIT/UI-REVIEW/Issue.>
- **Where:** <file path(s) + line range(s); for clusters, list surface scope.>
- **Pattern:** <CLUSTERS ONLY — recurrence count + surface list. Omit field for standalone blockers.>
- **Why this impacts feels-Al-Dente:** <one paragraph linking observation to the rubric axes (NOT a remediation path).>
- **Sources:** <bullet list of artifact citations.>
```

**Forbidden fields** (D-05): `Recommendation`, `Fix`, `Action`, `Proposed Phase`, `Effort`, `Priority`, `Next Step`, `TODO`, `When to address`, `v0.4 plan`. The grep gate enforces. The executor does NOT add these even with different naming (e.g., "Suggested approach", "What to do next", "Plan for v0.4").
</schema_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the D-07/D-08 grep gate at .planning/v0.3/check-assessment.sh</name>
  <read_first>
    - .planning/phases/14-synthesis-handoff/14-CONTEXT.md (D-07 + D-08 — locked grep pattern set)
    - .planning/phases/14-synthesis-handoff/14-RESEARCH.md §"Anti-Prescription Compliance Checklist" (composite regex verbatim)
    - .planning/v0.3/RUNBOOK.md (file-creation conventions in v0.3/)
  </read_first>
  <files>.planning/v0.3/check-assessment.sh</files>
  <action>
Create an executable shell script at `.planning/v0.3/check-assessment.sh` containing the D-08 composite regex from RESEARCH.md §"Anti-Prescription Compliance Checklist". The script:

1. Sets `TARGET="${1:-.planning/v0.3/ASSESSMENT.md}"` so it can be invoked as `bash check-assessment.sh` (default target) or `bash check-assessment.sh path/to/file.md` (custom target — useful during draft sweeps).
2. Refuses to run if the target file does not exist (exit 2 with stderr message).
3. Runs `grep -inE` with the verbatim D-08 composite alternation:

```
v0\.4|should (fix|add|build|implement|do|consider|address|tackle|prioritize)|\<(recommend|propose|suggest)(ed|s)?\>|must (fix|build|add|implement|do|address|prioritize)|next milestone (should|will|must|needs to)|TODO|action (item|step|plan)|next step|(roadmap|plan).{0,20}(for|of) v0|phase (1[5-9]|[2-9][0-9])
```

4. On match: prints matched lines to stderr, exits 1 (commit blocked).
5. On no match: prints `OK: <TARGET> passes the anti-prescription gate.` to stdout, exits 0.

Implementation skeleton (executor adapts as needed for portability across BSD grep / GNU grep — both must work):

```bash
#!/usr/bin/env bash
# .planning/v0.3/check-assessment.sh
# Phase 14 D-07/D-08 anti-prescription pre-commit grep gate.
# Blocks the SYNTH-02 forbidden-pattern set; allows past-phase citations (Phase 11/12/13).
set -uo pipefail

TARGET="${1:-.planning/v0.3/ASSESSMENT.md}"

if [ ! -f "$TARGET" ]; then
  echo "ERROR: $TARGET does not exist." >&2
  exit 2
fi

PATTERN='v0\.4|should (fix|add|build|implement|do|consider|address|tackle|prioritize)|\<(recommend|propose|suggest)(ed|s)?\>|must (fix|build|add|implement|do|address|prioritize)|next milestone (should|will|must|needs to)|TODO|action (item|step|plan)|next step|(roadmap|plan).{0,20}(for|of) v0|phase (1[5-9]|[2-9][0-9])'

if grep -inE "$PATTERN" "$TARGET" >&2; then
  echo "" >&2
  echo "FAIL: $TARGET contains forbidden patterns. See lines above." >&2
  echo "Anti-prescription gate (D-07/D-08) blocked the commit." >&2
  exit 1
fi

echo "OK: $TARGET passes the anti-prescription gate."
exit 0
```

After writing the file, `chmod +x .planning/v0.3/check-assessment.sh`.

**Self-test the gate** (per CONTEXT specifics — the gate must demonstrably catch a known-bad string):

```bash
# Should exit 1 (matches "v0.4")
echo "v0.4 should fix this" > /tmp/bad-test.md
bash .planning/v0.3/check-assessment.sh /tmp/bad-test.md  # expect exit 1

# Should exit 0 (Phase 11/12/13 allowed)
echo "Per Phase 13 D-02, the verdict is observational." > /tmp/good-test.md
bash .planning/v0.3/check-assessment.sh /tmp/good-test.md  # expect exit 0

rm /tmp/bad-test.md /tmp/good-test.md
```

The self-test runs per CONTEXT specifics ("the regex is itself verifiable by re-running on a known-bad string"). Implements D-07 and D-08 verbatim.
  </action>
  <verify>
    <automated>test -x .planning/v0.3/check-assessment.sh && bash -n .planning/v0.3/check-assessment.sh && printf 'Per Phase 13 D-02 the verdict is observational.\n' > /tmp/good-test.md && bash .planning/v0.3/check-assessment.sh /tmp/good-test.md && printf 'v0.4 should fix this\n' > /tmp/bad-test.md && (bash .planning/v0.3/check-assessment.sh /tmp/bad-test.md; test $? -eq 1) && rm /tmp/good-test.md /tmp/bad-test.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/v0.3/check-assessment.sh`
    - File is executable (`-rwxr-xr-x` or equivalent)
    - `bash -n .planning/v0.3/check-assessment.sh` returns 0 (no syntax errors)
    - Running against a known-good string (`Per Phase 13 D-02 the verdict is observational.`) exits 0
    - Running against a known-bad string (`v0.4 should fix this`) exits 1
    - Script body contains the literal substring `v0\.4`
    - Script body contains the literal substring `phase (1[5-9]|[2-9][0-9])`
    - Script body contains the literal substring `should (fix|add|build|implement|do|consider|address|tackle|prioritize)`
    - Script accepts an optional argument (`$1`) for target path; defaults to `.planning/v0.3/ASSESSMENT.md`
  </acceptance_criteria>
  <done>The grep gate is executable, syntactically valid, demonstrably catches the canonical bad string `v0.4`, and explicitly permits past-phase citations like `Phase 13 D-02`. Plan 2's pre-commit task invokes this exact script unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap ASSESSMENT.md skeleton with section placeholders + draft Section 3 Tier 1 + Tier 2 (10 entries)</name>
  <read_first>
    - .planning/phases/14-synthesis-handoff/14-CONTEXT.md (D-01 D-02 D-03 D-04 D-05 D-06 D-09 — schema, voice, tier mapping, layout)
    - .planning/phases/14-synthesis-handoff/14-RESEARCH.md §"Candidate Cluster Taxonomy" + §"Candidate Standalone Blocker Entries" (entry detail + candidate scores + source citations)
    - .planning/v0.3/WALKTHROUGH.md (read selectively — sections cited by entries 1-10: §Vote, §Cooking-Log, §Capture-Voice, §Capture-Photo, §Capture-URL, §Capture-Full, §History, §Onboarding, §Settings, §Push)
    - .planning/v0.3/UI-AUDIT.md (Cross-cutting observations — bullets 1-13 — and per-surface abstracts for entries 1-10)
    - .planning/v0.3/ui-reviews/vote-UI-REVIEW.md (axis-(i) evidence for B-3)
    - .planning/v0.3/ui-reviews/cooking-log-UI-REVIEW.md (axis-(i) evidence for B-4)
    - .planning/v0.3/ui-reviews/capture-voice-UI-REVIEW.md, capture-photo-UI-REVIEW.md, capture-url-UI-REVIEW.md (C-4 cluster evidence)
    - .planning/v0.3/ui-reviews/capture-full-UI-REVIEW.md (B-2 evidence)
    - .planning/v0.3/ui-reviews/history-UI-REVIEW.md (B-5 + B-10 evidence)
    - .planning/v0.3/ui-reviews/onboarding-UI-REVIEW.md (B-6 evidence)
    - .planning/v0.3/ui-reviews/settings-UI-REVIEW.md (B-7 evidence)
    - .planning/v0.3/ui-reviews/capture-photo-UI-REVIEW.md (B-1 evidence; same file referenced as C-4 cluster member; read once)
    - .planning/v0.3/ui-reviews/push-UI-REVIEW.md (B-13 evidence)
    - GitHub issues #1-#8 are referenced by number; the executor does NOT need to fetch issue body — citations are by Issue # only per Phase 12/13 D-12 (anchor-cite, not quote-excerpt).
    - CLAUDE.md (Architecture invariants 1-8 — load-bearing for axis (ii) evidence; especially #2 voting state computed, #3 denorm same-tx, #4 realtime broadcast)
  </read_first>
  <files>.planning/v0.3/ASSESSMENT.md</files>
  <action>
Create `.planning/v0.3/ASSESSMENT.md` with the full document skeleton (D-09 4-section layout) and draft entries 1 through 10 of Section 3 (the Tier 1 + Tier 2 set, 10 entries). The remaining 17 Tier 3 entries are drafted in Task 3.

**Step 1 — Skeleton:** Write the complete document scaffold so subsequent tasks fill content into known positions:

```markdown
# v0.3 Assessment — Synthesis & Handoff

> Phase 14 of v0.3 (Audit & Uniqueness Foundation). This document combines `WALKTHROUGH.md` (Phase 12) and `UI-AUDIT.md` (Phase 13) into a single ranked-findings list ordered by impact on the "feels Al Dente" question. Descriptive — does not name future phases or remediation paths.

## Executive summary

<!-- Composed in Plan 2 Task 1. Contains: 2-3 paragraphs (milestone-level conclusion, axis-(ii)-drives-Tier-1 observation, verdict-correlation finding from N-5), "How to read this document" subsection, "Calibration notes" subsection, "Limits of this assessment" subsection (T-6 P-12-Pu-01 + N-4 prod-data anomalies + iPhone-shape-Chromium scope + D-16 partial-reach push/history). -->

## Ranking method

<!-- Composed in Plan 2 Task 2. Contains: D-03 3-axis rubric exposed (axis i / ii / iii definitions); 0-2 scale; tier mapping (≥4 / 2-3 / 0-1); tie-breaker rule (axis i → axis ii → axis iii); reference table. -->

## Ranked findings

> 27 entries grouped into three tiers by total impact score per the Ranking method. Within tier, entries ordered by total descending then axis (i) descending. Tier ordering reflects impact on "feels Al Dente", not implementation priority — these are not the same axis.

### Tier 1 — total impact score ≥ 4

<!-- 2 entries, drafted below in this task. -->

### Tier 2 — total impact score 2-3

<!-- 8 entries, drafted below in this task. -->

### Tier 3 — total impact score 0-1

<!-- 17 entries, drafted in Task 3. -->

## Inputs to next /gsd-new-milestone cycle

<!-- Composed in Plan 2 Task 3. Contains: (a) Source artifacts; (b) Open framing questions (3-5 inquiries); (c) Explicit non-prescriptions. -->
```

**Step 2 — Draft Tier 1** (2 entries) using the D-05 schema verbatim. The Tier 1 ordering is locked: B-3 first (total 5), B-4 second (total 4).

For each Tier 1 entry, the prose targets ~250-350 words across all schema fields (these are the load-bearing entries; allow more depth than Tier 3). Use observational voice (D-06): "X is observed", "the surface ships", "the rendered result is" — NOT "we should fix" / "v0.4 must address".

**B-3 — `MEMBER_COUNT=2` hardcoded (Issue #4)**: Cite `WALKTHROUGH.md §Vote P-12-Vt-01` AND `WALKTHROUGH.md §Realtime P-12-RT-4` (cross-surface confirmation at the WS frame layer); cite `ui-reviews/vote-UI-REVIEW.md Pillar 6`; cite `Issue #4`. The `Why this impacts feels-Al-Dente` paragraph: identity-signature direct break (axis i:2) — vote chip pill is per `ui-reviews/vote-UI-REVIEW.md` "one of the most visually-distinctive Slow Food artifacts in the app"; axis (ii):2 — invariant #2 (CLAUDE.md voting state computed not stored) breaks at user-visible chip; axis (iii):1 — primary tap-path completes but produces wrong displayed state.

**B-4 — Cooking log re-finalize doubles `cook_count` (Issue #5)**: Cite `WALKTHROUGH.md §Cooking-Log P-12-CL-01`; cite `ui-reviews/cooking-log-UI-REVIEW.md Pillar 6`; cite `Issue #5`. The `Why this impacts feels-Al-Dente` paragraph: no direct identity impact (axis i:0); axis (ii):2 — invariant #3 (CLAUDE.md denormalized fields update in same DB transaction) breaks at user-visible "Dernière fois : aujourd'hui · Cuisinée 2 fois" after one cook; axis (iii):2 — data corruption surfaces in primary-path display AND propagates to scoring algorithm via `cook_count` recency input.

**Step 3 — Draft Tier 2** (8 entries) in this order (locked by total descending, axis (i) descending tie-break per D-02):

3. **C-4** — Capture-pipeline missing terminal state. Cluster across capture-voice (P-12-V01) + capture-photo (P-12-Ph02) + capture-url (P-12-U01). `Pattern` field: "3 surfaces — voice transcript stuck `(extraction en cours…)` / photo non-recipe stuck same / url drafts never promote per URL-01."
4. **B-2** — Ingredient parser corrupts `<int> <noun>` lines. Cite `WALKTHROUGH.md §Capture-Full P-12-F01`, `Issue #2`. Note possible propagation to capture-voice/photo/url via shared parser.
5. **B-5** — Per-log detail route missing. Cite `WALKTHROUGH.md §History P-12-H-02`, `Issue #6`.
6. **B-6** — 5-member capacity ceiling. Cite `WALKTHROUGH.md §Onboarding P-12-O04`, `Issue #7`. Note in `Where`: "live `frontend/lib/colors.ts` shows 5 swatches — see B-21 for WALKTHROUGH-vs-live-code delta."
7. **B-7** — `PATCH /api/households/me` 405. Cite `WALKTHROUGH.md §Settings P-12-S02`, `Issue #8`. The `Why this impacts feels-Al-Dente` paragraph cites back to B-6 (compounding observation per N-3 resolution).
8. **B-10** — CL-01 missing `GET /api/cooking-logs?days=14`. Cite `WALKTHROUGH.md §History P-12-H-01`. Note combined with B-5 the history feature is "effectively decommissioned" — observational, not prescriptive.
9. **B-1** — Sheet-01 (`paper-grain` overrides Tailwind `fixed`). Cite `WALKTHROUGH.md §Capture-Photo P-12-Ph01`, `Issue #1`.
10. **B-13** — Push UX three-gap cluster. Cite `WALKTHROUGH.md §Push P-12-Pu-02 / Pu-04 / Pu-05`. `Pattern` field: "3 friction-stacking gaps — banner-only entry / no Settings recovery / no admin-test affordance."

**Step 4 — Self-check the draft passes the gate** (per CONTEXT specifics — draft check, NOT pre-commit; the file is incomplete but sections drafted so far must already pass):

```bash
bash .planning/v0.3/check-assessment.sh
# expected: exit 0, "OK: .planning/v0.3/ASSESSMENT.md passes the anti-prescription gate."
```

**Voice discipline reminders for the executor:**
- Subjects are observations ("the chip semantics break in N≠2 households"), not actors ("the team should fix the chip").
- Past-phase citations ("Phase 13 D-02") are PERMITTED. Forward references blocked by gate.
- `Why this impacts feels-Al-Dente` paragraphs link to rubric axes — the words "axis (i)", "axis (ii)", "axis (iii)" are EXPECTED content, NOT forbidden.
- The word "should" only appears blocked when followed by `(fix|add|build|implement|do|consider|address|tackle|prioritize)` — the verb list is closed; "this should be observed" passes the gate. Avoid the closed verb list, period.
- The word "must" similarly blocked only with the closed verb list `(fix|build|add|implement|do|address|prioritize)`. Avoid.
- The bare `TODO` token is blocked. Refer to backlog items by name (`POLISH-02 backlog hygiene`) or `# TODO(productize)` source markers in `Where` field — but quote them inside backticks so the gate matches the literal substring; NOT bare-TODO in prose. Test: if the executor writes `# TODO(productize)` in prose, the gate matches `TODO`. Wrap in backticks: `` `# TODO(productize)` `` keeps the literal token but the surrounding character context is non-word — actually grep doesn't care about backticks, the regex matches the substring `TODO` regardless. **Resolution:** rewrite `# TODO(productize)` source-marker references as "the source-tagged-`# TODO_PRODUCTIZE` marker" with an underscore replacement, OR restructure to avoid quoting the token at all (e.g., "tagged for productize" prose). The B-9 entry MUST handle this carefully — see Task 3 for the same constraint.

Wait — re-reading D-08: `TODO` is blocked outright. The B-9 entry cites `# TODO(productize)` as source-marker syntax. Solution: in B-9 prose, write "tagged for productize" or "marked productize-deferred" without the literal `TODO` substring; cite the source marker location in `Where` field as `recipes.py:481-490` only, without quoting the marker keyword. The gate will not match "productize" alone.
  </action>
  <verify>
    <automated>test -f .planning/v0.3/ASSESSMENT.md && grep -c '^## Executive summary$' .planning/v0.3/ASSESSMENT.md | grep -q '^1$' && grep -c '^## Ranking method$' .planning/v0.3/ASSESSMENT.md | grep -q '^1$' && grep -c '^## Ranked findings$' .planning/v0.3/ASSESSMENT.md | grep -q '^1$' && grep -c '^## Inputs to next /gsd-new-milestone cycle$' .planning/v0.3/ASSESSMENT.md | grep -q '^1$' && grep -c '^### Tier 1' .planning/v0.3/ASSESSMENT.md | grep -q '^1$' && grep -c '^### Tier 2' .planning/v0.3/ASSESSMENT.md | grep -q '^1$' && grep -c '^### Tier 3' .planning/v0.3/ASSESSMENT.md | grep -q '^1$' && bash .planning/v0.3/check-assessment.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v0.3/ASSESSMENT.md` exists
    - Contains exactly one heading `## Executive summary`
    - Contains exactly one heading `## Ranking method`
    - Contains exactly one heading `## Ranked findings`
    - Contains exactly one heading `## Inputs to next /gsd-new-milestone cycle`
    - Contains heading `### Tier 1 — total impact score ≥ 4`
    - Contains heading `### Tier 2 — total impact score 2-3`
    - Contains heading `### Tier 3 — total impact score 0-1` (placeholder; entries follow in Task 3)
    - Section 3 contains 10 entries under Tier 1 + Tier 2 (Tier 1 = 2 entries, Tier 2 = 8 entries) — verify via `awk '/^### Tier 1/,/^### Tier 3/' .planning/v0.3/ASSESSMENT.md | grep -c '^#### '` returns 10
    - Each of the 10 entries contains the schema fields `**Tier:**`, `**Impact axes:**`, `**Observed:**`, `**Where:**`, `**Why this impacts feels-Al-Dente:**`, `**Sources:**` (`**Pattern:**` only on cluster entries C-4 and B-13)
    - No forbidden D-05 fields (`grep -E '\*\*(Recommendation|Fix|Action|Proposed Phase|Effort|Priority|Next Step|When to address|v0\.4 plan):\*\*' .planning/v0.3/ASSESSMENT.md` returns 0 matches)
    - `bash .planning/v0.3/check-assessment.sh` exits 0 (the in-progress draft passes the anti-prescription gate)
    - No literal `v0.4` substring in the in-progress draft — `grep -in 'v0\.4' .planning/v0.3/ASSESSMENT.md` returns 0 matches (mirrors Plan 2 Task 1/3 check; makes the gate-fail diagnostic actionable)
    - Tier 1 entry 1 title contains `MEMBER_COUNT=2` AND cites `Issue #4`
    - Tier 1 entry 2 title contains `cook_count` AND cites `Issue #5`
    - All 10 entries cite at least one of `WALKTHROUGH.md §`, `UI-AUDIT.md §`, `ui-reviews/`, or `Issue #` in their `Sources` field
  </acceptance_criteria>
  <done>ASSESSMENT.md exists with full skeleton + 10 entries (Tier 1 + Tier 2) drafted using D-05 schema and observational voice. The grep gate passes against the in-progress file. Task 3 fills Tier 3.</done>
</task>

<task type="auto">
  <name>Task 3: Draft Section 3 Tier 3 (17 entries) + final draft-pass grep gate</name>
  <read_first>
    - .planning/phases/14-synthesis-handoff/14-CONTEXT.md (D-05 schema; D-06 voice; T-1 / N-1 / N-2 resolutions for B-21 + C-3-merged + B-11)
    - .planning/phases/14-synthesis-handoff/14-RESEARCH.md §"Candidate Cluster Taxonomy" C-1 / C-2 / C-3 / C-6 / C-8 + §"Candidate Standalone Blocker Entries" B-8 / B-9 / B-11 / B-12 / B-14 / B-15 / B-16 / B-17 / B-18 / B-19 / B-20 / B-21 (entry detail + candidate scores)
    - .planning/v0.3/WALKTHROUGH.md (sections cited by Tier 3 entries: §Capture-Quick, §Capture-Full, §Capture-Photo, §Cooking-Log, §Exports, §Realtime, §Shortlist, §Onboarding, §Settings, §History, §Vote)
    - .planning/v0.3/UI-AUDIT.md "Cross-cutting observations" bullets 1, 6, 10, 11, 12 (the cluster-source bullets for C-1/C-6/C-2/C-8/B-21)
    - .planning/v0.3/ui-reviews/shortlist-UI-REVIEW.md (B-15 + B-16 evidence)
    - .planning/v0.3/ui-reviews/exports-UI-REVIEW.md (C-2 + C-6 + C-3 cluster member evidence)
    - .planning/v0.3/ui-reviews/realtime-UI-REVIEW.md (C-1 + C-6 + B-12 evidence)
    - .planning/v0.3/ui-reviews/onboarding-UI-REVIEW.md (B-17 + C-1 evidence; already read in Task 2)
    - .planning/v0.3/ui-reviews/settings-UI-REVIEW.md (B-19 + B-21 evidence; already read in Task 2)
    - PROJECT.md "Surfaced for follow-up (v0.2.2 backlog)" (B-8 / B-9 / B-21 backlog cross-link evidence)
  </read_first>
  <files>.planning/v0.3/ASSESSMENT.md</files>
  <action>
Append the 17 Tier 3 entries to Section 3 below the `### Tier 3 — total impact score 0-1` heading. Order locked: by total descending (1, 1, 1, ..., 0, 0), then axis (i) descending tie-break, then axis (ii) descending tie-break.

**Order (entries 11-27):**

11. **C-1** — Token-completeness gap (Tailwind-palette-literal cluster, 5 surfaces) — `(i:1 / ii:0 / iii:0, total 1)` — axis (i):1 gives precedence over later total-1 entries.
12. **C-6** — shadcn-default icons survived re-themeing (4 surfaces) — `(i:1 / ii:0 / iii:0, total 1)`.
13. **C-8** — i18n drift / next-intl invariant #6 (3 surfaces) — `(i:0 / ii:1 / iii:0, total 1)`.
14. **B-12** — `cooking.finalized` 7th broadcast event missing from canonical docstring (doc rot) — `(i:0 / ii:1 / iii:0, total 1)`.
15. **C-2** — No-debounce-on-submit cluster (4 surfaces) — `(i:0 / ii:0 / iii:1, total 1)`.
16. **C-3** — Validation-error UX cluster (5 surfaces; merged C-3+C-5 per N-1) — `(i:0 / ii:0 / iii:1, total 1)`.
17. **B-8** — TZ-01 UTC vs local-tz mismatch — `(i:0 / ii:0 / iii:1, total 1)`.
18. **B-9** — URL-01 URL extraction `# TODO(productize)` (see voice note below for the literal-token constraint) — `(i:0 / ii:0 / iii:1, total 1)`.
19. **B-11** — History feature buried + decommissioned — `(i:0 / ii:0 / iii:1, total 1)`.
20. **B-14** — capture-full title-only submit creates orphan structured recipe — `(i:0 / ii:0 / iii:1, total 1)`.
21. **B-15** — Install-PWA banner occludes vote affordances — `(i:0 / ii:0 / iii:1, total 1)`.
22. **B-16** — Decorative `<img>` traps pointer events on shortlist deck card — `(i:0 / ii:0 / iii:1, total 1)`.
23. **B-17** — `/onboarding/welcome` reachable for authenticated user — `(i:0 / ii:0 / iii:1, total 1)`.
24. **B-18** — Recipe-detail page has no vote affordance — `(i:0 / ii:0 / iii:1, total 1)`.
25. **B-19** — No "Quitter le foyer" path — `(i:0 / ii:0 / iii:1, total 1)`.
26. **B-20** — Capture tab/button copy drift from documentation — `(i:0 / ii:0 / iii:0, total 0)`.
27. **B-21** — POLISH-02 backlog hygiene + MEMBER_COLORS 4→5 swatch audit-time delta (combined per T-1 resolution) — `(i:0 / ii:0 / iii:0, total 0)`.

Each Tier 3 entry targets ~120-200 words (lighter than Tier 1/2). Use the D-05 schema; cluster entries (C-1, C-2, C-3, C-6, C-8) include the `Pattern` field with surface scope; standalone entries (B-8 through B-21) omit the `Pattern` field.

**Specific composition notes per entry:**

- **C-1 (Token-completeness gap):** `Pattern` = "5 surfaces — shortlist OUI thumb (`text-emerald-500`) / vote validé chip border (`border-emerald-500/50`) / cooking-log ChefHat (`text-emerald-700`) / realtime cooking-banner ChefHat / onboarding `MEMBER_COLORS` raw hex." `Why this impacts feels-Al-Dente`: Token-completeness is part of Phase 13 D-02's "feels Al Dente" hybrid definition; `globals.css` documents emerald (h≈145) as intentional Slow Food, but the implementation does not yet expose it as a semantic token. Identity-signature impact is peripheral (axis i:1) — the rendered colors ARE Slow Food; the system gap is internal.
- **C-6 (shadcn-default icons):** `Pattern` = "4 surfaces — exports `Download` / push `Bell` / cooking-log `ChefHat` / realtime `ChefHat`."
- **C-8 (i18n drift):** `Pattern` = "3 surfaces — settings hardcoded `Historique`/`Voir les cuissons récentes` (`page.tsx:175-183`) / HomeDecide partner-waiting strings (POLISH-01 cross-link) / cooking-log offline toast." Axis (ii):1 — invariant #6 broken at code layer but masked from user (rendered French is correct).
- **C-2 (No-debounce-on-submit):** `Pattern` = "4 surfaces — capture-quick (P-12-Q03 double-tap creates 2 drafts) / capture-full (cross-link) / capture-photo / exports (P-12-E03 rapid double-fetch)."
- **C-3 (Validation-error UX merged):** `Pattern` = "5 surfaces — capture-quick `Connexion impossible` for 422 (P-12-Q02) / capture-full propagated / cooking-log raw Pydantic 4000-char cap (P-12-CL-02) / exports offline-clickable button (P-12-E02) / onboarding race-409 missing recovery copy (P-12-O05)." Note in `Observed`: "Per N-1 resolution, this entry merges the C-3 toast cluster and the C-5 missing-recovery-copy cluster into one ranked entry."
- **B-9 (URL-01) — voice constraint:** The original WALKTHROUGH text and `recipes.py:481-490` source carry the `# TODO(productize)` marker. The bare `TODO` token is blocked by the gate. The executor MUST refer to the marker as "the source-tagged productize-deferred marker at `recipes.py:481-490`" or "the productize-tagged stub" — NOT quote `# TODO(productize)` literally. The `Where` field cites `backend/app/api/recipes.py:481-490` without quoting the marker keyword.
- **B-11 (History feature buried):** `Why this impacts feels-Al-Dente`: per N-2 resolution, this entry stays distinct from B-5 + B-10 even though combined the surface is "effectively decommissioned"; the IA-only finding (no main-nav link) is the stand-alone friction independent of route + endpoint. Cite cross-link to B-5 and B-10 in this entry's `Sources` field.
- **B-21 (POLISH-02 + MEMBER_COLORS delta):** Combine the POLISH-02 backlog-hygiene observation with the T-1 MEMBER_COLORS 4→5 audit-time delta in one entry. Two `Where` fields: `frontend/app/settings/page.tsx:154-162` (Copy button shipped) AND `frontend/lib/colors.ts:1-7` (5 swatches confirmed). `Why this impacts feels-Al-Dente`: zero impact on any axis; pure backlog-hygiene + WALKTHROUGH-vs-live-code observation. Sources: `WALKTHROUGH.md §Settings P-12-S01`, `UI-AUDIT.md "Cross-cutting observations" bullet 12`, `WALKTHROUGH.md §Onboarding O-04` (4-swatch claim), `frontend/lib/colors.ts:1-7` (live-code 5-swatch state).

**Step 2 — Run the draft-pass grep gate** (per CONTEXT specifics — first of the two-pass pattern; pre-commit pass is in Plan 2):

```bash
bash .planning/v0.3/check-assessment.sh
# expected: exit 0
```

**If the gate exits 1**, the executor:
1. Reads the matched lines from stderr.
2. Rewrites the offending phrase using observational voice (e.g., "v0.4 should fix" → "the gap is observed at <where>").
3. Re-runs the gate.
4. Iterates until exit 0.

The executor does NOT bypass the gate. The gate is non-bypassable per D-07.

**Final entry count check:**
```bash
awk '/^### Tier 1/,/^## Inputs to next /' .planning/v0.3/ASSESSMENT.md | grep -c '^#### '
# expected: 27
```
  </action>
  <verify>
    <automated>test -f .planning/v0.3/ASSESSMENT.md && [ "$(awk '/^### Tier 1/,/^## Inputs to next /' .planning/v0.3/ASSESSMENT.md | grep -c '^#### ')" = "27" ] && [ "$(awk '/^### Tier 3/,/^## Inputs to next /' .planning/v0.3/ASSESSMENT.md | grep -c '^#### ')" = "17" ] && bash .planning/v0.3/check-assessment.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v0.3/ASSESSMENT.md` Section 3 contains exactly 27 entries (Tier 1: 2; Tier 2: 8; Tier 3: 17). Verify: `awk '/^### Tier 1/,/^## Inputs to next /' .planning/v0.3/ASSESSMENT.md | grep -c '^#### '` returns 27
    - Tier 3 contains exactly 17 entries: `awk '/^### Tier 3/,/^## Inputs to next /' .planning/v0.3/ASSESSMENT.md | grep -c '^#### '` returns 17
    - All 5 Tier 3 cluster entries (C-1, C-2, C-3, C-6, C-8) include the `**Pattern:**` field
    - Standalone Tier 3 entries (B-8 through B-21) do NOT include `**Pattern:**` field
    - No forbidden D-05 fields appear: `grep -E '\*\*(Recommendation|Fix|Action|Proposed Phase|Effort|Priority|Next Step|When to address|v0\.4 plan):\*\*' .planning/v0.3/ASSESSMENT.md` returns 0 matches
    - B-9 entry does NOT contain the literal substring `# TODO` or `TODO(productize)` (gate would block) — `grep -in 'TODO' .planning/v0.3/ASSESSMENT.md` returns 0 matches
    - B-21 entry contains BOTH `POLISH-02` AND `MEMBER_COLORS` (combined per T-1 resolution)
    - C-3 entry mentions 5 surfaces in `Pattern` field (merged per N-1 resolution)
    - `bash .planning/v0.3/check-assessment.sh` exits 0 — anti-prescription gate passes against the full Section 3 draft
  </acceptance_criteria>
  <done>Section 3 (Ranked findings) is fully drafted with 27 entries across 3 tiers, all using the D-05 schema and observational voice. The grep gate passes. Plan 2 composes Sections 1, 2, 4 against this fixed entry set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auditor → ASSESSMENT.md | Synthesis writer (Claude) authors prose into the doc. No untrusted input crosses; no user-submitted data; no auth surface. |
| Repo → grep gate | Pre-commit script reads ASSESSMENT.md and applies forbidden-pattern regex. Local-only; no network. |

## STRIDE Threat Register

Phase 14 has **no attack surface change**. The phase produces only `.planning/v0.3/ASSESSMENT.md` and `.planning/v0.3/check-assessment.sh`. No code, no API, no auth, no data flow. The grep gate (D-07/D-08) is itself a security-adjacent control: it prevents the document from drifting into v0.4 prescription which would conflict with the SYNTH-02 success criterion.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I (Information disclosure) | ASSESSMENT.md citation excerpts | mitigate | Per Phase 13 D-12 + Phase 12 D-04 inheritance — citations are by header anchor (e.g., `WALKTHROUGH.md §Capture-Full P-12-F01`), NOT by quote excerpt. The schema (D-05 `Sources` field) requires anchor-cite format; the executor is reminded in Task 2/3 action prose. No PII, no secrets, no auth tokens cross into ASSESSMENT.md. |
| T-14-02 | T (Tampering — gate false negative) | check-assessment.sh regex | mitigate | The composite regex is codified verbatim in 14-RESEARCH.md §"Anti-Prescription Compliance Checklist" and reproduced in this plan body. Task 1 self-tests the gate against a known-bad string (`v0.4 should fix this`) and a known-good string (`Per Phase 13 D-02...`) — confirms the regex matches forward-construction and permits past-phase citations. Cross-platform: the regex uses POSIX `grep -inE` syntax compatible with both BSD grep (macOS) and GNU grep (Linux); fallback to `ripgrep` documented in 14-RESEARCH.md §"Environment Availability" if a BSD-grep regex variant emerges. |
| T-14-03 | E (Elevation of privilege via plan creep) | Executor scope discipline | accept | Per `feedback_executor_scope_creep` memory + audit-only milestone discipline (CLAUDE.md). The `files_modified` frontmatter lists ONLY `.planning/v0.3/ASSESSMENT.md` and `.planning/v0.3/check-assessment.sh`. The executor pattern is well-established across Phase 12 / Phase 13 — both produced only `.planning/v0.3/` artifacts with no product code edits. Risk accepted. |
</threat_model>

<verification>
**Plan 1 phase-level checks** (run after Task 3 completes):

1. Files exist:
   - `test -x .planning/v0.3/check-assessment.sh` (executable)
   - `test -f .planning/v0.3/ASSESSMENT.md` (regular file)

2. Document structure (4 top-level sections per D-09):
   - `grep -c '^## Executive summary$' .planning/v0.3/ASSESSMENT.md` → `1`
   - `grep -c '^## Ranking method$' .planning/v0.3/ASSESSMENT.md` → `1`
   - `grep -c '^## Ranked findings$' .planning/v0.3/ASSESSMENT.md` → `1`
   - `grep -c '^## Inputs to next /gsd-new-milestone cycle$' .planning/v0.3/ASSESSMENT.md` → `1`

3. Tier subsections (per D-02):
   - `grep -c '^### Tier 1' .planning/v0.3/ASSESSMENT.md` → `1`
   - `grep -c '^### Tier 2' .planning/v0.3/ASSESSMENT.md` → `1`
   - `grep -c '^### Tier 3' .planning/v0.3/ASSESSMENT.md` → `1`

4. Entry count (per D-01 25-35 target; locked at 27):
   - `awk '/^### Tier 1/,/^## Inputs to next /' .planning/v0.3/ASSESSMENT.md | grep -c '^#### '` → `27`
   - Tier 1: `awk '/^### Tier 1/,/^### Tier 2/' .planning/v0.3/ASSESSMENT.md | grep -c '^#### '` → `2`
   - Tier 2: `awk '/^### Tier 2/,/^### Tier 3/' .planning/v0.3/ASSESSMENT.md | grep -c '^#### '` → `8`
   - Tier 3: `awk '/^### Tier 3/,/^## Inputs to next /' .planning/v0.3/ASSESSMENT.md | grep -c '^#### '` → `17`

5. Schema-field discipline (per D-05):
   - Forbidden fields absent: `grep -cE '\*\*(Recommendation|Fix|Action|Proposed Phase|Effort|Priority|Next Step|When to address|v0\.4 plan):\*\*' .planning/v0.3/ASSESSMENT.md` → `0`
   - All 27 entries have `**Tier:**`: `grep -c '^- \*\*Tier:\*\*' .planning/v0.3/ASSESSMENT.md` → `27`
   - All 27 entries have `**Impact axes:**`: `grep -c '^- \*\*Impact axes:\*\*' .planning/v0.3/ASSESSMENT.md` → `27`
   - All 27 entries have `**Sources:**`: `grep -c '^- \*\*Sources:\*\*' .planning/v0.3/ASSESSMENT.md` → `27`

6. Anti-prescription gate (D-07/D-08):
   - `bash .planning/v0.3/check-assessment.sh` → exit 0

7. Citation discipline (per Phase 12 D-04 + Phase 13 D-12):
   - At least 20 entries cite `WALKTHROUGH.md §`: `grep -c 'WALKTHROUGH\.md §' .planning/v0.3/ASSESSMENT.md` → `≥20`
   - At least 10 entries cite `ui-reviews/`: `grep -c 'ui-reviews/' .planning/v0.3/ASSESSMENT.md` → `≥10`
   - GitHub issues #1-#8 each appear at least once: `grep -cE 'Issue #[1-8]' .planning/v0.3/ASSESSMENT.md` → `≥8`
</verification>

<success_criteria>
**SYNTH-01 (partial — Section 3 ranked findings list):** ✅ when 27 entries exist across Tier 1/2/3 in `.planning/v0.3/ASSESSMENT.md` per the locked tier-tally projection AND each entry cites its source artifact in the `Sources` field. Plan 2 closes the SYNTH-01 by composing Section 1's executive summary that frames the ranked list.

**SYNTH-02 (partial — anti-prescription gate active):** ✅ when `bash .planning/v0.3/check-assessment.sh` exits 0 against the in-progress draft AND the script self-tests demonstrate it catches `v0.4` and permits `Phase 13`. Plan 2 closes SYNTH-02 by running the gate one final time pre-commit (the second of the two-pass pattern per CONTEXT specifics).

**SYNTH-03:** Not closed by this plan — composed in Plan 2 (Section 4 "Inputs to next /gsd-new-milestone cycle").
</success_criteria>

<output>
After Plan 1 completes, create `.planning/phases/14-synthesis-handoff/14-01-SUMMARY.md` documenting: (a) the locked entry set (27 entries across 3 tiers); (b) tier-tally confirmation; (c) tension resolutions applied (T-1 through T-7, N-1 through N-5); (d) any executor-surfaced issues during drafting; (e) draft-pass grep-gate result. Plan 2 reads this summary to compose the Executive Summary and verify the entry set has not drifted.
</output>
