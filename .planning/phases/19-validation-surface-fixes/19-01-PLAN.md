---
phase: 19-validation-surface-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/ui/sheet.tsx
  - frontend/tests/e2e/capture-photo.spec.ts
autonomous: true
requirements: [VAL-01]
must_haves:
  truths:
    - "User opens PhotoUploader 'Ajouter une photo' on /recipes/new — Caméra and Photothèque buttons render fully within the 390×844 iPhone-shape viewport."
    - "Sibling Sheet consumers (VoiceModifySheet on /recipes/new voice tab, RegenerateSheet on /decide) also render in-viewport — the fix is at the shared primitive."
    - "capture-photo.spec.ts 'photo upload sheet is reachable on iPhone-sized viewports' is no longer marked test.fixme; it passes under the seeded Playwright project with toBeInViewport() assertions on Caméra + Photothèque."
  artifacts:
    - path: frontend/components/ui/sheet.tsx
      provides: "Radix Sheet wrapper without paper-grain class on SheetContent"
      contains: "data-slot=\"sheet-content\""
    - path: frontend/tests/e2e/capture-photo.spec.ts
      provides: "Photo capture spec with active (not test.fixme) viewport assertion"
      contains: "toBeInViewport"
  key_links:
    - from: frontend/components/ui/sheet.tsx
      to: frontend/app/globals.css
      via: ".paper-grain > * { position: relative }" rule is no longer reached by SheetContent (rule itself stays — Card / Dialog / Popover still rely on it)
      pattern: "paper-grain"
    - from: frontend/tests/e2e/capture-photo.spec.ts
      to: frontend/components/ui/sheet.tsx
      via: "Playwright drives SheetContent open via 'Ajouter une photo' trigger"
      pattern: "Ajouter une photo"
---

<objective>
Close VAL-01 / Sheet-01 / Issue #1.

Drop the `paper-grain` class from `SheetContent` in `frontend/components/ui/sheet.tsx:65` so the global `.paper-grain > * { position: relative }` rule in `frontend/app/globals.css:345-348` stops overriding Radix's `fixed` positioning. Sibling Sheet consumers (PhotoUploader source picker, VoiceModifySheet, RegenerateSheet) re-render inside the iPhone-shape viewport.

Re-enable the `capture-photo.spec.ts` "photo upload sheet is reachable" spec — strip the `test.fixme` marker so the existing `toBeInViewport()` assertions execute under the seeded Playwright project.

Purpose: Sheet-01 has gated `capture-photo.spec.ts` since v0.2.1 Phase 10 (2026-05-09). Closing it removes the longest-standing test.fixme in the suite and unblocks the bottom-sheet capture surface on real iPhones.

Output: 2 files edited, 1 Playwright spec activated, no new files created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-validation-surface-fixes/19-CONTEXT.md
@frontend/components/ui/sheet.tsx
@frontend/app/globals.css
@frontend/tests/e2e/capture-photo.spec.ts

<interfaces>
<!-- Extracted from frontend/components/ui/sheet.tsx — the executor only edits one className. -->

Current SheetContent className (line 65, the offending bit is `paper-grain`):
```tsx
className={cn(
  "paper-grain fixed z-50 flex flex-col gap-4 bg-popover bg-clip-padding text-sm text-popover-foreground shadow-card-hover transition duration-200 ease-in-out data-[side=bottom]:inset-x-0 data-[side=bottom]:bottom-0 ...",
  className
)}
```

Target: drop the leading `paper-grain ` token. Keep `bg-popover` (the visible surface) and `shadow-card-hover` (the depth cue). Sheet is a short-lived modal — texture isn't load-bearing for design identity (per D-19-03; Phase 21 may revisit under Pillar 6).

The `.paper-grain` rule (`frontend/app/globals.css:329-348`) STAYS — Card / Dialog / Popover content depend on it. Only `SheetContent` stops opting in.
</interfaces>

<existing_test>
```ts
// frontend/tests/e2e/capture-photo.spec.ts:73-105 — current state
// eslint-disable-next-line playwright/no-skipped-test -- real product bug surfaced 2026-05-09; see TODO below
test.fixme(
  'photo upload sheet is reachable on iPhone-sized viewports',
  async ({ page }) => {
    // TODO(productize): components/ui/sheet.tsx — `paper-grain` class on
    // SheetContent overrides Tailwind `fixed`, leaving the bottom-sheet
    // positioned in document flow at top: 702px. ...
    await page.goto('/recipes/new');
    await page.getByRole('tab', { name: 'Photo' }).click();
    const trigger = page.getByRole('button', { name: 'Ajouter une photo' });
    await expect(trigger).toBeVisible();
    await expect(trigger).toBeInViewport();
    await trigger.click();
    const camera = page.getByRole('button', { name: 'Caméra' });
    const library = page.getByRole('button', { name: 'Photothèque' });
    await expect(camera).toBeVisible();
    await expect(camera).toBeInViewport();
    await expect(library).toBeVisible();
    await expect(library).toBeInViewport();
  },
);
```

Target shape:
- Strip the `test.fixme(...)` call → `test(...)`.
- Remove the eslint-disable comment (no longer needed once test runs).
- Replace the `TODO(productize): ...` block inside the test body with a one-liner explaining what the spec guards now (`// Guards VAL-01 / Sheet-01 — components/ui/sheet.tsx must not re-add 'paper-grain' to SheetContent.`).
</existing_test>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop paper-grain from SheetContent</name>
  <files>frontend/components/ui/sheet.tsx</files>
  <read_first>
    - frontend/components/ui/sheet.tsx (full file — 148 lines)
    - frontend/app/globals.css:329-348 (the .paper-grain rule we are dodging — DO NOT delete the rule itself; Card/Dialog/Popover still need it)
    - frontend/tests/e2e/capture-photo.spec.ts:73-105 (test that proves the fix)
  </read_first>
  <behavior>
    - SheetContent renders without the `paper-grain` class.
    - SheetContent still renders with `bg-popover` (visible surface) and `shadow-card-hover` (depth) — only the texture wrapper is removed.
    - Sibling Sheet consumers (PhotoUploader source picker at `/recipes/new` Photo tab, VoiceModifySheet at voice tab, RegenerateSheet at /decide) all stay functional — they use the same primitive.
  </behavior>
  <action>
    In `frontend/components/ui/sheet.tsx:65`, edit the SheetContent `cn(...)` className argument:

    Change:
    ```
    "paper-grain fixed z-50 flex flex-col gap-4 bg-popover bg-clip-padding text-sm text-popover-foreground shadow-card-hover transition duration-200 ease-in-out data-[side=bottom]:inset-x-0 ..."
    ```
    To (drop the leading `paper-grain ` token, KEEP everything else byte-identical):
    ```
    "fixed z-50 flex flex-col gap-4 bg-popover bg-clip-padding text-sm text-popover-foreground shadow-card-hover transition duration-200 ease-in-out data-[side=bottom]:inset-x-0 ..."
    ```

    Add a one-line comment ABOVE the `className={cn(` line documenting WHY:
    `// VAL-01 / Sheet-01: paper-grain on SheetContent triggered .paper-grain > * { position: relative } in globals.css, overriding Tailwind 'fixed' and pushing the sheet off-screen on iPhone-shape viewports. Per D-19-03 the texture is not load-bearing on short-lived modal surfaces — drop entirely. DO NOT re-add without first solving the position-override regression at the inner-wrapper layer.`

    Do NOT modify globals.css. Do NOT modify Card.tsx / Dialog.tsx / Popover.tsx — those still need `.paper-grain`.
  </action>
  <acceptance_criteria>
    - `grep -nE '"paper-grain' frontend/components/ui/sheet.tsx` returns ZERO matches.
    - `grep -n 'bg-popover' frontend/components/ui/sheet.tsx` still returns exactly 1 match (the SheetContent line).
    - `grep -n 'shadow-card-hover' frontend/components/ui/sheet.tsx` still returns exactly 1 match.
    - `grep -n 'VAL-01' frontend/components/ui/sheet.tsx` returns 1 match (the new explanatory comment).
    - `cd frontend && npx eslint components/ui/sheet.tsx` exits 0 (no new lint errors).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `.paper-grain` rule in globals.css UNCHANGED (`grep -nE '\.paper-grain' frontend/app/globals.css | wc -l` returns at least 3 — the same lines that existed before).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q 'paper-grain' components/ui/sheet.tsx && echo "FAIL: paper-grain still present" && exit 1; grep -q 'VAL-01' components/ui/sheet.tsx && grep -q 'bg-popover' components/ui/sheet.tsx && npx tsc --noEmit && npx eslint components/ui/sheet.tsx</automated>
  </verify>
  <done>SheetContent renders without the paper-grain class; type-check + lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate capture-photo viewport spec</name>
  <files>frontend/tests/e2e/capture-photo.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/capture-photo.spec.ts (full file — 107 lines)
    - frontend/playwright.config.ts (confirms the seeded project + 390×844 viewport)
    - The SheetContent file you just edited in Task 1 — the fix MUST already be on disk before activating the spec
  </read_first>
  <behavior>
    - Spec "photo upload sheet is reachable on iPhone-sized viewports" runs (not skipped) under the seeded Playwright project.
    - Spec passes: trigger button visible+in-viewport, Caméra button visible+in-viewport, Photothèque button visible+in-viewport.
    - First spec ("photo capture promotes via canned stub") is UNCHANGED — same describe block, no fixme drift.
  </behavior>
  <action>
    In `frontend/tests/e2e/capture-photo.spec.ts:73-105`:

    1. Remove the `// eslint-disable-next-line playwright/no-skipped-test ...` comment (line 73).
    2. Change `test.fixme(` to `test(` on line 74.
    3. Inside the test body, replace the multi-line `// TODO(productize): components/ui/sheet.tsx — ...` comment block (lines 77-85) with a single-line guard comment:
       `// Guards VAL-01 / Sheet-01 (closed v0.4 Phase 19). components/ui/sheet.tsx must not re-add 'paper-grain' to SheetContent — the .paper-grain > * { position: relative } rule in globals.css would override Radix 'fixed' and push the sheet off-screen.`
    4. Leave the assertion body (lines 86-104) IDENTICAL — the `toBeInViewport()` chain is the existing structural assertion.
    5. DO NOT modify the first spec in the file ("photo capture promotes via canned stub").
  </action>
  <acceptance_criteria>
    - `grep -nE 'test\.fixme' frontend/tests/e2e/capture-photo.spec.ts` returns ZERO matches.
    - `grep -nE 'playwright/no-skipped-test' frontend/tests/e2e/capture-photo.spec.ts` returns ZERO matches.
    - `grep -n 'VAL-01' frontend/tests/e2e/capture-photo.spec.ts` returns 1 match.
    - `grep -nE 'photo upload sheet is reachable' frontend/tests/e2e/capture-photo.spec.ts` returns 1 match (still the same spec name).
    - `grep -c 'toBeInViewport' frontend/tests/e2e/capture-photo.spec.ts` returns 3 (trigger + camera + library — unchanged).
    - The spec runs under the seeded project and passes (run via `npx playwright test capture-photo.spec.ts --project=seeded` — depends on test DB + dev backend running; if either is down, executor records the missing dependency rather than landing a green test).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q 'test\.fixme' tests/e2e/capture-photo.spec.ts && echo "FAIL: fixme still present" && exit 1; grep -q 'VAL-01' tests/e2e/capture-photo.spec.ts && npx tsc --noEmit && npx playwright test tests/e2e/capture-photo.spec.ts --project=seeded --reporter=line</automated>
  </verify>
  <done>The spec runs and passes; the test.fixme marker is gone; structural assertion (toBeInViewport on Caméra + Photothèque) executes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | VAL-01 is a CSS / className edit. No auth surface, no input handling, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering | frontend/components/ui/sheet.tsx | accept | CSS-only change to a presentational primitive. No write paths, no auth. Re-add prevention is via the explanatory comment + the Playwright guard. |
| T-19-01-02 | Information Disclosure | n/a | accept | No PII or sensitive data flows through Sheet primitive. |
</threat_model>

<verification>
- `grep -nE '"paper-grain' frontend/components/ui/sheet.tsx` returns 0.
- `grep -nE 'test\.fixme' frontend/tests/e2e/capture-photo.spec.ts` returns 0.
- `cd frontend && npx tsc --noEmit` exits 0.
- `cd frontend && npx eslint components/ui/sheet.tsx tests/e2e/capture-photo.spec.ts` exits 0.
- `cd frontend && npx playwright test tests/e2e/capture-photo.spec.ts --project=seeded` passes (both specs).
- Manual sanity (dev server): open /recipes/new on the 390×844 Chromium viewport, tap Photo tab, tap "Ajouter une photo" — Caméra + Photothèque are visible and tappable without scrolling.
</verification>

<success_criteria>
1. SheetContent no longer carries `paper-grain` — confirmed by grep.
2. `test.fixme` removed from `capture-photo.spec.ts` — confirmed by grep.
3. Playwright spec passes under the seeded project — confirmed by `npx playwright test`.
4. `.paper-grain` CSS rule in `globals.css` UNCHANGED — Card / Dialog / Popover keep their texture.
</success_criteria>

<output>
After completion, create `.planning/phases/19-validation-surface-fixes/19-01-SUMMARY.md` per template.
</output>
