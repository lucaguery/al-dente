---
phase: 12-exploratory-feature-walkthrough
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - .planning/v0.3/WALKTHROUGH.md
  - .planning/v0.3/walkthrough-screenshots/
autonomous: true
requirements:
  - WALK-01
  - WALK-02
  - WALK-03
must_haves:
  truths:
    - "WALKTHROUGH.md §Shortlist has Golden path note + ≥3 weird-state probe entries with severity tags"
    - "WALKTHROUGH.md §Vote has Golden path note + ≥3 probes; vote-state probes verify rendered (not stored) state per invariant #2"
    - "WALKTHROUGH.md §Cooking Log has Golden path note + ≥3 probes; TZ-01 cross-linked if surfaced"
    - "WALKTHROUGH.md §History has Golden path note + ≥3 probes; CL-01 cross-linked (the GET /cooking-logs endpoint is missing)"
    - "Auditor remained logged in as member #3 throughout"
    - "Zero diff under frontend/ or backend/"
  artifacts:
    - path: ".planning/v0.3/WALKTHROUGH.md"
      provides: "4 decide/cook/history surfaces fully documented"
      min_severity_lines: 16  # 4 surfaces × ≥4 (golden + 3 probes) tagged entries
    - path: ".planning/v0.3/walkthrough-screenshots/"
      provides: "Visual evidence for shortlist/vote/cooking-log/history probes"
      min_files: 4
  key_links:
    - from: ".planning/v0.3/WALKTHROUGH.md §Cooking Log"
      to: "TZ-01 backlog entry"
      via: "Cross-link per D-06 (NOT a new GitHub issue) if probe re-discovers TZ-01"
      pattern: "TZ-01"
    - from: ".planning/v0.3/WALKTHROUGH.md §History"
      to: "CL-01 backlog entry"
      via: "Cross-link per D-06 — page renders empty due to missing GET endpoint"
      pattern: "CL-01"
---

<objective>
Probe the 4 decide-cook-history surfaces (Shortlist / Vote / Cooking Log / History) against the prod synthetic env via Playwright MCP. Each surface gets 1 golden-path traversal + ≥3 deliberate weird-state probes drawn from D-08's 4 probe kinds. Vote probes must verify the **rendered** computed state (Validé / Pressenti / Contesté / Rejeté / Sans avis) per architecture invariant #2 — never inspect a stored `state` column. Cooking Log probes will likely re-surface TZ-01 (the timezone bug) and History probes will re-surface CL-01 (missing list endpoint) — cross-link to backlog per D-06, do NOT file new issues.

Purpose: This is the heart of the daily-use loop ("on mange quoi?"). Probes here surface user-facing friction (vote-state UX, cooking-log finalize friction, history empty-state) that scripted golden-paths miss. State drift is accepted (D-09); auditor only invokes the Phase 11 teardown if a probe is genuinely blocked.

Output: 4 fully-populated decide-cook-history sections in WALKTHROUGH.md, ≥4 screenshots, zero edits to product code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md
@.planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md
@.planning/phases/12-exploratory-feature-walkthrough/12-02-SUMMARY.md
@.planning/v0.3/WALKTHROUGH.md
@RUNBOOK.md
@CLAUDE.md
@SPEC.md

<interfaces>
<!-- Architecture invariants and contracts the auditor must respect. -->

Architecture invariant #2 (voting state computed): The 5 states (Validé / Pressenti / Contesté / Rejeté / Sans avis) derive from rows in `votes` for `(shortlist_id, recipe_id)` via `services/voting.compute_vote_state`. Probes MUST verify the **rendered** state, never inspect any column.

Architecture invariant #3 (denormalized fields same-tx): `recipes.last_cooked_at` and `cook_count` update in the same DB transaction as cooking_logs insert. A probe of "double-finalize" must observe whether these denorm fields stay consistent.

Veto window (per SPEC.md, mentioned in CLAUDE invariant 2): The veto window closes on first `CookingLog` for the day. Vote-state probe boundary: try to vote no after a cook is finalized for the day's shortlist.

Endpoints involved (from frontend/tests/e2e/*.spec.ts golden references):
- GET  /api/shortlist/today                      — shortlist-vote.spec.ts
- POST /api/shortlist/regenerate                 — shortlist-vote.spec.ts (regenerate button)
- POST /api/votes                                — shortlist-vote.spec.ts:76-120
- POST /api/cooking-logs                         — cooking-log-create-finalize.spec.ts (start cook)
- POST /api/cooking-logs/{id}/finalize           — cooking-log-create-finalize.spec.ts
- GET  /api/cooking-logs                         — cooking-log-history.spec.ts (CL-01: this is missing in prod)

Locked French strings (next-intl, invariant #6):
- Shortlist swipe deck CTAs: regenerate prompt label varies — read live.
- Vote chip labels: "Validé" / "Pressenti" / "Contesté" / "Rejeté" / "Sans avis".
- Cooking CTAs: "Cuisiner" (start cook) / "Finaliser" (finalize).
- Cook offline toast (COOK-11): the toast text — read live.
- Empty-state: "Cette cuisson n'est plus disponible" (TZ-01 surfaces this when veto window has crossed).

Synthetic shortlist baseline (per RUNBOOK.md "Reference"):
- Today's shortlist contains 5 recipes: ragu-bolognese, coq-au-vin, butter-chicken, shawarma, tacos-boeuf.
- Vote states from 7 vote rows: Validé (ragu), Pressenti (coq), Contesté (butter), Rejeté (shawarma), Sans avis (tacos).
- Cooking_logs: ragu-bolognese (loved, -2d), poulet-citron (liked, -5d), burger-classique (disliked, -10d).

Known backlog (D-06 dedupe targets in this plan):
- TZ-01: `cooking_logs.py:72-78,118-126` — Python local-tz vs UTC date drift; late-evening cooks fall through. Surfaces as "Cette cuisson n'est plus disponible" when starting a cook close to midnight.
- CL-01: GET /api/cooking-logs (list) endpoint missing — `/cooking-logs` history page renders empty even with 3 seeded cooking_logs.
</interfaces>
</context>

<scope_constraint>
**HARD CONSTRAINT — NO product code changes.**

Permitted writes:
- `.planning/v0.3/WALKTHROUGH.md` (edit existing — fill in §Shortlist, §Vote, §Cooking Log, §History)
- `.planning/v0.3/walkthrough-screenshots/*.png` (create)

Permitted MCP browser actions:
- `mcp__playwright__*` against the PROD_URL (resolved in Plan 02; carry forward via 12-02-SUMMARY.md).
- All probes are member #3 scoped. Do NOT log out.

Forbidden:
- ANY edit under `frontend/` or `backend/`.
- Filing GitHub issues (Plan 05 batches).
- Logging out of member #3 mid-walkthrough.
- Running `uv run seed --prod-synthetic --teardown` unless a probe is genuinely blocked (D-09); document inline if invoked.
</scope_constraint>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Probe Shortlist + Vote surfaces (vote-state computed verification, invariant #2)</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-07/D-08/D-09 probe rules)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Per-Surface Probe Playbook" Surfaces 6-7 (shortlist, vote)
    - frontend/tests/e2e/shortlist-vote.spec.ts (the D-12 canary; covers 5 vote states with verbatim French labels)
    - SPEC.md (voting state machine, veto window logic)
    - .planning/v0.3/WALKTHROUGH.md (locate `## Shortlist` and `## Vote` headers)
    - .planning/phases/12-exploratory-feature-walkthrough/12-02-SUMMARY.md (carry forward PROD_URL, current inbox state)
  </read_first>
  <files>
    .planning/v0.3/WALKTHROUGH.md,
    .planning/v0.3/walkthrough-screenshots/shortlist-rapid-swipe.png,
    .planning/v0.3/walkthrough-screenshots/shortlist-regenerate-during-vote.png,
    .planning/v0.3/walkthrough-screenshots/shortlist-empty-state.png,
    .planning/v0.3/walkthrough-screenshots/vote-rapid-flip.png,
    .planning/v0.3/walkthrough-screenshots/vote-state-render-after-refresh.png,
    .planning/v0.3/walkthrough-screenshots/vote-veto-window.png
  </files>
  <action>
**Step 1 — Probe Shortlist (≥3 weird-state probes).**

  Navigate to `${PROD_URL}/` (HomeDecide). The synthetic env shows today's shortlist with 5 recipes (per RUNBOOK reference). Take a baseline `mcp__playwright__browser_snapshot` to confirm the deck rendered.

  - **Starting state:** "synthetic baseline (5 shortlist recipes), plus any drafts from Plan 02 (read 12-02-SUMMARY.md inbox state)."
  - **Golden path:** Mirror `frontend/tests/e2e/shortlist-vote.spec.ts` golden — observe the 5 cards with their starting vote chips (Validé/Pressenti/Contesté/Rejeté/Sans avis per the seed). Document any chip that doesn't match expected (this is invariant #2 verification at REST).

  ≥3 weird-state probes:
  1. **(Boundary)** Rapidly swipe through all 5 cards via `mcp__playwright__browser_click` on the next-card affordance (or whatever the swipe trigger is — read live). Observe the deck end-state. Does a regenerate prompt appear? Does the deck render empty correctly? Screenshot `shortlist-rapid-swipe.png`.
  2. **(Racing)** Click "Régénérer" while a vote is in flight. Two scenarios: (a) vote still pending → both succeed? Stale shortlist briefly visible? (b) regenerate completes first → does the in-flight vote land on the new shortlist or get dropped? Use `mcp__playwright__browser_network_requests` to inspect order. Screenshot `shortlist-regenerate-during-vote.png`.
  3. **(Invalid arrival / state drift)** After voting on every card (state drift accepted per D-09), refresh `/`. Does HomeDecide render the empty / completed state correctly? Are `Sans avis` recipes still filtered correctly? Screenshot `shortlist-empty-state.png`.

  Document each probe per D-04 in WALKTHROUGH §Shortlist.

**Step 2 — Probe Vote (≥3 weird-state probes; invariant #2 verification is mandatory).**

  Vote probes share state with the Shortlist probes above; document the carry-over state explicitly per D-09.

  - **Starting state:** "after Shortlist probes; current vote distribution: <list each recipe's current vote-chip>."
  - **Golden path:** Mirror `frontend/tests/e2e/shortlist-vote.spec.ts:76-120` golden — vote yes on a `Sans avis` recipe (e.g. tacos-boeuf), observe the chip flip to `Validé` (or `Pressenti` depending on partner state). Document.

  ≥3 weird-state probes:
  1. **(Racing)** On a recipe currently in `Sans avis` state, vote yes → IMMEDIATELY (no wait) vote no via `mcp__playwright__browser_click` on the opposite affordance. What's the final state? Optimistic-update race? Screenshot `vote-rapid-flip.png` after the dust settles. Use `mcp__playwright__browser_network_requests` to check the order POST /api/votes calls landed.
  2. **(Invariant probe — invariant #2)** After voting, hard-refresh via `mcp__playwright__browser_navigate('/')` (or `browser_evaluate(() => location.reload(true))` — bypass SW cache per RESEARCH §Risk 4). The chip MUST recompute and match — Validé / Pressenti / Contesté / Rejeté / Sans avis label is correct? If the rendered label differs from what the vote rows imply, that is a blocker (data-mismatch). Screenshot `vote-state-render-after-refresh.png`.
  3. **(Boundary — veto window)** Per SPEC.md, the veto window closes on first `CookingLog` for the day. Two sub-options for this probe:
     a. **If a cooking_log is already finalized for today** (state may carry over from earlier session or seed): try to vote no on a shortlist recipe. Does the UI prevent the vote? Backend 4xx? Document.
     b. **If no log is finalized for today**: skip this probe variant and substitute another (e.g. attempt to vote on a recipe NOT in today's shortlist via a deep-link to its detail page — `/recipes/<uuid>`).
     Screenshot `vote-veto-window.png` whichever variant runs.

  Document each probe per D-04 in WALKTHROUGH §Vote. Explicitly note which probe is the invariant #2 verification.

**Step 3 — Verify scope-creep guard.**
  ```bash
  git diff --name-only HEAD | grep -E "^(frontend|backend)/"
  ```
  MUST return empty.
  </action>
  <acceptance_criteria>
    - WALKTHROUGH.md §Shortlist has Golden path note + ≥3 entries each with `**Severity:**` line.
    - WALKTHROUGH.md §Vote has Golden path note + ≥3 entries each with `**Severity:**` line.
    - WALKTHROUGH.md §Vote contains explicit reference to invariant #2 verification (the post-refresh recompute probe). Searchable substring: `invariant` OR `computed`.
    - At least 4 of 6 expected screenshot files exist under `.planning/v0.3/walkthrough-screenshots/`.
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>SHORTLIST_SEV=$(awk '/^## Shortlist$/,/^## Vote$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); VOTE_SEV=$(awk '/^## Vote$/,/^## Cooking Log$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); INV2=$(awk '/^## Vote$/,/^## Cooking Log$/' .planning/v0.3/WALKTHROUGH.md | grep -ciE 'invariant|computed'); SHOTS=$(ls .planning/v0.3/walkthrough-screenshots/shortlist-*.png .planning/v0.3/walkthrough-screenshots/vote-*.png 2>/dev/null | wc -l | tr -d ' '); [ "$SHORTLIST_SEV" -ge 3 ] && [ "$VOTE_SEV" -ge 3 ] && [ "$INV2" -ge 1 ] && [ "$SHOTS" -ge 4 ] && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>Shortlist + Vote each have ≥3 severity-tagged probes, invariant #2 verification documented in §Vote, screenshots committed, no product-code drift.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Probe Cooking Log surface (create + finalize; expect TZ-01 surfacing)</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-06 backlog dedupe; D-09 state drift)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Per-Surface Probe Playbook" Surface 8 (cooking-log)
    - frontend/tests/e2e/cooking-log-create-finalize.spec.ts (golden path; TZ-01 fixme present)
    - .planning/v0.3/WALKTHROUGH.md (locate `## Cooking Log` header)
  </read_first>
  <files>
    .planning/v0.3/WALKTHROUGH.md,
    .planning/v0.3/walkthrough-screenshots/cooking-log-near-midnight.png,
    .planning/v0.3/walkthrough-screenshots/cooking-log-5kb-notes.png,
    .planning/v0.3/walkthrough-screenshots/cooking-log-finalize-offline.png
  </files>
  <action>
**Cooking Log surface — 3+ probes, expect TZ-01 surfacing per D-06.**

  - **Starting state:** "after Vote probes; current shortlist state documented inline."
  - **Golden path:** Click into a recipe (e.g. ragu-bolognese — already has a -2d cooking log per seed). Click "Cuisiner". Observe the cook-in-progress banner appearing. Then navigate to `/cooking-logs/<id>/finalize` (or whatever the finalize CTA path is — read live). Submit a finalize with notes + rating. Observe success.

  ≥3 weird-state probes:
  1. **(Known blocker — TZ-01)** Start a cook and try to finalize it close to local midnight. Two ways:
     a. If real-time clock cooperates and it's near midnight in the auditor's local TZ: just observe natural failure mode.
     b. Synthetic approach: use `mcp__playwright__browser_evaluate` to override `Date` in the page context (`window.Date = ...` — risky, may break other code) OR just document that the late-evening / midnight-cross window manifests as "Cette cuisson n'est plus disponible" per `cooking_logs.py:72-78,118-126`. **If the message surfaces during any probe, cross-link to TZ-01 backlog per D-06 — DO NOT file new issue.** Screenshot `cooking-log-near-midnight.png` if message appears.
  2. **(Boundary)** Finalize a fresh cook with notes = ~5KB of French text. Stored? Rendered correctly on the recipe detail card later? `mcp__playwright__browser_navigate` to the recipe detail and verify the displayed notes preserve the full content (or are truncated). Screenshot `cooking-log-5kb-notes.png`.
  3. **(Racing — offline finalize)** Start a cook. Use `mcp__playwright__browser_evaluate(() => window.dispatchEvent(new Event('offline')))` to simulate offline. Tap "Finaliser". Does the locked offline toast (COOK-11) fire? Document the toast French text verbatim. Then `dispatchEvent('online')` and verify the queued finalize replays (or doesn't). Screenshot `cooking-log-finalize-offline.png`.

  Document per D-04 in WALKTHROUGH §Cooking Log. If TZ-01 was surfaced, the entry's `Issue:` field MUST contain the literal substring `TZ-01` (cross-link, not a new issue URL).

**Verify scope-creep guard:**
  ```bash
  git diff --name-only HEAD | grep -E "^(frontend|backend)/"
  ```
  MUST return empty.
  </action>
  <acceptance_criteria>
    - WALKTHROUGH.md §Cooking Log has Golden path note + ≥3 entries each with `**Severity:**` line.
    - At least 2 of 3 expected screenshot files exist under `.planning/v0.3/walkthrough-screenshots/`.
    - If any probe surfaced TZ-01 behavior: WALKTHROUGH §Cooking Log contains the substring `TZ-01`.
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>COOK_SEV=$(awk '/^## Cooking Log$/,/^## History$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); SHOTS=$(ls .planning/v0.3/walkthrough-screenshots/cooking-log-*.png 2>/dev/null | wc -l | tr -d ' '); [ "$COOK_SEV" -ge 3 ] && [ "$SHOTS" -ge 2 ] && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>Cooking Log has ≥3 severity-tagged probes, TZ-01 cross-linked if surfaced, screenshots committed, no product-code drift.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Probe History surface (expect CL-01 surfacing — page renders empty)</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-06 backlog dedupe; CL-01 noted)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Per-Surface Probe Playbook" Surface 9 (history)
    - frontend/tests/e2e/cooking-log-history.spec.ts (golden path; CL-01 fixme present)
    - .planning/v0.3/WALKTHROUGH.md (locate `## History` header)
  </read_first>
  <files>
    .planning/v0.3/WALKTHROUGH.md,
    .planning/v0.3/walkthrough-screenshots/history-empty-due-to-CL-01.png,
    .planning/v0.3/walkthrough-screenshots/history-bad-uuid.png,
    .planning/v0.3/walkthrough-screenshots/history-empty-group-headers.png
  </files>
  <action>
**History surface — 3+ probes, expect CL-01 surfacing per D-06.**

  - **Starting state:** "after Cooking Log probes; the synthetic env has 3 seeded cooking_logs (ragu, poulet-citron, burger) plus any new logs from probes."
  - **Golden path:** Navigate to `${PROD_URL}/cooking-logs`. Observe what the page renders.

  ≥3 weird-state probes (the golden-path itself is likely the CL-01 surfacing):

  1. **(Known blocker — CL-01)** Per `STATE.md` v0.2.2 backlog, GET /api/cooking-logs (list) endpoint is missing. The page renders empty even with 3 seeded cooking_logs. **Document this as `blocker` severity, cross-link to CL-01 backlog — DO NOT file new issue.** Use `mcp__playwright__browser_network_requests` to confirm: is the page even calling a list endpoint? What does the network log show? Screenshot `history-empty-due-to-CL-01.png`.
  2. **(Friction)** If group-by-date headers are rendered (even with no items), check empty-group rendering. What about a future-dated cook (TZ-01-adjacent)? Screenshot `history-empty-group-headers.png`.
  3. **(Invalid arrival)** Deep-link `${PROD_URL}/cooking-logs/00000000-0000-0000-0000-000000000000` directly. 404 page? Crash? Auth redirect? Screenshot `history-bad-uuid.png`.

  Document per D-04. The CL-01 entry's `Issue:` field MUST contain the literal substring `CL-01` (cross-link).

**Verify scope-creep guard:**
  ```bash
  git diff --name-only HEAD | grep -E "^(frontend|backend)/"
  ```
  MUST return empty.

**Final touch — incremental commit point per D-20.** This is a natural commit boundary: 9 of 14 surfaces fully documented (5 captures + shortlist + vote + cooking log + history). Plan 04 picks up the cross-cutting set.
  </action>
  <acceptance_criteria>
    - WALKTHROUGH.md §History has Golden path note + ≥3 entries each with `**Severity:**` line.
    - WALKTHROUGH.md §History contains the substring `CL-01`.
    - At least 2 of 3 expected screenshot files exist under `.planning/v0.3/walkthrough-screenshots/`.
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>HIST_SEV=$(awk '/^## History$/,/^## Exports$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); HIST_LINK=$(awk '/^## History$/,/^## Exports$/' .planning/v0.3/WALKTHROUGH.md | grep -c 'CL-01'); SHOTS=$(ls .planning/v0.3/walkthrough-screenshots/history-*.png 2>/dev/null | wc -l | tr -d ' '); [ "$HIST_SEV" -ge 3 ] && [ "$HIST_LINK" -ge 1 ] && [ "$SHOTS" -ge 2 ] && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>History has ≥3 severity-tagged probes + CL-01 cross-link, screenshots committed, no product-code drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor browser → prod backend | Probes fire real POSTs against /api/votes, /api/cooking-logs, /api/shortlist/regenerate — every successful mutation writes to the synthetic household scope only. |
| auditor cookie identity | Member #3 session must persist; logging out would break scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02 | Information Disclosure / Privilege Escalation | Member-#3 session scope | mitigate | Auditor stays scoped to `[SYNTHETIC] Démo Al Dente`. Voting and cooking writes only ever reach the synthetic household via member #3's cookie identity. NEVER click "Quitter le foyer" or any logout control. |
</threat_model>

<verification>
**Plan-level verification:**
1. **Coverage:** WALKTHROUGH.md contains all 4 surface headers (Shortlist, Vote, Cooking Log, History) — already true from Plan 01 skeleton.
2. **Probe density:** Each surface has ≥3 severity-tagged entries (counted via `grep -c '^\*\*Severity:\*\*'` per section).
3. **Invariant #2 verification:** §Vote contains an explicit recompute-after-refresh probe (searchable: `invariant` OR `computed`).
4. **Backlog cross-links present:** §Cooking Log contains `TZ-01` (if surfaced); §History contains `CL-01`.
5. **Visual evidence:** ≥8 screenshot files in `.planning/v0.3/walkthrough-screenshots/` from this plan.
6. **Scope-creep guard:** `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.

Validation Architecture: explicitly N/A. Verification is markdown-shape + filesystem checks.
</verification>

<success_criteria>
- WALK-01 partial: 9 of 14 surfaces probed (5 captures + shortlist + vote + cooking log + history).
- WALK-02 partial: every section in this plan includes ≥3 deliberate weird-state probes from D-08 categories.
- WALK-03 partial: every probe entry uses the uniform finding template with severity tag.
- Vote-state probes verify rendered (computed) state per invariant #2 — at least one explicit recompute-after-refresh probe.
- Zero diff under `frontend/` or `backend/`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-exploratory-feature-walkthrough/12-03-SUMMARY.md` with:
- Per-surface probe count and severity breakdown for the 4 surfaces.
- Backlog cross-links made (TZ-01 surfaced? CL-01 surfaced?).
- List of NEW (non-backlog) blocker findings → these go to Plan 05 for filing, with WALKTHROUGH anchor reference.
- Carry-forward: any state drift that means Plan 04 starts from non-baseline (e.g. "shortlist is now exhausted; member #4 hasn't joined yet").
- Confirmation that the artifact allowlist held.
- Pointer to Plan 04: "Cross-cutting probe pass — exports / push / realtime sync / onboarding / settings (5 surfaces). Plan 04 is `autonomous: false` because push (D-19) and realtime two-context (D-15) may need operator confirmation."
</output>
