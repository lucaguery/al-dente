---
phase_num: 34
plan_id: "34-01"
plan_name: "LIVE-02 — prod-verify gate + photo signed-URL graceful fallback"
phase: "34-live-bug-sweep"
plan: 1
type: execute
wave: 1
depends_on: []
status: pending
requirement_ids: [LIVE-02]
requirements: [LIVE-02]
files_modified:
  - frontend/lib/hooks/useSignedPhotoUrl.ts
  - backend/app/routers/photos.py
  - backend/app/services/storage.py
  - .planning/phases/34-live-bug-sweep/34-01-prod-photo-url-probe.md
autonomous: false
user_setup: []

must_haves:
  truths:
    - "The deployed photo-url handler's response on a known-missing path is recorded as a one-line status + body in a .planning artefact before any code change."
    - "A request to GET /api/recipes/{any-uuid}/photo-url?path=does-not-exist.jpg returns 404 (not 500), regardless of whether the recipe exists."
    - "useSignedPhotoUrl swaps to the consumer's placeholder branch (src stays null) when the signed-URL fetch resolves to 404 — no console-500 noise, no infinite retry."
    - "Local seed walk of Accueil / Bibliothèque / Recette détail shows zero `500 Internal Server Error` console entries for /api/recipes/*/photo-url."
  artifacts:
    - path: ".planning/phases/34-live-bug-sweep/34-01-prod-photo-url-probe.md"
      provides: "Plan-step-0 evidence: prod handler probe status + body + decision routing"
      contains: "curl"
    - path: "frontend/lib/hooks/useSignedPhotoUrl.ts"
      provides: "Graceful fallback when getSignedPhotoUrl rejects with a 4xx — silent placeholder branch"
  key_links:
    - from: "frontend/lib/hooks/useSignedPhotoUrl.ts"
      to: "frontend/lib/recipes.ts → getSignedPhotoUrl()"
      via: "rejected-promise → setSrc(null) → consumer placeholder branch"
      pattern: "setSrc\\(null\\)"
---

<objective>
LIVE-02 — Photo signed-URL handler degrades gracefully on missing storage objects.

Purpose: B-02 punch-list bug — the local seed cascades 500s on every recipe with `recipes.photo_paths` entries that point to objects the seed never actually uploaded. Two open questions are settled by this plan: (a) does prod exhibit the same 500-on-missing-bytes shape? and (b) does `useSignedPhotoUrl` silently absorb the failure without console noise? Plan step 0 is HARD-REQUIRED — no code touches the repo before the prod probe runs and the result is recorded.

Output: A `34-01-prod-photo-url-probe.md` evidence artefact, a hardened photo-url handler (if prod 500s), and a verified-silent useSignedPhotoUrl fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-live-bug-sweep/34-CONTEXT.md
@CLAUDE.md
@frontend/CLAUDE.md
@backend/CLAUDE.md

@frontend/lib/hooks/useSignedPhotoUrl.ts
@backend/app/routers/photos.py
@backend/app/services/storage.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Plan-step-0 — probe the deployed photo-url handler on a known-missing path</name>
  <what-built>
    A reconnaissance request against the deployed Railway backend (via the prod Vercel rewrite at https://al-dente-pink.vercel.app) to characterize the handler's response on a non-existent storage path. This MUST run before any code changes. The result drives whether the rest of this plan touches the backend or stays frontend-only.

    Steps:
    1. Use the existing test cookie (Luca / test-token-luca) or a freshly minted prod-equivalent session. The auth contract is HttpOnly cookie via same-origin rewrite (invariant 8) — set the cookie in the browser DevTools network tab, or use `curl --cookie "aldente_auth=test-token-luca"` if the prod backend honors the seed token (it does not on Railway prod; use a real session cookie pulled from the browser instead).
    2. Issue:
       `curl -i -b "aldente_auth=<real-cookie-value>" "https://al-dente-pink.vercel.app/api/recipes/00000000-0000-0000-0000-000000000000/photo-url?path=does-not-exist.jpg"`
    3. Capture status code + Body.
    4. Also issue against a REAL recipe ID + a fabricated path: pick any recipe id from `/api/recipes?limit=1` (with the same cookie) and request `photo-url?path=fake-path.jpg`.
    5. Record both probes verbatim into a new file:
       `.planning/phases/34-live-bug-sweep/34-01-prod-photo-url-probe.md`
       Format:
       ```
       # LIVE-02 plan-step-0 — prod photo-url handler probe

       Date: 2026-05-18
       Auth: cookie aldente_auth=<redacted>

       ## Probe 1 — unknown recipe id
       Command: curl -i ... /api/recipes/00000000-…/photo-url?path=does-not-exist.jpg
       Status: <e.g. 404 Not Found>
       Body: <verbatim>

       ## Probe 2 — known recipe id, unknown path
       Command: curl -i ... /api/recipes/<real-id>/photo-url?path=fake-path.jpg
       Status: <e.g. 404 Not Found>
       Body: <verbatim>

       ## Decision
       - [ ] Prod returns 404 on both probes → Phase 34 LIVE-02 stays frontend-only (Task 3 path).
       - [ ] Prod returns 500 on either probe → Phase 34 LIVE-02 expands to backend handler hardening (Task 2 + Task 3).
       ```
    6. Commit the evidence file BEFORE proceeding: `gsd-sdk query commit "docs(34-01): record prod photo-url handler probe" --files .planning/phases/34-live-bug-sweep/34-01-prod-photo-url-probe.md`.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/34-live-bug-sweep/34-01-prod-photo-url-probe.md`.
    2. Confirm both probe blocks are populated with verbatim status codes + bodies.
    3. Confirm exactly one checkbox under "Decision" is ticked.
    4. If both probes returned 404 → respond `404` to skip Task 2.
    5. If either returned 500 → respond `500` to include Task 2 in the work.
    6. If the cookie can't be obtained (e.g. user is offline / Railway is down), respond `defer` and document the fallback in the probe file: assume worst case (treat as 500) so the backend hardening lands defensively.
  </how-to-verify>
  <resume-signal>Respond with one of: `404` (frontend-only scope), `500` (backend hardening required), or `defer` (proceed with backend hardening defensively).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend — catch missing-object errors in create_signed_photo_url; return 404 in router (conditional on Task 1 = 500 or defer)</name>
  <files>backend/app/services/storage.py, backend/app/routers/photos.py</files>
  <behavior>
    Test 1 — When `client.storage.from_(BUCKET).create_signed_url(path, ttl)` raises any Supabase/storage error that maps to "object not found" (typical shapes: 404 in the error message, `StorageException` with `statusCode == 404`, or the SDK returning a `{ "error": "...", "message": "Object not found" }` envelope), `create_signed_photo_url(path)` MUST raise `FileNotFoundError(path)` instead of leaking the raw exception.
    Test 2 — The router `GET /recipes/{recipe_id}/photo-url` catches `FileNotFoundError` from `create_signed_photo_url` and returns `HTTPException(status_code=404, detail="storage object missing")` rather than letting it bubble to 500.
    Test 3 — All other unexpected exceptions still propagate (so a real storage outage stays a 500).
  </behavior>
  <action>
    Only execute this task if Task 1's resume-signal was `500` or `defer`.

    In `backend/app/services/storage.py::create_signed_photo_url(path)`:
    - Wrap the `client.storage.from_(BUCKET).create_signed_url(path, SIGNED_URL_TTL_SECONDS)` call in `try/except Exception as exc`.
    - Detect the missing-object shape from supabase-py: the SDK raises `storage3.utils.StorageException` (or returns `{"error": "...", "statusCode": "404"}` in some versions). Match heuristically — check `getattr(exc, "code", None) == "NoSuchKey"`, `"404"` substring in `str(exc)`, or `"not found"`/`"not_found"` substring (case-insensitive) in `str(exc)`. If any match → `raise FileNotFoundError(path) from exc`.
    - Also handle the response-envelope shape: if `result` is a dict containing an `error` key or a `statusCode == "404"`, raise `FileNotFoundError(path)`. The existing code already normalizes signedURL keys — keep that path; only add the error-envelope branch above it.
    - Re-raise the original exception for any other case.

    In `backend/app/routers/photos.py::signed_photo_url`:
    - Wrap the `create_signed_photo_url(path)` call in `try / except FileNotFoundError`.
    - On `FileNotFoundError`: `raise HTTPException(status_code=404, detail="storage object missing")`. Match the existing 404 detail style ("recipe not found" / "path not on recipe").
    - Do not change the existing 2-step auth gate (recipe-household match, path-in-photo_paths). Those run BEFORE `create_signed_photo_url` and stay untouched.

    Add a brief test under `backend/tests/` if a `test_photos.py` exists already (grep first); otherwise SKIP the test scaffold — the punch list does not mandate backend test coverage for v0.7.1 (deferred to gh#28). If a test file does exist, add two new test cases:
    - Test that `create_signed_photo_url` re-raises `FileNotFoundError` when the underlying client raises an exception matching the missing-object heuristic (monkeypatch the client).
    - Test that the router returns 404 when `create_signed_photo_url` raises `FileNotFoundError` (use the existing FastAPI test client harness).

    Run `uv run pytest backend/tests/test_photos.py -x` if tests were added; otherwise confirm `uv run python -c "from app.routers.photos import router; print('ok')"` returns "ok".
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.services.storage import create_signed_photo_url; from app.routers.photos import signed_photo_url; print('imports ok')" 2>&1 | grep -q "imports ok"</automated>
  </verify>
  <done>If Task 1 indicated 500/defer: `create_signed_photo_url` raises `FileNotFoundError` on missing-object Supabase errors; `signed_photo_url` router returns 404 on `FileNotFoundError`; import smoke passes. If Task 1 indicated 404: this task is a no-op and is marked skipped in the SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontend — verify useSignedPhotoUrl's silent fallback on 404</name>
  <files>frontend/lib/hooks/useSignedPhotoUrl.ts</files>
  <behavior>
    Test 1 — When `getSignedPhotoUrl(recipeId, path)` rejects with any error (including a 404 from `/api/recipes/*/photo-url`), the hook MUST end in `{ src: null, onError: fn }` and the consumer renders its placeholder branch.
    Test 2 — The `onError` callback is invoked at most once per mount (per the existing D-04 single-retry budget). After the second failure the hook does NOT loop.
    Test 3 — A console capture during a fresh Accueil load with the broken seed shows zero `Failed to load resource: 500` and zero `Failed to load resource: 404` lines from `/api/recipes/*/photo-url`. (The 404 is fine if Task 2 landed; the hook surfaces it silently. The previous 500 must be gone.)
  </behavior>
  <action>
    Read `frontend/lib/hooks/useSignedPhotoUrl.ts` again and confirm the current `.catch(() => { if (alive) setSrc(null); })` branch on the initial fetch (line 47-52) already handles a rejected promise gracefully. If it does — and analysis shows it does — this task becomes a verification task with no code change required.

    Two things MAY still need a fix:
    1. `frontend/lib/recipes.ts → getSignedPhotoUrl` — read the file and confirm it rejects with a normal Error when the response is non-2xx. If it currently throws a different shape that bubbles past the hook's catch, harmonize.
    2. The hook's `onError` retry on `<img onError>`: when the refetch ALSO rejects, the inner `.catch(() => {})` (line 67-72) is a no-op. Keep this behavior — but double-check that the no-op does not leave `src` pointing to the stale broken URL. If it does, change line 67-72 to `.catch(() => { setSrc(null); })` so the consumer's placeholder takes over after the second failure too. This is a one-line tightening — DO IT.

    Then walk the local stack:
    - `cd backend && uv run uvicorn app.main:app --reload --port 8000` in one shell.
    - `cd frontend && npm run dev` in another.
    - `cd backend && uv run seed` once to populate the deterministic seed.
    - Visit http://localhost:3000/, http://localhost:3000/recipes, and one detail page (e.g. http://localhost:3000/recipes/<id>).
    - Open DevTools console with filter `photo-url`. Count `500 Internal Server Error` and `404 Not Found` lines.
    - After Task 2: 500 count MUST be 0. 404 count MAY be > 0 (silent — that's expected for the broken seed). Console MUST NOT show uncaught promise rejections or React error boundaries.
    - Without Task 2 (Task 1 returned 404): same counts — 500 count is 0 because the upstream prod handler returns 404 directly.

    Capture the console-message count + a screenshot of one of the affected pages in the SUMMARY.

    Skip Playwright wiring — the existing test suite isn't part of v0.7.1 scope per the REQUIREMENTS.md out-of-scope list (gh#28 test coverage stays in v0.8). The walk is the verification.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -5 && cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
    <human-check>Local Accueil + Bibliothèque + Recette détail walk shows zero `500 Internal Server Error` console lines for `/api/recipes/*/photo-url`. Photos render as gradient placeholders silently when bytes are missing. No uncaught rejection.</human-check>
  </verify>
  <done>useSignedPhotoUrl's two `.catch` branches both end in `setSrc(null)` (the second one was the silent no-op pre-fix). Lint + tsc clean. Local walk records zero photo-url 500s in console. Evidence captured in SUMMARY.</done>
</task>

</tasks>

<verification>
- `.planning/phases/34-live-bug-sweep/34-01-prod-photo-url-probe.md` exists and contains both probe results + a ticked decision checkbox.
- If backend hardening landed: `backend/app/services/storage.py::create_signed_photo_url` raises `FileNotFoundError` on missing objects; `backend/app/routers/photos.py::signed_photo_url` returns 404 on that.
- `frontend/lib/hooks/useSignedPhotoUrl.ts` second `.catch` branch sets `src` to null.
- Local seed walk on Accueil + Bibliothèque + Recette détail records zero `/api/recipes/*/photo-url` 500s.
</verification>

<success_criteria>
- LIVE-02 acceptance: local seed walk shows zero 500 console errors on Accueil / Bibliothèque / Recette détail.
- Prod probe documented in `34-01-prod-photo-url-probe.md` with a ticked decision; the artefact is the v0.7.1 HUMAN-UAT receipt.
- Backend (if touched) — clean rewrite, no compat shim. No new Alembic migration; pure handler change.
- Frontend — silent placeholder fallback verified; existing single-retry budget intact.
</success_criteria>

<out_of_scope>
- Re-seeding actual photo bytes into the local Supabase Storage emulator. The seed gap is acknowledged in CONTEXT.md and is not the fix surface for LIVE-02.
- Adding new Playwright coverage for the photo-url handler — gh#28 v0.8 scope.
- Refactoring `useSignedPhotoUrl` to a dual-render skeleton/spinner pattern — D-05 silent-swap discipline holds.
- Touching `frontend/lib/recipes.ts` for anything other than the rejection-shape verification.
- The cooking-log photo-url handler (`/cooking-logs/{id}/photos/...`) — separate read path, not in B-02 evidence.
</out_of_scope>

<rollback>
- `git revert <merge-commit>` reverts the backend handler change cleanly (no migration to undo).
- `git revert` on the frontend hook change is a one-line revert.
- The `34-01-prod-photo-url-probe.md` artefact is intentionally kept on rollback — it's research history.
</rollback>

<output>
Create `.planning/phases/34-live-bug-sweep/34-01-SUMMARY.md` per the standard template:
- Mark Task 2 as "skipped — prod returned 404" or "shipped — backend hardening" based on Task 1's outcome.
- Embed the verbatim probe results from `34-01-prod-photo-url-probe.md`.
- Note any decision routing impact on plans 02-05 (none expected — they're frontend-only and orthogonal).

Commit:
`gsd-sdk query commit "docs(34-01): LIVE-02 photo-url graceful fallback + prod probe" --files {modified-files} .planning/phases/34-live-bug-sweep/34-01-SUMMARY.md`
</output>
