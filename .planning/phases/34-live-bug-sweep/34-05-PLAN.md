---
phase_num: 34
plan_id: "34-05"
plan_name: "LIVE-05 + LIVE-06 — version bump + nested <main> fix"
phase: "34-live-bug-sweep"
plan: 5
type: execute
wave: 2
depends_on: ["34-01"]
status: pending
requirement_ids: [LIVE-05, LIVE-06]
requirements: [LIVE-05, LIVE-06]
files_modified:
  - frontend/package.json
  - frontend/app/page.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "`frontend/package.json` version field reads `0.7.1` (not `0.1.0`)."
    - "`VersionFooter` on /settings renders `v0.7.1 · {sha} · {env}` after a fresh build."
    - "Accueil DOM contains exactly ONE `<main>` element."
    - "`frontend/app/page.tsx` no longer wraps its content in a `<main>` element — the layout's `<main>` (frontend/app/layout.tsx:75) is the sole landmark."
  artifacts:
    - path: "frontend/package.json"
      provides: "Current milestone semver — 0.7.1"
      contains: "\"version\": \"0.7.1\""
    - path: "frontend/app/page.tsx"
      provides: "Page-level component without a `<main>` wrapper"
  key_links:
    - from: "frontend/components/VersionFooter.tsx"
      to: "process.env.NEXT_PUBLIC_APP_VERSION"
      via: "next.config.ts env block re-exports package.json version"
      pattern: "NEXT_PUBLIC_APP_VERSION"
    - from: "frontend/app/layout.tsx (line 75)"
      to: "the only `<main>` landmark"
      via: "wraps {children} including Home"
      pattern: "<main className=\"flex flex-col flex-1"
---

<objective>
LIVE-05 + LIVE-06 — Two trivial fixes bundled into one plan:
- LIVE-05: Bump `frontend/package.json` `"version"` from `0.1.0` to `0.7.1` so the VersionFooter on /settings reads the current milestone.
- LIVE-06: Strip the inner `<main>` from `frontend/app/page.tsx` so Accueil has exactly one main landmark (layout owns it).

Purpose: Both fixes are file-trivial and orthogonal in scope but both share the "production-grade hygiene" theme. Combining them keeps Phase 34's plan count at 5.

Output: A version bump + a single-main DOM.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-live-bug-sweep/34-CONTEXT.md
@.planning/phases/34-live-bug-sweep/34-01-SUMMARY.md
@CLAUDE.md
@frontend/CLAUDE.md

@frontend/package.json
@frontend/components/VersionFooter.tsx
@frontend/app/layout.tsx
@frontend/app/page.tsx
</context>

<interfaces>
<!-- frontend/app/layout.tsx line 75 (owner of the landmark): -->
```
<main className="flex flex-col flex-1 pb-[calc(5rem+env(safe-area-inset-bottom))]">
  {children}
</main>
```

<!-- frontend/app/page.tsx line 42 (the offender): -->
```tsx
return (
  <OnboardingGuard>
    <main className="flex flex-col flex-1">
      {showInstallHint ? (...) : null}
      <HomeDecide />
    </main>
  </OnboardingGuard>
);
```

<!-- frontend/components/VersionFooter.tsx (read-only contract): -->
```
const version = process.env.NEXT_PUBLIC_APP_VERSION ?? "0.0.0";
```
The `NEXT_PUBLIC_APP_VERSION` env var is set by `frontend/next.config.ts` re-exporting `process.env.npm_package_version` at build time. The footer reads the result at render time. **A fresh `npm run build` is required for the new version to flow.**
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LIVE-05 — bump frontend/package.json version to 0.7.1</name>
  <files>frontend/package.json</files>
  <behavior>
    Test 1 — `cat frontend/package.json | jq -r '.version'` outputs `0.7.1`.
    Test 2 — After `cd frontend && npm run build`, the VersionFooter renders `v0.7.1 · {sha} · {env}` (not `v0.1.0`). Confirmed by `grep "0.7.1" frontend/.next/static -r | head -3` finding the version baked into the bundle.
  </behavior>
  <action>
    1. Open `frontend/package.json`. Change line 3 from `"version": "0.1.0",` to `"version": "0.7.1",`.

    2. Do NOT touch any other field in package.json. Do NOT regenerate `package-lock.json` — `npm version` would add a git tag and that is not the workflow here. A literal field edit is correct.

    3. Confirm `frontend/next.config.ts` re-exports `process.env.npm_package_version` as `NEXT_PUBLIC_APP_VERSION` (it does, per the file's Phase 22 QW-02 comment in VersionFooter.tsx). If it does NOT, the footer's `?? "0.0.0"` fallback already hides the bug — but the punch list specifies the live value must be 0.7.x.

    4. Build to confirm: `cd frontend && npm run build`. The build must succeed. Then `cd frontend && npm run start` (or visit a fresh `npm run dev`). Navigate to `/settings`, scroll to the footer, confirm the rendered text starts with `v0.7.1 · ...`.

    5. Capture the rendered footer text verbatim in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && node -e "console.log(require('./package.json').version)" | grep -q "^0.7.1$"</automated>
    <human-check>After `npm run build && npm run start`, /settings footer renders `v0.7.1 · {sha} · {env}`.</human-check>
  </verify>
  <done>frontend/package.json `version` reads `0.7.1`. Built bundle and rendered footer confirm the value flows end-to-end. SUMMARY records the verbatim footer text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LIVE-06 — strip the inner `<main>` from frontend/app/page.tsx</name>
  <files>frontend/app/page.tsx</files>
  <behavior>
    Test 1 — `grep -c "<main" frontend/app/page.tsx` returns 0.
    Test 2 — `grep -c "<main" frontend/app/layout.tsx` returns 1 (unchanged — line 75 stays).
    Test 3 — `grep -rn "<main" frontend/app/` returns at most 1 line total (the layout's). If other page-level `<main>` wraps exist, they are separate WCAG offenders and out of scope for this plan — flag in SUMMARY but do not fix.
    Test 4 — Visiting `/` and inspecting the DOM via DevTools' accessibility tab shows exactly one `main` landmark (provided by layout.tsx).
    Test 5 — The existing Accueil layout is visually unchanged — the inner `<main className="flex flex-col flex-1">` becomes a `<div className="flex flex-col flex-1">` so the flex layout is preserved.
  </behavior>
  <action>
    1. Open `frontend/app/page.tsx`. Locate line 42: `<main className="flex flex-col flex-1">`. This is the inner `<main>` that conflicts with `frontend/app/layout.tsx` line 75's outer `<main>`.

    2. Replace line 42's opening tag from `<main className="flex flex-col flex-1">` to `<div className="flex flex-col flex-1">`. Update the matching closing tag (find the `</main>` that pairs with line 42 — should be near the end of the `return`) to `</div>`.

    3. Visually: nothing should change. The `flex flex-col flex-1` classes mean the div fills the available height the same way the main did. The outer `<main>` from layout.tsx already provides the landmark + the bottom-nav padding (`pb-[calc(5rem+env(safe-area-inset-bottom))]`), so the inner `<div>` only needs to flex within it.

    4. Run `cd frontend && npm run lint && npx tsc --noEmit`. Both clean.

    5. Walk locally:
       - `cd backend && uv run uvicorn app.main:app --reload --port 8000`
       - `cd frontend && npm run dev`
       - Visit `/` with the seed Luca cookie set.
       - DevTools → Accessibility panel → confirm exactly one `main` landmark in the tree.
       - DevTools → Elements → search for `<main` — exactly one result, owned by the layout.
       - Visually compare to pre-fix screenshot if available — layout should be byte-identical.

    6. Run `grep -rn "<main" frontend/app/` and record the output in the SUMMARY. If any other page (e.g. `/recipes/page.tsx`, `/settings/page.tsx`) also wraps in `<main>`, flag them in SUMMARY as candidate Phase 36 cleanup but DO NOT touch in this plan — out of scope per the LIVE-06 acceptance language ("either app/layout.tsx or app/page.tsx is rewrapped, not both").
  </action>
  <verify>
    <automated>cd frontend && grep -c "<main" app/page.tsx | grep -q "^0$" && cd frontend && grep -c "<main" app/layout.tsx | grep -q "^1$" && cd frontend && npm run lint 2>&1 | tail -3 && cd frontend && npx tsc --noEmit 2>&1 | tail -3</automated>
    <human-check>DevTools accessibility tree on `/` shows exactly one `main` landmark. Visual layout unchanged.</human-check>
  </verify>
  <done>frontend/app/page.tsx contains zero `<main>` tags. The inner element is now a `<div>` preserving the flex layout. Lint + tsc clean. Accessibility tree confirms a single landmark.</done>
</task>

</tasks>

<verification>
- `cat frontend/package.json | grep '"version"' | grep -q "0.7.1"`.
- `grep -c "<main" frontend/app/page.tsx` returns 0.
- `grep -c "<main" frontend/app/layout.tsx` returns 1.
- Local `/settings` footer reads `v0.7.1 · …`.
- Local `/` accessibility tree shows exactly one `main` landmark.
</verification>

<success_criteria>
- LIVE-05 acceptance: `/settings` footer shows `v0.7.x · {sha} · {env}` (specifically `v0.7.1` at milestone open per CONTEXT.md).
- LIVE-06 acceptance: Playwright snapshot at `/` shows one `main` element in the accessibility tree (not nested).
</success_criteria>

<out_of_scope>
- Per-phase semver bumps (CONTEXT.md: "Single bump at milestone open; no per-phase semver moves").
- Auditing every other page for nested `<main>` — only Accueil is in the LIVE-06 evidence. SUMMARY flags candidates for future phases but does not fix.
- Updating `package-lock.json` — version field is a metadata-only change; lockfile does not need to refresh.
- Adding a CI gate that asserts only one `<main>` exists per route — out of v0.7.1 scope.
</out_of_scope>

<rollback>
- `git revert <commit>` reverts both files cleanly.
- No DB, no backend touch.
</rollback>

<output>
Create `.planning/phases/34-live-bug-sweep/34-05-SUMMARY.md`:
- Record the verbatim rendered footer text after the bump.
- Record the verbatim output of `grep -rn "<main" frontend/app/` — exactly one match expected (the layout).
- Flag any additional candidate Phase 36 cleanup (other pages with `<main>` wraps, if discovered).

Commit:
`gsd-sdk query commit "fix(34-05): bump version to 0.7.1 + remove nested <main> on Accueil" --files frontend/package.json frontend/app/page.tsx .planning/phases/34-live-bug-sweep/34-05-SUMMARY.md`
</output>
