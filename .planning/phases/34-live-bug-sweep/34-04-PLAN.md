---
phase_num: 34
plan_id: "34-04"
plan_name: "LIVE-04 — Accueil marginalia branch guards on validéCount > 0"
phase: "34-live-bug-sweep"
plan: 4
type: execute
wave: 2
depends_on: ["34-01"]
status: pending
requirement_ids: [LIVE-04]
requirements: [LIVE-04]
files_modified:
  - frontend/components/HomeDecide.tsx
  - frontend/lib/i18n/fr.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "The Accueil subhead does NOT render `home.subhead.validated` (— déjà une idée validée) when the shortlist has zero rows in the computed `valide` state."
    - "When validéCount > 0, the subhead behavior is unchanged (still renders `validated`)."
    - "When validéCount === 0 AND at least one row is in `pressenti` state, the subhead renders `tentative` (existing behavior preserved)."
    - "When validéCount === 0 AND no row is in `pressenti`, the subhead renders `empty` (existing behavior preserved)."
    - "The derivation reads from the existing computed vote-state map (per invariant #2 — voting state is computed, not stored). No new DB column."
  artifacts:
    - path: "frontend/components/HomeDecide.tsx"
      provides: "subheadKey computation guards `validated` on validéCount > 0"
      contains: "validéCount"
  key_links:
    - from: "frontend/components/HomeDecide.tsx (subheadKey)"
      to: "computeVoteState(recipeVotes, members.length)"
      via: "allRowStates.filter(s => s === 'valide').length"
      pattern: "valide"
---

<objective>
LIVE-04 — Pre-vote Accueil marginalia copy branches on `validéCount === 0` so "déjà une idée validée" never appears when no Validé row is visible.

Purpose: B-09 punch-list bug — Accueil's H1 + marginalia composition claims "déjà une idée validée" via `home.subhead.validated`, but the user may be on a fresh seed with zero validés. SOBER-09 (Phase 36) structurally resolves this by porting the first-paint ledger that always exposes the Validé row. Until then, ship the explicit `validéCount > 0` guard so the contradiction stops immediately.

Output: A defensive guard in `HomeDecide.tsx`'s `subheadKey` computation that won't break under SOBER-09 either.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-live-bug-sweep/34-CONTEXT.md
@.planning/phases/34-live-bug-sweep/34-01-SUMMARY.md
@CLAUDE.md
@frontend/CLAUDE.md

@frontend/components/HomeDecide.tsx
@frontend/lib/i18n/fr.json
</context>

<interfaces>
<!-- Already-existing logic at frontend/components/HomeDecide.tsx lines 460-472: -->

```typescript
// State-derived Accueil subhead (D-13) — scans ALL shortlist recipes
// (not just dealable) to reflect the full vote landscape.
const allRowStates = shortlist.recipes.map((r) => {
  const recipeVotes = shortlist.votes.filter((v) => v.recipe_id === r.id);
  return computeVoteState(recipeVotes, session.members.length ?? 2);
});
const subheadKey: "validated" | "tentative" | "empty" =
  allRowStates.includes("valide")
    ? "validated"
    : allRowStates.includes("pressenti")
      ? "tentative"
      : "empty";
const subheadText = tSubhead(subheadKey);
```

`computeVoteState` is imported from `@/lib/voting` (frontend mirror of `backend/app/services/voting.compute_vote_state` — invariant #2). The five possible states are: `"valide" | "pressenti" | "contested" | "rejete" | "neutral"`.

i18n keys live at `frontend/lib/i18n/fr.json` under `home.subhead`:
- `validated` = "— déjà une idée validée"
- `tentative` = "— une piste, à confirmer"
- `empty` = "— personne ne s'est encore prononcé"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard the validated subhead on validéCount > 0</name>
  <files>frontend/components/HomeDecide.tsx</files>
  <behavior>
    Test 1 — When `allRowStates` contains at least one `"valide"`, `subheadKey === "validated"` (unchanged from current).
    Test 2 — When `allRowStates` does NOT contain `"valide"` but contains `"pressenti"`, `subheadKey === "tentative"` (unchanged).
    Test 3 — When `allRowStates` contains neither `"valide"` nor `"pressenti"`, `subheadKey === "empty"` (unchanged).
    Test 4 — Locked-fresh-seed case (CONTEXT.md targets this): when the shortlist has zero `valide` rows (e.g. all votes are unvoted or only `pressenti`/`contested`), the rendered marginalia is NOT "déjà une idée validée".
  </behavior>
  <action>
    Open `frontend/components/HomeDecide.tsx`. Locate lines 460-472 (the existing `allRowStates` + `subheadKey` derivation).

    Replace the existing ternary with a derivation that uses an explicit `validéCount` so the guard is readable and matches CONTEXT.md's directive verbatim:

    ```typescript
    // State-derived Accueil subhead (D-13 + Phase 34 LIVE-04) — scans ALL
    // shortlist recipes (not just dealable) to reflect the full vote
    // landscape. The `validated` branch requires validéCount > 0 to avoid
    // the contradiction the 260518-kba walkthrough flagged (B-09): the
    // marginalia "déjà une idée validée" used to render whenever any
    // recipe was in `valide` state — but pre-SOBER-09 the swipe-deck
    // hides the validated row, so the marginalia contradicted the view.
    // Guarding on `validéCount > 0` is correct in BOTH the current
    // dual-mode AND the SOBER-09 first-paint ledger; the guard remains
    // load-bearing after SOBER-09 lands (zero-validé seed case).
    const validéCount = allRowStates.filter((s) => s === "valide").length;
    const pressentiCount = allRowStates.filter((s) => s === "pressenti").length;
    const subheadKey: "validated" | "tentative" | "empty" =
      validéCount > 0
        ? "validated"
        : pressentiCount > 0
          ? "tentative"
          : "empty";
    const subheadText = tSubhead(subheadKey);
    ```

    The existing `allRowStates` computation (lines 462-465) STAYS untouched — both new derived counts read from the same map. Invariant #2 holds: state is computed from `votes` rows, not stored.

    Run `cd frontend && npm run lint && npx tsc --noEmit`. Both clean. (The `é` in `validéCount` is a valid JavaScript identifier character — TypeScript and the next-intl tooling accept it. If the executor's editor / lint chain rejects non-ASCII identifiers despite valid ECMAScript spec, fall back to `valideCount` / `pressentiCount` and add a comment explaining the ASCII spelling. The CONTEXT.md uses `validéCount` literally — match it if possible.)

    Walk locally:
    1. `cd backend && uv run seed` — confirm the seed produces at least one `valide` recipe (Ragu bolognese per the punch list).
    2. Visit `/`. With the seeded validé present, the subhead reads "— déjà une idée validée". Behavior preserved.
    3. Manually clear the validés (delete the `votes` rows that produced the Ragu `valide` state in local Supabase Studio) → reload `/` → subhead now reads "— une piste, à confirmer" (if any pressenti remains) or "— personne ne s'est encore prononcé" (if not).
    4. If clearing the seed is impractical, exercise the guard by stubbing `allRowStates` directly in React DevTools (set `validéCount` breakpoint and observe). Not load-bearing — the code is small enough that visual + unit reasoning suffice.

    Capture before/after screenshots in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && grep -nE "valid[ée]Count|pressentiCount" components/HomeDecide.tsx | head -5 && cd frontend && npm run lint 2>&1 | tail -3 && cd frontend && npx tsc --noEmit 2>&1 | tail -3</automated>
    <human-check>Against the seed (Ragu = validé), Accueil subhead reads "— déjà une idée validée". After clearing the Ragu validé votes, the subhead reads "— une piste, à confirmer" or "— personne ne s'est encore prononcé". The subhead NEVER reads "— déjà une idée validée" when zero validés exist.</human-check>
  </verify>
  <done>The `subheadKey` computation guards `validated` on `validéCount > 0`. Behavior preserved for the validé-present case. Lint + tsc clean. Local walk evidence in SUMMARY.</done>
</task>

</tasks>

<verification>
- `grep -nE "valid[ée]Count" frontend/components/HomeDecide.tsx` returns at least one line.
- The fallback chain `validéCount > 0 ? "validated" : pressentiCount > 0 ? "tentative" : "empty"` is the literal subheadKey derivation.
- Local Accueil walk with the seed shows the validated marginalia when Ragu is validé, and the tentative/empty marginalia when no validé exists.
</verification>

<success_criteria>
- LIVE-04 acceptance: fresh-seed Accueil load with NO Validé row shows neither "déjà une idée validée" nor any marginalia contradiction.
- The guard is structurally consistent with SOBER-09's first-paint ledger — when SOBER-09 lands, the guard remains correct (no rework).
- Invariant #2 honored — derivation reads from the existing `allRowStates` computed map; no new column.
</success_criteria>

<out_of_scope>
- Porting HomeDecide to Composition A first-paint ledger — that is SOBER-09 (Phase 36).
- Changing the marginalia copy strings — they stay as-is in fr.json.
- Cooking-banner / VoteSummary / EmptyState branches inside HomeDecide — none of those touch the subhead.
- Backend voting state computation — `computeVoteState` semantics are unchanged.
- Adding a `state` column to recipes (invariant #2 forbids).
</out_of_scope>

<rollback>
- `git revert <commit>` reverts cleanly.
- No DB or i18n keys added (the i18n keys reuse the existing `home.subhead.*` namespace).
- The frontend/lib/i18n/fr.json listing in files_modified is precautionary — the change is implementation-only and may NOT touch fr.json. Update the frontmatter at SUMMARY time to reflect the actual diff.
</rollback>

<output>
Create `.planning/phases/34-live-bug-sweep/34-04-SUMMARY.md`:
- Record the before/after screenshots of Accueil with validé present vs absent.
- Note explicitly that this guard remains load-bearing after SOBER-09 ships.

Commit:
`gsd-sdk query commit "fix(34-04): Accueil subhead guards validated marginalia on validéCount > 0" --files frontend/components/HomeDecide.tsx .planning/phases/34-live-bug-sweep/34-04-SUMMARY.md`
</output>
