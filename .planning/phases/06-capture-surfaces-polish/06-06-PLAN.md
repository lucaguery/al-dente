---
phase: 06-capture-surfaces-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/UrlCaptureTab.tsx
autonomous: true
requirements:
  - CAPTURE-12
must_haves:
  truths:
    - "User opens the URL tab and the submit button (Ajouter à la boîte de réception) renders at h-12 (48px) — the 48px tap-target floor is met"
    - "URL input retains its `font-mono text-sm` styling — URLs are code-like and the mono treatment is the convention from Phase 4"
    - "Helper card retains `bg-muted/60 p-3 text-sm text-muted-foreground` — Phase 5 tokens already render warm-taupe wash + faded ink"
    - "Inline validation error path (URL invalid) is preserved — `text-sm text-destructive mt-1` resolves to Phase 5 quieted-warm destructive"
  artifacts:
    - path: "frontend/components/UrlCaptureTab.tsx"
      provides: "URL capture tab with h-12 submit button"
      contains: "h-12 w-full"
  key_links:
    - from: "frontend/components/UrlCaptureTab.tsx (submit button)"
      to: "Phase 5 Button primitive at 48px floor"
      via: "className=\"h-12 w-full\""
      pattern: "h-12 w-full"
---

<objective>
Bring the URL capture surface into the Phase 5 design system. The URL tab is the simplest of the 5 capture surfaces — its visual treatment is already mostly Phase 5-token-correct (warm muted wash on the helper card, mono input, quieted destructive on the inline error). The only Phase 6 polish needed is raising the submit button to the 48px tap-target floor.

Purpose: Closes CAPTURE-12. The URL tab does not need structural rework — Phase 5 tokens flow through automatically via the inherited Input/Label/Button re-themes.
Output: One file updated with one surgical change (h-11 → h-12). Zero new i18n keys. Zero new shadcn primitives. Zero structural changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md
@.planning/phases/06-capture-surfaces-polish/06-CONTEXT.md
@.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@frontend/AGENTS.md
@frontend/lib/i18n/fr.json

<interfaces>
<!-- Phase 5 Button primitive — h-12 className override resolves to 48×n. -->

From frontend/lib/i18n/fr.json (existing keys — no new keys added):
- `recipes.url.field_label` = "URL de la recette"
- `recipes.url.field_placeholder` = "https://…"
- `recipes.url.helper` = "L'extraction automatique arrive bientôt — tu pourras compléter les détails dans la boîte de réception."
- `recipes.url.invalid` = "URL invalide. Vérifie le format (https://…)."
- `recipes.url.submit` = "Ajouter à la boîte de réception"
- `recipes.url.submitted_toast` = "URL ajoutée à la boîte de réception."
- `common.sending` = "Envoi…"
- `onboarding.errors.network` = (existing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise URL tab submit button from h-11 to h-12</name>
  <read_first>
    - frontend/components/UrlCaptureTab.tsx (full file — 105 LOC; current state: Label + Input at lines 63–77; helper card at lines 83–86; submit Button at lines 88–102 currently `h-11 w-full`)
    - .planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md §"Surface 7 — URL tab" (lines 382–393) — exact contract: h-12 w-full submit, font-mono text-sm preserved, helper card `bg-muted/60 p-3 text-sm text-muted-foreground` preserved
    - .planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md §"Component Inventory" row 5 (line 250) — change list: bump submit to h-12, URL input keeps font-mono, helper info row stays `bg-muted/60`
    - .planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md §"Color > Destructive — reserved for in Phase 6" — confirm `text-sm text-destructive mt-1` is correct for inline error
  </read_first>
  <files>frontend/components/UrlCaptureTab.tsx</files>
  <action>
Edit `frontend/components/UrlCaptureTab.tsx` with one surgical change. Business logic — `useState` for value/touched/submitting, `useMemo` URL validation (`new URL(...) + http/https check`), `handleSubmit` calling `postUrlCapture(value.trim())`, the `text-destructive` inline error gating on `showInlineError`, the toast.success/error calls — preserved byte-for-byte.

**Change — Raise the submit Button from h-11 to h-12:**
Locate the submit Button (current lines 88–102). Replace exactly:
```tsx
      <Button
        type="button"
        className="h-11 w-full"
        onClick={handleSubmit}
        disabled={!isValid || submitting}
      >
        {submitting ? (
          <>
            <Loader2 className="mr-2 h-4 w-4 animate-spin" />
            {tCommon("sending")}
          </>
        ) : (
          t("submit")
        )}
      </Button>
```
with exactly:
```tsx
      <Button
        type="button"
        className="h-12 w-full"
        onClick={handleSubmit}
        disabled={!isValid || submitting}
      >
        {submitting ? (
          <>
            <Loader2 className="mr-2 h-4 w-4 animate-spin" />
            {tCommon("sending")}
          </>
        ) : (
          t("submit")
        )}
      </Button>
```
Only `h-11 w-full` → `h-12 w-full`. The submit-state copy (`tCommon("sending")` / `t("submit")`) and the `<Loader2>` prefix are preserved verbatim.

**Audit (verify-no-edit) — confirm Phase 5 tokens already flow correctly:**
Use grep to verify the following lines are present and UNCHANGED (they should be — UI-SPEC §"Surface 7" + §"Component Inventory" row 5 explicitly say "no structural rework"):
- `<Input ... className="font-mono text-sm" ...>` — URL input (current line 71, `font-mono text-sm` preserved per UI-SPEC).
- Helper card `<div className="flex items-start gap-2 rounded-lg bg-muted/60 p-3 text-sm text-muted-foreground">` (line 83) — `bg-muted/60` resolves to warm-taupe wash via Phase 5 tokens.
- Inline error `<p className="text-sm text-destructive mt-1">{t("invalid")}</p>` (line 79) — Phase 5 destructive token is intentionally quieted warm-family red.

If any of those lines have drifted, this is OUTSIDE Phase 6 scope — raise a deviation; do NOT silently change them.

**Anti-patterns explicit (do NOT do):**
- DO NOT add `paper-grain` to the helper card — UI-SPEC §"Surface 7" + §"Paper-Grain placement": the helper card is informational chrome (warm-muted wash), not a "kitchen-counter recipe card." Paper-grain on it would compete with the v0.5 capture-surface paper-grain pattern (reserved for cards that represent recipe content).
- DO NOT change `font-mono text-sm` — URLs are code-like, mono is the W4 convention.
- DO NOT change `bg-muted/60` to `bg-card` or any other token — it's the warm-taupe wash and is correct.
- DO NOT add an icon to the submit button — text-only is the v0.1 convention; UI-SPEC §"Copywriting register" `Action verbs first` is satisfied by "Ajouter à la boîte de réception".
- DO NOT add new i18n keys.
- DO NOT change the `inputMode="url"`, `autoCapitalize="off"`, `autoCorrect="off"` — iOS keyboard hints, intentional.
- DO NOT change the validation logic. The client-side `new URL()` + http/https check is the T-02-04-03 mitigation; Phase 6 does not touch it.
  </action>
  <verify>
    <automated>cd frontend && grep -n 'h-12 w-full' components/UrlCaptureTab.tsx | wc -l | xargs -I{} test {} -eq 1 && grep -n 'h-11 w-full' components/UrlCaptureTab.tsx | wc -l | xargs -I{} test {} -eq 0 && grep -n 'h-11' components/UrlCaptureTab.tsx | wc -l | xargs -I{} test {} -eq 0 && grep -n 'font-mono text-sm' components/UrlCaptureTab.tsx | wc -l | xargs -I{} test {} -eq 1 && grep -n 'bg-muted/60 p-3 text-sm text-muted-foreground' components/UrlCaptureTab.tsx | wc -l | xargs -I{} test {} -eq 1</automated>
  </verify>
  <done>
- The submit Button uses `className="h-12 w-full"`.
- No `h-11` strings remain in the file.
- URL input keeps `className="font-mono text-sm"`.
- Helper card keeps `className="flex items-start gap-2 rounded-lg bg-muted/60 p-3 text-sm text-muted-foreground"`.
- Inline error keeps `className="text-sm text-destructive mt-1"`.
- All business logic (validation, handleSubmit, postUrlCapture call, redirect) byte-for-byte unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries. The URL submit path (`postUrlCapture` → backend `POST /api/recipes/url`) is unchanged. Inherits Phase 2 client-side `new URL()` validation (T-02-04-03) and backend re-validation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering (XSS) | Input renders `value` and `t("field_placeholder")`; inline error renders `t("invalid")` | accept | React JSX auto-escapes; user-typed URL is held in state, never rendered raw to HTML. The Input element treats the value as plain text. No `dangerouslySetInnerHTML`. |
| T-06-06-02 | URL injection (SSRF / phishing) | `postUrlCapture(value.trim())` accepts any http/https URL | accept | Phase 2 disposition T-02-04-03 unchanged: client validates http/https scheme via `new URL()`, backend re-validates with stricter rules (URL stored only; no scrape in v0.1). Phase 6 does not change either layer. |

No high-severity threats. ASVS L1 not applicable to a single h-11 → h-12 swap.
</threat_model>

<verification>
## URL tab smoke checks

```bash
# 1. Submit at h-12 w-full
grep -n 'h-12 w-full' frontend/components/UrlCaptureTab.tsx
# expected: 1 hit

# 2. No h-11 residue
grep -n 'h-11' frontend/components/UrlCaptureTab.tsx
# expected: 0 hits

# 3. URL input keeps font-mono
grep -n 'font-mono text-sm' frontend/components/UrlCaptureTab.tsx
# expected: 1 hit

# 4. Helper card unchanged
grep -n 'bg-muted/60 p-3 text-sm text-muted-foreground' frontend/components/UrlCaptureTab.tsx
# expected: 1 hit

# 5. Inline error unchanged
grep -n 'text-sm text-destructive mt-1' frontend/components/UrlCaptureTab.tsx
# expected: 1 hit

# 6. No new i18n keys
git diff frontend/lib/i18n/fr.json
# expected: no diff

# 7. Build + lint
cd frontend && npm run build && npm run lint
```

## Real-device smoke test (post-implementation)

On iPhone Safari PWA standalone:
1. Open `/recipes/new`, switch to the URL (URL) tab.
2. Confirm the URL input renders with mono font (Geist Mono via `font-mono` Tailwind utility) at 14px (text-sm).
3. Confirm the helper card renders as a warm-taupe wash with muted ink — info icon + body text.
4. Type an invalid URL (e.g. `not-a-url`) and tap outside the input → confirm the inline error renders with quieted-warm red (`text-destructive`) at the bottom of the input cluster.
5. Type a valid URL (e.g. `https://example.com/recipe`) → confirm the submit button enables and reads as 48px tall.
6. Tap submit → confirm success toast and redirect to `/inbox`.
</verification>

<success_criteria>
- [ ] CAPTURE-12 closed: URL submit button at h-12 w-full
- [ ] No `h-11` remaining in `UrlCaptureTab.tsx`
- [ ] URL input keeps `font-mono text-sm`
- [ ] Helper card and inline error class strings unchanged
- [ ] No new i18n keys
- [ ] Business logic byte-for-byte unchanged
- [ ] `npm run build` + `npm run lint` pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-capture-surfaces-polish/06-06-SUMMARY.md` documenting:
- One file modified with one-line diff (h-11 → h-12)
- grep proof: h-12 w-full present, no h-11 residue, font-mono and bg-muted/60 verified
- Real-device smoke test results
- Confirmation that the URL surface is the smallest Phase 6 plan — Phase 5 tokens already do most of the work
</output>
</content>
</invoke>