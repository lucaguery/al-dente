---
phase: 06-capture-surfaces-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/ui/alert-dialog.tsx
  - frontend/components/ui/card.tsx
  - frontend/components/ui/dialog.tsx
  - frontend/components/ui/sheet.tsx
  - frontend/app/globals.css
  - frontend/app/styleguide/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every Title primitive (alert-dialog, card, dialog, sheet) renders with `font-display` (Fraunces) — `font-heading` is gone everywhere except possibly in legacy node_modules code"
    - "globals.css no longer defines the deprecation aliases `--font-heading` and `--font-sans` in `@theme inline`"
    - "styleguide page imports `transitions` alongside `variants` so motion demos compile cleanly"
  artifacts:
    - path: "frontend/components/ui/alert-dialog.tsx"
      provides: "AlertDialogTitle primitive with font-display"
      contains: "font-display"
    - path: "frontend/components/ui/card.tsx"
      provides: "CardTitle primitive with font-display"
      contains: "font-display"
    - path: "frontend/components/ui/dialog.tsx"
      provides: "DialogTitle primitive with font-display"
      contains: "font-display"
    - path: "frontend/components/ui/sheet.tsx"
      provides: "SheetTitle primitive with font-display"
      contains: "font-display"
    - path: "frontend/app/globals.css"
      provides: "Token system without deprecated font aliases"
      contains: "--font-display"
    - path: "frontend/app/styleguide/page.tsx"
      provides: "Styleguide with transitions import"
      contains: "transitions"
  key_links:
    - from: "frontend/components/ui/{alert-dialog,card,dialog,sheet}.tsx"
      to: "var(--font-display)"
      via: "font-display Tailwind utility resolved by @theme block"
      pattern: "font-display"
---

<objective>
Close Phase 5 deferrals: sweep `font-heading` → `font-display` across the 4 shadcn Title primitives, remove the deprecation aliases from `globals.css @theme inline`, and add the missing `transitions` import to the styleguide page.

Purpose: Phase 5 left two cosmetic deferrals that block Phase 6 from declaring "every shadcn primitive uses Phase 5 tokens cleanly" — closing them here lets every other Phase 6 plan operate against a clean token surface.
Output: Four Title primitives swapped to `font-display`, two deprecation aliases removed from globals.css, one extra import on the styleguide page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md
@frontend/AGENTS.md

<interfaces>
<!-- Current state of font-heading usage (extracted via grep). Executor confirms the same line numbers before editing — files may have shifted. -->

frontend/app/globals.css lines 10–15 (current):
```css
  --font-display: var(--font-display);
  --font-body: var(--font-body);
  --font-mono: var(--font-mono);
  /* DEPRECATED — Phase 6 sweeps all `font-heading` and `font-sans` Tailwind utilities */
  --font-heading: var(--font-display);
  --font-sans: var(--font-body);
```

frontend/components/ui/alert-dialog.tsx line 126 (current):
```
"font-heading text-base font-medium sm:group-data-[size=default]/alert-dialog-content:group-has-data-[slot=alert-dialog-media]/alert-dialog-content:col-start-2",
```

frontend/components/ui/card.tsx line 41 (current):
```
"font-heading text-base leading-snug font-medium group-data-[size=sm]/card:text-sm",
```

frontend/components/ui/dialog.tsx line 133 (current):
```
"font-heading text-base leading-none font-medium",
```

frontend/components/ui/sheet.tsx line 117 (current):
```
"font-heading text-base font-medium text-foreground",
```

frontend/app/styleguide/page.tsx line 14 (current):
```ts
import { variants } from "@/lib/motion";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep font-heading → font-display in 4 Title primitives</name>
  <read_first>
    - frontend/components/ui/alert-dialog.tsx (current state — to confirm line 126)
    - frontend/components/ui/card.tsx (current state — to confirm line 41)
    - frontend/components/ui/dialog.tsx (current state — to confirm line 133)
    - frontend/components/ui/sheet.tsx (current state — to confirm line 117)
    - .planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md §"Phase 5 deferral closure (Phase 6 sweeps)" — table of file/line changes
    - frontend/app/globals.css (lines 10–15 — to see the deprecation alias context)
  </read_first>
  <files>frontend/components/ui/alert-dialog.tsx, frontend/components/ui/card.tsx, frontend/components/ui/dialog.tsx, frontend/components/ui/sheet.tsx</files>
  <action>
In each of the four files below, replace exactly one occurrence of the literal token `font-heading` with the literal token `font-display`. No other change. The replacements are inside string-class literals.

1. `frontend/components/ui/alert-dialog.tsx` — line 126: change `"font-heading text-base font-medium ...` → `"font-display text-base font-medium ...` (preserve everything else on the line including the `sm:group-data-[...]` chain).
2. `frontend/components/ui/card.tsx` — line 41: change `"font-heading text-base leading-snug font-medium group-data-[size=sm]/card:text-sm"` → `"font-display text-base leading-snug font-medium group-data-[size=sm]/card:text-sm"`.
3. `frontend/components/ui/dialog.tsx` — line 133: change `"font-heading text-base leading-none font-medium"` → `"font-display text-base leading-none font-medium"`.
4. `frontend/components/ui/sheet.tsx` — line 117: change `"font-heading text-base font-medium text-foreground"` → `"font-display text-base font-medium text-foreground"`.

After the four edits, run a repo-wide sanity check from the repo root:
```
grep -rn "font-heading" frontend/app frontend/components
```
Expected: zero hits. If any other file still references `font-heading` (a non-Title consumer not surfaced in UI-SPEC), replace it with `font-display` in place — UI-SPEC §"Phase 5 deferral closure" says "repo-wide grep ... must return zero hits".

Also check `font-sans ` (with trailing space, to skip e.g. `font-sans-serif` if it ever appears):
```
grep -rn "font-sans " frontend/app frontend/components
```
Expected: zero hits. If any consumer uses `font-sans` it should become `font-body` per the UI-SPEC role split (sans-for-body).

DO NOT modify the alias lines in `globals.css` yet — Task 2 owns that.
DO NOT introduce any other class changes; this is a literal token rename only.
  </action>
  <verify>
    <automated>cd frontend && grep -rn "font-heading" app components | wc -l | xargs -I{} test {} -eq 0 && grep -n "font-display" components/ui/alert-dialog.tsx components/ui/card.tsx components/ui/dialog.tsx components/ui/sheet.tsx | wc -l | xargs -I{} test {} -ge 4</automated>
  </verify>
  <done>`grep -rn "font-heading" frontend/app frontend/components` returns 0 hits; `grep -n "font-display" frontend/components/ui/{alert-dialog,card,dialog,sheet}.tsx` returns at least 1 hit per file (the Title primitive's class string).</done>
</task>

<task type="auto">
  <name>Task 2: Remove deprecated --font-heading and --font-sans aliases from globals.css</name>
  <read_first>
    - frontend/app/globals.css (full @theme inline block, lines 7–60 — to confirm the alias lines and surrounding context)
    - .planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md §"Phase 5 deferral closure" — confirms aliases must be removed AFTER Task 1's sweep
  </read_first>
  <files>frontend/app/globals.css</files>
  <action>
In `frontend/app/globals.css`, locate the `@theme inline { ... }` block. Within it, delete exactly these three consecutive lines (current lines 13–15):

```
  /* DEPRECATED — Phase 6 sweeps all `font-heading` and `font-sans` Tailwind utilities */
  --font-heading: var(--font-display);
  --font-sans: var(--font-body);
```

Leave `--font-display`, `--font-body`, `--font-mono` (lines 10–12) untouched. Leave the rest of the `@theme` block exactly as it is. The deletion removes 3 lines total (the comment line + the two `--font-*` declarations).

Rationale: Task 1 already eliminated every `font-heading` / `font-sans` Tailwind utility consumer. With no consumers, the alias declarations are dead code. Removing them locks the contract: `font-display` and `font-body` are the only legal font utilities going forward — any future drift back to `font-heading` / `font-sans` will fail to resolve at the Tailwind level (no `--font-heading` token defined) and a developer will notice.

DO NOT remove anything else from the @theme block. DO NOT alter the `--font-display` / `--font-body` / `--font-mono` declarations.
  </action>
  <verify>
    <automated>cd frontend && grep -n "font-heading\|font-sans" app/globals.css | wc -l | xargs -I{} test {} -eq 0 && grep -n "font-display" app/globals.css | wc -l | xargs -I{} test {} -ge 1 && npm run build --silent 2>&1 | tail -5</automated>
  </verify>
  <done>`grep -n "font-heading\|font-sans" frontend/app/globals.css` returns 0 hits; `--font-display`, `--font-body`, `--font-mono` still present; `npm run build` completes without unresolved-utility errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add `transitions` import to styleguide page</name>
  <read_first>
    - frontend/app/styleguide/page.tsx (line 14 — current import state; lines 220–270 to see motion demo consumers)
    - frontend/lib/motion.ts (the source of `transitions` and `variants` exports — already complete from Phase 5)
    - .planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md §"Phase 5 deferral closure" — last row, "Cosmetic; Phase 6 closes it"
  </read_first>
  <files>frontend/app/styleguide/page.tsx</files>
  <action>
In `frontend/app/styleguide/page.tsx`, change line 14 from:

```
import { variants } from "@/lib/motion";
```

to exactly:

```
import { variants, transitions } from "@/lib/motion";
```

This is a single-line import-extension. Do NOT add any new usage of `transitions` (the styleguide page already renders fine without it; the UI-SPEC calls this "cosmetic" and the import is staged so future motion demos can reference `transitions.fast` / `transitions.normal` without re-editing the import).

DO NOT modify any other line of the file. DO NOT introduce a `// eslint-disable` comment — the unused import is acceptable here per the UI-SPEC rationale (staged for future demos). If the project's lint config flags `transitions` as unused-import and fails the build, only then add a no-op reference inside the existing motion-demo section (e.g. `void transitions;` in a non-render block) and document why in a one-line comment. Otherwise leave as-is.
  </action>
  <verify>
    <automated>cd frontend && grep -n 'import { variants, transitions } from "@/lib/motion"' app/styleguide/page.tsx | wc -l | xargs -I{} test {} -eq 1 && npm run lint --silent 2>&1 | tail -5</automated>
  </verify>
  <done>Line 14 of `frontend/app/styleguide/page.tsx` reads exactly `import { variants, transitions } from "@/lib/motion";`. `npm run lint` passes (or surfaces only pre-existing warnings).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries — this plan modifies only re-themed shadcn primitives and a token alias removal. No client-server boundary, no untrusted input, no auth surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | globals.css token deletion | accept | No new attack surface — token deletion is build-time and produces a Tailwind compile error (loud failure) if any consumer references the removed alias. Inherits Phase 01.1 cookie-auth controls. |

No high-severity threats. ASVS L1 not applicable to a CSS token / class-string sweep.
</threat_model>

<verification>
## Phase 5 deferral closure smoke checks

```bash
# 1. Zero font-heading or font-sans utility references remain
grep -rn "font-heading\|font-sans " frontend/app frontend/components
# expected: 0 results

# 2. Title primitives now use font-display
grep -n "font-display" frontend/components/ui/alert-dialog.tsx frontend/components/ui/card.tsx frontend/components/ui/dialog.tsx frontend/components/ui/sheet.tsx
# expected: at least 4 results (one per file, on the Title primitive line)

# 3. globals.css aliases gone
grep -n "font-heading\|font-sans" frontend/app/globals.css
# expected: 0 results

# 4. styleguide imports transitions
grep -n 'import { variants, transitions } from "@/lib/motion"' frontend/app/styleguide/page.tsx
# expected: 1 result

# 5. Build still passes (proves no unresolved Tailwind utility from the alias removal)
cd frontend && npm run build
# expected: success
```
</verification>

<success_criteria>
- [ ] `grep -rn "font-heading\|font-sans " frontend/app frontend/components` returns 0
- [ ] All 4 Title primitives consume `font-display`
- [ ] `globals.css` no longer declares `--font-heading` or `--font-sans` aliases
- [ ] `frontend/app/styleguide/page.tsx` imports both `variants` and `transitions` from `@/lib/motion`
- [ ] `npm run build` and `npm run lint` complete cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-capture-surfaces-polish/06-01-SUMMARY.md` documenting:
- Files swept (4 Title primitives + globals.css + styleguide page)
- grep proof: zero `font-heading` / `font-sans` references remain
- Build + lint proof
- Note that Phase 6 surface plans (02–06) can now consume Phase 5 tokens cleanly without alias indirection
</output>
</content>
</invoke>