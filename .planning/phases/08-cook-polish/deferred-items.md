# Phase 08 — Deferred Items

Out-of-scope discoveries during plan execution that are NOT part of the current plan's blast radius. Logged here per the GSD scope-boundary rule rather than fixed inline.

## Lint warnings in `frontend/public/worker-9e66885325cabad7.js`

- **Discovered during:** Plan 08-05 verification (`npm run lint` from frontend/)
- **File:** `frontend/public/worker-9e66885325cabad7.js` (untracked generated PWA service worker artifact)
- **Warnings:**
  - `1:20  warning  Expected an assignment or function call and instead saw an expression  @typescript-eslint/no-unused-expressions`
  - `1:704 warning  Expected an assignment or function call and instead saw an expression  @typescript-eslint/no-unused-expressions`
- **Why deferred:** Pre-existing in working tree before Plan 08-05 began (listed as `??` in initial `git status`). Not authored by this plan. Generated by `next-pwa` build step. Either belongs in `.gitignore` or the lint config should ignore `frontend/public/worker-*.js`. Out of scope for the COOK-09 retheme blast radius.
- **Suggested fix:** Add `frontend/public/worker-*.js` to `frontend/eslint.config.mjs` `ignores` list, or add the path to `frontend/.gitignore` if regenerated on every build.
