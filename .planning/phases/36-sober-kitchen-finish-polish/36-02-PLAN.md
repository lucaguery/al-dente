---
phase: 36-sober-kitchen-finish-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/recipes/page.tsx
  - frontend/lib/recipes.ts
autonomous: false
requirements: [SOBER-11]
must_haves:
  truths:
    - "The Patine view renders the three section headers (« Héritage » / « Habitudes » / « À l'essai ») on first paint, with `(n)` count chips, even when one or more buckets are empty."
    - "When all 21 seeded recipes fall into a single bucket, the other two sections still render with `(0)` chips — Patine is never blank in dev."
    - "Combined with the SOBER-14 seed bump (Plan 36-04), at least one recipe lives in the Héritage bucket, exercising the high-patine visual layer."
  artifacts:
    - path: "frontend/app/recipes/page.tsx"
      provides: "PatinaView renders Héritage + Habitudes + Essai unconditionally, each with count chip; empty-bucket fallback string for the cards area"
      contains: "PatinaSection"
    - path: "frontend/lib/recipes.ts"
      provides: "groupByPatina helper unchanged in bucket boundaries (heritage = patina>=3, habitudes = patina==2, essai = patina<=1); JSDoc updated to reflect empty-bucket-render policy"
      contains: "groupByPatina"
  key_links:
    - from: "frontend/app/recipes/page.tsx PatinaView"
      to: "frontend/lib/recipes.ts groupByPatina + cookCountToPatina"
      via: "function call"
      pattern: "groupByPatina\\(recipes\\)"
    - from: "frontend/app/recipes/page.tsx PatinaSection"
      to: "design-system.html §15.B View C section-divider mockup (lines 1722-1727 area)"
      via: "unconditional render + count chip"
      pattern: "header.*Marginalia"
---

<objective>
Fix the Bibliothèque "Patine" view so it renders all three section headers (« Héritage » / « Habitudes » / « À l'essai ») with `(n)` count chips on every load, including when individual buckets are empty. Today the page only renders sections whose `bucket.length > 0`, which collapses the view to a blank container when the seed distribution puts everything in one bucket (the `B-06` walkthrough finding).

Purpose: Close SOBER-11. Make the Patine view a stable navigation surface — three sections always render, providing visual structure even on small or skewed seeds.

Output: Modified `frontend/app/recipes/page.tsx` (PatinaView) renders all three sections unconditionally with count chips; `frontend/lib/recipes.ts` keeps `groupByPatina` boundaries but the JSDoc reflects the new render policy. Combined with Plan 36-04's seed bump, the Patine view shows a meaningful distribution against the dev seed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-sober-kitchen-finish-polish/36-CONTEXT.md
@.planning/quick/260518-kba-ui-walkthrough-punch-list/PUNCH-LIST.md
@frontend/app/recipes/page.tsx
@frontend/lib/recipes.ts
@frontend/CLAUDE.md

<interfaces>
From `frontend/lib/recipes.ts`:
```typescript
export function cookCountToPatina(n: number): PatinaLevel; // 0 / 1-2 / 3-10 / >10 → 0 / 1 / 2 / 3
export function groupByPatina(recipes: readonly Recipe[]): {
  heritage: Recipe[];   // patina >= 3
  habitudes: Recipe[];  // patina === 2
  essai: Recipe[];      // patina <= 1
};
```

From `frontend/app/recipes/page.tsx`:
- `PatinaView` is rendered inside the `view === "patina"` branch of `RecipesPage`.
- `PatinaSection` already accepts `label`, `count`, `recipes`, `columnClass`.
- Current bug: `{grouped.heritage.length > 0 ? <PatinaSection ... /> : null}` (lines ~108-131). The three `length > 0` guards must be removed so all three sections render.
- `Marginalia` component already used inline for the count chip in `PatinaSection`.

From `frontend/lib/i18n/fr.json`:
- `home.library.patina_section.heritage`, `.habitudes`, `.essai` — the three section labels (already wired via `tPatina(...)`).
- If an "empty bucket" placeholder copy is needed (e.g. "Encore aucune recette") add it under `home.library.patina_section.empty` — keep terse, French, lowercase first letter unless sentence-leading.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Render all three Patine sections unconditionally with count chips and empty-bucket placeholder</name>
  <files>frontend/app/recipes/page.tsx, frontend/lib/i18n/fr.json, frontend/lib/recipes.ts</files>
  <behavior>
    - `PatinaView` renders three `<PatinaSection>` instances in order: Héritage, Habitudes, Essai. NO `length > 0` guards.
    - Each section header shows the label + the count chip (`· {n}`) — count is `recipes.length` (NOT zero-suppressed).
    - When a bucket is empty, the `<PatinaSection>` body renders an EmptyState-style placeholder paragraph: a single line of Caveat-slant marginalia text (e.g. "Encore aucune recette dans cette section"), NOT a fully-blank container.
    - The column-class differences are preserved (Héritage = single column, Habitudes = 2 cols, Essai = 3 cols) ONLY when the bucket has cards. The empty-bucket placeholder uses a single full-width line and does not render the empty grid.
    - The `groupByPatina` JSDoc in `lib/recipes.ts` is updated to reflect the new render policy ("empty buckets are still rendered by callers — see PatinaView in recipes/page.tsx").
    - i18n: add `home.library.patina_section.empty` = "Encore aucune recette dans cette section." (NBSP discipline aside — POLISH-01 covers the dot sweep; this string has no middle-dot.)
  </behavior>
  <action>
    1. In `frontend/app/recipes/page.tsx`, modify `PatinaView` (~lines 98-134):
       - Remove the three `{grouped.heritage.length > 0 ? <PatinaSection ... /> : null}` ternaries; render the three `<PatinaSection>` instances unconditionally.
       - Update `PatinaSection` (~lines 68-96) to render the count chip (`· {count}`) regardless of whether `count === 0`. The count chip is the Marginalia line already present — confirm it still renders when count is 0.
       - Inside `PatinaSection`'s body block (the `<div className={columnClass}>...</div>`), branch: if `recipes.length === 0`, render `<Marginalia size="sm" slant>{tPatina("empty")}</Marginalia>` instead of the empty grid. Pass `tPatina` down from `PatinaView` (already accepts it).
    2. In `frontend/lib/i18n/fr.json`, add `home.library.patina_section.empty`: `"Encore aucune recette dans cette section."` Verify the existing keys (`heritage`, `habitudes`, `essai`) are intact.
    3. In `frontend/lib/recipes.ts`, update the JSDoc on `groupByPatina` (~lines 252-256) to read: "Bucket recipes into the three Bibliothèque patine sections. Empty buckets are still rendered by callers as labeled sections with `(0)` chips and an empty-state line — the section structure is part of the Bibliothèque visual contract."
    4. Confirm `cookCountToPatina` boundaries stay unchanged (0 → 0, 1-2 → 1, 3-10 → 2, >10 → 3). Do NOT modify boundaries — Plan 36-04 covers seeding, this plan covers rendering.

    DO NOT touch:
    - `RecipeCard.tsx` or `RecipeRow.tsx` (SOBER-14 dogear and POLISH-01 NBSP own those files).
    - The grid/list view branches (only the patina branch).
    - `LibraryViewSwitch.tsx` (already correct).
    - `recipesCache` module-level state (correct; cache is shared across views).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - `PatinaView` renders three sections unconditionally; `grep -c 'PatinaSection' frontend/app/recipes/page.tsx` returns ≥ 3 (three render sites).
    - `grep -E 'heritage\.length > 0|habitudes\.length > 0|essai\.length > 0' frontend/app/recipes/page.tsx` returns zero matches.
    - `home.library.patina_section.empty` key exists in `fr.json` (`grep -c '"empty":' frontend/lib/i18n/fr.json` ≥ 1 under the relevant namespace).
    - `groupByPatina` boundaries unchanged in `lib/recipes.ts` (visual diff vs `git show HEAD~ -- frontend/lib/recipes.ts` shows JSDoc-only change).
    - `npm run lint` + `npx tsc --noEmit` clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Bibliothèque Patine view renders three labeled sections (Héritage / Habitudes / ...</name>
  <action>Human verification — see how-to-verify steps below.</action>
  <files>n/a (manual verification)</files>
  <what-built>Bibliothèque Patine view renders three labeled sections (Héritage / Habitudes / Essai) on first paint, with `(n)` count chips for every bucket. Empty buckets show a Caveat-slant marginalia line instead of a blank container.</what-built>
  <how-to-verify>
    1. Frontend + backend dev servers running (see Plan 36-01 verify steps for the bootstrap recipe).
    2. Open `http://localhost:3000/recipes` as Luca against the dev seed.
    3. Click the Patine view button in the `LibraryViewSwitch` (rightmost icon, `<i data-lucide="layers">`).
    4. Confirm THREE section headers render: Héritage, Habitudes, À l'essai. Each header has a count chip (`· N`) — the values depend on the seed but ALL three sections must be visible.
    5. For sections with `(0)` count: the empty-bucket Caveat-slant placeholder line must render. The section is NEVER fully empty.
    6. Switch back to Grille view + Liste view → confirm those views still render correctly (no regression in the localStorage view-switcher persistence).
    7. After Plan 36-04 ships (seed cook_count bump), re-run `uv run seed` + reload — the Héritage section should now contain at least one card (the bumped recipe).
  </how-to-verify>
  <resume-signal>Type "approved" if all three sections render with count chips on first paint (including empty-bucket placeholders); describe the issue otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure UI rendering change. No new data crosses boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | DoS (visual) | PatinaView render | accept | Empty-bucket placeholder bounds output; recipe.length scales with library size which is couple-scale (productize-later when N > 200). |
| T-36-02-02 | Information disclosure | i18n keys | accept | Empty-state string is French copy; no PII. |
</threat_model>

<verification>
- Lint + typecheck clean.
- Manual checkpoint verifies three sections always render with counts.
- No regression in Grille / Liste views.
- After SOBER-14 ships (Plan 36-04), Héritage bucket has ≥ 1 card.
</verification>

<success_criteria>
- SOBER-11 acceptance met: `/recipes` → Patine view shows three section headers with `(n)` counts on first paint; empty buckets render the placeholder line.
- Combined with Plan 36-04 (cook_count bump), at least one recipe lives in Héritage.
- The view-switcher persistence + anti-flash transition unchanged.
</success_criteria>

<output>
Create `.planning/phases/36-sober-kitchen-finish-polish/36-02-SUMMARY.md` with the receipt: PatinaView diff (guards removed), empty-bucket placeholder copy + i18n key, JSDoc updated in lib/recipes.ts, checkpoint outcome.
</output>
