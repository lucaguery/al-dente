---
phase: 36-sober-kitchen-finish-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/recipes/[id]/page.tsx
  - frontend/components/TableVote.tsx
  - frontend/app/globals.css
autonomous: false
requirements: [SOBER-12, SOBER-13, POLISH-03]
must_haves:
  truths:
    - "Recette détail (`/recipes/[id]`) renders the four cookbook gestures from design-system.html §15.C: terracotta-30 left margin-rule on ingredients, backdrop-blur title strip when a photo is present, Fraunces-italic numbered steps with terracotta numerals, Caveat-slant marginalia gutter for step-1 cooking-log notes."
    - "Recette détail meta-pill row is visually homogeneous: all four pills (prep_time_minutes, difficulty, cuisine, mood) are text-only — no leading Clock or Flame icon."
    - "Table-à-manger seat geometry renders on Accueil ledger rows: `.table-scene` with `.table-plate` + `.table-seat` north/south carrying state classes (`seat-state-valide`, `seat-state-pressenti`, `seat-state-contested`, `seat-state-rejected`, `seat-state-neutral`). DOM inspector confirms the locked geometry, not just two flat avatar generics."
  artifacts:
    - path: "frontend/app/recipes/[id]/page.tsx"
      provides: "Cookbook gestures verified + ingredient list with left margin-rule + numbered-step terracotta italic numerals + step-1 marginalia gutter + all-text pills (no Clock/Flame icons in the badge row)"
      contains: "Marginalia.*slant"
    - path: "frontend/components/TableVote.tsx"
      provides: "Locked seat geometry verified or restored (table-plate + 2 state-classed seats); any missing piece shipped"
      contains: "table-plate"
    - path: "frontend/app/globals.css"
      provides: "Left margin-rule utility (or terracotta-30 border-left token) on ingredient sections, IF missing"
      contains: ".ingredient-section|--terracotta-30|border-l-primary"
  key_links:
    - from: "frontend/app/recipes/[id]/page.tsx ingredient section"
      to: "frontend/app/globals.css terracotta-30 left margin-rule token/class"
      via: "borderLeft inline style or className utility"
      pattern: "borderLeft|border-l-|--terracotta-30"
    - from: "frontend/components/TableVote.tsx"
      to: "frontend/app/globals.css .table-plate / .table-seat / .seat-state-* classes"
      via: "className composition"
      pattern: "table-plate|table-seat|seat-state-"
---

<objective>
Verify and (where missing) ship the locked cookbook gestures on Recette détail per `docs/design-system.html` §15.C, harmonize the meta-pill row to all-text pills (per CONTEXT §POLISH-03 resolution), and verify the table-à-manger seat geometry on Accueil ledger rows. This is a **verification-first plan**: each gesture is either confirmed present (no code change for that gesture) or shipped (code change documented in the SUMMARY).

Purpose: Close SOBER-12 (Recette détail body matches §15.C), SOBER-13 (table-à-manger seat geometry verified live), and POLISH-03 (all-text-pills, drop the Clock + Flame icons on the meta row — per CONTEXT.md "all-text-pills" resolution).

Output: A modified Recette détail page (where gestures are missing or POLISH-03 changes are needed) + a modified `TableVote.tsx` (where the geometry was missing) + a SUMMARY.md documenting which gestures were already present vs needed code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-sober-kitchen-finish-polish/36-CONTEXT.md
@.planning/quick/260518-kba-ui-walkthrough-punch-list/PUNCH-LIST.md
@docs/design-system.html
@frontend/app/recipes/[id]/page.tsx
@frontend/components/TableVote.tsx
@frontend/components/Marginalia.tsx
@frontend/app/globals.css
@frontend/CLAUDE.md

<interfaces>
From `frontend/app/recipes/[id]/page.tsx` (current state — what already ships):
- Hero photo: 16:10 aspect, -38px bleed into the sticky topbar (already present, ~line 736).
- Backdrop-blur on the sticky topbar: `backdropFilter: "blur(12px)"` (already present, ~line 652).
- Step-1 marginalia: renders only when `recipeLog?.notes` is truthy AND `isFirst` (already present, ~lines 933-944). Uses `<Marginalia size="sm" slant>` with `display: block, margin: 4px 0 0 12px`.
- Numbered steps: Fraunces font + terracotta `var(--primary)` numeral (already present, ~lines 911-922).
- Ingredient quantities: terracotta numerals on the left of each ingredient name (already present, ~lines 866-880).
- Badge row (~lines 794-820): renders prep_time pill with `<Timer size={11} />` icon + difficulty pill with `<Flame size={11} />` icon + cuisine pill (text only) + mood pill (text only). POLISH-03 resolution = drop the Clock (Timer) AND Flame icons; all four pills become text-only.

From `docs/design-system.html` §15.C (the locked cookbook gestures):
1. Terracotta-30 LEFT margin-rule on the ingredient list — a vertical CSS border-left in terracotta at ~30% opacity. THIS APPEARS TO BE MISSING from the current code: the ingredient section has terracotta numerals (qty) on each line but no full-section border-left. VERIFY against the live render — if missing, ship.
2. Backdrop-blur title strip when a photo is present — the sticky topbar covers this (full-width 12px blur). VERIFY this composites correctly over the hero photo bleed.
3. Fraunces-italic numbered steps — currently the numerals use Fraunces (font-display) but NOT italic style. §15.C calls for italic. VERIFY by inspecting the step rendering — if numerals are upright, change to `fontStyle: "italic"`.
4. Caveat-slant marginalia gutter for step asides — currently the marginalia is inline below step 1's text with `marginLeft: 12px` (NOT a gutter offset). §15.C wants a left-gutter offset — the note reads as a marginal handwritten annotation against the printed step. VERIFY visually; if it reads as a flat caption, increase the left-gutter offset (e.g. negative margin into the gutter, or absolute positioning to the left of the step text).

From `frontend/components/TableVote.tsx` (current state):
- `.table-scene` wrapping `<div className="table-plate" aria-hidden />` and N `<span className="table-seat seat-{position} {stateClass}">` — geometry IS present in the JSX.
- `seatStateClass()` returns one of: `seat-state-valide`, `seat-state-pressenti`, `seat-state-contested`, `seat-state-rejected`, `seat-state-neutral`.
- Per-seat geometry depends on the corresponding CSS classes in `globals.css` rendering correctly (`.table-plate`, `.table-seat`, `.seat-north`, `.seat-south`, `.seat-state-*`).
- The punch-list (B-D-06) flagged the accessibility tree showing flat `<generic>` elements — this is partly an a11y-tree artifact (the seats are `<span>` with `role="img"`-equivalent semantics from `aria-label` on the parent scene). Visual verification on a real device or DOM inspector is required.

From `frontend/app/globals.css`:
- `.table-plate`, `.table-seat`, `.seat-state-*` are already defined (Phase 32 SOBER-03 ship). Visual verify only.
- `.ledger-card` + `.dogear` defined.
- No `.ingredient-margin-rule` utility — if SOBER-12 calls for one, add it here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Visual audit + spot-fix Recette détail cookbook gestures + drop Clock/Flame icons (POLISH-03)</name>
  <files>frontend/app/recipes/[id]/page.tsx, frontend/app/globals.css</files>
  <behavior>
    - For each of the four §15.C gestures, the implementer:
      a. Inspects the current render against the design-system.html §15.C reference (open both side-by-side in a browser).
      b. If the gesture is present → document in SUMMARY ("gesture N: already shipped, no change").
      c. If the gesture is missing or weak → ship the smallest possible code change to deliver it.
    - The meta-pill row drops the Timer (Clock) AND Flame icons from prep_time_minutes and difficulty pills. All four pills become text-only `<span className="badge">…</span>`. The `Timer` and `Flame` imports stay (Flame is still used on the sticky bottom CTA — verify before removing imports; Timer may be removable if it was only the meta row).
    - The cuisine + mood pills are unchanged (already text-only).
    - Ingredient list section gets the terracotta-30 left margin-rule if missing: a vertical border-left on the section wrapper at ~30% terracotta opacity, ~3-4px wide, indented from the page edge.
    - Numbered steps: if the numerals are upright, add `fontStyle: "italic"` to match §15.C.
    - Step-1 marginalia: verify the gutter offset reads as marginal. If it reads as flat-caption, increase the offset (e.g. add `marginLeft: "-8px"` or `paddingLeft: "16px"` to push it into the gutter; depends on the body block's padding).
  </behavior>
  <action>
    1. Open both `http://localhost:3000/recipes/{ragu-id-from-seed}` (live render) and `docs/design-system.html#recette` (mockup) side-by-side. The `ragu-id` is in the seed — find via the API: `curl http://localhost:8000/api/recipes | jq '.[] | select(.title | contains("Ragu")) | .id'` (or click through `/recipes`).
    2. For each of the 4 gestures in `<interfaces>` above, audit the current render and decide: present, weak, or missing. Document each decision in the SUMMARY.
    3. POLISH-03 (all-text-pills): in the badge row (~lines 794-820 of `recipes/[id]/page.tsx`), remove the `<Timer size={11} />` from the prep_time pill AND the `<Flame size={11} />` from the difficulty pill. The pills become `<span className="badge">{recipe.prep_time_minutes} min</span>` and `<span className="badge">{labels.difficulty(recipe.difficulty)}</span>`. Verify `Timer` is not used elsewhere in the file; if so, drop the import. `Flame` IS still used on the sticky bottom CTA (line ~1014) — keep that import.
    4. If gesture 1 (terracotta-30 left margin-rule) is missing: add either an inline style on the ingredient `<section>` wrapper (`borderLeft: "3px solid color-mix(in oklch, var(--primary) 30%, transparent)", paddingLeft: "12px"`) OR a reusable `.ingredient-margin-rule` utility class in `globals.css` and apply it. Either is acceptable — pick whichever reads cleaner in the existing inline-style register.
    5. If gesture 3 (italic numerals) is missing: add `fontStyle: "italic"` to the step numeral span (~line 914).
    6. If gesture 4 (marginalia gutter offset) reads as flat caption: adjust the `Marginalia` style — e.g. change `margin: "4px 0 0 12px"` to `margin: "4px 0 0 0", paddingLeft: "16px", borderLeft: "1px dotted color-mix(in oklch, var(--primary) 25%, transparent)"`. Goal: it reads as a marginal handwritten note, not a centered caption.
    7. Gesture 2 (backdrop-blur title strip) is visually validated by the checkpoint — likely no code change needed.

    DO NOT:
    - Modify the RecipeThread component (out of scope; SOBER-12 is the static-render gestures only).
    - Modify the sticky bottom CTA copy (POLISH-04 owns that, in Plan 36-06).
    - Modify the photo loading path (Phase 30 BUG-01 owns useSignedPhotoUrl; out of scope).
    - Modify enum labels rendering (Phase 35 ENUM-02 + 03 shipped this).
    - Touch `TableVote.tsx` here — Task 2 below owns it.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - Meta-pill row in `recipes/[id]/page.tsx` has NO `<Timer />` and NO `<Flame />` inside the badge spans (only on the bottom CTA, line ~1014).
    - `grep -n "Timer size" frontend/app/recipes/\[id\]/page.tsx` returns at most one hit if Timer is used elsewhere — should be zero hits in the meta row.
    - `grep -n "Flame size" frontend/app/recipes/\[id\]/page.tsx` returns exactly one hit (the bottom CTA line).
    - Each of the four §15.C gestures is either documented as already present or shipped — the SUMMARY enumerates each.
    - `npm run lint` + `npx tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify (and ship-if-missing) table-à-manger seat geometry on Accueil ledger rows</name>
  <files>frontend/components/TableVote.tsx, frontend/app/globals.css</files>
  <behavior>
    - `TableVote` renders a `.table-scene` containing `.table-plate` + N `.table-seat` elements with the position class (`seat-north`/`seat-south`/etc.) AND a state class (`seat-state-valide`/`pressenti`/`contested`/`rejected`/`neutral`).
    - Visual inspection on Accueil (post-vote ledger view) confirms the table-plate + seat geometry reads as "two seats at a table" — NOT "two avatars in a card."
    - If the geometry is visually flat (e.g. plates not rendering, halos not visible), the implementer ships the missing CSS or JSX. Phase 32 already shipped the classes; this task may be a no-op.
    - The `aria-label="Vote: {aggregate}"` on the scene parent is retained for screen readers.
  </behavior>
  <action>
    1. Open `http://localhost:3000/` as Luca against the seed. Vote on the un-voted Tacos card to flip the view into the post-vote ledger (where TableVote renders for each row).
    2. Inspect each shortlist row's seat scene via DevTools: confirm the DOM contains `<div class="table-scene ts-56"><div class="table-plate" /><span class="table-seat seat-north seat-state-X" /><span class="table-seat seat-south seat-state-Y" /></div>`.
    3. Confirm visual treatment:
       - The plate is a round element with a subtle radial gradient (Phase 32 CSS).
       - Seats are positioned at north/south of the plate, NOT centered on top of it.
       - State classes carry the expected box-shadow halos: emerald on `seat-state-valide`, terracotta on `seat-state-pressenti`, destructive on `seat-state-contested`, muted on `seat-state-neutral` / `seat-state-rejected`.
    4. If the geometry IS present (likely — Phase 32 shipped it): no code change. Document in SUMMARY: "SOBER-13 verified live, no code change required."
    5. If the geometry is missing or weak: ship the minimum fix.
       - Likely culprits if missing: a CSS rule was deleted, the `--patina` cascade is hiding the plate, or the state-class string is misspelled in `seatStateClass`. Inspect, fix, document.

    DO NOT modify `VoteSummary.tsx` here — it consumes `TableVote` but is in scope for Plan 36-06 (SOBER-09). TableVote.tsx itself is the only file in this task's scope.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "table-plate\|table-seat\|seat-state-" frontend/components/TableVote.tsx</automated>
  </verify>
  <done>
    - `grep -c "table-plate" frontend/components/TableVote.tsx` ≥ 1 (geometry present).
    - `grep -c "seat-state-" frontend/components/TableVote.tsx` ≥ 5 (all five state class branches).
    - Visual verification by user in Task 3 (checkpoint) confirms the geometry reads correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: SOBER-12 cookbook gestures audited + spot-fixed on Recette détail</name>
  <action>Human verification — see how-to-verify steps below.</action>
  <files>n/a (manual verification)</files>
  <what-built>SOBER-12 cookbook gestures audited + spot-fixed on Recette détail; POLISH-03 all-text-pills shipped (Clock + Flame removed from meta row); SOBER-13 table-à-manger seat geometry verified live (and any missing piece shipped).</what-built>
  <how-to-verify>
    1. Frontend + backend dev servers running; seed loaded.

    **SOBER-12 + POLISH-03 (Recette détail):**
    2. Open `/recipes/{ragu-id}` — Ragu bolognese is the seeded recipe with a cooking log + notes (the "Excellent ce soir." comment that drives the step-1 marginalia).
    3. Confirm the four §15.C gestures visible:
       - Ingredient list has a terracotta-30 left margin-rule (a vertical line at ~30% terracotta opacity running down the left edge of the ingredient block).
       - Hero photo (or its fallback gradient) bleeds up into the sticky topbar with backdrop-blur — read together as "photo with title strip on top."
       - Numbered steps: each numeral is Fraunces italic, terracotta color.
       - Step 1 has the "Excellent ce soir." cooking log note rendered in the left gutter as Caveat-slant marginalia (NOT as a flat caption below the step text).
    4. Confirm POLISH-03: the meta-pill row (prep time + difficulty + cuisine + mood) is HOMOGENEOUS — all four pills are text-only. NO Clock icon on "90 min", NO Flame icon on "Moyen."

    **SOBER-13 (table-à-manger seats):**
    5. Open `/` (Accueil). Vote on the un-voted Tacos card to enter the post-vote ledger view.
    6. Open Chrome/Safari DevTools, inspect a shortlist row's seat scene. Confirm:
       - `<div class="table-scene ts-56">` wrapper.
       - `<div class="table-plate" aria-hidden />` inside.
       - Two `<span class="table-seat seat-north seat-state-X">` and `seat-south seat-state-Y` elements with state classes matching the per-seat derivations from `seatStateClass`.
    7. Confirm visual treatment: each row reads as "two seats at a round table" — plate visible as a circle, seats positioned at top/bottom edges, state halos (emerald for valide, terracotta for pressenti, destructive for contested) visible.

    Document the SUMMARY: which of the 4 SOBER-12 gestures needed code vs were already present; whether SOBER-13 was a no-op; what POLISH-03 changes shipped.
  </how-to-verify>
  <resume-signal>Type "approved" if all four §15.C gestures are present, the meta-pill row is all-text, and the table-à-manger geometry reads correctly; describe issues otherwise (e.g. "step-1 marginalia still reads as caption," "table-plate not rendering at ts-56 size").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {user-authored cooking_log.notes → Recette détail step-1 marginalia} | T-32-05-01 mitigation already in place (React text children inside `<Marginalia>` — React default-escapes). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-01 | Information disclosure (XSS) | step-1 marginalia | mitigate | Unchanged from Phase 32 — `<Marginalia>` renders `recipeLog.notes` as React text child; React escapes by default. No `dangerouslySetInnerHTML`. |
| T-36-03-02 | DoS (visual) | TableVote geometry | accept | If a state class is misspelled and falls back to `seat-state-neutral`, the visual is silently degraded; verified by checkpoint Task 3. |
| T-36-03-03 | Tampering | POLISH-03 icon removal | accept | Pure JSX edit; `Timer`/`Flame` imports remain in scope (Flame on bottom CTA) or removed cleanly. No security boundary. |
</threat_model>

<verification>
- Lint + typecheck clean.
- Visual checkpoint covers all three requirements (SOBER-12, SOBER-13, POLISH-03).
- SUMMARY enumerates which §15.C gestures needed code vs were already shipped.
</verification>

<success_criteria>
- SOBER-12 acceptance met: Ragu detail (with photo + cooking-log note) shows all four §15.C cookbook gestures.
- SOBER-13 acceptance met: Accueil post-vote ledger rows show the table-à-manger geometry (`.table-plate` + `.table-seat` with state classes) inspectable via DOM tools.
- POLISH-03 acceptance met: meta-pill row is homogeneous all-text pills (no Clock, no Flame).
</success_criteria>

<output>
Create `.planning/phases/36-sober-kitchen-finish-polish/36-03-SUMMARY.md` with the receipt: gesture-by-gesture audit (4 gestures: present / shipped + diff), POLISH-03 changes (Timer/Flame imports affected), SOBER-13 outcome (verified vs shipped), checkpoint outcome.
</output>
