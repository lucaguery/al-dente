---
phase: 36-sober-kitchen-finish-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/BottomNav.tsx
autonomous: false
requirements: [SOBER-10]
must_haves:
  truths:
    - "The central « Ajouter » CTA reads visually as 'elevated CTA above the row' (not 'third tab with a terracotta pill')."
    - "The four flat sibling tabs (Accueil, Recettes, Profil) remain flush with the nav baseline; only the CTA pill lifts."
    - "Tap target hit-pad is unchanged; safe-area inset preserved; onboarding-route hide gate untouched."
  artifacts:
    - path: "frontend/components/BottomNav.tsx"
      provides: "Central-CTA variant with translateY(-12px) + drop shadow on the inner <span> only"
      contains: "translate-y-[-12px]"
  key_links:
    - from: "frontend/components/BottomNav.tsx (central-cta branch)"
      to: "Phase 31 mockup .scratch/capture-mockups/1-smart-paste.html"
      via: "inline transform on the inner pill span"
      pattern: "translate-y-\\[-12px\\]|translateY\\(-12px\\)"
---

<objective>
Lift the BottomNav central « Ajouter » CTA so it reads as an elevated CTA above the row, per the locked Phase 31 mockup (`.scratch/capture-mockups/1-smart-paste.html`) and the v0.7.1 SOBER-10 acceptance criterion. Phase 31 commit `62b4e96` shipped the terracotta pill but stopped short of the lift.

Purpose: Close the last visible gap in the BottomNav locked spec. The CTA must read as "the prominent capture entry point above the four sibling tabs," not "the third of four tabs."

Output: A modified `BottomNav.tsx` where the central-cta variant's inner `<span>` carries `translateY(-12px)` + a soft drop shadow, with the surrounding `<Link>` container retaining its `flex-1` shape (so the four siblings stay flush).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-sober-kitchen-finish-polish/36-CONTEXT.md
@frontend/components/BottomNav.tsx
@frontend/CLAUDE.md

<interfaces>
From `frontend/components/BottomNav.tsx`:
- `TABS: ReadonlyArray<Tab>` — 4 entries: Accueil (tab), Recettes (tab), Ajouter (central-cta), Profil (tab).
- `Tab = FlatTab | CentralCTA` discriminated union. `variant: "central-cta"` is the only CTA slot.
- Active treatment for the CTA is `ring-2 ring-primary/30 ring-offset-1 ring-offset-background` on the inner `<span>` (the additive ring, D-11 in 31-RESEARCH.md).
- The inner `<span aria-hidden>` already carries: `flex items-center justify-center rounded-full bg-primary text-primary-foreground w-14 h-14 transition-all duration-fast ease-craft active:scale-95`.
- The outer `<Link>` carries: `relative flex flex-col items-center justify-center flex-1 gap-1 text-xs font-medium transition-colors duration-fast ease-craft`.

Phase 31 NAV-01 invariants that MUST hold after this change:
- D-08/D-09: the « Ajouter » CTA only activates on exactly `/recipes/new`; Recettes prefix-match excludes `/recipes/new` (already in `isActive`).
- D-10: onboarding-route hide gate via `useSelectedLayoutSegment()` stays.
- D-11: pill is always filled; active state is the additive ring, not a transform of the pill geometry.
- D-12: mutually exclusive active across all 4 slots — unaffected by elevation.
- Safe-area inset on the `<nav>` element: `pb-[env(safe-area-inset-bottom)]` stays.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Elevate the central-CTA pill (translateY + drop shadow)</name>
  <files>frontend/components/BottomNav.tsx</files>
  <behavior>
    - The inner `<span>` (the `bg-primary` circle) is shifted up by 12px relative to its flow position via a CSS transform.
    - The pill carries a soft drop shadow that reads in both light and dark modes (use the existing `shadow-card` token or an equivalent `shadow-md`/`shadow-lg`-grade utility; favor a token over a one-off value).
    - The four flat sibling tabs (Accueil, Recettes, Profil) are NOT modified — their geometry stays flush.
    - Active treatment (`ring-2 ring-primary/30 ring-offset-1 ring-offset-background`) continues to additively layer with the lift; the pill never un-fills.
    - `active:scale-95` press feedback remains and composes cleanly with the translate.
    - The outer `<Link>` keeps `flex-1` so the slot still claims the same horizontal footprint — the elevation is purely vertical.
    - Hit-pad: the outer `<Link>` is the tap surface. The lifted pill must remain reachable inside the Link's flex column without bleeding outside the `<nav>` container. The lift is small enough (12px) that the pill is still vertically inside the nav (since `<nav>` has `min-h-[4.5rem]` = 72px and the pill is `w-14 h-14` = 56px — 8px slack at the top before bleed). If a future review judges 12px insufficient (visual review on a real iPhone may say 14-16px reads more "elevated"), bump the offset; do NOT add `overflow-visible` to the `<nav>` without a follow-up check that BottomNav backdrop-blur still composites correctly.
  </behavior>
  <action>
    Modify the central-cta branch in `BottomNav.tsx` (the `if (tab.variant === "central-cta")` block, lines ~80-103). Update the inner `<span>` className to add:
    1. `-translate-y-3` (Tailwind for `transform: translateY(-12px)`) — or use an inline style if Tailwind class composition is constrained by the existing `active:scale-95` transform.
    2. `shadow-card` (the existing project shadow token, see frontend/app/globals.css). If `shadow-card` is too subtle in dark mode, fall back to `shadow-lg`. Verify against the live dev render — the goal is "reads as elevated" not "specific shadow depth."

    IMPORTANT: Tailwind composes `-translate-y-3` and `active:scale-95` via the `transform` utility — both must coexist. If they conflict (one resetting the other), use a single inline `style={{ transform: `translateY(-12px) ${active ? "" : ""}` }}` with `active:scale-95` retained for the press feedback. Test the press animation in the dev build.

    DO NOT modify:
    - The outer `<Link>` `flex-1` (would break the 4-slot geometry).
    - The flat-tab branch (siblings stay flush).
    - The active-ring treatment.
    - The `<nav>` parent's `min-h-[4.5rem]` or `pb-[env(safe-area-inset-bottom)]` (safe-area + height invariants).

    No new i18n keys, no new TypeScript types, no new lucide imports.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - `BottomNav.tsx` central-cta branch has `-translate-y-3` (or equivalent inline transform) AND a shadow token on the inner `<span>`.
    - `grep -nE '(translate-y-3|translateY\(-12px\)|translateY\(-14px\))' frontend/components/BottomNav.tsx` returns at least one hit in the central-cta branch.
    - `npm run lint` clean.
    - `npx tsc --noEmit` clean.
    - The other three tab variants (Accueil/Recettes/Profil) are byte-identical to the current implementation (diff confined to the central-cta branch).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: BottomNav central « Ajouter » CTA elevation per SOBER-10 (translateY -12px + dro...</name>
  <action>Human verification — see how-to-verify steps below.</action>
  <files>n/a (manual verification)</files>
  <what-built>BottomNav central « Ajouter » CTA elevation per SOBER-10 (translateY -12px + drop shadow). Inner pill lifts above the four sibling tabs; the rest of the nav is unchanged.</what-built>
  <how-to-verify>
    1. `cd frontend &amp;&amp; npm run dev` (Next dev server on port 3000).
    2. In a separate terminal: `cd backend &amp;&amp; uv run uvicorn app.main:app --reload --port 8000` (backend on 8000). `uv run seed` if seed has not been loaded.
    3. Open `http://localhost:3000/` in Safari (or Chrome simulating iPhone 14 viewport — 390×844, touch). Sign in as Luca via the test cookie if needed: `document.cookie = "aldente_auth=test-token-luca; path=/; SameSite=Lax"`.
    4. Look at the BottomNav at the bottom of the screen. The « Ajouter » pill should sit visibly ABOVE the row of four flat tabs — like a button protruding upward from the nav bar, with a soft drop shadow underneath suggesting elevation.
    5. Tap the pill — confirm it routes to `/recipes/new` and that the `active:scale-95` press feedback still fires.
    6. Navigate to `/recipes/new` directly and confirm the active-ring (`ring-2 ring-primary/30`) renders ON the lifted pill.
    7. Compare side-by-side with `.scratch/capture-mockups/1-smart-paste.html` (open in browser) — the CTA shape and lift register should match.
    8. Test on a real iPhone if available (Vercel preview deploy or local dev via LAN IP) — confirm safe-area inset still works (the lifted pill should not be clipped at the bottom).
  </how-to-verify>
  <resume-signal>Type "approved" if the lift matches the locked mockup; describe the issue otherwise (e.g. "pill clipped at the top," "shadow not visible," "active ring renders below the lifted pill").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure CSS/JSX change. No data crosses boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Tampering | BottomNav DOM | accept | CSS-only change; no attack surface introduced. |
| T-36-01-02 | DoS (visual) | BottomNav safe-area | mitigate | Verify on real iPhone (checkpoint Task 2) — if safe-area inset interacts badly with the lifted pill, scope expands to nav container overflow handling. |
</threat_model>

<verification>
- Lint + typecheck clean (`npm run lint` + `npx tsc --noEmit`).
- Diff confined to the central-cta branch of `BottomNav.tsx`.
- Visual checkpoint approved by user (iPhone or 390×844 simulator).
</verification>

<success_criteria>
- SOBER-10 acceptance met: central « Ajouter » CTA reads as elevated above the four flat tabs on a real iPhone viewport.
- No regression in the four flat tab variants, the active-ring on the CTA, the safe-area inset, or the onboarding-route hide gate.
</success_criteria>

<output>
Create `.planning/phases/36-sober-kitchen-finish-polish/36-01-SUMMARY.md` when done with a brief receipt of: change site (single branch in BottomNav.tsx), Tailwind class diff, shadow token chosen, checkpoint outcome.
</output>
