---
phase: 36-sober-kitchen-finish-polish
plan: 06
type: execute
wave: 2
depends_on: [36-01, 36-02, 36-03, 36-04, 36-05]
files_modified:
  - frontend/components/HomeDecide.tsx
  - frontend/components/VoteSummary.tsx
  - frontend/app/globals.css
  - frontend/lib/i18n/fr.json
autonomous: false
requirements: [SOBER-09, SOBER-15, POLISH-04]
must_haves:
  truths:
    - "Accueil renders the Composition A ledger from FIRST PAINT — not gated on `allVoted` (`unvotedByMe.length === 0`). The pre-vote and post-vote screens look the same except for the un-voted row's affordance."
    - "On a fresh seed (no votes yet on Tacos), Accueil shows the ledger with: Ragu (Validé highlighted) + Coq (Pressenti) + Butter chicken (Contesté) + Tacos (un-voted, with inline vote affordance) + Shawarma (Rejeté, muted)."
    - "The un-voted row embeds the swipe-deck / vote-tap affordance inline as a special row variant — NOT as a separate full-screen overlay above the ledger. After the user votes on the row, it transitions to the appropriate computed state (Pressenti / Validé / Contesté / Rejeté) without unmounting/remounting the page."
    - "The Rejeté row renders in a muted state alongside the other four (CONTEXT §SOBER-15 resolution = option a, render muted; NOT hide)."
    - "On narrow viewports (320px), the bottom CTA copy 'Cuisiner ce soir' fits cleanly with the recipe title shown as Caveat-slant marginalia underneath the button. Long titles never clip or wrap the button text."
  artifacts:
    - path: "frontend/components/HomeDecide.tsx"
      provides: "The dual-mode `allVoted ? <VoteSummary /> : <ShortlistDeck />` toggle is replaced by a single `<VoteSummary />` (or renamed `<AccueilLedger />`) which renders the ledger from first paint; `<ShortlistDeck />` is removed from this surface. The un-voted card flows in as a special row variant inside the ledger composition."
      contains: "VoteSummary|AccueilLedger"
    - path: "frontend/components/VoteSummary.tsx"
      provides: "Updated to (a) render the rejete row in a muted variant instead of filtering it out (SOBER-15), (b) render the un-voted row(s) with an inline vote affordance (yes/no thumb buttons OR a tap-seat-on-the-table-vote-scene affordance — implementer's call based on which composes most cleanly with the existing `TableVote` primitive), (c) render the bottom CTA copy as 'Cuisiner ce soir' with the recipe title as marginalia underneath (POLISH-04)."
      contains: "row-state-rejete|seat-state-rejected|cook_short"
    - path: "frontend/app/globals.css"
      provides: "(if needed) `.shortlist-row.row-state-rejete` muted-variant class — 60% opacity, muted-foreground text, thin destructive-border-left rule — per CONTEXT §SOBER-15 visual treatment."
      contains: "row-state-rejete"
    - path: "frontend/lib/i18n/fr.json"
      provides: "New keys: `home.cta.cook_short` = 'Cuisiner ce soir'; existing `home.cta.cook_named` retained for places that still use the named variant. POLISH-04 changes the Accueil bottom CTA to `cook_short` + marginalia subline."
      contains: "cook_short"
  key_links:
    - from: "frontend/components/HomeDecide.tsx render branch"
      to: "frontend/components/VoteSummary.tsx"
      via: "single render path (no more shortlist-deck-or-summary branching on allVoted)"
      pattern: "VoteSummary"
    - from: "frontend/components/VoteSummary.tsx rejete row"
      to: "frontend/app/globals.css .row-state-rejete"
      via: "className composition"
      pattern: "row-state-rejete|muted-foreground"
    - from: "frontend/components/VoteSummary.tsx bottom CTA"
      to: "frontend/lib/i18n/fr.json home.cta.cook_short"
      via: "tHome('cta.cook_short')"
      pattern: "cook_short|Cuisiner ce soir"
---

<objective>
Port `HomeDecide.tsx` to the locked Composition A ledger from first paint: the un-voted card embeds as a special row variant inline at its alphabetical / state-derived position, the Validé row is visible immediately, and the dual-mode `unvotedCount === 0 ? <VoteSummary /> : <ShortlistDeck />` toggle is retired. Concurrently render the Rejeté row in a muted variant alongside the other four states (SOBER-15 resolution per CONTEXT §SOBER-15), and shorten the bottom CTA to "Cuisiner ce soir" with the recipe title rendered as Caveat-slant marginalia underneath to fix the 320px-viewport clip issue (POLISH-04).

This is the biggest plan in Phase 36 and the most semantically load-bearing. The first-paint-ledger port retires the dual-mode swipe-deck cleanly (MVP posture: clean rewrites, no compat shims).

Purpose: Close SOBER-09 (Composition A from first paint — user-locked decision), SOBER-15 (Rejeté row rendered muted), POLISH-04 (CTA truncation graceful on narrow phones). Structurally resolves LIVE-04 (Phase 34's marginalia branch guard becomes redundant but stays as defense-in-depth — per CONTEXT).

Output: A rewritten Accueil composition where `HomeDecide.tsx` renders the ledger from first paint, `VoteSummary.tsx` adopts an inline un-voted-row variant + muted-rejete-row variant + truncated bottom CTA, and `globals.css` carries any new utility classes the muted-rejete variant requires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-sober-kitchen-finish-polish/36-CONTEXT.md
@.planning/quick/260518-kba-ui-walkthrough-punch-list/PUNCH-LIST.md
@docs/design-system.html
@frontend/components/HomeDecide.tsx
@frontend/components/VoteSummary.tsx
@frontend/components/ShortlistDeck.tsx
@frontend/components/TableVote.tsx
@frontend/components/Marginalia.tsx
@frontend/app/globals.css
@frontend/lib/i18n/fr.json
@frontend/lib/votes.ts
@frontend/CLAUDE.md

<interfaces>
**Current `HomeDecide.tsx` render branches (~lines 546-590):**
```typescript
{shortlistIsEmpty ? (
  <EmptyState .../>  // shortlist exists but empty
) : allVoted ? (
  <VoteSummary recipes={dealableRecipes} votes={shortlist.votes} me partner ... />
) : (
  <ShortlistDeck shortlistId recipes={unvotedByMe} votes me partner onVoteApplied />
)}
```
Goal: collapse the `allVoted ? <VoteSummary /> : <ShortlistDeck />` branch to a single `<VoteSummary />` (renamed `<AccueilLedger />` if the executor prefers — but keep the component file name `VoteSummary.tsx` to limit blast radius; rename in a follow-up if desired).

**Current `VoteSummary.tsx`:**
- Filters Rejeté rows OUT (`rows = recipes.filter((r) => stateFor(r.id) !== "rejete")`). SOBER-15 inverts this — Rejeté rows SHOULD render in the muted variant.
- Bottom CTA copy: `tHome("cta.cook_named", { title: ctaTarget.title })` → "Cuisiner {long recipe title}". POLISH-04 changes to `tHome("cta.cook_short")` = "Cuisiner ce soir" with a `<Marginalia>` line underneath showing the recipe title.
- Does NOT today render an un-voted-row affordance — that's the new ShortlistDeck behavior being absorbed.

**Current `ShortlistDeck.tsx`:**
- Renders one card at a time with swipe + thumb buttons.
- Calls `onVoteApplied(vote)` on commit.
- After SOBER-09, the ledger composition absorbs the inline-row variant of the un-voted affordance; ShortlistDeck stops being mounted by HomeDecide. The file `ShortlistDeck.tsx` MAY remain in the repo (other surfaces — none found via grep) or be deleted in a follow-up. For this plan: leave `ShortlistDeck.tsx` on disk but un-import it from `HomeDecide.tsx`. A future cleanup can delete it.
- Also calls `postVote()` from `lib/votes.ts`; the new inline affordance reuses the same `postVote` call.

**From `frontend/lib/votes.ts`:**
- `postVote({shortlistId, recipeId, vote})` POSTs the vote to the backend; returns the canonical `ShortlistVote`.
- The realtime echo (`vote.created`) lands via `HomeDecide`'s existing handler.

**From `frontend/components/TableVote.tsx`:**
- Renders the `.table-scene` + seats for an EXISTING shortlist row (votes already in state).
- For the un-voted row, a special "seat tap to vote" affordance could be added — but per CONTEXT, the simplest port is to embed inline `<ShortlistThumbButtons>` (the existing primitive in `ShortlistCard.tsx`) below the row's `TableVote`. The implementer chooses the cleanest composition based on what reads as one consistent ledger.

**From `docs/design-system.html` §15.A (the locked spec mockup, ~lines 1488-1533):**
The ledger contains 4 rows (the seed Shawarma rejete-filtered out in the original spec; per SOBER-15 resolution it's restored as a 5th muted row):
- Risotto safran (is-valide variant — emerald-tint background, halo seats, marginalia "validé · à cuisiner")
- Tagliatelles aux champignons (pressenti — neutral row, partial halo on one seat)
- Bœuf bourguignon (contesté — neutral row, two-toned halos)
- Pizza margherita (sans avis — neutral row, no halos)

Bottom CTA: `<button class="btn btn-primary btn-lg" style="width:100%;"><i data-lucide="flame"></i>Cuisiner Risotto au safran</button>` — currently long-title. POLISH-04 truncates this.

**From CONTEXT §SOBER-15 (resolved: render muted):**
- Apply Phase 32's existing muted-tint variant if it exists; otherwise add a `.row-state-rejete` (or `.shortlist-row.row-state-rejete`) class with `--color-muted-foreground` text + 60% opacity + thin destructive-border-left rule.
- The TableVote seat state is already `seat-state-rejected` for the rejete aggregate — that's the per-seat treatment; the row-level treatment is what this class adds.

**From CONTEXT §POLISH-04 (truncation):**
- Bottom CTA reads "Cuisiner ce soir" (short).
- Recipe title rendered as Caveat-slant marginalia UNDERNEATH the button — separate visual layer, never clipped by the button width.
- Add i18n key `home.cta.cook_short` = "Cuisiner ce soir".

**Dependency rationale for `depends_on: [36-01..05]`:**
- 36-01 (BottomNav lift) — orthogonal; no shared file.
- 36-02 (Patine view) — orthogonal; no shared file.
- 36-03 (Recette détail + TableVote) — TableVote.tsx is touched by Plan 36-03 (verify), and VoteSummary.tsx (this plan) consumes TableVote. Sequencing avoids merge conflicts if Plan 36-03 ships TableVote changes.
- 36-04 (seed cook_count bump) — orthogonal but the SOBER-14 dogear is best demoed alongside the full Accueil port.
- 36-05 (design-system.html docs) — orthogonal; doc only.
The plan does NOT require Plan 36-01..05 to ship before this one CAN start work — it just sequences to a later wave to keep `HomeDecide.tsx` and `VoteSummary.tsx` editing serialized against the other plans that touch `frontend/app/globals.css`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port HomeDecide.tsx to render the ledger from first paint; retire the dual-mode toggle</name>
  <files>frontend/components/HomeDecide.tsx</files>
  <behavior>
    - The `allVoted ? <VoteSummary /> : <ShortlistDeck />` branch (~lines 569-590) collapses to a single `<VoteSummary />` render path. `ShortlistDeck` is no longer imported or rendered from `HomeDecide.tsx`.
    - `VoteSummary` receives a new prop `unvotedByMe: Recipe[]` (the recipes the local user hasn't voted on — used by VoteSummary to render the inline un-voted row affordance), in addition to its existing props.
    - The `shortlist.recipes` collection (NOT `dealableRecipes` — the rejete-filter is moved into VoteSummary's row-state-rejete render branch) is passed to VoteSummary so it can render all 5 states including muted Rejeté.
    - The `onVoteApplied` callback is passed to VoteSummary too (used by the inline affordance).
    - The subhead logic (`validéCount` / `pressentiCount` / `subheadKey` ~lines 471-483) is UNCHANGED — Phase 34 LIVE-04 hardened it and CONTEXT §"Cooperation with v0.7 phases" explicitly keeps the marginalia branch guard as defense-in-depth.
    - The empty-shortlist guard (`shortlistIsEmpty`) is unchanged.
    - The no-partner branch and the `!shortlist` (no shortlist for today) branch are unchanged.
    - All realtime handlers (`onVoteCreated`, `onShortlistCreated`, `onCookingStarted`, `onCookingFinalized`) are unchanged.
  </behavior>
  <action>
    1. In `HomeDecide.tsx`:
       - Remove the import of `ShortlistDeck` (~line 22).
       - In the main return (line ~569), replace the three-branch ternary:
         ```typescript
         ) : allVoted ? (
           <VoteSummary ... />
         ) : (
           <ShortlistDeck ... />
         )
         ```
         with a single `<VoteSummary />` render that receives `recipes={shortlist.recipes}` (NOT `dealableRecipes`), plus the new `unvotedByMe={unvotedByMe}` prop, plus `onVoteApplied={handleVoteApplied}`.
       - Keep the `unvotedByMe`/`allVoted` derivations local (~lines 453-458) — `unvotedByMe` is still useful for the inline affordance branching.
       - The `dealableRecipes` filter (~lines 446-450) can stay or be deleted — VoteSummary now owns the rejete-row logic. Cleaner to delete `dealableRecipes` to avoid stale references; the executor decides.
    2. Verify no other surface still uses ShortlistDeck (grep `ShortlistDeck` repo-wide). If grep shows only HomeDecide.tsx, the import removal here is safe. The `ShortlistDeck.tsx` file itself stays on disk — a follow-up plan can delete it.
    3. The `unvotedByMe` derivation today uses `dealableRecipes` (rejete-filtered). With SOBER-15 keeping the rejete row visible, the un-voted-by-me set for the inline affordance can still skip rejete recipes (you can't usefully vote on a recipe both members rejected). Either: keep `unvotedByMe` derived from `dealableRecipes` (skip rejete from the un-voted affordance), OR derive from `shortlist.recipes` directly (allow voting on rejete recipes too — semantically odd). Pick the first; explain in code comment.
    4. The bottom-CTA cook handler (`handleCookStart`) is unchanged. VoteSummary internally decides which row is the cook target.
    5. NO new useEffect, NO new useState, NO new realtime handler. The dual-mode collapse is pure render-tree restructuring.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "ShortlistDeck" frontend/components/HomeDecide.tsx</automated>
  </verify>
  <done>
    - `grep -c "ShortlistDeck" frontend/components/HomeDecide.tsx` returns 0 (import removed, JSX usage removed).
    - `grep -c "VoteSummary" frontend/components/HomeDecide.tsx` returns ≥ 2 (import + JSX usage).
    - The other render branches (partner-waiting, shortlist=null, shortlistIsEmpty) are byte-identical to current code (diff confined to the deck/summary toggle).
    - `npm run lint` + `npx tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adapt VoteSummary.tsx — render all 5 states (Rejeté muted), embed inline un-voted-row affordance, truncate bottom CTA</name>
  <files>frontend/components/VoteSummary.tsx, frontend/app/globals.css, frontend/lib/i18n/fr.json</files>
  <behavior>
    - VoteSummary accepts new props: `unvotedByMe: Recipe[]` + `onVoteApplied: (vote: ShortlistVote) => void` (in addition to existing).
    - VoteSummary's `rows` derivation stops filtering Rejeté. All 5 states render: Validé / Pressenti / Contesté / Rejeté / Sans avis.
    - The Rejeté row carries a `row-state-rejete` (or equivalent) class that delivers the muted visual treatment per CONTEXT §SOBER-15: muted-foreground text + 60% opacity + thin destructive-border-left rule.
    - For recipes the local user has not voted on (in `unvotedByMe`), the row renders an inline vote affordance: either (a) `<ShortlistThumbButtons>` from `ShortlistCard.tsx` reused inline next to the TableVote scene, OR (b) tap-the-seat-on-TableVote-to-vote (lower complexity if the implementer can add an `onSeatTap` handler to TableVote in this plan). Pick whichever composes most cleanly. The chosen approach is documented in the SUMMARY.
    - After the local user votes via the inline affordance, the row re-renders with the new computed state (e.g. Pressenti if only the user voted yes). The `onVoteApplied(vote)` callback updates HomeDecide's `shortlist.votes` state optimistically; the realtime echo overwrites canonically.
    - The bottom CTA copy is `tHome("cta.cook_short")` = "Cuisiner ce soir". A `<Marginalia size="sm" slant>` line BELOW the button shows the recipe title (e.g. `Ragu bolognese`). At 320px viewport: the button never clips, the title never wraps, the marginalia handles overflow with ellipsis if needed.
    - `home.cta.cook_named` stays in fr.json (other surfaces may use it — verify before deletion; if used only by VoteSummary, safe to remove in a follow-up).
  </behavior>
  <action>
    1. Update `VoteSummaryProps` to add `unvotedByMe: Recipe[]` + `onVoteApplied: (vote: ShortlistVote) => void`.
    2. Change `rows = recipes.filter((r) => stateFor(r.id) !== "rejete")` to `rows = recipes` (or sort by state-order if needed — see step 3). The empty-state guard (`if (rows.length === 0)`) stays as defense-in-depth.
    3. Optional but recommended: sort `rows` by computed state order to match §15.A layout — Validé first, then Pressenti, then Contesté, then Sans avis, then Rejeté (muted) at the bottom. The seeded display will read Validé → Pressenti → Contesté → un-voted (Sans avis with affordance) → Rejeté. Implement via a `STATE_ORDER` const + a stable sort.
    4. In the row-render loop, add a per-row variant decision:
       - `isValide` (existing) — emerald-tint background + halo.
       - `isRejete = state === "rejete"` (new) — apply `row-state-rejete` class.
       - `isUnvoted = unvotedByMe.some((u) => u.id === r.id)` (new) — render the inline vote affordance INSIDE the row.
    5. For the inline vote affordance (the un-voted row), pick ONE of:
       - **(a) Reuse `<ShortlistThumbButtons>`:** import from `ShortlistCard.tsx`, render below the row's title/meta block. Wire its yes/no buttons to call `postVote()` then `onVoteApplied()`. Match the row's compact height.
       - **(b) Tap-the-seat:** add an `onSeatTap?: (seatPosition: SeatPosition) => void` prop to `TableVote.tsx` (Task 3) and wire VoteSummary to translate seat taps to vote calls. Lower clutter; higher implementation cost.
       The implementer picks based on the live render; document in SUMMARY.
    6. Add the new CSS class `.shortlist-row.row-state-rejete` to `frontend/app/globals.css` (near the existing `.shortlist-row` block):
       ```css
       .shortlist-row.row-state-rejete {
         opacity: 0.6;
         color: var(--color-muted-foreground);
         border-left: 2px solid color-mix(in oklch, var(--destructive) 50%, transparent);
       }
       ```
       Visual register matches the muted-foreground text + 60% opacity + thin destructive-border-left rule per CONTEXT.
    7. Add i18n key `home.cta.cook_short` to `frontend/lib/i18n/fr.json` = "Cuisiner ce soir".
    8. Replace the bottom-CTA render (~lines 178-188):
       ```typescript
       <Button onClick={() => onCookStart(ctaTarget.id)} disabled={cookInFlight} className="w-full h-12">
         <Flame size={18} className="mr-2" aria-hidden />
         {tHome("cta.cook_short")}
       </Button>
       <Marginalia size="sm" slant className="text-center mt-1" style={{ fontSize: "13px" }}>
         {ctaTarget.title}
       </Marginalia>
       ```
       The title sits BELOW the button as marginalia. The button copy is fixed-length; the title sits in its own visual layer where wrapping/ellipsis is acceptable.
    9. Validate at 320px viewport (Safari device toolbar or Chrome dev-tools mobile preview). The button must not wrap, the title-marginalia must not overlap the next page section.

    DO NOT:
    - Modify the `TableVote.tsx` seat-state mapping unless adopting option (b) above (and document in SUMMARY if so).
    - Modify the realtime handlers — they live in HomeDecide and consume `onVoteApplied`.
    - Add a new vote-state to `computeVoteState` (invariant #2 — vote states are computed from existing rules; no new enum value).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; grep -c "row-state-rejete" frontend/components/VoteSummary.tsx frontend/app/globals.css | head -5</automated>
  </verify>
  <done>
    - `grep -c "row-state-rejete" frontend/app/globals.css` ≥ 1.
    - `grep -c "row-state-rejete" frontend/components/VoteSummary.tsx` ≥ 1.
    - `grep -c "cook_short" frontend/lib/i18n/fr.json` ≥ 1.
    - `grep -c "cook_short" frontend/components/VoteSummary.tsx` ≥ 1.
    - `grep -c "rows = recipes.filter" frontend/components/VoteSummary.tsx` returns 0 (the rejete-filter is removed from the rows derivation).
    - `npm run lint` + `npx tsc --noEmit` clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Accueil renders Composition A ledger from first paint (SOBER-09)</name>
  <action>Human verification — see how-to-verify steps below.</action>
  <files>n/a (manual verification)</files>
  <what-built>Accueil renders Composition A ledger from first paint (SOBER-09); the un-voted card embeds as an inline row variant; Rejeté row renders muted (SOBER-15); bottom CTA truncates to "Cuisiner ce soir" + title marginalia underneath (POLISH-04).</what-built>
  <how-to-verify>
    1. Frontend + backend dev servers running; `uv run seed` loaded; signed in as Luca.
    2. Open `http://localhost:3000/` at iPhone 14 viewport (390×844, touch).
    3. **First-paint ledger (SOBER-09):**
       - The screen shows the ledger from the very first render (NOT the swipe-deck). Confirm 5 rows visible (with the seed):
         a. Ragu bolognese — Validé highlighted (emerald tint, halo seats)
         b. Coq au vin — Pressenti (one halo seat)
         c. Butter chicken — Contesté (two-toned)
         d. Tacos boeuf — un-voted (special row with inline vote affordance)
         e. Shawarma — Rejeté (muted, ~60% opacity, destructive-border-left)
       - The Validé row (Ragu) reads "validé · à cuisiner" marginalia in emerald-emphasis.
       - The Rejeté row (Shawarma) reads with muted text and a thin destructive border on the left edge.
    4. **Inline un-voted affordance (SOBER-09):**
       - The Tacos row carries the vote affordance (thumb buttons or seat-tap, per executor choice).
       - Tap "J'aime cette recette" (or seat-tap-yes). The row transitions to Pressenti state (Luca voted yes, partner unvoted). No page unmount.
       - Refresh the seed (`uv run seed` from another terminal) to reset. Vote NO on Tacos — the row transitions to a different state (likely Rejeté since partner already pseudo-rejected via the seed? Verify with the seed votes).
    5. **Bottom CTA truncation (POLISH-04):**
       - The bottom CTA reads "Cuisiner ce soir" (short, fixed-length).
       - A Caveat-slant marginalia line below the button shows "Ragu bolognese" (the Validé recipe).
       - Resize the viewport to 320px width. The button still fits cleanly; the title underneath does not push it off-screen.
    6. **LIVE-04 consistency (defense-in-depth from CONTEXT):**
       - With NO Validé row present (re-seed with all rejete votes? Or zero-vote scenario), the subhead "déjà une idée validée" does NOT render — the marginalia branch guard from Phase 34 still works.
    7. **Realtime sync:**
       - Open a second browser session as the partner (`document.cookie = "aldente_auth=test-token-partner; path=/; SameSite=Lax"` then reload). Vote on Tacos as partner. Confirm the ledger row updates on Luca's session within ~200ms (the existing `vote.created` echo).
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 states render from first paint, the un-voted affordance works inline, the muted Rejeté row renders correctly, and the bottom CTA fits at 320px; describe issues otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Vote POST (`postVote()` already in lib/votes.ts). No new boundary; the inline affordance reuses the existing endpoint. |
| realtime echo → client | `vote.created` event reconciles optimistic and canonical state. Pre-existing path, no change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06-01 | Tampering | client-side computed state | accept | Invariant #2 unchanged — `computeVoteState` is the single derivation path; no new state slot. |
| T-36-06-02 | DoS (UX) | Inline un-voted affordance | mitigate | Reuse `postVote()` + `onVoteApplied()` optimistic path; rollback on failure already in `ShortlistDeck`'s pattern. Spam-tap mitigation: per-recipe `submittingFor` flag (mirror ShortlistDeck's pattern). |
| T-36-06-03 | Information disclosure | Rejeté row muted treatment | accept | Render policy choice — partner's no-vote becomes visible in the muted row. Per CONTEXT: "knowing the partner rejected something is informative ('we agreed off this')." Couple-scale; no PII concern. |
| T-36-06-04 | DoS (visual) | 320px viewport CTA layout | mitigate | Marginalia title underneath the button is in its own visual layer; ellipsis-on-overflow tolerated. Validated by checkpoint at 320px. |
</threat_model>

<verification>
- Lint + typecheck clean.
- Manual checkpoint covers all five rows on first paint, the inline un-voted affordance, the muted Rejeté treatment, the truncated CTA at 320px.
- LIVE-04 marginalia branch guard still works (verified at the empty-Validé state).
- No regression in realtime sync (vote.created, shortlist.created, cooking.started, cooking.finalized).
</verification>

<success_criteria>
- SOBER-09 acceptance met: fresh-seed Accueil shows the ledger from first paint; Ragu bolognese Validé visible AND un-voted Tacos card embedded as a row.
- SOBER-15 acceptance met: with the seed's Shawarma in Rejeté state, the ledger shows it muted (option a from CONTEXT).
- POLISH-04 acceptance met: at 320px viewport, the button does not wrap or clip; recipe title still legible underneath as marginalia.
- LIVE-04 marginalia branch guard still works (zero-Validé state still suppresses "déjà une idée validée"); SOBER-09 + LIVE-04 are mutually consistent.
</success_criteria>

<output>
Create `.planning/phases/36-sober-kitchen-finish-polish/36-06-SUMMARY.md` with the receipt: dual-mode toggle retired (HomeDecide.tsx diff), VoteSummary.tsx prop additions + row-state-rejete class, inline-affordance choice (thumb buttons vs seat-tap), bottom-CTA truncation diff, i18n key added, checkpoint outcome (all 5 rows, narrow-viewport CTA, realtime sync).
</output>
