---
phase: 36-sober-kitchen-finish-polish
plan: 07
type: execute
wave: 3
depends_on: [36-06]
files_modified:
  - frontend/app/recipes/page.tsx
  - frontend/app/recipes/[id]/page.tsx
  - frontend/components/RecipeCard.tsx
  - frontend/components/RecipeRow.tsx
  - frontend/components/VoteSummary.tsx
  - frontend/components/HomeDecide.tsx
  - frontend/lib/i18n/fr.json
  - frontend/app/globals.css
autonomous: false
requirements: [POLISH-01, POLISH-02]
must_haves:
  truths:
    - "Every visible `·` separator in `frontend/{app,components}` user-facing copy uses NBSP both sides via the `meta-sep` pattern (a `<span class=\"meta-sep\"> · </span>` element OR the literal NBSP characters `\\u00A0·\\u00A0` in i18n strings)."
    - "After POLISH-01 sweep: `grep -rn '·' frontend/{app,components}` returns matches ONLY inside `meta-sep` spans or NBSP-padded i18n strings — bare `·` next to a non-NBSP space is forbidden in user-facing copy."
    - "On Accueil first paint, with `Notification.permission === 'default'`, the locked H1 « On mange quoi ce soir ? » + Caveat marginalia subhead render FIRST. The PushPermissionBanner sits BELOW the shortlist ledger (or behind a Réglages CTA — implementer's call per CONTEXT)."
  artifacts:
    - path: "frontend/components/{RecipeCard,RecipeRow}.tsx, frontend/app/recipes/{[id]/,}page.tsx, frontend/components/VoteSummary.tsx, frontend/lib/i18n/fr.json"
      provides: "All visible `·` separators replaced by `meta-sep` spans (NBSP both sides) OR by the literal NBSP characters in i18n strings"
      contains: "meta-sep|\\u00A0"
    - path: "frontend/components/HomeDecide.tsx"
      provides: "PushPermissionBanner repositioned — no longer above the H1 header. New mount point is below the ledger (or as a Réglages CTA chip — implementer's call)."
      contains: "PushPermissionBanner"
    - path: "frontend/app/globals.css"
      provides: "`.meta-sep` utility class (if not already present) — renders an inline NBSP `·` NBSP separator at the foreground-muted color used by meta caption text"
      contains: ".meta-sep"
  key_links:
    - from: "All meta-row sites in frontend/{app,components}"
      to: "frontend/app/globals.css .meta-sep utility"
      via: "shared className composition"
      pattern: "className=\"meta-sep\"|class=\"meta-sep\""
    - from: "frontend/components/HomeDecide.tsx new push banner mount point"
      to: "below the shortlist ledger OR a settings affordance"
      via: "JSX placement"
      pattern: "PushPermissionBanner"
---

<objective>
French-typography sweep + push-banner relocation. Two small but cross-file polish items: (1) every visible `·` separator in user-facing copy gets NBSP both sides via the `meta-sep` pattern (POLISH-01); (2) the PushPermissionBanner moves off the top of Accueil so the locked H1 + marginalia is the first paint, the banner relocates BELOW the shortlist ledger or behind a Réglages CTA (POLISH-02).

Purpose: Close POLISH-01 (French typography hygiene) + POLISH-02 (Accueil hierarchy fix — locked spec says "shortlist au centre" not "notification opt-in front and center").

This plan depends on Plan 36-06 because that plan rewrites `HomeDecide.tsx` (one of the push-banner mount sites) and `VoteSummary.tsx` (one of the middle-dot sites). Landing after 36-06 avoids merge conflicts.

Output: A `meta-sep` utility class in `globals.css` (if not already present); every meta-row that uses `·` in `frontend/{app,components}` rewritten to use the utility (or NBSP literals in i18n); the PushPermissionBanner removed from above the H1 in `HomeDecide.tsx` and relocated to a sensible secondary position.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-sober-kitchen-finish-polish/36-CONTEXT.md
@.planning/quick/260518-kba-ui-walkthrough-punch-list/PUNCH-LIST.md
@frontend/components/HomeDecide.tsx
@frontend/components/VoteSummary.tsx
@frontend/components/RecipeCard.tsx
@frontend/components/RecipeRow.tsx
@frontend/app/recipes/page.tsx
@frontend/app/recipes/[id]/page.tsx
@frontend/components/PushPermissionBanner.tsx
@frontend/app/globals.css
@frontend/lib/i18n/fr.json
@frontend/CLAUDE.md

<interfaces>
**Current `·` separator sites in `frontend/{app,components}` (from `grep -rn '·' frontend/{app,components}`):**

| File | Line | Current form | Required form |
|------|------|--------------|---------------|
| `frontend/app/recipes/[id]/page.tsx` | ~852 | `· {recipe.servings} personnes` (regular space + `·` + space) | NBSP+`·`+NBSP OR `<span className="meta-sep">·</span>` |
| `frontend/app/recipes/[id]/page.tsx` | ~960 | `{...}{" "}·{" "}{...}` (template-literal style) | meta-sep span or NBSP literals |
| `frontend/app/recipes/page.tsx` | ~86 | `· {count}` (in PatinaSection Marginalia) | meta-sep span or NBSP |
| `frontend/components/RecipeCard.tsx` | ~151 | `<span aria-hidden>·</span>` (already hidden but spacing relies on flex gap) | replace with meta-sep span |
| `frontend/components/RecipeRow.tsx` | ~80 | `<span aria-hidden>·</span>` | replace with meta-sep span |
| `frontend/components/VoteSummary.tsx` | ~168 | `.join(" · ")` (joins cuisine + prep_time) | join with `"  ·  "` OR refactor to JSX with meta-sep span |

**Current `·` sites in i18n strings (`grep '·' frontend/lib/i18n/fr.json`):**
- Line 44: `"valide_meta": "validé · à cuisiner"` — regular spaces around `·`. Replace with ` ` literals: `"validé · à cuisiner"` (Note: in JSON, ` ` is a literal escape sequence that JavaScript parses correctly; the rendered text contains NBSP characters.)
- Line ~303: `"progress_capture": "Capture · {count, plural, ...}"` — same treatment.

**Current PushPermissionBanner mount sites in `HomeDecide.tsx` (after Plan 36-06):**
- After 36-06 ships, the dual-mode-toggle collapses. PushPermissionBanner is mounted near the top of the return JSX (~lines 408 and 509 in the pre-36-06 code; after 36-06, there will likely be one canonical mount point near the top).
- The locked spec (design-system.html §15.A + UI-SPEC §9.1) says first paint = H1 « On mange quoi ce soir ? » + Caveat marginalia subhead. The banner sits ABOVE these today — that's the bug.

**`PushPermissionBanner.tsx` (referenced):**
- Self-contained component; checks `Notification.permission` and renders either the opt-in card or `null` (when permission granted / denied / dismissed). Mount-point change is the only thing this plan does to push UX.
- Don't refactor the component; just move its mount site.

**The `meta-sep` utility (per ROADMAP success criterion 8 + POLISH-01 spec):**
- A reusable `<span className="meta-sep"> · </span>` rendered by all the meta-row sites.
- CSS: probably just `white-space: nowrap` + `color: inherit` + the NBSP characters baked in. Or, alternatively, use CSS `::before` / `::after` to insert the NBSP around a `·` content.
- Cleanest: a JSX component or className that renders ` · ` literal characters with `white-space: nowrap` so the separator never breaks across lines.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the meta-sep utility and apply it to every middle-dot separator in user-facing copy</name>
  <files>frontend/app/globals.css, frontend/app/recipes/page.tsx, frontend/app/recipes/[id]/page.tsx, frontend/components/RecipeCard.tsx, frontend/components/RecipeRow.tsx, frontend/components/VoteSummary.tsx, frontend/lib/i18n/fr.json</files>
  <behavior>
    - A `.meta-sep` CSS class exists in `globals.css` and renders an inline NBSP `·` NBSP separator at the inherited color, with `white-space: nowrap` so it never breaks across lines.
    - Two approaches acceptable (executor picks one and applies consistently):
      - **(a) CSS-driven:** `.meta-sep::before { content: "\\00A0"; } .meta-sep::after { content: "\\00A0"; } .meta-sep { white-space: nowrap; }` and the JSX renders `<span className="meta-sep">·</span>` — the NBSPs are CSS-generated, not in the DOM text content.
      - **(b) Literal NBSP characters:** `.meta-sep { white-space: nowrap; }` and the JSX renders `<span className="meta-sep">{" · "}</span>` — the NBSPs are in the DOM text content.
      Pick (b) for slightly better screen-reader behavior; document choice in SUMMARY.
    - Every site listed in the `<interfaces>` table gets the new pattern. Bare `· `+space sequences in JSX disappear.
    - i18n strings (`fr.json`) with middle-dot get NBSP literals via ` ` escape sequences.
    - The acceptance grep gate: `grep -rn '·' frontend/{app,components} | grep -v meta-sep` returns ZERO matches OR only flagged exceptions. (Some `·` may legitimately remain inside comments like `RecipeCard.tsx:6: // (cuisine Badge · relative last-cooked).` — comments are NOT user-facing copy. The grep gate should exclude `.tsx` lines that are part of a `//` or `/* */` comment.)
  </behavior>
  <action>
    1. Add `.meta-sep` to `frontend/app/globals.css` (near the existing `.text-caption` rules — `.meta-sep` is a meta-row primitive). Use approach (b):
       ```css
       .meta-sep {
         white-space: nowrap;
       }
       ```
       (The NBSPs live in the JSX text content, not in CSS.)
    2. For each site in the `<interfaces>` table, replace the bare `·` separator with `<span className="meta-sep">{" · "}</span>` (or wrap the existing aria-hidden span as `<span aria-hidden className="meta-sep">{" · "}</span>` where the existing span carried `aria-hidden`).
    3. For the `VoteSummary.tsx` join-on-array case (line ~168 in the pre-36-06 code; after 36-06 the line numbers shift): convert from `.join(" · ")` to a JSX render — interleave the array items with `<span className="meta-sep">{" · "}</span>`. Concretely: `array.reduce<ReactNode[]>((acc, item, idx) => idx === 0 ? [item] : [...acc, <span key={idx} className="meta-sep">{" · "}</span>, item], [])`. Or use a small helper function. The shape of the rendered string is unchanged from the user's perspective; only the whitespace is NBSP.
    4. Update `fr.json` strings: change `"validé · à cuisiner"` to `"validé · à cuisiner"` and `"Capture · {count, ...}"` to `"Capture · {count, ...}"`. Verify JSON parses by running `node -e "console.log(JSON.parse(require('fs').readFileSync('frontend/lib/i18n/fr.json','utf8')))"` or letting `next-intl`'s build do its thing.
    5. Add a comment to the code where the meta-sep pattern is introduced (any one of the modified files): "POLISH-01 — French typography: NBSP both sides of middle-dot via .meta-sep utility. Sweep applied in Plan 36-07."
    6. Validation: run `grep -rn '·' frontend/{app,components} | grep -v 'meta-sep' | grep -v '^[^:]*:[[:space:]]*//'` and confirm ZERO matches (filter out comment lines). Document the command output in the SUMMARY.

    DO NOT:
    - Modify the `VersionFooter.tsx` `·` separators (those are admin chrome, not French user copy; the version footer reads "v{version} · {sha} · {env}" which is technical metadata. If the POLISH-01 grep gate flags them, they can be sweep-applied OR flagged as an exception in the SUMMARY).
    - Modify comments (e.g. `RecipeCard.tsx:6` literal comment text).
    - Touch other Unicode separators (e.g. `—`, `–`); only `·` (U+00B7).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; grep -c "meta-sep" frontend/app/globals.css</automated>
  </verify>
  <done>
    - `grep -c "meta-sep" frontend/app/globals.css` ≥ 1.
    - `grep -rn '·' frontend/{app,components} | grep -v 'meta-sep' | grep -vE '^[^:]+:[0-9]+:[[:space:]]*(//|\*)'` returns ZERO matches (the regex filters out single-line `//` and block `*` comment continuations).
    - i18n keys `home.shortlist.valide_meta` and any other middle-dot key use ` ` literals.
    - `npm run lint` + `npx tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Relocate PushPermissionBanner below the shortlist ledger on Accueil</name>
  <files>frontend/components/HomeDecide.tsx</files>
  <behavior>
    - `<PushPermissionBanner />` no longer renders ABOVE the `<header>` block (the H1 + marginalia subhead).
    - Two acceptable new positions (per CONTEXT §SOBER-15 nuance + POLISH-02 spec — implementer picks):
      - **(a) Below the shortlist ledger** — mount the banner AFTER the `<VoteSummary />` in the return JSX, so first paint is H1 → ledger → push banner (a tertiary callout, not the lead).
      - **(b) Behind a Réglages CTA** — instead of inline, the banner becomes a small chip linking to `/settings` where the same opt-in is exposed. Larger refactor; out of this plan's scope unless the implementer judges (a) too noisy.
    - The OTHER PushPermissionBanner mount site (inside the "no shortlist" empty-state branch, line ~408 in pre-36-06 code) can stay or be removed — it's a secondary mount path. Pick consistency: either both above the H1 (current) or both below the content. Recommend both below.
    - The PushPermissionBanner component itself is unchanged.
    - When `Notification.permission !== 'default'`, the banner returns `null` already — no UX impact.
    - When `Notification.permission === 'default'`, the user sees the H1 + ledger first; the banner is a secondary affordance below.
  </behavior>
  <action>
    1. In `HomeDecide.tsx` (after Plan 36-06 lands), find the `<PushPermissionBanner />` mounts. After 36-06 ships, the canonical Accueil render branch is the single `<VoteSummary />` path. Move the banner mount from ABOVE the `<header>` block to BELOW the `<VoteSummary />` (or wherever the shortlist content ends — the banner is the LAST visible component before the BottomNav).
    2. If a second mount exists in the empty-state branch, move it consistently (below the EmptyState card, above the BottomNav).
    3. The cooking banner (`<CookingBanner />`) placement is UNCHANGED — that's a different banner with different priority. CookingBanner stays where it is.
    4. No new state, no new useEffect, no new prop. Pure JSX reordering.

    DO NOT:
    - Modify `PushPermissionBanner.tsx` itself.
    - Change `CookingBanner` position.
    - Add a settings-link chip variant (option b above) unless option (a) reads badly in the live render — and if so, document the choice.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - In `HomeDecide.tsx`, `PushPermissionBanner` is no longer rendered immediately after the opening `<div className="flex flex-col flex-1">` (its old top-of-screen slot).
    - The H1 `<header>` block renders BEFORE the banner in document order.
    - `npm run lint` + `npx tsc --noEmit` clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: NBSP middle-dot sweep applied via `.meta-sep` utility (POLISH-01)</name>
  <action>Human verification — see how-to-verify steps below.</action>
  <files>n/a (manual verification)</files>
  <what-built>NBSP middle-dot sweep applied via `.meta-sep` utility (POLISH-01); PushPermissionBanner moved below the H1 + shortlist ledger on Accueil (POLISH-02).</what-built>
  <how-to-verify>
    1. Frontend + backend dev servers running; seed loaded.
    2. **POLISH-01 (NBSP sweep):**
       - Open `/recipes/{ragu-id}` — inspect the ingredient section: `Ingrédients · 6 personnes` reads with NBSP both sides of `·` (visually: no awkward narrow gap between word and dot).
       - Open `/recipes` — Grille view: each card's meta row reads "Italienne · avant-hier" with NBSP both sides. Confirm via inspecting the DOM that the dot lives inside a `<span class="meta-sep">` carrying ` · `.
       - Open `/` (Accueil) post-vote — the ledger meta rows ("Italienne · 35 min") all use the meta-sep pattern.
       - From a terminal: `grep -rn '·' frontend/{app,components} | grep -v 'meta-sep' | grep -vE '^[^:]+:[0-9]+:[[:space:]]*(//|\*)'` returns zero matches.
    3. **POLISH-02 (push banner relocation):**
       - Clear notification permission (Safari: site settings → notifications → reset; or Chrome devtools: Application → Permissions → reset).
       - Reload `/` as Luca. Confirm the FIRST visible content is the page-header date row + H1 « On mange quoi ce soir ? » + Caveat marginalia subhead. The PushPermissionBanner is BELOW the shortlist content (or absent if permission was already granted/denied).
       - The H1 is the lead, not the notification opt-in.
       - Approve permission (or deny). Confirm the banner disappears.
    4. **Regression checks:**
       - Recette détail still renders cleanly; ingredient list left margin-rule + step numerals still there.
       - Bibliothèque view-switch persistence still works.
       - Post-vote ledger CTA copy unchanged from 36-06 ("Cuisiner ce soir" + title marginalia).
  </how-to-verify>
  <resume-signal>Type "approved" if the NBSP sweep is clean (grep returns zero) and the push banner sits below the H1; describe issues otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure UI typography + JSX reordering. No new data crosses boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07-01 | Information disclosure | i18n JSON parsing | accept | ` ` escapes are standard JSON; next-intl handles them transparently. No code execution. |
| T-36-07-02 | DoS (UX) | PushPermissionBanner relocation | mitigate | Banner self-suppresses when permission !== default. Worst case: a fresh-install user sees the banner two scroll-rows down — still reachable. |
</threat_model>

<verification>
- Lint + typecheck clean.
- POLISH-01 grep gate returns zero matches in user-facing copy.
- Visual checkpoint confirms NBSP rendering + push banner relocation.
- No regression in other meta-row sites.
</verification>

<success_criteria>
- POLISH-01 acceptance met: `grep -rn '·' frontend/{app,components} | grep -v meta-sep` (with comment filter) returns zero in user-facing copy.
- POLISH-02 acceptance met: fresh-seed Accueil with `Notification.permission === 'default'` shows H1 + marginalia first; push banner below the ledger.
- All five middle-dot sites + two i18n strings updated; meta-sep utility added to globals.css.
</success_criteria>

<output>
Create `.planning/phases/36-sober-kitchen-finish-polish/36-07-SUMMARY.md` with the receipt: meta-sep utility (CSS rule), per-file sweep table (which sites were updated), i18n keys changed, push banner old vs new mount point in HomeDecide.tsx, grep-gate output (zero matches), checkpoint outcome.
</output>
