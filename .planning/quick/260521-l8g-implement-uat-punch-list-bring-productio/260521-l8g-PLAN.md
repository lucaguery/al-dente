---
phase: quick-260521-l8g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/cli/seed.py
  - frontend/components/HomeDecide.tsx
  - frontend/components/ShortlistCard.tsx
  - frontend/components/ShortlistDeck.tsx
  - frontend/components/MemberDot.tsx
  - frontend/components/LibraryViewSwitch.tsx
  - frontend/app/recipes/page.tsx
  - frontend/app/recipes/[id]/page.tsx
  - frontend/components/BottomNav.tsx
  - frontend/components/RecipeThread/Composer.tsx
  - frontend/components/ui/dialog.tsx
  - frontend/messages/fr.json
autonomous: true
requirements:
  - B-01
  - B-02
  - B-03
  - D-01
  - D-02
  - D-03
  - D-04
  - D-06
  - P-01
  - P-02
  - P-03
  - P-04
tags: [ui, frontend, la-grille, adr-0004, uat-punch-list]
must_haves:
  truths:
    - "Bibliothèque view switcher exposes only Grille + Liste — Patine option is gone, and `/recipes` never renders Héritage/Habitudes/À l'essai sections."
    - "Recipe detail Ingrédients section has no terracotta left border-rule; only the card hairline remains."
    - "Accueil ledger (HomeDecide ledger branch) renders member avatars as ink `--foreground` (first member) + `--muted-foreground` (second member); cooking-log history and Settings still render slot-distinct colors."
    - "Accueil pre-vote deck identity dot (who voted so far) renders `--foreground` ink, not vivid rose."
    - "Notification banner on Accueil ledger view sits clear of the BottomNav + elevated central CTA (no overlap) — the ledger branch wraps content in a `<main>` with the same `pb-[calc(5rem+env(safe-area-inset-bottom))]` clearance as the deck branch, bumped to clear the 12px CTA nub."
    - "Bibliothèque Liste view rows scroll fully clear of the elevated central « + » nub — bottom padding accounts for nav 60px + 12px elevation."
    - "Central « + » in BottomNav renders with `boxShadow !== 'none'` — the elevation grammar is translate + soft drop-shadow."
    - "Capture sheet + URL-paste dialog close buttons expose French aria-label « Fermer » — no English « Close » survives in the served snapshot."
    - "Accueil pre-vote deck shows a clear binary affordance: yes = filled heart-terracotta, no = `lucide-heart-off` (or X). No two-identical-hearts ambiguity."
    - "Recipe detail step-marginalia connector is a left vertical hairline (`border-l border-faint-ink/30 pl-3` semantic), not a dashed-horizontal continuation line."
    - "Seed: `Poulet au citron` has `cuisine=french`; the 5 photoless recipes (Ragu bolognese, Coq au vin, Butter chicken, Shawarma, Tacos au boeuf) have `photo_paths=[]` so Bibliothèque mounts produce zero `/photo-url` 404s in test env."
  artifacts:
    - path: backend/app/cli/seed.py
      provides: "Poulet au citron cuisine=french; 5 photoless recipes with photo_paths=[]"
      contains: "cuisine=Cuisine.french"
    - path: frontend/components/LibraryViewSwitch.tsx
      provides: "Grille + Liste only view switcher"
      contains: "type LibraryView = \"grid\" | \"list\""
    - path: frontend/app/recipes/page.tsx
      provides: "Bibliothèque without Patine sectioning"
      contains: "view === \"list\""
    - path: frontend/app/recipes/[id]/page.tsx
      provides: "Ingrédients section without terracotta border-left"
    - path: frontend/components/HomeDecide.tsx
      provides: "Accueil ledger main padding parity + ink+muted ledger avatars"
    - path: frontend/components/ShortlistCard.tsx
      provides: "Pre-vote deck identity dot rendered as ink"
    - path: frontend/components/BottomNav.tsx
      provides: "Central « + » with translate + box-shadow"
      contains: "shadow"
    - path: frontend/components/ui/dialog.tsx
      provides: "French aria-label Fermer on Radix Dialog close"
      contains: "Fermer"
  key_links:
    - from: frontend/components/HomeDecide.tsx
      to: "ledger row avatar slot color"
      via: "inline style override to var(--foreground) / var(--muted-foreground)"
      pattern: "var\\(--foreground\\).*var\\(--muted-foreground\\)"
    - from: frontend/components/BottomNav.tsx
      to: "central « + » span"
      via: "Tailwind shadow utility on the bg-foreground inner span"
      pattern: "shadow-\\["
    - from: frontend/app/recipes/page.tsx
      to: "view state"
      via: "removal of patina view branch + 3rd radio"
      pattern: "view === \"patina\""
---

<objective>
Implement the 12 actionable UAT punch-list findings from
`.planning/quick/260521-uat-living-system-cross-check/PUNCH-LIST.md` to bring
production in line with the La Grille · Soft warmth sketches (ADR-0004).
Findings are grouped into 5 atomic tasks (one per commit) by file proximity.

Purpose:
- Close the residual 15% delta between ADR-0004 spec and the shipped UI.
- Match the locked design canon at `.claude/skills/sketch-findings-al-dente/`.
- Eliminate console noise that obscures real failures during walkthroughs.
- Honor MVP no-backcompat rule: clean rewrites, no compat shims.

Output:
- 5 atomic git commits (one per task), each independently revertable.
- Zero remaining Sober Kitchen residue (Patine IA, terracotta border-rule, vivid 5-slot avatars on Accueil).
- Zero console 404s on Bibliothèque mount in test env.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/CLAUDE.md
@backend/CLAUDE.md
@.planning/quick/260521-uat-living-system-cross-check/PUNCH-LIST.md
@.claude/skills/sketch-findings-al-dente/SKILL.md
@.claude/skills/sketch-findings-al-dente/references/screens.md
@.claude/skills/sketch-findings-al-dente/references/components.md
@.claude/skills/sketch-findings-al-dente/references/tokens.md
@docs/adr/0004-modern-sober-refresh.md

<interfaces>
<!-- Anchors pre-extracted to remove executor scavenger-hunting. -->

From `frontend/components/LibraryViewSwitch.tsx` (current state):
- `export type LibraryView = "grid" | "list" | "patina";`  (drop "patina")
- `VIEWS` array has 3 entries `{ key: "grid" | "list" | "patina", Icon: LayoutGrid | List | Layers }` (drop the Layers/patina entry)
- Imports `Layers` from `lucide-react` — drop the import after removing.
- Component is a pure controlled segmented input; parent (`frontend/app/recipes/page.tsx`) owns state + localStorage.

From `frontend/app/recipes/page.tsx`:
- `const [view, setView] = useState<LibraryView>("grid");`
- localStorage key: `"aldente.library.view"` — must reject stale `"patina"` reads and coerce to `"grid"`.
- Patine sub-components (lines ~62–140) render `grouped.heritage` / `grouped.habitudes` / `grouped.essai` sections via `tPatina("essai" | "heritage" | "habitudes")`. Remove the entire patina view branch + sub-components + the `grouped` derivation if it's only used by Patine.
- Translation keys in `frontend/messages/fr.json` namespace `home.library.view.patina.*` and `home.library.patina.*` can be deleted (i18n unused-key noise).

From `frontend/components/HomeDecide.tsx` (~25.9K — large file):
- Two top-level branches: deck composition (pre-vote) and ledger composition (post-vote).
- Deck branch's `<main>` uses `pb-[calc(5rem+env(safe-area-inset-bottom))]`. Ledger branch does NOT.
- Ledger row avatars currently bind to `--color-member-{slot}` CSS variables (rose/emerald/etc).
- "Notification banner" = `<PushPermissionBanner>` rendered inside the ledger branch.

From `frontend/components/MemberDot.tsx`:
- Shared primitive used in many surfaces. Do NOT change its default behavior.
- Either accept a new `variant` prop (`"slot" | "accueil-collapse"`) to switch bg/color tokens, or override via inline style at the call site on the Accueil-only renderings.

From `frontend/components/ShortlistCard.tsx` + `ShortlistDeck.tsx`:
- Pre-vote deck card shows a "who voted so far" identity dot (e.g. small rose dot + « Luca »).
- This dot is the surface called out by D-04.

From `frontend/components/BottomNav.tsx`:
- Line ~97: central « + » inner span has classes `flex items-center justify-center rounded-full bg-foreground text-... -translate-y-3`.
- No shadow currently. Add Tailwind arbitrary-value drop-shadow utility.

From `frontend/components/ui/dialog.tsx` (shadcn/Radix Dialog):
- The DialogClose primitive renders `<X />` with `sr-only` text « Close ». Replace with « Fermer ».
- Both the « + » Composer sheet and the URL-paste dialog instantiate this primitive.

From `frontend/app/recipes/[id]/page.tsx`:
- The Ingrédients block is at lines ~837–880, gated on `recipe.ingredients && recipe.ingredients.length > 0`.
- The terracotta border-left is applied to a `<section>` ancestor (PUNCH-LIST agent walked 6 levels of the DOM to find it). Hunt with grep for `oklch(0.54` or any `border-l-` / `borderLeft` near the ingredients block.
- "Marginal comment" (P-04) is between Step 01 and Step 02; the dashed-horizontal connector is the affordance to replace.

From `backend/app/cli/seed.py`:
- Line 205: `"title": "Poulet au citron",` — change adjacent `cuisine=` from `italian` to `french`.
- Lines 614–653: the 5-recipe bug-2-fix block that pre-populates `photo_paths` for `Ragu bolognese`, `Coq au vin`, `Butter chicken`, `Shawarma`, `Tacos au boeuf`. Replace `recipe_photo_paths = [f"{household.id}/{recipe_id}/{photo_uuid}.jpg"]` with `recipe_photo_paths = []` so no signed-URL fetch is attempted in test env. Per `frontend/CLAUDE.md` the bytes never upload in the hermetic test config — option (a) from the punch list.
- Line 1069 has `photo_paths=[photo_path]` for a different code path (prod-synthetic seed with real uploads via `run_prod_synthetic_seed`); LEAVE that path alone. Only touch the bug-2-fix block.
- Use `Cuisine.french` (Python enum import from `app.models.enums`) — do not hard-code the string.

Locked vocabularies (CLAUDE.md invariant — update both files if changing the enum, but D-06 is a data-value change, not an enum change):
- frontend: `frontend/lib/enums.ts`
- backend: `backend/app/models/enums.py`
- D-06 only changes a seed row's `cuisine` field; the enum already has `french`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — Backend seed fixes: D-06 cuisine + B-03 photoless drift</name>
  <files>backend/app/cli/seed.py</files>
  <action>
    Two atomic edits in `backend/app/cli/seed.py`, one commit.

    (a) D-06 cuisine fix: locate the `Poulet au citron` recipe definition (around line 205, `"title": "Poulet au citron"`) and change its `cuisine` field from `Cuisine.italian` to `Cuisine.french`. Verify the import already brings `Cuisine` in from `app.models.enums`; if not, add it. Do not touch any other cuisine assignment.

    (b) B-03 photoless drift: in the bug-2-fix block at lines ~614–653, change the assignment so the 5 named recipes (`Ragu bolognese`, `Coq au vin`, `Butter chicken`, `Shawarma`, `Tacos au boeuf`) end up with `photo_paths=[]`. Concretely: replace the branch that builds `recipe_photo_paths = [f"{household.id}/{recipe_id}/{photo_uuid}.jpg"]` with `recipe_photo_paths = []`. Drop any now-dead code (the `photo_uuid` generation in that branch). Keep the conditional structure if other recipes use it for non-photo seed concerns; if it's purely the photo-paths assignment, simplify to one unconditional `photo_paths=[]` for these 5 recipes.

    Do NOT touch the prod-synthetic seed path at line 1069 (`photo_paths=[photo_path]`) — that path uploads real bytes via `run_prod_synthetic_seed` and is gh#44 territory. Per frontend/CLAUDE.md, the test-env path withholds `SUPABASE_*` deliberately.

    Drop the inline comments that reference "bug 2 fix (260512-df0)" if they describe the now-removed behavior; replace with a one-line comment pointing to `260521-l8g` and PUNCH-LIST.md §B-03.

    Commit message: `fix(seed): drop photoless photo_paths + Poulet au citron cuisine (PUNCH-LIST B-03, D-06)`.
  </action>
  <verify>
    <automated>
      cd /Users/gulu3001/dev/al-dente/backend &amp;&amp; \
      grep -n "Poulet au citron" app/cli/seed.py | head -3 &amp;&amp; \
      python -c "import ast,sys; src=open('app/cli/seed.py').read(); assert 'cuisine=Cuisine.italian' not in src or src.count('cuisine=Cuisine.italian')==src.count('cuisine=Cuisine.italian'), 'check manually'; print('OK')" &amp;&amp; \
      uv run seed --reset 2>&amp;1 | tail -5 &amp;&amp; \
      uv run python -c "from app.db import SessionLocal; from app.models.recipe import Recipe; from app.models.enums import Cuisine; db=SessionLocal(); r=db.query(Recipe).filter(Recipe.title=='Poulet au citron').first(); assert r is not None, 'recipe missing'; assert r.cuisine == Cuisine.french, f'expected french, got {r.cuisine}'; assert r.photo_paths == [] or r.photo_paths is None, f'should have no photos: {r.photo_paths}'; print('OK', r.cuisine, r.photo_paths)" &amp;&amp; \
      uv run python -c "from app.db import SessionLocal; from app.models.recipe import Recipe; db=SessionLocal(); titles=['Ragu bolognese','Coq au vin','Butter chicken','Shawarma','Tacos au boeuf']; rs=db.query(Recipe).filter(Recipe.title.in_(titles)).all(); assert len(rs)==5, f'expected 5, got {len(rs)}'; bad=[r.title for r in rs if r.photo_paths]; assert not bad, f'still have photo_paths: {bad}'; print('OK all 5 photoless')"
    </automated>
  </verify>
  <done>
    `Poulet au citron` rows in the seeded DB have `cuisine == Cuisine.french`.
    The 5 named recipes have `photo_paths=[]`. `uv run seed --reset` exits 0.
    Only one file in the commit (`backend/app/cli/seed.py`).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — Bibliothèque cleanup: drop Patine view + Héritage/Habitudes/À l'essai IA (D-01)</name>
  <files>frontend/components/LibraryViewSwitch.tsx, frontend/app/recipes/page.tsx, frontend/messages/fr.json</files>
  <action>
    Single atomic commit removing the Patine view IA from Bibliothèque. ADR-0004
    wave 5 was supposed to do this but only dropped the `.ledger-card` CSS class
    — the radio + section concept survived.

    (1) `frontend/components/LibraryViewSwitch.tsx`:
      - Change the type to `export type LibraryView = "grid" | "list";`
      - Drop the `Layers` import from `lucide-react`.
      - Drop the `{ key: "patina", Icon: Layers }` entry from `VIEWS`.
      - Component otherwise unchanged.

    (2) `frontend/app/recipes/page.tsx`:
      - Remove the Patine sub-components (likely the `PatinaSection` / `PatinaGroup` helpers around lines ~62–140 + the `grouped.heritage` / `grouped.habitudes` / `grouped.essai` derivation if it's only used by Patine).
      - Remove the `view === "patina"` branch from the render.
      - Coerce stale localStorage reads: when reading `aldente.library.view`, if the stored value isn't exactly `"grid"` or `"list"`, default to `"grid"` (don't blindly cast — existing test fixtures may have `"patina"` stored).
      - Drop the `tPatina = useTranslations("home.library.patina")` hook + any sister hooks if they're no longer referenced.
      - Drop the `SOBER-11 (Plan 36-02)` comment block that documents the Patine three-section render contract — it's now obsolete.

    (3) `frontend/messages/fr.json`:
      - Delete the `home.library.view.patina` subtree (keys `aria`, etc.).
      - Delete the `home.library.patina` subtree (keys `heritage`, `habitudes`, `essai`).
      - Don't widen the deletion beyond these two subtrees — sibling keys must survive.

    Per MVP no-backcompat rule: no graceful fallback for the dropped value, just coerce-on-read once. No "deprecated" hint.

    Commit message: `feat(library): drop Patine view (ADR-0004 wave 5 cleanup; PUNCH-LIST D-01)`.
  </action>
  <verify>
    <automated>
      cd /Users/gulu3001/dev/al-dente/frontend &amp;&amp; \
      ! grep -rn "patina\\|Patine\\|Héritage\\|Habitudes\\|À l'essai" app/ components/ lib/ messages/ --include="*.ts" --include="*.tsx" --include="*.json" 2>/dev/null | grep -v "^Binary" | grep -v "// " | grep -v "PUNCH-LIST" &amp;&amp; \
      ! grep -n "Layers" components/LibraryViewSwitch.tsx &amp;&amp; \
      grep -c "type LibraryView = \"grid\" | \"list\"" components/LibraryViewSwitch.tsx &amp;&amp; \
      npm run lint 2>&amp;1 | tail -20 &amp;&amp; \
      npx tsc --noEmit 2>&amp;1 | tail -10
    </automated>
    <human-check>
      After dev server reload, `/recipes` shows the segmented control with only Grille + Liste icons.
      Selecting Liste renders the flat-row pattern. No "Héritage" / "Habitudes" / "À l'essai" headings appear in any DOM snapshot.
    </human-check>
  </verify>
  <done>
    Zero hits for `patina|Patine|Héritage|Habitudes|À l'essai` in `frontend/` source (excluding PUNCH-LIST.md references in comments).
    `LibraryView` type is `"grid" | "list"`. `npm run lint` + `npx tsc --noEmit` pass.
    `fr.json` no longer carries `home.library.patina` or `home.library.view.patina` subtrees.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 — Recipe detail polish: drop ingrédient border-rule + replace dashed marginalia connector (D-02 + P-04)</name>
  <files>frontend/app/recipes/[id]/page.tsx</files>
  <action>
    Two visual fixes on the recipe detail page, one commit.

    (1) D-02 — Drop the Sober Kitchen cookbook-ribbon residue from the Ingrédients
    section. The PUNCH-LIST agent walked 6 DOM levels up from the Ingrédients
    heading and found a `<section>` with computed
    `border-left: 3px solid oklch(0.540279 0.118683 35.4269 / 0.3)`.

    Hunt in `frontend/app/recipes/[id]/page.tsx` near the ingredients block
    (around lines 837–880, gated on `recipe.ingredients && recipe.ingredients.length > 0`):
      - grep for `border-l`, `borderLeft`, `oklch(0.54`, and any Tailwind arbitrary `border-l-[...]` utility.
      - The offending declaration may live as a Tailwind arbitrary value on the section, or as an inline `style={{ borderLeft: ... }}`. It is NOT in `globals.css` (the agent confirmed CSS-level cleanup is complete).
      - Remove the declaration entirely (do not soften it). The numbered Mono indices carry enough structural pressure per ADR-0004.
      - If the padding-left was only there to clear the rule (typical 12px), drop it too; keep card-level horizontal padding from the parent Card primitive.

    (2) P-04 — Replace the dashed-horizontal connector between recipe-step rows
    and their marginal-comment children with a left vertical hairline indent.
    The marginal comment is rendered when a recipe step has an associated free-form
    note (seed data inserts « Excellent ce soir. » between Step 01 and Step 02).
    The current implementation uses a dashed-horizontal continuation line.

    Replacement: render the marginal comment as a left-indented Mono note with
    `border-l border-faint-ink/30 pl-3` (or equivalent inline style using the
    tokens — `border-left: 1px solid color-mix(in oklab, var(--foreground) 30%, transparent); padding-left: 12px;`).
    Drop the dashed-horizontal-rule element entirely. Per
    `.claude/skills/sketch-findings-al-dente/references/components.md` §Data lists.

    If the marginal comment lives in a sub-component (e.g. `MarginNote.tsx` or
    `StepRow.tsx`) rather than inline in the page, edit the sub-component — but
    add its path to `files_modified` in the commit message body so the diff is
    self-describing.

    Commit message: `fix(recipe-detail): drop terracotta margin-rule + replace dashed marginalia connector (PUNCH-LIST D-02, P-04)`.
  </action>
  <verify>
    <automated>
      cd /Users/gulu3001/dev/al-dente/frontend &amp;&amp; \
      ! grep -rn "oklch(0.54\\|border-l-\\[3px\\|border-l-3" app/recipes/ components/ --include="*.tsx" 2>/dev/null &amp;&amp; \
      ! grep -rn "border-dashed\\|dashed.*marginalia\\|marginalia.*dashed" app/recipes/[id]/page.tsx components/ --include="*.tsx" 2>/dev/null | grep -v "PUNCH-LIST" &amp;&amp; \
      npm run lint 2>&amp;1 | tail -20 &amp;&amp; \
      npx tsc --noEmit 2>&amp;1 | tail -10
    </automated>
    <human-check>
      On `/recipes/{any-with-ingredients}`:
      - Ingrédients section has no terracotta vertical bar to the left of the heading or rows.
      - Card hairline border survives.
      - Between Step 01 « Faire revenir. » and Step 02 « Mijoter 1h. » (Ragu bolognese), the « Excellent ce soir. » marginal note renders with a faint left vertical rule + indent, not a dashed-horizontal line.
      Playwright MCP `browser_evaluate` on the Ingrédients section ancestors should return `borderLeft: 'none'` or `borderLeft: '0px ...'` for all ancestors up to the article element.
    </human-check>
  </verify>
  <done>
    Grep for `oklch(0.54` returns 0 hits under `frontend/app/recipes/` and `frontend/components/`.
    Grep for `border-dashed` near step-marginalia returns 0 hits.
    Lint + tsc pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 — Accueil member-color collapse: ledger avatars + deck identity dot to ink+muted (D-03 + D-04)</name>
  <files>frontend/components/HomeDecide.tsx, frontend/components/ShortlistCard.tsx, frontend/components/ShortlistDeck.tsx, frontend/components/MemberDot.tsx</files>
  <action>
    Surface ADR-0004 §Member colors collapse on the two Accueil-only surfaces.
    The 5-slot vivid model survives in tokens for productize-later — this task
    is an override at the call site, NOT a token deletion.

    (1) D-03 — Accueil ledger avatars. In `frontend/components/HomeDecide.tsx`,
    locate the ledger composition branch (post-vote view, the one that renders
    rows with member-avatar pairs on the left side). For each ledger row's
    avatar pair, override the slot-based background/foreground to the ink+muted
    pair:
      - First-listed member (Luca, by household join order) → `background: var(--foreground); color: var(--background);`
      - Second-listed member (Partner) → `background: var(--muted-foreground); color: var(--background);`
      - Keep the L/P glyphs (or whatever character the member's name resolves to).
      - Order is determined by `household.members` query order (id ASC = creation order).
    Implementation note: prefer extending `MemberDot.tsx` with a `variant` prop
    (`"slot"` default; `"accueil-collapse"` new) that switches the bg/color
    tokens, and pass `variant="accueil-collapse"` from the ledger row JSX. This
    keeps Settings + cooking-logs untouched (they continue using the default
    `variant="slot"`).
    If `MemberDot` isn't the rendering primitive for ledger rows (HomeDecide
    may inline its own avatar markup), do the equivalent inline-style override
    at that call site.

    (2) D-04 — Pre-vote deck identity dot. In `frontend/components/ShortlistCard.tsx`
    (or `ShortlistDeck.tsx` if the dot lives there), locate the "who voted so
    far" indicator (small dot + member name, e.g. « • Luca a voté »). Override
    its background from the slot variable to `var(--foreground)`. Same rule:
    first-listed-member → ink. Since only Luca-or-Partner ever appears
    pre-vote in a 2-member household, second-position-fallback isn't strictly
    needed, but apply the same `variant="accueil-collapse"` mapping for
    forward consistency.

    Hard constraints — do NOT change behavior on these surfaces:
      - Settings / Profil page member identity tiles (must stay slot-distinct).
      - Cooking log history rows (must stay slot-distinct).
      - The Cuisiner banner attribution (must stay slot-distinct).
      - The post-cook session log card.
    The override is ONLY for the two surfaces called out in PUNCH-LIST D-03 + D-04.

    Add a comment at each override site referencing ADR-0004 §Member colors and PUNCH-LIST D-03/D-04 so the next walker knows it's intentional.

    Commit message: `feat(accueil): collapse member avatars to ink+muted on ledger + deck identity (ADR-0004; PUNCH-LIST D-03, D-04)`.
  </action>
  <verify>
    <automated>
      cd /Users/gulu3001/dev/al-dente/frontend &amp;&amp; \
      grep -c "accueil-collapse\\|--foreground\\b.*Accueil\\|PUNCH-LIST D-03\\|PUNCH-LIST D-04" components/HomeDecide.tsx components/ShortlistCard.tsx components/ShortlistDeck.tsx components/MemberDot.tsx 2>/dev/null &amp;&amp; \
      npm run lint 2>&amp;1 | tail -20 &amp;&amp; \
      npx tsc --noEmit 2>&amp;1 | tail -10
    </automated>
    <human-check>
      Authenticated as Luca on `/`, post-vote ledger view: each row's avatar pair shows
      Luca as a filled ink-black circle + Partner as a filled muted-gray circle.
      Glyphs L and P survive.
      `browser_evaluate getComputedStyle` on the Luca dot returns `backgroundColor: "rgb(20, 17, 13)"` (or equivalent `--foreground` token resolution) — NOT `rgb(192, 54, 74)` (the rose).

      Switch to Partner auth (`document.cookie = "aldente_auth=test-token-partner; path=/; SameSite=Lax"`), reload `/` to see the pre-vote deck.
      The « Luca a voté » identity dot on the deck card renders ink, not rose.

      Settings page (`/onboarding/welcome` → flow or `/settings`): member tiles still render slot-distinct (rose + emerald). Verify regression-free.
      Cooking log history rows (any recipe detail's `cooking_log` section): still slot-distinct.
    </human-check>
  </verify>
  <done>
    Accueil ledger avatars and deck identity dot render in ink+muted; Settings + cooking-log surfaces unchanged.
    Lint + tsc pass. Override is comment-referenced as ADR-0004 + PUNCH-LIST D-03/D-04 at each call site.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5 — Safe-area + BottomNav polish: ledger pb parity + library row clearance + central « + » shadow + heart-off no-vote + Radix Fermer (B-01 + B-02 + P-02 + P-03 + P-01)</name>
  <files>frontend/components/HomeDecide.tsx, frontend/app/recipes/page.tsx, frontend/components/BottomNav.tsx, frontend/components/ShortlistCard.tsx, frontend/components/ShortlistDeck.tsx, frontend/components/ui/dialog.tsx, frontend/components/RecipeThread/Composer.tsx</files>
  <action>
    Five small polish fixes that share file proximity (bottom-nav / safe-area /
    deck-card / dialog), one atomic commit.

    (1) B-01 — Ledger branch safe-area padding. In `frontend/components/HomeDecide.tsx`,
    locate the post-vote ledger branch's `<main>` (or top-level wrapper). The
    deck branch already uses `pb-[calc(5rem+env(safe-area-inset-bottom))]`.
    Apply parity to the ledger branch and add 12px for the elevated central « + »
    nub:
      `pb-[calc(5rem+0.75rem+env(safe-area-inset-bottom))]`
    (5rem = 80px nav-reserved area in the existing scheme; +0.75rem = 12px CTA
    elevation). If the existing scheme uses a different reserved height,
    preserve that math — the goal is "nav 60px + 12px elevation + safe-area"
    clearance everywhere.
    Apply the SAME padding to the deck branch's `<main>` too (it currently only
    accounts for nav 60px, not the 12px nub — PUNCH-LIST B-02 implies the
    elevation occludes a region of content on those views as well; verify
    quickly with a grep before changing).

    (2) B-02 — Library row clearance. In `frontend/app/recipes/page.tsx`,
    locate the `<main>` (or scroll-container) wrapping the grid + list views.
    Apply the same `pb-[calc(5rem+0.75rem+env(safe-area-inset-bottom))]` so the
    bottom-most row scrolls clear of the « + » nub.
    NB: if there is a shared `<RootLayout>` `<main>` that already sets this, do
    NOT duplicate per-page; pick the right scope. Greg for existing
    `safe-area-inset-bottom` usage in `frontend/app/layout.tsx` and
    `frontend/app/recipes/layout.tsx` (if it exists) first.

    (3) P-02 — Central « + » box-shadow. In `frontend/components/BottomNav.tsx`
    (around line 97), the inner `<span class="...bg-foreground...-translate-y-3">`
    has `boxShadow: none`. Add the same shadow grammar as the deck card:
    `shadow-[0px_8px_24px_-8px_rgba(20,17,13,0.18)]`
    (or per ADR-0004 §Bottom nav, the specific elevation values — choose the
    one that matches the deck card so the system reads as coherent).
    Verify the resulting `boxShadow` computed style is NOT `none`.

    (4) P-03 — Heart-off "no" affordance. In `frontend/components/ShortlistCard.tsx`
    (or wherever the deck-card action buttons render), the left "no" button is
    currently an outlined `<Heart />` icon. Replace with `<HeartOff />` from
    `lucide-react`. Keep:
      - The button position (left of card).
      - The aria-label « Pas envie ce soir ».
      - The button geometry / colors (muted-foreground stroke; no fill).
    The right "yes" button keeps `<Heart fill="var(--primary)" stroke="..." />`
    unchanged.

    (5) P-01 — French close-dialog aria-label. In `frontend/components/ui/dialog.tsx`
    (the shadcn DialogClose primitive), the close button currently renders an
    `<X />` icon with `sr-only` text « Close » (or no aria-label, relying on
    Radix default English « Close »). Set the close button's
    `aria-label="Fermer"` and replace any sr-only « Close » text with « Fermer ».
    Verify both consumer sites still render correctly:
      - `frontend/components/RecipeThread/Composer.tsx` (« + » sheet)
      - The URL-paste dialog (wherever it instantiates Dialog — likely
        `frontend/components/RecipeThread/Composer.tsx` or a child).
    If the « Fermer » string is repeated in multiple sites, source it from
    `frontend/messages/fr.json` (key like `common.close = "Fermer"`) and read
    via `next-intl` per invariant #6. If only one consumer, an inline
    `aria-label="Fermer"` is acceptable for this MVP-mode quick.

    Commit message: `fix(ui): safe-area parity + central CTA shadow + heart-off no-vote + Fermer dialog label (PUNCH-LIST B-01, B-02, P-01, P-02, P-03)`.
  </action>
  <verify>
    <automated>
      cd /Users/gulu3001/dev/al-dente/frontend &amp;&amp; \
      grep -c "pb-\\[calc(5rem+0.75rem+env" components/HomeDecide.tsx app/recipes/page.tsx 2>/dev/null &amp;&amp; \
      grep -c "shadow-\\[0px_8px_24px" components/BottomNav.tsx &amp;&amp; \
      grep -c "HeartOff" components/ShortlistCard.tsx components/ShortlistDeck.tsx 2>/dev/null &amp;&amp; \
      grep -c "Fermer" components/ui/dialog.tsx messages/fr.json 2>/dev/null &amp;&amp; \
      ! grep -rn "\"Close\"\\|aria-label=\"Close\"" components/ui/dialog.tsx &amp;&amp; \
      npm run lint 2>&amp;1 | tail -20 &amp;&amp; \
      npx tsc --noEmit 2>&amp;1 | tail -10
    </automated>
    <human-check>
      iPhone-viewport (390x844) Playwright MCP `browser_evaluate`:
      - On `/` post-vote ledger: `PushPermissionBanner.getBoundingClientRect().bottom` is `<= 784 - 12` (clears nav + nub).
      - On `/recipes` Liste view: bottom-most row's `getBoundingClientRect().bottom + main.scrollTop` is `<= scrollHeight - 72px`.
      - `getComputedStyle(centralPlusInnerSpan).boxShadow !== 'none'`.
      - Deck-card "no" button renders the slashed-heart glyph (HeartOff), visually distinct from the filled "yes" heart.
      - Capture sheet («+» → open) close button has accessible name « Fermer » per `aria-label`, no « Close » survives in snapshot.
      - URL-paste sub-dialog close button: same.
    </human-check>
  </verify>
  <done>
    Five polish items shipped in one commit.
    Banner overlap: 0px on ledger and recipes.
    Central CTA computed shadow is non-empty.
    No-vote button uses `HeartOff` (or equivalent non-heart glyph).
    Both dialogs expose `aria-label="Fermer"`.
    Lint + tsc pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed CLI → DB | Test-env seed mutates Supabase Postgres rows. No external auth involved. |
| client → API photo-url | Authenticated request; 404 path is already self-healing in `useSignedPhotoUrl`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260521-l8g-01 | Tampering | seed.py mutations | accept | Test-env only; production uses `run_prod_synthetic_seed`, untouched by this task. |
| T-260521-l8g-02 | Information disclosure | DialogClose aria-label change | accept | « Fermer » exposed to screen readers is intentional (French-only invariant #6). |
| T-260521-l8g-03 | Denial of service | Safe-area padding regression | mitigate | Human-verify step on iPhone viewport (390x844) confirms no over-scroll regression on Capture / Cooking / Settings pages. |
| T-260521-l8g-SC | Tampering | npm/pip installs | accept | No package adds in this plan. `lucide-react` already vendored. |

No `[ASSUMED]` / `[SUS]` packages introduced. No `[SLOP]` packages. Package legitimacy audit not required (no installs).
</threat_model>

<verification>
After all 5 tasks complete, before final commit-push:

1. `cd backend && uv run seed --reset && uv run pytest -x 2>&1 | tail -20` — backend still green.
2. `cd frontend && npm run lint && npx tsc --noEmit && npm run build` — frontend builds.
3. **Manual Playwright MCP walkthrough on iPhone viewport (390x844):**
   - Auth as Luca; `/` ledger; verify ink+muted avatars + banner clears nav.
   - Auth as Partner; `/` deck; verify ink identity dot + HeartOff no-button.
   - `/recipes`; switch to Liste; verify no Patine option and rows clear « + » nub.
   - Open any recipe with ingredients; verify no terracotta border + marginal note has left-rule indent.
   - Open `/recipes/new` → tap «+»; verify dialog close button is « Fermer ».
   - `browser_console_messages({all: true, level: 'error'})` after mounting `/recipes` — 0 photo-url 404s.
4. `git log --oneline -5` shows 5 commits, one per task, each independently revertable.
</verification>

<success_criteria>
- 5 atomic commits land on `main` (or feature branch if user prefers — default is direct-to-main per project convention; deploys auto-trigger).
- All 12 punch-list findings (excluding D-05, already shipped at 687be0d) close:
  - **B-01** Accueil ledger pb clears nav + nub.
  - **B-02** Library Liste rows clear nub.
  - **B-03** Zero photo-url 404s on Bibliothèque mount.
  - **D-01** Patine view + IA gone.
  - **D-02** No terracotta Ingrédients border-rule.
  - **D-03** Accueil ledger avatars ink+muted.
  - **D-04** Pre-vote deck identity dot ink.
  - **D-06** Poulet au citron cuisine=french.
  - **P-01** Dialog close `aria-label="Fermer"`.
  - **P-02** Central « + » has box-shadow.
  - **P-03** No-vote button uses HeartOff (not duplicate heart).
  - **P-04** Step-marginalia uses left-rule indent (not dashed-horizontal).
- No regression on Settings / Cooking-log slot-distinct avatars.
- Backend coverage floor (v0.8 Phase 39 `fail_under = 85`) unaffected — seed changes don't touch covered code paths.
- `frontend/CLAUDE.md` is honored: no Turbopack flag added; ESLint flat config remains sole style authority.
- `CLAUDE.md` MVP no-backcompat rule honored: no patina-coercion stub left behind beyond the single localStorage-read coercion (which is mandatory for existing test fixtures).
</success_criteria>

<output>
Create `.planning/quick/260521-l8g-implement-uat-punch-list-bring-productio/260521-l8g-SUMMARY.md` when done, with one section per task and a single bullet-list of the 5 commit hashes.
</output>
