---
phase: 40-pure-frontend-restyles
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/loading.tsx
  - frontend/next.config.ts
  - frontend/lib/i18n/fr.json
autonomous: true
requirements:
  - SPLA-01
must_haves:
  truths:
    - "frontend/app/loading.tsx exists as a new file and is recognized by Next.js App Router as the root loading state"
    - "loading.tsx renders the BrandIcon table-à-manger logomark at 128px"
    - "loading.tsx renders the 'Al Dente.' wordmark with the accent dot in text-primary"
    - "loading.tsx renders the 'On mange quoi ce soir ?' tagline in text-muted-foreground"
    - "loading.tsx renders a 3-dot Geist Mono loader (animated bounce)"
    - "loading.tsx renders the 'v0.9.X · 2026' version footer (or current package.json version) in text-faint"
    - "loading.tsx uses bg-background (#FAFAF7) as its surface color"
    - "SPLA-02 is NOT shipped — no apple-touch-startup-image entries are added to app/layout.tsx in this plan"
  artifacts:
    - path: frontend/app/loading.tsx
      provides: "Root-level Next.js loading state with La Grille splash composition"
      min_lines: 30
      contains: "BrandIcon"
  key_links:
    - from: frontend/app/loading.tsx
      to: frontend/components/BrandIcon.tsx
      via: "Direct import of BrandIcon component"
      pattern: "from [\"']@/components/BrandIcon"
    - from: frontend/app/loading.tsx
      to: frontend/next.config.ts
      via: "process.env.NEXT_PUBLIC_APP_VERSION exposure from package.json"
      pattern: "NEXT_PUBLIC_APP_VERSION"
---

<objective>
Add `frontend/app/loading.tsx` as the App Router root loading state with the La Grille splash composition per SPLA-01 (sketch lines 1989-2013). SPLA-02 (iOS apple-touch-startup-image full size matrix) is explicitly deferred per D-09 and must NOT be shipped in this plan.

Purpose: Today, Next.js navigation transitions show no loading state at all — the screen blanks briefly. Adding `loading.tsx` at the App Router root gives every cross-segment transition a branded La Grille splash. Acts as the visual baseline for an iOS PWA boot splash (which arrives in a future phase per D-09). The 3-dot Geist Mono loader is the only animated element.

Output: New `loading.tsx` file, version exposure in `next.config.ts`, fr.json keys for the tagline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-pure-frontend-restyles/40-CONTEXT.md
@.planning/phases/40-pure-frontend-restyles/40-RESEARCH.md
@docs/adr/0004-modern-sober-refresh.md
@.claude/skills/sketch-findings-al-dente/SKILL.md

@frontend/app/layout.tsx
@frontend/components/BrandIcon.tsx
@frontend/next.config.ts
@frontend/package.json
@frontend/app/globals.css
@frontend/lib/i18n/fr.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose APP_VERSION from package.json via next.config.ts</name>
  <files>frontend/next.config.ts</files>
  <read_first>frontend/next.config.ts, frontend/package.json</read_first>
  <action>
  Read `frontend/next.config.ts` and check if it already exposes `NEXT_PUBLIC_APP_VERSION` via the `env` block. If yes, skip this task with a `// already exposed` comment in the SUMMARY.

  If not:

  1. At the top of `next.config.ts`, read `package.json` synchronously: `import packageJson from "./package.json" assert { type: "json" };` (or `with` instead of `assert` depending on Node version target — match the project's existing import style by reading other places in the codebase that import JSON, if any; otherwise use the synchronous Node `fs.readFileSync` + `JSON.parse` pattern).
  2. Add an `env` field to the exported config object:
     ```
     env: {
       NEXT_PUBLIC_APP_VERSION: packageJson.version,
     }
     ```
  3. If an `env` block already exists, append to it; do not overwrite.

  Constraint: do not break the existing `next-pwa` config or any other plugin chain that wraps the export.
  </action>
  <verify>cd frontend && npm run build 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `frontend/next.config.ts` contains `NEXT_PUBLIC_APP_VERSION`
    - `cd frontend && npm run build` succeeds
    - At runtime, `process.env.NEXT_PUBLIC_APP_VERSION` is defined (verified by Task 2)
  </acceptance_criteria>
  <done>Build clean, version env exposed</done>
</task>

<task type="auto">
  <name>Task 2: Add fr.json key for splash tagline</name>
  <files>frontend/lib/i18n/fr.json</files>
  <read_first>frontend/lib/i18n/fr.json</read_first>
  <action>
  Add (or update if existing) under `splash` namespace:
  - `splash.tagline` → `"On mange quoi ce soir ?"`

  Validate JSON. Additive only.

  Note: do NOT split the tagline (this is the un-emphasized form — splash uses no italic per sketch).
  </action>
  <verify>cd frontend && node -e "JSON.parse(require('fs').readFileSync('lib/i18n/fr.json'))" && echo OK</verify>
  <acceptance_criteria>
    - `frontend/lib/i18n/fr.json` parses
    - Contains key `splash.tagline` with value `"On mange quoi ce soir ?"`
  </acceptance_criteria>
  <done>Key present, JSON valid</done>
</task>

<task type="auto">
  <name>Task 3: Create frontend/app/loading.tsx with La Grille splash composition</name>
  <files>frontend/app/loading.tsx</files>
  <read_first>frontend/components/BrandIcon.tsx, frontend/app/layout.tsx, frontend/app/globals.css, frontend/lib/i18n/fr.json, .claude/skills/sketch-findings-al-dente/sources/002-refresh-direction-explorations/index.html</read_first>
  <action>
  Create new file `frontend/app/loading.tsx` as a Next.js App Router root loading state.

  Visual structure (centered on `bg-background` `#FAFAF7`):

  1. `BrandIcon` table-à-manger logomark at 128px (`className="size-32"` or width/height props per BrandIcon's API — read the component first to confirm prop shape).
  2. `Al Dente.` wordmark in Geist 500, `text-3xl`, with the trailing `.` in `text-primary`.
  3. Tagline in Geist 400, `text-base text-muted-foreground`: read from `splash.tagline` i18n key. Hardcode acceptable if this is a Server Component without next-intl access — but the project ships next-intl globally per invariant #6; use `useTranslations` if the component renders client-side, or `getTranslations()` from `next-intl/server` if server.
  4. 3-dot Geist Mono loader: three `<span>` elements with `font-mono` class, content `.`, with a `animate-bounce` Tailwind class staggered with inline `style={{ animationDelay: "0ms" }}`, `100ms`, `200ms` respectively. Color `text-muted-foreground`.
  5. Version footer at bottom: `<p className="text-caption font-mono text-foreground/40">v{process.env.NEXT_PUBLIC_APP_VERSION} · {new Date().getFullYear()}</p>`.

  Layout: `min-h-screen flex flex-col items-center justify-center gap-4 bg-background`.

  Constraints (verifiable):
  - File default-exports a function (Next.js App Router `loading.tsx` convention): `export default function Loading() { ... }`.
  - File does NOT contain any animation outside the 3-dot loader. No fade-ins, no skeleton shimmer.
  - File does NOT contain Fraunces, Cormorant, Caveat, paper-grain, Sober Kitchen, `bg-surface-rose-100`, or any non-La-Grille token.
  - File MUST contain the literal string `Al Dente.` (or `Al Dente` + `.` split for accent-dot styling).
  - File MUST contain `BrandIcon` import and usage.
  - File MUST contain `NEXT_PUBLIC_APP_VERSION`.
  - File does NOT import or reference `apple-touch-startup-image` (SPLA-02 is deferred).
  </action>
  <verify>cd frontend && npm run lint && npm run build && test -f app/loading.tsx && grep -E "BrandIcon|Al Dente|NEXT_PUBLIC_APP_VERSION" app/loading.tsx</verify>
  <acceptance_criteria>
    - `frontend/app/loading.tsx` exists with ≥30 lines
    - File contains a default export function (Next.js loading.tsx convention)
    - File contains `BrandIcon` import and usage
    - File contains the literal `Al Dente.` (or split form)
    - File contains `NEXT_PUBLIC_APP_VERSION`
    - File does NOT contain `Fraunces`, `Cormorant`, `Caveat`, `paper-grain`, `Sober Kitchen`, `bg-surface-rose-100`
    - File does NOT contain `apple-touch-startup-image`
    - `npm run lint` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>loading.tsx renders the full La Grille composition; build clean</done>
</task>

<task type="auto">
  <name>Task 4: Verify SPLA-02 deferred — no layout.tsx changes</name>
  <files>frontend/app/layout.tsx</files>
  <read_first>frontend/app/layout.tsx</read_first>
  <action>
  Read `frontend/app/layout.tsx` and confirm that the count of `apple-touch-startup-image` references is unchanged from the pre-Phase-40 baseline. The plan must NOT add any new `apple-touch-startup-image` `<link>` tags or metadata entries. This task is a guard — if any other task accidentally introduced one, this task surfaces it.

  No modifications to `layout.tsx` are required. If a baseline value of `apple-touch-startup-image` already exists in `layout.tsx` from a previous phase, preserve it exactly.
  </action>
  <verify>grep -c "apple-touch-startup-image" frontend/app/layout.tsx</verify>
  <acceptance_criteria>
    - `grep -c "apple-touch-startup-image" frontend/app/layout.tsx` returns the same value as before Phase 40 started (0 if never added, or the prior baseline count if a stub existed)
    - `frontend/app/layout.tsx` was not modified by this plan
  </acceptance_criteria>
  <done>SPLA-02 confirmed deferred; layout.tsx unchanged</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run lint` exits 0
- [ ] `cd frontend && npm run build` exits 0
- [ ] `test -f frontend/app/loading.tsx` returns 0
- [ ] `grep -E "BrandIcon|Al Dente|On mange quoi" frontend/app/loading.tsx | wc -l` returns ≥3
- [ ] `grep -E "Fraunces|Cormorant|Caveat|paper-grain|Sober Kitchen|bg-surface-rose-100|apple-touch-startup-image" frontend/app/loading.tsx | wc -l` returns 0
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- `loading.tsx` exists at App Router root with the full La Grille splash composition
- Version footer reads from build-time package.json
- SPLA-02 remains deferred — no apple-touch-startup-image entries added
- No Sober Kitchen tokens introduced
</success_criteria>

<output>
After completion, create `.planning/phases/40-pure-frontend-restyles/40-04-SUMMARY.md`
</output>
