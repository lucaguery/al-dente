---
phase: 37-test-infrastructure-service-branch-coverage
plan: 03
type: execute
wave: 3
depends_on: [37-02]
files_modified:
  - backend/tests/test_shortlist_unit.py
  - backend/tests/test_algorithm_unit.py
  - backend/tests/test_llm_thread_kinds.py
autonomous: true
requirements: [COV-03, COV-04, SERV-02, SERV-03, SERV-04]
tags: [pytest, unit-tests, algorithm, shortlist, llm, coverage-100, gemini-monkeypatch]

must_haves:
  truths:
    - "`score_recipe` is tested for every scoring branch: hard filters (status, cuisine, max_prep_time, exclude_protein, required_moods), soft scoring (season match, recency 0-14d clamp, mood overlap, cuisine penalty, protein penalty, jitter range)"
    - "`select_top5_with_diversity`, `select_top5_soft_diversity`, and `select_top_n_with_cold_start` are tested across all three cold-start branches (corpus <10 / 10-29 / 30+)"
    - "`generate_daily_shortlist` is tested for: empty candidate pool → returns None + no broadcast, partial pool (< 5 candidates) → shortlist has fewer than 5 picks, full pool (> 5 candidates) → shortlist has exactly 5 picks, idempotent re-run produces a deterministic set when jitter is monkeypatched"
    - "`generate_daily_shortlist` push-fan-out failure is swallowed (does not raise) and is logged at WARNING"
    - "The 5 user-emitted `TurnKind` values (text, voice, photo, url, answer) each have an end-to-end test through the thread-processing path with the Gemini SDK monkeypatched to a canned response (per D-37-04: `from google import genai` shape)"
    - "`pytest --cov=app/services/shortlist --cov=app/services/algorithm` reports 100.0% line coverage on both files"
  artifacts:
    - path: "backend/tests/test_algorithm_unit.py"
      provides: "Unit tests for `app.services.algorithm` — pure-function tests of `score_recipe`, `select_top5_with_diversity`, `select_top5_soft_diversity`, `select_top_n_with_cold_start` covering all weight/penalty/cold-start branches"
      contains: "from app.services.algorithm import"
    - path: "backend/tests/test_shortlist_unit.py"
      provides: "Tests for `app.services.shortlist.generate_daily_shortlist` covering empty/partial/full corpus, idempotent re-run, broadcast + push paths with monkeypatched `broadcast_to_household` and `send_push_to_household`"
      contains: "from app.services.shortlist import"
    - path: "backend/tests/test_llm_thread_kinds.py"
      provides: "TurnKind sweep — one test per user-emitted kind (text/voice/photo/url/answer) exercising the thread-processing path with Gemini monkeypatched"
      contains: "monkeypatch.setattr"
  key_links:
    - from: "backend/tests/test_algorithm_unit.py"
      to: "app.services.algorithm.score_recipe"
      via: "direct pure-function call with synthetic Recipe + ShortlistContext objects (random.uniform monkeypatched for determinism)"
      pattern: "score_recipe\\("
    - from: "backend/tests/test_shortlist_unit.py"
      to: "app.services.shortlist.generate_daily_shortlist"
      via: "async function call inside pytest.mark.asyncio; monkeypatches broadcast_to_household and send_push_to_household to no-op"
      pattern: "await generate_daily_shortlist\\("
    - from: "backend/tests/test_llm_thread_kinds.py"
      to: "app.services.llm._run_thread_llm"
      via: "monkeypatch _run_thread_llm AND _gemini() (genai.Client) to canned responses per D-37-04"
      pattern: "monkeypatch.setattr.*llm_module"
---

<objective>
Drive `app/services/algorithm.py` (17.6% → 100%, 60 missing lines) and `app/services/shortlist.py` (22.0% → 100%, 48 missing lines) to 100% line coverage via targeted unit tests, and add a per-`TurnKind` sweep for `app/services/llm.py` thread processing with the Gemini SDK monkeypatched (D-37-04).

Purpose: Close requirements COV-03 (algorithm → 100%), COV-04 (shortlist → 100%), SERV-02 (algorithm every weight/penalty branch), SERV-03 (shortlist empty/partial/full/idempotent corpus cases), SERV-04 (LLM thread processing × 5 user-emitted TurnKinds with Gemini monkeypatched).

Output:
- `backend/tests/test_algorithm_unit.py` — pure-function coverage of `algorithm.py`.
- `backend/tests/test_shortlist_unit.py` — async tests of `generate_daily_shortlist` with monkeypatched broadcast/push.
- `backend/tests/test_llm_thread_kinds.py` — 5 user-emitted TurnKind tests with Gemini monkeypatched.
- `pytest --cov=app/services/shortlist --cov=app/services/algorithm --cov-report=term-missing` reports 100.0% on both files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-test-infrastructure-service-branch-coverage/37-CONTEXT.md
@.planning/phases/37-test-infrastructure-service-branch-coverage/37-02-SUMMARY.md
@.planning/quick/260519-uxn-add-pytest-cov-to-backend-run-baseline-c/260519-uxn-SUMMARY.md
@backend/app/services/algorithm.py
@backend/app/services/shortlist.py
@backend/app/services/llm.py
@backend/tests/test_llm_thread.py
@backend/tests/conftest.py
@CLAUDE.md
@backend/CLAUDE.md
@.planning/CLAUDE.md

<interfaces>
<!-- Key contracts extracted from the codebase the executor must use as-is. -->

From `backend/app/services/algorithm.py`:
- `@dataclass class ShortlistFilters` — fields: `cuisine: str | None`, `max_prep_time: int | None`, `exclude_protein: str | None`, `required_moods: list[str]`.
- `@dataclass class ShortlistContext` — fields: `current_season: str`, `recent_cuisines: set[str]`, `recent_proteins: set[str]`, `filters: ShortlistFilters | None`.
- `score_recipe(recipe: Recipe, context: ShortlistContext) -> float | None` — returns `None` on hard-filter rejection; float on accept.
  - Hard filter branches: `recipe.status not in ("structured", "verified")`, `filters.cuisine`, `filters.max_prep_time`, `filters.exclude_protein`, `filters.required_moods`.
  - Soft scoring branches: season match (+1.0), recency (1.5 × min(days/14, 1)), required_moods overlap (+0.8 × ratio), recent_cuisines penalty (-0.5), recent_proteins penalty (-0.5), jitter (`random.uniform(0, 0.2)`).
  - To make tests deterministic: `monkeypatch.setattr(algorithm.random, "uniform", lambda a, b: 0.0)`.
- `select_top5_with_diversity(ranked)` — two-pass: first pass picks recipes that add new (cuisine, protein); second pass tops up. Both passes break at `len(picks) >= 5`.
- `select_top5_soft_diversity(ranked)` — simple top-5 by rank order (the while-loop body that just appends; the docstring promises diversity-as-tie-break but the current implementation does not actually rerank).
- `select_top_n_with_cold_start(candidates, corpus_size)` — branches: `<10` → top-5 by score, `<30` → soft, `>=30` → full diversity.

From `backend/app/models/recipe.py`:
- `Recipe.days_since_cooked()` — instance method that uses `last_cooked_at`. For unit tests, set `last_cooked_at = None` or a specific datetime; inspect the actual method shape during execution to confirm return type (likely int, possibly float).
- Required attributes touched by `score_recipe`: `.status`, `.cuisine`, `.prep_time_minutes`, `.main_protein`, `.mood` (list), `.seasonality` (list).

For test synthesis: build `Recipe` rows via `Recipe(**kwargs)` and `db.add(...)` using the `db_session` fixture, OR use lightweight stand-ins (`SimpleNamespace`) for the algorithm.py pure-function tests since no DB is read. Pick the simpler path per the section — pure-function tests should use stand-ins where `Recipe.days_since_cooked` can be stubbed via a `SimpleNamespace` providing the same attribute; `generate_daily_shortlist` tests need real `Recipe` rows so SQLAlchemy `select()` works.

From `backend/app/services/shortlist.py`:
- `generate_daily_shortlist(household_id, db=None, filters=None, generation=1) -> DailyShortlist | None` — async; returns None on empty corpus (Pitfall 8); broadcasts `shortlist.created` and triggers push fan-out (push exceptions swallowed at WARNING level).
- Calls `broadcast_to_household` (async) and `send_push_to_household` (sync). BOTH must be monkeypatched in tests to no-op functions (no real WebSocket peers; no real VAPID push).
- Calls `_current_season()` — uses `datetime.now(timezone.utc).month`. Monkeypatch this OR test against the season the current month yields (deterministic but brittle); safer to monkeypatch.
- Helper `_recent_cuisines_and_proteins(household_id, db)` — runs a JOIN against CookingLog. For an empty-cooking-log household this returns `(set(), set())`.

From `backend/app/services/llm.py` (per D-37-04):
- Gemini SDK import: `from google import genai` and `from google.genai import types`.
- `_client: genai.Client | None = None` (module-level singleton at line ~168).
- `_gemini() -> genai.Client` (line ~171) — lazy constructor; reads `settings.gemini_api_key`; raises `RuntimeError` if missing.
- `process_thread_turn(...)` (async) — main entry point that dispatches on `turn.kind` and calls `_run_thread_llm`.
- `_run_thread_llm(thread, pinned)` — calls `_gemini()` then `client.models.generate_content(...)` with the schema-constrained response.
- Monkeypatch strategy for SERV-04: replace `app.services.llm._run_thread_llm` with a coroutine that returns a canned `GeminiExtractedRecipe`. This avoids touching the SDK at all — `process_thread_turn` will use the canned extract for all downstream branches (extraction hash, advisory emission, summary insertion).
- Alternative monkeypatch: replace `app.services.llm._gemini` with `lambda: <MagicMock>` and have the mock client's `models.generate_content` return a `MagicMock(parsed=<GeminiExtractedRecipe>)`. Either approach is acceptable per the planner's-discretion clause in CONTEXT.md.
- The existing `tests/test_llm_thread.py` already monkeypatches `_run_thread_llm` extensively (see `_canned()` helper at line ~99 and the integration test patterns at lines ~150+). REUSE the canned-extract fixture shape; do not re-invent.

TurnKinds to test (SERV-04 scope per CONTEXT specifics):
- User-emitted (primary scope): `text`, `voice`, `photo`, `url`, `answer` — one test each via `process_thread_turn` after inserting a user turn of that kind via `_make_user_turn` (mirror the helper from `tests/test_llm_thread.py:59-76`).
- System-emitted (secondary): `proposal_accepted`, `proposal_dismissed` — exercised via existing tests in `test_llm_thread.py`; only add new tests if needed to hit coverage gaps in `process_thread_turn`'s dispatch arms.

From `backend/tests/test_llm_thread.py` (REFERENCE PATTERNS, do not import — copy the shape):
- `_make_user_turn(db, recipe_id, position, kind, payload)` — direct DB insert helper at line 59.
- `_make_system_turn(db, recipe_id, position, kind, payload)` — line 79.
- `_canned()` returns `canned_thread_extract([], frozenset())` — a `GeminiExtractedRecipe` shape at line 99.
- `monkeypatch.setattr(storage_module, "download_recipe_photo", lambda path: b"\\xff\\xd8\\xff")` (line ~236) — required for photo-kind tests so the photo bytes pipeline doesn't hit Supabase.

Per scope_fence: `test_llm_thread_kinds.py` is a NEW file (not an extension of `test_llm_thread.py`). The CONTEXT's discretion clause permits choosing new file vs extending — new file is preferred so the per-kind sweep is auditable as a unit. Re-state local helpers in the new file (no cross-test-file imports).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover services/algorithm.py to 100% via pure-function unit tests</name>
  <files>backend/tests/test_algorithm_unit.py</files>
  <behavior>
    - `score_recipe` returns `None` when `recipe.status == "draft"` (hard filter).
    - `score_recipe` returns `None` when `filters.cuisine == "asian"` and `recipe.cuisine == "italian"`.
    - `score_recipe` returns `None` when `filters.max_prep_time == 30` and `recipe.prep_time_minutes == 60`.
    - `score_recipe` returns `None` when `filters.max_prep_time == 30` and `recipe.prep_time_minutes is None` (the `or 999` fallback rejects).
    - `score_recipe` returns `None` when `filters.exclude_protein == "fish"` matches recipe.
    - `score_recipe` returns `None` when `filters.required_moods == ["quick"]` and recipe mood is `["comfort"]` (no overlap).
    - `score_recipe` with seasonality match adds +1.0 (assert with jitter monkeypatched to 0).
    - `score_recipe` with recipe never cooked (last_cooked_at None → days ≥ 14) adds +1.5 recency (the 14d clamp branch).
    - `score_recipe` with recipe cooked 7 days ago adds ~+0.75 recency (mid-range).
    - `score_recipe` with mood overlap against `required_moods` adds proportional 0.8 × ratio.
    - `score_recipe` with cuisine in `recent_cuisines` subtracts 0.5.
    - `score_recipe` with protein in `recent_proteins` subtracts 0.5.
    - `select_top5_with_diversity` with 5 distinct (cuisine, protein) picks all 5.
    - `select_top5_with_diversity` with 3 distinct + 2 duplicates picks 3 in pass 1, tops up to 5 in pass 2.
    - `select_top5_with_diversity` with fewer than 5 ranked → returns all of them.
    - `select_top5_soft_diversity` with 7 ranked → returns first 5.
    - `select_top_n_with_cold_start` corpus_size=5 → simple top-5.
    - `select_top_n_with_cold_start` corpus_size=15 → soft diversity branch.
    - `select_top_n_with_cold_start` corpus_size=30 → full diversity branch.
  </behavior>
  <action>
Per scope_fence: create `backend/tests/test_algorithm_unit.py`. No other files modified. Do NOT modify `app/services/algorithm.py`.

First action (mandatory per memory feedback_executor_scope_creep.md): read `.planning/phases/37-test-infrastructure-service-branch-coverage/37-02-SUMMARY.md` to confirm Plan 37-02 closed cleanly and the autouse seed fixture from Plan 37-01 is still in place.

Approach: pure-function tests against `score_recipe`, `select_top5_with_diversity`, `select_top5_soft_diversity`, `select_top_n_with_cold_start`. Build synthetic `Recipe` rows via `db_session.merge(Recipe(...))` and flush (because `Recipe.days_since_cooked()` is an ORM-bound method that may rely on `__init__` defaults). If `days_since_cooked` works on detached instances, prefer construction without `db_session` for speed.

Determinism: monkeypatch the random jitter for every `score_recipe` test:

```
monkeypatch.setattr("app.services.algorithm.random.uniform", lambda a, b: 0.0)
```

(Use `monkeypatch.setattr` with the dotted-path string form; it works because `algorithm.random` is the module-level `import random`.)

Structure (planner's discretion per CONTEXT.md):
- Group `score_recipe` tests by branch family: `class TestScoreRecipeHardFilters` (6 None-returning cases), `class TestScoreRecipeSoftScoring` (6 numeric-assertion cases). Numeric assertions use `pytest.approx(...)` because of floating-point + the jitter monkeypatch.
- Group `select_*` tests by function: `class TestSelectTop5WithDiversity` (3 cases), `class TestSelectTop5SoftDiversity` (1-2 cases), `class TestColdStart` (3 corpus-size cases).
- Build a `_make_recipe(...)` helper at module top to reduce repetition; takes the fields scoring touches as kwargs with sensible defaults.

Coverage check: after writing tests, run `cd backend && uv run pytest tests/test_algorithm_unit.py --cov=app/services/algorithm --cov-report=term-missing -v`. Result MUST be 100.0% for `app/services/algorithm.py`. Inspect the `Missing` column for any remaining lines and add targeted tests.

If a test reveals a real bug (e.g. `select_top5_soft_diversity` doesn't actually rerank for diversity as its docstring claims — that branch is dead code), DO NOT patch source. Capture as a follow-up in SUMMARY.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_algorithm_unit.py --cov=app/services/algorithm --cov-report=term-missing -v 2>&1 | tail -25</automated>
  </verify>
  <done>All tests pass; `app/services/algorithm.py` reads 100% with empty `Missing` column; SERV-02 acceptance (every weight + penalty + cold-start branch) demonstrably covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover services/shortlist.py to 100% via async generate_daily_shortlist tests</name>
  <files>backend/tests/test_shortlist_unit.py</files>
  <behavior>
    - `generate_daily_shortlist(household, db=db_session)` with the seeded household (21 structured recipes) → returns a `DailyShortlist` with up to 5 picks, calls monkeypatched `broadcast_to_household` once with event `"shortlist.created"`.
    - `generate_daily_shortlist(empty_household)` → returns `None`, does NOT call broadcast, does NOT call push (Pitfall 8 — empty corpus skip).
    - `generate_daily_shortlist(partial_household)` with only 2 structured recipes → returns shortlist with 2 picks.
    - `generate_daily_shortlist` called twice with the same filters and jitter monkeypatched to 0 → both calls produce the same `recipe_ids` ordering (SERV-03 idempotency).
    - `generate_daily_shortlist` when `send_push_to_household` raises → the exception is swallowed at WARNING level; the DailyShortlist is still returned (push must never break cron).
    - `_current_season()` correctly maps months 3-5 → spring, 6-8 → summer, 9-11 → autumn, 12+1+2 → winter.
    - `_recent_cuisines_and_proteins(household, db, days=14)` returns the cuisine/protein sets of recipes cooked in the last 14 days.
  </behavior>
  <action>
Per scope_fence: create `backend/tests/test_shortlist_unit.py`. No other files modified. Do NOT modify `app/services/shortlist.py`.

Approach: use `@pytest.mark.asyncio` and the `db_session` fixture. Tests work with the seeded test DB (the autouse seed fixture from Plan 37-01 gives us 21 structured recipes + a household with cooking logs).

Required monkeypatches per test that calls `generate_daily_shortlist`:
- `app.services.shortlist.broadcast_to_household` → an `AsyncMock` so the async-call expectation is recordable.
- `app.services.shortlist.send_push_to_household` → a `MagicMock` (sync). For the "push failure swallowed" test, set `side_effect = RuntimeError("vapid offline")`.
- For idempotency: monkeypatch `app.services.algorithm.random.uniform` to `lambda a, b: 0.0` so the jitter is zero across both calls.
- For `_current_season()` unit test: monkeypatch `app.services.shortlist.datetime` so `datetime.now(timezone.utc)` returns a datetime with the chosen month. Parametrize over `(month, expected_season)` for all 12 months.

Test structure (planner's discretion):
- `class TestGenerateDailyShortlist` — async tests for the main entry point covering empty/partial/full/idempotent/push-failure-swallowed.
- `class TestCurrentSeason` — parametrized over `(month, expected_season)` × 12 months. Patches `app.services.shortlist.datetime` to return a fixed `datetime` for `.now(timezone.utc)`.
- `class TestRecentCuisinesAndProteins` — exercises the JOIN: insert a CookingLog with a recipe of known cuisine/protein within 14 days, assert it appears; insert one 30 days ago, assert it does NOT appear.

For the empty-household test: create a new `Household` + `Member` in the rolled-back `db_session`, flush, then call `generate_daily_shortlist(new_household.id, db=db_session)`. The rolled-back fixture isolates this from the seeded household.

For the partial-household test: create a new household with exactly 2 `structured` recipes, call `generate_daily_shortlist`, assert `len(result.recipe_ids) == 2`.

For the idempotency test: monkeypatch jitter to 0, call twice with same filters, assert `result1.recipe_ids == result2.recipe_ids`. If the second call fails uniqueness on `DailyShortlist`, use `generation=2` for the second call and assert the picks ordering matches.

Coverage check: after writing tests, run `cd backend && uv run pytest tests/test_shortlist_unit.py --cov=app/services/shortlist --cov-report=term-missing -v`. MUST hit 100.0% on `app/services/shortlist.py`. Inspect `Missing` column for any remaining lines.

If a real bug is exposed, capture follow-up in SUMMARY; do not patch source.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_shortlist_unit.py --cov=app/services/shortlist --cov-report=term-missing -v 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass; `app/services/shortlist.py` reads 100% with empty `Missing` column; SERV-03 acceptance (empty / partial / full / idempotent) demonstrably covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TurnKind sweep through process_thread_turn with Gemini monkeypatched</name>
  <files>backend/tests/test_llm_thread_kinds.py</files>
  <behavior>
    - One test per user-emitted `TurnKind`: `text`, `voice`, `photo`, `url`, `answer`.
    - Each test inserts a user turn of that kind via a local `_make_user_turn` helper, monkeypatches `app.services.llm._run_thread_llm` to a coroutine returning a canned `GeminiExtractedRecipe`, then calls `await process_thread_turn(...)` and asserts a system summary turn is created with the extracted title in its payload.
    - For `photo` kind: also monkeypatches `app.services.storage.download_recipe_photo` to return canned JPEG bytes (`b"\\xff\\xd8\\xff"`) so the photo-bytes pipeline doesn't hit Supabase.
    - For `url` kind: monkeypatches the URL fetcher at the boundary (`app.services.llm.httpx` or whichever module httpx is used through) — OR mocks at the `_run_thread_llm` level since the test scope is "TurnKind dispatch through process_thread_turn", not URL fetching per se.
    - The Gemini SDK (`from google import genai`) is NEVER invoked over the network in any test — verified by leaving `GEMINI_API_KEY=stub` and asserting `_gemini()` is not called (its monkeypatched replacement records or simply MagicMock-returns).
    - Each test uses the seeded household's Luca member (auth_token `"test-token-luca"`) for ownership.
  </behavior>
  <action>
Per scope_fence: create `backend/tests/test_llm_thread_kinds.py`. No other files modified. Do NOT modify `app/services/llm.py`.

This test file is a focused SERV-04 sweep — one test per user-emitted TurnKind — and it COMPLEMENTS the existing `tests/test_llm_thread.py` (which already exercises many `_run_thread_llm` branches but does not have a clean per-kind matrix that's easy to audit). Do not duplicate the prompt-builder or hash tests from `test_llm_thread.py`.

Local helpers (re-state, do not import from `test_turns.py` or `test_llm_thread.py` per scope_fence on cross-test imports):
- `SEED_TOKEN = "test-token-luca"` constant at module top.
- `_make_user_turn(db, recipe_id, position, kind, payload)` — mirror of the helper at `test_llm_thread.py:59`.
- `_seeded_member(db) -> Member` — `db.scalar(select(Member).where(Member.auth_token == SEED_TOKEN))` plus an `assert member is not None, ...` error message pointing at Plan 37-01.
- `_make_recipe(db, household_id, member_id) -> Recipe` — minimal `Recipe(status="draft", title="...", household_id=..., created_by_member_id=..., ...)` with all NOT NULL columns set; mirror the pattern from `test_turns.py:104` and `test_llm_thread.py:191`.
- `_canned_extract() -> GeminiExtractedRecipe` — constructs a deterministic `GeminiExtractedRecipe(title="Test Recipe", ingredients=[GeminiIngredient(name="test")], steps=["étape 1"], cuisine="italian", main_protein="poultry", mood=["quick"], seasonality=["spring"])`. Reuse `from app.services.llm_fixtures import canned_thread_extract` if its signature matches — per existing usage in `test_llm_thread.py:48`.

Monkeypatch shape per D-37-04 (LOCKED — `from google import genai` is the SDK):

```
monkeypatch.setattr(
    "app.services.llm._run_thread_llm",
    AsyncMock(return_value=_canned_extract()),
)
```

This is the highest-leverage monkeypatch: it short-circuits BOTH `_gemini()` AND the live network call. The existing `test_llm_thread.py` uses the same pattern (`with patch("app.services.llm._run_thread_llm", ...)`) so it's already a sanctioned approach.

For belt-and-suspenders: also monkeypatch `app.services.llm._gemini` to `lambda: MagicMock()` so any code path that bypasses `_run_thread_llm` still won't hit the live SDK. Asserting the live SDK is untouched is sufficient via the absence of network errors when `GEMINI_API_KEY=stub`.

Test structure:
- `@pytest.mark.parametrize("kind,payload", [("text", {"text": "risotto"}), ("voice", {"transcript": "version légère"}), ("photo", {"photo_paths": ["path/img.jpg"]}), ("url", {"url": "https://example.com/recipe"}), ("answer", {"field": "cuisine", "value": "italian"})])` over `async def test_process_thread_turn_kind(...)`.
- Each test: insert user turn via `_make_user_turn`, commit, monkeypatch `_run_thread_llm` + storage + (for url) any URL fetcher, await `process_thread_turn(...)`, assert a `kind="summary"` system turn exists with the canned title in `payload["body"]` or `payload["text"]` (verify the exact key during execution against the current `process_thread_turn` implementation).

Coverage check: run `cd backend && uv run pytest tests/test_llm_thread_kinds.py -v` — all 5 parametrized cases pass. Coverage of `app/services/llm.py` is NOT required to reach 100% in this plan (per scope: SERV-04 is "tested with each TurnKind", not "100% coverage of llm.py"). Record the resulting `app/services/llm.py` coverage delta in the SUMMARY for reference.

If a test reveals a real bug in dispatch, capture follow-up in SUMMARY; do not patch source.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_llm_thread_kinds.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 parametrized TurnKind tests pass with the Gemini SDK monkeypatched per D-37-04; no network calls to Google AI; SERV-04 acceptance demonstrably covered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Combined-file coverage assertion + full-suite green run + phase-final delta capture</name>
  <files>(no files modified — verification only)</files>
  <behavior>
    - `pytest --cov=app/services/shortlist --cov=app/services/algorithm` reports 100.0% for BOTH files in one combined run.
    - The full backend suite still passes (no regression from the new test files).
    - The phase-final coverage delta for ALL 4 rules files is recorded (voting + auth from 37-02 + algorithm + shortlist from 37-03), demonstrating COV-02..05 collectively.
    - Repo-wide coverage number is captured (no `fail_under` asserted — D-37-05; Phase 39's job).
  </behavior>
  <action>
Run three acceptance commands and record the numbers in the SUMMARY.

1. Combined coverage on the two Plan 37-03 target files (no `fail_under` — D-37-05):

   ```
   cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_algorithm_unit.py tests/test_shortlist_unit.py --cov=app/services/shortlist --cov=app/services/algorithm --cov-report=term-missing
   ```

   Acceptance: both files show 100.0% in the per-file table; `Missing` column is empty for both.

2. Phase-final 4-file rules sweep — all 4 target files in one run:

   ```
   cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_voting_unit.py tests/test_auth_unit.py tests/test_algorithm_unit.py tests/test_shortlist_unit.py --cov=app/services/voting --cov=app/services/algorithm --cov=app/services/shortlist --cov=app/auth --cov-report=term-missing
   ```

   Acceptance: all 4 files show 100.0%. Capture the per-file table in the SUMMARY.

3. Full-suite green run (no regression):

   ```
   cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest --cov=app --cov-report=term-missing
   ```

   Acceptance: exit code 0; repo-wide TOTAL coverage number recorded in the SUMMARY (compare to Plan 37-01 baseline number recorded in 37-01-SUMMARY.md and Plan 37-02's recorded delta).

If either command fails the acceptance check, fix the test files (not source) and re-run. Per D-37-05, do NOT add `fail_under` to `pyproject.toml` — that's Phase 39 wiring.

Per memory feedback_verify_before_claiming_done.md: the Plan 37-03 SUMMARY MUST include the literal pytest output's TOTAL line (numerator, denominator, percentage) — not a paraphrase. Reviewer compares to baseline numbers.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_voting_unit.py tests/test_auth_unit.py tests/test_algorithm_unit.py tests/test_shortlist_unit.py --cov=app/services/voting --cov=app/services/algorithm --cov=app/services/shortlist --cov=app/auth --cov-report=term-missing 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 rules files at 100.0% in one combined run; full-suite still exits 0; phase-final coverage table in SUMMARY shows voting/auth/algorithm/shortlist baseline→100% deltas and repo-wide line coverage delta.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → Gemini SDK | All LLM tests monkeypatch `_run_thread_llm` and/or `_gemini` so no `from google import genai` network call occurs in CI; D-37-04 shape locked. |
| test → DB | New tests use the autouse seed fixture's seeded household for read-paths; create-then-rollback for new households in shortlist tests. |
| test → Supabase Storage | Photo-kind tests monkeypatch `app.services.storage.download_recipe_photo` to return canned bytes — no real bucket access. |
| test → WebSocket | Shortlist tests monkeypatch `broadcast_to_household` to an `AsyncMock` — no real WS peers. |
| test → Web Push (VAPID) | Shortlist tests monkeypatch `send_push_to_household` — no real VAPID push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Tampering | Tests inadvertently mutating seeded recipes via real DB writes | mitigate | All DB writes go through the `db_session` per-test fixture (connection-scoped transaction rolled back at teardown). Per-test inserts of new households / recipes / cooking logs are discarded at teardown. |
| T-37-03-02 | Information disclosure | Live Gemini SDK call leaking transcripts to Google AI logs | mitigate | D-37-04 (LOCKED): every LLM test monkeypatches `app.services.llm._run_thread_llm` (and optionally `_gemini`) to canned responses. `GEMINI_API_KEY=stub` is used in the verify-step env so even a missed monkeypatch would surface as a 401 from Google, not a successful call. |
| T-37-03-03 | DoS | `generate_daily_shortlist` push fan-out hitting real VAPID endpoint | mitigate | All shortlist tests monkeypatch `app.services.shortlist.send_push_to_household` to a no-op `MagicMock`. The push-failure-swallowed test sets `side_effect = RuntimeError(...)` to exercise the WARNING-level swallow branch. |
| T-37-03-04 | Spoofing | Tests inserting cooking logs that affect the seeded household's score for unrelated tests | accept | The connection-scoped transaction rolls back per test, so cooking-log inserts inside one test do not affect the seeded baseline for another test. Idempotency tests use jitter monkeypatch (deterministic). |
| T-37-03-05 | Repudiation | Coverage report claims 100% but a hidden branch is missed | mitigate | `--cov-report=term-missing` exposes the `Missing` column. The verify step's tail-20 output includes this column; reviewer can inspect that `Missing` is empty for the 4 target files. |
| T-37-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — only new test files using already-installed `pytest`, `pytest-asyncio`, `pytest-cov`, and stdlib `unittest.mock`. Audit table N/A. |

</threat_model>

<verification>
Plan-level acceptance:
1. `cd backend && uv run pytest tests/test_algorithm_unit.py tests/test_shortlist_unit.py --cov=app/services/shortlist --cov=app/services/algorithm --cov-report=term-missing` → both files at 100.0%.
2. `cd backend && uv run pytest tests/test_voting_unit.py tests/test_auth_unit.py tests/test_algorithm_unit.py tests/test_shortlist_unit.py --cov=app/services/voting --cov=app/services/algorithm --cov=app/services/shortlist --cov=app/auth --cov-report=term-missing` → ALL 4 rules files at 100.0%.
3. `cd backend && uv run pytest` → exit code 0 (full suite green, no regression).
4. `cd backend && uv run pytest tests/test_llm_thread_kinds.py -v` → 5 parametrized cases pass with the Gemini SDK monkeypatched.

Phase-final acceptance (handed to phase-close verifier):
- COV-02 (voting → 100%), COV-03 (algorithm → 100%), COV-04 (shortlist → 100%), COV-05 (auth → 100%), COV-06 (96 unblock), COV-07 (svg_sanitizer_test relocated), SERV-01 (voting × 5 states × member_count ∈ {1, 2}), SERV-02 (algorithm every branch), SERV-03 (shortlist empty/partial/full/idempotent), SERV-04 (LLM × 5 user-emitted TurnKinds) all closed.
</verification>

<success_criteria>
- All 6 `must_haves.truths` are demonstrably true.
- COV-03 (algorithm 100%), COV-04 (shortlist 100%), SERV-02, SERV-03, SERV-04 all closed.
- Together with Plan 37-01 and 37-02, the phase closes all 10 in-scope requirements (COV-02..07 + SERV-01..04).
- No source file under `backend/app/` was modified across any of the three plans (scope_fence enforced).
- No `fail_under` threshold added to `backend/pyproject.toml` (D-37-05 — Phase 39's job).
- SUMMARY.md captures: (a) Plan 37-03 per-file coverage deltas, (b) phase-final 4-rules-file 100% table, (c) repo-wide coverage delta from baseline, (d) any follow-up TODOs surfaced by tests that revealed source bugs.
</success_criteria>

<output>
Create `.planning/phases/37-test-infrastructure-service-branch-coverage/37-03-SUMMARY.md` when done. Include:
- Per-file coverage delta table for algorithm + shortlist (and the phase-final 4-file table from Task 4).
- Repo-wide line coverage: baseline (35.9% per quick-260519-uxn) → post-37-01 → post-37-02 → post-37-03.
- Explicit list of REQ-IDs closed: COV-03, COV-04, SERV-02, SERV-03, SERV-04 (plus reference to 37-01 closing COV-06/07 and 37-02 closing COV-02/05/SERV-01).
- Any source-bug TODOs captured during testing (scope_fence forbade in-flight patches).
</output>
