---
phase: 37-test-infrastructure-service-branch-coverage
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - backend/tests/test_voting_unit.py
  - backend/tests/test_auth_unit.py
autonomous: true
requirements: [COV-02, COV-05, SERV-01]
tags: [pytest, unit-tests, voting, auth, coverage-100]

must_haves:
  truths:
    - "`compute_vote_state` returns the correct state for all 5 outcomes (valide / pressenti / conteste / rejete / sans_avis) across household sizes 1 AND 2"
    - "Every branch in `app/auth.py` is exercised: cookie-only auth, Bearer-only auth, cookie-wins-when-both-present, missing both → 401, malformed Bearer (no `Bearer ` prefix) → 401, valid token but unknown member → 401, empty cookie/header values → 401"
    - "`set_auth_cookie` writes the cookie with HttpOnly + Secure + SameSite=Strict + Max-Age=7776000 + Path=/"
    - "`clear_auth_cookie` writes a Set-Cookie that expires aldente_auth"
    - "`generate_auth_token` returns 43-char URL-safe base64 strings"
    - "`pytest --cov=app/services/voting --cov=app/auth` reports 100.0% line coverage for both files"
  artifacts:
    - path: "backend/tests/test_voting_unit.py"
      provides: "Unit tests for `app.services.voting.compute_vote_state` covering all 5 VoteState values × member_count ∈ {1, 2}"
      contains: "from app.services.voting import compute_vote_state, VoteState"
    - path: "backend/tests/test_auth_unit.py"
      provides: "Unit tests for `app.auth` covering `_extract_token`, `generate_auth_token`, `set_auth_cookie`, `clear_auth_cookie`, and `current_member` (cookie / Bearer / missing / invalid paths)"
      contains: "from app.auth import"
  key_links:
    - from: "backend/tests/test_voting_unit.py"
      to: "app.services.voting.compute_vote_state"
      via: "direct function call with synthetic Vote objects (no DB round-trip)"
      pattern: "compute_vote_state\\("
    - from: "backend/tests/test_auth_unit.py"
      to: "app.auth.current_member"
      via: "TestClient request with Cookie/Authorization headers OR direct function call with mocked db.scalar"
      pattern: "current_member|_extract_token"
---

<objective>
Drive `app/services/voting.py` and `app/auth.py` to 100% line coverage via focused unit tests. These are the two easier 100% targets (voting has 12 missing lines, auth has 4 — total 16 lines to close). Doing them first gives fast feedback before Plan 37-03 tackles the harder algorithm/shortlist/LLM files.

Purpose: Close requirements COV-02 (voting → 100%), COV-05 (auth → 100%), and SERV-01 (`compute_vote_state` exercised for all 5 states across household sizes 1 and 2).

Output:
- `backend/tests/test_voting_unit.py` — new file with parametrized tests covering all 5 VoteStates × member counts.
- `backend/tests/test_auth_unit.py` — new file with unit tests covering all branches in `app/auth.py`.
- `pytest --cov=app/services/voting --cov=app/auth --cov-report=term-missing` reports 100.0% on both files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-test-infrastructure-service-branch-coverage/37-CONTEXT.md
@.planning/phases/37-test-infrastructure-service-branch-coverage/37-01-SUMMARY.md
@.planning/quick/260519-uxn-add-pytest-cov-to-backend-run-baseline-c/260519-uxn-SUMMARY.md
@backend/app/services/voting.py
@backend/app/auth.py
@backend/tests/conftest.py
@CLAUDE.md
@backend/CLAUDE.md
@.planning/CLAUDE.md

<interfaces>
<!-- Key contracts extracted from the codebase the executor must use as-is. -->

From `backend/app/services/voting.py`:
- `class VoteState(str, enum.Enum)` with members `valide`, `pressenti`, `conteste`, `rejete`, `sans_avis` (values: "valide", "pressenti", "conteste", "rejete", "sans_avis").
- `compute_vote_state(votes: Iterable[Vote], member_count: int) -> VoteState` — pure function, no DB.
- Branch order is LOCKED (mirrors `frontend/lib/votes.ts`):
  1. `yes_count == member_count` → `valide`
  2. `no_count == member_count` → `rejete`
  3. `yes_count >= 1 and no_count >= 1` → `conteste`
  4. `yes_count == 1 and voted == 1` → `pressenti`
  5. otherwise → `sans_avis`
- Vote objects need only a `.vote` attribute (string `"yes"` or `"no"`); use a lightweight stand-in (a `SimpleNamespace` or a small dataclass) — NOT a real `Vote` ORM row (no DB round-trip needed for pure-function tests).

From `backend/app/models/vote.py`:
- `Vote.vote` is a string column; the values used are `"yes"` and `"no"`.

From `backend/app/auth.py`:
- `AUTH_COOKIE_NAME = "aldente_auth"`
- `AUTH_COOKIE_MAX_AGE = 7776000` (90 days)
- `generate_auth_token() -> str` — wraps `secrets.token_urlsafe(32)`; returns a 43-char URL-safe base64 string.
- `set_auth_cookie(response: Response, token: str) -> None` — sets the cookie with `max_age=AUTH_COOKIE_MAX_AGE, path="/", secure=True, httponly=True, samesite="strict"`.
- `clear_auth_cookie(response: Response) -> None` — emits a deletion Set-Cookie with the same secure/httponly/samesite attributes.
- `_extract_token(aldente_auth: str | None, authorization: str | None) -> str` — cookie wins; Bearer fallback; missing both → `HTTPException(401, "missing auth")`. Empty-string cookie or Bearer is treated as absent (the `.strip()` + truthy check). Lowercase `bearer ` is accepted (`.lower().startswith("bearer ")`).
- `current_member(aldente_auth, authorization, db)` — calls `_extract_token`; looks up `Member.auth_token == token`; raises `HTTPException(401, "invalid token")` if `None`.

From `backend/tests/conftest.py` (set up by Plan 37-01):
- `_seeded_database` autouse session fixture provides `member_luca` with `auth_token="test-token-luca"` and `member_partner` with `auth_token="test-token-partner"` in the test DB. Use these for the happy-path `current_member` tests.
- `db_session` per-test fixture provides a rolled-back `Session`.
- `client` per-test fixture provides a `TestClient` with `get_db` overridden.

From `backend/tests/test_turns.py` (reference, do not import — copy the pattern):
- `SEED_TOKEN = "test-token-luca"` and `AUTH_HEADERS = {"Authorization": f"Bearer {SEED_TOKEN}"}` — but per scope_fence Plan 37-02 must not import test helpers from other test files. Re-state the constant in the new file.

Mock-style: prefer `unittest.mock.MagicMock` or a tiny inline class for `Response` and `Session.scalar` if you want to test `set_auth_cookie` / `current_member` without a full TestClient round-trip. For `current_member` happy path AND DB lookup, use the existing `client` fixture (TestClient) — pick a route that calls `current_member` via `Depends`, e.g. `GET /auth/session/me` (per `backend/app/routers/auth_session.py`) or `GET /recipes` (per `backend/app/routers/recipes.py`). Verify the route exists before using.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover services/voting.py to 100%</name>
  <files>backend/tests/test_voting_unit.py</files>
  <behavior>
    - `compute_vote_state([], member_count=2)` → `VoteState.sans_avis` (neither voted)
    - `compute_vote_state([yes, yes], member_count=2)` → `VoteState.valide`
    - `compute_vote_state([no, no], member_count=2)` → `VoteState.rejete`
    - `compute_vote_state([yes, no], member_count=2)` → `VoteState.conteste`
    - `compute_vote_state([yes], member_count=2)` → `VoteState.pressenti` (one yes, partner unvoted)
    - `compute_vote_state([no], member_count=2)` → `VoteState.sans_avis` (single no with 2 members is NOT rejete; falls through to sans_avis per branch order — check the branch table)
    - `compute_vote_state([yes], member_count=1)` → `VoteState.valide` (yes_count == member_count)
    - `compute_vote_state([no], member_count=1)` → `VoteState.rejete` (no_count == member_count)
    - `compute_vote_state([], member_count=1)` → `VoteState.sans_avis`
    - SERV-01 acceptance: all 5 VoteState values appear at least once across the parametrized cases, and member_count ∈ {1, 2} is covered.
  </behavior>
  <action>
Per scope_fence: create `backend/tests/test_voting_unit.py`. No other files modified. Do NOT modify `app/services/voting.py` (source file — out of scope).

Approach: pure-function tests against `compute_vote_state`. No DB round-trip needed; voting.py imports `Vote` only for the type hint. Use a tiny stand-in for vote rows — either a `types.SimpleNamespace(vote="yes")` per row or a lightweight dataclass — that exposes the `.vote` attribute the function reads.

Structure (planner's discretion per CONTEXT.md — recommended):
- One `@pytest.mark.parametrize` over `(votes_list, member_count, expected_state)` tuples covering at minimum the 9 cases in the `<behavior>` block.
- Plus a small standalone test asserting the branch ORDER is preserved (e.g. with `member_count=2` and two yes votes, `valide` wins over `conteste` — proves the early-return ordering is intact). This guards CLAUDE.md invariant #2 by pinning the function's contract.
- Plus one test that confirms `VoteState` enum values are the locked string literals (`"valide"`, `"pressenti"`, `"conteste"`, `"rejete"`, `"sans_avis"`) — this is the frontend-mirror drift detector mentioned in the voting.py docstring.

Coverage check: after writing tests, run `cd backend && uv run pytest tests/test_voting_unit.py --cov=app/services/voting --cov-report=term-missing -v`. Result MUST be 100.0% line coverage for `app/services/voting.py`. If any line is still missing, look at the `--cov-report=term-missing` output's `Missing` column and add a test that exercises that line.

If a test reveals a real bug in `voting.py` (e.g. branch order doesn't match the docstring contract), DO NOT patch `voting.py` — capture as a follow-up in the SUMMARY and adjust the test's `expected_state` to match actual behavior. The scope_fence forbids source modification in Phase 37.

Per memory feedback_executor_scope_creep.md, the executor must read `.planning/phases/37-test-infrastructure-service-branch-coverage/37-01-SUMMARY.md` first to confirm the autouse seed fixture is in place before running any tests.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_voting_unit.py --cov=app/services/voting --cov-report=term-missing -v 2>&1 | tail -25</automated>
  </verify>
  <done>All tests pass; `app/services/voting.py` coverage row reads `100%` with `Missing` column empty; the 5 VoteState values × member_count ∈ {1, 2} matrix is demonstrably covered (SERV-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover app/auth.py to 100%</name>
  <files>backend/tests/test_auth_unit.py</files>
  <behavior>
    - `generate_auth_token()` returns a 43-char string (URL-safe base64 of 32 bytes).
    - `set_auth_cookie(response, token)` adds a Set-Cookie header with `aldente_auth=<token>`, `Max-Age=7776000`, `Path=/`, `Secure`, `HttpOnly`, `SameSite=Strict` (case-insensitive attribute names; cookie value must be unquoted token).
    - `clear_auth_cookie(response)` adds a Set-Cookie header that expires `aldente_auth` (typically `Max-Age=0` or `Expires=...` in the past).
    - `_extract_token(cookie="abc", authorization=None)` returns `"abc"`.
    - `_extract_token(cookie=None, authorization="Bearer xyz")` returns `"xyz"`.
    - `_extract_token(cookie=None, authorization="bearer xyz")` returns `"xyz"` (lowercase prefix accepted per `.lower().startswith("bearer ")`).
    - `_extract_token(cookie="cookie-wins", authorization="Bearer header-loses")` returns `"cookie-wins"`.
    - `_extract_token(cookie="  ", authorization=None)` raises `HTTPException(401, "missing auth")` (empty after `.strip()`).
    - `_extract_token(cookie=None, authorization="NotBearer xyz")` raises `HTTPException(401, "missing auth")` (no `bearer ` prefix).
    - `_extract_token(cookie=None, authorization="Bearer  ")` raises `HTTPException(401, "missing auth")` (empty after `.strip()`).
    - `_extract_token(cookie=None, authorization=None)` raises `HTTPException(401, "missing auth")`.
    - `current_member` happy path with cookie returns the seeded Luca member.
    - `current_member` happy path with Bearer returns the seeded Luca member.
    - `current_member` with an unknown token raises 401 `"invalid token"`.
    - `current_member` with no auth raises 401 `"missing auth"`.
  </behavior>
  <action>
Per scope_fence: create `backend/tests/test_auth_unit.py`. No other files modified. The CONTEXT specifies this is a NEW file (no `test_auth.py` exists per the directory listing). Do NOT modify `app/auth.py`.

Two layers of testing:

1. **Pure-function tests** for `generate_auth_token`, `set_auth_cookie`, `clear_auth_cookie`, `_extract_token` — call directly without a TestClient round-trip. For `set_auth_cookie` and `clear_auth_cookie`, pass a `fastapi.Response()` instance and inspect `response.headers.get("set-cookie")` or `response.headers.getlist("set-cookie")` for the attributes. Assert on substring presence (`"secure"`, `"httponly"`, `"samesite=strict"`, `"max-age=7776000"`, `"path=/"`) — case-insensitive via `.lower()` since header capitalization varies.

2. **Integration tests** for `current_member` — use the existing `client` fixture (TestClient with `get_db` override). Pick a route that calls `current_member` via `Depends` and returns the member identity. Verify a route like `GET /auth/session/me` or similar exists in `backend/app/routers/auth_session.py` first; if no such route exists, hit any household-scoped endpoint that requires auth (e.g. `GET /recipes/` returns 200 with auth, 401 without). Test cases:
   - Cookie-auth happy path: `client.get("/<route>", cookies={"aldente_auth": "test-token-luca"})` → 200.
   - Bearer-auth happy path: `client.get("/<route>", headers={"Authorization": "Bearer test-token-luca"})` → 200.
   - Cookie wins when both present: `client.get("/<route>", cookies={"aldente_auth": "test-token-luca"}, headers={"Authorization": "Bearer invalid"})` → 200 (would 401 if Bearer beat cookie).
   - Unknown token: `client.get("/<route>", headers={"Authorization": "Bearer not-a-real-token"})` → 401 with detail `"invalid token"`.
   - Missing auth: `client.get("/<route>")` → 401 with detail `"missing auth"`.

   Use `SEED_TOKEN = "test-token-luca"` and `SEED_TOKEN_PARTNER = "test-token-partner"` as module-level constants (don't import from other test files per scope_fence).

Coverage check: after writing tests, run `cd backend && uv run pytest tests/test_auth_unit.py --cov=app/auth --cov-report=term-missing -v`. Result MUST be 100.0% line coverage for `app/auth.py`. The 4 missing lines from baseline are likely the `_extract_token` fallback / 401 branches plus possibly the `current_member` lazy `Member` import — verify by reading the `Missing` column.

If a test reveals a real bug, capture follow-up in SUMMARY; do not patch source.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_auth_unit.py --cov=app/auth --cov-report=term-missing -v 2>&1 | tail -25</automated>
  </verify>
  <done>All tests pass; `app/auth.py` coverage row reads `100%` with `Missing` column empty; both pure-function and integration paths are covered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Combined-file coverage assertion + full-suite green run</name>
  <files>(no files modified — verification only)</files>
  <behavior>
    - `pytest --cov=app/services/voting --cov=app/auth` reports 100.0% for BOTH files in one combined run.
    - The full backend suite still passes (no regression introduced by the new test files).
    - Coverage delta for the two rules files is recorded in the SUMMARY (e.g. voting 35.5% → 100.0%, auth 82.5% → 100.0%).
  </behavior>
  <action>
Run two acceptance commands and record the numbers.

1. Combined coverage on the two target files (no `fail_under` — per D-37-05, that's Phase 39's job):

   ```
   cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_voting_unit.py tests/test_auth_unit.py --cov=app/services/voting --cov=app/auth --cov-report=term-missing
   ```

   Acceptance: both files show 100.0% in the per-file table; `Missing` column is empty for both.

2. Full-suite green run (no regression):

   ```
   cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest --cov=app --cov-report=term-missing
   ```

   Acceptance: exit code 0; full-suite pass count is greater than or equal to the count after Plan 37-01 (because Plan 37-02 only adds tests). If any previously-passing test now fails, the new tests are interfering with shared state — diagnose and fix the new test file's isolation, do not modify source.

If either command fails the acceptance check, fix the test files (not source) and re-run. Per D-37-05, do NOT add `fail_under = 100` to `pyproject.toml` — that's Phase 39 wiring.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test DATABASE_URL_TEST=postgresql+psycopg2://postgres:postgres@localhost:5433/aldente_test ENVIRONMENT=test GEMINI_API_KEY=stub SUPABASE_URL=https://stub.supabase.co SUPABASE_SERVICE_ROLE_KEY=stub uv run pytest tests/test_voting_unit.py tests/test_auth_unit.py --cov=app/services/voting --cov=app/auth --cov-report=term-missing 2>&1 | tail -20</automated>
  </verify>
  <done>Combined-file run shows both files at 100.0%; full-suite still exits 0; SUMMARY records voting 35.5%→100.0% and auth 82.5%→100.0% deltas.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → DB | New unit tests use the autouse seed fixture's seeded members for auth-integration tests; no external network. |
| test → auth.py contract | Tests pin the cookie-wins-over-Bearer ordering (invariant #8); a refactor that flips the order would fail the dedicated `cookie wins when both present` test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Tampering | New tests inadvertently mutating seeded rows | mitigate | All DB writes in tests go through the `db_session` fixture (connection-scoped transaction rolled back at teardown — `conftest.py:38-52`). Pure-function tests for voting take no DB session. |
| T-37-02-02 | Spoofing | `current_member` integration tests passing wrong member token | accept | Tests use the seed-provided `"test-token-luca"` / `"test-token-partner"` constants verbatim; mismatch would cause the happy-path test to 401, surfacing the issue immediately. |
| T-37-02-03 | Information disclosure | Auth tests logging tokens in pytest output | mitigate | Token assertions use substring presence on Set-Cookie headers; do NOT `print()` token values. The constants are well-known seed values, not production secrets. |
| T-37-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — only new test files. Audit table N/A. |

</threat_model>

<verification>
Plan-level acceptance:
1. `cd backend && uv run pytest tests/test_voting_unit.py tests/test_auth_unit.py --cov=app/services/voting --cov=app/auth --cov-report=term-missing` → both files at 100.0%.
2. `cd backend && uv run pytest` → exit code 0 (full suite green).
3. Inspect `backend/tests/test_voting_unit.py`: parametrized over the 9 cases from `<behavior>` (or more); all 5 `VoteState` values covered; member_count ∈ {1, 2}.
4. Inspect `backend/tests/test_auth_unit.py`: covers `generate_auth_token`, `set_auth_cookie`, `clear_auth_cookie`, `_extract_token` (4 branches), `current_member` (cookie/Bearer/missing/invalid).
</verification>

<success_criteria>
- All 6 `must_haves.truths` are demonstrably true.
- COV-02 (voting 100%), COV-05 (auth 100%), SERV-01 (5 states × member_count ∈ {1, 2}) all closed.
- No source file under `backend/app/` was modified (scope_fence enforced).
- No `fail_under` threshold added to `backend/pyproject.toml` (D-37-05).
- SUMMARY.md captures the voting and auth coverage deltas (numerator/denominator + percentage before vs after).
</success_criteria>

<output>
Create `.planning/phases/37-test-infrastructure-service-branch-coverage/37-02-SUMMARY.md` when done. Include the per-file coverage delta table and an explicit reference to which REQ-IDs the plan closes (COV-02, COV-05, SERV-01) — Plan 37-03 will reference this SUMMARY.
</output>
