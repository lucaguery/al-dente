---
phase: 11-production-synthetic-household
plan: 05
type: execute
wave: 4
depends_on:
  - "11-01"
  - "11-02"
  - "11-03"
  - "11-04"
files_modified:
  - RUNBOOK.md
  - .planning/v0.3/RUNBOOK.md
autonomous: true
requirements:
  - SEED-04
  - SEED-03
must_haves:
  truths:
    - "Operator opens `RUNBOOK.md` at the repo root and finds the 4 critical commands above the fold: refresh / teardown / smoke check / how-to-join-from-iPhone."
    - "Each command is in a copy-pasteable code block with the EXACT env-var prefix (`ALDENTE_PROD_SEED=1 SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... DATABASE_URL=... uv run seed --prod-synthetic`) — no `set -a; source .env` muscle-memory traps (Pitfall 3)."
    - "RUNBOOK.md has a 'Pre-flight' section listing the 5 verifications: direct (non-PgBouncer) DATABASE_URL, env-var contract, recipe-photos bucket exists in Supabase dashboard with Public=OFF, ALDENTE_PROD_SEED is unset in shells where the operator runs the test seed, DEMO01 not already taken in households table."
    - "RUNBOOK.md has a 'Smoke check' section explaining the COUNT-diff idempotency proof from D-13: run twice in a row, confirm all 6 banner counts match."
    - "RUNBOOK.md has a 'Troubleshooting' section covering the 5 named pitfalls: Pitfall 1 (bucket name), Pitfall 3 (env-file), Pitfall 6 (DEMO01 race), Pitfall 8 (Storage 401), Pitfall 9 (teardown FK violation)."
    - "RUNBOOK.md has a 'Joining from iPhone' section showing the operator how the auditor (or operator's own phone) uses DEMO01 via the existing `/onboarding/join` flow."
    - "`.planning/v0.3/RUNBOOK.md` exists as a stub linking to the root RUNBOOK.md (satisfies ROADMAP §Phase 11 success criterion 4 verbatim)."
    - "RUNBOOK.md does NOT include any `vercel --prod` / Railway deploy commands (memory: feedback_no_manual_vercel_deploy)."
    - "Every command in RUNBOOK.md actually works against the seed implementation from Plans 01-04 — no documented commands reference flags that don't exist."
  artifacts:
    - path: "RUNBOOK.md"
      provides: "Operator runbook at repo root: refresh + teardown + smoke check + iPhone join + troubleshooting"
      contains: "DEMO01"
    - path: ".planning/v0.3/RUNBOOK.md"
      provides: "Stub linking to the root RUNBOOK.md per ROADMAP §Phase 11 success criterion 4"
      contains: "RUNBOOK.md"
  key_links:
    - from: "RUNBOOK.md commands"
      to: "Plan 01-04 CLI implementation"
      via: "Documented invocations match argparse flag names: --prod-synthetic, --teardown"
      pattern: "uv run seed --prod-synthetic"
    - from: ".planning/v0.3/RUNBOOK.md"
      to: "../../../RUNBOOK.md"
      via: "Markdown link at repo root"
      pattern: "RUNBOOK.md"
---

<objective>
Write the operator runbook. Two files:
1. **`RUNBOOK.md` at the repo root** (D-17) — the actual content. Operator-discoverable; same shape as Phase 10's `TESTING.md` (4 critical commands above the fold, rationale and troubleshooting below).
2. **`.planning/v0.3/RUNBOOK.md`** — a one-paragraph stub that links to the root file. Exists because ROADMAP §Phase 11 success criterion 4 says "a runbook committed under `.planning/v0.3/`". The split (real content at root, stub in milestone dir) gives operators a discoverable file at a top-level path AND satisfies the ROADMAP's path requirement.

This plan is the LAST in Wave 4 because the documented commands must reflect the FINAL CLI shape from Plans 01-04. Document what the code actually does — not what the planner thinks it should do. Verify every command in this plan by reading the implementation in `seed.py`.

Scope boundary: Two markdown files only. Do NOT modify `seed.py`, `storage.py`, or any code file. If the runbook reveals that a documented command doesn't actually work, FLAG it in the SUMMARY and stop — the fix is a follow-up plan, not a runbook rewrite.

The operator (Luca) is on macOS with `uv` installed and the prod Supabase credentials in their dev environment. The runbook does NOT need to teach Postgres or Supabase basics — it's an "I have credentials, what do I run" reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-production-synthetic-household/11-CONTEXT.md
@.planning/phases/11-production-synthetic-household/11-RESEARCH.md
@backend/app/cli/seed.py
@backend/app/services/storage.py

<interfaces>
<!-- Final CLI shape after Plans 01-04 (verify by reading seed.py before writing the runbook): -->

```bash
# Test seed (unchanged from Phase 10):
uv run seed
# Test seed regressions:
ALDENTE_PROD_SEED=1 uv run seed                    # refused: D-04 symmetric guard
uv run seed --teardown                             # refused: --teardown requires --prod-synthetic

# Prod-synthetic seed:
ALDENTE_PROD_SEED=1 uv run seed --prod-synthetic
# Pre-flight refusals:
uv run seed --prod-synthetic                       # refused: missing ALDENTE_PROD_SEED env var
ALDENTE_PROD_SEED=1 uv run seed --prod-synthetic   # refused if database_url is not supabase.co

# Prod-synthetic teardown:
ALDENTE_PROD_SEED=1 uv run seed --prod-synthetic --teardown
```

```
Required env vars for prod-synthetic invocations (all 4 mandatory):
- DATABASE_URL=<direct, NON-PgBouncer Supabase Postgres URL — D-24 advisory lock requires direct connection>
- SUPABASE_URL=<https://<project>.supabase.co>
- SUPABASE_SERVICE_ROLE_KEY=<from Supabase Dashboard -> Project Settings -> API>
- ALDENTE_PROD_SEED=1
```

```
Banner shape (post-seed):
======================================================================
  SYNTHETIC HOUSEHOLD SEEDED — <uuid>
  Synthetic invite code: DEMO01
======================================================================
  recipes:                          21
  members:                           2
  cooking_logs:                      3
  votes:                             4
  shortlists:                        1
  storage objects (synthetic/):     21
======================================================================
  Idempotency check: re-run this command and confirm counts match.
======================================================================
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RUNBOOK.md at repo root</name>
  <files>RUNBOOK.md</files>
  <read_first>
    - .planning/phases/11-production-synthetic-household/11-CONTEXT.md (D-17 — runbook coverage scope)
    - .planning/phases/11-production-synthetic-household/11-RESEARCH.md (the 10 named Pitfalls — Pitfalls 1, 3, 6, 8, 9 are MUST-COVER per the must_haves; Pitfalls 2, 4, 5, 7, 10 are by-design or implementation-internal — mention briefly under "By-design behavior" if at all)
    - backend/app/cli/seed.py (final implementation — verify every documented command flag exists)
    - .planning/ROADMAP.md (Phase 11 success criterion 4: "a runbook committed under `.planning/v0.3/`")
    - frontend/TESTING.md (the v0.2.1 Phase 10 runbook for shape reference — same look-and-feel; 4-command-then-explanation structure)
  </read_first>
  <action>
    Create `RUNBOOK.md` at the repo root with the EXACT structure below. Use real backtick-fenced code blocks. Replace `<placeholder>` only where genuinely operator-supplied — keep `DEMO01`, `[SYNTHETIC] Démo Al Dente`, env var names, and CLI flag names verbatim.

    ```markdown
    # Production Synthetic Household — Operator Runbook

    **Phase:** v0.3 Phase 11 (SEED-01..05)
    **Audience:** Operator (Luca) on macOS with `uv` and prod Supabase credentials.
    **Status:** Synthetic household is named **`[SYNTHETIC] Démo Al Dente`** with fixed invite code **`DEMO01`**.

    ## TL;DR — The four commands

    Refresh the synthetic household (idempotent — safe to re-run any time):

    ```bash
    cd backend
    DATABASE_URL=<direct-supabase-postgres-url> \
    SUPABASE_URL=<https://<project>.supabase.co> \
    SUPABASE_SERVICE_ROLE_KEY=<service-role-key> \
    ALDENTE_PROD_SEED=1 \
    uv run seed --prod-synthetic
    ```

    Tear down the synthetic household (deletes all rows + storage objects):

    ```bash
    cd backend
    DATABASE_URL=<direct-supabase-postgres-url> \
    SUPABASE_URL=<https://<project>.supabase.co> \
    SUPABASE_SERVICE_ROLE_KEY=<service-role-key> \
    ALDENTE_PROD_SEED=1 \
    uv run seed --prod-synthetic --teardown
    ```

    Smoke check (idempotency proof — D-13):

    ```bash
    # Run the refresh command twice in a row.
    # Both runs MUST print identical row counts in the banner:
    #   recipes=21 members=2 cooking_logs=3 votes=4 shortlists=1 storage_objects=21
    # Anything else means the seed is non-idempotent — file an issue.
    ```

    Join from your iPhone:

    ```
    1. Open the app on your iPhone (PWA or browser).
    2. On onboarding, choose "Rejoindre un foyer existant" / "Join existing household".
    3. Enter the invite code: DEMO01
    4. Pick a member name (you become member #3 — the 2 seeded members are immutable).
    ```

    ---

    ## Pre-flight (verify ONCE, before the first invocation)

    Five checks the operator runs before the first prod-synthetic seed. Skip any of these and the seed will either refuse or fail mid-run.

    1. **Direct (non-PgBouncer) `DATABASE_URL`.** Supabase offers a pooled URL (port 6543) and a direct URL (port 5432). The seed uses Postgres advisory locks (D-24 concurrency guard); PgBouncer in transaction-pool mode does NOT support `pg_advisory_xact_lock` across the lock-acquire/release boundary. Use the direct URL. Verify:
       ```bash
       echo "$DATABASE_URL" | grep -E ':5432|:6543'
       # Expected match: :5432  (direct)
       # If you see :6543, switch to the direct URL from the Supabase Dashboard -> Project Settings -> Database -> Connection string -> "URI" (NOT "Connection pooler").
       ```

    2. **Env-var contract.** All four are required for prod-synthetic; missing any will produce a clear `REFUSING:` message but it's faster to check before invoking:
       ```bash
       for v in DATABASE_URL SUPABASE_URL SUPABASE_SERVICE_ROLE_KEY ALDENTE_PROD_SEED; do
         [ -n "${!v}" ] && echo "$v set" || echo "$v MISSING"
       done
       ```

    3. **`recipe-photos` Supabase Storage bucket exists with Public=OFF.** The seed uploads to this bucket (its name is `recipe-photos`, NOT `recipes` — a CONTEXT.md transcription error caught during research; see Troubleshooting below). Verify in the Supabase Dashboard -> Storage -> Buckets. The bucket is created at infra setup time, not by this CLI. If missing, create it as Public=OFF.

    4. **`ALDENTE_PROD_SEED` is unset in your normal dev shell.** The test seed (`uv run seed` no flags) refuses to run if `ALDENTE_PROD_SEED=1` is set in your env (D-04 symmetric guard). This catches the inverse footgun where you forgot to `unset` after a prod run. Either run prod-synthetic in a one-shot inline-env invocation (the TL;DR pattern above) OR `unset ALDENTE_PROD_SEED` before running the test seed.

    5. **`DEMO01` not already in use.** First-time only — the synthetic household claims `DEMO01` permanently once seeded. If a real-user household has been issued `DEMO01` somehow (random collision is ~4.6e-10 per signup), the first seed run fails with `duplicate key value violates unique constraint "households_invite_code_key"`. To check before the first run:
       ```sql
       -- In the Supabase SQL editor:
       SELECT id, name FROM households WHERE invite_code = 'DEMO01';
       -- Expected: 0 rows. If non-zero, see Troubleshooting -> "DEMO01 already taken".
       ```

    ---

    ## After every refresh — the banner

    A successful `uv run seed --prod-synthetic` ends with:

    ```
    ======================================================================
      SYNTHETIC HOUSEHOLD SEEDED — <uuid>
      Synthetic invite code: DEMO01
    ======================================================================
      recipes:                          21
      members:                           2
      cooking_logs:                      3
      votes:                             4
      shortlists:                        1
      storage objects (synthetic/):     21
    ======================================================================
      Idempotency check: re-run this command and confirm counts match.
    ======================================================================
    ```

    Copy the `Synthetic invite code: DEMO01` line — that's how an auditor (or your iPhone) joins.

    The 5 vote states (Validé / Pressenti / Contesté / Rejeté / Sans avis) are computed at read-time from 4 vote rows + 5 shortlist recipes (the 5th has no vote rows = "Sans avis"). 4 vote rows × 1 shortlist = `votes=4` is correct.

    ---

    ## After every teardown — the banner

    A successful `uv run seed --prod-synthetic --teardown` ends with:

    ```
    ======================================================================
      SYNTHETIC HOUSEHOLD TEARDOWN — <uuid>
    ======================================================================
      votes removed:                     4    (or 0 if already torn down)
      cooking_logs removed:              3
      daily_shortlists removed:          1
      recipes removed:                  21
      members removed:                   2    (or 3 if auditor had joined)
      households removed:                1
      storage objects removed:          21
    ======================================================================
    ```

    Re-running the teardown on already-empty state prints all zeros plus `Note: nothing to remove (already torn down or never seeded).` — that's expected and idempotent.

    ---

    ## Troubleshooting

    ### "REFUSING: ALDENTE_PROD_SEED=1 set but --prod-synthetic flag NOT passed"
    You ran `uv run seed` (test mode) with `ALDENTE_PROD_SEED=1` still set in env. Fix: either pass `--prod-synthetic` or `unset ALDENTE_PROD_SEED` (D-04 symmetric guard). This is intentional — it prevents the inverse footgun where stale env vars convince the operator they're seeding test but the test guard's behavior diverges silently.

    ### "REFUSING: --prod-synthetic passed but ALDENTE_PROD_SEED env var not '1'"
    You passed `--prod-synthetic` but didn't set `ALDENTE_PROD_SEED=1`. Use the TL;DR pattern: prefix the env var on the same line as the command. The dual-key opt-in (flag AND env var) is by design — single key persists silently across shells; dual key requires deliberate invocation per run.

    ### "REFUSING: database_url does not contain 'supabase.co'"
    Your `DATABASE_URL` doesn't look like prod. Either you're pointing at localhost / test (good — but use the test seed, no flag) or your `DATABASE_URL` is unset and pydantic-settings is loading from `backend/.env` instead. Inline the env var on the command line as in the TL;DR.

    ### "REFUSING: missing photo(s) at app/cli/synthetic_photos/<slug>.jpg"
    The 21 committed JPGs (Plan 03) are missing from your working tree. Either `git pull` to fetch them or check that `backend/app/cli/synthetic_photos/` is not in your local `.gitignore`. Each slug listed in the error message must have a matching `<slug>.jpg` file.

    ### "REFUSING: Supabase Storage not configured"
    `SUPABASE_URL` or `SUPABASE_SERVICE_ROLE_KEY` is missing. The seed checks Storage credentials BEFORE any DB write (Pitfall 8 — fail-fast prevents partial state). Inline both on the command line.

    ### `duplicate key value violates unique constraint "households_invite_code_key"`
    A real-user household holds `DEMO01`. Either:
    - **Pre-seed (no real users yet):** run the SQL `SELECT id FROM households WHERE invite_code = 'DEMO01'`, then if you can confirm that row is a stale test artifact: `UPDATE households SET invite_code = NULL WHERE id = '<that-id>'` and re-run the seed.
    - **Real user collision (~4.6e-10 probability):** change the fixed code in `backend/app/cli/seed.py` (search for `"DEMO01"`) to e.g. `"DEMO02"`, push to main (Railway auto-deploys), re-run the seed. Update this RUNBOOK.md to reflect the new code.

    ### "Bucket recipe-photos not found" / Storage 404 on upload
    The bucket name is **`recipe-photos`** (with a hyphen), not `recipes`. Verify in the Supabase Dashboard -> Storage -> Buckets. If missing, create it Public=OFF (the existing photo-read flow expects private + signed URLs). This is the single most common typo because CONTEXT.md initially said "recipes bucket" — the actual name is in `backend/app/services/storage.py:34`.

    ### Teardown raises FK violation mid-cascade
    The CLI does the deletes in explicit FK-respecting order (votes -> cooking_logs -> daily_shortlists -> recipes -> members -> households per D-16) — this should not happen with the shipped code. If it does, it means the data shape diverged from the seeded shape (e.g. someone manually inserted rows referencing the synthetic household). Recovery: re-run the teardown — every DELETE is conditioned on `household_id`, so partial state is safe to retry.

    ### Storage cleanup raises after Postgres deletes succeeded
    The teardown banner shows `storage objects removed: FAILED` and a WARNING above. Postgres state is clean; the Storage prefix delete failed (likely transient — network, Supabase outage). Re-run the same teardown command — Postgres deletes are no-ops on second run; Storage delete retries the prefix list. The synthetic-household scope guards mean retry is safe.

    ### "Joining from iPhone" but the recipe cards show no photos
    The seed populated `recipes.photo_paths = ["synthetic/<slug>.jpg"]` (Pitfall 2 mitigation) so signed-URL reads should work. If photos still don't render: check that `recipe-photos` bucket has the `synthetic/` prefix populated (Storage banner reported `storage objects: 21`); verify your iPhone's auth cookie isn't stale (re-onboard via DEMO01); check the browser console for 401 / 404 from the photos route.

    ---

    ## Operator workflow patterns

    **Refreshing the audit env before a session:**
    ```bash
    # Wipe and re-seed in one shell:
    cd backend
    EXPORT_BLOCK="DATABASE_URL=... SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... ALDENTE_PROD_SEED=1"
    eval "$EXPORT_BLOCK uv run seed --prod-synthetic --teardown"
    eval "$EXPORT_BLOCK uv run seed --prod-synthetic"
    # Banner from the second invocation prints the invite code DEMO01.
    ```

    **Testing changes to the seed without touching prod:**
    ```bash
    # The local test seed is unaffected by Phase 11 — same as Phase 10.
    cd backend && uv run seed
    # Output ends with: seed: ok household=... member=... recipes=21 logs=3 shortlist=...
    ```

    ---

    ## By-design behavior (NOT bugs)

    - **Auditor (member #3) is wiped on teardown.** Teardown deletes all members of the synthetic household, including any member that joined via `DEMO01`. The auditor re-joins after the next seed. If an audit walkthrough is in progress, coordinate with the auditor before tearing down.
    - **Re-running the seed shifts cooking_log dates forward (D-10).** A log seeded "2 days ago" today is "2 days ago" again tomorrow — the date slides. Same for the shortlist's `date` field (D-11). Cooking history always looks fresh.
    - **Member auth tokens rotate on every run (D-18).** The seeded members never have a usable login session for a human; they exist as historical authors of the seeded recipes and votes. Joining as member #3 via DEMO01 is the only way a human authenticates against the synthetic household.
    - **The synthetic household's UUID is stable across runs but distinct from the test seed's UUID.** Different namespaces (`aldente.test.*` vs `aldente.prod.synthetic.*`) — they cannot collide.

    ---

    ## Reference: what the seed actually writes

    | Table | Rows | Scope |
    |-------|------|-------|
    | households | 1 | The synthetic household — name `[SYNTHETIC] Démo Al Dente`, invite_code `DEMO01` |
    | members | 2 | Luca (color #F43F5E) + Partner (color #10B981) — fresh tokens per run, not printed |
    | recipes | 21 | Same specs as the local test seed — single source of truth in `_recipe_specs()` |
    | cooking_logs | 3 | ragu-bolognese (loved, -2d), poulet-citron (liked, -5d), burger-classique (disliked, -10d) |
    | daily_shortlists | 1 | Today's shortlist — 5 recipes (ragu-bolognese, coq-au-vin, butter-chicken, shawarma, tacos-boeuf) |
    | votes | 4 | Produces all 5 computed states for the 2 seeded members |
    | Storage `synthetic/` | 21 | One JPG per recipe slug under `recipe-photos` bucket |

    Vote states (computed by `services/voting.compute_vote_state` — invariant #2):
    - **Validé:** ragu-bolognese (Luca yes, Partner yes)
    - **Pressenti:** coq-au-vin (Luca yes, Partner none)
    - **Contesté:** butter-chicken (Luca yes, Partner no)
    - **Rejeté:** shawarma (Luca no, Partner no)
    - **Sans avis:** tacos-boeuf (no vote rows)

    ---

    ## File map

    - `backend/app/cli/seed.py` — the CLI itself (mode dispatch, guards, run_test_seed, run_prod_synthetic_seed, run_teardown).
    - `backend/app/services/storage.py` — Supabase Storage helpers (`upload_synthetic_photo_idempotent`, `teardown_synthetic_storage`).
    - `backend/app/cli/synthetic_photos/<slug>.jpg` × 21 — committed JPG corpus.
    - `backend/app/cli/synthetic_photos/README.md` — license/source attribution per photo.
    - `.planning/v0.3/RUNBOOK.md` — stub link to this file (satisfies ROADMAP §Phase 11 SC #4).

    ---

    *Phase 11 — Production Synthetic Household. Last updated when the runbook was committed; refresh on any CLI shape change.*
    ```
  </action>
  <verify>
    <automated>test -f RUNBOOK.md && grep -c "DEMO01" RUNBOOK.md && grep -c "uv run seed --prod-synthetic" RUNBOOK.md && grep -c "uv run seed --prod-synthetic --teardown" RUNBOOK.md && grep -c "ALDENTE_PROD_SEED=1" RUNBOOK.md && grep -c "## Pre-flight" RUNBOOK.md && grep -c "## Troubleshooting" RUNBOOK.md && grep -c "Joining from iPhone\\|join from iPhone\\|Joining from your iPhone" RUNBOOK.md && grep -c "recipe-photos" RUNBOOK.md && grep -c "PgBouncer\\|advisory lock\\|:5432" RUNBOOK.md && ! grep -c "vercel --prod\\|railway up" RUNBOOK.md && echo "RUNBOOK.md OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `RUNBOOK.md` exists at repo root.
    - Contains the literal string `DEMO01` at least 5 times (TL;DR, banner, troubleshooting, reference table, vote states).
    - Contains the literal string `[SYNTHETIC] Démo Al Dente` at least 1 time.
    - Contains a code block with the exact invocation `ALDENTE_PROD_SEED=1` and `uv run seed --prod-synthetic` (refresh).
    - Contains a code block with `uv run seed --prod-synthetic --teardown` (teardown).
    - Has a `## Pre-flight` (or `## Pre-flight (verify ONCE...)`) section.
    - Has a `## Troubleshooting` section.
    - Has a section with iPhone joining instructions (matches one of: "Joining from iPhone", "Join from iPhone", "Joining from your iPhone").
    - References `recipe-photos` (the actual bucket name, NOT `recipes`).
    - References PgBouncer or `:5432` or "advisory lock" (Pre-flight check #1 — direct connection requirement).
    - Contains the EXACT vote-state mapping for at least one of: ragu-bolognese -> Validé, coq-au-vin -> Pressenti, butter-chicken -> Contesté, shawarma -> Rejeté, tacos-boeuf -> Sans avis.
    - Does NOT contain `vercel --prod`, `vercel deploy`, `railway up`, or `railway deploy` (memory: feedback_no_manual_vercel_deploy).
    - Documents the post-seed banner shape including `recipes:`, `members:`, `cooking_logs:`, `votes:`, `shortlists:`, `storage objects (synthetic/):` literal labels.
    - Documents the teardown banner shape including `votes removed:`, `cooking_logs removed:`, `daily_shortlists removed:`, `recipes removed:`, `members removed:`, `households removed:`, `storage objects removed:`.
    - Every documented CLI flag (`--prod-synthetic`, `--teardown`) actually exists in `backend/app/cli/seed.py` (verify by `grep` against the source).
    - All Pitfall references in Troubleshooting (Pitfalls 1, 3, 6, 8, 9 from RESEARCH.md) are present and resolved with concrete recovery steps.
  </acceptance_criteria>
  <done>
    `RUNBOOK.md` at repo root is operator-discoverable, copy-pasteable, and accurate to the shipped code from Plans 01-04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .planning/v0.3/RUNBOOK.md stub</name>
  <files>.planning/v0.3/RUNBOOK.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 11 success criterion 4 — verbatim path requirement)
    - RUNBOOK.md (Task 1's output — to confirm what to link to)
  </read_first>
  <action>
    1. Create the directory `.planning/v0.3/` if it does not already exist.

    2. Create `.planning/v0.3/RUNBOOK.md` with this EXACT content (one paragraph + a link). The path is fixed by ROADMAP §Phase 11 success criterion 4 ("a runbook committed under `.planning/v0.3/`"); the content is intentionally minimal because the operator-facing source of truth lives at the repo root.

    ```markdown
    # v0.3 Runbook (Phase 11)

    The operator-facing runbook for the production synthetic household lives at the **repo root** for discoverability:

    > [`RUNBOOK.md`](../../RUNBOOK.md)

    This file exists at `.planning/v0.3/RUNBOOK.md` per ROADMAP §Phase 11 success criterion 4 ("a runbook committed under `.planning/v0.3/`"). The split (real content at root, stub here) gives operators a discoverable file at a top-level path AND satisfies the milestone's path requirement.

    ## What's covered in the runbook

    - The four critical commands: refresh / teardown / smoke check / iPhone join.
    - Pre-flight checks (5 verifications before the first invocation).
    - Banner shapes for both refresh and teardown.
    - Troubleshooting for the 5 most likely failure modes.
    - By-design behaviors (sliding dates, auditor wipe, token rotation).

    ## When to update

    - The CLI shape changes (new flag, renamed command, changed env-var contract).
    - A new troubleshooting case is identified by the operator.
    - The synthetic household's invite code or label changes.

    ## See also

    - `.planning/phases/11-production-synthetic-household/11-CONTEXT.md` — locked decisions (D-01..D-24).
    - `.planning/phases/11-production-synthetic-household/11-RESEARCH.md` — technical research (Pitfalls 1-10, FK chain, advisory lock derivation).
    - `backend/app/cli/seed.py` — the implementation.
    - `backend/app/cli/synthetic_photos/README.md` — photo source/license attribution.
    ```
  </action>
  <verify>
    <automated>test -f .planning/v0.3/RUNBOOK.md && grep -c "RUNBOOK.md" .planning/v0.3/RUNBOOK.md && grep -c "../../RUNBOOK.md\\|../../../RUNBOOK.md" .planning/v0.3/RUNBOOK.md && grep -c "Phase 11" .planning/v0.3/RUNBOOK.md && echo "stub OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/v0.3/RUNBOOK.md` exists.
    - Contains a markdown link to the root `RUNBOOK.md` — the link target resolves correctly relative to `.planning/v0.3/` (i.e., `../../RUNBOOK.md`).
    - References "Phase 11" or "v0.3 Phase 11".
    - References the ROADMAP success criterion 4 verbatim or paraphrases the path requirement.
    - Does NOT duplicate the operational content of the root RUNBOOK.md (this is a stub, not a copy).
    - Mentions Phase 11's three CONTEXT/RESEARCH/seed.py source files for further reading.
  </acceptance_criteria>
  <done>
    Stub committed at the path required by ROADMAP §Phase 11 success criterion 4. Links resolve. Operator who navigates to `.planning/v0.3/` finds a clear pointer to the real runbook.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> RUNBOOK.md commands | Every command in the runbook gets executed verbatim. A typo here = an operator running the wrong thing against prod. |
| Documented commands -> shipped CLI | Plans 01-04 implement the actual flags. Runbook documents only what `seed.py` actually accepts. |
| RUNBOOK.md -> public eyes | Repo is currently private but should be assumed public for safety. The runbook MUST NOT contain real secrets, real DATABASE_URLs, or real service-role keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Tampering | runbook documenting wrong commands | mitigate | Acceptance criteria explicitly checks every documented flag against `seed.py`. The plan's `read_first` includes `seed.py` so the executor reads the source before writing the doc. |
| T-11-05-02 | Information Disclosure | secrets in markdown | mitigate | Every credential field is a `<placeholder>` with angle brackets — clearly a fill-in slot, not a real value. The runbook explicitly tells the operator to inline the env vars on the command line, not store them in a file. |
| T-11-05-03 | Spoofing | bucket name documented as `recipes` instead of `recipe-photos` | mitigate | Pitfall 1 is the lead troubleshooting entry. Acceptance criterion: `grep -c "recipe-photos" RUNBOOK.md >= 1`. |
| T-11-05-04 | Tampering | runbook drift from CLI changes | accept | Documented in the runbook itself (`## When to update`). No automated drift-detection — operator updates manually when changing the seed. Couple-scale; don't over-engineer. |
| T-11-05-05 | Elevation of Privilege | unauthorized deploy commands | mitigate | Acceptance criterion: `grep -c "vercel --prod\|railway up" RUNBOOK.md == 0` (memory: feedback_no_manual_vercel_deploy). The runbook explicitly does NOT include any deploy operations — only the seed CLI. |
| T-11-05-06 | Repudiation | who-ran-the-seed-when | accept | Out of scope per CONTEXT.md "Deferred Ideas". Operator's `git log` + commit timestamps suffice for the audit window. |
| T-11-05-07 | Denial of Service | operator confusion -> wrong command | mitigate | TL;DR section above the fold (Phase 10 TESTING.md shape). Pre-flight section (5 checks) before invocation. Banner shape documented so operator can recognize success. Troubleshooting covers the 5 most likely failure modes with recovery steps. |
| T-11-05-08 | Information Disclosure | invite code DEMO01 visible in runbook | accept | DEMO01 is intentionally fixed and predictable (D-14) — recognizable as synthetic if leaked. Anyone with the code can join the synthetic household, but that household is clearly labeled `[SYNTHETIC]` and contains no real-user data. |
</threat_model>

<verification>
```bash
# Both files exist
test -f RUNBOOK.md && test -f .planning/v0.3/RUNBOOK.md && echo "files OK"

# Root RUNBOOK has all required sections
for section in "TL;DR" "Pre-flight" "Troubleshooting" "By-design" "## Reference"; do
  grep -q "$section" RUNBOOK.md && echo "$section: OK" || echo "$section: MISSING"
done

# Documented flags exist in seed.py (drift check)
for flag in "--prod-synthetic" "--teardown"; do
  grep -q "\"$flag\"" backend/app/cli/seed.py && echo "$flag: in seed.py" || echo "$flag: MISSING in seed.py — runbook drifted"
done

# No deploy commands
grep -c "vercel --prod\|railway up\|railway deploy" RUNBOOK.md
# Expected: 0

# Stub link resolves
ls -la "$(dirname .planning/v0.3/RUNBOOK.md)/../../RUNBOOK.md" && echo "stub link resolves"

# Pitfall coverage
for pf in "PgBouncer\|:5432" "ALDENTE_PROD_SEED" "DEMO01" "Storage" "FK"; do
  grep -q "$pf" RUNBOOK.md && echo "$pf: covered" || echo "$pf: not covered"
done
```
</verification>

<success_criteria>
1. `RUNBOOK.md` at repo root with TL;DR (4 commands), Pre-flight (5 checks), banner shapes, Troubleshooting, By-design, Reference, File map sections.
2. Every documented CLI flag actually exists in `backend/app/cli/seed.py`.
3. `recipe-photos` (correct bucket) appears; `"recipes"` as a bucket name does not.
4. Pre-flight section calls out PgBouncer / `:5432` direct-connection requirement (Pitfall — advisory locks).
5. Troubleshooting covers Pitfalls 1, 3, 6, 8, 9 from RESEARCH.md with concrete recovery steps.
6. The `Joining from iPhone` section explains DEMO01 -> auditor as member #3 flow.
7. NO `vercel --prod` / Railway deploy commands anywhere (memory: feedback_no_manual_vercel_deploy).
8. `.planning/v0.3/RUNBOOK.md` is a stub linking to `../../RUNBOOK.md`; satisfies ROADMAP §Phase 11 success criterion 4 verbatim path requirement.
9. Both files committable (no broken markdown, no `<placeholder>` for content that should be literal).
</success_criteria>

<output>
After completion, create `.planning/phases/11-production-synthetic-household/11-05-SUMMARY.md` documenting:
- File sizes for both RUNBOOK.md files
- Confirmation that every documented flag exists in seed.py
- Confirmation that no deploy commands appear
- The list of Pitfalls explicitly covered in Troubleshooting
- Any drift between the runbook and the actual implementation discovered during writing (and whether the runbook was adjusted to match the code, NOT the other way around)
- Note any product-code concerns flagged but NOT fixed (per scope-creep memory)
</output>
