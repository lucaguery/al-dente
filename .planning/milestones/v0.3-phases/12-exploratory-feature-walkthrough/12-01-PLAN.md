---
phase: 12-exploratory-feature-walkthrough
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v0.3/WALKTHROUGH.md
  - .planning/v0.3/walkthrough-inputs/voice/01-clean-french.txt
  - .planning/v0.3/walkthrough-inputs/voice/02-garbled-accent.txt
  - .planning/v0.3/walkthrough-inputs/voice/03-very-short.txt
  - .planning/v0.3/walkthrough-inputs/photo/README.md
  - .planning/v0.3/walkthrough-inputs/photo/.gitkeep
  - .planning/v0.3/walkthrough-inputs/url/urls.md
  - .planning/v0.3/walkthrough-inputs/voice-modify/01-add-ingredient.txt
  - .planning/v0.3/walkthrough-inputs/voice-modify/02-substitute.txt
  - .planning/v0.3/walkthrough-screenshots/.gitkeep
autonomous: true
requirements:
  - WALK-03
must_haves:
  truths:
    - ".planning/v0.3/WALKTHROUGH.md exists with all 14 surface section headers, header-level-2"
    - "Severity rubric and uniform finding template (D-04) are documented in the skeleton"
    - "Backlog dedupe block (D-06) lists the 6 known backlog IDs verbatim"
    - "Canned input artifacts under .planning/v0.3/walkthrough-inputs/{voice,photo,url,voice-modify}/ exist with no Gemini API key leakage"
    - "GitHub label `audit:walkthrough` exists on lucaguery/al-dente"
    - "`gh auth status` confirms gh is authenticated against lucaguery/al-dente"
    - "Zero diff under frontend/ or backend/"
  artifacts:
    - path: ".planning/v0.3/WALKTHROUGH.md"
      provides: "Skeleton with 14 surface sections, severity rubric, backlog dedupe block, finding template"
      contains: "## Capture — Quick"
    - path: ".planning/v0.3/walkthrough-inputs/voice/"
      provides: "Canned voice inputs (transcripts; .m4a fallback) — D-13"
      min_files: 3
    - path: ".planning/v0.3/walkthrough-inputs/photo/README.md"
      provides: "Photo input manifest describing 3 expected JPGs (D-13)"
    - path: ".planning/v0.3/walkthrough-inputs/url/urls.md"
      provides: "Canned URL list (clean / paywall / non-recipe), no auth in query strings"
    - path: ".planning/v0.3/walkthrough-inputs/voice-modify/"
      provides: "Voice-modify transcripts — D-13"
      min_files: 2
    - path: ".planning/v0.3/walkthrough-screenshots/.gitkeep"
      provides: "Directory placeholder for D-18 screenshots"
  key_links:
    - from: ".planning/v0.3/WALKTHROUGH.md"
      to: ".planning/v0.3/walkthrough-inputs/"
      via: "Markdown link or path reference in golden-path notes"
      pattern: "walkthrough-inputs/"
    - from: ".planning/v0.3/WALKTHROUGH.md"
      to: ".planning/v0.3/walkthrough-screenshots/"
      via: "Filename convention <surface>-<probe-slug>.png referenced in template"
      pattern: "walkthrough-screenshots/"
---

<objective>
Bootstrap Phase 12 audit infrastructure: scaffold `.planning/v0.3/WALKTHROUGH.md` with all 13 surface sections, the locked severity rubric (D-01/D-02), the uniform finding template (D-04), and the backlog dedupe block (D-06); commit reusable canned inputs (D-13) under `.planning/v0.3/walkthrough-inputs/{voice,photo,url,voice-modify}/`; create the `audit:walkthrough` GitHub label on `lucaguery/al-dente` (D-03); verify `gh auth status` so subsequent plans can file issues without auth detours.

Purpose: Plans 02-04 require a non-empty WALKTHROUGH.md to write findings into incrementally (D-20), the canned inputs must be on-disk before any AI surface is probed (D-12 reproducibility), and the GitHub label must exist before Plan 05 batches issue creation (D-03).

Output: skeleton WALKTHROUGH.md, ~6-8 input artifacts, 1 GitHub label, verified `gh auth status` output captured in the WALKTHROUGH preamble.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md
@.planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md
@.planning/v0.3/RUNBOOK.md
@RUNBOOK.md
@CLAUDE.md
</context>

<scope_constraint>
**HARD CONSTRAINT — NO product code changes.** This phase produces only files under `.planning/v0.3/`, GitHub-side changes (label, issues), and screenshots. Any `git diff --name-only` match against `^(frontend|backend)/` is a fatal scope violation.

Permitted writes (this plan):
- `.planning/v0.3/WALKTHROUGH.md` (create)
- `.planning/v0.3/walkthrough-inputs/{voice,photo,url,voice-modify}/**` (create)
- `.planning/v0.3/walkthrough-screenshots/.gitkeep` (create)

Permitted GitHub changes (this plan):
- `gh label create audit:walkthrough --repo lucaguery/al-dente --color "FBCA04" --description "Phase 12 walkthrough findings"`

Forbidden:
- ANY edit under `frontend/` or `backend/`.
- Filing GitHub issues (Plan 05's job).
- Modifying `.planning/v0.3/RUNBOOK.md` (Phase 11 deliverable).
- Modifying `.planning/STATE.md` or `.planning/ROADMAP.md` (orchestrator commits those).
</scope_constraint>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify gh auth, create audit:walkthrough label, scaffold v0.3 directories</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-03 issue protocol; D-13 input layout; D-18 screenshot path)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"GitHub Issue Filing Template" and §"Recommended Artifact Structure"
    - RUNBOOK.md (verify the synthetic env exists and DEMO01 is the invite code — informational only; do NOT run the seed)
  </read_first>
  <files>
    .planning/v0.3/walkthrough-inputs/voice/.gitkeep,
    .planning/v0.3/walkthrough-inputs/photo/.gitkeep,
    .planning/v0.3/walkthrough-inputs/url/.gitkeep,
    .planning/v0.3/walkthrough-inputs/voice-modify/.gitkeep,
    .planning/v0.3/walkthrough-screenshots/.gitkeep
  </files>
  <action>
1. Run `gh auth status` and capture stdout. Required: output must show `Logged in to github.com as <user>` AND token has `repo` scope. If `gh auth status` errors, STOP and surface the auth gate to the user before continuing — do NOT attempt to proceed without GitHub auth.
2. Run `gh repo view lucaguery/al-dente --json name,owner` to confirm the repo is reachable. Capture stdout for the SUMMARY.
3. Idempotently create the GitHub label:
   ```bash
   gh label list --repo lucaguery/al-dente --json name | jq -r '.[].name' | grep -qx 'audit:walkthrough' \
     || gh label create audit:walkthrough \
          --repo lucaguery/al-dente \
          --color "FBCA04" \
          --description "Phase 12 walkthrough findings"
   ```
   Verify post-condition: `gh label list --repo lucaguery/al-dente --json name | jq -r '.[].name' | grep -qx 'audit:walkthrough'` exits 0.
4. Create the four input subdirectories and the screenshot directory with `.gitkeep` placeholders:
   ```bash
   for d in voice photo url voice-modify; do
     mkdir -p .planning/v0.3/walkthrough-inputs/$d
     : > .planning/v0.3/walkthrough-inputs/$d/.gitkeep
   done
   mkdir -p .planning/v0.3/walkthrough-screenshots
   : > .planning/v0.3/walkthrough-screenshots/.gitkeep
   ```
5. Verify the artifact allowlist held: `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` MUST return empty. If non-empty, ABORT and revert.
  </action>
  <acceptance_criteria>
    - `gh auth status` exit code is 0 AND output contains the substring `Logged in to github.com`.
    - `gh label list --repo lucaguery/al-dente --json name | jq -r '.[].name' | grep -qx 'audit:walkthrough'` exits 0.
    - `ls .planning/v0.3/walkthrough-inputs/{voice,photo,url,voice-modify}/.gitkeep` lists 4 files.
    - `ls .planning/v0.3/walkthrough-screenshots/.gitkeep` lists 1 file.
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/v0.3/walkthrough-screenshots/.gitkeep && for d in voice photo url voice-modify; do test -f .planning/v0.3/walkthrough-inputs/$d/.gitkeep || exit 1; done && gh label list --repo lucaguery/al-dente --json name | jq -r '.[].name' | grep -qx 'audit:walkthrough' && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>Directories scaffolded, label created on remote, gh auth verified, no product-code drift.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit canned inputs (transcripts + URL list + photo manifest)</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md §"Live AI-API cost handling" (D-13 enumerates required input set)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Canned Input Artifact Specs (D-13)" (formats, durations, content suggestions)
    - frontend/tests/e2e/capture-voice.spec.ts (the prod voice surface accepts JSON `{transcript: "..."}` — confirms transcripts are the load-bearing input format)
    - frontend/tests/e2e/capture-photo.spec.ts (multipart field name is `files`, not `photos`; useful for Plan 02 but not for this task)
    - frontend/tests/e2e/capture-url.spec.ts (golden URL pattern)
  </read_first>
  <files>
    .planning/v0.3/walkthrough-inputs/voice/01-clean-french.txt,
    .planning/v0.3/walkthrough-inputs/voice/02-garbled-accent.txt,
    .planning/v0.3/walkthrough-inputs/voice/03-very-short.txt,
    .planning/v0.3/walkthrough-inputs/voice-modify/01-add-ingredient.txt,
    .planning/v0.3/walkthrough-inputs/voice-modify/02-substitute.txt,
    .planning/v0.3/walkthrough-inputs/url/urls.md,
    .planning/v0.3/walkthrough-inputs/photo/README.md
  </files>
  <action>
Per D-13 the inputs must be reproducible and committed. Per RESEARCH §Canned Input Artifact Specs, the prod voice surface accepts JSON transcripts (verified via `capture-voice.spec.ts:14-23`). Commit transcripts as the primary canned-voice input format; if Plan 02's live read of the prod voice handler shows multipart-audio support, the operator can drop `.m4a` clips alongside in a follow-up — out of scope for this plan.

1. Create `.planning/v0.3/walkthrough-inputs/voice/01-clean-french.txt` with exact content:
   ```
   Risotto aux champignons, pour deux personnes. Riz arborio, parmesan, bouillon de légumes, oignon émincé, vin blanc sec, beurre, champignons de Paris.
   ```
2. Create `.planning/v0.3/walkthrough-inputs/voice/02-garbled-accent.txt` with content simulating heavy noise / partial transcription:
   ```
   euh risotto aux champignons hum pour... deux personnes je crois riz arboriooo parmesan bouillon ouais oignon émincé vin blanc sec beurre champignons de paris voilà
   ```
3. Create `.planning/v0.3/walkthrough-inputs/voice/03-very-short.txt` with exact content (boundary input):
   ```
   Pâtes au beurre.
   ```
4. Create `.planning/v0.3/walkthrough-inputs/voice-modify/01-add-ingredient.txt`:
   ```
   Rajoute des champignons et de la crème fraîche.
   ```
5. Create `.planning/v0.3/walkthrough-inputs/voice-modify/02-substitute.txt`:
   ```
   Remplace le bœuf par du poulet.
   ```
6. Create `.planning/v0.3/walkthrough-inputs/url/urls.md` with EXACT content (do NOT include any URLs containing `?api_key=`, `?token=`, or any query-string auth — T-03 mitigation):
   ```markdown
   # Phase 12 walkthrough — canned URL inputs

   Authored: 2026-05-09. Checked: 2026-05-09.

   ## 01 — Clean recipe site (golden path)
   https://www.marmiton.org/recettes/recette_risotto-aux-champignons_28057.aspx

   Expected: clean recipe extraction, structured promotion. Note: per URL-01 backlog,
   `recipes.py:481-490` is `# TODO(productize)` — drafts created from URL never promote.
   The probe documents this as a blocker per D-14 and cross-links URL-01 instead of
   filing a new issue.

   ## 02 — Paywalled site (negative test)
   https://www.lemonde.fr/cuisine/

   Expected: Gemini cannot read the paywalled body; promotion may fail or produce a
   stub. NOTE: paywall behavior is an external-content limitation, not a product bug —
   do NOT file a blocker for paywall-induced failure.

   ## 03 — Non-recipe URL (boundary)
   https://en.wikipedia.org/wiki/Risotto

   Expected: Wikipedia article, not a recipe. Gemini may extract structured fields
   from the article description. Document the actual extracted shape.
   ```
7. Create `.planning/v0.3/walkthrough-inputs/photo/README.md` describing the 3 expected JPGs (Plan 02 may add the actual JPGs in-flight if the operator captures them on iPhone; otherwise the surface is probed without canned photos and the section explicitly notes "no canned photo committed — used live screen-grab"):
   ```markdown
   # Phase 12 walkthrough — canned photo inputs

   Per D-13 each photo input is committed for reproducibility. The actual JPGs are
   added in Plan 02 (capture-photo probe) if the operator can supply them; if not,
   the photo surface is probed with live ad-hoc images and the WALKTHROUGH section
   explicitly notes which probe used which input.

   Expected files (each ≤200KB, JPEG 75-85%, ~1024px long edge):

   | File | Content | Purpose |
   |------|---------|---------|
   | 01-clean-cookbook.jpg | Well-lit top-down photo of a French cookbook page or printed recipe card | Golden path; Gemini OCR baseline |
   | 02-dim-handwritten.jpg | Dimly-lit handwritten recipe (notebook, low contrast) | Edge: handwriting + low light |
   | 03-non-recipe-landscape.jpg | A landscape (beach, mountain — anything non-food) | Negative test |

   Source: Unsplash CC0 / Pexels (same source pattern as Phase 11 D-20) OR operator-captured originals.
   No image with embedded EXIF API keys / location-leaking metadata.
   ```
8. Scrub-check the URL list one final time before commit: `grep -E '\?(api_key|token|key|secret|password)=' .planning/v0.3/walkthrough-inputs/url/urls.md` MUST return empty (T-03 mitigation).
9. Verify the artifact allowlist held: `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` MUST return empty.
  </action>
  <acceptance_criteria>
    - `ls .planning/v0.3/walkthrough-inputs/voice/*.txt | wc -l` returns 3.
    - `ls .planning/v0.3/walkthrough-inputs/voice-modify/*.txt | wc -l` returns 2.
    - `test -f .planning/v0.3/walkthrough-inputs/url/urls.md` returns 0.
    - `test -f .planning/v0.3/walkthrough-inputs/photo/README.md` returns 0.
    - `grep -E '\?(api_key|token|key|secret|password)=' .planning/v0.3/walkthrough-inputs/url/urls.md` returns empty (T-03).
    - All 3 voice transcripts are non-empty AND contain at least one French word matching `[éèàâîôûç]` (rough Frenchness check; invariant #6).
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(ls .planning/v0.3/walkthrough-inputs/voice/*.txt 2>/dev/null | wc -l | tr -d ' ')" = "3" ] && [ "$(ls .planning/v0.3/walkthrough-inputs/voice-modify/*.txt 2>/dev/null | wc -l | tr -d ' ')" = "2" ] && test -f .planning/v0.3/walkthrough-inputs/url/urls.md && test -f .planning/v0.3/walkthrough-inputs/photo/README.md && ! grep -qE '\?(api_key|token|key|secret|password)=' .planning/v0.3/walkthrough-inputs/url/urls.md && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>All 7 input artifacts written, no auth tokens in URL list, French content present, no product-code drift.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write WALKTHROUGH.md skeleton with 13 surface sections + rubric + dedupe block</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md §"Implementation Decisions" (D-01..D-20 — full rubric to encode in skeleton)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"WALKTHROUGH.md Skeleton" (copy-pasteable template starting at line ~648)
    - .planning/ROADMAP.md §"Phase 12" (success criteria #1 names the 13 surfaces in canonical order — D-11)
  </read_first>
  <files>.planning/v0.3/WALKTHROUGH.md</files>
  <action>
Write `.planning/v0.3/WALKTHROUGH.md` using the template from RESEARCH §"WALKTHROUGH.md Skeleton", adapted to the locked decisions. The skeleton MUST include, in this order:

1. **Frontmatter / preamble** (top of file):
   - Title: `# v0.3 Phase 12 — Exploratory Feature Walkthrough`
   - Auditor identity: `Claude (Playwright MCP) — member #3 (DEMO01)` (per D-19)
   - Realtime co-auditor: `member #4 (DEMO01, joined for the realtime section)` (per D-15)
   - Target environment: `prod Supabase, [SYNTHETIC] Démo Al Dente household` (per Phase 11 D-05)
   - Session date: `YYYY-MM-DD` (placeholder; Plan 02 fills in)
   - Session length: `~Xh` (placeholder)
   - Gemini call total: `~XX` (placeholder; Plan 05 sweep totals)

2. **"How to read this document"** section — embed the uniform finding template (D-04) verbatim:
   ```
   ### <severity-tag> <P-XX-NN>: <one-line title>
   **Severity:** blocker | friction | nit
   **Surface:** <surface name>
   **Probe kind:** garbage | racing | network | invalid-state
   **Starting state:** <one-liner>
   **Repro:**
   1. <step>
   2. <step>
   **Expected:** <one paragraph>
   **Actual:** <one paragraph>
   **Screenshot:** `walkthrough-screenshots/<surface>-<probe-slug>.png` (optional)
   **Issue:** <github-url> (blockers only — D-05)
   ```

3. **Severity rubric** block — encode D-01 / D-02 verbatim:
   - **blocker**: crash / 500 / data loss, OR primary intended action non-functional even via workaround. Files a GitHub issue.
   - **friction**: costs the user time, attention, or confidence. Stays in this doc as Phase 14 input.
   - **nit**: visual or copy polish. Stays in this doc as Phase 14 input.

4. **"Backlog dedupe (D-06)"** section listing the 6 known backlog IDs (must contain each ID as plain text so Plan 05 can grep):
   - `Sheet-01` (#1, https://github.com/lucaguery/al-dente/issues/1) — bottom sheet off-screen on iPhone viewport
   - `TZ-01` — `cooking_logs.py:72-78,118-126` timezone bug
   - `URL-01` — `recipes.py:481-490` URL extraction stub
   - `CL-01` — GET /cooking-logs (list) endpoint missing
   - `SEED-01-local` — local seed cross-day idempotency hole (closed for prod synthetic by Phase 11)
   - `POLISH-01` / `POLISH-02` — i18n sweep + Copy button on invite code

5. **13 surface sections** in D-11 order with EXACT level-2 headers (the executor in Plans 02-04 will grep for these to write into them):
   - `## Capture — Quick`
   - `## Capture — Full`
   - `## Capture — Voice`
   - `## Capture — Photo`
   - `## Capture — URL`
   - `## Shortlist`
   - `## Vote`
   - `## Cooking Log`
   - `## History`
   - `## Exports`
   - `## Push`
   - `## Realtime Sync`
   - `## Onboarding`
   - `## Settings`

   (That's 14 headers; the ROADMAP/CONTEXT D-11 lists 13 but the canonical "Settings" surface is implicit in Phase 12 success criterion #1's "settings" entry. RESEARCH §Per-Surface Probe Playbook also lists 14 numbered surfaces. Use 14 headers — one for each shipped surface — to ensure WALK-01 coverage; Settings is required.)

   Under each surface header, pre-populate:
   - `**Starting state:** <to be filled in Plan 02/03/04>`
   - `**Golden path:** <to be filled — references the corresponding e2e spec>`
   - `**Probes:**` (empty for now — written incrementally per D-20)
   - `**Gemini calls in this section:** 0 (or N/A for non-AI surfaces)`

6. **"Summary"** section (placeholder for Plan 05 sweep):
   - Findings by severity: Blockers: X (Y filed as new issues, Z cross-linked to backlog) / Friction: A / Nits: B
   - Gemini calls total: ~XX
   - Surfaces with no issues found: (list)

7. **"Inputs to Phase 14"** section: one paragraph stating that this document + `walkthrough-screenshots/` + filed GitHub issues form the Phase 14 input set.

After writing, verify:
- `grep -c '^## ' .planning/v0.3/WALKTHROUGH.md` returns ≥16 (14 surfaces + Summary + Inputs to Phase 14, plus any rubric/dedupe headers).
- `grep -F '## Capture — Quick' .planning/v0.3/WALKTHROUGH.md` returns 1 hit (similar for all 14 surface headers — see acceptance criteria below).
- The 6 backlog IDs (`Sheet-01`, `TZ-01`, `URL-01`, `CL-01`, `SEED-01-local`, `POLISH-01`) all grep-find in the file.
- The uniform template's `**Severity:** blocker | friction | nit` line is present once (in "How to read this document").
- `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </action>
  <acceptance_criteria>
    - `test -f .planning/v0.3/WALKTHROUGH.md` returns 0.
    - `wc -l .planning/v0.3/WALKTHROUGH.md` returns ≥80 (skeleton is non-trivial).
    - `grep -cF '^## ' .planning/v0.3/WALKTHROUGH.md` ≥ 16 (14 surfaces + Summary + Inputs to Phase 14).
    - All 14 exact surface headers present, verified by `grep -F '<header>' WALKTHROUGH.md` returning ≥1 for each.
    - All 6 backlog IDs (`Sheet-01`, `TZ-01`, `URL-01`, `CL-01`, `SEED-01-local`, `POLISH-01`) grep-found in file.
    - `grep -F 'Severity:' .planning/v0.3/WALKTHROUGH.md` returns ≥1 (template present).
    - `grep -F 'DEMO01' .planning/v0.3/WALKTHROUGH.md` returns ≥1 (auditor identity recorded).
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/v0.3/WALKTHROUGH.md && [ "$(wc -l < .planning/v0.3/WALKTHROUGH.md)" -ge 80 ] && for h in '## Capture — Quick' '## Capture — Full' '## Capture — Voice' '## Capture — Photo' '## Capture — URL' '## Shortlist' '## Vote' '## Cooking Log' '## History' '## Exports' '## Push' '## Realtime Sync' '## Onboarding' '## Settings'; do grep -qF "$h" .planning/v0.3/WALKTHROUGH.md || { echo "MISSING: $h"; exit 1; }; done && for b in Sheet-01 TZ-01 URL-01 CL-01 SEED-01-local POLISH-01; do grep -qF "$b" .planning/v0.3/WALKTHROUGH.md || { echo "MISSING backlog: $b"; exit 1; }; done && grep -qF 'DEMO01' .planning/v0.3/WALKTHROUGH.md && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>WALKTHROUGH.md skeleton committed with all 14 surface section headers, severity rubric, uniform finding template, backlog dedupe block, summary placeholder, and no product-code drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → public Git remote | URLs and transcripts authored locally are committed and pushed; anything secret-shaped that lands here becomes public. |
| local repo → GitHub label/issues API | `gh` invocations create remote artifacts on `lucaguery/al-dente`; label creation is reversible (gh label delete) but visible to anyone who watches the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Information Disclosure | RUNBOOK.md `DEMO01` invite code | accept | DEMO01 is already public in `RUNBOOK.md` per Phase 11 D-15 (printed to stdout, documented in committed runbook). Not a new leak — note as accepted. |
| T-03 | Information Disclosure | `walkthrough-inputs/url/urls.md` | mitigate | Task 2 step 8 greps for `?api_key=` / `?token=` / `?key=` / `?secret=` / `?password=` patterns and rejects commit if any match. URLs in this list are public marketing/recipe sites with no auth in query strings. |
</threat_model>

<verification>
**Plan-level verification (run after all 3 tasks):**

1. **Artifact allowlist held:** `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty. (Scope-creep guard.)
2. **WALKTHROUGH.md skeleton ready for incremental writing (D-20):** all 14 surface headers exist; Plans 02-04 can `grep`-locate their target section.
3. **Canned inputs reproducible (D-13):** all 5 transcript files + url list + photo README committed.
4. **GitHub label exists for Plan 05:** `gh label list --repo lucaguery/al-dente --json name | jq -r '.[].name' | grep -qx 'audit:walkthrough'`.
5. **gh authenticated:** `gh auth status` exit 0.

Validation Architecture: explicitly N/A per RESEARCH §"Validation Architecture: explicitly N/A". This phase has no testable code; verification is markdown-shape + filesystem checks executed inline by the verifier agent.
</verification>

<success_criteria>
- WALK-03 partial coverage: WALKTHROUGH.md skeleton exists with severity rubric and uniform finding template encoded — full coverage achieved across plans 02-05.
- All 5 transcript inputs + URL list + photo manifest committed.
- `audit:walkthrough` label exists on `lucaguery/al-dente`.
- `gh auth status` confirmed.
- Zero diff under `frontend/` or `backend/`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-exploratory-feature-walkthrough/12-01-SUMMARY.md` with:
- The exact `gh auth status` username + scope summary captured.
- The actual count of `## ` headers in WALKTHROUGH.md and the surface-header sanity check.
- Confirmation that the artifact allowlist held.
- Any decisions made under Claude's discretion (e.g. transcript content choices, photo README phrasing).
- Pointer to Plan 02 (next wave): "Capture surfaces probe pass — 5 surfaces (quick / full / voice / photo / url)."
</output>
