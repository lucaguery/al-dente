---
phase: 12-exploratory-feature-walkthrough
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - .planning/v0.3/WALKTHROUGH.md
  - .planning/v0.3/walkthrough-screenshots/
autonomous: false
requirements:
  - WALK-01
  - WALK-02
  - WALK-03
must_haves:
  truths:
    - "WALKTHROUGH.md §Exports has Golden path note + ≥3 weird-state probe entries with severity tags"
    - "WALKTHROUGH.md §Push has Golden path note + ≥3 entries; subscribe + 1 fired notification (D-19) covered, with operator confirmation if needed"
    - "WALKTHROUGH.md §Realtime Sync has Golden path note + 6 event-class probes + 1 reconnect probe (D-16); cookie-isolation behavior documented"
    - "WALKTHROUGH.md §Onboarding has Golden path note + ≥3 entries with severity tags"
    - "WALKTHROUGH.md §Settings has Golden path note + ≥3 entries; POLISH-02 cross-link if surfaced"
    - "Member #4 joined via DEMO01 for realtime probes (per D-15); auditor (member #3) never logged out"
    - "Zero diff under frontend/ or backend/"
  artifacts:
    - path: ".planning/v0.3/WALKTHROUGH.md"
      provides: "5 cross-cutting surfaces fully documented"
      min_severity_lines: 22  # 4 surfaces × ≥4 + realtime sync × ≥7 (6 events + reconnect) ≈ 23
    - path: ".planning/v0.3/walkthrough-screenshots/"
      provides: "Visual evidence for all 5 cross-cutting surfaces, especially realtime A-side observation pairs"
      min_files: 8  # heavier visual coverage for realtime
  key_links:
    - from: ".planning/v0.3/WALKTHROUGH.md §Settings"
      to: "POLISH-02 backlog (Copy button missing on invite code)"
      via: "Cross-link per D-06 if Copy button is missing"
      pattern: "POLISH-02"
    - from: ".planning/v0.3/WALKTHROUGH.md §Realtime Sync"
      to: "backend/app/services/realtime.py event class list"
      via: "Each of 6 event classes (recipe.created, recipe.promoted, recipe.updated, vote.created, shortlist.created, cooking.started) named in a probe"
      pattern: "recipe\\.created|recipe\\.promoted|recipe\\.updated|vote\\.created|shortlist\\.created|cooking\\.started"
---

<objective>
Probe the 5 cross-cutting surfaces (Exports / Push / Realtime Sync / Onboarding / Settings) against the prod synthetic env. Each surface gets 1 golden + ≥3 weird-state probes; Realtime Sync is the heaviest — 6 event-class probes + 1 reconnect (D-15/D-16/D-17). This plan is `autonomous: false` because (a) Push (D-19) likely needs the operator's iPhone for round-trip notification verification, and (b) Realtime two-context cookie-isolation behavior may need operator confirmation per RESEARCH §"Realtime Sync Two-Context Invocation Pattern" Step 0.

Purpose: These surfaces verify the system-level promises of the app — data export, notification delivery, multi-device sync, identity/auth flows, household management. Realtime is the load-bearing connective tissue (architecture invariant #4); if any of the 6 broadcast classes don't reach the other client, the multi-phone product proposition is broken.

Output: 5 fully-populated cross-cutting sections in WALKTHROUGH.md, ≥8 screenshots, member #4 joined into the synthetic household for realtime, zero edits to product code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md
@.planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md
@.planning/phases/12-exploratory-feature-walkthrough/12-03-SUMMARY.md
@.planning/v0.3/WALKTHROUGH.md
@RUNBOOK.md
@CLAUDE.md
@backend/app/services/realtime.py

<interfaces>
<!-- Architecture invariants and contracts the auditor must respect. -->

Architecture invariant #4 (realtime contract): All household-affecting mutations broadcast via `services/realtime.broadcast_to_household`. The 6 v0.1 event classes (verified in `backend/app/services/realtime.py` docstring lines 9-19):
  - `recipe.created`    — POST /api/recipes/quick (and other capture surfaces)
  - `recipe.promoted`   — services/llm.py BackgroundTask after Gemini structuring
  - `recipe.updated`    — PUT /api/recipes/{id}
  - `vote.created`      — POST /api/votes
  - `shortlist.created` — POST /api/shortlist/regenerate
  - `cooking.started`   — POST /api/cooking-logs

Frame shape (frontend/lib/ws.ts compatible): `{"type": "<event_type>", "payload": {...}}` JSON-encoded text frame.

Architecture invariant #8 (cookie auth): Both browser contexts authenticate via the `aldente_auth` HttpOnly cookie. Member #3 = auditor's persistent context. Member #4 = realtime co-auditor's context, joined via DEMO01.

Per RESEARCH §"Realtime Sync Two-Context Invocation Pattern" — Step 0:
  - `mcp__playwright__browser_tabs` MAY share cookies across tabs (single browser context).
  - **Recommended fallback**: two sequential MCP browser sessions (one per member) — clean cookie isolation guaranteed. Document the actual observed behavior in WALKTHROUGH preamble.

Push notifications (D-19, scope = subscribe + 1 fired):
  - `pushManager.subscribe()` runs against the prod backend.
  - Headless Chromium may not have an FCM/APNs receiver (RESEARCH §Risk 3).
  - Operator-triggered round-trip from iPhone is the documented fallback ("verified by Luca on YYYY-MM-DD HH:MM").

Settings (POLISH-02 backlog):
  - The invite-code section may lack a Copy button. If absent, friction-tag and cross-link POLISH-02; do NOT file new issue.

Onboarding (per `frontend/tests/e2e/invite-code-happy-path.spec.ts`):
  - Surface: `/onboarding/welcome` → "Rejoindre un foyer existant" → enter invite code → pick name + color.
  - Backend uppercase-filters the invite code (per `join/page.tsx#177-181` referenced in RESEARCH §Surface 13).
  - Color collision returns 409 (`households.py#171`).
</interfaces>
</context>

<scope_constraint>
**HARD CONSTRAINT — NO product code changes.**

Permitted writes:
- `.planning/v0.3/WALKTHROUGH.md` (edit existing — fill in §Exports, §Push, §Realtime Sync, §Onboarding, §Settings)
- `.planning/v0.3/walkthrough-screenshots/*.png` (create)

Permitted MCP browser actions:
- `mcp__playwright__*` against PROD_URL.
- All probes are member-#3-scoped EXCEPT realtime, which spawns member #4 via DEMO01.
- Member #4 may persist post-phase per CONTEXT D-15 (idempotent re-seed leaves them; harmless).
- Auditor (member #3) NEVER logs out. If the two-context realtime probe accidentally overwrites member #3's cookie (cookie-jar surprise per RESEARCH §Risk 2), ABORT the probe and switch to two sequential MCP sessions (the documented fallback).

Permitted operator interactions (this plan is `autonomous: false`):
- For D-19 push round-trip: operator may fire one notification from their iPhone or backend admin path; auditor records inline confirmation ("verified by Luca on YYYY-MM-DD HH:MM, notification arrived in ~Ns").
- For D-15 cookie-isolation Step 0: operator may confirm browser_tabs cookie behavior if the auditor cannot determine programmatically.

Forbidden:
- ANY edit under `frontend/` or `backend/`.
- Filing GitHub issues (Plan 05).
- Logging out of member #3 (the auditor — distinct from member #4 cookie surface in realtime).
- Running `uv run seed --prod-synthetic --teardown` unless genuinely blocked.
</scope_constraint>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Probe Exports + Onboarding + Settings surfaces (4-5 probes total per surface)</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-06 backlog; POLISH-02 carrier)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Per-Surface Probe Playbook" Surfaces 10 (Exports), 13 (Onboarding), 14 (Settings)
    - frontend/tests/e2e/settings.spec.ts (golden path: member name / household / signout)
    - frontend/tests/e2e/invite-code-happy-path.spec.ts (onboarding join flow)
    - .planning/v0.3/WALKTHROUGH.md (locate §Exports, §Onboarding, §Settings headers)
  </read_first>
  <files>
    .planning/v0.3/WALKTHROUGH.md,
    .planning/v0.3/walkthrough-screenshots/exports-json-download.png,
    .planning/v0.3/walkthrough-screenshots/exports-offline.png,
    .planning/v0.3/walkthrough-screenshots/exports-rapid-double.png,
    .planning/v0.3/walkthrough-screenshots/onboarding-bad-code.png,
    .planning/v0.3/walkthrough-screenshots/onboarding-lowercase-code.png,
    .planning/v0.3/walkthrough-screenshots/onboarding-color-collision.png,
    .planning/v0.3/walkthrough-screenshots/settings-long-name.png,
    .planning/v0.3/walkthrough-screenshots/settings-invite-copy.png
  </files>
  <action>
**Step 1 — Exports (≥3 probes; no e2e spec exists, refer to backend/app/routers/exports.py).**

  - **Starting state:** "post-Plan-03 state; auditor still member #3."
  - **Golden path:** Navigate to `${PROD_URL}/settings` (or wherever the export CTA lives — read live). Trigger JSON export. Use `mcp__playwright__browser_network_requests` to capture the response body — the file may not download cleanly via MCP (per RESEARCH §Surface 10 note). Verify the JSON shape: contains synthetic household data? Recipe count matches? Vote rows present?

  Probes:
  1. **(Golden)** Trigger JSON export, observe download (or response body via network log). File contains synthetic household data? Schema reasonable? Screenshot `exports-json-download.png`.
  2. **(Network)** Trigger export while offline (`browser_evaluate(() => window.dispatchEvent(new Event('offline')))`). Error state clear? Screenshot `exports-offline.png`.
  3. **(Boundary / Racing)** Trigger export twice rapidly. Two responses? Single coalesced? Use `mcp__playwright__browser_network_requests` to count. Screenshot `exports-rapid-double.png`.

**Step 2 — Onboarding (≥3 probes).**

  Onboarding probes are tricky: the auditor is ALREADY member #3. Logging out to test re-onboarding would violate the "never log out" rule. Two options per probe:
   a. Use `mcp__playwright__browser_navigate` to visit `/onboarding/welcome` directly even while authenticated — read what the page does (probably auto-redirects to `/`).
   b. Run the onboarding probes in PREP for the realtime Task 2 (member #4 join) — i.e. fold the onboarding probes into the act of joining as member #4. This is acceptable per D-08 kind 4 (invalid-state arrivals).

  - **Starting state:** "auditor logged in as member #3; onboarding probes use deep-link navigation to /onboarding/welcome."
  - **Golden path:** Document the redirect behavior when an authenticated user hits `/onboarding/welcome`. (Mirrors `auth.skip-onboarding.spec.ts`.)

  ≥3 weird-state probes (these can run in member-#4 join context if simpler):
  1. **(Garbage)** Submit invite code = `ZZZZZZ` (correct shape, doesn't exist in DB). Error state? Friction UX? Screenshot `onboarding-bad-code.png`.
  2. **(Boundary)** Submit invite code in lowercase: `demo01`. Does the input uppercase-filter it (per `join/page.tsx#177-181`)? Or 4xx? Screenshot `onboarding-lowercase-code.png`.
  3. **(Invalid state)** Try to join with a duplicate color (member #3 owns rose). Backend returns 409. Frontend surfaces what? Screenshot `onboarding-color-collision.png`.

  **NOTE:** If running the onboarding probes via member-#4 join would interfere with Task 2's realtime setup, run them via deep-link navigation INSTEAD — visiting `/onboarding/welcome` and submitting bad inputs without committing to a join. The exact path is at executor's discretion per CONTEXT §Claude's Discretion.

**Step 3 — Settings (≥3 probes).**

  - **Starting state:** "after Onboarding probes; back on `/settings` as member #3."
  - **Golden path:** Mirror `frontend/tests/e2e/settings.spec.ts`. Inspect: member name field, household name display, color, invite code section, signout button.

  ≥3 probes:
  1. **(Boundary)** Change member name to ~200 chars + emoji + diacritics. Does it submit? Render correctly across other surfaces (e.g. inbox card author byline)? Screenshot `settings-long-name.png`.
  2. **(Friction — POLISH-02)** Inspect the invite-code section: does it have a Copy button? Per `STATE.md` POLISH-02 backlog, the Copy button is on the carry-over polish list. **If absent**, friction-tag, cross-link to POLISH-02 — do NOT file new issue. Screenshot `settings-invite-copy.png`.
  3. **(Invalid state)** If a "Quitter le foyer" path exists, INSPECT only — DO NOT click it (would log out and break scope per T-02). Document its presence + expected behavior based on i18n strings. If no such path, substitute another probe (e.g. attempt to set member name to empty string).

  Document each probe per D-04 in WALKTHROUGH §Settings.

**Step 4 — Verify scope-creep guard:**
  ```bash
  git diff --name-only HEAD | grep -E "^(frontend|backend)/"
  ```
  MUST return empty.
  </action>
  <acceptance_criteria>
    - WALKTHROUGH.md §Exports has Golden path note + ≥3 entries each with `**Severity:**` line.
    - WALKTHROUGH.md §Onboarding has Golden path note + ≥3 entries each with `**Severity:**` line.
    - WALKTHROUGH.md §Settings has Golden path note + ≥3 entries each with `**Severity:**` line.
    - If POLISH-02 surfaces (no Copy button): WALKTHROUGH §Settings contains the substring `POLISH-02`.
    - At least 6 of 8 expected screenshot files exist.
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>EXP_SEV=$(awk '/^## Exports$/,/^## Push$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); ONB_SEV=$(awk '/^## Onboarding$/,/^## Settings$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); SET_SEV=$(awk '/^## Settings$/,/^## Summary$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); SHOTS=$(ls .planning/v0.3/walkthrough-screenshots/exports-*.png .planning/v0.3/walkthrough-screenshots/onboarding-*.png .planning/v0.3/walkthrough-screenshots/settings-*.png 2>/dev/null | wc -l | tr -d ' '); [ "$EXP_SEV" -ge 3 ] && [ "$ONB_SEV" -ge 3 ] && [ "$SET_SEV" -ge 3 ] && [ "$SHOTS" -ge 6 ] && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>Exports + Onboarding + Settings each have ≥3 severity-tagged probes, POLISH-02 cross-linked if surfaced, screenshots committed, no product-code drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Probe Realtime Sync — two-context, 6 event classes + reconnect (D-15/D-16/D-17)</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-15 two-context, D-16 6 event classes, D-17 observation-only)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Realtime Sync Two-Context Invocation Pattern (D-15/D-16)" — Step 0 cookie-isolation verification + canonical pattern
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Per-Surface Probe Playbook" Surface 12
    - backend/app/services/realtime.py (6 event classes — lines 9-19 — verified upstream)
    - frontend/tests/e2e/invite-code-happy-path.spec.ts (literal two-context blueprint; lines 38-163)
    - frontend/tests/e2e/drafts-inbox.spec.ts (single-context WS observation reference)
    - .planning/v0.3/WALKTHROUGH.md (locate §Realtime Sync header)
  </read_first>
  <files>
    .planning/v0.3/WALKTHROUGH.md,
    .planning/v0.3/walkthrough-screenshots/realtime-cookie-isolation-test.png,
    .planning/v0.3/walkthrough-screenshots/realtime-recipe-created.png,
    .planning/v0.3/walkthrough-screenshots/realtime-recipe-promoted.png,
    .planning/v0.3/walkthrough-screenshots/realtime-recipe-updated.png,
    .planning/v0.3/walkthrough-screenshots/realtime-vote-created.png,
    .planning/v0.3/walkthrough-screenshots/realtime-shortlist-created.png,
    .planning/v0.3/walkthrough-screenshots/realtime-cooking-started.png,
    .planning/v0.3/walkthrough-screenshots/realtime-reconnect.png
  </files>
  <action>
**This is the gnarliest section of the phase. Follow the Step-0 verification before committing to a context-isolation strategy.**

**Step 0 — Verify cookie-isolation behavior in MCP (per RESEARCH §"Realtime Sync Two-Context Invocation Pattern" Step 0).**

  In auditor's current MCP session (member #3, persistent cookie):
  1. `mcp__playwright__browser_tabs(action: 'new')` — open Tab 2.
  2. `mcp__playwright__browser_navigate` Tab 2 to `${PROD_URL}/onboarding/welcome`.
  3. Run join flow as member #4 with `DEMO01`, name "Auditor B", a free color.
  4. After join completes, `mcp__playwright__browser_tabs(action: 'select', index: 0)` (back to Tab 1).
  5. Inspect Tab 1's identity: navigate to `/settings` or any authenticated route, observe whose name is shown.
     - If still "Auditor" (member #3) → cookies are per-tab isolated → use multi-tab pattern.
     - If now "Auditor B" (member #4) → shared cookie jar → tab 2's join overwrote tab 1's cookie. **CRITICAL: auditor (member #3) is GONE; this counts as a logout-equivalent.** Per scope-creep guard, abort the multi-tab approach IMMEDIATELY and either (a) re-join as member #3 in tab 1 (best-effort recovery; document the recovery in WALKTHROUGH preamble) or (b) fall back to two sequential MCP sessions where the auditor re-spawns Claude's MCP browser as member #3 fresh.

  Document the OBSERVED behavior verbatim in WALKTHROUGH §Realtime Sync preamble (a one-paragraph note). Screenshot `realtime-cookie-isolation-test.png`.

**Step 1 — Set up canonical two-context pattern.**

  Based on Step 0 observation, choose:
  - **Multi-tab path** (cookies isolated): Tab 1 = member #3 observer, parked on `/`. Tab 2 = member #4, fires mutations.
  - **Two-session fallback** (cookies shared): The auditor manages two sequential MCP browser sessions. Member #3 in session 1 stays parked; member #4 spawned in session 2 fires mutations. Switch between sessions to observe.

  Document the chosen pattern in WALKTHROUGH §Realtime Sync preamble.

**Step 2 — Run the 6 event-class probes (D-16). Each probe:**
  1. Take baseline screenshot of A's view (typically home `/` showing drafts inbox + shortlist).
  2. In B, fire the mutation.
  3. Wait ~3s.
  4. Take observation screenshot of A's view.
  5. Diff via `mcp__playwright__browser_snapshot` DOM tree comparison or visual inspection.
  6. Record outcome: arrived (yes/no), within ~3s? Body excerpt from `mcp__playwright__browser_network_requests` WS frames if useful.

  Per D-16, run one probe per event class (canonical 6 classes from `backend/app/services/realtime.py:9-19`):

  1. **`recipe.created`** — In B, POST a quick capture (click "Quick" tab, type title, submit). Observe drafts inbox in A receive the new draft (slideUp animation). Screenshot `realtime-recipe-created.png`.
  2. **`recipe.promoted`** — In B, POST a voice transcript via canned `01-clean-french.txt`. Observe drafts inbox in A: draft slides in, then Badge cross-fades to `structured`. Screenshot `realtime-recipe-promoted.png`.
  3. **`recipe.updated`** — In B, navigate to a recipe's edit view and PUT a change. Observe A's view of that recipe (or the inbox card) updating. Screenshot `realtime-recipe-updated.png`.
  4. **`vote.created`** — In B, vote yes on a shortlist card. Observe A's vote chip flipping. Screenshot `realtime-vote-created.png`.
  5. **`shortlist.created`** — In B, click "Régénérer" on the shortlist. Observe A's swipe deck refreshing. Screenshot `realtime-shortlist-created.png`.
  6. **`cooking.started`** — In B, click "Cuisiner" on a recipe. Observe A's cooking banner appearing. Screenshot `realtime-cooking-started.png`.

  For each probe, write the WALKTHROUGH entry per D-04 with:
  - **Probe kind:** "racing" (cross-context observation timing)
  - **Severity:** `friction` if event arrived but slowly (>3s), `blocker` if event didn't arrive at all, `nit` for animation polish issues, PASS-with-no-finding if smooth.

**Step 3 — Reconnect probe (D-17, exactly 1).**

  In A, drop the WS:
  ```text
  mcp__playwright__browser_evaluate(function: """
  () => { window.dispatchEvent(new Event('offline')); }
  """)
  // Wait 2s.
  mcp__playwright__browser_evaluate(function: """
  () => { window.dispatchEvent(new Event('online')); }
  """)
  // Hard reload A:
  mcp__playwright__browser_navigate(url: PROD_URL + '/')
  ```
  Then in B, fire ONE recipe.created (POST quick capture). In A, verify the event arrives via WS reconnect. Screenshot `realtime-reconnect.png`.

**Step 4 — Operator confirmation checkpoint.**

  Pause and surface the realtime probe outcomes to the operator. The operator should:
  - Confirm cookie-isolation observation matches their understanding.
  - Spot-check that auditor (member #3) is still authenticated (`/settings` shows "Auditor", not "Auditor B").
  - Confirm no probe was misclassified (e.g. animation issue tagged `blocker` when it should be `friction`).

  Resume signal: operator types "approved" or describes corrections.

**Step 5 — Verify scope-creep guard:**
  ```bash
  git diff --name-only HEAD | grep -E "^(frontend|backend)/"
  ```
  MUST return empty.
  </action>
  <acceptance_criteria>
    - WALKTHROUGH.md §Realtime Sync preamble describes the observed cookie-isolation behavior (one paragraph, searchable for "isolated" or "shared" or "tab" or "session").
    - §Realtime Sync contains ≥7 entries each with `**Severity:**` line (6 event classes + 1 reconnect).
    - §Realtime Sync names all 6 event classes (regex match: each of `recipe.created`, `recipe.promoted`, `recipe.updated`, `vote.created`, `shortlist.created`, `cooking.started` appears at least once).
    - §Realtime Sync has a reconnect probe (searchable: `reconnect` or `WS drop` or `offline.*online`).
    - At least 6 of 8 expected screenshot files exist.
    - Auditor's member #3 session is still active (operator-confirmable post-checkpoint).
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>RT_SEV=$(awk '/^## Realtime Sync$/,/^## Onboarding$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); RT_EVENTS=$(awk '/^## Realtime Sync$/,/^## Onboarding$/' .planning/v0.3/WALKTHROUGH.md | grep -cE 'recipe\.created|recipe\.promoted|recipe\.updated|vote\.created|shortlist\.created|cooking\.started'); RT_RECON=$(awk '/^## Realtime Sync$/,/^## Onboarding$/' .planning/v0.3/WALKTHROUGH.md | grep -ciE 'reconnect|ws drop|offline'); SHOTS=$(ls .planning/v0.3/walkthrough-screenshots/realtime-*.png 2>/dev/null | wc -l | tr -d ' '); [ "$RT_SEV" -ge 7 ] && [ "$RT_EVENTS" -ge 6 ] && [ "$RT_RECON" -ge 1 ] && [ "$SHOTS" -ge 6 ] && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <what-built>Realtime Sync section with 6 event-class probes + 1 reconnect probe, member #4 joined for cross-context observation, cookie-isolation behavior documented.</what-built>
  <how-to-verify>
1. Open `.planning/v0.3/WALKTHROUGH.md` and scroll to §Realtime Sync.
2. Verify the preamble describes cookie-isolation observation (one paragraph).
3. Verify 7 probe entries (6 event classes + 1 reconnect), each with severity tag.
4. Verify each of the 6 event classes (`recipe.created`, `recipe.promoted`, `recipe.updated`, `vote.created`, `shortlist.created`, `cooking.started`) appears in at least one probe.
5. Open `.planning/v0.3/walkthrough-screenshots/` — verify 7-8 `realtime-*.png` files exist.
6. Confirm the auditor's primary MCP session is still authenticated as member #3 (visit `/settings` and verify the displayed name).
  </how-to-verify>
  <resume-signal>Type "approved" or describe corrections needed.</resume-signal>
  <done>Realtime Sync has ≥7 severity-tagged probes covering all 6 event classes + reconnect, cookie-isolation observation documented, member #3 still authenticated, screenshots committed, no product-code drift.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Probe Push surface (subscribe + 1 fired notification, D-19) — operator-assist</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-19 push depth)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Per-Surface Probe Playbook" Surface 11 (Push) + Risk 3
    - .planning/v0.3/WALKTHROUGH.md (locate `## Push` header)
  </read_first>
  <files>
    .planning/v0.3/WALKTHROUGH.md,
    .planning/v0.3/walkthrough-screenshots/push-subscribe-permission.png,
    .planning/v0.3/walkthrough-screenshots/push-subscribe-network.png,
    .planning/v0.3/walkthrough-screenshots/push-resubscribe-idempotent.png
  </files>
  <action>
**Push surface — subscribe + 1 fired notification per D-19. This task likely needs the operator's iPhone for the round-trip.**

  - **Starting state:** "after Realtime Sync probes; auditor still member #3 in primary MCP session."
  - **Golden path:** Navigate to `${PROD_URL}/settings`, find the "Activer les notifications" CTA. Tap it.
    - In headless Chromium, the permission prompt may auto-grant or auto-deny via Playwright's permission settings.
    - Use `mcp__playwright__browser_evaluate(function: "() => navigator.serviceWorker.ready.then(r => r.pushManager.getSubscription()).then(s => s ? s.endpoint : null)")` to check if a subscription was created.
    - Use `mcp__playwright__browser_network_requests` to verify a POST to `/api/push/subscribe` happened. Screenshot `push-subscribe-permission.png` and `push-subscribe-network.png`.

  Probes per D-19 (subscribe + 1 fired):

  1. **(Golden — subscribe side)** Tap "Activer les notifications" → grant permission → observe `pushManager.subscribe()` POSTs subscription to `/api/push/subscribe`. Document. Severity: PASS or `blocker` if subscription fails entirely.

  2. **(Send round-trip)** This is the operator-assist probe.
     - **Auditor attempts**: try to fire one notification programmatically — check the backend `services/push.py` for any test-fire endpoint accessible to authenticated users; if there's an admin /api/push/test endpoint or similar, attempt it via `mcp__playwright__browser_evaluate(() => fetch('/api/push/test', {method: 'POST'}))` (auditor's cookie auth).
     - **If no programmatic path exists**: surface this checkpoint to the operator. The operator fires one notification from their iPhone (which is already configured as a member of the synthetic household post-onboarding via DEMO01) OR triggers a real product event that should fire a notification (e.g. operator-side vote on a recipe).
     - **Auditor records the outcome inline**: "verified by Luca on YYYY-MM-DD HH:MM, notification arrived in ~Ns" OR "notification did not arrive within 30s — friction or blocker per D-01".

  3. **(Boundary — idempotency)** Re-tap "Activer les notifications" when already subscribed. Idempotent? Or duplicate subscription rows? Use `mcp__playwright__browser_network_requests` to inspect. Screenshot `push-resubscribe-idempotent.png`.

**Per D-19 explicitly:** "If the auditor cannot trigger a send programmatically, document and request operator assistance for one round-trip." This is **expected**, not a blocker. The operator's confirmation line is the artifact.

**Verify scope-creep guard:**
  ```bash
  git diff --name-only HEAD | grep -E "^(frontend|backend)/"
  ```
  MUST return empty.
  </action>
  <acceptance_criteria>
    - WALKTHROUGH.md §Push has Golden path note + ≥3 entries each with `**Severity:**` line.
    - §Push contains operator confirmation line OR a clear "operator round-trip pending" friction tag.
    - §Push records the subscription endpoint URL (or absence) verifiably (`pushManager.getSubscription()` output).
    - At least 2 of 3 expected screenshot files exist.
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>PUSH_SEV=$(awk '/^## Push$/,/^## Realtime Sync$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); PUSH_OPERATOR=$(awk '/^## Push$/,/^## Realtime Sync$/' .planning/v0.3/WALKTHROUGH.md | grep -ciE 'verified by|operator|round-trip'); SHOTS=$(ls .planning/v0.3/walkthrough-screenshots/push-*.png 2>/dev/null | wc -l | tr -d ' '); [ "$PUSH_SEV" -ge 3 ] && [ "$PUSH_OPERATOR" -ge 1 ] && [ "$SHOTS" -ge 2 ] && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <what-built>Push surface section with subscribe-side documented + 1 fired notification round-trip (operator-confirmed if needed) + idempotent re-subscribe probe.</what-built>
  <how-to-verify>
1. Open `.planning/v0.3/WALKTHROUGH.md` §Push.
2. Verify ≥3 severity-tagged probe entries.
3. Verify the round-trip outcome is documented: either programmatic fire succeeded, OR operator confirmation line present (e.g. "verified by Luca on YYYY-MM-DD HH:MM").
4. Verify subscription endpoint is recorded (or absence noted).
  </how-to-verify>
  <resume-signal>Operator: confirm the push round-trip outcome OR confirm "no operator iPhone available — friction-tag the round-trip".</resume-signal>
  <done>Push has ≥3 severity-tagged probes + operator confirmation line, screenshots committed, no product-code drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor primary session (member #3) → realtime co-auditor (member #4) | Two-context probe spawns member #4 via DEMO01; cookie-jar isolation is the load-bearing premise. If shared, member #3 cookie is overwritten and the audit "logs out" inadvertently. |
| browser → push backend | `pushManager.subscribe()` POSTs subscription endpoint (FCM/APNs URL) to /api/push/subscribe. The endpoint URL is the standard issued by FCM — no PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02 | Information Disclosure / Privilege Escalation | Member-#3 session scope | mitigate | Step 0 cookie-isolation verification gates the multi-tab pattern; if shared cookies, fall back to two sequential sessions. Auditor never logs out via UI. If member #3 cookie is overwritten by member #4 join, ABORT and re-join as member #3 explicitly. |
| T-05 | Information Disclosure | push subscription endpoint | accept | The subscription endpoint is the FCM/APNs URL standard — no PII. Recording it in WALKTHROUGH.md (markdown body) is acceptable. The WALKTHROUGH file is committed to the repo (public via the project's GitHub if applicable; check repo visibility before committing if endpoint URL contains opaque tokens). |
</threat_model>

<verification>
**Plan-level verification:**
1. **Coverage:** WALKTHROUGH.md contains all 5 surface headers (Exports, Push, Realtime Sync, Onboarding, Settings).
2. **Probe density:** Each non-realtime surface has ≥3 severity-tagged entries; Realtime Sync has ≥7 (6 events + reconnect).
3. **Realtime invariants:** All 6 event classes named in §Realtime Sync; cookie-isolation observation paragraph present; reconnect probe documented.
4. **Push round-trip:** §Push contains operator confirmation OR explicit "round-trip pending" friction tag.
5. **Backlog cross-links:** §Settings contains `POLISH-02` if Copy button missing.
6. **Visual evidence:** ≥14 screenshot files added by this plan in `.planning/v0.3/walkthrough-screenshots/`.
7. **Scope-creep guard:** `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
8. **Auditor identity:** Member #3 session still authenticated (operator-confirmed at Task 2 checkpoint).

Validation Architecture: explicitly N/A.
</verification>

<success_criteria>
- WALK-01 partial: 14 of 14 surfaces probed (all surfaces complete after this plan).
- WALK-02 partial: every section in this plan includes ≥3 deliberate weird-state probes; realtime has 6+1.
- WALK-03 partial: every probe entry uses the uniform finding template with severity tag.
- All 6 broadcast event classes have explicit cross-client probes (architecture invariant #4).
- Cookie-isolation behavior of `mcp__playwright__browser_tabs` documented.
- Push subscribe + 1 fired notification documented (programmatic or operator-assisted).
- Zero diff under `frontend/` or `backend/`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-exploratory-feature-walkthrough/12-04-SUMMARY.md` with:
- Per-surface probe count and severity breakdown for the 5 surfaces.
- Cookie-isolation observation (one sentence).
- Push round-trip outcome (programmatic or operator-confirmed timestamp).
- Backlog cross-links made (POLISH-02? others?).
- List of NEW (non-backlog) blocker findings → these go to Plan 05 for filing, with WALKTHROUGH anchor reference.
- Member #4's identity (name, color) — since they persist post-phase per D-15.
- Confirmation that the artifact allowlist held.
- Pointer to Plan 05: "Final pass — severity sweep, backlog dedupe, batch issue filing, WALKTHROUGH.md commit + bidirectional cross-link insertion."
</output>
