---
phase: 12-exploratory-feature-walkthrough
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - .planning/v0.3/WALKTHROUGH.md
  - .planning/v0.3/walkthrough-screenshots/
autonomous: true
requirements:
  - WALK-01
  - WALK-02
  - WALK-03
must_haves:
  truths:
    - "WALKTHROUGH.md §Capture — Quick contains a Golden path note + ≥3 weird-state probe entries with severity tags"
    - "WALKTHROUGH.md §Capture — Full contains a Golden path note + ≥3 weird-state probe entries with severity tags"
    - "WALKTHROUGH.md §Capture — Voice contains a Golden path note + ≥3 weird-state probe entries with severity tags + Gemini call count"
    - "WALKTHROUGH.md §Capture — Voice preamble records the live-confirmed surface contract (JSON transcript vs multipart audio per RESEARCH Open Question 3 resolution)"
    - "WALKTHROUGH.md §Capture — Photo contains a Golden path note + ≥3 weird-state probe entries with severity tags + Gemini call count + Sheet-01 cross-link if surfaced"
    - "WALKTHROUGH.md §Capture — URL contains a Golden path note + ≥3 weird-state probe entries with severity tags + Gemini call count + URL-01 cross-link"
    - "Screenshots referenced in capture sections exist under .planning/v0.3/walkthrough-screenshots/"
    - "Zero diff under frontend/ or backend/"
  artifacts:
    - path: ".planning/v0.3/WALKTHROUGH.md"
      provides: "5 capture surfaces fully documented"
      min_severity_lines: 20  # 5 surfaces × ≥4 (golden + 3 probes) tagged entries
    - path: ".planning/v0.3/walkthrough-screenshots/"
      provides: "Visual evidence for capture probes (≥1 per surface where useful)"
      min_files: 5
  key_links:
    - from: ".planning/v0.3/WALKTHROUGH.md §Capture — URL"
      to: "URL-01 backlog entry"
      via: "Cross-link footer per D-14 (NOT a new GitHub issue)"
      pattern: "URL-01"
    - from: ".planning/v0.3/WALKTHROUGH.md §Capture — Photo"
      to: "Sheet-01 #1 (if surfaced)"
      via: "Cross-link per D-06 (NOT a new GitHub issue)"
      pattern: "Sheet-01"
---

<objective>
Probe the 5 capture surfaces (Quick / Full / Voice / Photo / URL) against the prod synthetic env via Playwright MCP. For each surface: 1 golden-path traversal + ≥3 deliberate weird-state probes drawn from D-08's 4 probe kinds. Write findings into the corresponding `## Capture — *` section of `WALKTHROUGH.md` incrementally (D-20) using the uniform template (D-04). Cross-link known backlog items (Sheet-01 [#1], URL-01) per D-06/D-14 instead of filing new issues — Plan 05 batch-files genuinely-new blockers.

Purpose: Captures are the architectural front door (invariant #1 — async draft promotion). Every capture probe exercises the draft → structured (or failed) transition; finding-class is biased toward Gemini edges (voice/photo/url) and form validation (quick/full). 4 of the 5 surfaces touch live Gemini (D-12) — record per-section call counts.

Output: 5 fully-populated capture sections in WALKTHROUGH.md, ≥5 screenshots in `walkthrough-screenshots/`, zero edits to product code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md
@.planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md
@.planning/phases/12-exploratory-feature-walkthrough/12-01-SUMMARY.md
@.planning/v0.3/WALKTHROUGH.md
@RUNBOOK.md
@CLAUDE.md
@frontend/playwright.config.ts

<interfaces>
<!-- Key contracts the auditor needs. Extracted from existing e2e specs and CLAUDE invariants. -->

Architecture invariant #1 (capture pipeline): every capture surface returns a draft immediately, promotion runs server-side in a `BackgroundTask`. Probes MUST observe the draft → structured (or failed) transition as a separate step.

Capture surface endpoints (verified via grep frontend/tests/e2e/capture-*.spec.ts):
- POST /api/recipes/quick   — body: { title: string }                 — see capture-quick.spec.ts
- POST /api/recipes         — body: full payload (title, ingredients, etc.) — see capture-full.spec.ts
- POST /api/recipes/voice   — body: { transcript: string } (JSON, NOT audio multipart) — see capture-voice.spec.ts:14-23
- POST /api/recipes/photo   — body: multipart/form-data, field name `files` (NOT `photos`) — see capture-photo.spec.ts:43
- POST /api/recipes/url     — body: { url: string }                   — see capture-url.spec.ts

Locked French strings (next-intl, invariant #6) — used as accessible-name selectors in MCP:
- Tab labels: "Quick" / "Complète" / "Voix" / "Photo" / "URL"
- Photo source picker (bottom sheet): "Caméra" / "Photothèque"
- Add photo CTA: "Ajouter une photo"
- Draft awaiting analysis: "Brouillon en attente d'analyse"
- Vote labels (out of scope here, used in Plan 03): "Validé" / "Pressenti" / "Contesté" / "Rejeté" / "Sans avis"

Known backlog items the auditor MUST cross-link instead of refiling (D-06):
- Sheet-01 (#1, https://github.com/lucaguery/al-dente/issues/1): bottom sheet renders off-screen on iPhone viewport; `paper-grain` class overrides `fixed`. Surfaces during capture-photo "Ajouter une photo" → bottom sheet probe.
- URL-01: `recipes.py:481-490` URL extraction is `# TODO(productize)`. Drafts from URL never promote. Surfaces during capture-url golden-path probe.
</interfaces>
</context>

<scope_constraint>
**HARD CONSTRAINT — NO product code changes.**

Permitted writes (this plan):
- `.planning/v0.3/WALKTHROUGH.md` (edit existing skeleton — fill in §Capture — Quick / Full / Voice / Photo / URL)
- `.planning/v0.3/walkthrough-screenshots/*.png` (create)

Permitted MCP browser actions:
- `mcp__playwright__*` against the prod URL (read live: Vercel dashboard or operator confirmation; the runbook does NOT pin the URL — surface this as the FIRST step of Task 1 if not in the SUMMARY from Plan 01).
- All probes are member #3 scoped (auditor's persistent session). Do NOT log out.

Forbidden:
- ANY edit under `frontend/` or `backend/`.
- New `.spec.ts` files (per CONTEXT §domain).
- Filing GitHub issues (Plan 05 batches all of them).
- Logging out of member #3 mid-walkthrough (per CONTEXT §specifics).
- Running `uv run seed --prod-synthetic --teardown` unless a probe is genuinely blocked (D-09); document the reset inline in WALKTHROUGH.md if invoked.

If a probe surfaces a real product bug, RECORD IT and STOP that probe (per `feedback_executor_scope_creep`). Move on to the next probe.
</scope_constraint>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Onboard auditor (member #3) and probe Capture — Quick + Capture — Full surfaces</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-01..D-20 — full decision set)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Per-Surface Probe Playbook" Surfaces 1-2 (capture-quick, capture-full) and §"Tooling: Playwright MCP Surface Area"
    - frontend/tests/e2e/capture-quick.spec.ts (golden-path reference: title submit, draft round-trip)
    - frontend/tests/e2e/capture-full.spec.ts (full payload golden path)
    - frontend/tests/e2e/invite-code-happy-path.spec.ts (the join flow as member #3 — exact selectors and French strings)
    - .planning/v0.3/WALKTHROUGH.md (current skeleton — locate the `## Capture — Quick` and `## Capture — Full` headers)
  </read_first>
  <files>
    .planning/v0.3/WALKTHROUGH.md,
    .planning/v0.3/walkthrough-screenshots/capture-quick-empty-title.png,
    .planning/v0.3/walkthrough-screenshots/capture-quick-5kb-title.png,
    .planning/v0.3/walkthrough-screenshots/capture-quick-double-tap.png,
    .planning/v0.3/walkthrough-screenshots/capture-full-missing-required.png,
    .planning/v0.3/walkthrough-screenshots/capture-full-200-ingredients.png,
    .planning/v0.3/walkthrough-screenshots/capture-full-prefilled-bad-json.png
  </files>
  <action>
**Step 0 — Resolve prod URL.** RUNBOOK.md doesn't pin the public URL (RESEARCH §Open Questions #1). Resolve in this order:
  a. Check `.planning/PROJECT.md` for any `https://` URL.
  b. Check `frontend/vercel.json` or `frontend/next.config.ts` for any base URL hint.
  c. Run `gh api repos/lucaguery/al-dente --jq '.homepage'` — if non-empty, use that.
  d. If still ambiguous, surface a `checkpoint:human-action` to the user asking for the prod URL. Otherwise the rest of this plan cannot proceed.

  Cache the resolved URL as `PROD_URL` in your session and at the top of WALKTHROUGH.md preamble (replace placeholder `Target environment` line).

**Step 1 — Onboard as member #3.**
  - `mcp__playwright__browser_resize` width=390 height=844 (mirror playwright.config.ts:87 viewport).
  - `mcp__playwright__browser_navigate` to `${PROD_URL}/onboarding/welcome`.
  - Click "Rejoindre un foyer existant" (verified via `invite-code-happy-path.spec.ts`).
  - Type `DEMO01` into the invite code input.
  - Pick a name like "Auditor" and a free color.
  - After landing on `/`, capture an initial DOM snapshot via `mcp__playwright__browser_snapshot`. Verify the synthetic household is loaded (look for any of the 21 seeded recipe titles like "Risotto aux champignons", "Coq au vin", "Tacos de bœuf").
  - Record in WALKTHROUGH.md preamble: `Session date: <YYYY-MM-DD>`, `Auditor: member #3 (Auditor) joined via DEMO01 at <HH:MM>`.

**Step 2 — Probe Capture — Quick (3 probes minimum, drawing from D-08).**
  Update WALKTHROUGH §Capture — Quick:
  - **Starting state:** "post-Phase-11 baseline (21 recipes, 7 votes, 3 cooking_logs); auditor is member #3."
  - **Golden path:** Navigate to `${PROD_URL}/recipes/new`. Click the "Quick" tab. Type a clean title like "Tarte aux poireaux". Submit. Document the immediate draft return (per invariant #1) and the WS-driven promotion to `structured` in the inbox.

  Then run ≥3 weird-state probes:
  1. **(Garbage)** Empty title submit. Selector: the submit button (likely `button[type=submit]` or a button with French label like "Créer" / "Valider"). Observe: error state, silent acceptance, or 422? `mcp__playwright__browser_take_screenshot(filename: "capture-quick-empty-title.png")`. Move screenshot to `.planning/v0.3/walkthrough-screenshots/`.
  2. **(Boundary)** Title = ~5KB random French text + emoji 🍝🍝🍝 + diacritics éèàâç. Use `mcp__playwright__browser_type` with a long generated string. Does the draft promote? Does the inbox card render the over-long title without breaking layout? Screenshot `capture-quick-5kb-title.png`.
  3. **(Racing)** Type a clean title, then `mcp__playwright__browser_click` on the submit button TWICE rapidly (no wait between calls). Observe the network log via `mcp__playwright__browser_network_requests` — were two `POST /api/recipes/quick` requests fired? Are two drafts present in the inbox? Idempotency token? Screenshot `capture-quick-double-tap.png`.

  For each probe, document in WALKTHROUGH using the uniform template (D-04). Severity per D-01/D-02:
  - Crash / 500 / data corruption / non-functional primary action even via workaround → blocker
  - User loses time/attention/confidence → friction
  - Visual or copy polish → nit

  **Gemini calls in this section: 0** (Quick is non-AI per RESEARCH §Surface 1).

**Step 3 — Probe Capture — Full (3 probes minimum).**
  Navigate to `${PROD_URL}/recipes/new`, click "Complète" tab.
  - **Starting state:** "after Capture — Quick probes; inbox now contains <N> additional drafts depending on probe outcomes."
  - **Golden path:** Fill all required fields (title, optional cuisine/mood/season selectors, ingredients) with a coherent French recipe. Submit. Observe immediate draft + structured promotion.

  ≥3 weird-state probes:
  1. **(Garbage / boundary)** Skip required fields → submit. Or set Cuisine to a locked-vocab value (e.g. `italian`) but fill free-text fields with garbage like `​​   ` (zero-width + whitespace). Observe validation. Screenshot `capture-full-missing-required.png`.
  2. **(Boundary)** Ingredients list = 200 lines (use `mcp__playwright__browser_evaluate` to populate via `document.querySelector('textarea[name=ingredients]').value = ...; ...dispatchEvent(...)` if the form has a textarea, OR copy-paste a long block via `browser_type`). Submit. Renders? Submits? Promoted recipe preserves all 200? Screenshot `capture-full-200-ingredients.png`.
  3. **(Invalid arrival)** Navigate via `mcp__playwright__browser_navigate` to `${PROD_URL}/recipes/new?tab=full&prefilled=<bad-json>` (or whatever query-string parsing the page does). Does the form crash, ignore, or sanitize? Screenshot `capture-full-prefilled-bad-json.png`.

  Document per D-04. **Gemini calls: 0** (Full capture is non-AI per RESEARCH §Surface 2).

**Step 4 — Verify scope-creep guard.**
  After both surfaces are documented, run:
  ```bash
  git diff --name-only HEAD | grep -E "^(frontend|backend)/"
  ```
  MUST return empty. If non-empty, ABORT and revert.

**Note on screenshots:** `mcp__playwright__browser_take_screenshot` writes to a default location; immediately move/copy each screenshot to `.planning/v0.3/walkthrough-screenshots/<filename>.png` so it's tracked in the artifact allowlist.
  </action>
  <acceptance_criteria>
    - PROD_URL recorded in WALKTHROUGH.md preamble (no `<...>` placeholders remain in the Target environment line).
    - WALKTHROUGH.md §Capture — Quick contains the strings `Starting state:` and `Golden path:` and `Probes:` (or section structure equivalent), plus exactly ≥3 entries each with `**Severity:**` line.
    - WALKTHROUGH.md §Capture — Full same as above (≥3 severity-tagged entries).
    - At least 5 of the 6 expected screenshot files exist under `.planning/v0.3/walkthrough-screenshots/` (1 per probe; 6th is forgiveable if a probe genuinely had nothing visual to capture, but document why).
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
    - The Gemini call count line appears in both sections (`Gemini calls in this section: 0`).
  </acceptance_criteria>
  <verify>
    <automated>QUICK_SEV=$(awk '/^## Capture — Quick$/,/^## Capture — Full$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); FULL_SEV=$(awk '/^## Capture — Full$/,/^## Capture — Voice$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); SHOTS=$(ls .planning/v0.3/walkthrough-screenshots/capture-quick-*.png .planning/v0.3/walkthrough-screenshots/capture-full-*.png 2>/dev/null | wc -l | tr -d ' '); [ "$QUICK_SEV" -ge 3 ] && [ "$FULL_SEV" -ge 3 ] && [ "$SHOTS" -ge 5 ] && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>Auditor onboarded as member #3, Capture — Quick + Capture — Full each have golden-path note + ≥3 severity-tagged probe entries, screenshots committed, no product-code drift.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Probe Capture — Voice + Capture — Photo surfaces (live Gemini, D-12)</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-12 live AI coverage; D-13 canned inputs)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Per-Surface Probe Playbook" Surfaces 3-4 (capture-voice, capture-photo)
    - frontend/tests/e2e/capture-voice.spec.ts (voice surface accepts JSON `{transcript: "..."}`, NOT audio)
    - frontend/tests/e2e/capture-photo.spec.ts (multipart `files` field; Sheet-01 `test.fixme` line confirms #1)
    - .planning/v0.3/walkthrough-inputs/voice/01-clean-french.txt, 02-garbled-accent.txt, 03-very-short.txt
    - .planning/v0.3/walkthrough-inputs/photo/README.md (manifest; live photos may need ad-hoc capture if not pre-provided)
    - .planning/v0.3/WALKTHROUGH.md (locate `## Capture — Voice` and `## Capture — Photo` headers)
  </read_first>
  <files>
    .planning/v0.3/WALKTHROUGH.md,
    .planning/v0.3/walkthrough-screenshots/capture-voice-clean-french.png,
    .planning/v0.3/walkthrough-screenshots/capture-voice-garbled.png,
    .planning/v0.3/walkthrough-screenshots/capture-voice-very-short.png,
    .planning/v0.3/walkthrough-screenshots/capture-photo-bottom-sheet.png,
    .planning/v0.3/walkthrough-screenshots/capture-photo-non-recipe.png,
    .planning/v0.3/walkthrough-screenshots/capture-photo-dim-handwritten.png
  </files>
  <action>
**Step 1 — Capture — Voice (3+ probes; live Gemini per D-12).**

  - **Confirm surface contract first.** Per RESEARCH §Open Questions #3, the prod /api/recipes/voice endpoint accepts JSON `{transcript: "..."}` (per `capture-voice.spec.ts:14-23`). Confirm by clicking the "Voix" tab and observing the UI: if there's a textarea (transcript path) → use canned `.txt` transcripts. If there's a file picker (audio path) → operator must produce `.m4a` clips, OR the auditor inlines the textarea path via the equivalent flow if both are supported. Document the observed shape in WALKTHROUGH preamble of §Capture — Voice.

  - **Starting state:** "after Capture — Quick + Full probes; inbox state documented inline."
  - **Golden path:** Navigate to `/recipes/new`, click "Voix" tab, paste content of `.planning/v0.3/walkthrough-inputs/voice/01-clean-french.txt` (the risotto transcript). Submit. Observe immediate draft return + WS-driven promotion to `structured`. Document the resulting recipe shape (title, ingredient extraction quality). Screenshot the inbox card with the structured badge: `capture-voice-clean-french.png`.

  ≥3 weird-state probes:
  1. **(Garbage)** Submit transcript with 0 recognizable recipe content (e.g. `the cat sat on the mat in French`). Does Gemini still produce a structured recipe? What does it look like? Document the exact promoted shape. Screenshot `capture-voice-garbled.png`.
  2. **(Boundary)** Submit `.planning/v0.3/walkthrough-inputs/voice/03-very-short.txt` content ("Pâtes au beurre."). Does promotion succeed with sparse data? Or fail gracefully? Screenshot `capture-voice-very-short.png`.
  3. **(Racing)** Submit `02-garbled-accent.txt` and immediately (within ~200ms) navigate to `/` with `mcp__playwright__browser_navigate`. Did the BackgroundTask still complete? Did the inbox eventually receive the promoted draft? `mcp__playwright__browser_network_requests` to inspect the POST + WS frame.

  **Gemini calls in this section: ~3-4** (one per submit). Record exact count in the WALKTHROUGH section.

**Step 2 — Capture — Photo (3+ probes; live Gemini; expect Sheet-01 surfacing).**

  - **Starting state:** "after Capture — Voice probes; inbox state documented inline."
  - **Golden path:** Navigate to `/recipes/new`, click "Photo" tab, click "Ajouter une photo".

    **Expected surfacing of Sheet-01 [#1] HERE.** Per CONTEXT D-06 and RESEARCH §Surface 4, the bottom sheet (`Caméra` / `Photothèque`) renders OFF-SCREEN on iPhone-shape viewport (top: 702px) due to `paper-grain` class overriding `position: fixed`. Document this with screenshot `capture-photo-bottom-sheet.png`. Severity = `blocker` per D-01 (primary action non-functional even via workaround). **Cross-link to Sheet-01 [#1] per D-06 — DO NOT file a new GitHub issue.** The WALKTHROUGH entry's `Issue:` line MUST contain `https://github.com/lucaguery/al-dente/issues/1`.

    If Sheet-01 has been fixed by the time this runs (check first by looking at the actual sheet position via DOM snapshot — if `Caméra`/`Photothèque` buttons are in viewport, the bug is fixed), document as a friction or PASS finding instead.

  ≥3 weird-state probes (assuming Sheet-01 doesn't completely block all photo upload — if it does, document workaround attempt and mark all 3 as blocked):
  1. **(Garbage / negative test)** If a photo can be uploaded via workaround OR if photos exist in `.planning/v0.3/walkthrough-inputs/photo/`, upload `03-non-recipe-landscape.jpg` (a beach photo). Does Gemini hallucinate a "recipe"? Refuse? Crash? Screenshot `capture-photo-non-recipe.png`.
  2. **(Boundary / OCR edge)** Upload `02-dim-handwritten.jpg` if available. OCR succeeds? Title is sensible? Screenshot `capture-photo-dim-handwritten.png`.
  3. **(Racing or invalid state)** If photos can't be uploaded due to Sheet-01, document Sheet-01 itself as the primary blocker finding AND substitute a 3rd probe as: navigate to `/recipes/new?tab=photo&files=<garbage>` to see how the route handles bad query strings.

  **NOTE on photo inputs:** If `.planning/v0.3/walkthrough-inputs/photo/01-clean-cookbook.jpg` etc. don't exist (Plan 01 only committed the README manifest), document this in the section preamble: "Canned photos not yet committed — used live screen capture / iPhone gallery / skipped Probe N." This is acceptable per CONTEXT §Claude's Discretion — operator may add JPGs later as a follow-up.

  **Gemini calls in this section: ~2-3** (per successful upload). Record exact count.

**Step 3 — Verify scope-creep guard.**
  ```bash
  git diff --name-only HEAD | grep -E "^(frontend|backend)/"
  ```
  MUST return empty.
  </action>
  <acceptance_criteria>
    - WALKTHROUGH.md §Capture — Voice has Golden path note + ≥3 entries each with `**Severity:**` line + `Gemini calls in this section:` line.
    - WALKTHROUGH.md §Capture — Photo has Golden path note + ≥3 entries each with `**Severity:**` line + `Gemini calls in this section:` line.
    - If Sheet-01 surfaced: WALKTHROUGH §Capture — Photo contains `Sheet-01` AND `https://github.com/lucaguery/al-dente/issues/1`.
    - At least 5 of 6 expected screenshot files exist under `.planning/v0.3/walkthrough-screenshots/` (1 per probe).
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>VOICE_SEV=$(awk '/^## Capture — Voice$/,/^## Capture — Photo$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); PHOTO_SEV=$(awk '/^## Capture — Photo$/,/^## Capture — URL$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); GEMINI=$(awk '/^## Capture — Voice$/,/^## Capture — URL$/' .planning/v0.3/WALKTHROUGH.md | grep -c 'Gemini calls in this section'); SHOTS=$(ls .planning/v0.3/walkthrough-screenshots/capture-voice-*.png .planning/v0.3/walkthrough-screenshots/capture-photo-*.png 2>/dev/null | wc -l | tr -d ' '); [ "$VOICE_SEV" -ge 3 ] && [ "$PHOTO_SEV" -ge 3 ] && [ "$GEMINI" -ge 2 ] && [ "$SHOTS" -ge 5 ] && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>Capture — Voice + Capture — Photo each have ≥3 severity-tagged probes + Gemini call count, Sheet-01 cross-linked if surfaced, screenshots committed, no product-code drift.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Probe Capture — URL surface (live Gemini; expect URL-01 surfacing per D-14)</name>
  <read_first>
    - .planning/phases/12-exploratory-feature-walkthrough/12-CONTEXT.md (D-14 URL-01 cross-link rule)
    - .planning/phases/12-exploratory-feature-walkthrough/12-RESEARCH.md §"Per-Surface Probe Playbook" Surface 5 (capture-url)
    - frontend/tests/e2e/capture-url.spec.ts (golden URL surface; URL-01 fixme present)
    - .planning/v0.3/walkthrough-inputs/url/urls.md (the 3 canned URLs)
    - .planning/v0.3/WALKTHROUGH.md (locate `## Capture — URL` header)
  </read_first>
  <files>
    .planning/v0.3/WALKTHROUGH.md,
    .planning/v0.3/walkthrough-screenshots/capture-url-marmiton.png,
    .planning/v0.3/walkthrough-screenshots/capture-url-not-a-url.png,
    .planning/v0.3/walkthrough-screenshots/capture-url-slow-site.png
  </files>
  <action>
**URL surface — 3+ probes, expect the golden path itself to surface URL-01 per D-14.**

  - **Starting state:** "after Capture — Photo probes; inbox state documented inline."
  - **Golden-path-then-blocker:** Navigate to `/recipes/new`, click "URL" tab. Paste the marmiton.org URL from `.planning/v0.3/walkthrough-inputs/url/urls.md` line "01 — Clean recipe site". Submit. Observe:
    - The draft returns immediately (per invariant #1) — that part works.
    - The promotion never completes — draft stays at `status='draft'` indefinitely. **This is URL-01**: `recipes.py:481-490` is `# TODO(productize)`, URL extraction is stubbed.

    Per D-14, document this finding as `blocker` severity in WALKTHROUGH §Capture — URL, BUT cross-link to the existing URL-01 backlog notation:
    - Issue: cross-linked to backlog. URL-01 (recipes.py:481-490 — `# TODO(productize)`).
    - DO NOT file a new GitHub issue.

    Screenshot the stuck draft card: `capture-url-marmiton.png`.

  ≥3 total weird-state probes (the golden-path-as-blocker counts as ONE of them; need 2 more):

  1. **(Garbage)** Submit `not-a-url-just-text` as URL value. Does the backend return 422? Frontend show validation? Or silent failure? Screenshot `capture-url-not-a-url.png`. Severity per D-01/D-02.
  2. **(Network)** Submit a deliberately-slow URL. The url list lists wikipedia and a paywall; for the slow probe, use `https://httpbin.org/delay/30` directly (or document why this probe was skipped — e.g. if the URL-01 stub means the slow fetch never even kicks off, the probe trivially "passes" because the BackgroundTask short-circuits). Screenshot `capture-url-slow-site.png` (whatever state is rendered).
  3. **(Boundary)** Submit the wikipedia URL from `urls.md`. Even if URL-01 stops promotion, what does the draft card render? Title from URL? Default title? Document.

  **Gemini calls in this section:** likely 0-1 (URL-01 short-circuits before Gemini per `recipes.py:481-490`). Record observed count and explain.

**Verify scope-creep guard:**
  ```bash
  git diff --name-only HEAD | grep -E "^(frontend|backend)/"
  ```
  MUST return empty.

**Final touch — incremental commit point.** Per D-20 (incremental writing), this is a natural commit boundary: 5 capture surfaces fully documented, ready for Plan 03 to pick up the decide-cook-history surfaces. The orchestrator's git commit step packages the WALKTHROUGH.md update + screenshot additions.
  </action>
  <acceptance_criteria>
    - WALKTHROUGH.md §Capture — URL has Golden path note + ≥3 entries each with `**Severity:**` line + `Gemini calls in this section:` line.
    - WALKTHROUGH.md §Capture — URL contains the string `URL-01` (cross-link per D-14).
    - WALKTHROUGH.md §Capture — URL does NOT contain a github.com/lucaguery/al-dente/issues URL pointing to a new (non-#1) issue (D-14: do NOT file new for URL-01).
    - At least 2 of 3 expected screenshot files exist under `.planning/v0.3/walkthrough-screenshots/`.
    - `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>URL_SEV=$(awk '/^## Capture — URL$/,/^## Shortlist$/' .planning/v0.3/WALKTHROUGH.md | grep -c '^\*\*Severity:\*\*'); URL_LINK=$(awk '/^## Capture — URL$/,/^## Shortlist$/' .planning/v0.3/WALKTHROUGH.md | grep -c 'URL-01'); GEMINI=$(awk '/^## Capture — URL$/,/^## Shortlist$/' .planning/v0.3/WALKTHROUGH.md | grep -c 'Gemini calls in this section'); SHOTS=$(ls .planning/v0.3/walkthrough-screenshots/capture-url-*.png 2>/dev/null | wc -l | tr -d ' '); [ "$URL_SEV" -ge 3 ] && [ "$URL_LINK" -ge 1 ] && [ "$GEMINI" -ge 1 ] && [ "$SHOTS" -ge 2 ] && [ -z "$(git diff --name-only HEAD | grep -E '^(frontend|backend)/')" ] && echo OK</automated>
  </verify>
  <done>Capture — URL has ≥3 severity-tagged probes + Gemini call count + URL-01 cross-link, screenshots committed, no product-code drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor browser → prod backend | Probes fire real POSTs against prod /api/recipes/* — every successful capture writes to the synthetic household scope only. |
| canned inputs → Gemini | The transcripts and (if present) photos pass through Gemini 2.5 Flash unredacted; nothing private should be in the inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02 | Information Disclosure / Privilege Escalation | Member-#3 session scope | mitigate | Auditor stays scoped to `[SYNTHETIC] Démo Al Dente` for the entire plan. NEVER click "Quitter le foyer" or log-out controls; per CONTEXT §specifics "Don't log out". If session unexpectedly drops to a real-user identity, ABORT and surface to user. |
| T-03 | Information Disclosure | walkthrough-inputs/url/urls.md | mitigate | Plan 01 Task 2 step 8 already grep-verified no auth-bearing query strings. Re-check before each URL probe: confirm no `?api_key=` etc. is being submitted. |
</threat_model>

<verification>
**Plan-level verification:**
1. **Coverage:** `awk '/^## Capture/{print}' WALKTHROUGH.md` returns 5 lines (Quick / Full / Voice / Photo / URL).
2. **Probe density:** Each capture section has ≥3 severity-tagged entries (counted via `grep -c '^\*\*Severity:\*\*'` per section).
3. **Gemini accounting:** Voice + Photo + URL each have a `Gemini calls in this section:` line.
4. **Backlog cross-link:** §Capture — URL contains `URL-01`. §Capture — Photo contains `Sheet-01` if the bug is still present.
5. **Visual evidence:** `ls .planning/v0.3/walkthrough-screenshots/capture-*.png` returns ≥10 files.
6. **Scope-creep guard:** `git diff --name-only HEAD | grep -E "^(frontend|backend)/"` returns empty.

Validation Architecture: explicitly N/A. Verification is markdown-shape + filesystem checks executed inline.
</verification>

<success_criteria>
- WALK-01 partial: 5 of 13 surfaces probed (capture set complete).
- WALK-02 partial: every capture section includes ≥3 deliberate weird-state probes drawn from D-08 categories.
- WALK-03 partial: every probe entry uses the uniform finding template with severity tag.
- Zero diff under `frontend/` or `backend/`.
- Gemini call counts recorded for voice/photo/url; expect ≤10 calls combined per D-12 budget.
</success_criteria>

<output>
After completion, create `.planning/phases/12-exploratory-feature-walkthrough/12-02-SUMMARY.md` with:
- Resolved PROD_URL.
- Per-surface probe count and severity breakdown (e.g. "Capture — Quick: 3 probes, 1 friction, 2 nit").
- Total Gemini calls across the 5 surfaces.
- List of any new blocker findings (these go to Plan 05 for filing) — by title, with WALKTHROUGH anchor reference.
- List of backlog cross-links made (Sheet-01? URL-01?).
- Confirmation that the artifact allowlist held.
- Pointer to Plan 03: "Decide-cook-history probe pass — shortlist / vote / cooking log / history (4 surfaces)."
</output>
