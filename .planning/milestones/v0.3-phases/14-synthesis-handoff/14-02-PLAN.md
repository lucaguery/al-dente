---
phase: 14
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - .planning/v0.3/ASSESSMENT.md
autonomous: true
requirements:
  - SYNTH-01
  - SYNTH-02
  - SYNTH-03
must_haves:
  truths:
    - "ASSESSMENT.md Section 1 (Executive summary) contains 2-3 paragraphs summarizing the milestone-level finding (axis-(ii)-drives-Tier-1) plus the verdict-correlation observation (N-5)"
    - "ASSESSMENT.md Section 1 contains a 'How to read this document' subsection that front-loads the descriptive-not-prescriptive boundary"
    - "ASSESSMENT.md Section 1 contains a 'Limits of this assessment' subsection naming D-16 partial-reach + iPhone-Chromium scope + N-4 prod-data anomalies + T-6 P-12-Pu-01 audit-blocker"
    - "ASSESSMENT.md Section 2 (Ranking method) exposes the D-03 3-axis rubric: axis (i) / (ii) / (iii) definitions, 0-2 scale, tier mapping >=4/2-3/0-1, tie-breaker rule"
    - "ASSESSMENT.md Section 4 contains exactly three subsections per D-10: '(a)' Source artifacts, '(b)' Open framing questions, '(c)' Explicit non-prescriptions"
    - "Section 4(b) contains 3-5 framing questions all phrased as inquiries (How / Should / What), zero phrased as constraints"
    - "Section 4(c) contains the 5 explicit non-prescription bullets locked in CONTEXT D-10(c) (the floor — additions permitted)"
    - "Pre-commit grep gate (the second pass per CONTEXT specifics) exits 0 against the final ASSESSMENT.md"
    - "Git commit lands with ASSESSMENT.md + check-assessment.sh tracked in .planning/v0.3/"
  artifacts:
    - path: ".planning/v0.3/ASSESSMENT.md"
      provides: "Complete synthesis document (Sections 1-4) — composed against the Plan 1 entry set"
      min_lines: 600
      contains: "## Inputs to next /gsd-new-milestone cycle"
  key_links:
    - from: ".planning/v0.3/ASSESSMENT.md Section 4(a)"
      to: ".planning/v0.3/WALKTHROUGH.md, UI-AUDIT.md, ui-reviews/, walkthrough-screenshots/"
      via: "artifact path enumeration"
      pattern: "\\.planning/v0\\.3/"
    - from: ".planning/v0.3/ASSESSMENT.md Section 4(a)"
      to: "lucaguery/al-dente issues #1-#8"
      via: "Issue # enumeration"
      pattern: "Issue #[1-8]"
    - from: ".planning/v0.3/ASSESSMENT.md Section 4(b) framing questions"
      to: "Section 3 ranked findings"
      via: "in-paragraph backref to entry IDs / tiers"
      pattern: "Tier [123]"
    - from: ".planning/v0.3/check-assessment.sh"
      to: ".planning/v0.3/ASSESSMENT.md"
      via: "pre-commit invocation; exit 0 required"
      pattern: "phase \\(1\\[5-9\\]\\|\\[2-9\\]\\[0-9\\]\\)"
---

<objective>
Compose the framing prose around Plan 1's ranked-findings core: Section 1 (Executive summary), Section 2 (Ranking method), Section 4 (Inputs to next /gsd-new-milestone cycle). Run the D-07 pre-commit grep gate as the second of the two-pass enforcement pattern. Commit the complete `.planning/v0.3/ASSESSMENT.md` and `.planning/v0.3/check-assessment.sh` to `main`.

Purpose: Plan 1 produced 27 ranked entries with locked scores and tier assignments. Plan 2 wraps them in the framing layer that makes the document coherent for a downstream reader: a milestone-level executive summary, an exposed rubric (D-04), and a handoff section that names inputs without dictating outputs (SYNTH-03 + D-10).

Output: Final `.planning/v0.3/ASSESSMENT.md` complete in all 4 sections; grep gate passes; commit lands on `main`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-synthesis-handoff/14-CONTEXT.md
@.planning/phases/14-synthesis-handoff/14-RESEARCH.md
@.planning/phases/14-synthesis-handoff/14-01-SUMMARY.md
@.planning/v0.3/ASSESSMENT.md
@.planning/v0.3/UI-AUDIT.md
@CLAUDE.md

<framing_question_set>
Per CONTEXT D-10(b) — 3-5 inquiries, NOT constraints. Verbatim guidance from RESEARCH.md "Inputs to Next /gsd-new-milestone Cycle — Draft Section Structure" subsection (b). The executor uses these EXACTLY (or refines wording while preserving inquiry-form). All 5 are mandatory floor.

Note on phrasing: the original RESEARCH draft uses the literal token "v0.4" in the question text. The grep gate D-08 blocks "v0.4". The executor MUST rewrite the questions using "the next milestone" or "the next /gsd-new-milestone cycle" instead. Example: "How does v0.4 weigh remediation depth..." -> "How does the next milestone weigh remediation depth...". The parenthetical citations are preserved.

1. How does the next milestone weigh remediation depth vs. new capability development? (cites the impact distribution: 2 Tier-1 architecture-invariant violations + 8 Tier-2 entries spanning capacity, capture-pipeline, history-feature, and validation surfaces — without ordering them against new-capability work.)

2. Are architecture-invariant violations (axis ii >= 1) bundled into a single coordinated phase, or surfaced into independent per-invariant phases? (cites the cluster taxonomy + 5-surface invariant-violation footprint per UI-AUDIT.md cross-cutting bullet 9 — without dictating bundle shape.)

3. How does the next milestone weigh closing structural-correctness gaps (axis ii) vs. closing identity-signature gaps (axis i)? (cites the axis distribution: Tier 1 axis-ii-heavy; Tier 2 axis-i-heavy on token-completeness clusters — without ordering the axes.)

4. Are cluster entries with cross-surface footprint handled per-cluster or per-surface? (cites C-1 emerald-literal across 5 surfaces, C-2 no-debounce across 4 surfaces, C-4 capture-pipeline across 3 surfaces — without dictating phase shape.)

5. What does the audit corpus's iPhone-shape-Chromium-only scope imply for the next audit budget? (cites the "Limits of this assessment" — without proposing a cross-browser audit phase.)

VOICE-DISCIPLINE TEST: each question must avoid the D-08 blocked verb sequences. Specifically, "should (fix|add|build|implement|do|consider|address|tackle|prioritize)" — note that "address" and "tackle" are in the blocked list. Question 2's original RESEARCH wording "Should architecture-invariant violations be addressed coordinated or independently?" matches "should ... addressed" via the "should address" alternation. The rewritten phrasing above ("Are architecture-invariant violations ... bundled or surfaced") avoids the blocked verb sequence and the imperative voice. Apply the same lens to every question.
</framing_question_set>

<non_prescription_set>
Per CONTEXT D-10(c) — the 5 bullets below are the FLOOR. Additions permitted but each must use "ASSESSMENT.md does not X" present-tense observational frame. Verbatim from CONTEXT D-10(c).

Note on phrasing: CONTEXT D-10(c) bullet 3 reads "ASSESSMENT.md does not propose phase shapes, plan shapes, or v0.4 requirements." The literal "v0.4" substring would be caught by the grep gate. Rewrite as "future-milestone requirements" or "next-milestone requirements" to preserve meaning while passing the gate. Bullet 4 similarly uses "v0.4 implementation choices" — rewrite as "future implementation choices".

- ASSESSMENT.md does not order remediation.
- ASSESSMENT.md does not estimate effort.
- ASSESSMENT.md does not propose phase shapes, plan shapes, or future-milestone requirements.
- ASSESSMENT.md does not assert that addressing a Tier 1 finding will flip a surface verdict from warning to ok — verdict shifts depend on multi-finding bundles and future implementation choices.
- Tier ordering reflects impact on "feels Al Dente", not implementation priority — these are not the same axis.

VOICE-DISCIPLINE TEST: "ASSESSMENT.md does not propose phase shapes" passes (descriptive scope statement about THIS document; "propose" appears but the gate blocks "propose" only as a verb of action toward future work — describing what THIS doc does NOT do is the inverse). The phrase "does not propose" is a structural negation, not a verb of action toward future work. Cross-reference: CONTEXT D-08's recommend/propose/suggest entry says "as verbs of action toward future work" — the negated form is a permitted exception per the table footnote. However the grep gate is regex-based and CANNOT distinguish negated-vs-affirmative use. The bare token "propose" matches. The gate WILL flag this line. The executor decides: (a) reword "does not propose" as "does not name" or "does not author"; or (b) accept the gate flag and exempt this specific line via a comment. RECOMMEND option (a): "ASSESSMENT.md does not name future phase shapes, plan shapes, or future-milestone requirements." This avoids the literal "propose" token entirely.
</non_prescription_set>

<exec_summary_seed_observations>
The 3 milestone-level observations the Executive Summary frames. Plan 1 SUMMARY.md confirms the underlying entry set.

1. Axis-(ii)-drives-Tier-1 observation: Tier 1 contains 2 entries (B-3 MEMBER_COUNT=2 hardcoded; B-4 cook_count idempotency). Both score axis (ii):2 — architecture-invariant violation visible to user. Identity-signature impact (axis i) concentrates at Tier 2 with token-completeness clusters (C-1) and capacity-ceiling-with-affordance gap (B-6). The milestone-level finding: the v0.3 audit surfaces exactly 2 user-visible architecture-invariant violations as the load-bearing impact entries, while the design system at the rendering layer (verdict distribution 5 ok / 9 warning / 0 fail) is robust.

2. Verdict-correlation observation (N-5): Per UI-AUDIT.md cross-cutting bullets 7-8: ok-verdicts correlate with editorial discipline + identity-signature presence, NOT absence of bugs. 5 of 5 ok-surfaces ALSO have Pillar 6 score <= 2/4. Verdict-shifts in future audits depend on multi-finding bundles, not single-blocker resolution.

3. Mean and distribution: 14 surfaces; mean 6-pillar score 20.21/24 (vs v0.2 anchor 22.4/24); verdict distribution 5 ok / 9 warning / 0 fail; 27 ranked findings (Tier 1: 2; Tier 2: 8; Tier 3: 17). Phase 13 D-13's WALKTHROUGH-evidence-pulls-scores-down docking discipline drove the 22.4 -> 20.21 delta — Pillar 6 (Experience Design) is the corpus-level deficit (0 of 14 surfaces hit 4/4).
</exec_summary_seed_observations>

<limits_of_assessment_items>
Section 1 "Limits of this assessment" subsection content. Inherits Phase 13 D-16 partial-reach annotation.

- iPhone-shape-Chromium-only viewport. The corpus exercises 393 by 852 viewport (matching v0.2.1 Phase 10 Playwright config). Cross-browser quirks (Safari iOS, Chrome Android) and tablet/desktop layouts are out of scope per REQUIREMENTS.md Out of Scope.

- Couple-scale (4-member) synthetic household. N greater than 5 capacity behavior is not exercised; Issue #7 capacity ceiling stands at N=5; behavior beyond N=5 is not extrapolated.

- D-16 partial-reach surfaces (push, history). Phase 13 marked these "Partially reached." Push: OS-rendered notification UI is not auditable as a frontend surface. History: page renders empty for valid data due to CL-01 missing endpoint (B-10). Ranking entries for these surfaces are read with this caveat.

- Audit-environment-only blockers (excluded from ranking per T-6). P-12-Pu-01 (push subscribe blocked under headless Chromium) is an audit-process observation, not a product finding. P-12-Pu-04 stuck-toggle reproduction folds into B-13.

- Audit-corpus prod-data anomalies (per N-4). Coq au vin's cook_count=2 (cited as B-4 evidence); 7+ stuck "(extraction en cours...)" drafts in inbox (cited as C-4 cluster cumulative); Joe's unfinalized active cook on Pad thai tofu; auditor + Joe persist as members 3 and 4 from Phase 12 cookie collisions. These are corpus-state observations, not product findings.

- Phase 14 reads the live state with these anomalies present. No re-probing; no synthetic-env reset between Phase 12, 13, and 14.
</limits_of_assessment_items>

<calibration_notes_items>
Section 1 "Calibration notes" subsection — sourced from UI-AUDIT.md "Calibration notes". Optional inclusion per CONTEXT Claude's Discretion. RECOMMENDED INCLUDE.

- v0.2 anchor: UI-AUDIT.md notes a v0.2 milestone-level mean of 22.4/24 across 5 phases (best 23/24). v0.3 mean is 20.21/24 — a 2.19-point drop driven by Phase 13 D-13's WALKTHROUGH-evidence-pulls-scores-down discipline. The v0.3 mean is NOT a regression in design quality; it reflects a more rigorous evidence-pulling rubric applied to the same surfaces.

- Pillar 6 corpus-level deficit (per UI-AUDIT.md cross-cutting bullet 5): 0 of 14 surfaces score 4/4 on Experience Design. Score distribution: 0/4 (push), 1/4 (capture-photo, capture-url, vote, cooking-log, history, exports), 2/4 (capture-quick, capture-full, capture-voice, shortlist, realtime, onboarding, settings). The gap is the milestone-level finding that whole-phase v0.2 scoring (22.4/24 average) didn't isolate.

- Score band per D-15: Phase 13's calibration anchor was v0.2's 22.4/24. The v0.3 mean of 20.21/24 places the corpus in the "shipped quality, with friction" band — not in the "needs rework" band. This is descriptive context for the ranked findings, not a verdict-equivalent score.
</calibration_notes_items>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose Section 1 (Executive summary) and Section 2 (Ranking method)</name>
  <read_first>
    - .planning/phases/14-synthesis-handoff/14-CONTEXT.md (D-03 rubric, D-04 expose-the-rubric, D-09 layout)
    - .planning/phases/14-synthesis-handoff/14-RESEARCH.md "Tier-Tally Projection" + "Net-New Patterns From WALK + AUDIT Combination" N-5
    - .planning/phases/14-synthesis-handoff/14-01-SUMMARY.md (locked entry set + scoring confirmation)
    - .planning/v0.3/ASSESSMENT.md (the in-progress draft from Plan 1 — Section 3 is fixed)
    - .planning/v0.3/UI-AUDIT.md "Calibration notes" + "Cross-cutting observations" bullets 7-8 (verdict-correlation evidence for N-5)
  </read_first>
  <files>.planning/v0.3/ASSESSMENT.md</files>
  <action>
Replace the placeholder content in Section 1 (Executive summary) and Section 2 (Ranking method) with composed prose. Section 3 is already drafted; do NOT modify entry order, scores, or schema fields.

Section 1 — Executive summary (target ~600-900 words across all subsections):

Compose 2-3 paragraphs per CONTEXT D-09 paragraph 1 framing the 3 seed observations from the exec_summary_seed_observations block above:

- Paragraph 1: Milestone-level corpus framing. State the surface count (14), the verdict distribution (5 ok / 9 warning / 0 fail), the 6-pillar mean (20.21/24), the ranked-findings count (27), and the per-tier split (2 / 8 / 17). Cite Phase 13 D-15's calibration anchor (v0.2 22.4/24).

- Paragraph 2: The axis-(ii)-drives-Tier-1 observation (load-bearing finding). Frame: Tier 1's two entries (B-3 MEMBER_COUNT=2; B-4 cook_count) both score axis (ii):2. Identity-signature impact concentrates at Tier 2 token-completeness clusters. The design system at the rendering layer is robust; the structural correctness gaps concentrate at specific architecture-invariant break sites.

- Paragraph 3: The verdict-correlation observation (N-5). Per UI-AUDIT.md "Cross-cutting observations" bullets 7-8. Phrase observationally: the ok-verdict correlates with editorial discipline and identity-signature presence; it does NOT correlate with absence of Pillar 6 friction. Verdict-shifts in future audits depend on multi-finding bundles, not single-blocker resolution.

Then add three subsections after the 3 paragraphs:

"How to read this document" (~150-200 words). Front-load the descriptive-not-prescriptive boundary:
- This document is descriptive. It surfaces opportunities and tradeoffs.
- This document does not order remediation, name phases, estimate effort, or prioritize implementation.
- Tier ordering reflects impact on "feels Al Dente", NOT implementation priority — these are different axes.
- Each ranked entry exposes its 3-axis impact score inline. A reader can challenge a rank by pointing at a specific axis score.
- Section 4 names the inputs the next /gsd-new-milestone cycle consumes — without dictating outputs.

"Calibration notes" (~150-250 words). Use the 3 items in calibration_notes_items above. Cite UI-AUDIT.md "Calibration notes".

"Limits of this assessment" (~200-300 words). Use the 6 items in limits_of_assessment_items above. The frame: this assessment is honest about what it does NOT cover. Inherits Phase 13's D-16 "Partially reached" annotation; extends to corpus level.

Section 2 — Ranking method (target ~400-500 words):

Compose the rubric-exposure prose per D-04. Sections in order:

- 1-paragraph intro: "Ranked findings are scored on three orthogonal axes, each 0-2, total 0-6. Tier mapping: total 4 or higher -> Tier 1; total 2-3 -> Tier 2; total 0-1 -> Tier 3. Within tier, entries are ordered by total score descending, with axis (i) as tie-breaker, then axis (ii)."

- Axis definitions (use a heading per axis or a definition list):
  - Axis (i) — Identity-signature impact (0-2). Verbatim from D-03 (or paraphrased into observational prose). Include the identity-signature list (Fraunces display, terracotta primary, paper-grain Card chassis, two-layer warm-brown shadows, framer-motion swipe-deck, Slow Food editorial copy register).
  - Axis (ii) — Invariant-violation visible to user (0-2). Verbatim from D-03. Reference CLAUDE.md invariants 1-8.
  - Axis (iii) — Friction at primary tap-path (0-2). Verbatim from D-03. Define primary tap-path per surface (capture submit / vote tap / cooking-log finalize / onboarding share-code copy).

- Reference table (small markdown table with columns Total score, Tier, Read).

- Tie-breaker explanatory note (~1 paragraph): When two entries have identical total scores within a tier, axis (i) takes precedence (identity-signature impact most directly addresses the "feels Al Dente" question), then axis (ii) (correctness), then axis (iii) (friction).

Self-check the gate after Section 1 + 2 are composed. Run bash .planning/v0.3/check-assessment.sh. Expect exit 0. If exit 1, the executor reads stderr matched lines, rewrites the offending phrase observationally, re-runs. The gate is non-bypassable.

Voice discipline reminders specific to this task:
- The Executive Summary has natural gravitational pull toward "and so the next milestone should..." conclusions. RESIST. Per CONTEXT D-06 + Pitfall 1 in RESEARCH.md.
- "How to read this document" is meta-prose ABOUT the doc — describing what it is (descriptive), not what the next milestone should do.
- "Calibration notes" cites Phase 13 D-15. Past-phase citations PERMITTED.
- "Limits of this assessment" describes scope of THIS doc, not constraints on the next milestone.
- The word "should" never appears outside a citation context. The word "must" never appears. The word "recommend" / "propose" / "suggest" never appears in prose-of-action-toward-future-work form.
- The literal substring "v0.4" never appears anywhere. Use "the next milestone" or "future milestone" instead.
  </action>
  <verify>
    <automated>bash -c 'grep -q "^## Executive summary$" .planning/v0.3/ASSESSMENT.md && grep -q "^## Ranking method$" .planning/v0.3/ASSESSMENT.md && grep -q "How to read this document" .planning/v0.3/ASSESSMENT.md && grep -q "Limits of this assessment" .planning/v0.3/ASSESSMENT.md && grep -q "Axis (i)" .planning/v0.3/ASSESSMENT.md && grep -q "Axis (ii)" .planning/v0.3/ASSESSMENT.md && grep -q "Axis (iii)" .planning/v0.3/ASSESSMENT.md && bash .planning/v0.3/check-assessment.sh && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Section 1 contains 3 distinct paragraphs of milestone-level prose
    - Section 1 contains a "How to read this document" subsection (heading or bold-label formatting; either acceptable)
    - Section 1 contains a "Limits of this assessment" subsection
    - Section 1 contains a "Calibration notes" subsection (recommended; not strictly required by D-09 — but calibration_notes_items was included)
    - Section 1 mentions all of: surface count (14), verdict distribution, mean score (20.21/24), entry count (27), per-tier split (2 / 8 / 17)
    - Section 2 contains explicit definitions for "Axis (i)", "Axis (ii)", "Axis (iii)" (literal substrings)
    - Section 2 contains the tier-mapping reference table
    - Section 2 cites architecture invariants by reference to CLAUDE.md (literal substring CLAUDE.md or "architecture invariants" in axis (ii) definition)
    - bash .planning/v0.3/check-assessment.sh exits 0
    - No forbidden D-05 fields appear — count returns 0
    - Section 3 entry count unchanged: awk extraction returns 27
    - No literal "v0.4" substring anywhere — grep -in 'v0\.4' returns 0 matches
  </acceptance_criteria>
  <done>Sections 1 and 2 are fully composed in observational voice; the rubric is exposed per D-04; the corpus-level framing is in place. The grep gate passes. Section 3 is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Compose Section 4 (Inputs to next /gsd-new-milestone cycle)</name>
  <read_first>
    - .planning/phases/14-synthesis-handoff/14-CONTEXT.md D-10 (handoff section depth = artifacts + framing questions + non-prescriptions)
    - .planning/phases/14-synthesis-handoff/14-RESEARCH.md "Inputs to Next /gsd-new-milestone Cycle — Draft Section Structure" subsections (a)/(b)/(c)
    - .planning/v0.3/ASSESSMENT.md (Sections 1, 2, 3 already composed)
  </read_first>
  <files>.planning/v0.3/ASSESSMENT.md</files>
  <action>
Replace the Section 4 placeholder with three subsections per D-10.

Subsection (a) — Source artifacts: enumerate the artifact paths the next milestone-discovery cycle consumes. The list (verbatim from RESEARCH.md subsection (a)):

- .planning/v0.3/WALKTHROUGH.md — Phase 12 exploratory-feature output (1,276 lines, ~64 severity-tagged findings across 14 surfaces; 7 GitHub issues cross-linked; 4 backlog cross-links).
- .planning/v0.3/UI-AUDIT.md — Phase 13 milestone aggregator (14 surface verdicts at 5 ok / 9 warning / 0 fail; mean 20.21/24; 13 cross-cutting observations; calibration notes).
- .planning/v0.3/ui-reviews/ — 14 per-surface 6-pillar UI-REVIEWs with verdict + boilerplate/earned table + WALKTHROUGH cross-link footers.
- .planning/v0.3/walkthrough-screenshots/ (51 PNGs) + .planning/v0.3/ui-reviews/screenshots/ (27 PNGs) — visual evidence committed in git.
- GitHub issues lucaguery/al-dente audit:walkthrough label: #1 (Sheet-01 sheet positioning), #2 (ingredient parser), #3 (Voice/Photo stuck-extraction cross-surface dedupe), #4 (MEMBER_COUNT=2 hardcoded), #5 (cook_count idempotency), #6 (missing detail route), #7 (5-member capacity ceiling), #8 (PATCH /api/households/me 405).
- .planning/REQUIREMENTS.md (v0.3) and .planning/PROJECT.md "Surfaced for follow-up (v0.2.2 backlog)" — orthogonal backlog state at v0.3 ship.

Subsection (b) — Open framing questions: use the 5 questions from framing_question_set above. The 5 are mandatory floor; additional inquiries permitted as long as each preserves inquiry-form (How / Are / What / Coordination scope) and avoids the D-08 blocked verb sequences.

Pre-amble paragraph for subsection (b): "The questions below are inquiries the next milestone-discovery cycle reckons with. Each cites the assessment input that surfaces the inquiry. Phrasing such as 'How does X weigh Y?' surfaces the question without dictating the answer."

Subsection (c) — Explicit non-prescriptions: use the 5 bullets from non_prescription_set above (with the "propose"-substitution applied to bullet 3 — use "name" instead of "propose"). The 5 are mandatory floor.

Pre-amble paragraph for subsection (c): "This document is bounded. The following statements describe what the assessment does NOT do."

CRITICAL VOICE-DISCIPLINE REVIEW before running the gate:

1. Question 2 in framing_question_set: the rewritten phrasing is "Are architecture-invariant violations (axis ii >= 1) bundled into a single coordinated phase, or surfaced into independent per-invariant phases?" Verify no blocked-verb-sequence ("should fix", "should add", etc.) appears.

2. Non-prescription bullet 3 in non_prescription_set: rewritten to "ASSESSMENT.md does not name future phase shapes, plan shapes, or future-milestone requirements." Verify no literal "propose" token, no literal "v0.4" token.

3. Non-prescription bullet 4: rewritten to use "warning" / "ok" instead of the verdict glyphs (the glyphs would render but the textual "from warning to ok" reads cleaner). Optionally use the glyphs directly if rendering is preferred.

4. Question 5 in framing_question_set: avoid "should" entirely — the phrasing "What does X imply for the next audit budget?" is non-imperative.

5. Subsection (a) artifact list: contains the literal substring "PATCH /api/households/me 405" — this is a code path reference, not prescription. Permitted.

Self-check the gate after Section 4 is composed (DRAFT pass — not pre-commit yet). Run bash .planning/v0.3/check-assessment.sh. Expect exit 0. If exit 1 because of "v0.4" in the framing questions or non-prescriptions, rewrite those substrings and re-run.

Final structural verification command set (after the gate passes):
- grep -c '^## Inputs to next /gsd-new-milestone cycle$' .planning/v0.3/ASSESSMENT.md should return 1
- subsections (a), (b), (c) — count framing questions: awk extraction should return 5 (or between 3 and 5)
- subsection (c) line count: at least 5 bullets matching the "ASSESSMENT.md does not" pattern OR the "Tier ordering reflects" pattern
  </action>
  <verify>
    <automated>bash -c 'grep -q "^## Inputs to next /gsd-new-milestone cycle$" .planning/v0.3/ASSESSMENT.md && grep -qE "^### \(a\)" .planning/v0.3/ASSESSMENT.md && grep -qE "^### \(b\)" .planning/v0.3/ASSESSMENT.md && grep -qE "^### \(c\)" .planning/v0.3/ASSESSMENT.md && Q=$(awk "/^### \(b\)/,/^### \(c\)/" .planning/v0.3/ASSESSMENT.md | grep -cE "^[0-9]+\. ") && [ "$Q" -ge 3 ] && [ "$Q" -le 5 ] && [ "$(grep -c "^- ASSESSMENT\.md does not" .planning/v0.3/ASSESSMENT.md)" -ge 3 ] && bash .planning/v0.3/check-assessment.sh && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Section 4 contains exactly one heading "## Inputs to next /gsd-new-milestone cycle"
    - Section 4 contains exactly three subsections marked (a), (b), (c) — heading patterns "### (a)", "### (b)", "### (c)" or equivalent named headings preserving the (a)/(b)/(c) ordering
    - Section 4(a) lists at minimum WALKTHROUGH.md, UI-AUDIT.md, ui-reviews/, screenshots, GitHub issues #1 through #8, REQUIREMENTS.md, PROJECT.md
    - Section 4(b) contains 3-5 numbered framing questions
    - Section 4(c) contains at least 3 bullets matching "^- ASSESSMENT\.md does not" pattern (the floor 4 bullets), plus the "Tier ordering reflects" bullet
    - No literal "v0.4" substring anywhere — grep -in 'v0\.4' returns 0 matches
    - Pre-commit gate passes — bash .planning/v0.3/check-assessment.sh exits 0
    - All 27 Section 3 entries unchanged — awk extraction returns 27
  </acceptance_criteria>
  <done>Section 4 (Inputs to next /gsd-new-milestone cycle) is fully composed with artifacts list + 3-5 framing questions + 5 non-prescriptions. The full ASSESSMENT.md is now complete in all 4 sections. The grep gate passes against the complete draft.</done>
</task>

<task type="auto">
  <name>Task 3: Run pre-commit grep gate (D-07 second pass), commit ASSESSMENT.md + check-assessment.sh, push to main</name>
  <read_first>
    - .planning/phases/14-synthesis-handoff/14-CONTEXT.md (D-07 grep-gate-twice convention)
    - .planning/phases/14-synthesis-handoff/14-RESEARCH.md "Anti-Prescription Compliance Checklist"
    - .planning/v0.3/ASSESSMENT.md (the complete artifact ready for commit)
    - .planning/v0.3/check-assessment.sh (the gate)
    - CLAUDE.md Deployment section ("Push to main is the only deploy path") — Phase 14 doesn't deploy, but the commit lands on main and triggers no auto-deploy because .planning/ is outside frontend/ and backend/ deploy hooks
  </read_first>
  <files>.planning/v0.3/ASSESSMENT.md, .planning/v0.3/check-assessment.sh</files>
  <action>
This task runs the D-07 pre-commit grep gate (the second pass per CONTEXT specifics — "the grep gate should run twice during execution. Once as a draft check ...; once before the commit (D-07 contract). Same script invocation, both times.") and lands the commit.

Step 1 — Final pre-commit grep gate. Run bash .planning/v0.3/check-assessment.sh. Expect exit 0 with stdout "OK: .planning/v0.3/ASSESSMENT.md passes the anti-prescription gate." If exit 1, do NOT commit. Read stderr matched lines, fix the offending substrings, re-run the gate. Iterate until exit 0.

Step 2 — Final structural verification. Run the verification command set:
- grep -c '^## Executive summary$' .planning/v0.3/ASSESSMENT.md — must return 1
- grep -c '^## Ranking method$' .planning/v0.3/ASSESSMENT.md — must return 1
- grep -c '^## Ranked findings$' .planning/v0.3/ASSESSMENT.md — must return 1
- grep -c '^## Inputs to next /gsd-new-milestone cycle$' .planning/v0.3/ASSESSMENT.md — must return 1
- grep -c '^### Tier [123]' .planning/v0.3/ASSESSMENT.md — must return 3
- entry count via awk extraction — must return 27
- forbidden-field count — must return 0
- Sources field count — must return 27 (every entry has Sources)

If any verification fails, fix the document and re-run the gate. Do NOT commit until all verifications pass.

Step 3 — Stage the files. Run git add .planning/v0.3/ASSESSMENT.md .planning/v0.3/check-assessment.sh. Run git status. Verify the staged set is exactly two files; nothing in frontend/ or backend/ is staged (per feedback_executor_scope_creep audit-only discipline).

Step 4 — Commit using GSD tools. Use gsd-tools commit with message "feat(14-02): synthesize v0.3 assessment — closes SYNTH-01/02/03". The commit message follows the project's existing convention (per recent git log: "feat(13-04): UI-AUDIT.md milestone aggregator — closes AUDIT-04"). Pass --files .planning/v0.3/ASSESSMENT.md .planning/v0.3/check-assessment.sh to constrain the commit scope.

Step 5 — Verify the commit landed cleanly. Run git log -1 --stat .planning/v0.3/ — most recent commit lists ASSESSMENT.md and check-assessment.sh as additions. Run git status — clean working tree.

Step 6 — Push to main (per CLAUDE.md Deployment section: "Push to main is the only deploy path"). Run git push origin main. Note: .planning/ is outside frontend/ and backend/ deploy hooks. The push lands the commit but triggers no Vercel/Railway deploy. This is correct — Phase 14 ships an audit document, not product code.

Important — DO NOT run vercel --prod, railway up, or any manual deploy command (per feedback_no_manual_vercel_deploy memory). The push is the deploy path.

Step 7 — Final state check. Run git rev-parse HEAD to capture the new commit SHA for the SUMMARY.md. Record the commit SHA in the Plan 2 SUMMARY.md "Final commit" section.
  </action>
  <verify>
    <automated>bash -c 'bash .planning/v0.3/check-assessment.sh && [ "$(grep -c "^## Executive summary$" .planning/v0.3/ASSESSMENT.md)" = "1" ] && [ "$(grep -c "^## Ranking method$" .planning/v0.3/ASSESSMENT.md)" = "1" ] && [ "$(grep -c "^## Ranked findings$" .planning/v0.3/ASSESSMENT.md)" = "1" ] && [ "$(grep -c "^## Inputs to next /gsd-new-milestone cycle$" .planning/v0.3/ASSESSMENT.md)" = "1" ] && [ "$(awk "/^### Tier 1/,/^## Inputs to next /" .planning/v0.3/ASSESSMENT.md | grep -c "^#### ")" = "27" ] && git log -1 --pretty=%s | grep -q "14-02" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - bash .planning/v0.3/check-assessment.sh exits 0 (D-07 pre-commit pass — second of two-pass enforcement)
    - All 4 top-level sections present (each grep -c returns 1)
    - All 3 tier subheadings present (grep -c '^### Tier [123]' returns 3)
    - Entry count locked at 27 (awk extraction returns 27)
    - Forbidden D-05 fields absent (grep -cE forbidden pattern returns 0)
    - Every entry has Sources field (grep -c '^- \*\*Sources:\*\*' returns 27)
    - No literal "v0.4" substring anywhere — grep -in 'v0\.4' returns 0
    - Past-phase citations PERMITTED — grep -c 'Phase 1[123]' returns at least 5 (Phase 11/12/13 cited multiple times)
    - Git commit landed with subject containing "14-02" — git log -1 --pretty=%s matches /14-02/
    - Staged-file set was exactly .planning/v0.3/ASSESSMENT.md and .planning/v0.3/check-assessment.sh (NOTHING in frontend/ or backend/ — verify post-commit via git show --stat HEAD)
    - Push to main succeeded (git status clean, branch up to date with origin/main)
    - Post-push deploy-hook non-firing verifiable: `git show --stat HEAD` lists ONLY paths under `.planning/v0.3/` (paths excluded from Vercel/Railway deploy hooks per their respective build settings — `.planning/` sits outside `frontend/` and `backend/` deploy roots)
  </acceptance_criteria>
  <done>The full ASSESSMENT.md passes the D-07/D-08 pre-commit grep gate; the commit lands on main with exactly the two intended files; SYNTH-01/02/03 are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auditor -> ASSESSMENT.md | Synthesis writer (Claude) authors prose into the doc. No untrusted input crosses; no user-submitted data; no auth surface. |
| Repo -> grep gate | Pre-commit script reads ASSESSMENT.md and applies forbidden-pattern regex. Local-only; no network. |
| Local -> origin/main | git push of audit-only document (.planning/ scope). Does not trigger Vercel/Railway deploy hooks. |

## STRIDE Threat Register

Phase 14 has no attack surface change. The phase produces only .planning/v0.3/ASSESSMENT.md and .planning/v0.3/check-assessment.sh. No code, no API, no auth, no data flow. The grep gate (D-07/D-08) is itself a security-adjacent control: it prevents the document from drifting into prescription which would conflict with the SYNTH-02 success criterion.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I (Information disclosure) | ASSESSMENT.md citation excerpts | mitigate | Per Phase 13 D-12 + Phase 12 D-04 inheritance — citations are by header anchor (e.g., "WALKTHROUGH.md SS Capture-Full P-12-F01"), NOT by quote excerpt. The schema (D-05 Sources field) requires anchor-cite format. No PII, no secrets, no auth tokens cross into ASSESSMENT.md. Plan 1 Task 2/3 actions reinforce the anchor-cite-only convention. |
| T-14-02 | T (Tampering — gate false negative) | check-assessment.sh regex | mitigate | The composite regex is codified verbatim in 14-RESEARCH.md "Anti-Prescription Compliance Checklist" and reproduced in Plan 1 body. Plan 1 Task 1 self-tests the gate against a known-bad string ("v0.4 should fix this") and a known-good string ("Per Phase 13 D-02...") — confirms the regex matches forward-construction and permits past-phase citations. Cross-platform: the regex uses POSIX grep -inE syntax compatible with both BSD grep (macOS) and GNU grep (Linux); fallback to ripgrep documented in 14-RESEARCH.md "Environment Availability" if a BSD-grep regex variant emerges. |
| T-14-03 | E (Elevation of privilege via plan creep) | Executor scope discipline | accept | Per feedback_executor_scope_creep memory + audit-only milestone discipline (CLAUDE.md). The files_modified frontmatter lists ONLY .planning/v0.3/ASSESSMENT.md (Plan 2) and .planning/v0.3/check-assessment.sh (Plan 1). The executor pattern is well-established across Phase 12 / Phase 13 — both produced only .planning/v0.3/ artifacts with no product code edits. Task 3 verifies the staged set post-commit via git show --stat HEAD. Risk accepted. |
| T-14-04 | T (Tampering — accidental deploy) | git push origin main | mitigate | .planning/ is outside frontend/ and backend/ deploy hooks. Push lands the commit; no Vercel/Railway deploy triggers. Task 3 explicitly forbids manual deploy commands per feedback_no_manual_vercel_deploy. |
</threat_model>

<verification>
Plan 2 phase-level checks (run after Task 3 completes):

1. ASSESSMENT.md complete in all 4 D-09 sections:
   - grep -c '^## Executive summary$' .planning/v0.3/ASSESSMENT.md returns 1
   - grep -c '^## Ranking method$' .planning/v0.3/ASSESSMENT.md returns 1
   - grep -c '^## Ranked findings$' .planning/v0.3/ASSESSMENT.md returns 1
   - grep -c '^## Inputs to next /gsd-new-milestone cycle$' .planning/v0.3/ASSESSMENT.md returns 1

2. Section 1 framing subsections present:
   - grep -q 'How to read this document' .planning/v0.3/ASSESSMENT.md
   - grep -q 'Limits of this assessment' .planning/v0.3/ASSESSMENT.md

3. Section 2 rubric exposed (D-04):
   - grep -q 'Axis (i)' .planning/v0.3/ASSESSMENT.md
   - grep -q 'Axis (ii)' .planning/v0.3/ASSESSMENT.md
   - grep -q 'Axis (iii)' .planning/v0.3/ASSESSMENT.md

4. Section 4 has all three D-10 subsections:
   - grep -qE '^### \(a\)' .planning/v0.3/ASSESSMENT.md (or "### Source artifacts")
   - grep -qE '^### \(b\)' .planning/v0.3/ASSESSMENT.md (or "### Open framing questions")
   - grep -qE '^### \(c\)' .planning/v0.3/ASSESSMENT.md (or "### Explicit non-prescriptions")

5. Section 4(b) contains 3-5 framing questions (count via awk extraction).

6. Section 4(c) contains 5 non-prescription bullets matching the "ASSESSMENT.md does not" pattern OR the "Tier ordering reflects" pattern.

7. Anti-prescription gate (D-07/D-08 second pass):
   - bash .planning/v0.3/check-assessment.sh exits 0
   - grep -in 'v0\.4' .planning/v0.3/ASSESSMENT.md returns 0 matches

8. Past-phase citations preserved (D-08 explicit allowance):
   - grep -cE 'Phase 1[123]' .planning/v0.3/ASSESSMENT.md returns >= 5

9. Section 3 entry count unchanged (Plan 1's work preserved):
   - awk '/^### Tier 1/,/^## Inputs to next /' .planning/v0.3/ASSESSMENT.md | grep -c '^#### ' returns 27

10. Commit landed:
    - git log -1 --pretty=%s contains "14-02"
    - git show --stat HEAD lists exactly .planning/v0.3/ASSESSMENT.md and .planning/v0.3/check-assessment.sh (no frontend/ or backend/ paths)
    - git status returns clean working tree
    - Branch up to date with origin/main
</verification>

<success_criteria>
SYNTH-01 (closed by Plan 2): ASSESSMENT.md exists with a single ranked findings list combining WALKTHROUGH and UI-AUDIT entries, ordered by impact on "feels Al Dente", citing source artifacts in every entry's Sources field. The list is framed by Section 1's executive summary and Section 2's exposed rubric.

SYNTH-02 (closed by Plan 2): The document is demonstrably descriptive — D-08 grep gate passes (independently verifiable). Section 1's "How to read this document" subsection front-loads the descriptive-not-prescriptive boundary. The schema (D-05) prevented forbidden fields throughout.

SYNTH-03 (closed by Plan 2): Section 4 explicitly names the artifacts (a), the open framing questions (b), and the explicit non-prescriptions (c) the next /gsd-new-milestone cycle should consume — naming inputs without dictating outputs.
</success_criteria>

<output>
After Plan 2 completes, create .planning/phases/14-synthesis-handoff/14-02-SUMMARY.md documenting: (a) final document statistics (line count, entry count, section count); (b) draft-pass and pre-commit grep-gate results (exit codes, any iterations); (c) tension-resolution confirmations (T-1..T-7, N-1..N-5 all applied); (d) commit SHA + push confirmation; (e) any voice-discipline rewrites the executor applied during composition (especially the v0.4 -> "next milestone" substitution and the "propose" -> "name" substitution). The SUMMARY closes Phase 14.
</output>
