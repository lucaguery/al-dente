---
phase: 13-design-quality-originality-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v0.3/ui-reviews/capture-quick-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/capture-full-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/capture-voice-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/capture-photo-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/capture-url-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/screenshots/capture-quick-*.png
  - .planning/v0.3/ui-reviews/screenshots/capture-full-*.png
  - .planning/v0.3/ui-reviews/screenshots/capture-voice-*.png
  - .planning/v0.3/ui-reviews/screenshots/capture-photo-*.png
  - .planning/v0.3/ui-reviews/screenshots/capture-url-*.png
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]
tags: [audit, ui, visual, design]

must_haves:
  truths:
    - "Each of the 5 capture surfaces has a per-surface UI-REVIEW.md at .planning/v0.3/ui-reviews/<surface>-UI-REVIEW.md"
    - "Each per-surface UI-REVIEW carries an originality verdict tag from the fixed enum {Feels Al Dente ✅ / Mixed ⚠ / Feels Generic ❌}"
    - "Each per-surface UI-REVIEW includes a boilerplate|earned 2-column table with ≥1 row in each column (or 'None observed' with a one-line justification per D-03)"
    - "Each per-surface UI-REVIEW includes a 6-pillar table (Copywriting / Visuals / Color / Typography / Spacing / Experience Design — each /4) with a total /24"
    - "Each scored surface has ≥1 screenshot under .planning/v0.3/ui-reviews/screenshots/ linked from its UI-REVIEW"
    - "WALKTHROUGH-surfaced findings overlapping a pillar finding are cross-linked inline as `(See WALKTHROUGH.md §<surface> — <finding-id>)` per D-12"
  artifacts:
    - path: ".planning/v0.3/ui-reviews/capture-quick-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for capture-quick"
      contains: "## Originality Verdict"
    - path: ".planning/v0.3/ui-reviews/capture-full-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for capture-full"
      contains: "## Originality Verdict"
    - path: ".planning/v0.3/ui-reviews/capture-voice-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for capture-voice"
      contains: "## Originality Verdict"
    - path: ".planning/v0.3/ui-reviews/capture-photo-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for capture-photo"
      contains: "## Originality Verdict"
    - path: ".planning/v0.3/ui-reviews/capture-url-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for capture-url"
      contains: "## Originality Verdict"
    - path: ".planning/v0.3/ui-reviews/screenshots/"
      provides: "PNG visual evidence for the 5 capture surfaces (~10-20 PNGs total)"
      contains: "capture-*.png"
  key_links:
    - from: ".planning/v0.3/ui-reviews/<surface>-UI-REVIEW.md"
      to: ".planning/v0.3/WALKTHROUGH.md §<surface>"
      via: "inline cross-link `(See WALKTHROUGH.md §<surface> — <finding-id>)` per D-12"
      pattern: "See WALKTHROUGH.md"
    - from: ".planning/v0.3/ui-reviews/<surface>-UI-REVIEW.md"
      to: ".planning/v0.3/ui-reviews/screenshots/<surface>-*.png"
      via: "relative markdown image/link"
      pattern: "./screenshots/.*\\.png"
---

<objective>
Score the 5 capture surfaces (capture-quick, capture-full, capture-voice, capture-photo, capture-url) under the 6-pillar `/gsd-ui-review` rubric and produce a per-surface UI-REVIEW.md for each, anchored by Playwright MCP screenshots committed alongside.

Purpose: capture surfaces are the heaviest UI density of the audit (5 of 14 surfaces) and per CONTEXT.md D-07 they go first to calibrate the auditor's scoring against the locked Slow Food design system. Establishes the rubric application pattern that Plans 13-02 and 13-03 follow.

Output: 5 per-surface UI-REVIEW.md files + ~10-20 screenshots committed under `.planning/v0.3/ui-reviews/screenshots/`.

**Audit-only constraint (load-bearing — `feedback_executor_scope_creep`):** This plan writes ONLY to `.planning/v0.3/ui-reviews/`. NO `frontend/` changes. NO `backend/` changes. NO product-code changes whatsoever. The auditor reads frontend code to score it; modifying it is out of scope for the entire v0.3 milestone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-design-quality-originality-audit/13-CONTEXT.md
@.planning/v0.3/WALKTHROUGH.md
@~/.claude/agents/gsd-ui-auditor.md
@CLAUDE.md
@frontend/playwright.config.ts

<interfaces>
<!-- Per-surface UI-REVIEW skeleton (mandatory structure per CONTEXT.md D-04). -->
<!-- Executor MUST write each of the 5 files following this exact shape. -->

```markdown
# UI Review — <Surface Name>

**Audited:** 2026-05-09
**Auditor:** Claude (Phase 13, manual scoring per CONTEXT.md D-06)
**Synthetic env:** [SYNTHETIC] Démo Al Dente @ https://al-dente-pink.vercel.app
**Viewport:** iPhone-shape Chromium 390×844 (isMobile, hasTouch — mirrors frontend/playwright.config.ts)
**Reach status:** Reached | Partially reached (note what couldn't be observed) — per D-16

## Originality Verdict

**Verdict:** <Feels Al Dente ✅ | Mixed ⚠ | Feels Generic ❌>

| Boilerplate elements | Earned elements |
|----------------------|-----------------|
| <element> (`frontend/path:line` or screenshot crop ref) | <element> (`frontend/path:line` or screenshot crop ref) |
| <element> | <element> |

(If empty in either column, write `None observed` with a one-line justification per D-03.)

## 6-Pillar Score: N/24

| Pillar | Score | Key Finding |
|--------|-------|-------------|
| Copywriting | N/4 | <one-line> |
| Visuals | N/4 | <one-line> |
| Color | N/4 | <one-line> |
| Typography | N/4 | <one-line> |
| Spacing | N/4 | <one-line> |
| Experience Design | N/4 | <one-line> |

## Detailed Findings

(Order: lowest-scoring pillar first; ties broken by impact on verdict.)

### Pillar X: <Name> (N/4)
- <finding with `frontend/path:line` reference>
- <finding> (See WALKTHROUGH.md §<surface> — <finding-id>) ← when overlapping per D-12

[repeat per pillar]

## Screenshots

- `./screenshots/<surface>-canonical.png` — <caption>
- `./screenshots/<surface>-<state>.png` — <caption>

## WALKTHROUGH cross-links (context inherited per D-11)

- WALKTHROUGH.md §<surface name> — 2-3 line context summary; full findings cross-linked inline above where they impact pillar scores
```

<!-- Surface → frontend code mapping (auditor reads these to score). -->
<!-- From CONTEXT.md §code_context Integration Points + the audit unit list (D-05). -->

| Surface | WALKTHROUGH §header | Frontend code paths |
|---------|---------------------|---------------------|
| capture-quick | `## Capture — Quick` | `frontend/app/recipes/new/page.tsx`, `frontend/components/RecipeForm.tsx` (quick tab), `frontend/components/RecipeDraftCard.tsx`, `frontend/app/inbox/` (draft inbox) |
| capture-full | `## Capture — Full` | `frontend/app/recipes/new/page.tsx`, `frontend/components/RecipeForm.tsx` (full tab), `frontend/app/recipes/[id]/page.tsx` (structured recipe view) |
| capture-voice | `## Capture — Voice` | `frontend/components/VoiceCaptureTab.tsx`, `frontend/components/VoiceInput.tsx`, `frontend/components/VoiceModifySheet.tsx` |
| capture-photo | `## Capture — Photo` | `frontend/components/PhotoCaptureTab.tsx`, `frontend/components/PhotoUploader.tsx`, `frontend/components/ui/sheet.tsx` (Sheet-01 #1 lives here per WALKTHROUGH) |
| capture-url | `## Capture — URL` | `frontend/components/UrlCaptureTab.tsx`, `frontend/app/recipes/new/page.tsx` (URL tab routing) |

<!-- Auditor session continuity: do NOT log out / re-onboard. Auditor is already a member of -->
<!-- [SYNTHETIC] Démo Al Dente from Phase 12 (joined via DEMO01). -->
<!-- If session lost: re-join via DEMO01 → "Rejoindre un foyer existant" → pick auditor name. -->
<!-- See CONTEXT.md §Auditor identity. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Score 3 capture surfaces (quick + full + voice)</name>
  <files>
    .planning/v0.3/ui-reviews/capture-quick-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/capture-full-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/capture-voice-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/screenshots/capture-quick-*.png,
    .planning/v0.3/ui-reviews/screenshots/capture-full-*.png,
    .planning/v0.3/ui-reviews/screenshots/capture-voice-*.png
  </files>

  <read_first>
    REQUIRED reading before any browser navigation or scoring (do not skip):

    1. `.planning/phases/13-design-quality-originality-audit/13-CONTEXT.md` — apply D-01..D-16 uniformly. The verdict shape (D-01), Al Dente definition (D-02), minimum boilerplate/earned (D-03), file structure (D-04), audit unit (D-05), manual-scoring mechanism (D-06), screenshot density (D-08), storage path (D-09), commit policy (D-10), WALKTHROUGH read-first (D-11), cross-link format (D-12), score-docking rule (D-13), unreachable-surface fallback (D-16) — all governing.
    2. `~/.claude/agents/gsd-ui-auditor.md` — the canonical 6-pillar rubric with grep audit methods. Per CONTEXT D-06 you do NOT spawn this agent — you apply the rubric inline. Per D-08/D-09/D-10 the rubric's `<screenshot_approach>` (CLI) and `<gitignore_gate>` are SUPERSEDED — use `mcp__playwright__*` tools and commit PNGs.
    3. `.planning/v0.3/WALKTHROUGH.md` — read JUST the 3 surface sections you'll score before scoring each (do NOT load all 131KB upfront): `## Capture — Quick` (lines ~61-124), `## Capture — Full` (lines ~125-193), `## Capture — Voice` (lines ~194-249). Per D-11 read each surface section IMMEDIATELY before scoring that surface.
    4. Frontend code per surface (read once you've navigated to each surface):
       - capture-quick: `frontend/app/recipes/new/page.tsx`, `frontend/components/RecipeForm.tsx`, `frontend/components/RecipeDraftCard.tsx`
       - capture-full: `frontend/app/recipes/new/page.tsx`, `frontend/components/RecipeForm.tsx`, `frontend/app/recipes/[id]/page.tsx`
       - capture-voice: `frontend/components/VoiceCaptureTab.tsx`, `frontend/components/VoiceInput.tsx`, `frontend/components/VoiceModifySheet.tsx`
    5. `frontend/app/globals.css` — Slow Food design tokens (terracotta primary at h≈35°, warm-cream, paper-grain, two-layer warm-brown shadows, Fraunces italic, IBM Plex Sans). D-02a token-compliance check measures against this.
    6. `frontend/playwright.config.ts` lines 86-90 — viewport spec to mirror in `mcp__playwright__*` (390×844, isMobile, hasTouch).
    7. `CLAUDE.md` — Architecture invariants 1, 6 inform Pillar 1 (French-only via next-intl) and the originality judgment.
  </read_first>

  <action>
    Audit-only sequence per surface (repeat 3 times: capture-quick → capture-full → capture-voice, in WALKTHROUGH order per D-07):

    **STEP A — Verify auditor session (once at task start):**
    1. `mcp__playwright__browser_navigate` to `https://al-dente-pink.vercel.app`. Confirm landed on home/decide screen as a household member (cookie session persists from Phase 12 per invariant #8). If redirected to `/onboarding/welcome`, re-join via `DEMO01` → "Rejoindre un foyer existant" → pick the auditor name. DO NOT log out, DO NOT create a fresh account.
    2. Configure browser context viewport to 390×844 (mirror frontend/playwright.config.ts seeded project — iPhone-shape Chromium). Use `mcp__playwright__browser_resize` if a resize tool exists; otherwise navigate fresh after setting context options.

    **STEP B — Per surface (5 sub-steps):**
    1. **Read WALKTHROUGH section JUST-IN-TIME** (per D-11): Read just the `## Capture — <Surface>` section from `.planning/v0.3/WALKTHROUGH.md` (use `Read` with offset/limit to grab only the ~5-10KB block). Note inherited context: starting state, blockers found, friction findings — these will dock pillar scores per D-13.
    2. **Navigate + capture screenshots** (per D-08, 2-4 PNGs per surface):
       - `mcp__playwright__browser_navigate` to the surface URL (e.g. `/recipes/new` then click the relevant tab — quick / full / voice / photo / url).
       - `mcp__playwright__browser_take_screenshot` for: (a) canonical state (entry point), (b) ≥1 meaningful state variant per D-08 (empty / loaded / mid-interaction). Save to `.planning/v0.3/ui-reviews/screenshots/<surface>-<state-slug>.png`. State slug examples: `-canonical`, `-empty-state`, `-with-input`, `-loading`, `-after-submit`. Filename pattern: lowercase-hyphenated, no spaces.
       - Thin surfaces (capture-url is thin per CONTEXT specifics) may legitimately get just 1-2 PNGs; rich ones (capture-voice with the modify sheet, capture-quick with draft inbox round-trip) earn 3-4. Floor is AUDIT-03's "≥1."
    3. **Read frontend code for the surface** (paths in <interfaces> table). Apply the per-pillar grep audit methods from `~/.claude/agents/gsd-ui-auditor.md` SCOPED to those file paths only:
       - Pillar 1 Copywriting: grep for generic CTA patterns ("Submit", "OK", raw English fallthrough); cross-check against next-intl message keys. Hardcoded user-facing strings violate invariant #6 → docks the score.
       - Pillar 2 Visuals: focal point, icon-only buttons w/ aria-labels, visual hierarchy (size/weight/color).
       - Pillar 3 Color: count `text-primary`/`bg-primary`/`border-primary` usage; flag accent overuse (>10 unique elements per file); flag hardcoded `#hex`/`rgb()`. Per D-02a verify terracotta primary is the accent (not raw shadcn red).
       - Pillar 4 Typography: count distinct `text-{xs..5xl}` and `font-{thin..extrabold}` classes. Per D-02a verify Fraunces italic is reserved for display moments, IBM Plex Sans is body default.
       - Pillar 5 Spacing: count distinct `p-/px-/py-/m-/gap-/space-` classes; flag arbitrary `[Npx]` values; verify spacing matches the Tailwind v4 scale.
       - Pillar 6 Experience Design: loading states present, error boundaries, empty states, disabled states for actions, confirmation for destructive actions. Per D-13: WALKTHROUGH-surfaced friction/blockers (e.g. capture-photo's Sheet-01 [#1]) DOCK this pillar.
       - Score each pillar 1-4 with evidence (file:line refs).
    4. **Form the originality verdict** (per D-01/D-02/D-03):
       - Tag from fixed enum: `Feels Al Dente ✅` (token compliance + editorial cohesion both pass) | `Mixed ⚠` (token compliance passes, editorial cohesion partial) | `Feels Generic ❌` (token compliance fails, regardless of cohesion).
       - Build the boilerplate|earned 2-column table with ≥1 concrete element each. If genuinely empty in one direction, write `None observed` with one-line justification per D-03.
       - D-15 calibration: v0.2 Phases 5-9 averaged 22.4/24 across whole phases. Per-surface scores may legitimately fall below or above this; use as sanity check, not target.
    5. **Write `.planning/v0.3/ui-reviews/<surface>-UI-REVIEW.md`** following the EXACT skeleton in <interfaces>. Use the `Write` tool (per gsd-ui-auditor "ALWAYS use the Write tool, never `Bash(cat << 'EOF')`"). Doc opens with verdict (per D-04 ordering: verdict tag → boilerplate/earned table → 6-pillar table → detailed findings → screenshots → WALKTHROUGH cross-links footer).
    6. Cross-link inline `(See WALKTHROUGH.md §<surface> — <finding-id>)` per D-12 wherever a pillar finding overlaps a WALKTHROUGH finding (use Phase 12's existing finding numbering — e.g. `P-12-Q01`, `P-12-F01`).

    **HARD CONSTRAINTS (audit-only — `feedback_executor_scope_creep`):**
    - DO NOT modify any file under `frontend/` or `backend/`. The audit READS frontend code to score it; modification is out of scope for the entire v0.3 milestone. If a probe surfaces a fixable bug, document the score impact and STOP — do not fix.
    - DO NOT extend `frontend/tests/e2e/` or commit new spec files. This phase is `mcp__playwright__*` agent-driven, not test-suite-driven (mirrors Phase 12 pattern).
    - DO NOT file new GitHub issues for visual findings — the per-surface UI-REVIEW IS the surface (per CONTEXT §"Not in this phase"). Issues remain Phase 12's filing mechanism.
    - DO NOT log out of the auditor session, DO NOT re-onboard with a new account, DO NOT trigger Phase 11's seed teardown unless state genuinely blocks observation (escape hatch only, mirrors Phase 12 D-09).
  </action>

  <verify>
    <automated>
      test -f .planning/v0.3/ui-reviews/capture-quick-UI-REVIEW.md && \
      test -f .planning/v0.3/ui-reviews/capture-full-UI-REVIEW.md && \
      test -f .planning/v0.3/ui-reviews/capture-voice-UI-REVIEW.md && \
      grep -lE "Feels Al Dente ✅|Mixed ⚠|Feels Generic ❌" .planning/v0.3/ui-reviews/capture-quick-UI-REVIEW.md .planning/v0.3/ui-reviews/capture-full-UI-REVIEW.md .planning/v0.3/ui-reviews/capture-voice-UI-REVIEW.md | wc -l | grep -q "^3$" && \
      grep -lE "Boilerplate elements" .planning/v0.3/ui-reviews/capture-quick-UI-REVIEW.md .planning/v0.3/ui-reviews/capture-full-UI-REVIEW.md .planning/v0.3/ui-reviews/capture-voice-UI-REVIEW.md | wc -l | grep -q "^3$" && \
      grep -lE "Copywriting.*\\|.*[1-4]/4" .planning/v0.3/ui-reviews/capture-quick-UI-REVIEW.md .planning/v0.3/ui-reviews/capture-full-UI-REVIEW.md .planning/v0.3/ui-reviews/capture-voice-UI-REVIEW.md | wc -l | grep -q "^3$" && \
      ls .planning/v0.3/ui-reviews/screenshots/capture-quick-*.png 2>/dev/null | head -1 && \
      ls .planning/v0.3/ui-reviews/screenshots/capture-full-*.png 2>/dev/null | head -1 && \
      ls .planning/v0.3/ui-reviews/screenshots/capture-voice-*.png 2>/dev/null | head -1
    </automated>
  </verify>

  <acceptance_criteria>
    - `.planning/v0.3/ui-reviews/capture-quick-UI-REVIEW.md` exists, contains `## Originality Verdict`, contains a verdict tag from `{Feels Al Dente ✅ | Mixed ⚠ | Feels Generic ❌}`, contains a `Boilerplate elements | Earned elements` table with ≥1 row per column (or `None observed` with justification), contains a 6-pillar table with all 6 pillars scored N/4, and contains a `## Screenshots` section linking ≥1 .png from `./screenshots/`.
    - Same conditions for `capture-full-UI-REVIEW.md` and `capture-voice-UI-REVIEW.md`.
    - `.planning/v0.3/ui-reviews/screenshots/capture-quick-*.png`, `capture-full-*.png`, `capture-voice-*.png` each have ≥1 file with size > 0.
    - For each of the 3 UI-REVIEW files, where WALKTHROUGH §`Capture — <surface>` has findings, those findings are cross-linked inline as `(See WALKTHROUGH.md §<surface> — <finding-id>)` per D-12.
    - For each surface where WALKTHROUGH had a blocker/friction finding (e.g. capture-full P-12-F01 ingredient parser), the relevant pillar score reflects D-13 docking.
    - NO files modified under `frontend/` or `backend/` (verify with `git status frontend/ backend/` showing clean).
  </acceptance_criteria>

  <done>3 per-surface UI-REVIEW files committed at `.planning/v0.3/ui-reviews/{capture-quick,capture-full,capture-voice}-UI-REVIEW.md`, each with verdict + boilerplate/earned table + 6-pillar score + screenshots; ~6-12 PNGs committed under `.planning/v0.3/ui-reviews/screenshots/capture-{quick,full,voice}-*.png`. No product code modified.</done>
</task>

<task type="auto">
  <name>Task 2: Score 2 capture surfaces (photo + url)</name>
  <files>
    .planning/v0.3/ui-reviews/capture-photo-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/capture-url-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/screenshots/capture-photo-*.png,
    .planning/v0.3/ui-reviews/screenshots/capture-url-*.png
  </files>

  <read_first>
    1. Same Phase 13 CONTEXT.md as Task 1 (D-01..D-16 — particularly D-13 which heavily impacts these two surfaces).
    2. `~/.claude/agents/gsd-ui-auditor.md` — same rubric, applied identically.
    3. `.planning/v0.3/WALKTHROUGH.md` — read JUST these surface sections just-in-time:
       - `## Capture — Photo` (lines ~250-312) — has a known-blocker (Sheet-01 #1) landing the bottom sheet off-screen. Per D-13 this DOCKS Pillar 6 (Experience Design) for capture-photo, possibly Pillar 5 (Spacing) too.
       - `## Capture — URL` (lines ~313-382) — has the URL-01 blocker per D-14 (URL extraction is `# TODO(productize)`; drafts never promote). Per D-13 this DOCKS Pillar 6 for capture-url.
    4. Frontend code:
       - capture-photo: `frontend/components/PhotoCaptureTab.tsx`, `frontend/components/PhotoUploader.tsx`, `frontend/components/ui/sheet.tsx` (Sheet-01 site at line ~64 per CONTEXT)
       - capture-url: `frontend/components/UrlCaptureTab.tsx`, `frontend/app/recipes/new/page.tsx`
    5. `frontend/app/globals.css` and `frontend/playwright.config.ts` lines 86-90 (viewport spec).
    6. `.planning/PROJECT.md` §"Surfaced for follow-up" — for context on Sheet-01 [#1] and URL-01 backlog severity.
  </read_first>

  <action>
    Same audit-only sequence as Task 1, applied to the remaining 2 capture surfaces in WALKTHROUGH order: capture-photo → capture-url.

    **Specific notes per surface (in addition to the standard sequence):**

    **capture-photo:**
    - Sheet-01 [#1] is a load-bearing finding here (bottom sheet off-screen on iPhone viewport). Per D-13: this is a blocker-severity user-impact issue → Pillar 6 (Experience Design) is DOCKED accordingly. Score the pillar grep-method as the upper bound (sheet primitive is correctly themed in code) but dock based on user impact (sheet doesn't render in viewport).
    - Capture screenshots showing: (a) canonical entry to the photo capture tab, (b) the off-screen-sheet state if reproducible at the synthetic env (per WALKTHROUGH §Capture — Photo finding), (c) any working state (e.g. a successfully-promoted photo recipe from the synthetic 21).
    - Per D-13's balance note in CONTEXT specifics: don't drop the score from 23/24 to 12/24 — drop it to ~19-20/24 if visual quality is otherwise top-tier.

    **capture-url:**
    - Per D-14 in Phase 12 CONTEXT, URL-01 is the URL extraction `# TODO(productize)` blocker. The auditor should describe the user-visible failure (URL submitted → draft created → never promotes to structured) and reference WALKTHROUGH §Capture — URL findings.
    - Per D-13: Pillar 6 (Experience Design) DOCKED for capture-url because the primary intended action is non-functional. The visual design of the URL tab itself may still score high on Pillars 1-5 — score those independently per the rubric.
    - Thin surface per CONTEXT specifics — 1-2 screenshots may suffice (entry, draft-stuck-as-draft state).

    **Standard sequence (identical to Task 1, repeat 2x):**
    1. Verify auditor session at `https://al-dente-pink.vercel.app` (cookie persists from Task 1's browser context — same MCP session).
    2. Per surface: read WALKTHROUGH §section just-in-time → navigate + screenshot (2-4 PNGs per D-08, thin surfaces 1-2 OK) → read frontend code → apply 6 pillar grep methods scoped to those files → form verdict per D-01/D-02/D-03 (with D-13 docking applied) → write `<surface>-UI-REVIEW.md` per D-04 skeleton → cross-link WALKTHROUGH per D-12.

    **HARD CONSTRAINTS:** Identical to Task 1. NO `frontend/` or `backend/` modifications. NO new e2e specs. NO new GitHub issues. NO logging out.
  </action>

  <verify>
    <automated>
      test -f .planning/v0.3/ui-reviews/capture-photo-UI-REVIEW.md && \
      test -f .planning/v0.3/ui-reviews/capture-url-UI-REVIEW.md && \
      grep -lE "Feels Al Dente ✅|Mixed ⚠|Feels Generic ❌" .planning/v0.3/ui-reviews/capture-photo-UI-REVIEW.md .planning/v0.3/ui-reviews/capture-url-UI-REVIEW.md | wc -l | grep -q "^2$" && \
      grep -lE "Boilerplate elements" .planning/v0.3/ui-reviews/capture-photo-UI-REVIEW.md .planning/v0.3/ui-reviews/capture-url-UI-REVIEW.md | wc -l | grep -q "^2$" && \
      grep -lE "Copywriting.*\\|.*[1-4]/4" .planning/v0.3/ui-reviews/capture-photo-UI-REVIEW.md .planning/v0.3/ui-reviews/capture-url-UI-REVIEW.md | wc -l | grep -q "^2$" && \
      ls .planning/v0.3/ui-reviews/screenshots/capture-photo-*.png 2>/dev/null | head -1 && \
      ls .planning/v0.3/ui-reviews/screenshots/capture-url-*.png 2>/dev/null | head -1
    </automated>
  </verify>

  <acceptance_criteria>
    - `.planning/v0.3/ui-reviews/capture-photo-UI-REVIEW.md` exists with `## Originality Verdict`, verdict tag from the fixed enum, boilerplate/earned table, 6-pillar table summing to N/24, ≥1 screenshot link.
    - Same for `capture-url-UI-REVIEW.md`.
    - capture-photo UI-REVIEW references Sheet-01 (#1) and reflects D-13 score docking on Pillar 6 (Experience Design).
    - capture-url UI-REVIEW references URL-01 backlog notation per D-14 (Phase 12) and reflects D-13 score docking on Pillar 6.
    - `.planning/v0.3/ui-reviews/screenshots/capture-photo-*.png` and `capture-url-*.png` each have ≥1 file with size > 0.
    - WALKTHROUGH cross-links inline per D-12 wherever findings overlap.
    - NO files modified under `frontend/` or `backend/`.
  </acceptance_criteria>

  <done>2 per-surface UI-REVIEW files committed at `.planning/v0.3/ui-reviews/{capture-photo,capture-url}-UI-REVIEW.md`; ~2-6 PNGs committed; D-13 docking applied for the 2 known-blocker pillars (capture-photo Pillar 6 for Sheet-01, capture-url Pillar 6 for URL-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor browser session ↔ prod synthetic household | Auditor uses an HttpOnly cookie session scoped to the `[SYNTHETIC] Démo Al Dente` household (from Phase 12 join via `DEMO01`). The audit READS this surface; it does not mutate. |
| `.planning/v0.3/ui-reviews/` ↔ git | Markdown + PNG artifacts are committed (per D-10). No secrets in scope; no PII (synthetic data only). |

## STRIDE Threat Register

(Phase 13 is audit-only — writes only to `.planning/`, reads from prod via the existing synthetic-household auditor session. No production exposure, no user-data touched, no code shipped. Threat surface is intentionally minimal.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | I (Information disclosure) | Screenshots committed under `.planning/v0.3/ui-reviews/screenshots/` | mitigate | Auditor is scoped to the `[SYNTHETIC] Démo Al Dente` household via the persistent Phase 12 cookie session. All 21 recipes, votes, cooking_logs in this scope are synthetic per Phase 11 D-04 (hard-refusal guard) and D-06 (scope assertion). Per CONTEXT.md hard constraint and Phase 12 specifics: DO NOT log out / DO NOT re-onboard with a new account — staying scoped means screenshots cannot capture real-user PII. Verify before commit: filenames + linked PNGs reference only synthetic surfaces. |
| T-13-02 | T (Tampering) | Product code in `frontend/` or `backend/` accidentally modified during audit | mitigate | Hard constraint in every task `<action>` block: NO `frontend/` changes, NO `backend/` changes, NO product-code changes. `feedback_executor_scope_creep.md` is the load-bearing memory anti-pattern. Verifiable at task close via `git status frontend/ backend/` showing clean (only `.planning/v0.3/ui-reviews/**` modified). |
</threat_model>

<verification>
After both tasks complete, verify:
- 5 per-surface UI-REVIEW files exist at `.planning/v0.3/ui-reviews/{capture-quick,capture-full,capture-voice,capture-photo,capture-url}-UI-REVIEW.md` (one per capture surface per CONTEXT D-05).
- Each contains the 5 mandatory sections in D-04 order: verdict tag → boilerplate/earned table → 6-pillar table → detailed findings → screenshots/WALKTHROUGH cross-links footer.
- All verdict tags match the fixed enum {`Feels Al Dente ✅` | `Mixed ⚠` | `Feels Generic ❌`} per D-01.
- 6-pillar tables include all 6 pillars (Copywriting, Visuals, Color, Typography, Spacing, Experience Design) with N/4 scores per pillar and a totalled /24.
- ≥1 screenshot per surface under `.planning/v0.3/ui-reviews/screenshots/` linked from the corresponding UI-REVIEW.
- D-13 score docking applied where WALKTHROUGH had blocker/friction findings (notably capture-photo Pillar 6 for Sheet-01, capture-url Pillar 6 for URL-01, capture-quick Pillar 1 for any generic copy findings, capture-full Pillar 6 for ingredient-parser blocker P-12-F01).
- `git status frontend/ backend/` shows clean — NO product code modified.
</verification>

<success_criteria>
Plan 13-01 complete when:
- 5 of the 14 surfaces (the capture surfaces, per CONTEXT D-07 sequencing) have committed UI-REVIEW.md files at `.planning/v0.3/ui-reviews/`.
- Each file follows the D-04 skeleton (verdict at top, 6-pillar score defending it).
- Each file has its 6-pillar score + originality verdict + boilerplate/earned table + ≥1 screenshot + WALKTHROUGH cross-links.
- Phase 13's calibration anchor established: capture surfaces scored consistently against the v0.2 22.4/24 baseline (per D-15) so Plans 13-02 and 13-03 can score the remaining 9 surfaces against the same standard.
- AUDIT-01, AUDIT-02, AUDIT-03 partially satisfied for the 5 capture surfaces (full satisfaction across all 14 surfaces happens in Plan 13-03; aggregator in Plan 13-04).
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-quality-originality-audit/13-01-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. The SUMMARY should list the 5 per-surface UI-REVIEW files created, the screenshot count per surface, the verdict-tag distribution (e.g. "3 Feels Al Dente ✅, 1 Mixed ⚠, 1 Feels Generic ❌"), and any cross-cutting observations the auditor noticed across the 5 capture surfaces (will feed into Plan 13-04's UI-AUDIT.md cross-cutting observations section per D-14).
</output>
</content>
</invoke>