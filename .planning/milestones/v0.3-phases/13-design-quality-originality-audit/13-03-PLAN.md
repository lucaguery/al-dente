---
phase: 13-design-quality-originality-audit
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - .planning/v0.3/ui-reviews/exports-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/push-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/realtime-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/onboarding-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/settings-UI-REVIEW.md
  - .planning/v0.3/ui-reviews/screenshots/exports-*.png
  - .planning/v0.3/ui-reviews/screenshots/push-*.png
  - .planning/v0.3/ui-reviews/screenshots/realtime-*.png
  - .planning/v0.3/ui-reviews/screenshots/onboarding-*.png
  - .planning/v0.3/ui-reviews/screenshots/settings-*.png
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]
tags: [audit, ui, visual, design]

must_haves:
  truths:
    - "Each of the 5 cross-cutting + thinner surfaces (exports, push, realtime, onboarding, settings) has a per-surface UI-REVIEW.md at .planning/v0.3/ui-reviews/<surface>-UI-REVIEW.md"
    - "Each per-surface UI-REVIEW carries an originality verdict tag from {Feels Al Dente ✅ / Mixed ⚠ / Feels Generic ❌}"
    - "Each per-surface UI-REVIEW includes a boilerplate|earned 2-column table"
    - "Each per-surface UI-REVIEW includes a 6-pillar table summing to N/24"
    - "Each scored surface has ≥1 screenshot linked from its UI-REVIEW (push may have only 1-2 per CONTEXT specifics; realtime audits 3 visual loci per CONTEXT specifics)"
    - "WALKTHROUGH-surfaced findings overlapping a pillar finding are cross-linked inline per D-12"
    - "push-UI-REVIEW.md notes per D-16 'Partially reached' if the auditor cannot trigger a notification send beyond subscription state"
    - "After this plan completes, all 14 surfaces from D-05 have a per-surface UI-REVIEW (5 + 4 + 5 = 14 — matches AUDIT-04 'one row per surface listed in WALK-01')"
  artifacts:
    - path: ".planning/v0.3/ui-reviews/exports-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for exports"
      contains: "## Originality Verdict"
    - path: ".planning/v0.3/ui-reviews/push-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for push (with D-16 partial-reach notation)"
      contains: "## Originality Verdict"
    - path: ".planning/v0.3/ui-reviews/realtime-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for realtime (3 cross-cutting visual loci per CONTEXT specifics)"
      contains: "## Originality Verdict"
    - path: ".planning/v0.3/ui-reviews/onboarding-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for onboarding (multi-screen flow covered in one file per D-05)"
      contains: "## Originality Verdict"
    - path: ".planning/v0.3/ui-reviews/settings-UI-REVIEW.md"
      provides: "Per-surface 6-pillar score + originality verdict for settings"
      contains: "## Originality Verdict"
  key_links:
    - from: ".planning/v0.3/ui-reviews/<surface>-UI-REVIEW.md"
      to: ".planning/v0.3/WALKTHROUGH.md §<surface>"
      via: "inline cross-link `(See WALKTHROUGH.md §<surface> — <finding-id>)` per D-12"
      pattern: "See WALKTHROUGH.md"
    - from: ".planning/v0.3/ui-reviews/<surface>-UI-REVIEW.md"
      to: ".planning/v0.3/ui-reviews/screenshots/<surface>-*.png"
      via: "relative markdown link"
      pattern: "./screenshots/.*\\.png"
---

<objective>
Score the 5 cross-cutting and thinner surfaces (exports, push, realtime, onboarding, settings) under the 6-pillar `/gsd-ui-review` rubric and produce a per-surface UI-REVIEW.md for each, anchored by Playwright MCP screenshots.

Purpose: closes the per-surface scoring set (5 from Plan 13-01 + 4 from Plan 13-02 + 5 from this plan = 14 surfaces matching CONTEXT D-05 + WALKTHROUGH §-headers exactly, satisfying AUDIT-04 "one row per surface listed in WALK-01"). Per CONTEXT D-07 these go third — natural product flow first (Plans 13-01/13-02), cross-cutting concerns at the end.

Output: 5 per-surface UI-REVIEW.md files + ~8-15 screenshots committed under `.planning/v0.3/ui-reviews/screenshots/`.

**Audit-only constraint:** identical to Plans 13-01/13-02. NO `frontend/` changes. NO `backend/` changes. NO product-code changes whatsoever (per `feedback_executor_scope_creep`).

**Why depends_on 13-02:** continued auditor session, continued calibration, sequential execution within the same MCP browser context for consistency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-design-quality-originality-audit/13-CONTEXT.md
@.planning/v0.3/WALKTHROUGH.md
@~/.claude/agents/gsd-ui-auditor.md
@CLAUDE.md
@.planning/phases/13-design-quality-originality-audit/13-01-SUMMARY.md
@.planning/phases/13-design-quality-originality-audit/13-02-SUMMARY.md

<interfaces>
<!-- Per-surface UI-REVIEW skeleton — IDENTICAL to Plans 13-01 / 13-02. Reproduced here for executor convenience. -->

```markdown
# UI Review — <Surface Name>

**Audited:** 2026-05-09
**Auditor:** Claude (Phase 13, manual scoring per CONTEXT.md D-06)
**Synthetic env:** [SYNTHETIC] Démo Al Dente @ https://al-dente-pink.vercel.app
**Viewport:** iPhone-shape Chromium 390×844 (isMobile, hasTouch)
**Reach status:** Reached | Partially reached (note what couldn't be observed) — per D-16

## Originality Verdict
**Verdict:** <Feels Al Dente ✅ | Mixed ⚠ | Feels Generic ❌>

| Boilerplate elements | Earned elements |
|----------------------|-----------------|
| <element> | <element> |

## 6-Pillar Score: N/24
| Pillar | Score | Key Finding |
|--------|-------|-------------|
| Copywriting | N/4 | ... |
| Visuals | N/4 | ... |
| Color | N/4 | ... |
| Typography | N/4 | ... |
| Spacing | N/4 | ... |
| Experience Design | N/4 | ... |

## Detailed Findings
(Order: lowest-scoring pillar first.)

## Screenshots
- `./screenshots/<surface>-<state>.png` — <caption>

## WALKTHROUGH cross-links (context inherited per D-11)
- WALKTHROUGH.md §<surface> — 2-3 line context summary
```

<!-- Surface → frontend code mapping for this plan's 5 surfaces. -->

| Surface | WALKTHROUGH §header | Frontend code paths | Notes |
|---------|---------------------|---------------------|-------|
| exports | `## Exports` (line ~702) | `frontend/app/settings/` (exports section), search for `Export\|export` in `frontend/components/` | Thin surface |
| push | `## Push` (line ~766) | `frontend/components/PushPermissionBanner.tsx`, search for `pushManager` / `subscribe` in `frontend/lib/` | Per Phase 12 D-19: subscription + 1 fired notification depth. Auditor may not be able to trigger send → D-16 "Partially reached" |
| realtime | `## Realtime Sync` (line ~876) | `frontend/components/RealtimeProvider.tsx`, `frontend/lib/realtime/` (if exists), `frontend/components/CookingBanner.tsx`, `frontend/components/RecipeDraftCard.tsx` (drafts inbox) | Cross-cutting — visual surface = inbox badge + drafts list updating + cooking banner appearing (3 loci per CONTEXT specifics) |
| onboarding | `## Onboarding` (line ~1011) | `frontend/app/onboarding/welcome/page.tsx`, `frontend/app/onboarding/create/page.tsx`, `frontend/app/onboarding/join/page.tsx`, `frontend/app/onboarding/share-code/page.tsx` | Multi-screen flow (4 screens) — covered in ONE UI-REVIEW per D-05 |
| settings | `## Settings` (line ~1094) | `frontend/app/settings/` (page.tsx + sub-routes) | Thin surface — WALKTHROUGH had "behaves as documented" so Phase 13 carries weight per CONTEXT specifics |

<!-- Per CONTEXT specifics: -->
<!-- - push: D-19 from Phase 12 — operator-confirmed; auditor may only see subscription card + permission state. Document per D-16. -->
<!-- - realtime: visual surface is inbox badge + drafts list + cooking banner — audit these 3 loci, not a single screen. -->
<!-- - settings: WALKTHROUGH was thin → Phase 13's UI-REVIEW carries more weight in Phase 14 ranking. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Score 3 cross-cutting surfaces (exports + push + realtime)</name>
  <files>
    .planning/v0.3/ui-reviews/exports-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/push-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/realtime-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/screenshots/exports-*.png,
    .planning/v0.3/ui-reviews/screenshots/push-*.png,
    .planning/v0.3/ui-reviews/screenshots/realtime-*.png
  </files>

  <read_first>
    1. `.planning/phases/13-design-quality-originality-audit/13-CONTEXT.md` — D-01..D-16 (especially D-16 reach-status notation for push, and CONTEXT specifics for realtime's 3 visual loci).
    2. `~/.claude/agents/gsd-ui-auditor.md` — 6-pillar rubric.
    3. `.planning/v0.3/WALKTHROUGH.md` — read JUST these surface sections just-in-time per D-11:
       - `## Exports` (lines ~702-765)
       - `## Push` (lines ~766-875)
       - `## Realtime Sync` (lines ~876-1010)
    4. Frontend code:
       - exports: search `frontend/app/settings/` for the exports surface; `grep -rn "Export\|export" frontend/components/ frontend/app/` to locate it
       - push: `frontend/components/PushPermissionBanner.tsx`, `grep -rn "pushManager\|subscribe" frontend/lib/ frontend/components/`
       - realtime: `frontend/components/RealtimeProvider.tsx`, plus the 3 visual loci per CONTEXT specifics — `frontend/components/CookingBanner.tsx` (banner appearing), `frontend/components/RecipeDraftCard.tsx` (drafts list updating), inbox badge (search `frontend/app/inbox/` and `frontend/components/BottomNav.tsx` for badge code)
    5. `frontend/app/globals.css` — Slow Food tokens (D-02a check).
    6. `CLAUDE.md` invariant #4 (realtime contract — `services/realtime.broadcast_to_household` is the spine; the frontend visual surface is what gets re-rendered when events arrive).
    7. `.planning/PROJECT.md` §"Surfaced for follow-up" — informs how to score push (D-19 limitation from Phase 12 may persist).
  </read_first>

  <action>
    Same audit-only sequence as Plans 13-01/13-02, applied to the 3 cross-cutting surfaces in WALKTHROUGH order: exports → push → realtime.

    **Specific notes per surface:**

    **exports:**
    - Thin surface per CONTEXT D-07. 1-2 screenshots may suffice (entry from settings, the export action UI).
    - WALKTHROUGH §Exports likely has thin context — Phase 13 carries more weight. Score the visual quality of what IS there: copy clarity, action affordance, post-click feedback (toast / file download / etc.).

    **push:**
    - Per Phase 12 D-19 and CONTEXT specifics: the auditor may only be able to capture the subscription prompt + permission state. The actual notification UI (system banner) is OS-rendered — not a frontend surface to audit visually.
    - Per D-16: mark `Reach status: Partially reached` and document what could be observed (PushPermissionBanner, permission grant flow, subscription state) and what couldn't (the OS notification UI itself).
    - Capture: (a) the PushPermissionBanner UI (canonical), (b) post-grant state if reproducible. 1-2 PNGs per CONTEXT specifics.
    - Per D-13: if WALKTHROUGH §Push surfaced friction (e.g. unclear copy, missing post-grant feedback), Pillar 1 / Pillar 6 docked accordingly.

    **realtime:**
    - Cross-cutting surface per CONTEXT specifics — visual surface is THREE loci: inbox badge, drafts list updating, cooking banner appearing. Capture screenshots of each locus where observable: (a) inbox badge with count, (b) drafts list mid-update or post-update with new draft visible, (c) cooking banner appearing.
    - 2-4 PNGs total per D-08 across the 3 loci.
    - Per D-15: realtime UI-REVIEW audits these 3 loci, NOT a single screen. The "surface" is conceptual — write the UI-REVIEW as 3 sub-sections inside the standard skeleton.
    - Per CLAUDE.md invariant #4: the realtime broadcast spine is `services/realtime.broadcast_to_household`. Frontend re-renders on event arrival via `RealtimeProvider.tsx`. Score what users see (badge update, list re-render, banner appear) — Pillar 6 (Experience Design) is the load-bearing pillar here (does state arrive coherently? does it feel responsive? are stale states handled?).
    - Per D-13: if WALKTHROUGH §Realtime Sync surfaced friction (event lag, missing transitions, stale-state bugs), Pillar 6 docked accordingly.

    **Standard sequence (identical to prior plans, repeat 3x):**
    1. Verify auditor session at `https://al-dente-pink.vercel.app`. Cookie persists from prior plans / Phase 12.
    2. Per surface: read WALKTHROUGH §section just-in-time → navigate + screenshot per D-08 → read frontend code → apply 6-pillar grep methods scoped to those files → form verdict per D-01/D-02/D-03 (with D-13 docking and D-16 reach-status notation where applicable) → write `<surface>-UI-REVIEW.md` per D-04 skeleton via `Write` tool → cross-link WALKTHROUGH per D-12.

    **HARD CONSTRAINTS:** Identical to Plans 13-01/13-02. NO `frontend/` or `backend/` modifications. NO new e2e specs. NO new GitHub issues. NO logging out / re-onboarding.
  </action>

  <verify>
    <automated>
      test -f .planning/v0.3/ui-reviews/exports-UI-REVIEW.md && \
      test -f .planning/v0.3/ui-reviews/push-UI-REVIEW.md && \
      test -f .planning/v0.3/ui-reviews/realtime-UI-REVIEW.md && \
      grep -lE "Feels Al Dente ✅|Mixed ⚠|Feels Generic ❌" .planning/v0.3/ui-reviews/exports-UI-REVIEW.md .planning/v0.3/ui-reviews/push-UI-REVIEW.md .planning/v0.3/ui-reviews/realtime-UI-REVIEW.md | wc -l | grep -q "^3$" && \
      grep -lE "Boilerplate elements" .planning/v0.3/ui-reviews/exports-UI-REVIEW.md .planning/v0.3/ui-reviews/push-UI-REVIEW.md .planning/v0.3/ui-reviews/realtime-UI-REVIEW.md | wc -l | grep -q "^3$" && \
      grep -lE "Copywriting.*\\|.*[1-4]/4" .planning/v0.3/ui-reviews/exports-UI-REVIEW.md .planning/v0.3/ui-reviews/push-UI-REVIEW.md .planning/v0.3/ui-reviews/realtime-UI-REVIEW.md | wc -l | grep -q "^3$" && \
      ls .planning/v0.3/ui-reviews/screenshots/exports-*.png 2>/dev/null | head -1 && \
      ls .planning/v0.3/ui-reviews/screenshots/push-*.png 2>/dev/null | head -1 && \
      ls .planning/v0.3/ui-reviews/screenshots/realtime-*.png 2>/dev/null | head -1
    </automated>
  </verify>

  <acceptance_criteria>
    - 3 UI-REVIEW files exist at `.planning/v0.3/ui-reviews/{exports,push,realtime}-UI-REVIEW.md`.
    - Each contains `## Originality Verdict`, verdict tag from the fixed enum, boilerplate/earned table with ≥1 row per column (or `None observed` justification per D-03), 6-pillar table with all 6 pillars scored N/4.
    - push-UI-REVIEW.md explicitly notes per D-16 the partial-reach status (subscription + permission observable; OS notification UI not auditable as a frontend surface).
    - realtime-UI-REVIEW.md audits 3 visual loci (inbox badge + drafts list + cooking banner) per CONTEXT specifics, with screenshots per locus where observable.
    - Each surface has ≥1 screenshot at `.planning/v0.3/ui-reviews/screenshots/{exports,push,realtime}-*.png` with size > 0.
    - WALKTHROUGH cross-links inline per D-12 wherever findings overlap.
    - NO files modified under `frontend/` or `backend/`.
  </acceptance_criteria>

  <done>3 per-surface UI-REVIEW files committed at `.planning/v0.3/ui-reviews/{exports,push,realtime}-UI-REVIEW.md`; ~4-8 PNGs committed (push thin, realtime spans 3 loci). D-13 docking + D-16 reach-status applied per surface. No product code modified.</done>
</task>

<task type="auto">
  <name>Task 2: Score 2 surfaces (onboarding + settings) — closes the 14-surface set</name>
  <files>
    .planning/v0.3/ui-reviews/onboarding-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/settings-UI-REVIEW.md,
    .planning/v0.3/ui-reviews/screenshots/onboarding-*.png,
    .planning/v0.3/ui-reviews/screenshots/settings-*.png
  </files>

  <read_first>
    1. `.planning/phases/13-design-quality-originality-audit/13-CONTEXT.md` — D-01..D-16 (especially D-05 multi-screen-per-file rule for onboarding's 4 screens; CONTEXT specifics on settings carrying more Phase 14 weight due to thin WALKTHROUGH).
    2. `~/.claude/agents/gsd-ui-auditor.md` — 6-pillar rubric.
    3. `.planning/v0.3/WALKTHROUGH.md` — read JUST these surface sections just-in-time:
       - `## Onboarding` (lines ~1011-1093)
       - `## Settings` (lines ~1094-end)
    4. Frontend code:
       - onboarding: `frontend/app/onboarding/welcome/page.tsx`, `frontend/app/onboarding/create/page.tsx`, `frontend/app/onboarding/join/page.tsx`, `frontend/app/onboarding/share-code/page.tsx`
       - settings: `frontend/app/settings/` (`ls` to find sub-routes; e.g. `frontend/app/settings/page.tsx`)
    5. `frontend/app/globals.css` — Slow Food tokens.
    6. `CLAUDE.md` invariant #6 (French-only via next-intl) — onboarding is the entry point; copy fidelity matters most here. Hardcoded English fallbacks in onboarding would be a high-impact Pillar 1 finding.
    7. `.planning/PROJECT.md` §"Surfaced for follow-up" — POLISH-01 (i18n sweep on partner-waiting strings) and POLISH-02 (Copy button on invite code) likely live in onboarding/share-code; cross-link if observed.
  </read_first>

  <action>
    Same audit-only sequence applied to the final 2 surfaces in WALKTHROUGH order: onboarding → settings.

    **Specific notes per surface:**

    **onboarding:**
    - Multi-screen surface per CONTEXT D-05 — 4 screens (welcome / create / join / share-code) covered in ONE UI-REVIEW with screenshots per screen.
    - Visiting requires the auditor to NOT log out (the synthetic auditor session is already past onboarding). Two options to capture:
      - Option A (preferred — non-invasive): Open the onboarding URLs directly in the SAME browser context (`/onboarding/welcome`, `/onboarding/create`, `/onboarding/join`, `/onboarding/share-code`). Some screens may redirect because the user is already authenticated → capture the redirect behavior as part of the audit (Pillar 6 finding: does the app handle "already onboarded" cleanly?).
      - Option B (only if A fails): Use a SECOND `mcp__playwright__browser_tabs` context with NO cookie (fresh anonymous context) to capture the onboarding flow as a new user would see it. Document the approach in the UI-REVIEW so it's reproducible.
    - DO NOT log out the auditor's primary session.
    - Capture: (a) welcome screen, (b) create-or-join branch, (c) join with invite-code state, (d) share-code post-create state. 3-4 PNGs.
    - Per CONTEXT specifics on POLISH-01: if WALKTHROUGH §Onboarding flagged untranslated partner-waiting strings, Pillar 1 docked. POLISH-02 (Copy button on invite code) is a Pillar 6 affordance gap — score accordingly.
    - File names: `onboarding-welcome.png`, `onboarding-create.png`, `onboarding-join.png`, `onboarding-share-code.png`.

    **settings:**
    - Thin surface per CONTEXT D-07. WALKTHROUGH was thin per CONTEXT specifics ("behaves as documented") — Phase 13's UI-REVIEW therefore CARRIES MORE WEIGHT in Phase 14 ranking. Score thoroughly even though there's less inherited context.
    - Capture: settings landing page + 1-2 sub-views if visually distinct (e.g. household-info, preferences, exports section if scoped here vs. its own surface). 2-3 PNGs.
    - Settings may overlap with exports surface (Plan 13-03 Task 1) — clarify scope: exports = the export action surface; settings = everything else. If they share a parent route, cross-reference between the two UI-REVIEWs.

    **Standard sequence (identical to prior tasks, repeat 2x):**
    1. Verify auditor session at `https://al-dente-pink.vercel.app`. Continue from prior task's MCP session.
    2. Per surface: read WALKTHROUGH §section just-in-time → navigate + screenshot per D-08 (onboarding 3-4 PNGs covering 4 screens, settings 2-3 PNGs) → read frontend code → apply 6-pillar grep methods → form verdict per D-01/D-02/D-03 (with D-13 docking) → write `<surface>-UI-REVIEW.md` per D-04 skeleton via `Write` tool → cross-link WALKTHROUGH per D-12.

    **CLOSING CHECK after both surfaces written:**
    Verify all 14 surface UI-REVIEWs now exist (5 from 13-01 + 4 from 13-02 + 5 from 13-03 = 14):
    ```bash
    ls .planning/v0.3/ui-reviews/*-UI-REVIEW.md | wc -l
    # Must output: 14
    ```
    This sets up Plan 13-04 (the UI-AUDIT.md aggregator) for completion of AUDIT-04.

    **HARD CONSTRAINTS:** Identical to prior plans. NO `frontend/` or `backend/` modifications. NO new e2e specs. NO new GitHub issues. NO logging out the auditor's primary session (Option B above uses a SEPARATE context if needed for onboarding, NOT a logout of the primary).
  </action>

  <verify>
    <automated>
      test -f .planning/v0.3/ui-reviews/onboarding-UI-REVIEW.md && \
      test -f .planning/v0.3/ui-reviews/settings-UI-REVIEW.md && \
      grep -lE "Feels Al Dente ✅|Mixed ⚠|Feels Generic ❌" .planning/v0.3/ui-reviews/onboarding-UI-REVIEW.md .planning/v0.3/ui-reviews/settings-UI-REVIEW.md | wc -l | grep -q "^2$" && \
      grep -lE "Boilerplate elements" .planning/v0.3/ui-reviews/onboarding-UI-REVIEW.md .planning/v0.3/ui-reviews/settings-UI-REVIEW.md | wc -l | grep -q "^2$" && \
      grep -lE "Copywriting.*\\|.*[1-4]/4" .planning/v0.3/ui-reviews/onboarding-UI-REVIEW.md .planning/v0.3/ui-reviews/settings-UI-REVIEW.md | wc -l | grep -q "^2$" && \
      ls .planning/v0.3/ui-reviews/screenshots/onboarding-*.png 2>/dev/null | head -1 && \
      ls .planning/v0.3/ui-reviews/screenshots/settings-*.png 2>/dev/null | head -1 && \
      [ "$(ls .planning/v0.3/ui-reviews/*-UI-REVIEW.md 2>/dev/null | wc -l | tr -d ' ')" = "14" ]
    </automated>
  </verify>

  <acceptance_criteria>
    - 2 UI-REVIEW files exist at `.planning/v0.3/ui-reviews/{onboarding,settings}-UI-REVIEW.md`.
    - Each contains `## Originality Verdict`, verdict tag, boilerplate/earned table, 6-pillar table summing to N/24.
    - onboarding-UI-REVIEW.md covers all 4 screens (welcome / create / join / share-code) per D-05 with screenshots per screen (≥3 PNGs).
    - settings-UI-REVIEW.md scored thoroughly per CONTEXT specifics (carries more weight due to thin WALKTHROUGH).
    - `.planning/v0.3/ui-reviews/screenshots/onboarding-*.png` (≥3 PNGs) and `settings-*.png` (≥1 PNG) committed.
    - WALKTHROUGH cross-links inline per D-12. Where POLISH-01 / POLISH-02 backlog items are observed, cross-link inline.
    - **CLOSING TOTAL**: `ls .planning/v0.3/ui-reviews/*-UI-REVIEW.md | wc -l` returns exactly 14 (matching CONTEXT D-05's 14-surface set, satisfying AUDIT-04's "one row per surface listed in WALK-01" precondition).
    - NO files modified under `frontend/` or `backend/`.
  </acceptance_criteria>

  <done>2 per-surface UI-REVIEW files committed at `.planning/v0.3/ui-reviews/{onboarding,settings}-UI-REVIEW.md`; all 14 surface UI-REVIEWs now exist; ~4-6 PNGs added in this task. AUDIT-01/02/03 fully satisfied across all 14 surfaces. Plan 13-04 can now produce the aggregator (AUDIT-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor browser session ↔ prod synthetic household | Continued cookie session from prior plans / Phase 12. Read-only audit. |
| (optional) fresh browser context for onboarding capture | A SECOND tab/context with NO cookie may be used to capture the onboarding flow as a new user would see it. This context joins NO household (auditor never completes onboarding in it). |
| `.planning/v0.3/ui-reviews/` ↔ git | Markdown + PNG artifacts committed per D-10. Synthetic data only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | I (Information disclosure) | Onboarding screenshots may show invite-code sharing UI | mitigate | The synthetic invite code `DEMO01` is already published in the synthetic env (Phase 11 deliverable) and committed to `.planning/phases/11-*/`. Capturing it in a screenshot does not leak any new information. Verify before commit: any captured invite codes match `DEMO01` (the synthetic), not a real-user code. If a fresh context is used (Option B in onboarding capture), it does NOT join a household — no real-user codes can be captured. |
| T-13-06 | T (Tampering) | Product code in `frontend/` or `backend/` accidentally modified | mitigate | Hard constraint in every task: NO product code changes. `feedback_executor_scope_creep.md` is the load-bearing memory. Verifiable via `git status frontend/ backend/` showing clean. |
| T-13-07 | I (Information disclosure) | Auditor accidentally logs out and re-onboards into a real account during onboarding capture | accept-with-mitigation | Hard constraint in `<action>`: DO NOT log out the primary session. If onboarding capture requires a fresh-user perspective, use a SEPARATE browser context (Option B), NOT a logout. The synthetic auditor session must persist across the entire phase (Phase 12 + Phase 13 plans). Mitigated by explicit instruction; risk is human-error class. |
</threat_model>

<verification>
After both tasks complete, verify:
- 5 per-surface UI-REVIEW files exist for this plan's surfaces at `.planning/v0.3/ui-reviews/{exports,push,realtime,onboarding,settings}-UI-REVIEW.md`.
- Combined with Plans 13-01 (5 files) and 13-02 (4 files), exactly 14 UI-REVIEW files total exist at `.planning/v0.3/ui-reviews/*-UI-REVIEW.md` — matching CONTEXT D-05's 14-surface set.
- All 14 verdict tags from the fixed enum per D-01.
- All 14 6-pillar tables present, summing to N/24.
- D-13 score docking + D-16 reach-status notation applied per surface where applicable (push partial reach, etc.).
- ≥1 screenshot per surface (40-60 PNGs total across the milestone per D-08 budget).
- `git status frontend/ backend/` shows clean — NO product code modified.
- AUDIT-01, AUDIT-02, AUDIT-03 fully satisfied at the per-surface level. AUDIT-04 awaits Plan 13-04's milestone aggregator.
</verification>

<success_criteria>
Plan 13-03 complete when:
- 5 of 14 surfaces (the cross-cutting + thinner ones — exports, push, realtime, onboarding, settings) have committed UI-REVIEW.md files.
- All 14 surface UI-REVIEW files exist after this plan completes (closes the per-surface scoring set).
- Each file follows the D-04 skeleton (verdict at top, 6-pillar score defending it).
- D-13 docking applied where WALKTHROUGH had findings.
- D-16 reach-status notation present in push-UI-REVIEW (partial reach for OS notification UI) and any other partial-reach surfaces.
- The set of 14 UI-REVIEW files is now ready to be aggregated by Plan 13-04 into the milestone-level UI-AUDIT.md (closing AUDIT-04).
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-quality-originality-audit/13-03-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. The SUMMARY should list the 5 per-surface UI-REVIEW files created in this plan, plus a "milestone scoring set summary": tally of all 14 UI-REVIEWs across Plans 13-01/13-02/13-03 with verdict-tag distribution, average 6-pillar score, lowest-scoring surface, highest-scoring surface, and surfaces marked "Partially reached" per D-16. This summary feeds directly into Plan 13-04's UI-AUDIT.md aggregator (D-14: cross-cutting observations section).
</output>
</content>
</invoke>