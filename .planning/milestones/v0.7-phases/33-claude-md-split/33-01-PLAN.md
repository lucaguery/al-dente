---
phase: 33-claude-md-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/CLAUDE.md
  - backend/CLAUDE.md
  - frontend/CLAUDE.md
  - frontend/AGENTS.md
  - CLAUDE.md
autonomous: true
requirements:
  - DX-01
user_setup: []

must_haves:
  truths:
    - "Locked artifacts (ROADMAP.md success criterion #3, REQUIREMENTS.md DX-01, PROJECT.md gh#27 row) reflect the AGENTS.md deletion override before the deletion lands (D-12)"
    - "The `gsd-docs-update` tooling refresh path is empirically tested against a non-root marker block, and the outcome decides whether `<!-- GSD:stack -->` + `<!-- GSD:conventions -->` markers move to scoped files or stay at root (D-04)"
    - "A plan-time mapping table covers every pre-split content unit (17 H2 sections + 3 bullets inside GSD:conventions + 1 highlight bullet bundle inside GSD:stack) with explicit `Destination file` and `Survives at root?` columns (D-13 Layer 1)"
    - "`.planning/CLAUDE.md` exists and contains the `<!-- GSD:workflow -->` block — only that — relocated from root (D-01)"
    - "`backend/CLAUDE.md` exists and leads with the Gemini SDK correction, followed by backend conventions bullets, backend stack versions, and the `alembic upgrade head` deploy line (D-06, D-11, D-07)"
    - "`frontend/CLAUDE.md` exists and leads with the Next.js 16 framework-version warning (folded from `frontend/AGENTS.md`), followed by Tests Playwright posture, then frontend conventions bullets, then frontend stack versions (D-05, D-10, D-12)"
    - "`frontend/AGENTS.md` is deleted from disk (D-12) — the previous `frontend/CLAUDE.md` `@AGENTS.md` re-export is replaced with real content in the same change"
    - "Root `CLAUDE.md` loses `## Current state`, `## Tests`, `## Gemini SDK`, and the `alembic upgrade head` sentence of `## Deployment` (D-05, D-06, D-07, D-08)"
    - "Root `CLAUDE.md` gains new Source-of-truth pointers to `backend/CLAUDE.md`, `frontend/CLAUDE.md`, and `.planning/CLAUDE.md`, and the existing `frontend/AGENTS.md` pointer is removed (D-12)"
    - "Root `CLAUDE.md` retains architecture invariants #1–#8 verbatim — invariants #2 (voting state computed) and #7 (single uvicorn worker + APScheduler) are NOT duplicated into `backend/CLAUDE.md` (D-11 reminder)"
    - "All Layer-2 grep gates from D-13 pass at plan close: zero backend tokens at root, zero frontend tokens at root, zero frontend tokens in `backend/CLAUDE.md`, zero backend tokens in `frontend/CLAUDE.md`, `frontend/AGENTS.md` not found, ≤ 60 lines of guidance at root excluding `<!-- GSD:* -->` blocks"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 33 success criterion #3 updated for AGENTS.md deletion override"
      contains: "frontend/AGENTS.md` is deleted"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DX-01 entry updated for AGENTS.md deletion override"
      contains: "Next.js 16 warning lives in `frontend/CLAUDE.md`"
    - path: ".planning/PROJECT.md"
      provides: "v0.7 locked-decisions table gh#27 row updated"
      contains: "Delete `frontend/AGENTS.md`"
    - path: ".planning/CLAUDE.md"
      provides: "GSD Workflow Enforcement block relocated from root"
      contains: "GSD:workflow-start"
    - path: "backend/CLAUDE.md"
      provides: "Backend-specific guidance: Gemini SDK + conventions + stack + alembic deploy line"
      contains: "google-genai"
    - path: "frontend/CLAUDE.md"
      provides: "Frontend-specific guidance: Next.js 16 warning + Tests + conventions + stack"
      contains: "node_modules/next/dist/docs"
    - path: "CLAUDE.md"
      provides: "Cross-cutting rules only: invariants, vocabularies, MVP posture, source-of-truth pointers"
      contains: "Architecture invariants"
  key_links:
    - from: "CLAUDE.md"
      to: "backend/CLAUDE.md"
      via: "Source of truth pointer list"
      pattern: "backend/CLAUDE.md"
    - from: "CLAUDE.md"
      to: "frontend/CLAUDE.md"
      via: "Source of truth pointer list"
      pattern: "frontend/CLAUDE.md"
    - from: "CLAUDE.md"
      to: ".planning/CLAUDE.md"
      via: "Source of truth pointer list"
      pattern: "\\.planning/CLAUDE\\.md"
---

<objective>
Restructure `CLAUDE.md` so the root file carries only cross-cutting rules (architecture invariants, locked vocabularies, MVP posture, source-of-truth pointers, GSD-managed cross-cutting blocks) and backend/frontend/GSD guidance lives in scoped files (`backend/CLAUDE.md`, `frontend/CLAUDE.md`, `.planning/CLAUDE.md`). Per D-12, `frontend/AGENTS.md` is deleted in the same change — its Next.js 16 warning is folded into the new `frontend/CLAUDE.md`.

Purpose: Shrink per-turn context cost of the root file (target ≤ 60 lines of guidance excluding `<!-- GSD:* -->` blocks). Scoped guidance loads only where Claude is working.

Output: Updated root `CLAUDE.md`, three new scoped CLAUDE.md files, deleted `frontend/AGENTS.md`, three locked-artifact edits (ROADMAP / REQUIREMENTS / PROJECT) reflecting the override, and a SUMMARY.md capturing the D-04 verification outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-claude-md-split/33-CONTEXT.md
@CLAUDE.md
@frontend/AGENTS.md
@frontend/CLAUDE.md

<mapping_table>
<!-- D-13 Layer 1: every pre-split content unit, explicit destination, explicit survives-at-root status. -->
<!-- Source-line numbers reference CLAUDE.md at HEAD (114 lines, see 33-CONTEXT.md §canonical_refs). -->

| Source line range | Content one-liner | Destination file | Survives at root? |
|-------------------|-------------------|------------------|-------------------|
| 1-3 | `# CLAUDE.md` heading + "Keep this file short" preamble | CLAUDE.md | Yes |
| 5-11 | `## Source of truth` — 5 bullets (PROJECT.md, STATE.md, SPEC.md, frontend/AGENTS.md, docs/design-system.html) | CLAUDE.md | Yes (frontend/AGENTS.md bullet removed per D-12; 3 new bullets added for scoped CLAUDE.md files) |
| 13-19 | `## Repo layout` — 3-bullet orientation | CLAUDE.md | Yes |
| 21-23 | `## Current state` — v0.5 shipped marker | DELETED | No (D-08) |
| 25-27 | `## MVP phase posture` — "no backward-compat shims" rule | CLAUDE.md | Yes |
| 29-40 | `## Architecture invariants` — 8 numbered invariants | CLAUDE.md | Yes (all 8 verbatim) |
| 42-44 | `## Locked vocabularies` — enum drift rule | CLAUDE.md | Yes |
| 46-48 | `## Productize-later TODOs` — `# TODO(productize)` convention | CLAUDE.md | Yes |
| 50-54 (sentence 1+3) | `## Deployment` — push-to-main + hosting | CLAUDE.md | Yes (D-07) |
| 50-54 (sentence 2) | `## Deployment` — Railway runs `alembic upgrade head` | backend/CLAUDE.md | No (D-07) |
| 56-58 | `## Tests` — Playwright posture | frontend/CLAUDE.md | No (D-05) |
| 60-62 | `## Gemini SDK` — `google-genai` not `google-generativeai` | backend/CLAUDE.md | No (D-06) |
| 64-68 | `<!-- GSD:project -->` block | CLAUDE.md | Yes (D-01) |
| 70-74 | `<!-- GSD:stack -->` block (single highlights paragraph) | frontend/CLAUDE.md + backend/CLAUDE.md (split by sentence) — fallback: root unchanged | Conditional on D-04 outcome |
| 76-82 bullet 1 | `<!-- GSD:conventions -->` frontend bullet (ESLint, strict TS, `@/*`, `--webpack`) | frontend/CLAUDE.md | Conditional on D-04 outcome (fallback: stays in root marker) |
| 76-82 bullet 2 | `<!-- GSD:conventions -->` backend bullet (`uv`, SQLAlchemy 2.0, Pydantic v2, APScheduler) | backend/CLAUDE.md | Conditional on D-04 outcome |
| 76-82 bullet 3 | `<!-- GSD:conventions -->` Comments bullet ("why not what") | CLAUDE.md | Yes (hand-written prose after split, per D-01) |
| 84-88 | `<!-- GSD:architecture -->` block | CLAUDE.md | Yes (D-01) |
| 90-94 | `<!-- GSD:skills -->` block | CLAUDE.md | Yes (D-01) |
| 96-107 | `<!-- GSD:workflow -->` block | .planning/CLAUDE.md | No (D-01, mandated by ROADMAP success criterion #4) |
| 109-114 | `<!-- GSD:profile -->` block | CLAUDE.md | Yes (D-01) |
| frontend/AGENTS.md L1-5 | Next.js 16 framework-version warning | frontend/CLAUDE.md | N/A (source file deleted per D-12) |
| frontend/CLAUDE.md (existing) | `@AGENTS.md` one-line re-export | OVERWRITTEN | No (replaced with real scoped content) |

</mapping_table>

<source_of_truth_pointer_rewrite>
<!-- The new pointer block for root CLAUDE.md §Source of truth. Replaces the existing 5-bullet list. -->
<!-- frontend/AGENTS.md line removed (D-12). 3 new scoped-file lines added. Order preserved otherwise. -->

- **`.planning/PROJECT.md`** — current state, milestone goals, requirements, key decisions. **Read first.** Refreshed via `/gsd-transition`.
- **`.planning/STATE.md`** — live position: current milestone, phase, plan, progress.
- **`SPEC.md`** — locked data model, capture pipeline, scoring algorithm, voting state machine, auth scheme, original 4-wave build plan. Read before designing new features. Note: the auth scheme there has been superseded — see invariant 8 below.
- **`backend/CLAUDE.md`** — backend-specific rules (SQLAlchemy 2.0 typed style, Alembic conventions, `uv` workflow, Gemini SDK correction, `alembic upgrade head` deploy contract). Load when working in `backend/`.
- **`frontend/CLAUDE.md`** — frontend-specific rules (Next.js 16 breaking changes, ESLint-as-formatter, `@/*` alias, `--webpack` build flag, Playwright posture). Load when working in `frontend/`.
- **`.planning/CLAUDE.md`** — GSD workflow enforcement. Load when working in `.planning/` or running GSD commands.
- **`docs/design-system.html`** — living design system reference (Sober Kitchen). Locked tokens (terracotta sober + Cormorant + Caveat), patine cards, table-à-manger voting, marginalia register, brand-mark loader, plus locked screens for Accueil / Bibliothèque / Recette with porting checklist. Open in browser before designing new UI; do not duplicate its decisions in ad-hoc CSS.

</source_of_truth_pointer_rewrite>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update locked artifacts for AGENTS.md override (D-12)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/phases/33-claude-md-split/33-CONTEXT.md (D-12 specifies the exact replacement text for all three files)
    - .planning/ROADMAP.md lines 76-86 (Phase 33 block — success criterion #3 to edit)
    - .planning/REQUIREMENTS.md lines 32-34 (DX-01 entry to edit)
    - .planning/PROJECT.md lines 19-32 (v0.7 Current Milestone block — line 20 "CLAUDE.md split" decision summary + line 31 gh#27 row)
  </read_first>
  <action>
    Edit three locked artifacts to reflect the AGENTS.md deletion override per D-12. This MUST happen before any actual file deletion/creation so the verifier and plan-checker don't fail against stale criteria.

    1. `.planning/ROADMAP.md` Phase 33 success criterion #3 (currently line 83): replace the text "`frontend/CLAUDE.md` exists and contains every frontend-specific rule that was in root (Next.js 16 breaking changes, ESLint-as-formatter, `@/*` alias, `--webpack` build flag rationale); `frontend/AGENTS.md` is untouched." with "`frontend/CLAUDE.md` exists and contains every frontend-specific rule that was in root (Next.js 16 breaking changes, ESLint-as-formatter, `@/*` alias, `--webpack` build flag rationale); `frontend/AGENTS.md` is deleted; its Next.js 16 warning is folded verbatim into `frontend/CLAUDE.md`."

    2. `.planning/REQUIREMENTS.md` DX-01 entry (currently line 34): replace the clause "`frontend/AGENTS.md` stays in place (cross-tool — Cursor / Aider read it)" with "`frontend/AGENTS.md` is deleted; its Next.js 16 warning lives in `frontend/CLAUDE.md`".

    3. `.planning/PROJECT.md` v0.7 locked-decisions table gh#27 row (currently line 31): replace "Keep `frontend/AGENTS.md` (cross-tool) **and** add `frontend/CLAUDE.md` referencing it" with "Delete `frontend/AGENTS.md`; fold its Next.js 16 warning into a new `frontend/CLAUDE.md`. Reverses the original cross-tool decision because Claude Code is the only assistant in active use as of v0.7." Also update the rationale column (third pipe-separated cell) to "Claude Code is the only AI assistant in active use; the AGENTS.md cross-tool convention is dead weight in this repo today (revisit gate: second AI assistant added to active workflow)."

    Do NOT touch any other text in these files. Do NOT create or delete any files in this task — file deletion happens in Task 5 after the scoped files exist.
  </action>
  <verify>
    <automated>
      grep -c "frontend/AGENTS.md\` is deleted" .planning/ROADMAP.md   # ≥ 1
      grep -c "Next.js 16 warning lives in \`frontend/CLAUDE.md\`" .planning/REQUIREMENTS.md   # ≥ 1
      grep -c "Delete \`frontend/AGENTS.md\`" .planning/PROJECT.md   # ≥ 1
      grep -c "stays in place (cross-tool" .planning/REQUIREMENTS.md   # 0 (old clause removed)
      grep -c "Keep \`frontend/AGENTS.md\` (cross-tool)" .planning/PROJECT.md   # 0 (old clause removed)
      test -f frontend/AGENTS.md   # MUST still exist — deletion is Task 5
    </automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 33 success criterion #3 now says "deleted" with the verbatim-folded clause.
    - REQUIREMENTS.md DX-01 no longer contains the "stays in place (cross-tool)" clause.
    - PROJECT.md gh#27 row decision column starts with "Delete `frontend/AGENTS.md`".
    - PROJECT.md gh#27 row rationale column references the "Claude Code is the only AI assistant" reason.
    - `frontend/AGENTS.md` still exists on disk (deletion is Task 5).
    - No other text in any of the three files was modified.
  </acceptance_criteria>
  <done>All three locked artifacts updated. Phase 33 plan-checker and verifier can now run against the new criteria without false negatives.</done>
</task>

<task type="auto">
  <name>Task 2: Verify gsd-docs-update behavior at non-root paths (D-04)</name>
  <files>frontend/CLAUDE.md, backend/CLAUDE.md, .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md</files>
  <read_first>
    - .planning/phases/33-claude-md-split/33-CONTEXT.md §D-04 (verification protocol + fallback)
    - CLAUDE.md lines 64-114 (the 7 GSD-managed marker blocks at root — the working pattern to mirror)
    - Look in `$HOME/.claude/get-shit-done/` for any docs-update skill, command, or workflow file: try `ls $HOME/.claude/get-shit-done/workflows/` and `grep -rln "GSD:" $HOME/.claude/get-shit-done/` to locate the refresh-path code.
  </read_first>
  <action>
    Empirically determine whether `gsd-docs-update` (whatever form the skill takes — slash command, workflow markdown, or hand-run grep+rewrite) can refresh `<!-- GSD:* -->` marker blocks located at non-root paths (`backend/CLAUDE.md`, `frontend/CLAUDE.md`).

    Protocol:

    1. Create a TEMPORARY probe file at `backend/CLAUDE.md` containing only:
       ```
       <!-- GSD:stack-start source:codebase/STACK.md -->
       ## Technology Stack — probe
       PROBE-MARKER-7733 (this text will be overwritten by gsd-docs-update if it finds the block)
       <!-- GSD:stack-end -->
       ```
       Do the same at `frontend/CLAUDE.md` (replacing the existing `@AGENTS.md` one-liner) using `<!-- GSD:conventions-start source:CONVENTIONS.md -->` ... `<!-- GSD:conventions-end -->` with a `PROBE-MARKER-7733` body.

    2. Invoke the `gsd-docs-update` refresh path. Methods to try in order:
       a. If a `/gsd-docs-update` slash command exists, invoke it from this turn (note: the executor may need to surface this as a checkpoint if invoking it requires a separate user-initiated command).
       b. If a `gsd-sdk` CLI handler exists for it (try `gsd-sdk query docs.update --help` or similar), invoke it directly.
       c. Inspect `$HOME/.claude/get-shit-done/workflows/docs-update.md` (or similar) to learn the refresh algorithm and replicate it manually (read source files at `.planning/codebase/STACK.md` and `.planning/codebase/CONVENTIONS.md`, regenerate the block bodies).

    3. After invocation, check whether `PROBE-MARKER-7733` was overwritten:
       - `grep -c "PROBE-MARKER-7733" backend/CLAUDE.md` → 0 means tool found and refreshed the block (PASS).
       - `grep -c "PROBE-MARKER-7733" frontend/CLAUDE.md` → 0 means same (PASS).
       - Either still containing the probe marker means the tool only knows about root `CLAUDE.md` (FAIL → D-04 fallback applies).

    4. Record outcome at `.planning/phases/33-claude-md-split/33-VERIFICATION-D04.md`. File format:
       ```
       # D-04 Verification — gsd-docs-update at non-root paths

       **Date:** {YYYY-MM-DD}
       **Outcome:** PASS | FAIL

       ## Method invoked
       {a/b/c from protocol above + exact command(s) run}

       ## Result
       - `backend/CLAUDE.md` probe overwritten? {yes/no}
       - `frontend/CLAUDE.md` probe overwritten? {yes/no}

       ## Decision
       - If PASS: Task 3 splits GSD:stack + GSD:conventions bullets into scoped files (paths in mapping table apply as written).
       - If FAIL: Task 6 keeps GSD:stack and GSD:conventions blocks at root unchanged; Task 3 omits the stack + conventions bullets from scoped files (only Tests/Gemini/alembic/Next.js-warning move).
       ```

    5. After recording the outcome, REMOVE the probe content from `backend/CLAUDE.md` and `frontend/CLAUDE.md` — both files will be rewritten with real content in Tasks 3 and 4. (Do not delete the files themselves — leave them empty or with a single placeholder comment.)

    Capture the PASS/FAIL outcome — it gates the shape of Tasks 3, 4, and 6.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md   # file exists
      grep -cE "^\*\*Outcome:\*\* (PASS|FAIL)$" .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md   # ≥ 1
      grep -c "PROBE-MARKER-7733" backend/CLAUDE.md frontend/CLAUDE.md   # 0 (probe content removed before next task)
    </automated>
  </verify>
  <acceptance_criteria>
    - `33-VERIFICATION-D04.md` exists with an explicit PASS or FAIL line.
    - Method invoked is documented (which of a/b/c, with command/file names).
    - Probe content `PROBE-MARKER-7733` no longer appears in either scoped file.
    - The PASS/FAIL outcome is unambiguous — Task 3's branching behavior depends on it.
  </acceptance_criteria>
  <done>D-04 verification outcome is on disk. Tasks 3, 4, 6 know which branch (full split vs. fallback) to follow.</done>
</task>

<task type="auto">
  <name>Task 3: Create scoped CLAUDE.md files (backend, frontend, .planning)</name>
  <files>backend/CLAUDE.md, frontend/CLAUDE.md, .planning/CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the pre-split root file — Tasks 3+6 move content out of it)
    - frontend/AGENTS.md (5-line Next.js 16 warning to fold into frontend/CLAUDE.md verbatim)
    - .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md (Task 2 output — determines whether GSD:stack + GSD:conventions bullets move here or stay at root)
    - .planning/phases/33-claude-md-split/33-CONTEXT.md §D-10, §D-11, §specifics (file structure and opening lines)
  </read_first>
  <action>
    Write the three scoped CLAUDE.md files. The exact body of `backend/CLAUDE.md` and `frontend/CLAUDE.md` depends on the D-04 outcome captured in Task 2:

    **`.planning/CLAUDE.md`** (D-04 outcome irrelevant — always the same shape):
    ```
    # .planning/CLAUDE.md

    Guidance for Claude Code working in `.planning/` or invoking GSD commands.

    <!-- GSD:workflow-start source:GSD defaults -->
    ## GSD Workflow Enforcement

    Before using Edit, Write, or other file-changing tools, start work through a GSD command so planning artifacts and execution context stay in sync.

    Use these entry points:
    - `/gsd-quick` for small fixes, doc updates, and ad-hoc tasks
    - `/gsd-debug` for investigation and bug fixing
    - `/gsd-execute-phase` for planned phase work

    Do not make direct repo edits outside a GSD workflow unless the user explicitly asks to bypass it.
    <!-- GSD:workflow-end -->
    ```

    **`backend/CLAUDE.md`** — opens with the Gemini SDK correction (most training-data-misaligned content first per D-11), then conventions, then stack, then the alembic deploy line. Shape:

    ```
    # backend/CLAUDE.md

    Guidance for Claude Code working in `backend/`. Loaded when the working directory is under `backend/`.

    ## Gemini SDK

    Uses **`google-genai`** (the new unified Google AI SDK), not the legacy `google-generativeai`. Imports look like `from google import genai`. If your training data references `google.generativeai`, that's the wrong SDK for this repo.

    ## Deployment

    Railway runs `alembic upgrade head` before uvicorn restart on each deploy.
    ```

    If D-04 PASSED, also append (in this order):

    ```
    <!-- GSD:conventions-start source:CONVENTIONS.md -->
    ## Conventions

    - **Backend:** `uv`-managed Python 3.12. SQLAlchemy 2.0 typed style. Pydantic v2. APScheduler in-process (single worker — invariant 7).
    <!-- GSD:conventions-end -->

    <!-- GSD:stack-start source:codebase/STACK.md -->
    ## Technology Stack

    Authoritative backend versions live in `backend/pyproject.toml`. Highlights: FastAPI + SQLAlchemy 2.0 + Alembic + google-genai + APScheduler + pywebpush; Python 3.12 via `uv`.
    <!-- GSD:stack-end -->
    ```

    If D-04 FAILED, omit those two GSD-marker blocks from `backend/CLAUDE.md` entirely (they stay at root unchanged in Task 6).

    **`frontend/CLAUDE.md`** — opens with the Next.js 16 framework-version warning folded from `frontend/AGENTS.md` (most training-data-misaligned content first per D-10), then Tests, then conventions, then stack. The current `frontend/CLAUDE.md` is a one-line `@AGENTS.md` import — overwrite the entire file. Shape:

    ```
    # frontend/CLAUDE.md

    Guidance for Claude Code working in `frontend/`. Loaded when the working directory is under `frontend/`.

    <!-- BEGIN:nextjs-agent-rules -->
    ## This is NOT the Next.js you know

    This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
    <!-- END:nextjs-agent-rules -->

    ## Tests

    `@playwright/test` is wired; specs in `frontend/tests/e2e/`, config in `frontend/playwright.config.ts`. **v0.2.1 Phase 10 is the milestone expanding this** — committed full-screen coverage plus an idempotent backend seed (`uv run seed`). No Python test runner yet.
    ```

    If D-04 PASSED, also append (in this order):

    ```
    <!-- GSD:conventions-start source:CONVENTIONS.md -->
    ## Conventions

    - **Frontend:** ESLint flat config (`frontend/eslint.config.mjs`) is the formatter authority — no Prettier. TypeScript strict. Path alias `@/*` → `frontend/`. Build is `next build --webpack` intentionally (not Turbopack).
    <!-- GSD:conventions-end -->

    <!-- GSD:stack-start source:codebase/STACK.md -->
    ## Technology Stack

    Authoritative frontend versions live in `frontend/package.json`. Highlights: Next.js 16.2.4 + React 19.2.4 + Tailwind v4 + shadcn/ui + framer-motion + next-pwa + next-intl; Node 20+.
    <!-- GSD:stack-end -->
    ```

    If D-04 FAILED, omit those two GSD-marker blocks from `frontend/CLAUDE.md` entirely.

    The `<!-- BEGIN:nextjs-agent-rules -->` markers are preserved verbatim in `frontend/CLAUDE.md` (33-CONTEXT.md §code_context option (a) — cleanest for future tooling). The `## This is NOT the Next.js you know` H2 heading replaces the H1 from the old `frontend/AGENTS.md` (so the scoped CLAUDE.md has exactly one H1).

    Do NOT add architecture invariants #2 or #7 to `backend/CLAUDE.md` — they stay at root (D-11 reminder). The grep gate in Task 7 enforces this.

    Do NOT delete `frontend/AGENTS.md` in this task — that's Task 5, after Task 4 prunes the root file's pointer to it.
  </action>
  <verify>
    <automated>
      test -f .planning/CLAUDE.md
      test -f backend/CLAUDE.md
      test -f frontend/CLAUDE.md
      grep -c "GSD:workflow-start" .planning/CLAUDE.md   # ≥ 1
      grep -c "google-genai" backend/CLAUDE.md   # ≥ 1
      grep -c "alembic upgrade head" backend/CLAUDE.md   # ≥ 1
      grep -c "node_modules/next/dist/docs" frontend/CLAUDE.md   # ≥ 1
      grep -c "playwright" frontend/CLAUDE.md   # ≥ 1
      # Invariants stay at root, not duplicated:
      grep -c "Voting state is computed" backend/CLAUDE.md   # 0
      grep -c "Single uvicorn worker" backend/CLAUDE.md   # 0
      # If D-04 PASSED, marker blocks live in scoped files:
      # (executor: assert against the recorded outcome — PASS implies ≥1 GSD:conventions-start in both backend and frontend; FAIL implies 0)
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/CLAUDE.md` contains the GSD:workflow marker pair + body, nothing else of substance.
    - `backend/CLAUDE.md` leads with the Gemini SDK H2, followed by the Deployment H2 with the `alembic upgrade head` sentence.
    - `frontend/CLAUDE.md` leads with the `<!-- BEGIN:nextjs-agent-rules -->` block (with H2 heading), followed by the Tests H2.
    - Architecture invariants #2 and #7 do NOT appear in `backend/CLAUDE.md` (no "Voting state is computed" / "Single uvicorn worker" substrings).
    - If D-04 PASSED (per VERIFICATION-D04.md): `backend/CLAUDE.md` and `frontend/CLAUDE.md` each contain one `<!-- GSD:conventions-start -->` block and one `<!-- GSD:stack-start -->` block.
    - If D-04 FAILED: neither scoped file contains any GSD: marker; those blocks stay at root in Task 6.
    - `frontend/AGENTS.md` still exists on disk (deletion is Task 5).
  </acceptance_criteria>
  <done>Three scoped CLAUDE.md files exist with correct content per D-04 outcome. Root pruning can proceed in Task 4.</done>
</task>

<task type="auto">
  <name>Task 4: Prune root CLAUDE.md to cross-cutting rules only</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the file being pruned — read the entire pre-split file to confirm line numbers before editing)
    - .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md (Task 2 output — determines whether GSD:stack + GSD:conventions blocks stay at root or are removed)
    - .planning/phases/33-claude-md-split/33-CONTEXT.md §specifics (target root shape) + §decisions (D-01, D-05, D-06, D-07, D-08)
  </read_first>
  <action>
    Edit root `CLAUDE.md` to remove all backend-, frontend-, GSD-workflow-, and stale-state content. Add new Source-of-truth pointers for the scoped files.

    Concrete edits:

    1. **Replace the §Source of truth bullet list** (current lines 5-11) with the new pointer block from the `<source_of_truth_pointer_rewrite>` section in `<context>` above (5 → 7 bullets: removes `frontend/AGENTS.md`, adds `backend/CLAUDE.md` / `frontend/CLAUDE.md` / `.planning/CLAUDE.md`).

    2. **Delete §Current state** (current lines 21-23, the "v0.5 (Mixed Sweep) shipped 2026-05-13" block). Per D-08, this static line goes stale on every milestone and `.planning/STATE.md` is already the live source.

    3. **Edit §Deployment** (current lines 50-54). After edit, the section must contain only:
       ```
       ## Deployment

       - **Push to `main` is the only deploy path.** Both apps auto-deploy in ~60s. **Never run `vercel --prod` or manual Railway deploys.**
       - Hosting: Vercel (frontend, free) + Railway (backend, ~$5/mo) + Supabase (Postgres + Storage, free). Couple-scale workload assumed throughout.
       ```
       The middle bullet ("Railway runs `alembic upgrade head`...") is removed (moved to `backend/CLAUDE.md` in Task 3, per D-07).

    4. **Delete §Tests** (current lines 56-58). Moved to `frontend/CLAUDE.md` per D-05.

    5. **Delete §Gemini SDK** (current lines 60-62). Moved to `backend/CLAUDE.md` per D-06.

    6. **Edit the `<!-- GSD:conventions -->` block** (current lines 76-82). After edit, the block must contain ONLY the comments bullet:
       ```
       <!-- GSD:conventions-start source:CONVENTIONS.md -->
       ## Conventions

       - **Comments:** Explain *why*, not *what*. Document non-obvious business logic, workarounds, and known limitations only.
       <!-- GSD:conventions-end -->
       ```
       The frontend bullet and backend bullet are removed (moved to scoped files in Task 3 if D-04 PASSED).

       Per D-01, the comments bullet stays at root inside the marker (a single-bullet block is still refresh-safe). Note: this means `<!-- GSD:conventions -->` stays at root either way — only its frontend/backend bullets move conditionally on D-04.

    7. **If D-04 PASSED** (per VERIFICATION-D04.md), DELETE the entire `<!-- GSD:stack -->` block at root (current lines 70-74). The frontend half + backend half now live in the scoped files. The remaining root structure goes from GSD:project → GSD:stack → GSD:conventions → GSD:architecture → GSD:skills → GSD:workflow → GSD:profile to GSD:project → GSD:conventions → GSD:architecture → GSD:skills → GSD:profile (GSD:stack removed, GSD:workflow removed in step 8).

    8. **Delete the `<!-- GSD:workflow -->` block** (current lines 96-107). Moved to `.planning/CLAUDE.md` per D-01 + ROADMAP success criterion #4.

    Architecture invariants #1–#8 (current lines 29-40) MUST remain verbatim. Locked vocabularies, MVP posture, Repo layout, Productize-later TODOs all stay unchanged. GSD:project, GSD:architecture, GSD:skills, GSD:profile blocks all stay at root unchanged (D-01).

    After editing, the natural shape of root `CLAUDE.md` is:
    `# CLAUDE.md` + preamble → §Source of truth (7 bullets) → §Repo layout → §MVP phase posture → §Architecture invariants (8 numbered) → §Locked vocabularies → §Productize-later TODOs → §Deployment (push + hosting) → GSD:project → GSD:conventions (comments-only) → GSD:architecture → GSD:skills → GSD:profile.
    (If D-04 FAILED, GSD:stack stays at root unchanged and GSD:conventions still holds all 3 bullets — see Task 6 conditional behavior.)

    Per D-13's wc gate, total lines OUTSIDE `<!-- GSD:* -->` blocks must be ≤ 60. Count by hand if unsure; the gate in Task 7 enforces it.
  </action>
  <verify>
    <automated>
      grep -c "v0.5 (Mixed Sweep)" CLAUDE.md   # 0
      grep -c "## Tests" CLAUDE.md   # 0
      grep -c "## Gemini SDK" CLAUDE.md   # 0
      grep -c "alembic upgrade head" CLAUDE.md   # 0
      grep -c "GSD:workflow-start" CLAUDE.md   # 0
      grep -c "backend/CLAUDE.md" CLAUDE.md   # ≥ 1 (new Source of truth pointer)
      grep -c "frontend/CLAUDE.md" CLAUDE.md   # ≥ 1
      grep -c "\.planning/CLAUDE\.md" CLAUDE.md   # ≥ 1
      grep -c "frontend/AGENTS\.md" CLAUDE.md   # 0 (old pointer removed)
      grep -c "Architecture invariants" CLAUDE.md   # ≥ 1 (still at root)
      grep -c "Locked vocabularies" CLAUDE.md   # ≥ 1 (still at root)
      grep -c "MVP phase posture" CLAUDE.md   # ≥ 1 (still at root)
    </automated>
  </verify>
  <acceptance_criteria>
    - All 5 deletions land (Current state, Tests, Gemini SDK, alembic deploy line, GSD:workflow).
    - Source-of-truth block has 7 bullets including 3 scoped CLAUDE.md pointers; no `frontend/AGENTS.md` pointer remains.
    - Architecture invariants #1–#8 are byte-for-byte unchanged from pre-split.
    - GSD:conventions block at root retains only the comments bullet.
    - If D-04 PASSED: GSD:stack block is deleted from root.
    - If D-04 FAILED: GSD:stack block remains at root unchanged.
    - File still parses as valid markdown (single H1, ordered H2 sections).
  </acceptance_criteria>
  <done>Root CLAUDE.md contains only cross-cutting rules. Counted lines outside marker blocks fit the ≤ 60 target (verified by Task 7 gate).</done>
</task>

<task type="auto">
  <name>Task 5: Delete frontend/AGENTS.md</name>
  <files>frontend/AGENTS.md</files>
  <read_first>
    - frontend/AGENTS.md (the file being deleted — confirm its 4-line content is now present in frontend/CLAUDE.md before deletion)
    - frontend/CLAUDE.md (confirm the Next.js 16 warning was folded in by Task 3)
    - CLAUDE.md (confirm the §Source of truth bullet for `frontend/AGENTS.md` was removed in Task 4)
  </read_first>
  <action>
    Delete `frontend/AGENTS.md` from disk using `git rm frontend/AGENTS.md`. Per D-12, the file is no longer needed — its Next.js 16 warning has been folded verbatim into `frontend/CLAUDE.md` (Task 3) and the root CLAUDE.md pointer to it has been removed (Task 4).

    This is a single command:
    ```bash
    git rm frontend/AGENTS.md
    ```

    No content edits, no other file touched.
  </action>
  <verify>
    <automated>
      test ! -f frontend/AGENTS.md   # file gone from working tree
      git status --short frontend/AGENTS.md | grep -c "^D"   # 1 (deletion staged)
      grep -c "This is NOT the Next.js you know" frontend/CLAUDE.md   # ≥ 1 (content preserved in scoped file)
    </automated>
  </verify>
  <acceptance_criteria>
    - `frontend/AGENTS.md` no longer exists in the working tree.
    - The deletion is staged for the commit.
    - `frontend/CLAUDE.md` still contains the Next.js 16 warning (the H2 heading "This is NOT the Next.js you know" is the source-of-truth substring).
    - No other file modified by this task.
  </acceptance_criteria>
  <done>frontend/AGENTS.md is gone. The override per D-12 is complete on disk.</done>
</task>

<task type="auto">
  <name>Task 6: D-04 fallback — leave GSD:stack and GSD:conventions full bullets at root if verification failed</name>
  <files>CLAUDE.md (conditional — only if D-04 FAILED)</files>
  <read_first>
    - .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md (the gate — if Outcome is PASS, skip this task entirely)
    - CLAUDE.md (current state after Task 4)
  </read_first>
  <action>
    Conditional task. Read `33-VERIFICATION-D04.md`:

    - If **Outcome: PASS**, this task is a no-op. Skip immediately and move to Task 7.

    - If **Outcome: FAIL**, restore the frontend and backend bullets inside the root `<!-- GSD:conventions -->` block — Task 4 left only the comments bullet, but the fallback requires all 3 bullets at root since the tool can't refresh scoped marker blocks. The block at root after restore must read:
      ```
      <!-- GSD:conventions-start source:CONVENTIONS.md -->
      ## Conventions

      - **Frontend:** ESLint flat config (`frontend/eslint.config.mjs`) is the formatter authority — no Prettier. TypeScript strict. Path alias `@/*` → `frontend/`. Build is `next build --webpack` intentionally (not Turbopack).
      - **Backend:** `uv`-managed Python 3.12. SQLAlchemy 2.0 typed style. Pydantic v2. APScheduler in-process (single worker — invariant 7).
      - **Comments:** Explain *why*, not *what*. Document non-obvious business logic, workarounds, and known limitations only.
      <!-- GSD:conventions-end -->
      ```

      The GSD:stack block at root is already untouched by Task 4 under D-04 FAIL, so no edit needed there.

      Acknowledge in the SUMMARY (Task 8) that the D-04 fallback applied and grep gates in Task 7 are run against the relaxed expectations (frontend tokens may legitimately appear at root inside the GSD:conventions block — the Task 7 grep gates are written to tolerate this case for `eslint.config.mjs`, `@/*`, `--webpack` only when D-04 FAILED).
  </action>
  <verify>
    <automated>
      # If D-04 PASS: this task is a no-op; the assertion is informational only.
      # If D-04 FAIL: all 3 bullets present at root.
      grep -c "Outcome:\*\* PASS" .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md || true
      # The Task 7 gate runs the conditional grep with awareness of this outcome.
    </automated>
  </verify>
  <acceptance_criteria>
    - If D-04 PASS: no file modified; task documented as no-op.
    - If D-04 FAIL: root GSD:conventions block has all 3 bullets restored; GSD:stack block at root unchanged.
    - Either way, the outcome is logged in 33-VERIFICATION-D04.md.
  </acceptance_criteria>
  <done>Conditional fallback applied (or skipped). The repo state matches the D-04 outcome's contract.</done>
</task>

<task type="auto">
  <name>Task 7: Run D-13 Layer 2 grep gates + line-count gate</name>
  <files>.planning/phases/33-claude-md-split/33-VERIFICATION-GATES.md</files>
  <read_first>
    - .planning/phases/33-claude-md-split/33-CONTEXT.md §D-13 (exact gate commands)
    - .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md (determines whether frontend/backend tokens may legitimately appear at root inside the GSD:conventions block)
    - CLAUDE.md, backend/CLAUDE.md, frontend/CLAUDE.md (the files being graded)
  </read_first>
  <action>
    Run every D-13 Layer 2 grep gate and record pass/fail at `.planning/phases/33-claude-md-split/33-VERIFICATION-GATES.md`. All gates must PASS (or be explicitly waived per the D-04 fallback) before commit.

    Gates to run, in order:

    1. **Backend tokens at root must be zero** (excluding architecture invariants #2 and #7 wording which legitimately uses `uvicorn` and `APScheduler`):
       ```bash
       grep -cE "SQLAlchemy|google-genai|google\.genai|^.*alembic upgrade head" CLAUDE.md
       ```
       The grep is content-substring-based. Expected: 0.

       For `uvicorn` and `APScheduler` (which appear inside invariant #7), use the more precise:
       ```bash
       grep -vE "^[[:space:]]*[0-9]+\. \*\*Single uvicorn worker" CLAUDE.md | grep -c "uvicorn"
       ```
       Expected: 0 (any `uvicorn` mention outside the invariant header line is a leak).

       Note: the original D-13 grep `grep -rn "SQLAlchemy\|alembic\|uvicorn\|APScheduler\|google-genai\|google.genai" CLAUDE.md` is too loose because invariant #7 legitimately uses `uvicorn` and `APScheduler`. Use the refined gates above.

    2. **Frontend tokens at root must be zero** (D-04 PASS case) OR **frontend tokens at root only inside the GSD:conventions block** (D-04 FAIL case):
       ```bash
       grep -cE "@/\*|eslint\.config\.mjs|--webpack|Playwright|next-pwa" CLAUDE.md
       ```
       D-04 PASS: expected 0. D-04 FAIL: expected ≥ 0 but `grep -B1 "eslint.config.mjs" CLAUDE.md | grep -c "GSD:conventions"` must be ≥ 1 (proving the leak is contained to the marker block).

    3. **Frontend tokens at root in pointer text are allowed.** The string `frontend/CLAUDE.md` legitimately appears in §Source of truth and is not a leak. Gates above use specific keywords that don't collide.

    4. **Backend file purity** — no frontend tokens in `backend/CLAUDE.md`:
       ```bash
       grep -cE "@/\*|--webpack|ESLint|Playwright" backend/CLAUDE.md
       ```
       Expected: 0.

    5. **Frontend file purity** — no backend tokens in `frontend/CLAUDE.md`:
       ```bash
       grep -cE "SQLAlchemy|alembic" frontend/CLAUDE.md
       ```
       Expected: 0.

    6. **AGENTS.md gone:**
       ```bash
       test ! -f frontend/AGENTS.md && echo "PASS" || echo "FAIL"
       ```

    7. **Invariants not duplicated into backend:**
       ```bash
       grep -cE "Voting state is computed|Single uvicorn worker.*APScheduler" backend/CLAUDE.md
       ```
       Expected: 0.

    8. **Root line-count gate** — count lines outside `<!-- GSD:* -->` blocks; result ≤ 60:
       ```bash
       awk '/<!-- GSD:.*-start/{skip=1} /<!-- GSD:.*-end/{skip=0; next} !skip' CLAUDE.md | grep -v "^[[:space:]]*$" | wc -l
       ```
       Expected: ≤ 60 lines of non-blank content outside marker blocks. (If the count includes the H1 + preamble, that's intentional — they count toward guidance.)

    9. **Locked artifacts updated** (re-verification of Task 1):
       ```bash
       grep -c "frontend/AGENTS\.md\` is deleted" .planning/ROADMAP.md   # ≥ 1
       grep -c "stays in place (cross-tool" .planning/REQUIREMENTS.md   # 0
       grep -c "Delete \`frontend/AGENTS\.md\`" .planning/PROJECT.md   # ≥ 1
       ```

    Record outcomes at `33-VERIFICATION-GATES.md`. Format:
    ```
    # D-13 Layer 2 Verification — Phase 33 grep gates

    **Date:** {YYYY-MM-DD}
    **D-04 outcome:** PASS | FAIL (referenced from 33-VERIFICATION-D04.md)

    ## Gate results

    | # | Gate | Command | Expected | Actual | Status |
    |---|------|---------|----------|--------|--------|
    | 1 | Backend tokens at root | {cmd} | 0 | {n} | PASS/FAIL |
    | ... | ... | ... | ... | ... | ... |

    ## Final verdict

    {PASS — all gates green / FAIL — gates #X, #Y need rework}
    ```

    If any gate fails, do NOT proceed to Task 8 (commit). Fix the underlying file and rerun.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/33-claude-md-split/33-VERIFICATION-GATES.md
      grep -c "Final verdict" .planning/phases/33-claude-md-split/33-VERIFICATION-GATES.md   # ≥ 1
      grep -cE "PASS — all gates green" .planning/phases/33-claude-md-split/33-VERIFICATION-GATES.md   # ≥ 1 (otherwise gates failed)
      # Re-run the cheapest cross-cutting gates inline for executor confidence:
      awk '/<!-- GSD:.*-start/{skip=1} /<!-- GSD:.*-end/{skip=0; next} !skip' CLAUDE.md | grep -v "^[[:space:]]*$" | wc -l   # ≤ 60
      test ! -f frontend/AGENTS.md
    </automated>
  </verify>
  <acceptance_criteria>
    - All 9 gates ran and outcomes are recorded in `33-VERIFICATION-GATES.md`.
    - Final verdict is "PASS — all gates green" (if FAIL, the executor MUST loop back and fix).
    - Line-count gate (#8): CLAUDE.md has ≤ 60 lines of non-blank content outside `<!-- GSD:* -->` blocks.
    - `frontend/AGENTS.md` not on disk.
    - Architecture invariants #2 and #7 not duplicated to `backend/CLAUDE.md`.
  </acceptance_criteria>
  <done>Every gate green. Commit is safe.</done>
</task>

<task type="auto">
  <name>Task 8: Commit + write SUMMARY.md</name>
  <files>.planning/phases/33-claude-md-split/33-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md (D-04 outcome to record in SUMMARY)
    - .planning/phases/33-claude-md-split/33-VERIFICATION-GATES.md (gate outcomes to record in SUMMARY)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY format template)
  </read_first>
  <action>
    Write `33-01-SUMMARY.md` recording:
    - What shipped (the 8 files mutated, see frontmatter `files_modified`)
    - The D-04 verification outcome (PASS or FAIL, method used, file overwritten or not)
    - The D-13 Layer 2 grep gate verdict
    - Any deviations from the plan (e.g., if a probe in Task 2 needed manual override)
    - The line count of root `CLAUDE.md` before vs. after (pre-split: 114 lines; post-split: target ≤ 60 lines of guidance + ~50 lines of GSD-managed blocks)

    Then commit everything atomically:
    ```bash
    git add CLAUDE.md backend/CLAUDE.md frontend/CLAUDE.md .planning/CLAUDE.md \
            .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md \
            .planning/phases/33-claude-md-split/33-VERIFICATION-D04.md \
            .planning/phases/33-claude-md-split/33-VERIFICATION-GATES.md \
            .planning/phases/33-claude-md-split/33-01-SUMMARY.md
    git rm --cached frontend/AGENTS.md 2>/dev/null || true  # already staged for deletion by Task 5's git rm
    git commit -m "docs(33): split CLAUDE.md — root keeps invariants; backend/frontend/.planning scoped (DX-01)

    - Root CLAUDE.md: drop Current state, Tests, Gemini SDK, alembic deploy line, GSD:workflow block
    - backend/CLAUDE.md: Gemini SDK + alembic deploy + (conditionally) GSD:conventions backend + GSD:stack backend
    - frontend/CLAUDE.md: Next.js 16 warning (folded from AGENTS.md) + Tests + (conditionally) GSD:conventions frontend + GSD:stack frontend
    - .planning/CLAUDE.md: GSD:workflow block relocated from root
    - frontend/AGENTS.md: deleted (D-12 override; Claude Code is the only AI assistant in active use)
    - ROADMAP.md / REQUIREMENTS.md / PROJECT.md: locked-artifact edits for the override (D-12)
    - D-04 verification: {PASS/FAIL outcome from 33-VERIFICATION-D04.md}
    - D-13 Layer 2 gates: all PASS"
    ```

    Per CLAUDE.md MVP posture: single commit, clean rewrite, no compat shim. The locked-artifact edits land in the same commit as the file deletion + scoped-file creation (D-12 requirement: same commit or strictly before).
  </action>
  <verify>
    <automated>
      test -f .planning/phases/33-claude-md-split/33-01-SUMMARY.md
      git log -1 --pretty=%s | grep -c "docs(33): split CLAUDE.md"   # ≥ 1
      git log -1 --name-status | grep -c "^D[[:space:]]*frontend/AGENTS.md"   # 1 (deletion in this commit)
      git log -1 --name-status | grep -c "^A[[:space:]]*backend/CLAUDE.md"   # 1 (new file)
      git log -1 --name-status | grep -c "^A[[:space:]]*\.planning/CLAUDE.md"   # 1 (new file)
      # frontend/CLAUDE.md was overwritten not created — appears as M not A:
      git log -1 --name-status | grep -c "frontend/CLAUDE.md"   # ≥ 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `33-01-SUMMARY.md` exists, references both D-04 and D-13 verification files, and lists the 8 mutated files.
    - Single git commit on `main` (per MVP posture) containing all changes: 7 file modifications/creations + 1 file deletion + the 3 verification/summary artifacts.
    - Commit message references DX-01 and the D-04 outcome.
    - Deletion of `frontend/AGENTS.md` is part of this commit.
    - The 3 locked-artifact edits (ROADMAP / REQUIREMENTS / PROJECT) land in the SAME commit as the deletion + scoped-file creation per D-12.
  </acceptance_criteria>
  <done>Phase 33 shipped. Root CLAUDE.md is materially shorter; scoped files exist; AGENTS.md gone; locked artifacts caught up.</done>
</task>

</tasks>

<threat_model>
No new attack surface — pure documentation restructure; threats unchanged from pre-split CLAUDE.md content. ASVS L1 unaffected. No package installs, no auth/data flow changes, no API surface changes.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | None — docs-only change | accept | N/A. No code paths, no data, no boundaries touched. |
</threat_model>

<verification>
Three-layer phase verification:
1. **Plan-time mapping table (D-13 Layer 1)** — see `<mapping_table>` in `<context>` above. One row per pre-split content unit (every H2 + every bullet inside GSD:conventions + the GSD:stack highlights paragraph). Plan-checker reads this before approving execution.
2. **D-04 empirical verification gate** — Task 2 produces `33-VERIFICATION-D04.md` with PASS/FAIL. Outcome gates the shape of Tasks 3, 4, 6.
3. **D-13 Layer 2 grep gates** — Task 7 produces `33-VERIFICATION-GATES.md` with all 9 gates and a final verdict. Verdict must be PASS before commit.
</verification>

<success_criteria>
Phase 33 ships when:
1. Root `CLAUDE.md` has ≤ 60 lines of guidance outside `<!-- GSD:* -->` blocks (gate #8).
2. Root `CLAUDE.md` contains only architecture invariants, locked vocabularies, MVP posture, source-of-truth pointers, repo layout, productize-later TODOs, and the deploy push-to-main/hosting sentences (D-07).
3. `backend/CLAUDE.md` exists with Gemini SDK + alembic deploy + (conditionally) backend conventions + backend stack (D-04 outcome dependent).
4. `frontend/CLAUDE.md` exists with Next.js 16 warning + Tests + (conditionally) frontend conventions + frontend stack (D-04 outcome dependent).
5. `.planning/CLAUDE.md` exists with the GSD:workflow block.
6. `frontend/AGENTS.md` is deleted (D-12).
7. `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md` reflect the D-12 override.
8. `33-VERIFICATION-D04.md` records the D-04 outcome with method invoked.
9. `33-VERIFICATION-GATES.md` records all 9 D-13 Layer 2 gates as PASS.
10. All changes in a single atomic commit per MVP posture.
</success_criteria>

<output>
Write SUMMARY at `.planning/phases/33-claude-md-split/33-01-SUMMARY.md` when done. Single commit per MVP posture.
</output>
