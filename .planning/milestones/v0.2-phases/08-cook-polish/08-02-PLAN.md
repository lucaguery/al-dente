---
phase: 08-cook-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/CookingBanner.tsx
autonomous: true
requirements: [COOK-07]
must_haves:
  truths:
    - "User starts cooking and sees the CookingBanner rendered as a paper-grain Card with subtle terracotta wash (`bg-primary/8`) — not the previous flat emerald `bg-valide-tint` slab"
    - "User taps the `Finaliser` button which is rendered via `<Button asChild>` wrapping `<Link>` (NOT a raw `<a>` with hand-rolled inline-flex classes)"
    - "Both `Finaliser` and `Passer` buttons clear the 48px (h-12) tap-target floor"
    - "ChefHat icon stays emerald (preserved Phase-3 cooking-active role-call signal)"
  artifacts:
    - path: "frontend/components/CookingBanner.tsx"
      provides: "Re-themed CookingBanner with paper-grain Card, bg-primary/8 wash, Button asChild Finaliser"
      contains: "Button asChild"
    - path: "frontend/components/CookingBanner.tsx"
      provides: "Paper-grain texture + warm shadow"
      contains: "paper-grain"
  key_links:
    - from: "frontend/components/CookingBanner.tsx Finaliser action"
      to: "/cooking-logs/{logId}/finalize"
      via: "<Button asChild className=\"h-12\"><Link href={...}>...</Link></Button>"
      pattern: "Button asChild"
    - from: "frontend/components/CookingBanner.tsx surface"
      to: "Phase 5 design system"
      via: "paper-grain + bg-primary/8 + shadow-card tokens"
      pattern: "paper-grain"
---

<objective>
Close the W4 UI-REVIEW gap COOK-07: re-theme the CookingBanner into the Phase 5/6/7 design system and convert the `Finaliser` raw `<a>` (with hand-rolled `inline-flex items-center justify-center h-12 px-4 rounded-md bg-primary text-primary-foreground text-sm font-medium gap-1` classes) to `<Button asChild>` wrapping a `<Link>`. Both buttons must clear the 48px tap-target floor.

Purpose: Restore Button-primitive routing (eliminates the raw-anchor pattern flagged in W4 UI-REVIEW), bring the banner surface into the paper-on-wood card system, and signal "cooking in flight" via the subtle terracotta-active wash instead of the previous emerald-validation tint.

Output:
- `frontend/components/CookingBanner.tsx` re-themed: outer container becomes a paper-grain Card with `bg-primary/8 shadow-card`; Finaliser becomes `<Button asChild className="h-12">` wrapping `<Link>`; Passer becomes `<Button variant="ghost" className="h-12">` (size="sm" + px-3 removed); ChefHat preserved emerald.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cook-polish/08-CONTEXT.md
@.planning/phases/08-cook-polish/08-UI-SPEC.md
@frontend/CLAUDE.md
@frontend/AGENTS.md
@frontend/components/CookingBanner.tsx
@frontend/components/ui/button.tsx

<read_first>
- 08-UI-SPEC.md §"Component Inventory" row for `frontend/components/CookingBanner.tsx` — exact class string changes (1)–(4)
- 08-UI-SPEC.md §"Surface 5 — CookingBanner component" — full revised JSX
- 08-UI-SPEC.md §"Tap-target audit" rows 1–2 (CookingBanner Finaliser + Passer)
- 08-UI-SPEC.md §"Phase 8 paper-grain placement" row "CookingBanner paper-grain Card frame"
- 08-UI-SPEC.md §"Acceptance Criteria" row COOK-07
- 08-UI-SPEC.md §"Verification queries" §2 + §3
- frontend/components/CookingBanner.tsx (full file, 73 LOC) — current implementation
- frontend/components/ui/button.tsx — confirms `asChild` prop available via Radix Slot
- frontend/AGENTS.md — Next.js 16 training-data drift warning (the existing `<Link href=...>` import from `next/link` + Radix Slot `asChild` pattern is verified in the existing Phase 5 Button primitive)
</read_first>

<interfaces>
<!-- Existing types/exports the executor consumes verbatim — no exploration needed -->

From frontend/components/ui/button.tsx (Phase 5 re-theme):
```typescript
// Button primitive supports asChild (Radix Slot pattern)
// When asChild=true, propagates className + variant + size styles to the child element.
export const Button: React.FC<ButtonProps & { asChild?: boolean }>;
// variants: default | ghost | secondary | destructive | outline | link
// sizes: default | sm | lg | icon
// default size carries built-in horizontal padding (px-4) and gap-1.5
```

From next/link:
```typescript
import Link from "next/link";
// <Link href={string}>...</Link> — Phase 8 wraps Link inside <Button asChild>
```

Existing CookingBanner.tsx props (preserved):
```typescript
export type CookingBannerProps = {
  logId: string;
  recipeTitle: string;
  onSkip: () => void;
};
```

Existing i18n keys (preserved — no new keys):
- `home.cooking_banner.title` → "En train de cuisiner"
- `home.cooking_banner.finalize` → "Finaliser"
- `home.cooking_banner.skip` → "Passer"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme CookingBanner outer container to paper-grain Card with bg-primary/8 wash</name>
  <files>frontend/components/CookingBanner.tsx</files>
  <read_first>
    - frontend/components/CookingBanner.tsx line 35 (current outer `<div>` className)
    - 08-UI-SPEC.md §"Component Inventory" CookingBanner.tsx step (1)
    - 08-UI-SPEC.md §"Phase 8 shadow application" row "CookingBanner paper-grain Card frame"
    - 08-UI-SPEC.md §"Accent reserved-for in Phase 8" item 5 (CookingBanner background tint)
  </read_first>
  <action>
**At line 35** (the outer `<div role="region">`), replace the className string:

**Before:**
```tsx
className="mx-6 mt-4 flex items-center gap-3 px-4 py-3 min-h-16 rounded-2xl bg-valide-tint border border-border"
```

**After:**
```tsx
className="mx-6 mt-4 flex items-center gap-3 px-4 py-3 min-h-16 rounded-2xl bg-primary/8 paper-grain shadow-card border border-border"
```

Three substitutions:
- `bg-valide-tint` → `bg-primary/8` (terracotta-active wash at 8% alpha — the cooking-in-flight register; emerald is reserved for the ChefHat icon and the Validé vote chip)
- ADD `paper-grain` (Phase 5 utility — `::before` pseudo-element overlays the SVG noise at 6% multiply on cream; clips via the rounded-2xl border-radius automatically)
- ADD `shadow-card` (warm two-layer paper-on-wood shadow per Phase 5)
- preserved: `mx-6 mt-4 flex items-center gap-3 px-4 py-3 min-h-16 rounded-2xl border border-border`

Per 08-UI-SPEC §"Accent reserved-for" item 5: "CookingBanner background tint — `bg-primary/8` (terracotta at 8% alpha) on the paper-grain Card frame. Subtle wash, NOT the full terracotta saturation reserved for primary CTAs. The CookingBanner is informational chrome that says 'you're in the middle of cooking' — the 8% wash is the 'warm-active' register without competing with the Finaliser CTA inside it."

**DO NOT** change:
- The `role="region"` + `aria-labelledby="cooking-banner-title"` attributes (preserved)
- The ChefHat icon className `text-emerald-700 dark:text-emerald-300 shrink-0` (per CONTEXT.md: "the cooking-active role-call signal is preserved emerald — the page reads as 'cooking in flight (terracotta active wash) → press Finaliser to validate'")
- The body div containing the title + recipe-title spans (preserved verbatim)
  </action>
  <acceptance_criteria>
    - `grep -nF 'bg-primary/8 paper-grain shadow-card' frontend/components/CookingBanner.tsx` → 1 hit on the outer `<div>`
    - `grep -nF 'bg-valide-tint' frontend/components/CookingBanner.tsx` → 0 hits
    - `grep -nF 'paper-grain' frontend/components/CookingBanner.tsx` → ≥1 hit
    - `grep -nF 'text-emerald-700 dark:text-emerald-300' frontend/components/CookingBanner.tsx` → 1 hit (ChefHat preserved)
    - `grep -nF 'role="region"' frontend/components/CookingBanner.tsx` → 1 hit (a11y preserved)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "bg-primary/8 paper-grain shadow-card" components/CookingBanner.tsx | grep -q "^1$" && grep -cF "bg-valide-tint" components/CookingBanner.tsx | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>Outer div carries paper-grain + bg-primary/8 + shadow-card; bg-valide-tint removed; emerald ChefHat preserved; ARIA region preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Finaliser raw <a> to <Button asChild> + simplify Passer Button (W4 closure)</name>
  <files>frontend/components/CookingBanner.tsx</files>
  <read_first>
    - frontend/components/CookingBanner.tsx lines 53-70 (current Finaliser Link + Passer Button block)
    - 08-UI-SPEC.md §"Component Inventory" CookingBanner.tsx steps (2)–(3)
    - 08-UI-SPEC.md §"Surface 5 — CookingBanner component" full revised JSX
    - 08-UI-SPEC.md §"Tap-target audit" rows 1–2
    - 08-UI-SPEC.md §"Verification queries" §2
  </read_first>
  <action>
**At lines 53-70** (the action buttons block), replace the entire Finaliser `<Link>` and the Passer `<Button>` with the new patterns.

**Before** (lines 53-70):
```tsx
<div className="flex items-center gap-2 shrink-0">
  <Link
    href={`/cooking-logs/${logId}/finalize`}
    className="inline-flex items-center justify-center h-12 px-4 rounded-md bg-primary text-primary-foreground text-sm font-medium gap-1"
  >
    <Sparkles size={16} aria-hidden />
    {t("finalize")}
  </Link>
  <Button
    type="button"
    variant="ghost"
    size="sm"
    className="h-12 px-3"
    onClick={onSkip}
  >
    {t("skip")}
  </Button>
</div>
```

**After:**
```tsx
<div className="flex items-center gap-2 shrink-0">
  <Button asChild className="h-12">
    <Link href={`/cooking-logs/${logId}/finalize`}>
      <Sparkles size={16} aria-hidden />
      {t("finalize")}
    </Link>
  </Button>
  <Button
    type="button"
    variant="ghost"
    className="h-12"
    onClick={onSkip}
  >
    {t("skip")}
  </Button>
</div>
```

Two changes:

1. **Finaliser** (W4 COOK-07 closure): the raw `<Link>` with hand-rolled `"inline-flex items-center justify-center h-12 px-4 rounded-md bg-primary text-primary-foreground text-sm font-medium gap-1"` classes is replaced by `<Button asChild className="h-12"><Link href={...}>...</Link></Button>`. The Button primitive (Phase 5 re-theme) provides:
   - `bg-primary text-primary-foreground` (terracotta surface)
   - `inline-flex items-center justify-center` (layout)
   - `rounded-md` (corner radius)
   - `text-sm font-medium` (typography)
   - `gap-1.5` from the size-default
   - All propagated to the inner `<Link>` via Radix Slot
   - Plus inherited focus ring, disabled state, ease-craft transitions
   The explicit `className="h-12"` enforces the 48px tap-target floor.

2. **Passer**: remove `size="sm"` (would shadow the explicit `h-12` with the size-default `h-9`) and remove `px-3` (the size-default horizontal padding from the shadcn Button primitive carries through; Phase 8 surface contract declares no sub-4px padding on this button — see 08-UI-SPEC §"Tap-target audit" row 2: "remove `size='sm'` so the size-default `h-10` does not shadow the explicit `h-12`"). The remaining `type="button"`, `variant="ghost"`, `className="h-12"`, `onClick={onSkip}` are sufficient.

Imports preserved (already at top of file):
- `import { ChefHat, Sparkles } from "lucide-react";`
- `import Link from "next/link";`
- `import { useTranslations } from "next-intl";`
- `import { Button } from "@/components/ui/button";`

No new imports required.

**DO NOT** change:
- The `t("finalize")` / `t("skip")` i18n calls — keys preserved (`home.cooking_banner.finalize` / `skip`)
- The `Sparkles size={16} aria-hidden` icon
- The Passer onClick handler
- Any `role="region"`, `aria-labelledby`, or other a11y attributes on the outer container
  </action>
  <acceptance_criteria>
    - `grep -nF 'Button asChild' frontend/components/CookingBanner.tsx` → ≥1 hit
    - `grep -nF 'inline-flex items-center justify-center h-12 px-4 rounded-md' frontend/components/CookingBanner.tsx` → 0 hits (the hand-rolled inline-flex pattern is removed)
    - `grep -nE 'size="sm"|className="h-12 px-3"' frontend/components/CookingBanner.tsx` → 0 hits (Passer Button simplified)
    - `grep -cF 'h-12' frontend/components/CookingBanner.tsx` → 2 (Finaliser Button + Passer Button)
    - `grep -nF 'cooking-logs/${logId}/finalize' frontend/components/CookingBanner.tsx` → 1 hit (route preserved; the wrapping primitive changed, the destination is unchanged)
    - `grep -nF 'Sparkles size={16}' frontend/components/CookingBanner.tsx` → 1 hit (icon preserved)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "CookingBanner" | head -5; npm run lint 2>&1 | grep -E "CookingBanner" | head -5; echo done</automated>
  </verify>
  <done>Finaliser uses `<Button asChild className="h-12">` wrapping `<Link>`; Passer uses `<Button variant="ghost" className="h-12">` without size/px-3; raw inline-flex hand-rolled pattern removed; TypeScript + ESLint pass; visual integrity preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /cooking-logs/{logId}/finalize route | Authenticated route guarded by middleware + cookie session; logId is from props (server-emitted via realtime cooking.started, not user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering / Open Redirect | Finaliser `<Link href={`/cooking-logs/${logId}/finalize`}>` | accept | The `logId` is a server-emitted UUID delivered via realtime cooking.started event (see CookingBannerProps + CookingBanner-using component flow). The `<Link>` constructs a same-origin path; Next.js sanitizes the href (no protocol injection possible from a UUID). No new attack surface vs the existing v0.1 raw `<a href={...}>` — just changed which primitive renders the anchor. |
| T-08-02-02 | XSS via JSX content | `t("finalize")` / `t("skip")` rendered inside Button children | accept | Author-controlled fr.json strings; React auto-escapes text nodes. No `dangerouslySetInnerHTML`. |
| T-08-02-03 | UI Spoofing / Phishing surface | Banner appears on home screen | accept | Banner only renders when an active CookingLogResponse exists for the household (server-validated via getActiveCookingLog + cookie-auth). No untrusted content; recipe title is from the household's own captured recipes. |
</threat_model>

<verification>
After both tasks:
1. `cd frontend && npx tsc --noEmit` → no new errors
2. `cd frontend && npm run lint` → no new errors
3. Manual check: visual smoke test (08-UI-SPEC §"Real-device smoke test" item 5) — start a cook, banner appears with paper-grain texture + faint terracotta wash + emerald ChefHat icon + Finaliser at 48px tall (visually identical surface from user POV but rendered via Button primitive) + Passer ghost at 48px tall.
4. All 6 grep patterns from `acceptance_criteria` across both tasks evaluate as expected (08-UI-SPEC §"Verification queries" §2 + §3).
</verification>

<success_criteria>
- COOK-07 closed: Finaliser is `<Button asChild className="h-12"><Link>...</Link></Button>` (NOT raw `<a>` or `<Link>` with hand-rolled classes); Passer is `<Button variant="ghost" className="h-12">` (size="sm" + px-3 removed); both clear 48px floor.
- Banner re-themed: outer container now paper-grain Card with subtle terracotta wash (`bg-primary/8`) and warm shadow (`shadow-card`); emerald ChefHat preserved as cooking-active role-call signal; emerald `bg-valide-tint` removed.
- No new i18n keys; no behavior changes (props, onClick handler, route destination preserved); no a11y regressions (ARIA region + labelledby preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/08-cook-polish/08-02-SUMMARY.md`.
</output>
