---
phase: 08-cook-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/RatingPicker.tsx
autonomous: true
requirements: [COOK-08]
must_haves:
  truths:
    - "User taps a RatingPicker card on the finalize screen and the press feedback eases over 100ms (paper-physics depression to scale-95) — NOT the previous instant snap"
    - "Each RatingPicker card carries paper-grain texture (cards-on-a-kitchen-counter idiom)"
    - "Selected-state colors preserved verbatim: loved (rose+terracotta), liked (emerald valide-tint), disliked (warm-taupe)"
    - "Helper line typography folded from `text-xs leading-4` into `text-sm leading-5` (Phase 8 type-scale 4-size discipline)"
    - "User on iOS reduce-motion sees the press feedback collapse to instant — no 100ms transition"
  artifacts:
    - path: "frontend/components/RatingPicker.tsx"
      provides: "RatingPicker with paper-grain + transition-transform 100ms ease-craft active:scale-95 press feedback"
      contains: "transition-transform duration-100"
    - path: "frontend/components/RatingPicker.tsx"
      provides: "Helper text size folded to text-sm leading-5"
      contains: "text-sm text-foreground-muted leading-5"
  key_links:
    - from: "frontend/components/RatingPicker.tsx <button> press"
      to: "Phase 5 motion language (--ease-craft via Tailwind utility)"
      via: "transition-transform duration-100 ease-craft active:scale-95"
      pattern: "ease-craft active:scale-95"
    - from: "frontend/components/RatingPicker.tsx surface"
      to: "Phase 5 paper-grain anchor"
      via: "paper-grain utility on each rating button"
      pattern: "paper-grain"
---

<objective>
Close the W4 UI-REVIEW gap COOK-08: replace the existing instant `transition-all duration-150 active:scale-[0.98]` press feedback with the locked Phase 8 paper-physics `transition-transform duration-100 ease-craft active:scale-95` press, add `paper-grain` to each rating card so the cluster joins the kitchen-counter card system, and fold the helper-line typography from `text-xs leading-4` into `text-sm leading-5` to keep the Phase 8 type scale at exactly 4 sizes (`text-display` / `text-title` / `text-base` / `text-sm`).

Purpose: Restore the deliberate-craftsman press feel on the highest-touch interaction of the cook-time loop (the rating cards are the focal cluster of the finalize screen) while bringing the cards into the paper-grain anchor system.

Output:
- `frontend/components/RatingPicker.tsx` modified at lines 67-71 (className array) and line 83 (helper span typography). All other code (CARDS array, UNSELECTED, props, render structure, i18n calls) preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cook-polish/08-CONTEXT.md
@.planning/phases/08-cook-polish/08-UI-SPEC.md
@frontend/CLAUDE.md
@frontend/AGENTS.md
@frontend/components/RatingPicker.tsx
@frontend/lib/motion.ts
@frontend/app/globals.css

<read_first>
- 08-UI-SPEC.md §"Component Inventory" row for `frontend/components/RatingPicker.tsx` — exact line 67-71 diff before/after
- 08-UI-SPEC.md §"Surface 7 — RatingPicker component" — explicit pre/post diff
- 08-UI-SPEC.md §"Phase 8 motion contract" row "RatingPicker press feedback (COOK-08 W4 closure)"
- 08-UI-SPEC.md §"Typography" row for "RatingPicker helper" — Phase 8 size-fold rationale (text-xs → text-sm)
- 08-UI-SPEC.md §"Phase 8 paper-grain placement" row "RatingPicker rating cards" — adds paper-grain to each <button>
- 08-UI-SPEC.md §"Verification queries" §4 + §5
- frontend/components/RatingPicker.tsx (full file, 92 LOC) — current implementation; lines 67-71 = press transition; line 83 = helper span
- frontend/lib/motion.ts — `easeCraft` cubic-bezier (consumed via Tailwind utility `ease-craft`)
- frontend/app/globals.css — confirms `--ease-craft` token at line 101 + reduced-motion clamp at lines 378-385 (Phase 8 motion contract: "transition-transform duration-100 collapses to 0ms when OS-level reduce-motion is on")
- frontend/AGENTS.md — Next.js 16 training-data drift warning (the `<button>` element + Tailwind utility `transition-transform duration-100 ease-craft active:scale-95` is plain CSS, no Next.js API surface)
</read_first>

<interfaces>
<!-- Existing types/exports the executor consumes verbatim — no exploration needed -->

From frontend/lib/cooking.ts:
```typescript
export type LogRating = "loved" | "liked" | "disliked";
```

Existing RatingPicker props (preserved):
```typescript
type Props = {
  value: LogRating | null;
  onChange: (next: LogRating) => void;
};
```

Existing CARDS array (preserved verbatim):
```typescript
const CARDS: CardSpec[] = [
  { value: "loved",    Icon: Heart,    iconFilled: true,
    selectedClass: "bg-surface-rose-100 border-2 border-primary text-primary" },
  { value: "liked",    Icon: ThumbsUp,
    selectedClass: "bg-valide-tint border-2 border-emerald-500 text-emerald-700 dark:text-emerald-300" },
  { value: "disliked", Icon: Meh,
    selectedClass: "bg-surface-muted border-2 border-foreground-muted text-foreground" },
];
const UNSELECTED = "bg-card border border-border text-foreground hover:bg-secondary/50";
```

Existing i18n keys (preserved — no new keys):
- `cooking_log.rating.loved` → "Adoré" | `loved_helper` → "On la refait sans hésiter"
- `cooking_log.rating.liked` → "Bien"  | `liked_helper`  → "C'était bon"
- `cooking_log.rating.disliked` → "Passable" | `disliked_helper` → "On évite la prochaine fois"

Tailwind utility `ease-craft` is available in Phase 5 via globals.css `@theme` token; the className `ease-craft` resolves to `transition-timing-function: var(--ease-craft)` = cubic-bezier(0.32, 0.72, 0.0, 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace press transition + add paper-grain on RatingPicker buttons (COOK-08 closure)</name>
  <files>frontend/components/RatingPicker.tsx</files>
  <read_first>
    - frontend/components/RatingPicker.tsx lines 60-91 (the `<button>` JSX and surrounding map)
    - 08-UI-SPEC.md §"Surface 7 — RatingPicker component" — exact pre/post diff
    - 08-UI-SPEC.md §"Component Inventory" RatingPicker.tsx step (1)–(3)
  </read_first>
  <action>
**At lines 67-71** (the className array passed to each rating `<button>`), replace the array contents.

**Before:**
```tsx
className={[
  "h-20 w-full flex items-center gap-4 px-4 rounded-xl shadow-card",
  "transition-all duration-150 active:scale-[0.98]",
  "focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background focus-visible:outline-none",
  selected ? selectedClass : UNSELECTED,
].join(" ")}
```

**After:**
```tsx
className={[
  "h-20 w-full flex items-center gap-4 px-4 rounded-xl shadow-card paper-grain",
  "transition-colors transition-transform duration-100 ease-craft active:scale-95",
  "focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background focus-visible:outline-none",
  selected ? selectedClass : UNSELECTED,
].join(" ")}
```

Two changes:

1. **First array entry** — append `paper-grain` to the surface row:
   - Before: `"h-20 w-full flex items-center gap-4 px-4 rounded-xl shadow-card"`
   - After:  `"h-20 w-full flex items-center gap-4 px-4 rounded-xl shadow-card paper-grain"`
   - The `paper-grain` Tailwind utility (Phase 5) overlays the SVG noise via `::before`; clips automatically to `rounded-xl`. Per 08-UI-SPEC §"Phase 8 paper-grain placement": "the existing RatingPicker uses raw `<button>` elements (not the Card primitive), so the inherited grain does NOT apply automatically. Phase 8 adds `paper-grain` to each RatingPicker `<button>` className to bring the cards into the system."

2. **Second array entry** — replace press transition class string:
   - Before: `"transition-all duration-150 active:scale-[0.98]"`
   - After:  `"transition-colors transition-transform duration-100 ease-craft active:scale-95"`
   - Per CONTEXT.md: "Replaces the instant snap with a 100ms paper-physics press (matches Phase 5 motion vocabulary — `--ease-craft` curve)."
   - Per 08-UI-SPEC §"Phase 8 motion contract": "Do NOT use `transition-all` (which would also catch the `transition-colors` already declared and merge into a conflicting timing). Existing `transition-colors duration-150 active:scale-[0.98]` is replaced wholesale with the new class string."
   - Note the canonical-utility upgrade `active:scale-[0.98]` → `active:scale-95` (CONTEXT.md explicitly specifies `active:scale-95`; the latter is a Tailwind-canonical 5% depression vs the 2% bracketed arbitrary value).
   - Note the explicit `transition-colors` class is preserved separately so color transitions on selection-state changes continue to animate (it would have been carried implicitly by `transition-all` previously); both `transition-colors` and `transition-transform` are listed so each property has its own targeted transition with the same `duration-100 ease-craft` timing.

**DO NOT** change:
- Third array entry (focus-visible ring classes) — preserved
- Fourth array entry (`selected ? selectedClass : UNSELECTED`) — preserved (selected-state color mappings unchanged per 08-UI-SPEC §"Component Inventory" RatingPicker step (4): "Selected-state color treatment per CONTEXT.md: keep the existing selectedClass mapping... No color changes in Phase 8")
- The `<button>` outer attributes (`type="button"`, `aria-pressed={selected}`, `onClick`) — preserved
- The Icon JSX at lines 73-78 — preserved
- The label `<span className="text-base font-semibold leading-6">` at line 80-82 — preserved
- CARDS array at top of file — preserved
- UNSELECTED constant — preserved
- Props type — preserved
- Imports — preserved
  </action>
  <acceptance_criteria>
    - `grep -nF 'transition-transform duration-100 ease-craft active:scale-95' frontend/components/RatingPicker.tsx` → 1 hit
    - `grep -nF 'transition-colors transition-transform duration-100' frontend/components/RatingPicker.tsx` → 1 hit
    - `grep -nF 'transition-all duration-150 active:scale-[0.98]' frontend/components/RatingPicker.tsx` → 0 hits
    - `grep -nF 'active:scale-[0.98]' frontend/components/RatingPicker.tsx` → 0 hits
    - `grep -nF 'paper-grain' frontend/components/RatingPicker.tsx` → 1 hit (on the rating button surface)
    - `grep -nF 'shadow-card' frontend/components/RatingPicker.tsx` → 1 hit (preserved on the same row as paper-grain)
    - `grep -nF 'border-2 border-primary text-primary' frontend/components/RatingPicker.tsx` → 1 hit (loved selectedClass preserved)
    - `grep -nF 'border-2 border-emerald-500' frontend/components/RatingPicker.tsx` → 1 hit (liked selectedClass preserved)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "transition-transform duration-100 ease-craft active:scale-95" components/RatingPicker.tsx | grep -q "^1$" && grep -cF 'active:scale-[0.98]' components/RatingPicker.tsx | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>Press transition string replaced with the locked COOK-08 closure; paper-grain added to surface row; selected-state colors and focus ring classes preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Fold helper-line typography from text-xs leading-4 to text-sm leading-5</name>
  <files>frontend/components/RatingPicker.tsx</files>
  <read_first>
    - frontend/components/RatingPicker.tsx line 83 (the helper `<span>`)
    - 08-UI-SPEC.md §"Typography" row "RatingPicker helper" — Phase 8 size-fold rationale
  </read_first>
  <action>
**At line 83** (the helper span inside the rating button), update the className.

**Before:**
```tsx
<span className="text-xs text-foreground-muted leading-4">
  {t(`${v}_helper`)}
</span>
```

**After:**
```tsx
<span className="text-sm text-foreground-muted leading-5">
  {t(`${v}_helper`)}
</span>
```

One substitution: `text-xs leading-4` → `text-sm leading-5`. Per 08-UI-SPEC §"Typography" RatingPicker-helper row: "the previous `text-xs leading-4` (12/16) is folded into `text-sm leading-5` (14/20) so the Phase 8 type scale resolves to exactly 4 sizes (`text-display` / `text-title` / `text-base` / `text-sm`). The helper line gains 2px of body and 4px of leading; legibility on iPhone PWA improves with no layout regressions (`h-20` rating card has ample vertical room)."

Per 08-UI-SPEC §"Type scale used in Phase 8": "**4 distinct sizes** — `text-display` (32–44), `text-title` (24), `text-base` (16), `text-sm` (14). **No `text-xs` (12px) on Phase 8 surfaces.** The previous Phase 4/7 RatingPicker helper line that consumed `text-xs leading-4` is folded into `text-sm leading-5` as part of this phase's polish."

**DO NOT** change:
- The `t(`${v}_helper`)` translation call (preserved — keys `loved_helper` / `liked_helper` / `disliked_helper` are in fr.json verbatim)
- The wrapping `<div className="flex-1 flex flex-col items-start gap-0.5 text-left">` (line 79) — preserved
- The label span at line 80-82 (`<span className="text-base font-semibold leading-6">`) — preserved
  </action>
  <acceptance_criteria>
    - `grep -nF 'text-sm text-foreground-muted leading-5' frontend/components/RatingPicker.tsx` → 1 hit
    - `grep -nF 'text-xs text-foreground-muted leading-4' frontend/components/RatingPicker.tsx` → 0 hits
    - `grep -nF 'text-xs' frontend/components/RatingPicker.tsx` → 0 hits (no other text-xs consumer in this file)
    - `grep -nF '`${v}_helper`' frontend/components/RatingPicker.tsx` → 1 hit (translation call preserved)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -cF "text-xs" components/RatingPicker.tsx | grep -q "^0$" && grep -cF "text-sm text-foreground-muted leading-5" components/RatingPicker.tsx | grep -q "^1$" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "RatingPicker" | head -3 && echo done</automated>
  </verify>
  <done>Helper span uses `text-sm leading-5`; no `text-xs` consumer remains in RatingPicker.tsx; type scale on Phase 8 surfaces resolves to exactly 4 sizes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → onChange handler | onChange is a parent-supplied callback (CookingLogFinalize); LogRating values are typed at compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | onChange callback receives a LogRating value | accept | The CARDS array enumerates all three valid LogRating values; the `onClick={() => onChange(v)}` closes over the typed `v: LogRating`. TypeScript guarantees no value other than "loved"/"liked"/"disliked" can be emitted. Server-side PUT /api/cooking-logs/{id} additionally validates the rating (D-03 server gate). |
| T-08-03-02 | XSS via JSX content | `t(v)` and `t(`${v}_helper`)` rendered inside `<span>` | accept | next-intl message values are author-controlled fr.json strings; React auto-escapes text nodes. No `dangerouslySetInnerHTML`. |
| T-08-03-03 | DoS via animation loop | `transition-transform duration-100 ease-craft active:scale-95` | accept | CSS-only transition triggered by `:active` pseudo-class; no JS animation loop, no requestAnimationFrame. iOS Safari's compositor handles transform on a separate thread; reduced-motion media query (globals.css:378-385) clamps to 0ms. No DoS surface. |
</threat_model>

<verification>
After both tasks:
1. `cd frontend && npx tsc --noEmit` → no new errors
2. `cd frontend && npm run lint` → no new errors on RatingPicker.tsx
3. Real-device smoke test (08-UI-SPEC §"Real-device smoke test" item 6 + 9): on the finalize page, tap a rating card → confirm a subtle 100ms scale-95 depression (paper-physics press); verify all 3 selected states render correctly; enable iOS reduce-motion → tap a card → confirm the press collapses to instant.
4. All grep patterns from `acceptance_criteria` across both tasks evaluate as expected (08-UI-SPEC §"Verification queries" §4 + §5).
</verification>

<success_criteria>
- COOK-08 closed: line 67-71 className array carries `paper-grain` on the surface row + `transition-colors transition-transform duration-100 ease-craft active:scale-95` on the motion row; the previous `transition-all duration-150 active:scale-[0.98]` is removed.
- Phase 8 type-scale 4-size discipline: helper span at line 83 uses `text-sm leading-5`; no `text-xs` consumer remains in this file.
- Selected-state colors preserved verbatim: loved (`bg-surface-rose-100 border-2 border-primary text-primary`), liked (`bg-valide-tint border-2 border-emerald-500 text-emerald-700`), disliked (`bg-surface-muted border-2 border-foreground-muted text-foreground`).
- No new i18n keys; props + onChange behavior preserved; aria-pressed + focus-visible ring + h-20 + shadow-card preserved; CARDS array + UNSELECTED constant + render structure preserved verbatim.
- iOS reduced-motion media query in globals.css clamps the press to 0ms (no per-component useReducedMotion call).
</success_criteria>

<output>
After completion, create `.planning/phases/08-cook-polish/08-03-SUMMARY.md`.
</output>
