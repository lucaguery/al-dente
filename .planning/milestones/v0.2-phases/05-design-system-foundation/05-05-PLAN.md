---
phase: 05-design-system-foundation
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-03, 05-04]
files_modified:
  - frontend/components/ui/alert-dialog.tsx
  - frontend/components/ui/badge.tsx
  - frontend/components/ui/button.tsx
  - frontend/components/ui/card.tsx
  - frontend/components/ui/dialog.tsx
  - frontend/components/ui/input.tsx
  - frontend/components/ui/label.tsx
  - frontend/components/ui/scroll-area.tsx
  - frontend/components/ui/select.tsx
  - frontend/components/ui/separator.tsx
  - frontend/components/ui/sheet.tsx
  - frontend/components/ui/skeleton.tsx
  - frontend/components/ui/sonner.tsx
  - frontend/components/ui/tabs.tsx
  - frontend/components/ui/textarea.tsx
autonomous: true
requirements: [DESIGN-07]
must_haves:
  truths:
    - "Every shadcn primitive in `frontend/components/ui/*` reflects the new tokens — no unmodified shadcn defaults remain anywhere in the app"
    - "Card, Dialog, Sheet, Select, AlertDialog content surfaces consume the `paper-grain` utility class so every card-like surface in the app inherits the warm-noise overlay automatically"
    - "Surface primitives (Card, Dialog, Sheet, AlertDialog) use `shadow-card` / `shadow-card-hover` / overlay tones from the warm shadow scale (Plan 01) — no `shadow-lg`, no `bg-black/10`"
    - "Interactive primitives (Button, Input, Textarea, Tabs, Select, Badge) use `transition-colors duration-fast ease-craft` — replacing `transition-all` and `transition-colors` (no duration) per UI-SPEC §Component Inventory"
    - "Tabs active-tab indicator uses terracotta `--primary` instead of the inherited shadcn pattern"
    - "All `font-heading` references (in card.tsx, dialog.tsx, sheet.tsx, alert-dialog.tsx) continue to render correctly via the one-phase alias from Plan 03 — no component breakage"
  artifacts:
    - path: "frontend/components/ui/card.tsx"
      provides: "Card primitive with paper-grain texture applied to base div + warm border"
      contains: "paper-grain"
    - path: "frontend/components/ui/button.tsx"
      provides: "Button primitive with ease-craft + duration-fast transitions; default size raised h-8→h-10, lg raised h-9→h-11"
      contains: "duration-fast"
    - path: "frontend/components/ui/dialog.tsx"
      provides: "Dialog primitive with paper-grain on content, warm overlay, shadow-card"
      contains: "paper-grain"
    - path: "frontend/components/ui/sheet.tsx"
      provides: "Sheet primitive with paper-grain on content, warm overlay, shadow-card-hover (was shadow-lg)"
      contains: "shadow-card-hover"
    - path: "frontend/components/ui/tabs.tsx"
      provides: "Tabs primitive with terracotta active indicator + ease-craft transitions"
      contains: "duration-fast"
  key_links:
    - from: "card.tsx Card div"
      to: "globals.css .paper-grain utility (Plan 01) + /textures/paper-grain.svg (Plan 02)"
      via: "className paper-grain"
      pattern: "paper-grain"
    - from: "dialog.tsx DialogContent + sheet.tsx SheetContent + alert-dialog.tsx AlertDialogContent + select.tsx SelectContent"
      to: "globals.css .paper-grain utility"
      via: "className paper-grain"
      pattern: "paper-grain"
    - from: "button.tsx + input.tsx + textarea.tsx + tabs.tsx + select.tsx transition classes"
      to: "globals.css --ease-craft + --duration-fast tokens (Plan 01 Task 2)"
      via: "Tailwind utilities duration-fast ease-craft"
      pattern: "duration-fast"
---

<objective>
Re-theme all 15 shadcn primitives in `frontend/components/ui/*` in place per UI-SPEC §Component Inventory. Apply the `paper-grain` utility to every card-like surface (Card, DialogContent, SheetContent, AlertDialogContent, SelectContent), replace cool shadows + overlays with warm equivalents, swap `transition-all` / `transition-colors` (no duration) for `transition-colors duration-fast ease-craft`, raise base Button + Input default heights to align with the new scale, route Tabs active-indicator through `--primary`, and verify token-driven styling on Skeleton + Sonner + Label + ScrollArea + Separator + Badge (no structural changes for those).

Purpose: Closes DESIGN-07 — the goal-backward truth "every shadcn primitive reflects the new tokens, no unmodified shadcn defaults remain." Phases 6-9 inherit these primitives unchanged: any screen that consumes Card, Button, Dialog, Sheet automatically picks up the Slow Food artisanal identity without per-screen work.
Output: 15 modified files in `frontend/components/ui/` matching the per-primitive hints in UI-SPEC §Component Inventory verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@.planning/phases/05-design-system-foundation/05-01-PLAN.md
@.planning/phases/05-design-system-foundation/05-03-PLAN.md
@frontend/components/ui/alert-dialog.tsx
@frontend/components/ui/badge.tsx
@frontend/components/ui/button.tsx
@frontend/components/ui/card.tsx
@frontend/components/ui/dialog.tsx
@frontend/components/ui/input.tsx
@frontend/components/ui/label.tsx
@frontend/components/ui/scroll-area.tsx
@frontend/components/ui/select.tsx
@frontend/components/ui/separator.tsx
@frontend/components/ui/sheet.tsx
@frontend/components/ui/skeleton.tsx
@frontend/components/ui/sonner.tsx
@frontend/components/ui/tabs.tsx
@frontend/components/ui/textarea.tsx
@frontend/AGENTS.md

<interfaces>
<!-- After Plans 01, 03, 04 ship, these tokens/utilities exist for primitives to consume: -->

From frontend/app/globals.css (after Plan 01):
- Tailwind utilities: `bg-primary`, `bg-secondary`, `bg-muted`, `bg-card`, `bg-popover`, `bg-foreground`, `text-foreground`, `text-foreground-muted`, `text-muted-foreground`, `border-border`, `border-input`, `ring-ring` (all reading the new terracotta + warm-cream + warm-taupe + ink palette)
- `shadow-card`, `shadow-card-hover`, `shadow-nav` utilities (warm two-layer shadows)
- `duration-fast`, `duration-normal`, `ease-craft` utilities (motion tokens)
- `paper-grain` utility class (texture overlay)

From frontend/app/globals.css (after Plan 03):
- `font-heading` Tailwind utility resolves to Fraunces via `--font-heading: var(--font-display)` alias (one-phase bridge — Phase 6 sweeps)
- `font-display`, `font-body`, `font-mono` Tailwind utilities resolve to the new font variables
- `body { font-family: var(--font-body) }` so any unstyled element inherits IBM Plex Sans

From frontend/lib/motion.ts (after Plan 04):
- `easeCraft`, `transitions`, `variants` exports (only consumed when a primitive uses framer-motion — none of the 15 primitives use framer-motion in their current implementation; this import is reserved for downstream phases)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme surface primitives (Card, Dialog, Sheet, AlertDialog, Select) — apply paper-grain + warm shadows + warm overlays</name>
  <files>frontend/components/ui/card.tsx, frontend/components/ui/dialog.tsx, frontend/components/ui/sheet.tsx, frontend/components/ui/alert-dialog.tsx, frontend/components/ui/select.tsx</files>
  <read_first>
    - .planning/phases/05-design-system-foundation/05-UI-SPEC.md (§Component Inventory lines 492-516 — 15-row table; §Paper-Grain "Application contract" lines 397-405)
    - frontend/components/ui/card.tsx (current Card className lines 14-16)
    - frontend/components/ui/dialog.tsx (current DialogContent line 64; DialogOverlay line 42; DialogTitle line 133)
    - frontend/components/ui/sheet.tsx (current SheetContent line 65; SheetOverlay line 40; SheetTitle line 117)
    - frontend/components/ui/alert-dialog.tsx (current AlertDialogContent line 61; AlertDialogOverlay line 39; AlertDialogTitle line 126)
    - frontend/components/ui/select.tsx (current SelectContent line 72; SelectTrigger line 47)
  </read_first>
  <action>
Apply the per-primitive UI-SPEC §Component Inventory hints. Do NOT add wrapper components. Single-line className edits per primitive surface.

(a) `frontend/components/ui/card.tsx` — UI-SPEC row "card.tsx":
On the Card div className (line 15), insert the `paper-grain` token at the head of the class string and replace `ring-1 ring-foreground/10` with `border border-border`. Result:
```
className={cn(
  "paper-grain group/card flex flex-col gap-4 overflow-hidden rounded-xl bg-card py-4 text-sm text-card-foreground border border-border has-data-[slot=card-footer]:pb-0 has-[>img:first-child]:pt-0 data-[size=sm]:gap-3 data-[size=sm]:py-3 data-[size=sm]:has-data-[slot=card-footer]:pb-0 *:[img:first-child]:rounded-t-xl *:[img:last-child]:rounded-b-xl",
  className
)}
```
The CardTitle `font-heading` reference (line 41) stays unchanged — Plan 03's alias resolves it to Fraunces.

(b) `frontend/components/ui/dialog.tsx` — UI-SPEC row "dialog.tsx":
- DialogOverlay (line 42): replace `bg-black/10` with `bg-foreground/15` (warm overlay tone). Result:
  ```
  "fixed inset-0 isolate z-50 bg-foreground/15 duration-100 supports-backdrop-filter:backdrop-blur-xs ..."
  ```
- DialogContent (line 64): insert `paper-grain` at head of class string, replace `ring-1 ring-foreground/10` with `shadow-card border border-border`. Result:
  ```
  "paper-grain fixed top-1/2 left-1/2 z-50 grid w-full max-w-[calc(100%-2rem)] -translate-x-1/2 -translate-y-1/2 gap-4 rounded-xl bg-popover p-4 text-sm text-popover-foreground shadow-card border border-border duration-100 outline-none sm:max-w-sm data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95"
  ```
- DialogTitle `font-heading` reference (line 133) stays unchanged — alias resolves.

(c) `frontend/components/ui/sheet.tsx` — UI-SPEC row "sheet.tsx":
- SheetOverlay (line 40): replace `bg-black/10` with `bg-foreground/15`.
- SheetContent (line 65): insert `paper-grain` at head, replace `shadow-lg` with `shadow-card-hover`. Result class string opens with:
  ```
  "paper-grain fixed z-50 flex flex-col gap-4 bg-popover bg-clip-padding text-sm text-popover-foreground shadow-card-hover transition duration-200 ease-in-out ..."
  ```
- SheetTitle `font-heading` reference (line 117) stays unchanged — alias resolves.

(d) `frontend/components/ui/alert-dialog.tsx` — UI-SPEC row "alert-dialog.tsx":
- AlertDialogOverlay (line 39): replace `bg-black/10` with `bg-foreground/15`.
- AlertDialogContent (line 61): insert `paper-grain` at head, replace `ring-1 ring-foreground/10` with `shadow-card border border-border`.
- AlertDialogTitle `font-heading` (line 126) stays unchanged — alias resolves.

(e) `frontend/components/ui/select.tsx` — UI-SPEC row "select.tsx":
- SelectContent (line 72): insert `paper-grain` at head, replace `shadow-md ring-1 ring-foreground/10` with `shadow-card border border-border`. Result opens:
  ```
  "paper-grain relative z-50 max-h-(--radix-select-content-available-height) min-w-36 origin-(--radix-select-content-transform-origin) overflow-x-hidden overflow-y-auto rounded-lg bg-popover text-popover-foreground shadow-card border border-border duration-100 ..."
  ```
- SelectTrigger height (line 47): change `data-[size=default]:h-8` → `data-[size=default]:h-11` (matches Input task below); change `transition-colors` → `transition-colors duration-fast ease-craft`.

Hard rules:
- Do NOT remove the `data-slot="..."` attributes anywhere — radix primitives depend on them.
- Do NOT change the radix portal/overlay structure (only className strings).
- Do NOT add new variants. UI-SPEC §Out of Scope: "CVA variant explosion on primitives — in-place re-theme is the chosen vector. Variants are not added."
- The `font-heading` references in Card/Dialog/Sheet/AlertDialog Title elements are intentionally kept — Plan 03's `--font-heading: var(--font-display)` alias resolves them through Phase 6 cleanup.
  </action>
  <verify>
    <automated>grep -F "paper-grain" frontend/components/ui/card.tsx && grep -F "border border-border" frontend/components/ui/card.tsx && ! grep -F "ring-1 ring-foreground/10" frontend/components/ui/card.tsx && grep -F "paper-grain" frontend/components/ui/dialog.tsx && grep -F "bg-foreground/15" frontend/components/ui/dialog.tsx && grep -F "shadow-card" frontend/components/ui/dialog.tsx && ! grep -F "bg-black/10" frontend/components/ui/dialog.tsx && grep -F "paper-grain" frontend/components/ui/sheet.tsx && grep -F "shadow-card-hover" frontend/components/ui/sheet.tsx && ! grep -F "shadow-lg" frontend/components/ui/sheet.tsx && grep -F "bg-foreground/15" frontend/components/ui/sheet.tsx && grep -F "paper-grain" frontend/components/ui/alert-dialog.tsx && grep -F "bg-foreground/15" frontend/components/ui/alert-dialog.tsx && grep -F "shadow-card" frontend/components/ui/alert-dialog.tsx && grep -F "paper-grain" frontend/components/ui/select.tsx && grep -F "duration-fast ease-craft" frontend/components/ui/select.tsx && grep -F "data-[size=default]:h-11" frontend/components/ui/select.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/ui/card.tsx` Card div className contains `paper-grain` AND `border border-border`; does NOT contain `ring-1 ring-foreground/10`
    - `frontend/components/ui/dialog.tsx` DialogContent className contains `paper-grain` AND `shadow-card` AND `border border-border`; DialogOverlay contains `bg-foreground/15` (not `bg-black/10`)
    - `frontend/components/ui/sheet.tsx` SheetContent contains `paper-grain` AND `shadow-card-hover`; does NOT contain `shadow-lg`; SheetOverlay contains `bg-foreground/15`
    - `frontend/components/ui/alert-dialog.tsx` AlertDialogContent contains `paper-grain` AND `shadow-card` AND `border border-border`; AlertDialogOverlay contains `bg-foreground/15`
    - `frontend/components/ui/select.tsx` SelectContent contains `paper-grain` AND `shadow-card`; SelectTrigger contains `duration-fast ease-craft` AND `data-[size=default]:h-11`
    - All five files retain their `data-slot=` attributes (radix contract preserved)
    - All four `font-heading` references in card/dialog/sheet/alert-dialog Title elements are still present (alias-resolved per Plan 03)
  </acceptance_criteria>
  <done>Surface primitives consume paper-grain texture, warm shadows from Plan 01's scale, and warm-foreground overlays — every card-like surface across the app inherits the artisanal identity automatically through the next phases.</done>
</task>

<task type="auto">
  <name>Task 2: Re-theme interactive primitives (Button, Input, Textarea, Tabs, Badge) — duration-fast ease-craft transitions + size scale shift + terracotta tab indicator</name>
  <files>frontend/components/ui/button.tsx, frontend/components/ui/input.tsx, frontend/components/ui/textarea.tsx, frontend/components/ui/tabs.tsx, frontend/components/ui/badge.tsx</files>
  <read_first>
    - .planning/phases/05-design-system-foundation/05-UI-SPEC.md (§Component Inventory lines 492-516 — exact hints for button/input/textarea/tabs/badge)
    - frontend/components/ui/button.tsx (current cva base line 8; sizes lines 23-35)
    - frontend/components/ui/input.tsx (current className line 11)
    - frontend/components/ui/textarea.tsx (current className line 10)
    - frontend/components/ui/tabs.tsx (current TabsTrigger className lines 66-69 — base, line variant, default variant active, after pseudo-element)
    - frontend/components/ui/badge.tsx (current cva base line 8 — uses `transition-all`)
  </read_first>
  <action>

(a) `frontend/components/ui/button.tsx` — UI-SPEC row "button.tsx":
- Base cva (line 8): replace `transition-all` with `transition-colors duration-fast ease-craft`. The rest of the base string stays.
- Size variants (lines 23-35): change `default: "h-8 ..."` → `default: "h-10 ..."` (raises 32px → 40px). Change `lg: "h-9 ..."` → `lg: "h-11 ..."` (raises 36px → 44px). Other sizes (xs, sm, icon, icon-xs, icon-sm, icon-lg) stay as-is.
- Per UI-SPEC §Component Inventory: "component sites that need 48px tap targets continue to declare `h-12` explicitly per Phase 4 D-08 floor" — this is a Phase 4/Phase 6+ contract, not a Phase 5 default override. Do NOT raise the default to h-12.
- Do NOT add new variants. radix-nova preset variants stay.

(b) `frontend/components/ui/input.tsx` — UI-SPEC row "input.tsx":
On line 11, replace `h-8` with `h-11` (raises 32px → 44px). Replace `transition-colors` with `transition-colors duration-fast ease-craft`. Result class string opens:
```
"h-11 w-full min-w-0 rounded-lg border border-input bg-transparent px-2.5 py-1 text-base transition-colors duration-fast ease-craft outline-none ..."
```
The `border-input` automatically picks up the new warm-tinted `--input` from Plan 01 — no additional change needed.

(c) `frontend/components/ui/textarea.tsx` — UI-SPEC row "textarea.tsx":
On line 10, replace `transition-colors` with `transition-colors duration-fast ease-craft`. The `min-h-16` (64px) stays — UI-SPEC says "leave." Family inherits from `body { font-family: var(--font-body) }` — no className font change.

(d) `frontend/components/ui/tabs.tsx` — UI-SPEC row "tabs.tsx":
- TabsTrigger base class (line 66): replace `transition-all` with `transition-colors duration-fast ease-craft`.
- TabsTrigger after-pseudo (line 69): the indicator color is currently `after:bg-foreground`. Replace with `after:bg-primary` so the active-tab indicator reads terracotta per UI-SPEC.
- Result, line 69 class string becomes:
  ```
  "after:absolute after:bg-primary after:opacity-0 after:transition-opacity group-data-horizontal/tabs:after:inset-x-0 group-data-horizontal/tabs:after:bottom-[-5px] group-data-horizontal/tabs:after:h-0.5 group-data-vertical/tabs:after:inset-y-0 group-data-vertical/tabs:after:-right-1 group-data-vertical/tabs:after:w-0.5 group-data-[variant=line]/tabs-list:data-active:after:opacity-100"
  ```

(e) `frontend/components/ui/badge.tsx` — UI-SPEC row "badge.tsx":
On line 8 (cva base), replace `transition-all` with `transition-colors duration-fast ease-craft`. UI-SPEC: "keep the radius but verify the warm-tone hover transitions use `duration-fast ease-craft`; no color changes (variants already token-driven)." Do NOT change the `rounded-4xl` or any variant strings — they're already token-driven.

Hard rules:
- Do NOT add new variants to any of these primitives.
- Do NOT change `data-slot=` attributes.
- Do NOT touch the `disabled:`, `aria-invalid:`, `focus-visible:` modifiers — they pick up new tokens automatically through `--ring`, `--destructive`, `--input`.
- The `transition-all` → `transition-colors duration-fast ease-craft` swap is per UI-SPEC; intentionally narrowing the property list to colors lets transform-based hover/active effects (`active:translate-y-px` in Button) snap instantly while color hovers ease — matches UI-SPEC §Motion intent.
  </action>
  <verify>
    <automated>grep -F "transition-colors duration-fast ease-craft" frontend/components/ui/button.tsx && ! grep -F "transition-all outline-none select-none" frontend/components/ui/button.tsx && grep -F 'default:' frontend/components/ui/button.tsx | grep -F "h-10" && grep -F 'lg:' frontend/components/ui/button.tsx | grep -F "h-11" && grep -F "h-11" frontend/components/ui/input.tsx && grep -F "transition-colors duration-fast ease-craft" frontend/components/ui/input.tsx && ! grep -F '"h-8 w-full' frontend/components/ui/input.tsx && grep -F "transition-colors duration-fast ease-craft" frontend/components/ui/textarea.tsx && grep -F "transition-colors duration-fast ease-craft" frontend/components/ui/tabs.tsx && grep -F "after:bg-primary" frontend/components/ui/tabs.tsx && ! grep -F "after:bg-foreground" frontend/components/ui/tabs.tsx && grep -F "transition-colors duration-fast ease-craft" frontend/components/ui/badge.tsx && ! grep -F "transition-all focus-visible:border-ring" frontend/components/ui/badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/ui/button.tsx` cva base contains `transition-colors duration-fast ease-craft`; does NOT contain `transition-all` on the base
    - `frontend/components/ui/button.tsx` `default` size starts with `h-10` (not `h-8`)
    - `frontend/components/ui/button.tsx` `lg` size starts with `h-11` (not `h-9`)
    - `frontend/components/ui/input.tsx` className contains `h-11` (not `h-8`); contains `transition-colors duration-fast ease-craft`
    - `frontend/components/ui/textarea.tsx` className contains `transition-colors duration-fast ease-craft`
    - `frontend/components/ui/tabs.tsx` TabsTrigger contains `transition-colors duration-fast ease-craft` AND `after:bg-primary`; does NOT contain `after:bg-foreground`
    - `frontend/components/ui/badge.tsx` cva base contains `transition-colors duration-fast ease-craft`; does NOT contain `transition-all` on the base (the focus-visible classes that mention `transition-` other tokens may remain)
    - All five files retain their existing variant structures (no new variants added)
  </acceptance_criteria>
  <done>Interactive primitives use the new motion contract (one curve, fast duration, color transitions only); Button + Input default heights raised one notch on the new scale; Tabs active indicator reads terracotta; Badge transition narrowed to colors per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Verify token-driven primitives (Skeleton, Sonner, Label, ScrollArea, Separator) reflect new tokens automatically — no structural changes</name>
  <files>frontend/components/ui/skeleton.tsx, frontend/components/ui/sonner.tsx, frontend/components/ui/label.tsx, frontend/components/ui/scroll-area.tsx, frontend/components/ui/separator.tsx</files>
  <read_first>
    - .planning/phases/05-design-system-foundation/05-UI-SPEC.md (§Component Inventory lines 492-516 — rows for skeleton/sonner/label/scroll-area/separator state "no structural change" or "verify only")
    - frontend/components/ui/skeleton.tsx (current bg-muted reference line 7)
    - frontend/components/ui/sonner.tsx (current --normal-bg / --normal-text / --normal-border / --border-radius CSS variables lines 33-36)
    - frontend/components/ui/label.tsx (no font-family declaration — inherits from body)
    - frontend/components/ui/scroll-area.tsx (current bg-border on thumb line 49)
    - frontend/components/ui/separator.tsx (current bg-border line 20)
  </read_first>
  <action>
These five primitives are already token-driven and require NO structural changes. Their behavior changes automatically as Plans 01 + 03 ship the new token values.

Walk through each file and:
1. Read the file
2. Confirm the listed token references exist as expected
3. If everything is in order, do not modify the file
4. If any deviation is found (e.g., a hardcoded color crept in), correct it inline per the rules below

(a) `frontend/components/ui/skeleton.tsx`:
Confirm the className contains `bg-muted` (line 7). Per UI-SPEC: "ensure `bg-muted` resolves to the new warm-taupe `--muted` so skeletons feel like fading kraft paper, not gray. (Single token resolution check, no code change beyond verifying.)"
- If `bg-muted` is present and the only color reference: NO EDIT — the warm-taupe resolves automatically.
- If a hardcoded `bg-zinc-*` / `bg-gray-*` / `bg-slate-*` snuck in: replace with `bg-muted`.

(b) `frontend/components/ui/sonner.tsx`:
Confirm the four CSS variables in the `style` prop (lines 33-36) read:
- `"--normal-bg": "var(--popover)"`
- `"--normal-text": "var(--popover-foreground)"`
- `"--normal-border": "var(--border)"`
- `"--border-radius": "var(--radius)"`
Per UI-SPEC: "Replace `--normal-bg: var(--popover)` already correct; verify `--border-radius: var(--radius)` → terracotta-warm; the icon set is fine; do **not** add paper-grain (toasts are too small for grain to read — stays in design-direction's 'chrome, not card' exclusion)."
- All four variables already resolve to the new warm-cream/warm-border/warm-radius tokens automatically.
- Do NOT add `paper-grain`.
- Do NOT change the lucide icon set (CircleCheckIcon, InfoIcon, TriangleAlertIcon, OctagonXIcon, Loader2Icon).
- NO EDIT expected.

(c) `frontend/components/ui/label.tsx`:
Per UI-SPEC: "No structural change. Verify family inheritance from `body { font-family: var(--font-body) }`."
- Confirm the className contains no `font-*` family override (it doesn't — current class string is `flex items-center gap-2 text-sm leading-none font-medium select-none ...`).
- The `body { font-family: var(--font-body) }` rule from Plan 03 propagates IBM Plex Sans automatically.
- NO EDIT expected.

(d) `frontend/components/ui/scroll-area.tsx`:
Per UI-SPEC: "No structural change. Verify scrollbar thumb color reads as warm-gray in light mode."
- Confirm ScrollAreaThumb className contains `bg-border` (line 49). The warm-tinted `--border` from Plan 01 resolves automatically.
- NO EDIT expected.

(e) `frontend/components/ui/separator.tsx`:
Per UI-SPEC: "No structural change. Verify color reads `--border` (warm-tinted)."
- Confirm Separator className contains `bg-border` (line 20). Auto-resolves.
- NO EDIT expected.

Output expectation: this task most likely produces zero file changes — its purpose is to verify token-resolution invariants and surface any drift via the acceptance criteria below. If all five files pass, document "verified, no edits required" in the SUMMARY.

Hard rules:
- Do NOT add `paper-grain` to Sonner (UI-SPEC explicit exclusion — toasts are chrome, not cards).
- Do NOT add font-family overrides to Label (inherits from body).
- Do NOT change skeleton's `animate-pulse` to a custom animation. UI-SPEC: "keep `animate-pulse`."
  </action>
  <verify>
    <automated>grep -F "bg-muted" frontend/components/ui/skeleton.tsx && grep -F "animate-pulse" frontend/components/ui/skeleton.tsx && ! grep -E "bg-(zinc|gray|slate)-" frontend/components/ui/skeleton.tsx && grep -F '"--normal-bg": "var(--popover)"' frontend/components/ui/sonner.tsx && grep -F '"--normal-text": "var(--popover-foreground)"' frontend/components/ui/sonner.tsx && grep -F '"--normal-border": "var(--border)"' frontend/components/ui/sonner.tsx && grep -F '"--border-radius": "var(--radius)"' frontend/components/ui/sonner.tsx && ! grep -F "paper-grain" frontend/components/ui/sonner.tsx && ! grep -E "font-(sans|serif|mono|display|body|heading)" frontend/components/ui/label.tsx && grep -F "bg-border" frontend/components/ui/scroll-area.tsx && grep -F "bg-border" frontend/components/ui/separator.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/ui/skeleton.tsx` contains `bg-muted` AND `animate-pulse`; does NOT contain `bg-zinc-*` / `bg-gray-*` / `bg-slate-*`
    - `frontend/components/ui/sonner.tsx` contains all four CSS variables: `"--normal-bg": "var(--popover)"`, `"--normal-text": "var(--popover-foreground)"`, `"--normal-border": "var(--border)"`, `"--border-radius": "var(--radius)"`
    - `frontend/components/ui/sonner.tsx` does NOT contain `paper-grain`
    - `frontend/components/ui/label.tsx` does NOT contain any `font-` family Tailwind utility (inherits from body)
    - `frontend/components/ui/scroll-area.tsx` ScrollAreaThumb contains `bg-border`
    - `frontend/components/ui/separator.tsx` Separator contains `bg-border`
    - Per-primitive: token-driven styling resolves to the Plan 01 / Plan 03 values without per-file edits (verified by inspection in SUMMARY)
  </acceptance_criteria>
  <done>Five token-driven primitives confirmed to inherit the new palette + typography automatically; no structural changes required; sonner correctly excluded from paper-grain texture per UI-SPEC; SUMMARY documents the verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | JSX className-only changes to existing components. No new attack surface, no new data flows, no new dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | (n/a) | 15 components/ui/*.tsx files | accept | All edits are className strings on existing JSX or CSS-variable values inside a `style` prop. No new event handlers, no new data flow, no new external requests. Existing focus-visible / aria-invalid / disabled accessibility modifiers preserved. |
| T-05-12 | T (Tampering) | shadcn primitive contract | accept | `data-slot` and `data-variant` attributes preserved on every primitive — radix-ui contract intact. No new variants introduced (UI-SPEC §Out of Scope explicit). |
</threat_model>

<verification>
After all 3 tasks land:
- `cd frontend && npm run build` succeeds (no Tailwind v4 unknown-utility errors — `paper-grain`, `duration-fast`, `ease-craft`, `shadow-card`, `shadow-card-hover` all resolve from Plans 01)
- `cd frontend && npm run lint` succeeds (no eslint regressions on JSX class strings)
- `grep -rn "paper-grain" frontend/components/ui/` returns ≥5 hits (Card, DialogContent, SheetContent, AlertDialogContent, SelectContent)
- `grep -rn "duration-fast" frontend/components/ui/` returns ≥5 hits (Button, Input, Textarea, Tabs, Select, Badge)
- `grep -rn "after:bg-foreground" frontend/components/ui/tabs.tsx` returns 0 hits (replaced by `after:bg-primary`)
- `grep -rn "bg-black/10" frontend/components/ui/` returns 0 hits (replaced by `bg-foreground/15` everywhere)
- `grep -rn "shadow-lg" frontend/components/ui/sheet.tsx` returns 0 hits (replaced by `shadow-card-hover`)
- `grep -rn "ring-1 ring-foreground/10" frontend/components/ui/` returns 0 hits (replaced by `border border-border` on surface primitives)
- All 15 primitives type-check: `cd frontend && npx tsc --noEmit` succeeds
- TypeScript exports unchanged (no breaking renames) — every consumer of Card / Button / Dialog / Sheet / etc. still imports the same names
</verification>

<success_criteria>
- DESIGN-07 closed: every shadcn primitive in `frontend/components/ui/*` reflects the new tokens (paper-grain on surfaces, warm shadows, warm overlays, ease-craft transitions, terracotta tab indicator, raised default heights on Button + Input/Select); no unmodified shadcn defaults remain
- All 15 files type-check and lint clean
- No new variants added (UI-SPEC §Out of Scope)
- `font-heading` references in surface Title elements continue to render via Plan 03's one-phase alias — Phase 6 audit task will sweep them
- Sonner correctly excluded from paper-grain (chrome, not card)
- Label, ScrollArea, Separator inherit warm tokens automatically with no per-file edits
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-foundation/05-05-SUMMARY.md` documenting:
- Per-primitive change log (file → line → before → after) for the 10 modified files (Card, Dialog, Sheet, AlertDialog, Select from Task 1; Button, Input, Textarea, Tabs, Badge from Task 2)
- Verification status for the 5 unmodified files (Task 3 confirmation)
- Any deviation from UI-SPEC §Component Inventory hint and rationale
- Confirmation that `font-heading` aliases hold across Card/Dialog/Sheet/AlertDialog Title primitives
- Note for Phase 6 sweep: list files still using `font-heading` Tailwind utility (card.tsx:41, dialog.tsx:133, sheet.tsx:117, alert-dialog.tsx:126) for the alias-removal sweep
</output>
