---
phase: 07-decide-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/globals.css
  - frontend/components/ColdStartChip.tsx
autonomous: true
requirements:
  - DECIDE-03
  - DECIDE-05
must_haves:
  truths:
    - "User opens the daily decide screen with corpus < 10 recipes and sees the ColdStartChip rendered as a paper-grain card with terracotta Sparkles, Fraunces-italic body copy, and a 48px (h-12) dismiss button."
    - "User taps the ColdStartChip dismiss button and the 48px hit area is hittable around the visible icon."
    - "Future executor reading globals.css at the --color-valide-tint definition immediately sees the canonical-name comment lock and cannot accidentally re-introduce `--color-validé-tint` (with French accent) without overriding the comment."
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "DECIDE-03 invariant lock comment immediately above the --color-valide-tint declaration at line 72"
      contains: "DECIDE-03 invariant lock"
    - path: "frontend/components/ColdStartChip.tsx"
      provides: "Re-themed cold-start chip with paper-grain card surface, terracotta Sparkles, Fraunces italic body, h-12 dismiss"
      contains: "paper-grain"
  key_links:
    - from: "frontend/components/ColdStartChip.tsx"
      to: "Phase 5 token system (bg-card, paper-grain utility, shadow-card, text-primary, font-display italic)"
      via: "Tailwind classes"
      pattern: "bg-card paper-grain shadow-card"
    - from: "frontend/components/ColdStartChip.tsx"
      to: "D-08 48px tap-target floor"
      via: "h-12 w-12 dismiss Button"
      pattern: "h-12 w-12"
    - from: "frontend/app/globals.css"
      to: "DECIDE-03 token reconciliation"
      via: "1-line CSS comment at line 72"
      pattern: "DECIDE-03 invariant lock"
---

<objective>
Close two of the five Phase 7 deliverables in a single tightly-scoped plan:

1. **DECIDE-03 token reconciliation** — add a 1-line CSS comment at the `--color-valide-tint` definition site in `globals.css` that declares the canonical (un-accented) form and forbids re-introduction of the accented form. No code change; the comment is the cheap invariant lock per CONTEXT.md decision.

2. **DECIDE-05 ColdStartChip retheme + W4 D-08 closure** — full retheme of `ColdStartChip.tsx` to the Phase 5 system (paper-grain card surface, terracotta Sparkles icon, Fraunces italic body copy mirroring Phase 6 D-Voice register) AND raise the dismiss button from `h-8 w-8` (32px) to `h-12 w-12` (48px) to clear the W4-raised tap-target floor.

Purpose: this plan owns the smallest, most isolated changes in Phase 7 (one CSS file + one 64-LOC component). Bundling them keeps the dependency graph clean — neither file is touched by any other Phase 7 plan, so this plan runs in Wave 1 in parallel with Plans 02–04.

Output: 1 line of CSS comment + a fully-rethemed ColdStartChip component preserving all behavioral logic (useSyncExternalStore dismiss machinery, sessionStorage gate, dispatch event, ARIA label, early-return-on-dismissed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-decide-polish/07-CONTEXT.md
@.planning/phases/07-decide-polish/07-UI-SPEC.md
@.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@.planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md
@.planning/notes/v0.2-design-direction.md
@frontend/CLAUDE.md
@frontend/AGENTS.md

<interfaces>
<!-- Existing exports/utilities the executor will consume. No new types. -->

From `frontend/components/ColdStartChip.tsx` (existing — to be edited in place):
```tsx
// Preserved (DO NOT modify):
const STORAGE_KEY = "dismissed_cold_start_chip";
const DISMISS_EVENT = "aldente:chip-dismissed";
function subscribe(cb: () => void) { ... }
function getSnapshot(): boolean { ... }
export function ColdStartChip() {
  const t = useTranslations("home.cold_start");          // body key: "Ajoute plus de recettes pour de meilleures suggestions."
  const tCommon = useTranslations("common");             // close key: "Fermer"
  const dismissed = useSyncExternalStore(subscribe, getSnapshot, () => false);
  function handleDismiss() { ... }                        // unchanged
  if (dismissed) return null;                             // unchanged
  return ( /* JSX — re-themed in this plan */ );
}
```

From `frontend/app/globals.css` lines 66–72 (existing — comment to be inserted at line 72):
```css
  /* Phase 3 — Decide layer.
     `--color-valide-tint` is reserved exclusively for the "Validé" winning
     row in the "Tout vu" summary and the cooking-banner background. Hue
     axis h≈145 (emerald) deliberately distinct from the brand-rose accent
     so a "consensus reached" surface reads semantically without competing
     with the primary CTA. Per 03-UI-SPEC.md §"Phase 3 token additions". */
  --color-valide-tint: var(--valide-tint);
```

From Phase 5 token system (consumed by ColdStartChip retheme):
- `bg-card` — warm-cream card surface
- `paper-grain` — utility wired in Phase 5 onto the `Card` primitive's surface; usable as a standalone class
- `shadow-card` — Phase 5 warm two-layer paper-on-wood shadow
- `text-primary` — terracotta accent
- `font-display` — Fraunces (variable, opsz + wght + ital axes)
- `text-foreground` — deep ink

From `lucide-react` (already imported in ColdStartChip): `Sparkles`, `X`.

From `@/components/ui/button`: `Button` primitive (Phase 5 re-themed). `variant="ghost"`, `size="icon"`, `className="h-12 w-12"`.
</interfaces>
</context>

<read_first>
The executor MUST read these files before changing a single line. Each citation is load-bearing:

- **`.planning/phases/07-decide-polish/07-UI-SPEC.md` §"Color > DECIDE-03 token reconciliation comment lock"** — the EXACT comment string to insert and its placement at line 72.
- **`.planning/phases/07-decide-polish/07-UI-SPEC.md` §"Surface 4 — ColdStartChip"** — the verbatim line-by-line edits to ColdStartChip.tsx (outer div className, Sparkles className, body p className, Button className).
- **`.planning/phases/07-decide-polish/07-UI-SPEC.md` §"Tap-target audit"** — the locked post-Phase-7 invariant for the ColdStartChip dismiss (`h-12 w-12`, DECIDE-05 closure).
- **`.planning/phases/07-decide-polish/07-CONTEXT.md` §"ColdStartChip (DECIDE-05 W4 closure)"** — confirms the retheme rationale and the preserve-existing items.
- **`.planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md` §"D-Voice callout"** — the pattern this ColdStartChip retheme mirrors (Fraunces italic body register).
- **`frontend/app/globals.css` lines 60–80** — the canonical token block where the comment lands.
- **`frontend/components/ColdStartChip.tsx`** (entire 64 LOC) — the component being modified in place.
- **`frontend/AGENTS.md`** — Next.js 16.2.4 has training-data drift; consult `frontend/node_modules/next/dist/docs/` if any Next-specific question arises (none expected for this plan; both files are pure CSS / pure React).
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: DECIDE-03 — insert canonical-name comment lock at globals.css:72</name>
  <files>frontend/app/globals.css</files>
  <read_first>
    - 07-UI-SPEC.md §"Color > DECIDE-03 token reconciliation comment lock" (the EXACT comment string is specified)
    - 07-UI-SPEC.md §"Surface 5 — DECIDE-03 token comment lock" (placement contract)
    - frontend/app/globals.css lines 60–80 (the existing block; the comment must land immediately above `--color-valide-tint:`)
  </read_first>
  <action>
    Open `frontend/app/globals.css`. Locate the line:
    ```
    --color-valide-tint: var(--valide-tint);
    ```
    (currently line 72 per the file as read; if line numbers have drifted, locate by exact string match).

    Insert a single CSS comment line immediately ABOVE that declaration. The exact comment text — verbatim, including backticks around the accented form, the "DECIDE-03 invariant lock" phrase, and the leading two-space indent to match surrounding indentation — is:

    ```
      /* CANONICAL — DO NOT introduce `--color-validé-tint` (with French accent). DECIDE-03 invariant lock. */
    ```

    Result (lines 71–73 area, with comment at the new line 72, declaration shifting to line 73):
    ```css
      so a "consensus reached" surface reads semantically without competing
      with the primary CTA. Per 03-UI-SPEC.md §"Phase 3 token additions". */
      /* CANONICAL — DO NOT introduce `--color-validé-tint` (with French accent). DECIDE-03 invariant lock. */
      --color-valide-tint: var(--valide-tint);
    ```

    Do NOT touch any other line. Do NOT introduce a new variable or alias. Do NOT add a CI gate (CONTEXT.md explicitly rejects tooling debt). The comment IS the deliverable.

    Why this exact wording: the comment must (a) declare the canonical name, (b) name the forbidden accented form so a future grep/CMD-F finds it, and (c) cite DECIDE-03 so the invariant is traceable to a requirement ID. The phrase "DECIDE-03 invariant lock" is the grep anchor used in the verification queries.
  </action>
  <acceptance_criteria>
    - `grep -c "DECIDE-03 invariant lock" frontend/app/globals.css` returns ≥ 1.
    - `grep -c "DO NOT introduce" frontend/app/globals.css` returns ≥ 1 (and the match is in the same line as DECIDE-03).
    - `grep -c "color-validé-tint" frontend/app/globals.css` returns ≥ 1 — the accented form appears EXCLUSIVELY in the new comment, nowhere as a real declaration.
    - The line immediately following the new comment is unchanged: `  --color-valide-tint: var(--valide-tint);`.
    - `git diff frontend/app/globals.css` shows exactly +1 line added (the comment) and zero lines removed.
    - The frontend dev server still starts (`cd frontend && npm run dev`) without CSS parse errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "DECIDE-03 invariant lock" app/globals.css && grep -q "DO NOT introduce" app/globals.css && grep -A1 "DECIDE-03 invariant lock" app/globals.css | tail -1 | grep -q "color-valide-tint"</automated>
  </verify>
  <done>
    The 1-line CSS comment is in place at the `--color-valide-tint` definition site. Future executors reading globals.css see the canonical-name contract before they read the declaration. Phase 7's DECIDE-03 deliverable is closed with zero code churn.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DECIDE-05 — full ColdStartChip retheme + h-12 dismiss</name>
  <files>frontend/components/ColdStartChip.tsx</files>
  <read_first>
    - 07-UI-SPEC.md §"Surface 4 — ColdStartChip" (verbatim line-by-line spec, including the post-Phase-7 layout block)
    - 07-UI-SPEC.md §"Tap-target audit" — the LOCKED `h-12 w-12` post-Phase-7 invariant for the dismiss button
    - 07-UI-SPEC.md §"Typography > ColdStartChip body copy" — `font-display italic text-sm text-foreground` (mirrors Phase 6 D-Voice callout register one notch tighter at 14px)
    - 07-UI-SPEC.md §"Color > Accent reserved-for in Phase 7" entry 5 — Sparkles icon `text-primary` (terracotta) so the chip reads as "first-run guidance, not error"
    - 07-UI-SPEC.md §"Paper-Grain > ColdStartChip frame" — yes, paper-grain on the chip (informational card surface; grain reinforces "this is paper")
    - 07-CONTEXT.md §"ColdStartChip (DECIDE-05 W4 closure)" — preserve the useSyncExternalStore machinery, sessionStorage gate, dispatch event, ARIA label
    - 06-UI-SPEC.md §"D-Voice callout" — the Fraunces-italic-body-on-paper-grain pattern this chip mirrors at the 14px register
    - frontend/components/ColdStartChip.tsx (entire 64 LOC) — read end to end before editing; the edits are line-by-line surgical
  </read_first>
  <action>
    Edit `frontend/components/ColdStartChip.tsx` with the following five surgical changes. Preserve everything else byte-for-byte.

    **Change 1 — outer div className (existing line 45):**
    From:
    ```tsx
    <div className="mx-6 mt-4 flex items-center gap-2 px-3 py-2 rounded-xl bg-surface-rose-50 border border-border">
    ```
    To:
    ```tsx
    <div className="mx-6 mt-4 flex items-center gap-3 px-4 py-3 rounded-xl bg-card paper-grain shadow-card border border-border">
    ```
    Three edits inside the className: (a) `gap-2` → `gap-3` (balance the larger touch chrome below); (b) `px-3 py-2` → `px-4 py-3` (give the paper-grain texture room to read); (c) `bg-surface-rose-50` → `bg-card paper-grain shadow-card` (replaces the legacy v0.1 alias with the Phase 5 system). The `mx-6 mt-4`, `flex items-center`, `rounded-xl`, `border border-border` chunks are preserved. Do NOT add `paper-grain` to a button or a full-page background — only this one card-surface element receives it.

    **Change 2 — Sparkles icon className (existing line 47–50, on `<Sparkles size={16} className="text-foreground-muted" aria-hidden />`):**
    From:
    ```tsx
    <Sparkles size={16} className="text-foreground-muted" aria-hidden />
    ```
    To:
    ```tsx
    <Sparkles size={16} className="text-primary" aria-hidden />
    ```
    Single change: `text-foreground-muted` → `text-primary`. `size={16}` and `aria-hidden` are preserved. Reads as "first-run guidance, not error" per CONTEXT.md decision.

    **Change 3 — body p className (existing line 51):**
    From:
    ```tsx
    <p className="text-sm font-medium leading-5 flex-1">{t("body")}</p>
    ```
    To:
    ```tsx
    <p className="font-display italic text-sm text-foreground flex-1">{t("body")}</p>
    ```
    Replaces `text-sm font-medium leading-5` with `font-display italic text-sm text-foreground`. The `flex-1` is preserved. Same `t("body")` consumer (key `home.cold_start.body` → "Ajoute plus de recettes pour de meilleures suggestions.") — DO NOT change the i18n key. The Fraunces italic at 14px matches the Phase 6 D-Voice callout register one notch tighter for the chip's compact horizontal footprint.

    **Change 4 — dismiss Button className (existing line 56):**
    From:
    ```tsx
    <Button
      type="button"
      variant="ghost"
      size="icon"
      className="h-8 w-8"
      onClick={handleDismiss}
      aria-label={tCommon("close")}
    >
    ```
    To:
    ```tsx
    <Button
      type="button"
      variant="ghost"
      size="icon"
      className="h-12 w-12"
      onClick={handleDismiss}
      aria-label={tCommon("close")}
    >
    ```
    Single change: `h-8 w-8` → `h-12 w-12`. This is the **DECIDE-05 W4 D-08 closure** — non-negotiable. Every other prop on the Button (`type`, `variant="ghost"`, `size="icon"`, `onClick`, `aria-label`) is preserved.

    **Change 5 — preserve everything else verbatim:**
    - The `"use client"` directive at line 1 — unchanged.
    - The Phase 3 comment block at lines 3–5 — unchanged.
    - All imports (`useSyncExternalStore`, `Sparkles, X`, `useTranslations`, `Button`) — unchanged.
    - `STORAGE_KEY = "dismissed_cold_start_chip"` — unchanged.
    - `DISMISS_EVENT = "aldente:chip-dismissed"` — unchanged.
    - `subscribe(cb)` and `getSnapshot()` helpers — unchanged.
    - `useTranslations("home.cold_start")` and `useTranslations("common")` calls — unchanged. ZERO new i18n keys.
    - `useSyncExternalStore(subscribe, getSnapshot, () => false)` call — unchanged.
    - `handleDismiss()` body (sessionStorage write + dispatchEvent) — unchanged.
    - `if (dismissed) return null;` early return — unchanged.
    - `<X size={16} />` inside the button — unchanged (the icon size stays at 16; only the button hit area grows).
    - The closing tags — unchanged.

    **AnimatePresence fade-out (OPTIONAL — defer):** UI-SPEC §Motion entry "ColdStartChip dismiss" notes an optional `<AnimatePresence>` + `variants.fadeIn` wrap for visual continuity. **DO NOT implement this in Task 2.** Reason: it adds two imports (`AnimatePresence, motion` from framer-motion + `variants, transitions` from `@/lib/motion`) and an extra wrapping element. Per UI-SPEC: "Cosmetic refinement; not strictly required." If the post-implementation real-device smoke test shows the instant-disappear feels jarring, the executor may add the wrap as a follow-up commit. Default for this task: DO NOT add it.

    **Why these exact edits:** the chip is a 64-LOC component with a small surface; surgical line-level edits keep the diff readable and prove the behavioral logic is preserved. The retheme is purely visual; no behavioral change.
  </action>
  <acceptance_criteria>
    - `grep -c "h-12 w-12" frontend/components/ColdStartChip.tsx` returns ≥ 1 (dismiss button).
    - `grep -c "h-8 w-8" frontend/components/ColdStartChip.tsx` returns 0 (legacy size removed).
    - `grep -c "bg-surface-rose-50" frontend/components/ColdStartChip.tsx` returns 0 (legacy alias removed).
    - `grep -c "paper-grain" frontend/components/ColdStartChip.tsx` returns ≥ 1 (Phase 5 system applied).
    - `grep -c "shadow-card" frontend/components/ColdStartChip.tsx` returns ≥ 1.
    - `grep -c "text-primary" frontend/components/ColdStartChip.tsx` returns ≥ 1 (Sparkles).
    - `grep -c "font-display italic" frontend/components/ColdStartChip.tsx` returns ≥ 1 (body copy).
    - `grep -c "STORAGE_KEY\|DISMISS_EVENT\|useSyncExternalStore\|handleDismiss\|aria-label" frontend/components/ColdStartChip.tsx` returns ≥ 5 (machinery preserved).
    - `git diff frontend/components/ColdStartChip.tsx` shows changes ONLY inside the JSX className strings and the Sparkles icon className — no changes to imports, helpers, the hook call, the dismiss handler, the early return, or the i18n keys.
    - `frontend/lib/i18n/fr.json` is NOT modified by this task (`git diff frontend/lib/i18n/fr.json` shows no changes from this plan).
    - `cd frontend && npm run lint` passes.
    - `cd frontend && npx tsc --noEmit` passes.
    - The frontend dev server still mounts the chip on the daily decide screen when corpus < 10 (unchanged consumer in HomeDecide).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "h-12 w-12" components/ColdStartChip.tsx && ! grep -q "h-8 w-8" components/ColdStartChip.tsx && ! grep -q "bg-surface-rose-50" components/ColdStartChip.tsx && grep -q "paper-grain" components/ColdStartChip.tsx && grep -q "text-primary" components/ColdStartChip.tsx && grep -q "font-display italic" components/ColdStartChip.tsx && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>
    ColdStartChip renders as a paper-grain card with terracotta Sparkles, Fraunces-italic 14px body copy, and a 48px (h-12 w-12) dismiss button. The useSyncExternalStore dismiss machinery, sessionStorage gate, dispatch event, and ARIA label are preserved byte-for-byte. DECIDE-05 W4 D-08 floor gap is closed inline. No new i18n keys. No new component files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — Phase 7 introduces zero new HTTP routes, zero new data inputs, zero new auth surfaces. This plan touches one CSS file (declarative tokens — no code paths) and one client component that renders a hard-coded i18n string (`home.cold_start.body`) inside React's auto-escaping JSX. All trust boundaries inherited from Phase 01.1 (cookie-auth) remain unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | `frontend/app/globals.css` token block | accept | Build-time-only CSS, no runtime mutation. Tampering would require modifying the deployed bundle, which is out of scope for ASVS L1 (frontend bundle integrity is delegated to Vercel). |
| T-07-01-02 | Information Disclosure | ColdStartChip body copy | accept | The string is a hard-coded i18n key (`home.cold_start.body` → "Ajoute plus de recettes pour de meilleures suggestions.") — no PII, no household data, no recipe data flows through this component. |
| T-07-01-03 | XSS via i18n string | ColdStartChip `{t("body")}` interpolation | mitigate | React's default JSX text-node escaping applies to `{t("body")}`. The next-intl `t()` returns a string, not raw HTML. Verification: `grep -c "dangerouslySetInnerHTML" frontend/components/ColdStartChip.tsx` returns 0. |
| T-07-01-04 | Spoofing | sessionStorage `dismissed_cold_start_chip` flag | accept | Local-only UX state with no security implication. A user manipulating their own sessionStorage to suppress (or re-show) a hint chip causes no harm beyond their own session. |

**Verification check (automated):** `grep -c "dangerouslySetInnerHTML" frontend/components/ColdStartChip.tsx` returns 0.
</threat_model>

<verification>
After both tasks complete:

1. `cd frontend && npm run lint` — PASSES.
2. `cd frontend && npx tsc --noEmit` — PASSES.
3. Phase 7 grep queries (UI-SPEC §"Verification queries"):
   - Query #1: `grep -n "h-12 w-12" frontend/components/ColdStartChip.tsx` — at least 1 hit.
   - Query #2a: `grep -n "bg-surface-rose-50" frontend/components/ColdStartChip.tsx` — 0 hits.
   - Query #2b: `grep -n "paper-grain" frontend/components/ColdStartChip.tsx` — at least 1 hit.
   - Query #3: `grep -n "text-primary" frontend/components/ColdStartChip.tsx` — at least 1 hit.
   - Query #11: `grep -n "DECIDE-03 invariant lock\|DO NOT introduce" frontend/app/globals.css` — at least 1 hit.
   - Query #14: `git diff frontend/lib/i18n/fr.json` — zero changes.
4. `cd frontend && npm run dev` boots without CSS parse error or React error in console.
5. STRIDE verification: `grep -c "dangerouslySetInnerHTML" frontend/components/ColdStartChip.tsx` returns 0.
6. Visual sanity (deferred to Phase 7's combined real-device smoke test, owned by the orchestrator): when corpus < 10 recipes, the ColdStartChip on the daily decide screen renders as paper-grain card + terracotta Sparkles + Fraunces-italic body + h-12 dismiss.
</verification>

<success_criteria>
- DECIDE-03 invariant-lock comment lives at `frontend/app/globals.css:72` (or wherever `--color-valide-tint:` is declared if line numbers shift).
- ColdStartChip dismiss button is `h-12 w-12` (48px) — W4 D-08 floor closed inline.
- ColdStartChip outer div uses `bg-card paper-grain shadow-card` (Phase 5 system) — NOT `bg-surface-rose-50` (legacy v0.1).
- ColdStartChip Sparkles icon uses `text-primary` (terracotta).
- ColdStartChip body uses `font-display italic text-sm text-foreground`.
- All preserved invariants intact: `useSyncExternalStore`, `STORAGE_KEY`, `DISMISS_EVENT`, `handleDismiss`, `if (dismissed) return null`, `aria-label={tCommon("close")}`, body key `home.cold_start.body`.
- Zero new i18n keys.
- `npm run lint` + `npx tsc --noEmit` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-decide-polish/07-01-SUMMARY.md` documenting:
- The exact CSS comment line inserted at globals.css and its line number (post-edit).
- The 5 surgical edits to ColdStartChip.tsx with before/after class strings.
- Any deviation from the UI-SPEC (none expected; if the AnimatePresence fade-out is added as a follow-up, document it).
- Confirmation that `useSyncExternalStore` machinery + sessionStorage + dispatch event + ARIA label are preserved.
- Output of all 6 grep verification queries from the verification section.
</output>
</content>
</invoke>