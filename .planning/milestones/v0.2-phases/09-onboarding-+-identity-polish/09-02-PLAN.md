---
phase: 09-onboarding-+-identity-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/onboarding/welcome/page.tsx
  - frontend/app/onboarding/create/page.tsx
  - frontend/app/onboarding/share-code/page.tsx
  - frontend/app/onboarding/join/page.tsx
autonomous: true
requirements:
  - ONBOARD-07
  - ONBOARD-08
tags:
  - onboarding
  - typography
  - paper-grain
  - tap-target

must_haves:
  truths:
    - "User opens the Welcome screen and sees the wordmark in Fraunces italic display register + a stacked pair of paper-grain CTA Cards (mirroring Phase 6 D-Voice callout pattern) — both at h-12 interior tap target."
    - "User advances to Create or Join and sees the form-body wrapped in a paper-grain Card with a Fraunces italic display title above the fields; submit + back buttons are h-12 (48px floor)."
    - "User completes Create and lands on Share-code — the celebratory body lives in a paper-grain Card; the invite code renders in Fraunces italic terracotta `text-3xl tracking-widest` (cookbook-recipe-card-number gesture) instead of the previous mono `tracking-[0.3em]` block; copy + done buttons are h-12."
    - "User on Join sees an invite-code Input that keeps mono uppercase entry-time register (typing UX preserved); the Fraunces italic register is reserved for read-time display on share-code + Settings."
    - "Zero new i18n keys added; every string sources from existing `frontend/lib/i18n/fr.json`."
  artifacts:
    - path: "frontend/app/onboarding/welcome/page.tsx"
      provides: "Wordmark text-display + paper-grain CTA Card pair (Phase 6 D-Voice mirror) + h-12 interior Link"
      min_lines: 50
      contains: "paper-grain"
    - path: "frontend/app/onboarding/create/page.tsx"
      provides: "Form-body wrapped in paper-grain Card with text-display title + h-12 submit + h-12 back"
      min_lines: 140
      contains: "paper-grain"
    - path: "frontend/app/onboarding/share-code/page.tsx"
      provides: "Body Card with text-display title + Fraunces italic terracotta invite-code display + h-12 copy + h-12 done"
      min_lines: 80
      contains: "font-display italic text-3xl tracking-widest"
    - path: "frontend/app/onboarding/join/page.tsx"
      provides: "Form-body wrapped in paper-grain Card with text-display title + h-12 submit + h-12 back; mono Input preserved"
      min_lines: 250
      contains: "paper-grain"
  key_links:
    - from: "frontend/app/onboarding/welcome/page.tsx"
      to: "frontend/components/ui/card.tsx (Phase 5 paper-grain primitive)"
      via: "import + className composition"
      pattern: "import.*Card.*from.*\"@/components/ui/card\""
    - from: "frontend/app/onboarding/share-code/page.tsx"
      to: "Phase 9 identity signature (Fraunces italic terracotta invite code)"
      via: "verbatim class string repeated on Settings invite-code (Plan 03)"
      pattern: "font-display italic text-3xl tracking-widest text-primary"
    - from: "frontend/app/onboarding/create/page.tsx"
      to: "frontend/components/ColorSwatchPicker.tsx + frontend/components/ui/input.tsx + Label"
      via: "existing form-field composition preserved verbatim INSIDE the new paper-grain Card wrapper"
      pattern: "ColorSwatchPicker"
---

<objective>
Re-theme the 4 onboarding screens (Welcome, Create, Share-code, Join) onto the Phase 5 token system + Phase 6/7/8 patterns. Deliver a coherent first-touch path: paper-grain Cards everywhere a body surface lives, Fraunces italic display titles at every screen, h-12 tap-target floor on every interactive control, Fraunces italic terracotta invite-code as the load-bearing identity element.

Purpose: Closes ONBOARD-07 (household create screen re-themed) + ONBOARD-08 (household join invite-code entry screen re-themed). The Welcome and Share-code screens are not directly numbered requirements but are part of the onboarding flow boundary per 09-CONTEXT.md §"Phase Boundary" — they must polish coherently with Create + Join or the first-touch experience fragments.

Output: 4 modified files. Zero new components extracted (the OnboardingCard wrapper extraction noted as "Claude's Discretion" in CONTEXT.md is judged NOT worth the extraction overhead — 4 screens, each with ~30-line Card wrapper, low duplication; documented inline in each page).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-onboarding-+-identity-polish/09-CONTEXT.md
@.planning/phases/09-onboarding-+-identity-polish/09-UI-SPEC.md
@.planning/phases/05-design-system-foundation/05-UI-SPEC.md
@.planning/phases/06-capture-surfaces-polish/06-UI-SPEC.md

@frontend/CLAUDE.md
@frontend/AGENTS.md
@frontend/app/onboarding/welcome/page.tsx
@frontend/app/onboarding/create/page.tsx
@frontend/app/onboarding/share-code/page.tsx
@frontend/app/onboarding/join/page.tsx
@frontend/components/ui/card.tsx
@frontend/components/ui/button.tsx
@frontend/components/ui/input.tsx
@frontend/components/ColorSwatchPicker.tsx
@frontend/lib/i18n/fr.json

<interfaces>
<!-- Phase 5 Card primitive contract (paper-grain on cards is Phase 5 system invariant). -->
<!-- Phase 6 D-Voice callout pattern is the visual reference for the Welcome CTA pair. -->

Phase 5 Card primitive (frontend/components/ui/card.tsx):
- Accepts `className` for composition
- Has `paper-grain` utility from globals.css applied when class is added
- shadow-card token applies warm-brown paper-on-wood lift

Phase 6 D-Voice callout pattern (mirrored for Welcome CTA Cards):
```tsx
<Card className="paper-grain shadow-card border-l-[3px] border-primary/60 p-4 ...">
  <span className="font-display italic text-base">{label}</span>
</Card>
```

Phase 5 Button primitive defaults: variant="default" surface is terracotta;
variant="ghost" inherits transparent; variant="secondary" inherits muted-card.
The `size="icon"` default is `size-8` (32px) — NEEDS h-12 w-12 override
on Phase 9 surfaces per D-08 floor.

Existing onboarding state machine (DO NOT change):
- /onboarding/welcome → router.push("/onboarding/create" | "/onboarding/join")
- /onboarding/create → router.replace("/onboarding/share-code?code=...")
- /onboarding/share-code → router.replace("/")
- /onboarding/join → router.replace("/")

Existing i18n keys (verified present in fr.json — DO NOT add new):
- home.title (Al Dente)
- onboarding.welcome.tagline, .create_cta, .join_cta
- onboarding.create.title, .household_name_label, .household_name_placeholder, .member_name_label, .color_label, .submit
- onboarding.join.title, .code_label, .code_placeholder, .code_helper, .member_name_label, .color_label, .submit
- onboarding.share_code.title, .body, .copy_cta, .copied_toast, .done_cta
- onboarding.errors.network, .code_not_found, .color_taken
- common.back, common.saving
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme Welcome + Share-code (entry + post-create celebratory screens)</name>
  <files>frontend/app/onboarding/welcome/page.tsx, frontend/app/onboarding/share-code/page.tsx</files>

  <read_first>
    - frontend/app/onboarding/welcome/page.tsx (45 LOC — current rose-era wordmark + 2 plain Buttons)
    - frontend/app/onboarding/share-code/page.tsx (85 LOC — current mono tracking-[0.3em] invite-code block)
    - 09-UI-SPEC.md §"Surface 1 — Welcome screen" (line ~489-535) — exact JSX scaffold for paper-grain CTA Card pair
    - 09-UI-SPEC.md §"Surface 4 — Onboarding Share-code" (line ~577-615) — exact JSX scaffold for body Card + Fraunces italic invite display
    - 09-UI-SPEC.md §"Component Inventory > Application components touched in Phase 9" — welcome row + share-code row
    - 06-UI-SPEC.md §D-Voice callout pattern (line ~96, ~134, ~187, ~268+) — the source pattern being mirrored
    - 09-CONTEXT.md §"Specifics > Welcome CTA pair" + "Invite-code display" code blocks
  </read_first>

  <action>
    **Welcome screen** (`frontend/app/onboarding/welcome/page.tsx`) — replace entire body of `OnboardingWelcomePage()` return JSX with the paper-grain CTA Card pair pattern per UI-SPEC §Surface 1:

    Add these imports at top (preserve existing imports for `useRouter`, `useTranslations`):
    ```tsx
    import Link from "next/link";
    import { ChevronRight } from "lucide-react";
    import { Card } from "@/components/ui/card";
    ```
    Remove the now-unused `Button` import (the two `<Button>` instances are replaced with paper-grain Cards wrapping `<Link>`s) AND the unused `useRouter` (the `<Link>` handles navigation, no programmatic `router.push` needed).

    Replace the entire `<section>` JSX body with:
    ```tsx
    <section className="flex flex-col flex-1 items-center justify-center px-6 py-16 bg-background">
      <header className="flex flex-col items-center gap-2 text-center">
        {/* Wordmark — Fraunces italic display register (mirrors Phase 7 daily date header + Phase 8 recipe-detail hero) */}
        <h1 className="text-display">{tHome("title")}</h1>
        {/* Tagline — IBM Plex Sans body */}
        <p className="text-base text-foreground-muted mt-2 text-center">
          {t("tagline")}
        </p>
      </header>

      <div className="flex-1" />

      {/* CTA Card pair — paper-grain Cards mirroring Phase 6 D-Voice callout pattern.
          Each Card surrounds a Link; the Link IS the tap target at h-12 interior. */}
      <div className="flex flex-col gap-3 w-full max-w-xs">
        <Card className="paper-grain shadow-card border-l-[3px] border-primary/60 p-4 transition-colors duration-fast ease-craft hover:bg-card/95">
          <Link
            href="/onboarding/create"
            className="flex items-center justify-between h-12"
          >
            <span className="font-display italic text-base">{t("create_cta")}</span>
            <ChevronRight className="text-primary" aria-hidden />
          </Link>
        </Card>
        <Card className="paper-grain shadow-card border-l-[3px] border-primary/60 p-4 transition-colors duration-fast ease-craft hover:bg-card/95">
          <Link
            href="/onboarding/join"
            className="flex items-center justify-between h-12"
          >
            <span className="font-display italic text-base">{t("join_cta")}</span>
            <ChevronRight className="text-primary" aria-hidden />
          </Link>
        </Card>
      </div>
    </section>
    ```

    Per UI-SPEC line ~514-525: `font-display italic text-base` is the Fraunces italic register on the CTA labels (mirrors Phase 6 D-Voice callout headline). `ChevronRight className="text-primary"` is the only terracotta-tinted icon on the screen — the affordance signal. The `border-l-[3px] border-primary/60` 3px hairline mirrors Phase 6 line ~134.

    **Share-code screen** (`frontend/app/onboarding/share-code/page.tsx`) — replace entire body of `ShareCodeInner()` return JSX per UI-SPEC §Surface 4:

    Add this import at top (preserve existing):
    ```tsx
    import { Card } from "@/components/ui/card";
    ```

    Replace the existing `<section className="flex flex-col flex-1 bg-background px-6 pt-12 pb-32">...` body with:
    ```tsx
    <section className="flex flex-col flex-1 bg-background px-6 pt-12 pb-32">
      <Card className="paper-grain shadow-card mx-6 my-6 px-6 py-6 flex flex-col gap-4">
        {/* Editorial title — Fraunces italic display register */}
        <h1 className="text-display">{t("title")}</h1>
        {/* Body copy — IBM Plex Sans muted */}
        <p className="text-base text-foreground-muted">{t("body")}</p>

        {/* THE invite-code monogram — load-bearing identity element.
            Repeated VERBATIM on Settings invite-code (Plan 03) for first-touch ↔ re-find consistency. */}
        <div className="font-display italic text-3xl tracking-widest text-center py-4 text-primary">
          {code}
        </div>

        {/* Copy Button — h-12 secondary variant */}
        <Button variant="secondary" className="h-12" onClick={onCopy}>
          <Copy className="h-4 w-4 mr-2" aria-hidden />
          {t("copy_cta")}
        </Button>
      </Card>

      {/* Bottom-fixed done CTA — h-12 default (terracotta) */}
      <div
        className="fixed bottom-0 inset-x-0 px-6 pb-6 bg-background/80 backdrop-blur-sm"
        style={{ paddingBottom: "calc(env(safe-area-inset-bottom) + 1.5rem)" }}
      >
        <Button
          variant="default"
          className="h-12 w-full"
          onClick={() => router.replace("/")}
        >
          {t("done_cta")}
        </Button>
      </div>
    </section>
    ```

    Notes per UI-SPEC line ~613-616:
    - The Card is the entire body surface; the bottom-fixed done CTA sits OUTSIDE the Card, on page bg.
    - The `font-display italic text-3xl tracking-widest text-primary` line is the editorial signature of the v0.2 milestone — Plan 03 will repeat this VERBATIM on the Settings invite-code.
    - Copy Button bumped from `h-11` → `h-12` per UI-SPEC §"Tap-target audit" line ~97.
    - Done Button bumped from `h-11 w-full` → `h-12 w-full` per UI-SPEC §"Tap-target audit" line ~98.
    - The wrapping `<Card>` REPLACES the existing `<div className="text-[28px] font-mono font-semibold tracking-[0.3em] py-6 px-8 bg-surface-muted rounded-lg text-center mt-6">` chrome (the Card now provides the surface) AND the standalone `<h1 className="text-xl font-semibold">` (now `text-display`).
    - The `mx-6 my-6` Card margins: the `<section>` already has `px-6 pt-12 pb-32`. The Card's `mx-6` creates a centered Card (mirrors UI-SPEC line ~216). If executor finds the Card visually overshooting the section padding (double-margin), prefer `mx-0` and adjust — this is layout-judgment.
    - Preserve `useEffect` redirect logic, `onCopy` handler, `Suspense` wrapper, `searchParams` reading verbatim — chrome retheme only, no functional change.

    **Critical preservation rules for both files:**
    - Zero new i18n keys
    - Zero changes to navigation logic, redirect logic, or business state
    - Zero hardcoded color hex (only the inherited `text-primary` token references)
    - 48px tap-target floor on all interactive controls (h-12 on Welcome interior Links, share-code Copy Button, share-code Done Button)
  </action>

  <acceptance_criteria>
    - `grep -n "text-display" frontend/app/onboarding/welcome/page.tsx` returns at least 1 hit (the wordmark).
    - `grep -n "paper-grain" frontend/app/onboarding/welcome/page.tsx` returns exactly 2 hits (one per Card).
    - `grep -n "border-l-\[3px\]" frontend/app/onboarding/welcome/page.tsx` returns exactly 2 hits.
    - `grep -n "h-12" frontend/app/onboarding/welcome/page.tsx` returns at least 2 hits (one per Link interior row).
    - `grep -n "h-11" frontend/app/onboarding/welcome/page.tsx` returns 0 hits (the old Button heights are gone).
    - `grep -n "font-display italic text-base" frontend/app/onboarding/welcome/page.tsx` returns 2 hits (CTA labels).
    - `grep -n "font-display italic text-3xl tracking-widest text-primary" frontend/app/onboarding/share-code/page.tsx` returns exactly 1 hit (the invite code).
    - `grep -n "paper-grain" frontend/app/onboarding/share-code/page.tsx` returns at least 1 hit.
    - `grep -n "h-12" frontend/app/onboarding/share-code/page.tsx` returns at least 2 hits (copy + done).
    - `grep -n "h-11" frontend/app/onboarding/share-code/page.tsx` returns 0 hits.
    - `grep -n "tracking-\[0.3em\]" frontend/app/onboarding/share-code/page.tsx` returns 0 hits (old mono register replaced).
    - `grep -n "text-display" frontend/app/onboarding/share-code/page.tsx` returns at least 1 hit (the title).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm run lint` exits 0 (no new ESLint warnings).
    - i18n key count unchanged: `wc -l frontend/lib/i18n/fr.json` matches pre-Phase-9 baseline.
  </acceptance_criteria>

  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; grep -q "text-display" app/onboarding/welcome/page.tsx &amp;&amp; [ "$(grep -c "paper-grain" app/onboarding/welcome/page.tsx)" = "2" ] &amp;&amp; [ "$(grep -c "border-l-\[3px\]" app/onboarding/welcome/page.tsx)" = "2" ] &amp;&amp; ! grep -q "h-11" app/onboarding/welcome/page.tsx &amp;&amp; grep -q "font-display italic text-3xl tracking-widest text-primary" app/onboarding/share-code/page.tsx &amp;&amp; grep -q "paper-grain" app/onboarding/share-code/page.tsx &amp;&amp; ! grep -q "h-11" app/onboarding/share-code/page.tsx &amp;&amp; ! grep -q "tracking-\[0.3em\]" app/onboarding/share-code/page.tsx</automated>
  </verify>

  <done>
    Welcome wordmark in Fraunces italic display + 2 paper-grain CTA Cards (D-Voice mirror) + h-12 interior Links. Share-code body wrapped in paper-grain Card with text-display title + Fraunces italic terracotta invite-code identity signature + h-12 Copy + h-12 done. Zero new i18n keys. TypeScript + ESLint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-theme Create + Join (form-body Card wrap + h-12 floor)</name>
  <files>frontend/app/onboarding/create/page.tsx, frontend/app/onboarding/join/page.tsx</files>

  <read_first>
    - frontend/app/onboarding/create/page.tsx (142 LOC — current `flex flex-col gap-6 px-6 pt-6 pb-32` form-body div)
    - frontend/app/onboarding/join/page.tsx (263 LOC — same shape + invite-code Input mono register)
    - 09-UI-SPEC.md §"Surface 2 — Onboarding Create" (line ~537-566)
    - 09-UI-SPEC.md §"Surface 3 — Onboarding Join" (line ~568-575) — same shape as Create, mono Input preserved
    - 09-UI-SPEC.md §"Tap-target audit" (line ~91-105) — back button + submit floor specifications
    - 09-UI-SPEC.md §"Component Inventory" rows for create/join (line ~460-461)
    - 09-UI-SPEC.md §"Shadows" line ~216 — `mx-6 my-6` Card centering rationale
  </read_first>

  <action>
    **Create screen** (`frontend/app/onboarding/create/page.tsx`):

    Add this import at top (preserve existing):
    ```tsx
    import { Card } from "@/components/ui/card";
    ```

    Step 1 — Bump back button to `h-12 w-12` (UI-SPEC §"Tap-target audit" line ~99). Replace:
    ```tsx
    <Button
      size="icon"
      variant="ghost"
      aria-label={tCommon("back")}
      onClick={() => router.back()}
    >
      <ChevronLeft />
    </Button>
    ```
    with:
    ```tsx
    <Button
      size="icon"
      variant="ghost"
      className="h-12 w-12"
      aria-label={tCommon("back")}
      onClick={() => router.back()}
    >
      <ChevronLeft />
    </Button>
    ```

    Step 2 — Bump the right-side spacer to match: replace `<span className="w-8" aria-hidden />` with `<span className="w-12" aria-hidden />` (UI-SPEC §Surface 2 line ~544).

    Step 3 — Wrap the form-body div in a paper-grain Card with text-display title.

    Replace:
    ```tsx
    <div className="flex flex-col gap-6 px-6 pt-6 pb-32">
      <div className="flex flex-col gap-2">
        <Label htmlFor="create-household-name">...</Label>
        <Input id="create-household-name" ... />
      </div>
      <div className="flex flex-col gap-2">
        <Label htmlFor="create-member-name">...</Label>
        <Input id="create-member-name" ... />
      </div>
      <div className="flex flex-col gap-2">
        <Label>...</Label>
        <ColorSwatchPicker ... />
      </div>
    </div>
    ```

    with:
    ```tsx
    <div className="px-6 pt-6 pb-32">
      <Card className="paper-grain shadow-card px-6 py-6 flex flex-col gap-6">
        {/* Form-body editorial title — Fraunces italic display register.
            Mirrors header chrome label one level deeper inside the form. */}
        <h2 className="text-display">{t("title")}</h2>
        {/* Existing 3 fields preserved verbatim */}
        <div className="flex flex-col gap-2">
          <Label htmlFor="create-household-name">{t("household_name_label")}</Label>
          <Input
            id="create-household-name"
            value={householdName}
            onChange={(e) => setHouseholdName(e.target.value)}
            placeholder={t("household_name_placeholder")}
            maxLength={60}
            autoFocus
            required
          />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="create-member-name">{t("member_name_label")}</Label>
          <Input
            id="create-member-name"
            value={memberName}
            onChange={(e) => setMemberName(e.target.value)}
            maxLength={60}
            required
          />
        </div>
        <div className="flex flex-col gap-2">
          <Label>{t("color_label")}</Label>
          <ColorSwatchPicker
            value={color}
            onChange={setColor}
            aria-label={t("color_label")}
          />
        </div>
      </Card>
    </div>
    ```

    Per UI-SPEC §"Shadows" line ~216 the Card lives at `mx-6 my-6 px-6 py-6` — but the existing parent `<div>` already supplies `px-6 pt-6 pb-32` page-level padding. The cleanest implementation is to keep the parent's horizontal padding and let the Card use `px-6 py-6` for its OWN padding (NOT `mx-6 my-6` — that would compound margins). The result: page has 24px horizontal padding, Card sits flush left/right inside that, Card has 24px interior padding. If visual testing on iPhone reveals the Card feels "edge-to-edge" rather than "framed," add `my-6` to the Card and reduce parent `pt-6` to `pt-0` (executor judgment, document in SUMMARY.md).

    Step 4 — Bump submit Button from `h-11 w-full` to `h-12 w-full` (UI-SPEC §"Tap-target audit" line ~94):
    ```tsx
    <Button
      className="h-12 w-full"
      disabled={!canSubmit}
      onClick={onSubmit}
    >
    ```

    **Join screen** (`frontend/app/onboarding/join/page.tsx`) — same shape as Create, with the critical preservation that the invite-code Input keeps its existing mono register (`className="text-center font-mono tracking-[0.3em] uppercase"` per UI-SPEC §Surface 3 line ~575).

    Step 1 — Add `import { Card } from "@/components/ui/card";`.

    Step 2 — Bump back button to `h-12 w-12` and right-spacer to `w-12` (same pattern as Create).

    Step 3 — Wrap the form-body in a paper-grain Card. Replace the outer `<div className="flex flex-col gap-6 px-6 pt-6 pb-32">` with:
    ```tsx
    <div className="px-6 pt-6 pb-32">
      <Card className="paper-grain shadow-card px-6 py-6 flex flex-col gap-6">
        <h2 className="text-display">{t("title")}</h2>
        {/* All 3 existing field <div>s preserved INSIDE the Card — DO NOT touch them.
            This includes the invite-code Input with its existing
            className="text-center font-mono tracking-[0.3em] uppercase" — that
            mono register is the entry-time UX (typing the code) and is INTENTIONALLY
            DIFFERENT from the read-time Fraunces italic register on share-code +
            Settings. They co-exist. */}
        <div className="flex flex-col gap-2">
          <Label htmlFor="join-code">{t("code_label")}</Label>
          <Input ... preserved verbatim ... />
          {/* error/helper conditional preserved */}
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="join-member-name">{t("member_name_label")}</Label>
          <Input ... preserved ... />
        </div>
        <div className="flex flex-col gap-2">
          <Label>... color ...</Label>
          <ColorSwatchPicker ... preserved ... />
        </div>
      </Card>
    </div>
    ```

    Step 4 — Bump submit Button from `h-11 w-full` to `h-12 w-full`.

    **Critical preservation rules (BOTH FILES):**
    - DO NOT change form state, validation, debounce, fetchPreview, onSubmit, error-state UI, or the Suspense wrapper — chrome retheme only.
    - DO NOT touch the invite-code Input's `font-mono tracking-[0.3em] uppercase` — that is the entry-time register. UI-SPEC §Surface 3 line ~575 makes this explicit.
    - DO NOT add new i18n keys.
    - The `Loader2 className="animate-spin h-4 w-4 mr-2"` inside the submitting state is preserved verbatim.
    - The error/helper conditional (`{codeError ? ... : <p ... helper>}`) is preserved verbatim INSIDE the Card.
    - The bottom-fixed submit bar layout (`fixed bottom-0 inset-x-0 px-6 pb-6 bg-background/80 backdrop-blur-sm`) is preserved verbatim — only the inner Button's `h-11 w-full` becomes `h-12 w-full`.
  </action>

  <acceptance_criteria>
    - `grep -n "paper-grain" frontend/app/onboarding/create/page.tsx` returns at least 1 hit (the form-body Card).
    - `grep -n "text-display" frontend/app/onboarding/create/page.tsx` returns at least 1 hit (the form-body title).
    - `grep -n "h-12 w-12" frontend/app/onboarding/create/page.tsx` returns at least 1 hit (the back Button).
    - `grep -n "h-12 w-full" frontend/app/onboarding/create/page.tsx` returns at least 1 hit (the submit Button).
    - `grep -n "h-11" frontend/app/onboarding/create/page.tsx` returns 0 hits.
    - `grep -n "w-8\"" frontend/app/onboarding/create/page.tsx` returns 0 hits (old spacer width replaced with w-12).
    - `grep -n "paper-grain" frontend/app/onboarding/join/page.tsx` returns at least 1 hit.
    - `grep -n "text-display" frontend/app/onboarding/join/page.tsx` returns at least 1 hit.
    - `grep -n "h-12 w-12" frontend/app/onboarding/join/page.tsx` returns at least 1 hit.
    - `grep -n "h-12 w-full" frontend/app/onboarding/join/page.tsx` returns at least 1 hit.
    - `grep -n "h-11" frontend/app/onboarding/join/page.tsx` returns 0 hits.
    - **CRITICAL invariant — mono Input preserved:** `grep -n "tracking-\[0.3em\]" frontend/app/onboarding/join/page.tsx` returns exactly 1 hit (the invite-code Input — entry-time register kept).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm run lint` exits 0.
    - i18n key count unchanged across this entire phase: `wc -l frontend/lib/i18n/fr.json` matches pre-Phase-9 baseline.
  </acceptance_criteria>

  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; grep -q "paper-grain" app/onboarding/create/page.tsx &amp;&amp; grep -q "text-display" app/onboarding/create/page.tsx &amp;&amp; grep -q "h-12 w-12" app/onboarding/create/page.tsx &amp;&amp; grep -q "h-12 w-full" app/onboarding/create/page.tsx &amp;&amp; ! grep -q "h-11" app/onboarding/create/page.tsx &amp;&amp; grep -q "paper-grain" app/onboarding/join/page.tsx &amp;&amp; grep -q "text-display" app/onboarding/join/page.tsx &amp;&amp; grep -q "h-12 w-12" app/onboarding/join/page.tsx &amp;&amp; grep -q "h-12 w-full" app/onboarding/join/page.tsx &amp;&amp; ! grep -q "h-11" app/onboarding/join/page.tsx &amp;&amp; [ "$(grep -c "tracking-\[0.3em\]" app/onboarding/join/page.tsx)" = "1" ]</automated>
  </verify>

  <done>
    Create + Join form-bodies wrapped in paper-grain Cards with text-display titles. Submit + back buttons hit h-12 floor. Invite-code Input mono entry-time register preserved verbatim. Form state, validation, debounce, error handling untouched. Zero new i18n keys.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/households (POST create) | Untrusted user input (household_name, member_name, color_hex) |
| Browser → /api/households/by-code/{code} (GET preview) | Untrusted invite code from URL path; debounced client-side fetch |
| Browser → /api/households/join (POST) | Untrusted invite_code + member_name + color_hex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | XSS | Invite code rendered to JSX (share-code + welcome screens; subsequently in Plan 03 Settings) | mitigate | React auto-escaping renders the `code` value as text content (NOT `dangerouslySetInnerHTML`). Verification: `grep -rn "dangerouslySetInnerHTML" frontend/app/onboarding` returns 0 hits. The Phase 9 retheme adds NO new template-literal injection points. |
| T-09-02-02 | XSS | Form input values reflected in error toasts | mitigate | Toast payloads use translated i18n strings (`tErrors("network")`, `tErrors("code_not_found")`) — the toast layer (sonner) treats these as text. No untrusted user input is interpolated into error messages. |
| T-09-02-03 | Spoofing | Invite-code submission to existing backend endpoint | accept | Phase 9 only changes visual rendering of the Join form; the underlying `api<JoinResponse>("/api/households/join", { method: "POST", body: ... })` flow is unchanged. Authn/authz are backend concerns (cookie auth from Phase 01.1) — out of scope for a frontend retheme. |
| T-09-02-04 | Information Disclosure | Code preview endpoint /api/households/by-code/{code} | accept | The preview returns `household_name + taken_colors` — both intentionally public per v0.1 SPEC.md (joiner needs to see what they're joining). Phase 9 does not change this contract; the retheme only re-styles where the response data renders (invite-code Input with disabled swatches). |
| T-09-02-05 | DoS via debounce bypass | Code preview fetch loop | accept | The existing debounce (300ms `setTimeout` cleared on each keystroke per `frontend/app/onboarding/join/page.tsx:87-97`) is preserved verbatim by Phase 9. T-01-06-06 mitigation from Phase 01.1 is retained. |
| T-09-02-06 | Input validation bypass | Form submission | accept | All validation (length, color presence, code format) is server-side per existing flow + Pydantic models. Client-side checks (`canSubmit` boolean) are UX gates only and are preserved verbatim. Phase 9 introduces no new client-trusted validation. |
</threat_model>

<verification>
**Phase-level smoke checks:**

```bash
# All 4 onboarding screens have paper-grain
grep -rn "paper-grain" frontend/app/onboarding 2>&1 | wc -l
# expected: at least 5 (welcome 2 cards + create 1 + join 1 + share-code 1)

# All Phase 9 onboarding submits hit h-12 floor
grep -rn "h-11" frontend/app/onboarding 2>&1
# expected: 0 hits

# Fraunces italic display register on every onboarding screen
grep -rn "text-display" frontend/app/onboarding 2>&1
# expected: at least 4 (welcome wordmark + create title + join title + share-code title)

# Identity signature on share-code (will be repeated VERBATIM on Settings in Plan 03)
grep -n "font-display italic text-3xl tracking-widest text-primary" frontend/app/onboarding/share-code/page.tsx 2>&1
# expected: 1 hit

# Mono Input register preserved on Join only (entry-time register)
grep -rn "tracking-\[0.3em\]" frontend/app/onboarding 2>&1
# expected: exactly 1 hit (join code Input)

# Zero new i18n keys
wc -l frontend/lib/i18n/fr.json
# expected: 353 (matches pre-Phase-9 baseline)

# TypeScript + ESLint clean
cd frontend && npx tsc --noEmit && npm run lint
# expected: both succeed
```
</verification>

<success_criteria>
- [ ] Welcome wordmark renders in `text-display` Fraunces italic
- [ ] Welcome stacks 2 paper-grain CTA Cards with 3px terracotta-60 left border + ChevronRight terracotta tint
- [ ] Welcome CTA Cards have h-12 interior Link (tap target floor)
- [ ] Onboarding Create + Join form-bodies wrapped in paper-grain Card with `text-display` title above fields
- [ ] Onboarding Create + Join back buttons bumped from default `size-8` → `h-12 w-12`
- [ ] Onboarding Create + Join submit buttons bumped from `h-11 w-full` → `h-12 w-full`
- [ ] Share-code body wrapped in paper-grain Card with `text-display` title
- [ ] Share-code invite code renders as `font-display italic text-3xl tracking-widest text-primary` (Fraunces italic terracotta — first appearance of identity signature)
- [ ] Share-code copy + done buttons hit h-12 floor
- [ ] Invite-code Input on Join page keeps mono `tracking-[0.3em] uppercase` (entry-time register preserved)
- [ ] Zero new i18n keys (`wc -l frontend/lib/i18n/fr.json` matches pre-Phase-9 line count)
- [ ] No `h-11` left on any onboarding surface
- [ ] No `F43F5E` references introduced
- [ ] `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-onboarding-+-identity-polish/09-02-SUMMARY.md` documenting:
- Whether `mx-6 my-6` vs `mx-0` Card centering was chosen and why (judged from real-device test if possible, otherwise from local dev preview)
- Whether OnboardingCard.tsx wrapper was extracted (no, per planner judgment — duplication acceptable; document the call)
- Whether `tracking-widest` (locked) or `tracking-[0.15em]` (CONTEXT.md fallback) was used for the invite code on share-code
- Confirmation: zero new i18n keys, h-11 fully purged from onboarding tree
</output>
