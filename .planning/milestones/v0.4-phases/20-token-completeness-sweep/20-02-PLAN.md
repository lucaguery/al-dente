---
phase: 20
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - frontend/components/ShortlistCard.tsx
  - frontend/components/VoteSummary.tsx
  - frontend/components/CookingBanner.tsx
  - frontend/components/CookingLogCard.tsx
  - frontend/components/RatingPicker.tsx
  - frontend/app/cooking-logs/page.tsx
  - frontend/app/cooking-logs/[id]/page.tsx
  - frontend/app/styleguide/page.tsx
autonomous: true
requirements: [TOK-03]
must_haves:
  truths:
    - "No emerald-* Tailwind literals (`text-emerald-500`, `border-emerald-500`, `text-emerald-700`, `border-emerald-500/30`, etc.) remain in the 7 audit-cited surface files."
    - "All surfaces consume the new `--color-valide-foreground` / `--color-valide-emphasis` / `--color-valide-border` / `--color-cooking-foreground` tokens from Plan 20-01."
    - "`/styleguide` page surfaces both new token groups with swatch + foreground sample."
    - "RatingPicker keeps its 'liked' branch semantically — same UX, new tokens."
---

<objective>
Migrate the 7 audit-cited surfaces from emerald Tailwind literals to the semantic tokens added by Plan 20-01. Add a `/styleguide` section showcasing the new tokens.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Migrate 6 component files</name>
  <files>frontend/components/ShortlistCard.tsx, VoteSummary.tsx, CookingBanner.tsx, CookingLogCard.tsx, RatingPicker.tsx, frontend/app/cooking-logs/page.tsx, frontend/app/cooking-logs/[id]/page.tsx</files>
  <read_first>
    - The 7 files listed (current emerald-literal sites grep'd in CONTEXT.md §domain)
    - .planning/phases/20-token-completeness-sweep/20-CONTEXT.md §D-20-07, D-20-08
    - .planning/phases/20-token-completeness-sweep/20-01-SUMMARY.md (tokens shipped)
  </read_first>
  <action>
Replace literal Tailwind emerald classes with token-backed arbitrary-value syntax:

- `text-emerald-500` → `text-[var(--color-valide-foreground)]`
- `border-emerald-500` (solid) → `border-[var(--color-valide-foreground)]`
- `border-emerald-500/30` → `border-[var(--color-valide-border-faint)]`
- `border-emerald-500/50` → `border-[var(--color-valide-border)]`
- `bg-emerald-500/10` → `bg-[color-mix(in_srgb,var(--color-valide-foreground)_10%,transparent)]` (verify Tailwind v4 supports this; if not, add `--color-valide-bg-faint` token)
- `text-emerald-700` → `text-[var(--color-valide-emphasis)]`
- `text-emerald-700 dark:text-emerald-300` → single `text-[var(--color-valide-emphasis)]` (token swaps via dark-mode override in globals.css)
- For CookingBanner specifically, `text-emerald-700 dark:text-emerald-300` → `text-[var(--color-cooking-foreground)]`

**Specific replacements per file:**

1. `ShortlistCard.tsx:165` — "Validé" stamp: `border-emerald-500 text-emerald-500` → `border-[var(--color-valide-foreground)] text-[var(--color-valide-foreground)]`
2. `ShortlistCard.tsx:256` — Heart button: `border-emerald-500/50 hover:bg-emerald-500/10` → `border-[var(--color-valide-border)] hover:bg-[color-mix(in_srgb,var(--color-valide-foreground)_10%,transparent)]` (or new `--color-valide-bg-faint` token if color-mix is brittle)
3. `ShortlistCard.tsx:258` — Heart icon: `text-emerald-500` → `text-[var(--color-valide-foreground)]`
4. `VoteSummary.tsx:60` — `border border-emerald-500/30` → `border border-[var(--color-valide-border-faint)]`
5. `VoteSummary.tsx:74` — `border-emerald-500/30` → `border-[var(--color-valide-border-faint)]`
6. `CookingBanner.tsx:39` — `text-emerald-700 dark:text-emerald-300` → `text-[var(--color-cooking-foreground)]`
7. `CookingLogCard.tsx:58` — `border border-emerald-500/30` → `border border-[var(--color-valide-border-faint)]`
8. `RatingPicker.tsx:36` — `border-2 border-emerald-500 text-emerald-700 dark:text-emerald-300` → `border-2 border-[var(--color-valide-foreground)] text-[var(--color-valide-emphasis)]`
9. `cooking-logs/page.tsx:225` — `border border-emerald-500/30` → `border border-[var(--color-valide-border-faint)]`
10. `cooking-logs/[id]/page.tsx:50` — `border border-emerald-500/30` → `border border-[var(--color-valide-border-faint)]`

Verify with grep that NO `emerald-` literals remain in the 7 files after the edits.
  </action>
  <acceptance_criteria>
    - `grep -c "emerald-" frontend/components/ShortlistCard.tsx frontend/components/VoteSummary.tsx frontend/components/CookingBanner.tsx frontend/components/CookingLogCard.tsx frontend/components/RatingPicker.tsx frontend/app/cooking-logs/page.tsx "frontend/app/cooking-logs/[id]/page.tsx"` returns 0 across the 7 files combined
    - `grep -c "color-valide-foreground\|color-valide-emphasis\|color-valide-border\|color-cooking-foreground" frontend/components/ShortlistCard.tsx frontend/components/VoteSummary.tsx frontend/components/CookingBanner.tsx frontend/components/CookingLogCard.tsx frontend/components/RatingPicker.tsx frontend/app/cooking-logs/page.tsx "frontend/app/cooking-logs/[id]/page.tsx"` returns ≥ 8 across the files
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx eslint <the 7 files>` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Phase 20 tokens section to /styleguide</name>
  <files>frontend/app/styleguide/page.tsx</files>
  <read_first>
    - frontend/app/styleguide/page.tsx (existing structure)
    - .planning/phases/20-token-completeness-sweep/20-CONTEXT.md §D-20-09
  </read_first>
  <action>
Add a new section "Phase 20 tokens" with two subsections:

1. **Emerald-replacement tokens** — render 4 swatches (40×40 round) for: `--color-valide-foreground`, `--color-valide-emphasis`, `--color-valide-border`, `--color-cooking-foreground`. Each swatch labelled with token name + hex value.
2. **Member-color tokens** — render 5 chips (50×30 rounded-pill) using each `--color-member-{slot}-bg` as background and `--color-member-{slot}-foreground` as text. Each chip shows the slot name (rose/amber/emerald/sky/violet) and the hex value.

Use the existing /styleguide section pattern (heading + grid + swatch components). No new dependencies.
  </action>
  <acceptance_criteria>
    - `grep -n "Phase 20 tokens\|emerald-replacement\|member-color tokens" frontend/app/styleguide/page.tsx` returns ≥ 1 match
    - `grep -c "color-valide-foreground\|color-cooking-foreground\|color-member-" frontend/app/styleguide/page.tsx` returns ≥ 9 matches (4 emerald + 5 member groups)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx eslint app/styleguide/page.tsx` exits 0
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Disposition |
|--------|-------------|
| `color-mix(in srgb, ...)` not supported by older Safari | mitigate — verify Next.js 16 + Safari support; add explicit alpha tokens if not |
| Visual regression on dark mode | accept — dark-mode tokens override the same names |
</threat_model>

<verification>
- 7 audit-cited surfaces have zero remaining `emerald-` literals.
- New tokens consumed via `text-[var(--token)]` syntax.
- /styleguide renders both token groups.
</verification>
