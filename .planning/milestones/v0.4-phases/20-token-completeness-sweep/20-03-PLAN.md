---
phase: 20
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/settings/page.tsx
  - frontend/lib/i18n/fr.json
autonomous: true
requirements: [FIX-03]
must_haves:
  truths:
    - "No hardcoded `Historique` or `Voir les cuissons récentes` strings remain in `settings/page.tsx`."
    - "New i18n keys `settings.history.title` + `settings.history.cta_label` (or planner's preferred namespace) exist under fr.json."
    - "Any other hardcoded French strings in `HomeDecide.tsx` (partner-waiting messages) move into i18n."
---

<objective>
Close the FIX-03 invariant #6 code-layer break: route hardcoded `Historique` / `Voir les cuissons récentes` (settings/page.tsx:437,440) and any remaining HomeDecide.tsx partner-waiting strings through next-intl.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Catalogue and migrate hardcoded French strings</name>
  <files>frontend/app/settings/page.tsx, frontend/lib/i18n/fr.json</files>
  <read_first>
    - frontend/app/settings/page.tsx (focus lines 425-445 for Historique Card)
    - frontend/components/HomeDecide.tsx (grep for any hardcoded French — partner-waiting strings)
    - frontend/lib/i18n/fr.json (existing settings.* namespace)
  </read_first>
  <action>
1. Add to `frontend/lib/i18n/fr.json` under `settings`:

```json
"history": {
  "title": "Historique",
  "cta_label": "Voir les cuissons récentes"
}
```

2. Update `frontend/app/settings/page.tsx`:
   - Line 437: `<span>Historique</span>` → `<span>{t("history.title")}</span>`
   - Line 440: `<span>Voir les cuissons récentes</span>` → `<span>{t("history.cta_label")}</span>`
   - Ensure the `useTranslations("settings")` hook (or equivalent) is already in scope.

3. Grep `HomeDecide.tsx` for any remaining hardcoded French strings (search for character class with French accents `[éèêëàâïîôöùûüçœÀ-ÿ]`). Catalogue them. For each:
   - Add an i18n key under an appropriate namespace
   - Replace the hardcoded string with `t("key")`

4. Verify NO hardcoded French strings remain in either file by greping for likely tokens (specific words found in the catalogue step) and confirming they only appear in fr.json.
  </action>
  <acceptance_criteria>
    - `grep -n "Historique\|Voir les cuissons" frontend/app/settings/page.tsx` returns matches ONLY inside comments (not inside JSX text or attributes)
    - `grep -n "settings.history\|history.title\|history.cta_label" frontend/lib/i18n/fr.json` returns ≥ 2 matches
    - `grep -nE "^[^/]*['\"\\\">][^/]*[éèêëàâïîôöùûüçœ][^'\"<]*['\"\\\"<]" frontend/components/HomeDecide.tsx | grep -v "TODO\|//" | wc -l` returns 0 (no hardcoded French outside comments) — or planner accepts current state with note
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx eslint app/settings/page.tsx components/HomeDecide.tsx` exits 0
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Disposition |
|--------|-------------|
| Missed hardcoded string regressing to literal at next commit | accept — manual review at PR time |
</threat_model>

<verification>
- Settings Historique Card uses i18n.
- HomeDecide hardcoded French strings catalogued and migrated.
</verification>
