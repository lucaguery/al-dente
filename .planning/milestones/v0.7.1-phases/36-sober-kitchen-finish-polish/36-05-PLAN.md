---
phase: 36-sober-kitchen-finish-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design-system.html
autonomous: true
requirements: [SOBER-16]
must_haves:
  truths:
    - "`docs/design-system.html` §15 BottomNav mockups (Accueil + Bibliothèque mockups, lines ~1539-1544 and ~1731-1736) show the post-Phase-27 layout: 4 slots = Accueil + Recettes + central « Ajouter » CTA + Profil. NO 'Réception' tab."
    - "`docs/design-system.html` §15 mockups match the live `frontend/components/BottomNav.tsx` shape (3 flat tabs + 1 central-cta)."
    - "Historical context blocks in §15 (rows-table at ~line 1462-1466, the 'reporté' note for Capture+Réception refactor at ~line 1876, ~1954, ~1989) MAY retain the 'Réception' word as historical context — only the LIVE mockups need to change."
  artifacts:
    - path: "docs/design-system.html"
      provides: "Updated §15.A (Accueil) and §15.B (Bibliothèque) BottomNav HTML mockups reflecting 4 slots = Accueil + Recettes + central-cta + Profil; no Réception slot in the LIVE mockups."
      contains: "central"
  key_links:
    - from: "docs/design-system.html §15.A BottomNav mockup"
      to: "frontend/components/BottomNav.tsx TABS array"
      via: "visual parity"
      pattern: "central|Ajouter"
---

<objective>
Update `docs/design-system.html` §15 to reflect the post-Phase-27 4-slot BottomNav (Accueil + Recettes + central « Ajouter » CTA + Profil). Today the §15.A Accueil + §15.B Bibliothèque mockups still show a 4-tab layout including a "Réception" tab that was removed in Phase 27. The design system as documented must re-align with the design system as built.

Purpose: Close SOBER-16. Doc-only change — no JS, no TS, no CSS. The §15 mockups are the locked visual contract that subsequent planners/executors reference; drift between mockup and built is a planning hazard.

Output: Modified `docs/design-system.html` with the two LIVE BottomNav mockup blocks updated. Historical context paragraphs about "Capture & Réception refactor en cours" stay (they're historical truth — the refactor happened in Phase 27); only the LIVE mockup HTML changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-sober-kitchen-finish-polish/36-CONTEXT.md
@docs/design-system.html
@frontend/components/BottomNav.tsx

<interfaces>
**Current state in `docs/design-system.html`:**

§15.A Accueil mockup (~lines 1539-1544):
```html
<div class="bottom-nav">
  <div class="bottom-nav-tab active"><i data-lucide="home"...>Accueil</span></div>
  <div class="bottom-nav-tab"><i data-lucide="book-open"...>Recettes</span></div>
  <div class="bottom-nav-tab"><i data-lucide="inbox"...>Réception</span></div>
  <div class="bottom-nav-tab"><i data-lucide="settings"...>Réglages</span></div>
</div>
```

§15.B Bibliothèque mockup (~lines 1731-1736):
```html
<div class="bottom-nav">
  <div class="bottom-nav-tab"><i data-lucide="home"...>Accueil</span></div>
  <div class="bottom-nav-tab active"><i data-lucide="book-open"...>Recettes</span></div>
  <div class="bottom-nav-tab"><i data-lucide="inbox"...>Réception</span></div>
  <div class="bottom-nav-tab"><i data-lucide="settings"...>Réglages</span></div>
</div>
```

**Live shape in `frontend/components/BottomNav.tsx` (the target):**

4 slots: Accueil (tab) + Recettes (tab) + central-cta « Ajouter » (the elevated terracotta pill with `<Plus />` icon) + Profil (tab — renamed from "Réglages").

**Important nuances:**
- The CSS class `.bottom-nav-tab` is the flat-tab style. The central-cta does NOT use `.bottom-nav-tab` — it's a structurally different element (centered elevated pill). A new CSS class or inline style is needed for the central-cta in the mockup.
- "Réglages" → "Profil" rename happened in Phase 9 / Phase 31; the live `frontend/components/BottomNav.tsx` uses the i18n key `nav.profile` → "Profil". The §15 mockups should match.
- Historical-context blocks at ~lines 1462-1466 (rows table about "Capture / Réception reporté"), ~1876 ("Capture et Réception attendent leur refactor"), ~1954 (`# Capture & Réception : repris après le refactor parallèle`), ~1989 ("⏸ Capture & Réception — refactor en cours") describe the historical state before Phase 27. These are historical truth — DO NOT remove them. The acceptance check (`grep "Réception" returns zero matches OTHER THAN historical context blocks`) explicitly tolerates these references.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update the two LIVE BottomNav mockups in design-system.html §15</name>
  <files>docs/design-system.html</files>
  <behavior>
    - §15.A Accueil mockup BottomNav block: 4 slots in order = Accueil (active) + Recettes + central « Ajouter » CTA + Profil. NO Réception. NO "Réglages" — use "Profil".
    - §15.B Bibliothèque mockup BottomNav block: 4 slots in order = Accueil + Recettes (active) + central « Ajouter » CTA + Profil.
    - The central « Ajouter » CTA in the mockup is a visually elevated terracotta pill containing a `<i data-lucide="plus">` icon, sitting above the row baseline (translateY(-12px) via inline style or a new `.bottom-nav-cta` class). It should READ as the elevated CTA when the design-system.html page is opened in a browser.
    - The mockup matches the SOBER-10 lifted CTA delivered by Plan 36-01 (so the doc and built artifact converge).
    - Historical-context blocks elsewhere in §15 (rows table, "refactor en cours" paragraphs) are NOT modified — they describe the historical state and stay.
    - The companion "breakdown" text below each mockup (which references "Réception" in any descriptive context) is updated only if it actively describes the live composition (i.e., if it says "le BottomNav contient Accueil / Recettes / Réception / Réglages" change that to reflect the new layout; if it's historical context like "Capture et Réception attendent leur refactor" — keep).
  </behavior>
  <action>
    1. Open `docs/design-system.html`. Locate the two BottomNav mockup blocks:
       - Block 1 (Accueil, ~lines 1539-1544): inside `id="accueil"` section, in the `.phone-frame` for the §15.A mockup.
       - Block 2 (Bibliothèque, ~lines 1731-1736): inside `id="bibliotheque"` section's Patine-view mockup (`.phone-frame`).
    2. Replace each block with the new 4-slot layout. Use this template (adapt per-mockup for the active state):
       ```html
       <div class="bottom-nav">
         <div class="bottom-nav-tab active"><i data-lucide="home" style="width:20px;height:20px;"></i><span>Accueil</span></div>
         <div class="bottom-nav-tab"><i data-lucide="book-open" style="width:20px;height:20px;"></i><span>Recettes</span></div>
         <div class="bottom-nav-cta"><span class="cta-pill"><i data-lucide="plus" style="width:22px;height:22px;"></i></span><span class="cta-label">Ajouter</span></div>
         <div class="bottom-nav-tab"><i data-lucide="settings" style="width:20px;height:20px;"></i><span>Profil</span></div>
       </div>
       ```
       The Accueil mockup has `.active` on Accueil; the Bibliothèque mockup has `.active` on Recettes.
    3. Add a small CSS block for `.bottom-nav-cta` + `.cta-pill` near the existing `.bottom-nav-tab` style (~line 558). The goal: render the central elevated pill in the mockup. Suggested rules:
       ```css
       .bottom-nav-cta {
         flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 4px;
         font-size: 11px; color: var(--foreground-muted);
       }
       .bottom-nav-cta .cta-pill {
         display: inline-flex; align-items: center; justify-content: center;
         width: 56px; height: 56px; border-radius: 50%;
         background: var(--primary); color: var(--primary-foreground);
         transform: translateY(-12px);
         box-shadow: 0 4px 14px rgba(0,0,0,0.18);
       }
       .bottom-nav-cta .cta-label { transform: translateY(-8px); }
       ```
       Tune the box-shadow / colors against existing tokens in the doc. The mockup should READ as "elevated CTA above the row" — same visual shape as the live `BottomNav.tsx` after Plan 36-01.
    4. Audit the breakdown copy below each mockup. If any copy block says "le BottomNav contient Accueil / Recettes / Réception / Réglages" or similar live-description text, rewrite to "le BottomNav contient Accueil / Recettes / Ajouter (CTA central) / Profil." If the copy says "Capture et Réception attendent leur refactor" (historical), KEEP.
    5. DO NOT modify:
       - The rows table at ~line 1462-1466 (historical row about "Capture / Réception reporté").
       - Paragraphs at ~lines 1876, ~1954, ~1989 referencing "Capture & Réception — refactor en cours" (historical truth).
       - The `.bottom-nav-tab.active` CSS at lines 564-571 (existing tab style stays).
       - Anything outside §15 (`id="accueil"`, `id="bibliotheque"`, `id="recette"` sections).
  </action>
  <verify>
    <automated>grep -c "bottom-nav-cta" docs/design-system.html</automated>
  </verify>
  <done>
    - `grep -c "bottom-nav-cta" docs/design-system.html` returns at least 2 (both mockups use the new central-cta slot).
    - `grep -E 'data-lucide="inbox"' docs/design-system.html` returns ZERO matches in the LIVE mockup blocks (both `<div class="bottom-nav">…</div>` blocks at ~lines 1539 and ~1731 contain no `data-lucide="inbox"`). The grep MAY still match in historical context paragraphs — that's allowed.
    - Specifically: counted live-mockup occurrences via `awk '/<div class="bottom-nav">/,/<\/div>/' docs/design-system.html | grep -c 'data-lucide="inbox"'` should be 0.
    - "Réglages" → "Profil" in the LIVE mockup spans (the `<span>` tags inside the new mockup blocks). Other "Réglages" in the doc (e.g. headings, body copy) MAY remain unless they describe the live BottomNav literally.
    - The new mockups render visually correctly when `docs/design-system.html` is opened in a browser (browser renders the inline CSS + lucide icon stubs).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Static HTML/CSS documentation file. No code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05-01 | Information disclosure | docs/design-system.html | accept | Public-style doc; no secrets. |
| T-36-05-02 | Tampering | doc ↔ code drift | mitigate | Acceptance grep gate (`data-lucide="inbox"` returns zero in live mockups) prevents accidental partial updates. |
</threat_model>

<verification>
- `grep -c "bottom-nav-cta" docs/design-system.html` ≥ 2.
- Live mockup blocks contain no `inbox` icon refs.
- Open `docs/design-system.html` in a browser; both §15.A Accueil and §15.B Bibliothèque mockups show the elevated central CTA.
</verification>

<success_criteria>
- SOBER-16 acceptance met: `docs/design-system.html` §15 LIVE mockups show 4 slots (Accueil + Recettes + central CTA + Profil); historical-context paragraphs about Capture/Réception refactor stay; design system as documented matches design system as built post-Phase-31/Phase-36-01.
</success_criteria>

<output>
Create `.planning/phases/36-sober-kitchen-finish-polish/36-05-SUMMARY.md` with the receipt: which two mockup blocks were updated (line numbers), new CSS rules added, audit decisions for breakdown copy (kept / rewrote), historical-context blocks preserved.
</output>
