---
phase_num: 34
plan_id: "34-02"
plan_name: "LIVE-01 — /cooking-logs renders log cards + dedicated empty copy"
phase: "34-live-bug-sweep"
plan: 2
type: execute
wave: 2
depends_on: ["34-01"]
status: pending
requirement_ids: [LIVE-01]
requirements: [LIVE-01]
files_modified:
  - frontend/app/cooking-logs/page.tsx
  - frontend/lib/i18n/fr.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Navigating to /cooking-logs as the seeded Luca renders 3 cooking-log cards (loved Ragu / liked Poulet citron / disliked Burger) grouped by date with Fraunces-italic section headers."
    - "The empty-state copy on /cooking-logs references cooking logs, NOT 'Aucune recette pour le moment'."
    - "Two new i18n keys exist: `cooking_logs.empty_heading` and `cooking_logs.empty_body`."
    - "The EmptyState component on /cooking-logs reads `cooking_logs.empty_heading` / `cooking_logs.empty_body`, not `recipes.empty_heading` / `recipes.empty_body`."
  artifacts:
    - path: "frontend/app/cooking-logs/page.tsx"
      provides: "Fixed cooking-log history view with correct fetch wiring and cooking-log-specific empty copy"
    - path: "frontend/lib/i18n/fr.json"
      provides: "New `cooking_logs.empty_heading` + `cooking_logs.empty_body` keys"
      contains: "cooking_logs"
  key_links:
    - from: "frontend/app/cooking-logs/page.tsx"
      to: "fetchCookingLogs(14) from @/lib/cooking"
      via: "useEffect Promise.all"
      pattern: "fetchCookingLogs"
    - from: "frontend/app/cooking-logs/page.tsx"
      to: "frontend/lib/i18n/fr.json#cooking_logs"
      via: "useTranslations('cooking_logs')"
      pattern: "cooking_logs"
---

<objective>
LIVE-01 — /cooking-logs renders the cooking-log cards returned by `GET /api/cooking-logs` and uses cooking-log-specific empty copy.

Purpose: B-01 punch-list bug — the seeded household has 3 cooking logs but the page renders the empty state. The existing page-level header comment acknowledges the empty-copy semantic mismatch as deferred tech debt ("Phase 20 i18n sweep" — never landed). This plan closes both: fixes the (likely) state-slot mis-wire AND lands the dedicated cooking-log empty copy.

Output: A working `/cooking-logs` page against the seeded stack + two new i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-live-bug-sweep/34-CONTEXT.md
@.planning/phases/34-live-bug-sweep/34-01-SUMMARY.md
@CLAUDE.md
@frontend/CLAUDE.md

@frontend/app/cooking-logs/page.tsx
@frontend/lib/i18n/fr.json
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Already in the codebase. -->

From frontend/lib/cooking.ts (existing):
- `fetchCookingLogs(days: number): Promise<CookingLogResponse[]>` — bare-array shape (no envelope) per D-17-02
- `CookingLogResponse` shape includes `id`, `recipe_id`, `rating ("loved"|"liked"|"disliked")`, `cooked_at`, `notes`, `photo_paths`
- `getCookingLogSignedPhotoUrl(logId, path): Promise<string>` — used by `CookingLogHistoryRow`

From frontend/components/EmptyState.tsx (existing):
- `<EmptyState icon={IconComponent} heading={string} body={string} />`

From frontend/lib/api.ts (existing):
- `api<T>(path: string): Promise<T>` — HttpOnly cookie auth via same-origin Next.js rewrite (invariant 8)
- The page currently calls `api<Array<{ id: string; title: string }>>("/api/recipes?limit=500")` — this stays.

From the existing page header comment (line 12-13):
- `GET /cooking-logs?days=N` is the backend route (Phase 17, 17-01). The wire shape is a bare `CookingLogResponse[]`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diagnose the empty-state regression — read the page + cooking.ts + verify against seed</name>
  <files>frontend/app/cooking-logs/page.tsx (read only), frontend/lib/cooking.ts (read only)</files>
  <behavior>
    Test 1 — `fetchCookingLogs(14)` against a freshly seeded backend returns an array of length 3 (one per seeded cooking log). The body shape includes `id`, `recipe_id`, `rating`, `cooked_at`.
    Test 2 — The bug is one of: (a) state-slot mis-wire (page assigns the array to the wrong setter), (b) fetch route mismatch (page calls a non-existent endpoint), (c) silent catch swallowing a non-empty array as `[]`, or (d) `CookingLogCardData` type drift causing the map to drop rows. Exactly ONE of these matches the live behavior.
  </behavior>
  <action>
    1. Open `frontend/app/cooking-logs/page.tsx` and re-read carefully. Note line 84: `fetchCookingLogs(14)` — appears correct. Note lines 92-97: the map from `rawLogs` → enriched `CookingLogCardData`. Note line 98-101: silent `catch` setting logs to `[]`.

    2. Open `frontend/lib/cooking.ts` and confirm `fetchCookingLogs(days: number)` exists, the path it hits (`/api/cooking-logs?days={days}`), and the response parsing. Note any wrapping (`api<...>` vs `fetch` vs an envelope unwrap).

    3. Start the local stack:
       - `cd backend && uv run seed` (idempotent)
       - `cd backend && uv run uvicorn app.main:app --reload --port 8000`
       - `cd frontend && npm run dev`
    4. With the seed Luca cookie set (`document.cookie = "aldente_auth=test-token-luca; path=/; SameSite=Lax"`), navigate to `/cooking-logs`.
    5. In DevTools, run:
       `await fetch("/api/cooking-logs?days=14", { credentials: "include" }).then(r => r.json())`
       Confirm the response is an array of length 3.
    6. Add a `console.log("[34-02] rawLogs", rawLogs, "enriched", enriched)` line temporarily inside the `useEffect`'s `try` block (between line 89 and 97). Reload `/cooking-logs`. Read the console:
       - If `rawLogs.length === 0` but the fetch above returned 3 → the fetch path inside `fetchCookingLogs` is wrong (route mismatch) OR `cooking.ts` strips/swallows the response.
       - If `rawLogs.length === 3` but `enriched.length === 0` → the map drops rows (likely `titleById` lookup is failing because `/api/recipes?limit=500` returns drafts that don't include the seeded ones; or the recipe IDs don't match).
       - If `enriched.length === 3` but state still shows EmptyState → the `setLogs(enriched)` call is broken or the render branch logic is wrong.
       - If the `try` block never logs and `catch` fires → an exception is being swallowed. Inspect the exception by replacing the silent catch in line 98 with `catch (err) { console.error("[34-02] cooking-logs fetch error", err); ... }`.

    7. Record the root cause in a comment block at the top of the modified `page.tsx` (will be cleaned up in Task 2). Format: `// LIVE-01 root cause: <one-line description>`.

    8. Revert the temporary `console.log` / `console.error` additions in `page.tsx`. Keep only the root-cause comment for Task 2 to delete after the fix lands.
  </action>
  <verify>
    <automated>cd frontend && grep -n "LIVE-01 root cause" app/cooking-logs/page.tsx</automated>
    <human-check>Browser fetch `await fetch("/api/cooking-logs?days=14")` returns an array of length 3 against the seed. The root-cause comment in `page.tsx` matches the live console behavior.</human-check>
  </verify>
  <done>Root cause is documented as a one-line comment in `page.tsx`. The branch (a/b/c/d) is identified and verified by inspection of both the network response and the React state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the wiring + land cooking-log empty-copy i18n keys</name>
  <files>frontend/app/cooking-logs/page.tsx, frontend/lib/i18n/fr.json</files>
  <behavior>
    Test 1 — Navigate to `/cooking-logs` while authenticated as seeded Luca → 3 cooking-log cards render, grouped by date with Fraunces-italic section headers (existing visual chrome).
    Test 2 — Clear the database (or use an empty-logs user) → EmptyState renders with `cooking_logs.empty_heading` heading text ("Aucun repas cuisiné cette semaine" or equivalent French) and `cooking_logs.empty_body` body text — NOT "Aucune recette pour le moment".
    Test 3 — `grep -n "cooking_logs" frontend/lib/i18n/fr.json` returns at least two lines: `empty_heading` and `empty_body`.
    Test 4 — `grep -n "tRecipes" frontend/app/cooking-logs/page.tsx` returns zero lines (the recipes namespace is no longer borrowed for empty copy — replace with `tEmpty` or similar bound to `cooking_logs`).
  </behavior>
  <action>
    1. **Land i18n keys first** (no code dependency yet, but lets typecheck succeed for Task 2.2).
       Open `frontend/lib/i18n/fr.json`. Find a logical place for a new top-level `"cooking_logs"` namespace (alongside the existing namespaces — there's a `"cooking_log"` singular namespace used by `rating` per line 160 in CookingLogHistoryRow; add the plural `"cooking_logs"` as a sibling). Insert:
       ```
       "cooking_logs": {
         "empty_heading": "Aucun repas cuisiné cette semaine",
         "empty_body": "Quand tu marqueras une recette comme cuisinée, elle apparaîtra ici."
       },
       ```
       Two-line copy ground rules (per CLAUDE.md invariant 6 — French-only via next-intl day one): natural French, no anglicisms, mirror the tone of the existing `recipes.empty_heading` ("Aucune recette pour le moment") and `recipes.empty_body` ("Ajoute ta première recette pour commencer."). The copy above is the executor's call — if you have a better French-native phrasing, use it. The keys are LOCKED.

    2. **Fix the wiring** based on Task 1's root cause:
       - If root cause was (a) state-slot mis-wire — fix the `setLogs(enriched)` target / assignment.
       - If (b) route mismatch — change `fetchCookingLogs` argument or the underlying path in `frontend/lib/cooking.ts` to hit the correct route. (Confirm with backend `app/routers/cooking_logs.py` for the canonical path — likely `/cooking-logs?days={days}` proxied via the Next.js rewrite to `/api/cooking-logs?days=...`. The frontend `api()` wrapper prepends `/api`.)
       - If (c) silent-catch swallow — the catch is shipping `setLogs([])` on a real exception, masking the bug. Tighten: log the exception in `process.env.NODE_ENV !== "production"` mode, but still fall through to empty state in prod. (Don't toast — the file comment explicitly mandates no toast.)
       - If (d) type drift — fix the map / lookup to handle the actual response shape.

       Whatever the root cause, after the fix the render must produce 3 cards against the seed.

    3. **Swap the EmptyState i18n keys**:
       - Line 22-23: `const tRecipes = useTranslations("recipes");` → REMOVE this binding (it is no longer used after the swap below).
       - Add: `const tEmpty = useTranslations("cooking_logs");` near where `tRecipes` lived.
       - Lines 126-128: `heading={tRecipes("empty_heading")}` → `heading={tEmpty("empty_heading")}`; `body={tRecipes("empty_body")}` → `body={tEmpty("empty_body")}`.
       - Remove the file-header comment block (lines 16-19 — the "Empty-state copy: …" tech-debt paragraph) since the debt is now paid. Leave the rest of the comment block (Phase 17 wiring intent) intact.
       - Remove the `// LIVE-01 root cause:` diagnostic comment from Task 1 once the fix lands.

    4. Run `cd frontend && npm run lint` and `cd frontend && npx tsc --noEmit`. Both must pass cleanly.

    5. Walk the local stack: with `uv run seed` having run, navigate to `/cooking-logs` and confirm 3 cards render. Then bust the seed (option A — manually drop the 3 cooking_logs rows from local Supabase Studio; option B — use an empty seed by running `uv run seed --variant empty` if such a variant exists, otherwise skip this and accept that the empty-state path was verified by the keys being present + the `logs.length === 0` branch being trivially reachable in code). Capture both states in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && grep -c "cooking_logs" lib/i18n/fr.json | grep -qE "^[2-9]|^[1-9][0-9]" && cd frontend && npm run lint 2>&1 | tail -3 && cd frontend && npx tsc --noEmit 2>&1 | tail -3</automated>
    <human-check>Against seeded backend, `/cooking-logs` renders 3 cards grouped by date with Fraunces-italic section headers. Manually emptying the cooking_logs table → empty state shows the new French copy, not "Aucune recette pour le moment".</human-check>
  </verify>
  <done>Three seed cooking logs render on /cooking-logs. New `cooking_logs` namespace exists in fr.json with `empty_heading` + `empty_body`. EmptyState reads the new keys. `tRecipes` binding removed. Lint + tsc clean.</done>
</task>

</tasks>

<verification>
- `grep -n "cooking_logs" frontend/lib/i18n/fr.json` finds at least the empty_heading + empty_body keys.
- `grep -n "tRecipes" frontend/app/cooking-logs/page.tsx` returns zero lines.
- `/cooking-logs` against the seeded backend renders 3 cards.
- `/cooking-logs` against an empty-logs household renders the new French copy.
</verification>

<success_criteria>
- LIVE-01 acceptance: navigate to `/cooking-logs` while authenticated as Luca against the seed → 3 cards render grouped by date with Fraunces-italic section headers per v0.2 Phase 8.
- Empty state copy says cooking-logs-specific French, not the borrowed recipes namespace.
- The page header comment block no longer claims "Phase 20 i18n sweep" as pending — that debt is paid.
</success_criteria>

<out_of_scope>
- Adding `EN` translations — the project is French-only day one (invariant 6).
- Refactoring `CookingLogHistoryRow` into a shared component with `CookingLogCard` (Phase 21 polish per existing comment).
- Pagination beyond `days=14` — the existing 14-day window is the spec.
- Cooking-log photo upload Playwright coverage — gh#28 v0.8.
- Detail-page (`/cooking-logs/[id]`) — already shipped Phase 17, no regression to fix here.
</out_of_scope>

<rollback>
- `git revert <commit>` cleanly reverts both `page.tsx` and `fr.json` changes.
- No DB migration, no backend touch.
</rollback>

<output>
Create `.planning/phases/34-live-bug-sweep/34-02-SUMMARY.md`:
- Record the Task 1 root cause finding verbatim (which of a/b/c/d).
- Record the new French copy for `cooking_logs.empty_heading` / `cooking_logs.empty_body`.
- Embed before/after screenshots if practical.

Commit:
`gsd-sdk query commit "fix(34-02): /cooking-logs renders log cards + dedicated empty copy" --files frontend/app/cooking-logs/page.tsx frontend/lib/i18n/fr.json .planning/phases/34-live-bug-sweep/34-02-SUMMARY.md`
</output>
