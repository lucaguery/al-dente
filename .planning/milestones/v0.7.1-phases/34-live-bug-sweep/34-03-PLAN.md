---
phase_num: 34
plan_id: "34-03"
plan_name: "LIVE-03 — Settings renders both household members"
phase: "34-live-bug-sweep"
plan: 3
type: execute
wave: 2
depends_on: ["34-01"]
status: pending
requirement_ids: [LIVE-03]
requirements: [LIVE-03]
files_modified:
  - frontend/app/settings/page.tsx
  - frontend/lib/i18n/fr.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Visiting /settings as the seeded Luca shows two member blocks — `Toi · Luca` (existing) and `Partenaire · {partner-name}` (new)."
    - "The partner block carries the same visual structure as the Toi block: member dot color + name."
    - "The partner name is read from `useSession().session.members[]`, filtered to exclude `session.me.id`."
    - "`grep -rn 'household.members\\|session.members' frontend/app/settings/` returns at least one matching fetch path."
  artifacts:
    - path: "frontend/app/settings/page.tsx"
      provides: "Settings page now renders partner member block under the Toi block"
    - path: "frontend/lib/i18n/fr.json"
      provides: "New `settings.partner_label` i18n key for the 'Partenaire' label"
      contains: "partner_label"
  key_links:
    - from: "frontend/app/settings/page.tsx"
      to: "useSession().session.members"
      via: "filter members where id !== session.me.id"
      pattern: "session\\.members"
---

<objective>
LIVE-03 — Settings "Foyer" section renders both Luca and partner blocks.

Purpose: B-07 punch-list bug — Settings reads `session.me` but not `session.members`, so the partner never appears even though the data exists in context (the Partner correctly renders on Accueil voting cards via the same `useSession`). The fix is minimal: filter `session.members` to exclude `me.id`, render a sibling Card with the same chrome.

Output: A Settings page that renders one Card per household member.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-live-bug-sweep/34-CONTEXT.md
@.planning/phases/34-live-bug-sweep/34-01-SUMMARY.md
@CLAUDE.md
@frontend/CLAUDE.md

@frontend/app/settings/page.tsx
@frontend/components/SessionProvider.tsx
@frontend/lib/i18n/fr.json
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Already in the codebase. -->

From frontend/components/SessionProvider.tsx (existing):
```typescript
export type SessionMember = {
  id: string;
  name: string;
  color_hex: string;
};
export type SessionData = {
  household_id: string;
  household_name: string;
  invite_code: string;
  me: SessionMember;
  members: ReadonlyArray<SessionMember>;  // <-- ALL members including `me`
};
export function useSession(): { status, session, refresh };
```

From frontend/app/settings/page.tsx (existing, line 56):
- `const { status, session, refresh } = useSession();`
- The existing Toi block (lines 283-343) uses `<Card>` + `<MemberDot colorHex={...} />` + name span. The partner block mirrors this WITHOUT the rename affordance (only `me` can rename `me`).

From frontend/components/MemberDot.tsx:
- `<MemberDot colorHex={string} />` — the existing dot component.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add i18n key for the "Partenaire" label</name>
  <files>frontend/lib/i18n/fr.json</files>
  <behavior>
    Test 1 — `frontend/lib/i18n/fr.json` contains a `settings.partner_label` key with value `"Partenaire"`.
    Test 2 — The existing `settings.member_label` ("Toi" — used by the existing Toi block) is unchanged.
  </behavior>
  <action>
    Open `frontend/lib/i18n/fr.json`. Locate the `"settings"` namespace (grep for `"settings":` — should exist; the file already uses `t("member_label")` from this namespace at line 285 of `settings/page.tsx`).

    Add a sibling key: `"partner_label": "Partenaire"`.

    Run `cd frontend && npx tsc --noEmit` to confirm the JSON parses (next-intl types the keys).
  </action>
  <verify>
    <automated>cd frontend && grep -c "partner_label" lib/i18n/fr.json | grep -q "^1$"</automated>
  </verify>
  <done>The `settings.partner_label` key exists with value "Partenaire". Lint + tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the partner Card block on /settings</name>
  <files>frontend/app/settings/page.tsx</files>
  <behavior>
    Test 1 — `/settings` while authenticated as the seeded Luca renders TWO member blocks in the Foyer section: one `Toi · Luca` (existing, with rename affordance) and one `Partenaire · {partner-name}` (new, NO rename affordance — only `me` renames `me`).
    Test 2 — The partner block uses the same `<Card className="paper-grain shadow-card p-6 flex flex-col gap-2">` chrome as the Toi block (lines 283-343), with `<MemberDot colorHex={partner.color_hex} />` and the partner's `name`.
    Test 3 — If `session.members.length === 1` (solo household — productize edge case), the partner block does NOT render. The Toi block stays.
    Test 4 — If `session.members.length > 2` (future-proofing — not in scope for v0.7.1 but the code should not crash), all non-`me` members render in a stable order (sorted by `id` for determinism).
  </behavior>
  <action>
    1. Open `frontend/app/settings/page.tsx`. Locate the existing Toi Card block (lines 283-343 — the `<Card className="paper-grain shadow-card p-6 flex flex-col gap-2">` that contains the rename affordance).

    2. Compute the partner list once near the top of the component (after `const { status, session, refresh } = useSession();` at line 56):
       ```typescript
       const partners = useMemo(() => {
         if (!session) return [];
         return session.members
           .filter((m) => m.id !== session.me.id)
           .slice()
           .sort((a, b) => a.id.localeCompare(b.id));
       }, [session]);
       ```
       Import `useMemo` from `react` if not already imported. The sort by `id` is determinism, not UX — v0.7.1 has exactly 2 members and the order is irrelevant for couple-scale, but it guards against a future 3-member family without re-planning.

    3. Render the partner block(s) IMMEDIATELY AFTER the closing `</Card>` of the Toi block (around line 343). The structure mirrors the Toi block minus the rename affordance:
       ```tsx
       {partners.map((partner) => (
         <Card
           key={partner.id}
           className="paper-grain shadow-card p-6 flex flex-col gap-2"
         >
           <span className="text-sm text-foreground-muted">
             {t("partner_label")}
           </span>
           <div className="flex items-center gap-3">
             <MemberDot colorHex={partner.color_hex} />
             <span className="text-base font-medium flex-1">
               {partner.name}
             </span>
           </div>
         </Card>
       ))}
       ```
       Do NOT add a Pencil affordance — only `me` renames `me`. Do NOT add a "last-active hint" — CONTEXT.md says "matches the Toi block's visual structure for symmetry" and the Toi block has no last-active. The CONTEXT references "Phase 9 onboarding-identity spec" for visual structure; the spec defines the member dot + name pair, which is what we ship. Last-active is a productize-later concern (no field on `SessionMember` today).

    4. Confirm `useTranslations("settings")` is already bound at the top of the file (the existing `t("member_label")` proves it is — same binding works for `t("partner_label")`).

    5. Confirm `MemberDot` is already imported (`grep -n "MemberDot" frontend/app/settings/page.tsx`). If not, add `import { MemberDot } from "@/components/MemberDot";` near the other imports.

    6. Run `cd frontend && npm run lint && npx tsc --noEmit`. Both clean.

    7. Walk locally: `cd backend && uv run seed && uv run uvicorn app.main:app --reload --port 8000` in one shell, `cd frontend && npm run dev` in another. Set the seed Luca cookie, navigate to `/settings`. Confirm two blocks render — Toi + Luca, then Partenaire + the seeded partner name (likely "Partenaire" or "Léa" depending on what `uv run seed` writes; just confirm it matches what `await fetch("/api/households/me").then(r => r.json())` returns for the second member).

    8. Optional — exercise the `partners.length === 0` branch by manually editing `session.members` in DevTools (`React DevTools → SessionProvider → state → snapshot.session.members`). Confirm the partner block disappears and the Toi block stays. Not load-bearing for v0.7.1 — skip if it costs more than 30 seconds.
  </action>
  <verify>
    <automated>cd frontend && grep -n "partner_label\|partners.map" app/settings/page.tsx | wc -l | grep -qE "^[2-9]|^[1-9][0-9]" && cd frontend && npm run lint 2>&1 | tail -3 && cd frontend && npx tsc --noEmit 2>&1 | tail -3</automated>
    <human-check>/settings against the seed shows two member blocks: Toi/Luca and Partenaire/{partner-name}. Member dots have distinct colors. Only the Toi block has a Pencil rename button.</human-check>
  </verify>
  <done>Two member blocks render on /settings. Partner block has matching chrome minus the rename affordance. Lint + tsc clean. Acceptance walk evidence in SUMMARY.</done>
</task>

</tasks>

<verification>
- `grep -rn "session.members" frontend/app/settings/` returns at least one line (the new `partners` memo).
- `grep -n "partner_label" frontend/lib/i18n/fr.json` returns 1 line.
- `/settings` against seed shows 2 member blocks; partner block has no rename Pencil.
</verification>

<success_criteria>
- LIVE-03 acceptance: `/settings` while authenticated as Luca shows two member blocks (Toi · Luca + Partenaire · {name}).
- The fetch path is observably `session.members` (not a new `useHousehold()` hook) — CONTEXT.md prefers extending the existing path.
</success_criteria>

<out_of_scope>
- Settings page redesign / member management UX (CONTEXT.md "Deferred Ideas").
- Last-active hint — no field on SessionMember; productize-later.
- Member kick / leave-household affordances — out of v0.7.1 scope.
- WebSocket-driven Settings auto-refresh when a member renames remotely — CONTEXT.md deferred.
- Rendering the household name / invite code (already rendered elsewhere on the Settings page; not part of this plan).
</out_of_scope>

<rollback>
- `git revert <commit>` reverts both files cleanly.
- No DB migration, no backend touch.
</rollback>

<output>
Create `.planning/phases/34-live-bug-sweep/34-03-SUMMARY.md`:
- Record the partner name as it appears in the seed (e.g. "Léa") to confirm the wiring is real, not stubbed.
- Optionally embed a screenshot of `/settings` showing both blocks.

Commit:
`gsd-sdk query commit "fix(34-03): Settings renders both household members" --files frontend/app/settings/page.tsx frontend/lib/i18n/fr.json .planning/phases/34-live-bug-sweep/34-03-SUMMARY.md`
</output>
